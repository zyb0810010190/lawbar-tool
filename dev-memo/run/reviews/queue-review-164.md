# Queue review — WI-RELEASE-G19-SUPPLY-CHAIN-POSTURE-00 (GOVERNANCE lane)

Lane: M0 gate-19 supply-chain posture **governance/authoring** (Type: EVIDENCE, release-governance MEDIUM risk). Governs — does NOT execute — a FUTURE lane that RUNS a READ-ONLY supply-chain inventory / `npm audit` / provenance sweep and produces `docs/release/gate19-supply-chain-posture-00.md`. Changes NO dependency/lockfile; changes NO source/test/config; runs NO sweep in this lane; clears NO gate; decides NO user go-live / license-legal hard-stop.
Date: 2026-07-08. Branch: `release-g19-supply-chain-posture-governance` (from synced `main` @ `451ae58`; created BEFORE any edit per the pre-flight guardrail — verified off-main; NO commit on local main). Batch: window 1/3 since marker `0586be7` (`451ae58` batch-249 closeout) — no batch closeout this lane.

## What this is
The authoring/governance lane of WI-RELEASE-G19-SUPPLY-CHAIN-POSTURE-00. It writes the governed queue WI ONLY — it changes NO dependency/lockfile, runs NO sweep, changes NO source/test/config, clears NO gate. Gate 19 ("Supply-chain posture") is **OPEN [Δ]** (prior evidence: `better-sqlite3` native, `docx@9.7.1` MIT/pure-JS with an ACCEPTABLE dependency-risk-review `audit-mr63d6y7`, + two internal committed tarballs; blocker: dependency inventory + license review + `npm audit` + native-binary provenance for `better-sqlite3` + OCR engine, SBOM optional).

The governed WI defines the ten sweep requirements the FUTURE exec lane must satisfy (manifests + lockfiles to inspect; runtime-dependency inventory; native-binary/tarball/OCR-engine provenance; scoped **read-only** `npm audit`; license **inventory** preserving the gate-17 boundary; exclusions/post-v1; evidence artifact; deterministic pass/fail; evidence-advance-without-clearing-gate-17/4/6; residual risks), keeps gates 4/6/12/13/17/20 uncleared, keeps gate 19 `OPEN`, forbids ALL dependency/lockfile mutation (no `npm install`/`audit fix`/`update`) and any license/legal/business **decision**, requires any Critical/High advisory in a v1-production dep to be SURFACED (never silently passed), and decides no STOP-AND-ASK hard-stop (4/11/17/21) and no go-live.

**Grounding (verified 2026-07-08; exec lane re-verifies):** 10 `package.json` + 9 `package-lock.json` (`native/evidence-core` has none); v1 Mac-client runtime deps `better-sqlite3 ^12.9.0` (native, in 3 manifests, `ocr-persistence` abi-smoke guard), `docx 9.7.1` (exact, MIT/pure-JS, prior ACCEPTABLE `audit-mr63d6y7`), two committed tarballs `apps/lawbar-desktop/dist-tarballs/case-box-{contract,persistence}-0.1.0.tgz`; devDeps `electron ^34` / `electron-builder ^25` / `playwright` / `typescript`; OCR engine `@gutenye/ocr-node ^1.4.8` (`services/ocr-worker`); `services/ocr-worker-bakeoff` (tesseract/paddleocr-onnx) OUT of the production graph (ADR-11A.1); OCR not wired into the v1 Mac client; real-PaddleOCR model-gated/post-v1.

## cc-suite recording (per .claude/rules/cc-suite.md §"Required recording")
Path 1 runner foreground, resolved runner path `/Users/zhongyibao/.claude/plugins/cache/xiaolai/cc-suite/0.2.18/scripts/codex-runner.mjs`, retrievable YES, no failure class, no fallback. review-plan on the governed queue WI (inlined; codex instructed not to read the filesystem). Completed first attempt, no timeout.

### /cc-suite:review-plan (gpt-5.5/medium/read-only; on the governed queue WI)
- `review-plan-mrbl1lgh-qu0e89` · **READY (Low-risk clarifications)** (no Critical/High/Medium). All 5 dimensions PASS: (1) internal consistency — governance-only; the future lane is a read-only inventory/audit/provenance sweep producing docs, not dependency changes; (2) completeness — covers 10 manifests / 9 lockfiles / no-lockfile note / v1 runtime deps / `better-sqlite3` / `docx@9.7.1` / tarballs / Electron+build devDeps / OCR engine / excluded bakeoff / post-v1 OCR posture; (3) feasibility — explicitly read-only (`npm audit`/`npm ls`/metadata reads allowed; `npm install`/`audit fix`/`update`/dep-edit/lockfile-edit/tarball-rebuild forbidden); (4) ambiguity — license kept to descriptive inventory, gate 17 preserved; (5) risk & sequencing — gate 19 stays `OPEN` with a `[Δ]` marker, movement deferred, gates 4/6/12/13/17/20 uncleared, STOP-AND-ASK 4/11/17/21 preserved, no gate-6 audit, schema 12. **Two Low clarifications, both applied:** (a) the future lane's `/cc-suite:review-plan` "on the produced artifacts" reworded to a **POST-ARTIFACT review of the produced evidence** (posture doc + gate-19 row diff, inlined — the docs-evidence convention), not a fresh plan review; (b) "accepted-with-reason" in the disposition criteria clarified — it means RECORDING a *prior explicit* acceptance (e.g. the `docx@9.7.1` ACCEPTABLE review) or a *proposed/temporary* posture for the user to ratify, NOT a NEW agent-made go-live/security risk-acceptance; any *blocking* production Critical/High stays surfaced/escalated and does not clear gate 19. · rawOutput sha256 `9470c12c2f5509aec3b685bac94b8db1444b41c58ab76f0b91f5a09692760903`.

## Verdict: READY (governance authored, review-plan READY; governance-only; the future exec lane runs a read-only supply-chain inventory/audit/provenance sweep, keeps a license inventory not a decision, surfaces any Critical/High advisory, and produces the posture doc; gate 19 stays OPEN; dependent gates 4/6/12/13/17/20 uncleared; go-live-independent; no dependency/lockfile change; no STOP-AND-ASK decided)

QUEUE_REVIEW_VERDICT=PASS

## Gates (this governance lane)
- `scripts/workflow/check-queue.sh` → QUEUE LINT PASSED (re-run after the two Low clarifications; queue.linted regenerated).
- `scripts/workflow/check-contract-integrity.sh` → PASS (to run pre-commit).
- Governed queue.md sha256 `2744e686…` (content-bound by `govern-queue.sh` into `queue.governed`).
- `CURRENT_SCHEMA_VERSION` unchanged (12); no dependency change, no package.json/package-lock.json mutation, no product source/test/schema/contract change; no posture-doc creation, no ADR/brief edit, no license decision, no gate-6 run, no readiness refresh, no clearing of gates 4/6/12/13/17/20, no go-live decision — only the queue governance (`queue.{md,linted,reviewed,governed}`) + this review artifact.

## Deferred findings
None deferred as open — the two precision Lows were applied to the governed WI (post-artifact review wording; "accepted-with-reason" disposition clarification). Gate 19's move to PARTIAL/CLEARED is not this lane's to grant (stays OPEN) and does not imply go-live; gates 4/6/12/13/17/20 stay uncleared; the license *decision* (gate 17), signing/distribution (gate 4), full-project audit (gate 6), and the final GO/NO-GO + the STOP-AND-ASK hard-stops (4/11/17/21) remain the user's.
