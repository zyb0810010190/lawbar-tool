# Queue review — WI-RELEASE-G19-SUPPLY-CHAIN-POSTURE-00 (EXECUTION lane)

Lane: M0 gate-19 supply-chain posture **execution** (Type: EVIDENCE, release-governance MEDIUM risk). RAN the READ-ONLY supply-chain inventory / `npm audit` / provenance sweep, produced `docs/release/gate19-supply-chain-posture-00.md`, and updated the gate-19 evidence row. Changed NO dependency/lockfile; changed NO source/test/config; ran NO `npm install`/`audit fix`/`update`; edited NO ADR/brief; made NO license/legal decision; cleared NO gate; decided NO user go-live hard-stop.
Date: 2026-07-08. Branch: `release-g19-supply-chain-posture-exec` (from synced `main` @ `7e30063`; created BEFORE any edit per the pre-flight guardrail — verified off-main; NO commit on local main). Batch: window 1/3 since marker `50377fd` (`7e30063` batch-250 closeout) — no batch closeout this lane.

## What this is
The execution lane of governed WI-RELEASE-G19-SUPPLY-CHAIN-POSTURE-00 (governed commit `e6ec53d`, queue.governed sha256 `2744e686…`, PR #218 merge `50377fd`). It ran the read-only sweep and authored `docs/release/gate19-supply-chain-posture-00.md`. NO dependency/lockfile mutation, NO product source/test/config change.

**Sweep results (verified against the real tree this lane; Node `v24.14.0` / npm `11.9.0`, 2026-07-08):**
- **`npm audit --omit=dev` = 0 vulnerabilities** across all 6 packages (`apps/lawbar-desktop` + `services/ocr-{worker,persistence,ingestion,review}` + `case-box-persistence`) — the v1 production dependency surface is clean.
- **Runtime deps:** `better-sqlite3` 12.10.0 (MIT, native; `prebuild-install || node-gyp rebuild --release`, compiled `.node` present, `abi-smoke` guard), `docx` 9.7.1 (MIT/pure-JS; prior ACCEPTABLE review `audit-mr63d6y7`), two first-party committed tarballs (`case-box-{contract,persistence}` 0.1.0, traced to in-repo source), OCR engine `@gutenye/ocr-node` 1.4.8 + `ocr-common` 1.4.8 + `ocr-models` 1.4.2 (all MIT; not wired into the v1 client, model-gated/post-v1). Build/runtime devDeps `electron` 34.5.8, `electron-builder` 25.1.8.
- **License inventory:** all MIT or first-party — descriptive only, NOT a gate-17 decision.
- **SURFACED (R-G19-1):** the dev-inclusive audit shows 13 advisories (1 moderate, 12 high), almost all in the build toolchain (electron-builder chain: tar/node-gyp/dmg-builder/cacache/tmp/form-data/js-yaml) — advisory-only, not shipped. Notably `electron` 34.5.8 itself carries **2 HIGH advisories** (ASAR integrity bypass; macOS AppleScript injection); `electron` is a declared devDependency but IS the shipped runtime, so it is surfaced as R-G19-1 (recommend a bump before gate-4 distribution) — **recorded, not fixed here, not clearing gate 19, not a new agent risk-acceptance** (the accept/bump decision is the user's at gate 4).

**Deliverables (two tracked files + this review artifact):**
1. NEW `docs/release/gate19-supply-chain-posture-00.md` — §1 manifests+lockfiles; §2 runtime-dependency inventory (declared→resolved); §3 native-binary/tarball/OCR provenance; §4 scoped read-only `npm audit` results (production 0; dev/build 13 advisory; the electron-runtime HIGH surfaced); §5 license inventory (gate-17 boundary preserved); §6 exclusions/post-v1; §7 posture sweep result; §8 feeds-gate-19-without-clearing-17/4/6; §9 residuals/follow-up WIs.
2. `docs/release/go-live-readiness-report.md` — the gate-19 ROW ONLY: `OPEN [Δ]` → `OPEN — supply-chain inventory + audit + provenance recorded [Δ]` (still NOT cleared; roll-up bucket unchanged). No other row edited.

## cc-suite recording (per .claude/rules/cc-suite.md §"Required recording")
Path 1 runner foreground, resolved runner path `/Users/zhongyibao/.claude/plugins/cache/xiaolai/cc-suite/0.2.18/scripts/codex-runner.mjs`, retrievable YES, no failure class, no fallback. review-plan on the produced posture doc + the gate-19 row diff (inlined; codex given the actual sweep results to check for honest reporting). Completed first attempt, no timeout.

### /cc-suite:review-plan (gpt-5.5/medium/read-only; on the produced posture doc + gate-19 row diff)
- `review-plan-mrbleqv6-mqmxu4` · **READY (Low-risk clarifications)** (no Critical/High/Medium). All 5 dimensions PASS: consistency (inventory + audit results match the actual sweep; no hidden advisory); completeness (10 manifests + 9 lockfiles; runtime deps + OCR engine + build devDeps + native provenance + tarballs + scoped audits + license inventory + the dev-inclusive 13-advisory posture incl. the electron-runtime issue); feasibility (docs-only read-only evidence — no dependency bump/lockfile mutation/install/audit-fix/source/test/config/schema/contract/ADR/brief change); ambiguity + risk (the electron-runtime HIGH advisories surfaced not accepted/fixed, routed to a later dependency WI / gate 4; gate 19 stays OPEN with `[Δ]`, no roll-up move; gates 4/6/12/13/17/20 not cleared; STOP-AND-ASK + GO/NO-GO user-owned; license section descriptive inventory only). **Three Low wording clarifications, all applied:** (a) the status line "One High advisory …" → "one surfaced Electron-runtime risk item (covering two HIGH advisories)"; (b) "the v1 production dependency surface is clean: 0 advisories" → scoped to `--omit=dev` with the shipped-Electron-runtime advisory surfaced separately; (c) §7 "PASS" reworded to "Posture sweep result: PASS (evidence recorded) — Gate 19 remains OPEN" to prevent a gate-clearance misread. · rawOutput sha256 `12416da45c88caf3712daa2b7cfee0872b466679539ad5f5407b44c1d82513b4`.

## Verdict: READY (sweep executed, posture recorded; read-only; production audit 0; provenance traced; license inventory not a decision; electron-runtime HIGH surfaced + routed to gate 4; gate 19 stays OPEN; dependent gates 4/6/12/13/17/20 uncleared; go-live-independent; no dependency/lockfile change; no STOP-AND-ASK decided)

QUEUE_REVIEW_VERDICT=PASS

## Gates (this execution lane)
- Read-only sweep executed (manifest/lockfile reads + `npm audit --omit=dev` × 6 + dev-inclusive audit + provenance) → posture recorded. No dependency/lockfile mutation.
- `scripts/workflow/check-queue.sh` → QUEUE LINT PASSED (queue.linted timestamp side-effect restored — this exec lane does NOT re-stage queue governance).
- `scripts/workflow/check-contract-integrity.sh` → PASS (14 contract docs clean).
- `CURRENT_SCHEMA_VERSION` unchanged (12); no dependency change, no `package.json`/`package-lock.json` mutation, no product source/test/schema/contract change — only the NEW posture doc + the gate-19 evidence row + this review artifact. No ADR/brief edit, no license decision, no gate-6 run, no readiness refresh, no clearing of gates 4/6/12/13/17/20, no go-live decision.

## Deferred findings
None deferred as open — the three precision Lows were applied (status-line advisory-count wording; `--omit=dev` scoping; §7 PASS-not-clearance wording). Residual/follow-up recorded in the posture doc §9: **R-G19-1** (bump `electron` off 34.5.8 before gate-4 distribution — user-owned; a dependency-bump WI, out of this read-only lane); **R-G19-2** (reproducible-rebuild check for the two committed tarballs, post-v1); dev/build toolchain advisories (monitor; post-v1 toolchain bump); SBOM (optional/post-v1); the gate-17 license **decision** (user-owned; this inventory is its input); OCR-service re-audit when/if wired into the v1 client (post-v1). Gate 19's clearance is not this lane's to grant (stays OPEN) and does not imply go-live; gates 4/6/12/13/17/20 stay uncleared; the final GO/NO-GO + the STOP-AND-ASK hard-stops (4/11/17/21) remain the user's.
