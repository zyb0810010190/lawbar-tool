# Queue review — WI-RELEASE-ELECTRON-RUNTIME-ADVISORY-BUMP-00 (GOVERNANCE lane)

Lane: Electron runtime-advisory remediation **governance/authoring** (Type: IMPL, framework/runtime-dependency + native-module **HIGH risk**). Governs — does NOT execute — a FUTURE lane that performs the minimal Electron bump off 34.5.8 to resolve gate-19 residual **R-G19-1** (2+ HIGH advisories on the shipped runtime), gated on the full desktop test/build suite + a `better-sqlite3` native rebuild + a rollback plan. Changes NO dependency/lockfile; runs NO bump; clears NO gate; decides NO user hard-stop.
Date: 2026-07-08. Branch: `release-electron-runtime-advisory-bump-governance` (from synced `main` @ `f005b01`; created BEFORE any edit per the pre-flight guardrail — verified off-main; NO commit on local main). Batch: window 1/3 since marker `f68183f` (`f005b01` batch-252 closeout) — no batch closeout this lane.

## What this is
The authoring/governance lane of WI-RELEASE-ELECTRON-RUNTIME-ADVISORY-BUMP-00. It writes the governed queue WI ONLY — it changes NO dependency/lockfile, runs NO bump. This is the **first lane in the release effort authorized to change a dependency** (all prior lanes forbade it); the user explicitly authorized the Electron bump. Gate 19's supply-chain sweep surfaced **R-G19-1**: the v1 Mac client's shipped Electron runtime `electron 34.5.8` (declared a devDep, bundled as the runtime by electron-builder) carries HIGH advisories.

**Grounding (verified 2026-07-08 via read-only `npm audit`; exec lane re-verifies):** declared `electron: ^34.0.0`, resolved 34.5.8; `npm audit` vulnerable range `<=39.8.4`; `fixAvailable: {electron, 43.1.0, isSemVerMajor:true}` — the audit-resolved clean target is a **semver-MAJOR jump (34→43)** with real breaking-change + native-ABI risk. The governed WI defines the ten remediation requirements (current version+advisories; a precise minimal-version candidate-test procedure NOT defaulting to 43.1.0; exact package/lock files + lockfile-confinement proof; install command; re-audit verification; the full desktop test/build gate incl. Electron smoke/main/IPC tests + `run dist`; the `better-sqlite3` native rebuild + abi-smoke; a rollback plan with post-rollback verification; the evidence artifact; deterministic pass/fail + feeds-gate-19/4-without-clearing), keeps gate 19 OPEN (R-G19-1 *remediated*, not *cleared*) and gates 4/6/12/13/17/20 uncleared, forbids unrelated dependency upgrades + a license/distribution decision, and routes any electron-builder/source-compat requirement to a HARD STOP + a separate authorized WI.

## cc-suite recording (per .claude/rules/cc-suite.md §"Required recording")
Path 1 runner foreground, resolved runner path `/Users/zhongyibao/.claude/plugins/cache/xiaolai/cc-suite/0.2.18/scripts/codex-runner.mjs`, retrievable YES, no failure class, no fallback. review-plan on the governed queue WI (inlined; codex instructed not to read the filesystem). HIGH-risk WI → broker review REQUIRED (self-review forbidden). Two attempts (NEEDS-FIX → fixes applied → READY).

### /cc-suite:review-plan attempts (gpt-5.5/medium/read-only; on the governed queue WI)
- **Attempt 1** — `review-plan-mrblxtzb-ux0bq4` · **NEEDS-FIX** (2 scope-widening escape hatches, no Critical). High: the allowed-files only permit electron devDep + lockfile + docs, but the out-of-scope + forbidden sections carried a "minimal review-approved compatibility shim" AND an "electron-builder may be authorized as part of THIS WI" path — either could silently convert a minimal bump into a toolchain/source migration. Plus Lows: tighten the minimal-version candidate procedure; verify post-rollback lockfile+version; record `run dist` infeasibility reason. rawOutput sha256 `2781284e5593321fbce6c12237d6a8d65853a973e98b47883568b7383abc4c11`.
- **Fixes applied** (all): (1) BOTH escape hatches replaced with HARD STOP → roll back + open/amend a SEPARATE explicitly-authorized WI (in the out-of-scope, forbidden-files, Gates STOP-points, risk-flag (c), and exec-acceptance #7 — every occurrence); (2) requirement #2 rewritten as an explicit candidate procedure — re-verify the vulnerable range, test the LOWEST published version strictly above `39.8.4` FIRST, step up only with recorded audit/test-fail reasons, never default to `43.1.0`, and record the candidate-enumeration source + exact candidates tested; (3) requirement #3 now requires a lockfile-confinement PROOF — enumerate every changed lockfile package + justify each as Electron-owned/transitive, any non-Electron movement = HARD STOP; (4) requirement #8 now requires post-rollback verification (package.json back to `^34.0.0`, lockfile back to `34.5.8`, empty diff vs pre-bump, native rebuilt for Electron 34, gate green); (5) requirement #6 `run dist` infeasibility allowed ONLY with a recorded concrete environment reason + smoke/main/IPC still passing.
- **Attempt 2 (re-review)** — `review-plan-mrbm2dyz-urul2o` · **READY** (no Critical/High/Medium). "All prior High/Medium required fixes are addressed, and I do not see a new scope-widening or gate-clearing path." Confirmed per-dimension: scope control (electron devDep + Electron-owned lockfile subtree only), gate independence (gate 19 stays OPEN; 4/6/12/13/17/20 + GO/NO-GO uncleared/user-owned), risk handling (34→43 major-migration risk, full desktop tests + packaging + native rebuild + abi-smoke + cc-suite audit/verify), rollback (concrete + post-rollback verification), evidence quality, and "No new widening path: Confirmed." One execution-hygiene Low (record the candidate-enumeration source + exact candidates tested) — **applied** to requirement #2 (reviewer noted the wording was already sufficient for governance). rawOutput sha256 `7a7120db9d68d791196c0ec86d068ff0623259d70a53912b4bbf7bc99a339caa`.

## Verdict: READY (governance authored; NEEDS-FIX → fixes applied → re-review READY; governance-only; the future HIGH-risk exec lane performs the minimal user-authorized Electron bump, gated on the desktop test/build suite + native rebuild + rollback, running cc-suite audit+verify; gate 19 stays OPEN (R-G19-1 remediated, not cleared); gates 4/6/12/13/17/20 uncleared; signing/license/GO-NO-GO user-owned; no unrelated dependency upgrade)

QUEUE_REVIEW_VERDICT=PASS

## Gates (this governance lane)
- `scripts/workflow/check-queue.sh` → QUEUE LINT PASSED (re-run after the fixes + the exec-hygiene Low; queue.linted regenerated).
- `scripts/workflow/check-contract-integrity.sh` → PASS (to run pre-commit).
- Governed queue.md sha256 `35f6aaf4…` (content-bound by `govern-queue.sh` into `queue.governed`).
- `CURRENT_SCHEMA_VERSION` unchanged (12); no dependency change, no lockfile mutation, no product source/test change; no remediation doc, no ADR/brief edit, no gate clear, no go-live decision — only the queue governance (`queue.{md,linted,reviewed,governed}`) + this review artifact.

## Deferred findings
None deferred as open — the two NEEDS-FIX High/Medium escape hatches were fully closed (converted to HARD STOP + separate-WI), the three Lows applied, and the re-review returned READY with no new finding. The Electron bump itself is the FUTURE exec lane's HIGH-risk work (cc-suite audit+verify required). Gate 19 stays OPEN (R-G19-1 remediated, not cleared); gates 4/6/12/13/17/20 uncleared; signing/notarization/distribution (gate 4) + license (gate 17) + the final GO/NO-GO (gate 21) remain the user's.
