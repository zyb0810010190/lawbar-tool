# Queue review — WI-RELEASE-G14-BACKUP-RECOVERY-00 (EXECUTION lane)

Lane: M0 gate-14 backup + recovery procedure **execution** (Type: EVIDENCE, release-governance MEDIUM risk). Authors the operator backup + recovery runbook + updates the gate-14 evidence row. Changes NO product source/test/config; runs NO live backup; writes NO backup software; adds NO dependency; edits NO brief; clears NO gate; decides NO user go-live hard-stop.
Date: 2026-07-07. Branch: `release-g14-backup-recovery-exec` (from synced `main` @ `1466dfd`). Batch: window 1/3 since marker `abab91f` (`1466dfd` batch-228 closeout) — no batch closeout this lane.

## What this is
The execution lane of governed WI-RELEASE-G14-BACKUP-RECOVERY-00 (governed commit `02f23bf`, queue.governed sha256 `3e8a9f7b…`, PR #196 merge `abab91f`). It authored `docs/release/gate14-backup-recovery-00.md` — the operator data-survival runbook — with the backup manifest grounded in the REAL shipped v1 layout read from source at exec time (`~/Library/Application Support/lawbar/` = single `case-box.sqlite` [audit tables inside] + `case-box-documents/` blob dir + `theme-preference.json`; re-verified from `apps/lawbar-desktop/electron/main.ts:27,116` + `apps/lawbar-desktop/src/caseBox/caseBoxRuntime.ts:14,42`). It documents backup-as-directory (app-closed preferred; WAL-sidecar caveat for hot copy) + drop-directory restore (rename-aside-then-copy) + the `PRAGMA integrity_check` + audit `event_count==COUNT==MAX(sequence)` + blob-presence verification, and correctly distinguishes what runs automatically on open (`journal_mode=WAL` + `applySchema` version-gate, `openSqliteCaseBoxPersistence.ts:41,50` / `schema.ts:704`) from the on-demand checks (`PRAGMA integrity_check`, `verifyAuditChainForMatter` — `types.ts:147`), rejecting the brief's implied automatic startup audit-chain verification. It **cites the executed gate-7 crash-recovery drill** (`gate7-crash-recovery-drill-00.md`, PASS) as its verification rather than re-running a live backup, and carries the drill residuals R-DRILL-1/2. It logs the brief §14 two-file-manifest divergence as `RECONCILIATION-NEEDED` (§7) with a proposed bounded follow-up WI, **without editing the brief**. It defers gates 18/15/12 to their own lanes and maps gates 6/7.

Deliverables (exactly two tracked files + this review artifact):
1. NEW `docs/release/gate14-backup-recovery-00.md` — the procedure + verification record (§1 manifest; §2 backup; §3 restore; §4 verification; §5 drill evidence; §6 scope/status; §7 reconciliation log; §8 residual follow-ups).
2. `docs/release/go-live-readiness-report.md` — the gate-14 ROW ONLY: `OPEN` → `OPEN — procedure documented + drill-verified [Δ]` (PARTIAL-eligible at next readiness refresh), still NOT cleared. Roll-up buckets left CONSISTENT (gate 14 stays in the OPEN bucket — status is still OPEN; the bucket move to PARTIAL is deferred to a holistic readiness-refresh lane, the established owner of roll-up reconciliation, which also weighs the gate-18 overlap). No other row / no roll-up line edited.

## cc-suite recording (per .claude/rules/cc-suite.md §"Required recording")
Path 1 runner foreground, resolved runner path `/Users/zhongyibao/.claude/plugins/cache/xiaolai/cc-suite/0.2.18/scripts/codex-runner.mjs`, retrievable YES, no failure class, no fallback. Docs-evidence convention: review-plan on the PRODUCED runbook + the gate-14 row diff, both inlined into the prompt. Completed first attempt, no timeout.

### /cc-suite:review-plan (gpt-5.5/medium/read-only; on the produced runbook + gate-14 row diff)
- `review-plan-mraar1jh-fk0pg4` · **READY (Low-risk clarifications)** (no Critical/High/Medium). All 7 specific confirmations YES: (1) manifest grounded in the real shipped layout, does NOT copy the brief's `audit-log.sqlite`/`blobs/` model as reality; (2) brief §14 divergence flagged `RECONCILIATION-NEEDED` + follow-up WI, brief not edited; (3) gate-7 drill cited honestly as verification (no new live backup claimed), residuals R-DRILL-1/2 carried; (4) startup behavior not overclaimed (WAL + applySchema auto vs integrity_check + verifyAuditChainForMatter on-demand); (5) gates 18/15/12 deferred, gates 6/7 mapped not cleared; (6) gate 14 kept non-cleared (row stays OPEN, evidence-only), no GO/NO-GO / no STOP-AND-ASK decided; (7) all transcripts use fixture/placeholder paths, no real lawyer data. One **Low**: the restore step used `rm -rf "$DEST"` — recommend rename-then-copy for operator safety — **applied**: §3 now moves the existing dir aside (`mv "$DEST" "$DEST.pre-restore-…"`) before dropping the backup, and only removes the aside copy after §4 verification passes. · rawOutput sha256 `2b5302cd0ba1b32f297d775ed6807db7d5c21d749a20c283cf4799bdf9216045`.

## Verdict: READY (procedure documented + drill-verified; manifest grounded in reality; brief divergence flagged not resolved; gates 18/15/12 deferred; gate 14 non-cleared; go-live-independent)

QUEUE_REVIEW_VERDICT=PASS

## Gates (this execution lane)
- `scripts/workflow/check-queue.sh` → QUEUE LINT PASSED (queue.linted timestamp side-effect restored — this exec lane does NOT re-stage queue governance).
- `scripts/workflow/check-contract-integrity.sh` → PASS.
- `CURRENT_SCHEMA_VERSION` unchanged (12); no product source/test/package/schema/contract change — only the NEW runbook + the gate-14 evidence row + this review artifact. No live backup run, no new backup software, no new dependency, no brief edit, no gate-6 run, no export-format cert (gate 18), no rollback-dry-run impl (gate 15), no audit-chain deep-dive (gate 12), no unilateral gate-14 clear, no go-live decision.

## Deferred findings
None deferred as open — the one Low was applied (rename-aside restore). Residual follow-ups recorded in the runbook §8 (separate future WIs, not run here): R14-FUP-1 (brief §14 two-file → single-file manifest reconciliation; overlaps drill R-DRILL-1); R14-FUP-2 (gate-18 export/backup FORMAT certification); R14-FUP-3 (optionally wire automatic post-open audit-chain verification — product-source change, separate governed WI); R14-FUP-4 (stronger hot-copy WAL-completeness check; counterpart of drill R-DRILL-2). Gate 14's clearance is not this lane's to grant and does not imply go-live; the final GO/NO-GO + the three STOP-AND-ASK hard-stops (4/11/21) remain the user's.
