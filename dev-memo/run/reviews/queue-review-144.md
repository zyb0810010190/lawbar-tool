# Queue review — WI-RELEASE-G7-CRASH-RECOVERY-DRILL-00 (authoring/governance)

Lane: M0 gate-7 crash-recovery-drill WI **authoring/governance** (Type: EVIDENCE, release-governance MEDIUM risk). Governance-authoring only — this lane produces the governed queue WI so a FUTURE lane authors + runs the crash-recovery drill; it runs NO drill, implements NOTHING, edits no drill doc/report, decides NEITHER user risk-decision (D-G7-1/D-G7-2), and makes NO go-live decision.
Date: 2026-07-06. Branch: `release-g7-crash-recovery-drill-governance` (from synced `main` @ `df16c83`). Batch: 1/3 since marker `b1eacfb` — no batch closeout this lane.

## What this is
Authorizes (per explicit user authorization 2026-07-06) a FUTURE execution lane to author + run the **crash-recovery drill** that the shipped gate-7 robustness policy (`docs/release/gate7-robustness-policy-00.md`, WI-RELEASE-G7-ROBUSTNESS-POLICY-00, PR #193 merge `b1eacfb`) recommends and its user-decision D-G7-2 may require — the load-bearing closure evidence for gate-7 criteria 1/2/4 (crash/data-loss tolerance; local-first persistence/recovery; audit/event integrity under failure). The future lane produces `docs/release/gate7-crash-recovery-drill-00.md` defining ten drill requirements — (1) fixture / disposable data-directory expectations (isolated path mirroring `case-box.sqlite` + `audit-log.sqlite` + `blobs/`, NEVER real `~/Library/Application Support/lawbar/` data); (2) backup/restore procedure (backup-as-directory per brief §14 — the two SQLite files + the `blobs/` dir; restore = drop-directory-back); (3) simulated crash/interruption boundary (`kill -9`/power-loss taken MID-WRITE deterministically); (4) SQLite `PRAGMA integrity_check` on both DBs + startup schema-integrity check; (5) audit/event integrity incl. the `event_count == COUNT(*) == MAX(sequence)` invariant + chain continuity across crash/restart (overlaps gate 12); (6) expected recovery result (all seeded rows intact; in-flight txn fully committed OR fully rolled back, never torn); (7) required evidence artifacts (command transcripts, integrity_check output, invariant query, before/after counts, environment — fixture-only, NO real case data); (8) explicit PASS/FAIL criteria (a FAIL is a class-2 robustness STOP); (9) manual-only / scripted / hybrid mode (default recommendation: hybrid; a scripted harness is a drill artifact, NOT a product mutation/fuzz/crash-injection harness, NO new dependency); (10) how the result feeds gate 7 WITHOUT auto-clearing it (a PASS is evidence; gate 7 still needs USER decisions D-G7-1/D-G7-2). It maps dependencies (not cleared): gate 7 (this drill is the policy's required closure evidence), gate 12 (audit-chain overlap), gate 14 (backup+recovery procedure exercised), gate 15 (rollback overlap), gate 6 (a drill FAIL would surface there). NO drill EXECUTION in this authoring lane; NO product crash-injection harness; NO new dependency; NO gate-6 run; NO go-live/STOP-AND-ASK decision; NO unilateral gate-7 clear; NO D-G7-1/D-G7-2 decision.

## cc-suite recording (per .claude/rules/cc-suite.md §"Required recording")
Path 1 runner foreground, resolved runner path `/Users/zhongyibao/.claude/plugins/cache/xiaolai/cc-suite/0.2.18/scripts/codex-runner.mjs`, retrievable YES, no failure class, no fallback. Governance-authoring lane → review-plan only (the exec lane's review-plan + any harness audit belong to the future execution lane). The full queue WI was inlined into the prompt (governance range; inlined for consistency with the session's timeout-avoidance convention). Completed first attempt, no timeout.

### /cc-suite:review-plan (gpt-5.5/medium/read-only; on the WI)
- `review-plan-mr8uuimw-2leg0s` · **READY** (no Critical/High/Medium). All five questions answered YES/none: (1) scopes a crash-recovery DRILL procedure + evidence record, NOT a drill run in this lane, NOT a product mutation/fuzz/crash-injection harness; (2) the ten requirements are complete + v1-local-first (fixture-only, `PRAGMA integrity_check`, the audit `event_count==COUNT==MAX(sequence)` invariant, backup-as-directory restore); (3) gate 7 kept NON-CLEARED, D-G7-1/D-G7-2 left to the user, a drill PASS treated as evidence only; (4) gate-7/12/14/15/6 dependencies mapped without clearing; (5) no drill execution / product harness / gate-6 audit / new dependency / go-live decision smuggled in. Two **Low** observations: (a) "three-file manifest" was imprecise (the backup set is two SQLite files + the `blobs/` directory) — **applied**: requirement 2 now names the two SQLite files + the `blobs/` dir explicitly; (b) if the future lane makes the drill scripted/hybrid, it should state the "drill artifact, not product harness" boundary in its own scope + review packet before editing `scripts/` — **applied**: added that reminder to requirement 2. Both non-blocking. · rawOutput sha256 `94a9624e991929aaee58d1259883282b9bcde6580aee1728e2455a2872a5b2da`.

## Verdict: READY (governed gate-7 crash-recovery-drill WI; governance-only; no drill run; no unilateral clear; go-live-independent)

QUEUE_REVIEW_VERDICT=PASS

## Gates (this authoring lane)
- `scripts/workflow/check-queue.sh` → PASS.
- `scripts/workflow/check-contract-integrity.sh` → PASS.
- `CURRENT_SCHEMA_VERSION` unchanged (12); no source/test/drill/report/native/schema/contract/dependency code touched — this lane commits ONLY the queue governance + this review artifact. No drill run, no gate-6 run, no risk-acceptance, no go-live decision.

## Deferred findings
None. review-plan READY; both Lows applied (manifest precision + the exec-lane harness-boundary reminder). The FUTURE execution lane owes: the drill doc (ten requirements + the actual drill run on a fixture data dir + the recorded result), the gate-7 evidence-row update (non-unilateral — a PASS is evidence, gate 7 stays non-cleared), any scripted drill harness (as a separately-scoped drill artifact, no new dependency), and its own review-plan (+ an audit if a harness lands). Gate 7's clear depends on USER decisions D-G7-1 (accept v1 without mutation testing) + D-G7-2 (require the drill) — this drill supplies evidence, it does NOT decide those or imply go-live; the final GO/NO-GO verdict + the three STOP-AND-ASK hard-stops (4/11/21) remain the user's.
