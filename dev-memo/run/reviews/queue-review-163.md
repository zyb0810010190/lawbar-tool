# Queue review — WI-RELEASE-G13-OPERATOR-CHECKLIST-RUNBOOK-00 (EXECUTION lane)

Lane: M0 gate-13 operator checklist + runbook completion **execution** (Type: EVIDENCE, release-governance MEDIUM risk). COMPLETES the two stale scaffold docs (`docs/release/operator-checklist.md` + `docs/release/ocr-worker-runbook.md`) with a Mac-client operator section + case-box local-first operating procedures, cross-references the gate 14/15/2/20 evidence WITHOUT clearing them, reconciles the stale OCR-worker *service* framing against the locked v1 Mac-desktop local-first posture, and updates the gate-13 evidence row. Changes NO product source/test/config; edits NO ADR/brief; implements NO OCR/Forms; clears NO gate; decides NO user go-live hard-stop.
Date: 2026-07-07. Branch: `release-g13-operator-checklist-runbook-exec` (from synced `main` @ `4ecae92`; created BEFORE any edit per the pre-flight guardrail — verified off-main; NO commit on local main). Batch: window 1/3 since marker `224d13b` (`4ecae92` batch-248 closeout) — no batch closeout this lane.

## What this is
The execution lane of governed WI-RELEASE-G13-OPERATOR-CHECKLIST-RUNBOOK-00 (governed commit `35a8adb`, queue.governed sha256 `7e2bd54d…`, PR #216 merge `224d13b`). It completed the two scaffold docs and updated ONLY the gate-13 evidence row. NO ADR/brief edit, NO product source/test/config change, NO OCR/Forms implementation.

**Grounding (verified against shipped source this lane, per `AGENTS.md` §"Authoring discipline"):** case data at `~/Library/Application Support/lawbar/` (`electron/main.ts` — `app.getPath("userData")`, product name `lawbar`) = `case-box.sqlite` (`src/caseBox/caseBoxRuntime.ts`, `CASE_BOX_DB_FILENAME`) + `case-box-documents/` (`main.ts`, `documentStorageRoot`) + `theme-preference.json` (`src/persistence/themePreference.ts`); a FileVault production-launch gate (`main.ts`, `[lawbar:fileVault]`); a main-process single-file chooser (`dialog.showOpenDialog`, `openFile`; renderer never supplies a path). **The OCR worker is NOT wired into the v1 Mac client** — `grep` over `apps/lawbar-desktop/{electron,src}` + `package.json` returned no OCR import / `OCR_WORKER` reference — so the OCR *service* runbook is reconciled as `post-v1 / deferred`; gate-2's real-PaddleOCR path is model-gated/post-v1 (R-G2-3).

**Deliverables (exactly three tracked files + this review artifact):**
1. `docs/release/operator-checklist.md` — completed for the v1 Mac-client surface: §1 Mac-client operator section (install/launch + FileVault gate; data location; first-run; normal op; quit/restart; offline/local-first); §2 case-box local-first operating procedures; §3 gate 14 (backup/recovery) + gate 15 (rollback) references (NOT cleared); §4 gate 2 (OCR) + gate 20 (telemetry) references (NOT cleared); §5 the reconciled OCR-service scaffold sections labelled `post-v1 / deferred`; §6 RECONCILIATION-NEEDED note; §7 "before gate 13 can move" completion checklist (roll-up move left unchecked, out of scope); §8 residual/post-v1.
2. `docs/release/ocr-worker-runbook.md` — completed + RECONCILED: a prominent RECONCILIATION-NEEDED block (what was stale, why deferred — OCR not wired into the client + local-loopback-not-service per `client-local-first.md`, resolution) + every original service-mode section retained and explicitly labelled `post-v1 / deferred`, never silently rewritten.
3. `docs/release/go-live-readiness-report.md` — the gate-13 ROW ONLY: `OPEN` → `OPEN — operator checklist + runbook docs completed [Δ]` (still NOT cleared; movement belongs to the next holistic readiness refresh); roll-up bucket unchanged; no other row edited.

**RECONCILIATION-NEEDED (three-place audit trail, per the governed WI):** logged (i) in the runbook itself (prominent block + per-section `post-v1 / deferred` labels with the what/why), (ii) in `operator-checklist.md` §6, AND (iii) here in this review artifact + (iv) the exec commit message. What was stale: the OCR-worker *service* production-profile framing (S3/HTTPS-allowlist/production-env/healthcheck/shutdown). Why deferred: the OCR worker is not wired into the v1 Mac client, and per `client-local-first.md` the OCR worker — when integrated — is an in-process/local-loopback transport, not a public/deployed service. Not rewritten to erase the prior posture; a future ADR/reconciliation WI owns any change to locked direction. This is a `RECONCILIATION-NEEDED` record, not an ADR decision.

## cc-suite recording (per .claude/rules/cc-suite.md §"Required recording")
Path 1 runner foreground, resolved runner path `/Users/zhongyibao/.claude/plugins/cache/xiaolai/cc-suite/0.2.18/scripts/codex-runner.mjs`, retrievable YES, no failure class, no fallback. review-plan on the produced doc completions + the gate-13 row diff (inlined; codex instructed not to read the filesystem). Completed first attempt, no timeout.

### /cc-suite:review-plan (gpt-5.5/medium/read-only; on the produced completions + gate-13 row diff)
- `review-plan-mrbjiwkg-mba8jq` · **READY (Low-risk clarifications)** (no Critical/High/Medium). All 5 dimensions PASS: consistency (OCR framing consistently `post-v1/deferred`; gate 13 stays OPEN with `[Δ]`, roll-up unchanged; gates 14 OPEN / 15 PARTIAL / 2 PARTIAL / 20 PARTIAL cited-not-cleared); completeness (Mac-client section grounded in the verified facts; case-box local-first procedures present; completion checklist keeps the roll-up move out of scope; three-place reconciliation trail asserted); feasibility (docs-only; no source/test/config/schema/contract/ADR/brief/dependency/readiness-refresh/OCR/Forms/GO-NO-GO/STOP-AND-ASK change); ambiguity + risk (OCR reconciliation honest given the OCR worker is not wired into the client; no gate wrongly cleared; no silent rewrite). **Three Low precision clarifications, all applied:** (a) gate-13 row "PARTIAL/CLEARED-eligible at next readiness refresh" → "movement, if any, belongs to the next holistic readiness refresh" (avoid a too-strong "CLEARED-eligible"); (b) the runbook's "ingests PDF/Word/MD + manual paste (per the brief)" sentence softened — the precise v1 client ingest formats are defined by the brief/client surface, NOT certified by this runbook; (c) the operator-checklist "no network entitlement" line kept explicitly `intended` / confirmed-at-gate-4, not a shipped fact. · rawOutput sha256 `4c5dd06068832062141e4a8a9b4b9c4fb3d2fcfaf831ae625fc8d8599ad47eaa`.

## Verdict: READY (completion executed; docs-only; two operator docs completed + reconciled; gate 14/15/2/20 cited without clearing; OCR service-framing reconciled post-v1/deferred with a three-place audit trail; gate 13 stays OPEN; dependent gates uncleared; go-live-independent; no STOP-AND-ASK decided)

QUEUE_REVIEW_VERDICT=PASS

## Gates (this execution lane)
- Doc completion executed (Mac-client operator section + case-box local-first procedures + gate 14/15/2/20 references-without-clearing + OCR-service reconciliation post-v1/deferred + completion checklist) → PASS. No ADR/brief edit.
- `scripts/workflow/check-queue.sh` → QUEUE LINT PASSED (queue.linted timestamp side-effect restored — this exec lane does NOT re-stage queue governance).
- `scripts/workflow/check-contract-integrity.sh` → PASS (14 contract docs clean).
- `CURRENT_SCHEMA_VERSION` unchanged (12); no product source/test/package/schema/contract change — only the two completed operator docs + the gate-13 evidence row + this review artifact. No ADR/brief edit, no OCR/Forms implementation, no dependency change, no gate-6 run, no readiness refresh, no clearing of gates 2/6/12/14/15/18/19/20, no go-live decision.

## Deferred findings
None deferred as open — the three precision Lows were applied (row wording; runbook ingest-format softening; operator-checklist "intended/pending gate-4" wording). Residual notes recorded in the completed docs: the OCR-service→local-first reconciliation may need a follow-up ADR/reconciliation WI when/if OCR is wired into the client (post-v1); a full illustrated operator manual is post-v1; gate 13's roll-up move belongs to the holistic readiness refresh. Gate 13's clearance is not this lane's to grant (stays OPEN) and does not imply go-live; gates 2/6/12/14/15/18/19/20 stay uncleared; the final GO/NO-GO + the STOP-AND-ASK hard-stops (4/11/17/21) remain the user's.
