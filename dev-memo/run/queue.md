## WI-A3-LINK-DEFERRED-CLEANUP-00: D2/D3/D4 governance/doc reconciliation + D5 renderer DTO-sync hardening (Type: TEST)
Type: TEST
Scope: Clear the four lightweight deferred items left by the A3 link IPC/UI sequence (D2/D3/D4 doc-consistency + D5 test-hardening), with NO runtime/product behavior change. Concretely: (D3) update the LIVING IPC ADR docs/adr/ADR-evidence-a3-link-ipc-surface.md §2 request-DTO table so the `UnlinkLinkDto` Fields cell reads `matterId`, `linkId`, `unlinkReason` and the `RelinkLinkDto` Fields cell reads `matterId`, `linkId` — matching the ALREADY-MERGED canonical DTOs (`UNLINK_LINK_DTO_FIELDS` = matterId,linkId,unlinkReason; `RELINK_LINK_DTO_FIELDS` = matterId,linkId in apps/lawbar-desktop/src/caseBox/dto/link.ts, READ-ONLY) and §5's existing "Matter: `matterId` is renderer-specified" boundary text; documentation sync ONLY, NO IPC/DTO runtime change. (D5) add ONE entry `["LINK_RESPONSE_FIELDS", ["tenant_id", "actor_user_id", "payload_json", "exportFlag", "export_flag"]]` to the `RESPONSE_ALLOWLISTS` array in apps/lawbar-desktop/tests/renderer-dto-sync.test.mjs so the existing per-entry authority-exclusion test ALSO proves the canonical `LINK_RESPONSE_FIELDS` (id,matter_id,source_type,source_id,anchor_id,status,created_at,unlinked_at,unlink_reason — read from dto/link.ts via the test's existing `extractCanonical()` reader) admits NONE of those forbidden/authority fields; test-hardening ONLY (the projection invariant already holds and is IPC-tested by tests/ipc-link-handlers.unit.test.mjs — this centralizes it in the renderer sync guard), NO `exportFlag` added to `LINK_RESPONSE_FIELDS`, NO renderer row DTO widened. (D2 + D4) mark the two stale-prose findings RESOLVED in dev-memo/deferred-audit-findings.md by updating their block entries in-place (status closed + a resolution row citing this lane), per .claude/rules/cc-suite.md §"Durable backlog file" ("update the row's status field in-place to closed and cite the resolution commit — the row stays visible"). D2: the "no errorMap change" prose is stale because the authorized one-line `anchor_referenced` exhaustiveness sync DID land (apps/lawbar-desktop/src/caseBox/errorMap.ts:24 `anchor_referenced: "anchor is referenced"`); the AUTHORITATIVE source is already correct (ADR §3 lines 115-117 documents the optional additive errorMap allowance). D4: the "export flags on link rows" prose was the DESIGN-LANE queue.md snapshot (since OVERWRITTEN — survives only in git history); the AUTHORITATIVE design artifact (dev-memo/design/2026-06-26-audited-evidence-links-ui.md §3 lines 49-57: "`exportFlag` is NOT on `RendererLink`... the list row MUST NOT show it") and the current queue.md are already correct. The PROPOSED handling for D2/D4 is to record the superseding resolution in the LIVING backlog (deferred-audit-findings.md) and NOT retroactively rewrite the committed point-in-time audit-trail snapshots (dev-memo/run/reviews/queue-review-094.md, dev-memo/study/2026-06-26-batch-audit-174.md, dev-memo/study/2026-06-26-batch-audit-175.md) — rewriting an as-committed governance snapshot would corrupt the audit trail. review-plan MUST adjudicate this (see Gates); if review-plan instead directs a direct in-snapshot prose correction, STOP and re-review with an amended Allowed-files list before editing any snapshot.
Source of truth: apps/lawbar-desktop/src/caseBox/dto/link.ts (READ-ONLY — the canonical `UNLINK_LINK_DTO_FIELDS` / `RELINK_LINK_DTO_FIELDS` the D3 ADR table mirrors + `LINK_RESPONSE_FIELDS` the D5 test asserts); apps/lawbar-desktop/src/caseBox/errorMap.ts (READ-ONLY — the landed `anchor_referenced` line proving D2 prose stale); docs/adr/ADR-evidence-a3-link-ipc-surface.md §2/§3/§5 (the IPC contract being doc-synced for D3 + the already-correct D2 authority); dev-memo/design/2026-06-26-audited-evidence-links-ui.md §3 (READ-ONLY — the already-correct D4 authority); apps/lawbar-desktop/tests/renderer-dto-sync.test.mjs (the `RESPONSE_ALLOWLISTS` + `extractCanonical()` precedent the D5 entry follows); dev-memo/deferred-audit-findings.md (LINK-IPC-T1-D2 / LINK-UI-DESIGN-D4 / LINK-UI-T1-D5 block entries being closed; LINK-IPC-T1-D1 stays DEFERRED — untouched); .claude/rules/{cc-suite,autonomy,execution-discipline,staging-hygiene,evidence-genie}.md
Allowed files: docs/adr/ADR-evidence-a3-link-ipc-surface.md, apps/lawbar-desktop/tests/renderer-dto-sync.test.mjs, dev-memo/deferred-audit-findings.md, dev-memo/run/queue.md, dev-memo/run/queue.linted, dev-memo/run/queue.reviewed, dev-memo/run/queue.governed, dev-memo/run/reviews/queue-review-097.md
Forbidden files: apps/lawbar-desktop/src/caseBox/** (READ-ONLY — NO IPC/handler/DTO/errorMap change; D2 only acknowledges the ALREADY-landed errorMap line, it does NOT edit errorMap.ts), apps/lawbar-desktop/renderer/** (NO renderer UI/source/type/i18n change — D5 is a TEST-file change only; renderer row DTOs MUST NOT widen), apps/lawbar-desktop/electron/** (NO IPC/preload change), services/** (NO persistence change), services/case-box-persistence/src/sqlite/schema.ts (NO schema/`CURRENT_SCHEMA_VERSION` change), docs/contracts/** (NO contract change), docs/adr/ADR-evidence-a3-link-create-operation.md + any OTHER docs/adr/*.md (ONLY ADR-evidence-a3-link-ipc-surface.md is edited), dev-memo/design/** (the design artifact is authoritative + already correct — NOT edited; the §11-12 D1-fold correction is OUT OF SCOPE this lane per the current authorization's "keep D1 deferred and untouched"), dev-memo/run/reviews/queue-review-094.md + dev-memo/study/2026-06-26-batch-audit-174.md + dev-memo/study/2026-06-26-batch-audit-175.md (committed point-in-time audit-trail SNAPSHOTS — NOT rewritten unless review-plan explicitly directs + an amended Allowed-files re-review), package-lock.json + dist-tarballs/** (NO dependency/tarball churn — never staged), native/**, .claude/**, .github/**, AGENTS.md, CLAUDE.md, GEMINI.md, scripts/workflow/**, migrations/**, committed dev-memo/run/evidence/** marker/ledger/key/evidence-run files, committed keys, dev-memo/run/reports/** (untracked local run-state — never staged), dev-memo/run/override-reason.md (untracked residue — never staged), Evidence-Genie-M0-Developer-Handover.md (root intake — READ-ONLY, never moved/deleted), xiaolai-dev-workflow-study.md (root intake — READ-ONLY, never moved/deleted); NO D1 work, NO anchor IPC, NO Electron integration seed hook, NO renderer UI/IPC/preload/persistence/schema/audit-contract/dependency change, NO push/merge/branch-delete
Gates: broker /cc-suite:review-plan REQUIRED before implementation — it MUST (1) confirm the D3 ADR §2 table edit exactly matches the canonical `UNLINK_LINK_DTO_FIELDS`/`RELINK_LINK_DTO_FIELDS` and introduces NO runtime/DTO change; (2) confirm the D5 `RESPONSE_ALLOWLISTS` entry asserts the real `LINK_RESPONSE_FIELDS` (and that it admits none of tenant_id/actor_user_id/payload_json/exportFlag/export_flag), is TEST-only, and adds no `exportFlag` to `LINK_RESPONSE_FIELDS`; (3) ADJUDICATE the D2/D4 historical-snapshot question — confirm closing the findings via the LIVING deferred-audit-findings.md backlog (status-in-place + resolution row) WITHOUT rewriting the committed queue-review-094.md / batch-audit-174.md / batch-audit-175.md snapshots is the correct cleanup, OR direct an alternative (in which case STOP and re-review with amended Allowed files before editing any snapshot); (4) confirm D1 stays deferred + untouched and no runtime code changes are required by any item. If review-plan returns NEEDS-FIX, STOP and report. After implementation: npm --prefix apps/lawbar-desktop test (renderer-dto-sync + full desktop suite GREEN); scripts/workflow/check-contract-integrity.sh exits 0; broker /cc-suite:audit + /cc-suite:verify on the diff (no Critical/High/Medium open before commit). No A0.7 custody required UNLESS review-plan identifies an A0.7-dependent action (none expected — no geometry/marker/renderer-UI code is touched).
Acceptance criteria: docs/adr/ADR-evidence-a3-link-ipc-surface.md §2 `UnlinkLinkDto` Fields cell contains `matterId` and `RelinkLinkDto` Fields cell contains `matterId` (set-equal to the canonical DTO field arrays) and grep shows no other ADR §2 row changed; apps/lawbar-desktop/tests/renderer-dto-sync.test.mjs `RESPONSE_ALLOWLISTS` contains the `LINK_RESPONSE_FIELDS` entry and the test `response projection: LINK_RESPONSE_FIELDS excludes every authority field` PASSES (and `npm --prefix apps/lawbar-desktop test` exits 0 with the full suite GREEN, no test count regression); dev-memo/deferred-audit-findings.md shows LINK-IPC-T1-D2, LINK-UI-DESIGN-D4, LINK-UI-T1-D5 each with status closed + a resolution row citing this lane, and LINK-IPC-T1-D1 UNCHANGED (still deferred); scripts/workflow/check-contract-integrity.sh exits 0; CURRENT_SCHEMA_VERSION remains 12; no apps/src-caseBox, renderer-source, electron, services, schema, contract, package-lock, or dependency file changed; broker review-plan READY + audit no Critical/High/Medium + verify ALL CLOSED; the committed diff is confined to the Allowed files.
Risk flags: LOW-RISK doc/test cleanup — NO product/runtime behavior change, NO renderer source, NO IPC/preload/persistence/schema/contract/dependency change. The one code-adjacent change (D5) is a TEST-file regression-coverage addition asserting an invariant that already holds at the canonical/IPC layer. Load-bearing care points: (a) the D3 ADR table edit MUST mirror the merged canonical DTOs exactly (no fictional fields, no widening); (b) the D5 entry MUST NOT add `exportFlag` to `LINK_RESPONSE_FIELDS` or widen any renderer row DTO; (c) D2/D4 MUST NOT corrupt the committed audit-trail snapshots — the resolution lives in the living backlog; (d) D1 stays deferred + untouched; (e) no runtime A0.7 mechanism. If any item proves to require a runtime/source change (stop-point 1/2), or review-plan directs editing a committed snapshot (stop-point 3), STOP and report. No push, no merge, no branch delete.
Depends on: none
Commit boundary: governance (mark-reviewed + govern) run STANDALONE before the commit (never bundled with git commit). ONE cleanup commit — the ADR (D3) + the test file (D5) + deferred-audit-findings.md (D2/D4/D5/D3 closes) + governance artifacts (queue.md/.linted/.reviewed/.governed + reviews/queue-review-097.md), staged by exact path; NO src/caseBox, renderer-source, electron, services, schema, contract, package-lock, dependency, design-artifact, or committed-snapshot file; NO marker/evidence/key/report file; no push; no merge.
