## WI-RELEASE-G7-ROBUSTNESS-POLICY-00: define + govern the M0 gate-7 robustness/mutation-test policy with explicit closure criteria (Type: EVIDENCE, release-governance MEDIUM risk)

## Review packet (compact)
- Active plan summary: convert M0 go-live **gate 7** (`docs/release/go-live-readiness-report.md` §1 gate 7 = OPEN — "Mutation-test / robustness policy … accept v1 without mutation testing OR bounded sweep") from an ambiguous OPEN blocker into a governed, reviewable robustness-policy DECISION with explicit closure criteria. The FUTURE execution lane produces a robustness-policy decision/assessment doc (`docs/release/gate7-robustness-policy-00.md`) that: (a) enumerates the v1 robustness properties + the existing evidence; (b) states what gate 7 must PROVE (the closure criteria, below); (c) RECOMMENDS a v1 policy; and (d) surfaces the risk-acceptance (e.g. "accept v1 without mutation testing") as a USER decision — it does NOT unilaterally clear gate 7. This is a MANUAL-TRUTH v1, local-first, offline-first tool (`.claude/rules/client-local-first.md`); the robustness bar is "a lawyer's local case data survives crashes + is recoverable", not distributed-system fault tolerance.
- Exact target files (future exec lane): a NEW `docs/release/gate7-robustness-policy-00.md` (the policy/decision doc) + the gate-7 evidence ROW of `docs/release/go-live-readiness-report.md` (update to reflect the policy authored + point to the doc, keeping gate 7 OPEN or moving to PARTIAL/CLEARED-pending-user-acceptance per what the evidence supports — NOT a unilateral CLEARED) + `dev-memo/run/reviews/` (the exec lane's review artifact).
- Exact acceptance criteria: below.
- Exact out-of-scope: NO robustness/mutation-test IMPLEMENTATION (no mutation-testing harness, no fuzzer, no crash-injection code); NO gate-6 full-project audit; NO release-doc finalization beyond the one policy doc + the gate-7 row; NO signing/notarization/公开 distribution decision; NO 律师法 conclusion; NO final GO/NO-GO; NO source/test/package/config/schema/contract change. If the policy recommends a bounded mutation/fuzz sweep, that sweep is a SEPARATE future WI, not run here.
- Closure criteria the policy doc must define (what gate 7 must prove before go-live) — at minimum:
  1. **Crash / data-loss tolerance**: a crash (kill -9 / power loss) mid-write must not corrupt the case-box SQLite DBs or the append-only hash-chained audit log; the WAL/synchronous-write posture of `better-sqlite3` + the startup schema-integrity check (brief §14) are the mechanism; the policy states the acceptance bar (e.g. "no partial-write corruption; a crashed transaction rolls back atomically").
  2. **Local-first persistence / recovery**: the backup-as-directory model (brief §14 — Time Machine over `~/Library/Application Support/lawbar/`: matter SQLite + audit-log SQLite + content-hash blob store) + "drop directory back, run schema integrity check on startup" recovery; the policy states what a recovery drill must show (overlaps gate 14/15).
  3. **Offline / online reconciliation**: v1 is fully offline (no cloud sync) — the policy states this is N/A for v1 and out of scope (no reconciliation surface exists), so it is not a gate-7 blocker.
  4. **Audit / event integrity under failure**: the hash-chained audit log's tamper-evidence + `event_count == COUNT(*) == MAX(sequence)` invariant must hold across a crash/restart (overlaps gate 12); the policy states the acceptance bar.
  5. **Failure-mode handling**: the stable `CaseBoxPersistenceError`/`OcrQueueError` code surface + fail-closed behavior (e.g. `not_implemented` FAILS, the tenant/matter guards, the ocr-worker graceful shutdown per gate 5) + the response-projection/forbidden-field boundary; the policy states these are the failure-mode contract.
  6. **Sufficient evidence to clear / partially clear**: the policy DECIDES whether v1's existing deterministic conformance + hardening + impl-parity suites + the audit-chain invariants + the crash-safety properties are sufficient to accept v1 WITHOUT mutation testing (mutation/fuzz deferred post-v1) OR whether a bounded mutation/fuzz sweep is required; PLUS a documented crash-recovery drill (a manual or scripted power-loss/kill-9 + restart + integrity-check that a future lane runs). The clear/partial decision + the residual are recorded; the risk-acceptance is the USER's.
- Dependencies mapped: **gate 6** (full-project cc-suite audit — robustness findings could surface there; gate 7's policy informs the audit's robustness lens); **gate 12** (tamper-evidence / audit-chain integrity — overlaps closure-criterion 4); **gate 14** (backup + recovery procedure — the recovery drill in criterion 2); **gate 15** (rollback procedure tested — the crash-recovery drill overlaps); **gate 5** (all package tests pass — the existing deterministic suites are the baseline evidence). These are noted as related, NOT cleared by this WI.
- Essential refs: `docs/release/go-live-readiness-report.md` §1 gate 7 (+ gates 6/12/14/15/5); `dev-memo/plan-go-live-readiness-00.md` §1 gate 7 (the blueprint's "robustness policy decision"); `docs/product/project-requirements-brief.md` §14 (backup/recovery — Time Machine + schema-integrity-check) + §4 (offline default; crash reporting OFF); `.claude/rules/client-local-first.md` (v1 local-first/offline posture); `.claude/rules/security-boundary.md` §"Go-live independence".
- Review questions: (1) Do the closure criteria correctly frame gate 7 as a v1 local-first robustness policy (crash-safety + recovery + audit-integrity), NOT distributed fault-tolerance? (2) Is offline/online reconciliation correctly marked N/A for v1? (3) Does the WI correctly keep gate 7 NOT unilaterally CLEARED — the risk-acceptance (accept-without-mutation-testing) is surfaced for USER confirmation? (4) Are the gate-6/12/14/15/5 dependencies mapped without clearing them? (5) Is any robustness/mutation-test implementation, gate-6 audit, or go-live decision smuggled in?

Type: EVIDENCE
Scope: Convert gate 7 into a governed robustness-policy DECISION with explicit closure criteria, making NO go-live decision and implementing NO robustness code. The FUTURE execution lane produces `docs/release/gate7-robustness-policy-00.md` — enumerating the v1 robustness properties + existing evidence, stating the six closure criteria above (crash/data-loss tolerance; local-first persistence/recovery; offline/online reconciliation = N/A v1; audit/event integrity under failure; failure-mode handling; sufficient-evidence-to-clear), RECOMMENDING a v1 policy (accept-without-mutation-testing + a documented crash-recovery drill, OR a bounded mutation/fuzz sweep as a separate WI), and SURFACING the risk-acceptance as a USER decision — then updates ONLY the gate-7 evidence row of `docs/release/go-live-readiness-report.md` to reflect the policy authored (gate 7 → OPEN-with-policy / PARTIAL / CLEARED-pending-user-acceptance per the evidence; NOT a unilateral CLEARED). It maps the gate-6/12/14/15/5 dependencies without clearing them; it PRESERVES every still-non-cleared gate + the STOP-AND-ASK gates (4/11/21); it makes NO final GO/NO-GO, resolves NO STOP-AND-ASK, runs NO gate-6 audit, implements NO robustness/mutation/fuzz/crash-injection code, and changes NO source/test/package/config/schema/contract. If the policy recommends a mutation/fuzz sweep or a crash-recovery drill, those are SEPARATE future WIs (not run here). If review asks to run gate 6 or to unilaterally clear gate 7 or decide the risk-acceptance for the user, REJECT and report.
Source of truth: docs/release/go-live-readiness-report.md §1 gate 7 (the OPEN robustness-policy gate) + gates 6/12/14/15/5 (dependencies); dev-memo/plan-go-live-readiness-00.md §1 gate 7 (the blueprint's robustness-policy-decision framing); docs/product/project-requirements-brief.md §14 (backup/recovery: Time Machine over the data dir + schema-integrity check on startup; backup manifest = matter SQLite + audit-log SQLite + blob store) + §4 (fully offline default; crash reporting OFF); .claude/rules/client-local-first.md (v1 = local-first/offline-first Mac desktop; the robustness bar is local-data survival, not distributed FT); .claude/rules/security-boundary.md §"Go-live independence" (a gate policy ≠ go-live); AGENTS.md §"Critical invariants" (audit-chain event_count==COUNT==MAX(sequence); stable OcrQueueError codes; payload_json canonical); .claude/rules/autonomy.md + staging-hygiene.md + loc-guardian.md
Allowed files: docs/release/gate7-robustness-policy-00.md (the NEW policy/decision doc the FUTURE lane produces) + docs/release/go-live-readiness-report.md (the gate-7 evidence ROW ONLY) + dev-memo/run/reviews/ (the exec lane's own review artifact). NOTE: THIS authoring lane commits ONLY the queue governance (queue.md/linted/reviewed/governed) + reviews/queue-review-142.md — NO policy/report change. The future exec lane records its evidence in the COMMIT MESSAGE + review artifact and does NOT re-stage this lane's queue governance.
Forbidden files: docs/release/ OTHER than the NEW gate7-robustness-policy-00.md + the gate-7 row of go-live-readiness-report.md (the other audit/assessment reports are READ-ONLY evidence — NOT edited), docs/adr/ + docs/product/ (READ-ONLY — NO brief/ADR edit), docs/contracts/, apps/ + services/ (NO source/test/renderer/IPC/persistence/contract change — NO mutation-test/fuzz/crash-injection harness), services/*/src/sqlite/schema.ts (CURRENT_SCHEMA_VERSION stays 12), migrations/, native/, package.json/package-lock.json of any package (no dependency — no mutation-testing/fuzzing tool added), dev-memo/run/intake/ (raw samples — never staged), dev-memo/ OTHER than run/reviews/ (no other plan/design edit), scripts/ + .claude/ + .github/; NO robustness/mutation/fuzz/crash-injection implementation; NO gate-6 full-project audit; NO release-doc finalization beyond the one policy doc + the gate-7 row; NO signing/notarization/public-distribution decision; NO 律师法 conclusion; NO final GO/NO-GO decision; NO unilateral gate-7 CLEARED. Push is NEVER automatic (autonomy hard-stop): local commit governance is the default; any branch push + PR open happens ONLY under explicit per-invocation user authorization; NO merge; NO branch delete
Gates: EVIDENCE/policy lane (MEDIUM release-governance risk — a robustness-policy decision doc; no code, no go-live decision, no risk-acceptance made for the user). (1) THIS authoring lane: broker /cc-suite:review-plan on this queue WI, Path 1, retrievable, then govern. (2) The FUTURE exec lane: produce the policy doc + the gate-7 row update, then broker /cc-suite:review-plan on the PRODUCED policy (the docs-policy convention gate, mirroring the gate-3-assessment + readiness-refresh EVIDENCE precedent — a code-diff audit is not applicable to a policy doc). (3) scripts/workflow/check-contract-integrity.sh exit 0; scripts/workflow/check-queue.sh PASS (both lanes). Forbidden-scope scan before commit: CURRENT_SCHEMA_VERSION EQUALS 12; no source/test/package/schema/contract change; no mutation-test/fuzz/crash-injection code; no gate-6 run; no GO/NO-GO; no unilateral gate-7 clear; no raw sample. STOP-points: review/policy asks to run gate 6, unilaterally clear gate 7, decide the risk-acceptance for the user, or implement a mutation/fuzz/crash harness → REJECT/STOP and report; any forbidden file would change → STOP.
Acceptance criteria: check-queue and check-contract-integrity PASS and CURRENT_SCHEMA_VERSION EQUALS 12 and the diff CONTAINS only the new policy doc + the gate-7 row + the review artifact and no source/test/package/schema/contract change appears and no mutation-test/fuzz/crash-injection code appears. The policy doc must RENDER the six closure criteria (crash/data-loss tolerance; local-first persistence/recovery; offline/online reconciliation marked N/A for v1; audit/event integrity under failure; failure-mode handling; sufficient-evidence-to-clear-or-partial), must MAP the gate-6/12/14/15/5 dependencies, must RECOMMEND a v1 policy while SURFACING the accept-without-mutation-testing risk as a USER decision, and MUST NOT unilaterally mark gate 7 CLEARED or assert go-live. The gate-7 evidence row must be updated to reflect the policy authored + point to the doc while keeping gate 7 non-cleared (OPEN-with-policy / PARTIAL / CLEARED-pending-user-acceptance). review-plan (this lane) must RETURN READY.
Risk flags: MEDIUM (release-governance — a robustness policy that could over-claim readiness or make a risk-acceptance the user should own). Care points: (a) NO UNILATERAL CLEAR — gate 7's clear depends on a USER risk-acceptance (accept v1 without mutation testing); the policy RECOMMENDS + surfaces, never decides for the user; (b) V1 SCOPE — the robustness bar is local-first data survival + recovery + audit integrity, NOT distributed fault-tolerance; offline/online reconciliation is N/A v1; (c) NO IMPLEMENTATION — no mutation-test/fuzz/crash-injection harness (that would be a separate WI + a new dependency = hard-stop); (d) DEPENDENCIES NOT CLEARED — gate 6/12/14/15/5 are mapped, not cleared; (e) GO-LIVE INDEPENDENCE — a gate policy does NOT imply go-live; the final verdict + the three STOP-AND-ASK hard-stops remain the user's; (f) AUDIT/BRIEF READ-ONLY — the brief + other release/audit reports are evidence, not edit targets. If any cannot hold, STOP and report.
Depends on: none
Commit boundary: TWO separate lanes. (1) THIS authoring lane commits ONLY the queue governance (queue.md/linted/reviewed/governed) + reviews/queue-review-142.md — no policy/report change, no implementation, no decision. (2) The FUTURE execution lane commits ONLY the new docs/release/gate7-robustness-policy-00.md + the gate-7 evidence-row update to go-live-readiness-report.md + its own review artifact under dev-memo/run/reviews/, records its evidence in the COMMIT MESSAGE, and does NOT re-stage this lane's queue governance. NO source/test/package/schema/contract/mutation-harness file in either lane; NO staging of dev-memo/run/intake/ or untracked residue; push is NEVER automatic (autonomy hard-stop) — local commit governance is the default, any branch push + PR open happens ONLY under explicit per-invocation user authorization; no merge; no branch delete. THIS authoring lane implements NOTHING and decides NOTHING — it only governs the gate-7 robustness-policy WI.
