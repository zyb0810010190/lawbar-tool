## WI-DT1: casebox:deadline:transition IPC handler (main-process + preload)
Type: IMPL
Scope: Add the `casebox:deadline:transition` IPC handler and its main-process/preload surface, mirroring the shipped WI-802 fact-transition seam (`transitionFactHandler`). NO renderer change, NO contract change, NO persistence change. STRUCTURAL NOTE: the renderer bridge (`renderer/api.ts`, `renderer/types.ts`, and the `renderer-dto-sync` PAIR entry) is deliberately deferred to WI-DT3 so this WI touches ZERO `apps/*/renderer/*` paths and therefore does NOT trigger the path-based UI design-artifact gate; `electron/preload.mts` imports the transition DTO from `../src/caseBox/dto.js` (the canonical IPC DTO), not from `renderer/types.ts`, so the preload surface needs no renderer file; the `renderer-dto-sync` test uses a hardcoded PAIRS list, so a canonical src DTO without a renderer counterpart is ignored (test stays green) until WI-DT3 adds the renderer side + PAIR. In `apps/lawbar-desktop/src/caseBox/dto/deadline.ts` add: `DeadlineTransitionTarget = "met"|"missed"|"withdrawn"`, `TransitionDeadlineDto {matterId, deadlineId, to, transition_reason?}`, `TRANSITION_DEADLINE_DTO_FIELDS`, `TRANSITION_DEADLINE_FORBIDDEN_FIELDS` (id, tenant_id, actor_user_id, matter_id, at, status, met_at, previous_status — all server-authority / lifecycle / create-only fields; matter_id forbidden because the renderer uses matterId; transition_reason is the one legit optional input, NOT forbidden), `TRANSITION_DEADLINE_RESPONSE_FIELDS` (same non-authority deadline fields as LIST_DEADLINES_RESPONSE_FIELDS: id, matter_id, kind, source_rule_citation, due_at, owner_user_id, status, met_at, previous_status, transition_reason), `RendererTransitionedDeadlineRow`, `TransitionDeadlineResult`. In `deadlineHandlers.ts` add `transitionDeadlineHandler(payload, provide, now)` mirroring `transitionFactHandler`: shape guard; forbidden-field guard; unknown-field guard; validate matterId/deadlineId non-empty; validate `to` ∈ {met,missed,withdrawn}; capture active tenant + actor + ISO timestamp once; `getMatter` → unknown_matter / tenant_mismatch; SCOPED `getDeadline({tenant_id, matter_id: matterId, deadline_id: deadlineId})` preflight → invalid_payload if null (fail-closed, do NOT call transitionDeadline); EDGE-BOUND reason rule — `transition_reason` is REQUIRED + non-empty IFF the existing deadline status is "missed" AND `to === "met"`, and FORBIDDEN (rejected with invalid_payload, not dropped) for every other transition (derive from the preflight `existing.status`, NOT from `to` alone); call `persistence.transitionDeadline(deadlineId, {to, actor_user_id: actorId, at: atIso, ...(missedToMet ? {transition_reason} : {})})`; project through `TRANSITION_DEADLINE_RESPONSE_FIELDS`; map thrown `illegal_transition` etc. via `mapThrownError(err, {channel: CHANNEL.deadlineTransition})`. Export it from `handlers.ts`. Add `deadlineTransition: "casebox:deadline:transition"` to `CHANNEL` in `handlerShared.ts`. Register it in `electron/ipc/caseBoxHandlers.ts` (import + `ipcMain.handle(CHANNEL.deadlineTransition, ...)` passing the clock fn as the fact handler does). In `electron/preload.mts` add the TransitionDeadlineDto/Result import (from src dto) + the method + `ipcRenderer.invoke("casebox:deadline:transition", dto)`. Add IPC unit tests (incl. a response no-leak test asserting tenant_id/actor_user_id are absent from the projected row). NO renderer/** edit.
Source of truth: apps/lawbar-desktop/src/caseBox/factHandlers.ts (transitionFactHandler precedent, lines 239-323); apps/lawbar-desktop/src/caseBox/dto/fact.ts (TransitionFactDto/FIELDS/FORBIDDEN/RESPONSE precedent, lines 188-253); services/case-box-persistence/src/types.ts (transitionDeadline:173, DeadlineTransitionOpts:372, getDeadline:190, GetDeadlineQuery:241 — READ-ONLY); services/case-box-persistence/src/transitions.ts (allowed deadline edges pending→met/missed/withdrawn + missed→met reason-required — READ-ONLY); apps/lawbar-desktop/electron/ipc/caseBoxHandlers.ts (fact registration:120 + deadline list:99); apps/lawbar-desktop/electron/preload.mts (fact transition:100 + deadline list:95, imports DTO from src/caseBox/dto.js)
Allowed files: apps/lawbar-desktop/src/caseBox/dto/deadline.ts, apps/lawbar-desktop/src/caseBox/deadlineHandlers.ts, apps/lawbar-desktop/src/caseBox/handlers.ts, apps/lawbar-desktop/src/caseBox/handlerShared.ts, apps/lawbar-desktop/electron/ipc/caseBoxHandlers.ts, apps/lawbar-desktop/electron/preload.mts, apps/lawbar-desktop/tests/ipc-casebox-handlers.unit.test.mjs, dev-memo/deferred-audit-findings.md
Forbidden files: services/**, docs/contracts/**, apps/lawbar-desktop/renderer/**, apps/lawbar-desktop/tests/renderer-view-matter.test.mjs, apps/lawbar-desktop/tests/renderer-deadline-urgency.test.mjs, apps/lawbar-desktop/tests/renderer-dto-sync.test.mjs, apps/lawbar-desktop/tests/_view-matter-dom.mjs, .claude/**, scripts/workflow/**, dev-memo/run/**
Gates: npm --prefix apps/lawbar-desktop test
Acceptance criteria: `casebox:deadline:transition` is registered and callable; the handler rejects non-object payloads, unknown fields, and every TRANSITION_DEADLINE_FORBIDDEN_FIELDS member (incl. client-supplied actor_user_id / at / status); validates matterId, deadlineId, and `to` ∈ {met,missed,withdrawn}; performs getMatter + active-tenant check (unknown_matter / tenant_mismatch); performs a scoped getDeadline preflight and fails closed (invalid_payload, no transition) when the deadline is unknown / wrong-matter / wrong-tenant; requires a non-empty transition_reason ONLY for the missed→met edge and rejects transition_reason on any other transition; server-injects actor_user_id + timestamp; surfaces persistence illegal_transition via mapThrownError; projects the response through TRANSITION_DEADLINE_RESPONSE_FIELDS with no tenant_id/actor leak; preload exposes transitionDeadline; `npm --prefix apps/lawbar-desktop test` passes (new IPC tests cover legal transitions, illegal transition, missed→met reason-required, reason-forbidden-otherwise, forbidden/unknown fields, cross-matter/tenant fail-closed, response projection/no-leak, and the persistence-call shape); the WI touches NO apps/*/renderer/* path; NO services/** or docs/contracts/** edit
Risk flags: IPC write surface (security-adjacent) → full cc-suite review-plan→audit→verify required; mirrors shipped WI-802 fact transition; the ONE divergence is the edge-bound (missed→met) reason rule derived from the scoped preflight (not from `to`); persistence/contract are READ-ONLY (forbidden) — transitionDeadline + audit kinds already exist; renderer bridge + dto-sync PAIR intentionally deferred to WI-DT3 so this WI has NO design-gate trigger (temporary: the new canonical TRANSITION_DEADLINE_RESPONSE_FIELDS is verified by DT1's dynamic no-leak IPC test and gains the static dto-sync RESPONSE_ALLOWLISTS check in DT3); client-supplied authority/timestamp MUST be rejected, not dropped
Depends on: none
Commit boundary: one local commit for the deadline-transition IPC handler + preload + IPC tests

## WI-DT2: Design artifact for deadline status-transition controls
Type: ASSET
Scope: Produce the concrete design artifact at `dev-memo/design/2026-06-07-deadline-transition.md` for the deadline status-transition UI that WI-DT3 will implement and that WI-DT3's PR body will cite to satisfy the path-based UI design-artifact gate. The artifact MUST specify: (1) controls to mark a PENDING deadline `met` / `missed` / `withdrawn`, and a MISSED deadline `met`; (2) the `missed → met` required-reason capture flow (a 2-step reason prompt; reason mandatory + non-empty for this edge only); (3) which affordances appear per current status (pending → met/missed/withdrawn; missed → met; met/withdrawn → no transition action, terminal/no-op in v1); (4) error handling that keeps the row + shows an inline error instead of wiping state (the WI-D4 lesson); (5) success refresh behavior; (6) accessibility (keyboard focus, labels/roles, the reason input is a labeled required field); (7) placement within the existing Deadlines section of `viewMatterDeadlines.ts` (alongside existing status text + urgency pill). DOC/ASSET WI: creates ONE markdown file, edits NO source.
Source of truth: apps/lawbar-desktop/renderer/screens/viewMatterDeadlines.ts (existing Deadlines section + status/urgency rendering); apps/lawbar-desktop/renderer/screens/viewMatterDocketProposals.ts (the WI-D4 2-step reason-capture + inline-error precedent to mirror); services/case-box-persistence/src/transitions.ts (allowed deadline edges); dev-memo/plan-batch-casebox-deadline-transition-00.md (proposal)
Allowed files: dev-memo/design/2026-06-07-deadline-transition.md
Forbidden files: apps/**, services/**, docs/contracts/**, .claude/**, scripts/workflow/**, dev-memo/run/**
Gates: test -f dev-memo/design/2026-06-07-deadline-transition.md
Acceptance criteria: `dev-memo/design/2026-06-07-deadline-transition.md` exists and specifies met/missed/withdrawn controls, the missed→met required-reason 2-step capture, per-status affordance visibility, inline-error-keeps-row handling, success-refresh, accessibility, and placement within the existing Deadlines section; NO source/test/contract/electron file is edited; it does not describe reminders, scheduling, docket edit, or any new persistence/contract feature
Risk flags: doc-only ASSET; no code change; this is the design input WI-DT3 implements and cites in its PR body for the path-based design gate; not Type UI (creates the artifact, changes no renderer path) so the queue-lint UI gate does not require a Design artifact field on this block
Depends on: none
Commit boundary: one local commit for the design artifact

## WI-DT3: deadline status-transition renderer UI + bridge
Type: UI
Design artifact: dev-memo/design/2026-06-07-deadline-transition.md
Scope: Land the renderer bridge for `transitionDeadline` and implement the WI-DT2 design in the Deadlines section. Bridge: in `renderer/types.ts` add the renderer `TransitionDeadlineDto` interface + `RENDERER_TRANSITION_DEADLINE_DTO_FIELDS`; in `renderer/api.ts` import them, add `transitionDeadline` to both interfaces, and map via `stripDtoFields(dto, RENDERER_TRANSITION_DEADLINE_DTO_FIELDS)`; in `tests/renderer-dto-sync.test.mjs` add the PAIR `["RENDERER_TRANSITION_DEADLINE_DTO_FIELDS","TRANSITION_DEADLINE_DTO_FIELDS"]` and add `["TRANSITION_DEADLINE_RESPONSE_FIELDS", ["tenant_id","actor_user_id"]]` to RESPONSE_ALLOWLISTS (the static authority-exclusion check the WI-DT1 canonical response allowlist now needs). UI: in `renderer/screens/viewMatterDeadlines.ts` (or an extracted sibling `viewMatterDeadlineTransitions.ts` if loc-guardian risk appears) add the transition affordances per the artifact: a pending deadline → met/missed/withdrawn; a missed deadline → met with a required non-empty reason via a 2-step prompt (mirror the WI-D4 docket-proposal capture). On success, refresh the deadlines view. On error, KEEP the row and show an inline error (do NOT refresh-wipe — the WI-D4 dismiss-error lesson). Call `window.lawbar.caseBox.transitionDeadline`. Update the shared stub `tests/_view-matter-dom.mjs` ONLY if an existing disclosure test requires the new `transitionDeadline` method (add a default stub to keep sibling tests green — the known change-magnet). Add renderer tests. NO src/**, electron/**, or backend edits (the handler + preload exist from WI-DT1).
Source of truth: dev-memo/design/2026-06-07-deadline-transition.md (WI-DT2 artifact); apps/lawbar-desktop/renderer/screens/viewMatterDocketProposals.ts (WI-D4 2-step reason capture + inline-error-keeps-row + re-entrancy precedent); apps/lawbar-desktop/renderer/api.ts (fact-transition strip-mapping precedent, line 143); apps/lawbar-desktop/renderer/types.ts (TransitionFactDto:161 + RENDERER_*_DTO_FIELDS:257); apps/lawbar-desktop/src/caseBox/dto/deadline.ts (canonical TransitionDeadlineDto from WI-DT1 — the sync target)
Allowed files: apps/lawbar-desktop/renderer/types.ts, apps/lawbar-desktop/renderer/api.ts, apps/lawbar-desktop/renderer/screens/viewMatterDeadlines.ts, apps/lawbar-desktop/renderer/screens/viewMatterDeadlineTransitions.ts, apps/lawbar-desktop/tests/renderer-dto-sync.test.mjs, apps/lawbar-desktop/tests/_view-matter-dom.mjs, apps/lawbar-desktop/tests/renderer-view-matter.test.mjs, apps/lawbar-desktop/tests/renderer-deadline-urgency.test.mjs, apps/lawbar-desktop/tests/renderer-deadline-transition.test.mjs, dev-memo/deferred-audit-findings.md
Forbidden files: apps/lawbar-desktop/src/**, apps/lawbar-desktop/electron/**, services/**, docs/contracts/**, .claude/**, scripts/workflow/**, dev-memo/run/**
Gates: npm --prefix apps/lawbar-desktop test
Acceptance criteria: renderer/types.ts + renderer/api.ts expose transitionDeadline and strip renderer-forbidden fields; renderer-dto-sync passes with the new PAIR and the new RESPONSE_ALLOWLISTS entry (TRANSITION_DEADLINE_DTO_FIELDS ↔ renderer set equal; TRANSITION_DEADLINE_RESPONSE_FIELDS excludes tenant_id/actor_user_id); the Deadlines section shows transition affordances exactly per the WI-DT2 artifact (pending → met/missed/withdrawn; missed → met); the missed→met action captures a required non-empty reason via a 2-step prompt and refuses an empty reason; a successful transition refreshes the view; a failed transition keeps the row and shows an inline error WITHOUT wiping state; if the shared stub is updated it keeps all sibling renderer tests green; new renderer tests cover the success path, missed→met reason-required, the empty-reason refusal, and the error-keeps-row path; loc-guardian stays under the fail threshold (extract the sibling helper if needed); `npm --prefix apps/lawbar-desktop test` passes; NO src/**, electron/**, services/**, or docs/contracts/** edit
Risk flags: Type UI → path-based design-artifact gate enforced (Design artifact cited above; produced by WI-DT2 earlier in the batch); shared-stub `_view-matter-dom.mjs` is a change-magnet (update only if a disclosure test requires it, keep siblings green); error-path must not refresh-wipe (WI-D4 lesson); loc-guardian risk on viewMatterDeadlines.ts (437 pure LOC; extract a sibling if the addition approaches the cap); cc-suite audit→verify required
Depends on: WI-DT1, WI-DT2
Commit boundary: one local commit for the deadline-transition renderer UI + bridge + tests
