## WI-A3-LINK-CREATE-T1: audited createLink persistence operation (A0.7-gated, custody 9b)
Type: IMPL
Requires-A07: yes
A0.7-classification: WI-A3-LINK-CREATE-T1 implements the createLink OPERATION that inserts court-facing evidence link rows + emits a tamper-evident LINK_CREATED audit event over legal-record link state. It changes legal-record presentation + evidence-link lifecycle, so it is A0.7-gated (the same A3 evidence-link surface WI-A3-UNLINK-T1 / WI-A3-T5-RESOLVE / WI-A3-EXPORT-T1 gated under custody 9b). It carries `Requires-A07: yes` and runs under A07-KEY-00 custody mode 9b: the human mints the local marker + runs the A0.7-gated `check-gates.sh` with the HMAC key; the agent never receives the key; the implementation commit is blocked until the human reports gated PASS. If review-plan says this WI must NOT be A0.7-gated, STOP and report.
Scope: Implement the minimal headless audited createLink OPERATION — the emitter of the LINK_CREATED kind shipped in PR #139, per the merged link-create ADR (docs/adr/ADR-evidence-a3-link-create-operation.md, D1-D10). Add a method to the CONCRETE SqliteCaseBoxPersistence class: createLink({ tenant_id, matter_id, source_type, source_id, anchor_id, actor_user_id }) inserts ONE durable case_box_links row (generated ULID id; provisional status `needs_review`; created_at = single nowIso() stamp; unlinked_at NULL + unlink_reason NULL; deterministic payload_json of the link identity) AND appends EXACTLY ONE tamper-evident v2 LINK_CREATED audit event (action create, entity_type link, before_state_hash null, after_state_hash over the persisted state excluding the resolver-derived status, timestamp = the same stamp, no reason) in the SAME BEGIN IMMEDIATE transaction. SQLite-only (links are an A3 SQLite-only feature; InMemoryCaseBoxPersistence has NO link support, so the method goes on the concrete class, NOT the shared CaseBoxPersistence interface — InMemory untouched). The insert + audit append are atomic: no link row without a chain event, no chain event without a link row.
Required decisions to record: (1) SURFACE — a method on the CONCRETE SqliteCaseBoxPersistence only, `#runImmediateWrite((db, deps) => applyCreateLinkSqlite(...))`, backed by `prepareCreateLink` + `insertLinkRow` + `applyCreateLinkSqlite` in linkRepoQueries.ts (extend the existing unlink/relink file), reusing linkStateForHash/entityStateHash/priorHeadOf/eventHashFn/buildCaseBoxAuditEvent. If review-plan prefers different, record/STOP. (2) CREATE-EVENT HASH — before_state_hash null (create); after_state_hash = entityStateHash(linkStateForHash(newRow)) EXCLUDING the resolver-derived status (the WI-A3-UNLINK-T1 convention); single stamp shared by created_at + the event timestamp. (3) INITIAL STATE — status provisional `needs_review` (A3-RESOLVE: valid is never a default; the caller runs resolveLinkStatuses afterward); unlinked_at NULL + unlink_reason NULL (active); deterministic payload_json = JSON of { id, tenant_id, matter_id, source_type, source_id, anchor_id, created_at }. (4) VALIDATION — boundary-validate (before any write): input object; tenant_id/matter_id/source_id/anchor_id/actor_user_id non-empty strings; source_type in the 5-enum (evidence|note|question|calcTerm|claimElement) -> invalid_argument; matter exists + tenant match (requireMatterTenant) -> unknown_matter/tenant_mismatch; anchor exists in tenant+matter (SELECT 1 FROM case_box_anchors) -> invalid_argument (missing anchor). (5) EVIDENCE EXISTENCE — ADR-EXTENSION: the user's T1 authorization requires "missing evidence is rejected" + "tenant/matter/evidence/anchor boundaries enforced", which extends ADR D4 (it required only source_type enum + non-empty source_id, NOT source existence). This WI adds: for source_type='evidence', the case_box_evidence_items row (source_id) MUST exist in the same tenant+matter (SELECT 1) -> else invalid_argument (missing evidence). The other source_types (note/question/calcTerm/claimElement) have NO backing table, so only a non-empty source_id is validated (a recorded known gap). FLAG to review-plan: confirm implementing this evidence-existence check now (extending ADR D4) is acceptable, or STOP for an ADR amendment; confirm the non-evidence-source handling. (6) DUPLICATE — a generated-id collision -> duplicate_id (deterministic rejection; ULID makes it rare); NO logical-duplicate (source+anchor) uniqueness (the V11 schema has none). (7) ATOMIC — one BEGIN IMMEDIATE: build the event, INSERT the row, append the event; exactly one event per create; rejections leave NO row and NO event (validated before any write). (8) INTEGRATION (read-through, no behavior change) — after createLink + resolveLinkStatuses a structurally valid link resolves valid + exports a clean citation; the created link can subsequently be unlinkLink/relinkLink'd through the live operations. (9) NO SCHEMA/CONTRACT/UI — no schema/CURRENT_SCHEMA_VERSION change, no docs/contracts change (LINK_CREATED shipped #139), no resolver/export/UI/IPC/export-rendering change, no link DELETE, no InMemory/shared-interface change, no dependency. (10) A0.7 — A0.7-gated, custody 9b (Requires-A07: yes).
Source of truth: docs/adr/ADR-evidence-a3-link-create-operation.md (D1-D10 — the create design + the required tuple + sequencing); docs/adr/audit-event-kind-preservation.md (the kind tuple the chain verifier enforces); docs/contracts/case-box-contract/src/audit-log.ts (READ-ONLY — buildCaseBoxAuditEvent + the LINK_CREATED kind shipped #139); services/case-box-persistence/src/sqlite/linkRepoQueries.ts + SqliteCaseBoxPersistence.ts + auditChain.ts (READ-ONLY pattern — the unlink/relink applyXxxSqlite + #runImmediateWrite + entityStateHash/priorHeadOf/eventHashFn shape to mirror); services/case-box-persistence/src/sqlite/{linkStatusResolverQueries,exportCitationQueries}.ts (READ-ONLY — the read paths the created link feeds); services/case-box-persistence/src/sqlite/schema.ts (READ-ONLY — case_box_links V11/V12 + case_box_anchors + case_box_evidence_items columns; NOT changed); dev-memo/plan-batch-casebox-evidence-a3-link-create-t1-00.md (this plan); .claude/rules/{autonomy,cc-suite,security-boundary,evidence-genie,loc-guardian}.md (user-authorized lane; HIGH-RISK persistence + audit security; broker required; A0.7 invariants).
Allowed files: services/case-box-persistence/src/sqlite/linkRepoQueries.ts (extend — add CreateLinkInput + prepareCreateLink + insertLinkRow + applyCreateLinkSqlite; reuse the existing helpers), services/case-box-persistence/src/sqlite/SqliteCaseBoxPersistence.ts (add the createLink method + import; concrete class only, NOT the shared interface), services/case-box-persistence/src/index.ts (export the new CreateLinkInput type only), services/case-box-persistence/tests/hardening-link-status-resolver.test.mjs (EXTEND the already-wired file — add the createLink operation tests; do NOT add a new test file, do NOT touch package.json; the WI-A3-UNLINK-T1 lesson), dev-memo/plan-batch-casebox-evidence-a3-link-create-t1-00.md (this plan), dev-memo/run/queue.md, dev-memo/run/queue.linted, dev-memo/run/queue.reviewed, dev-memo/run/queue.governed, dev-memo/run/reviews/queue-review-092.md, dev-memo/study/** (a batch-audit study packet if a window closes), dev-memo/cc-suite-reliability-log.md (only if a TIMEOUT retry occurs), dev-memo/deferred-audit-findings.md (only if a finding is deferred)
Forbidden files: services/case-box-persistence/src/sqlite/schema.ts (NO SQLite schema/version change — CURRENT_SCHEMA_VERSION stays 12; NO CHECK/FK/cascade/index), services/case-box-persistence/src/sqlite/{linkStatusResolverQueries,exportCitationQueries,anchorDeleteGuardQueries}.ts (NO resolver/export/guard BEHAVIOR change — integration tested read-only), services/case-box-persistence/src/inMemoryRepo.ts + the shared CaseBoxPersistence interface (NO InMemory/interface change — links are SQLite-only; STOP if it appears necessary), services/case-box-persistence/src/errors.ts (NO new error code — invalid_argument/unknown_matter/tenant_mismatch/duplicate_id/invalid_payload already exist), docs/contracts/** (the LINK_CREATED kind already shipped in PR #139 — NO contract change; STOP and ask if a consumer assertion strictly requires one), docs/contracts/case-box-contract/src/generated/** (NO regenerate), apps/**, native/**, .claude/**, .github/**, AGENTS.md, CLAUDE.md, GEMINI.md, scripts/workflow/**, scripts/gen-types.mjs, migrations/**, package-lock.json / any dependency manifest, services/case-box-persistence/package.json, committed dev-memo/run/evidence/** marker/ledger/key/evidence-run files, committed keys, dev-memo/run/reports/** (untracked local run-state — never staged), dev-memo/run/override-reason.md (untracked residue — never staged), Evidence-Genie-M0-Developer-Handover.md (root intake — READ-ONLY, never moved/deleted), xiaolai-dev-workflow-study.md (root intake — READ-ONLY, never moved/deleted); NO link DELETE path, NO row deletion, NO schema/version change, NO resolver/export/UI/IPC/export-rendering/canonicalization/verifier change, NO contract change, NO emitter beyond LINK_CREATED, NO dependency, NO marker/key/gate change, NO new test file, NO InMemory/shared-interface change
Gates: broker /cc-suite:review-plan REQUIRED before implementation (confirm A0.7-gated custody-9b classification; confirm the EVIDENCE-EXISTENCE ADR-EXTENSION — implementing the "missing evidence rejected" check now (extending ADR D4) is acceptable, or STOP for an ADR amendment; confirm the non-evidence-source handling; confirm the concrete-class surface; confirm before_state_hash null + after_state_hash excluding status; confirm provisional needs_review + deterministic payload_json + single stamp; confirm the validation set + duplicate-id rejection; confirm the atomic one-event-per-create; confirm CONTRACT-ONLY consumption — no contract edit, no schema/version, no resolver/export/UI change; confirm no row deletion); if review-plan returns NEEDS-FIX, STOP and report; if review-plan says this must NOT be A0.7-gated, STOP and report; if review-plan demands a schema change / a contract change / an ADR amendment / a canonicalization or verifier change / an InMemory or shared-interface change / a new dependency, STOP and ask; A0.7-gated check-gates.sh -> GATES OK under custody mode 9b (the human mints the marker + runs check-gates.sh with the HMAC key; the agent never receives the key; the agent's own check-gates.sh fails closed exit 2 without the key — expected); broker /cc-suite:audit + /cc-suite:verify on the implementation diff after authoring (no Critical/High/Medium open before commit); npm --prefix services/case-box-persistence test passes (the new createLink suite + all existing suites incl. unlink/relink + resolver/export green); npm --prefix docs/contracts/case-box-contract test passes (consumer unchanged); scripts/workflow/check-contract-integrity.sh exits 0; loc-guardian: no hand-written source file over the 800 fail threshold (linkRepoQueries.ts ~269 + the create op well under; SqliteCaseBoxPersistence.ts ~626 + 1 method under; the test file under the 1200 test fail threshold); no schema/contract/dependency/InMemory change; no committed marker/ledger/key/evidence-run; root intake untouched
Acceptance criteria: createLink({ tenant_id, matter_id, source_type, source_id, anchor_id, actor_user_id }) inserts EXACTLY ONE case_box_links row (generated ULID id; status == 'needs_review'; unlinked_at IS NULL; unlink_reason IS NULL; created_at == the single stamp; deterministic payload_json) and appends EXACTLY ONE LINK_CREATED event (action create, entity_type link, entity_id == new id, before_state_hash null, correct after_state_hash, timestamp == created_at, no reason) atomically (event_count == COUNT(*) == MAX(sequence)); unknown matter -> unknown_matter, tenant mismatch -> tenant_mismatch, bad source_type / empty source_id / empty actor_user_id -> invalid_argument, missing anchor (tenant+matter) -> invalid_argument, missing evidence for source_type='evidence' (tenant+matter) -> invalid_argument, a generated-id collision -> duplicate_id — EACH leaving NO row and NO event; after createLink + resolveLinkStatuses a structurally valid link resolves 'valid' and buildExportCitations returns a clean citation; the created link can subsequently be unlinkLink'd (-> broken/UNLINKED) and relinkLink'd; legacy unlink/relink + resolver/export tests stay green; no schema version bump; no link row deleted; fixtures synthetic-only (no private evidence content); the committed diff is confined to the Allowed files; npm --prefix services/case-box-persistence test passes; npm --prefix docs/contracts/case-box-contract test passes; scripts/workflow/check-contract-integrity.sh exits 0; A0.7-gated check-gates.sh returns GATES OK (human custody 9b); broker review-plan READY + audit no Critical/High/Medium + verify ALL CLOSED
Risk flags: IMPL — a persistence CREATE over court-facing evidence link state that appends to the tamper-evident audit chain — HIGH-RISK (persistence + audit-chain security surface; broker review-plan + audit + verify REQUIRED) and A0.7-gated (custody 9b; Requires-A07: yes — court-facing legal-record creation). The insert + audit append MUST be atomic (one BEGIN IMMEDIATE; exactly one LINK_CREATED event per create; no row without event, no event without row). NOTE the EVIDENCE-EXISTENCE ADR-EXTENSION (decision 5) — flagged to review-plan; if review-plan requires an ADR amendment first, STOP. MUST NOT weaken audit-chain tamper-evidence (uses the existing canonicalization + buildCaseBoxAuditEvent + eventHashFn; the verifier enforces the kind tuple), MUST NOT change schema/version, MUST NOT change resolver/export/UI behavior, MUST NOT change the contract, MUST NOT touch InMemory or the shared interface, MUST NOT introduce a dependency, MUST NOT weaken the A3-DB-00 encryption hard stop or any Evidence invariant. SQLite-only. No push, no merge.
Depends on: none
Commit boundary: governance commit separate (dev-memo/run/queue.md + queue.linted/reviewed/governed + dev-memo/run/reviews/queue-review-092.md); ONE implementation commit -- services/case-box-persistence/src/sqlite/linkRepoQueries.ts + services/case-box-persistence/src/sqlite/SqliteCaseBoxPersistence.ts + services/case-box-persistence/src/index.ts + services/case-box-persistence/tests/hardening-link-status-resolver.test.mjs (EXTEND, not new) (+ dev-memo/plan-batch-casebox-evidence-a3-link-create-t1-00.md), staged by exact path; the human-gated A0.7 marker/key/evidence files are NEVER staged; if the batch guard requires a closeout before the implementation commit, run the Layer-B closeout first; no push; no merge
