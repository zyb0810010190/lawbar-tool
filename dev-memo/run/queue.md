## WI-A3-LINK-D1-ROUNDTRIP-T1: close LINK-IPC-T1-D1 with a bounded real-db Electron link round-trip (Type: TEST; A0.7 commit-gated)
Type: TEST
Requires-A07: yes
Design artifact: docs/adr/ADR-evidence-a3-link-d1-roundtrip-closure.md
Source design: docs/adr/ADR-evidence-a3-link-d1-roundtrip-closure.md (D1-ROUNDTRIP-00, rev-1 §0/§11/§12/§13/§14)
Scope: Implement the rev-1 ADR closure path for LINK-IPC-T1-D1 — a real-db packaged-Electron integration test that drives the `casebox:link:*` round-trip, seeded by a DEFAULT-OFF, env-gated `globalThis` test hook in `electron/main.ts`. (Now unblocked: WI-A3-INTERNAL-DEPS-COMMIT-TARBALLS-T2 made clean/packaged builds bundle the current `case-box-contract` with the LINK_* audit kinds, so `createLink`'s audit builder accepts `LINK_CREATED`.) Deliver: (1) a bounded hook in `apps/lawbar-desktop/electron/main.ts` mirroring the tarball-PoC pattern (`electron/main.ts` `LAWBAR_TARBALL_POC_TEST_HOOK`): `if (process.env.LAWBAR_CASEBOX_LINK_SEED_TEST_HOOK === "true") { const { seedLinkRoundtripFixture } = await import("../src/caseBox/testSeed/linkRoundtripSeed.js"); globalThis.__lawbarCaseBoxLinkSeed = seedLinkRoundtripFixture; }` — `import()` in main's NATIVE ESM loader; production launches (env unset) install nothing; (2) a TEST-ONLY seed module `apps/lawbar-desktop/src/caseBox/testSeed/linkRoundtripSeed.ts` taking a FIXED `{matterId, documentId}` input, obtaining main's ALREADY-OPEN better-sqlite3 handle via `getCaseBoxRuntime().db` (singleton; materialized by the prior createMatter IPC — NO 2nd connection, NO arbitrary DB path), reading the matter's main-injected `tenant_id`, and INSERTing FIXED synthetic rows (NO raw SQL passthrough, NO caller payload) into `case_box_document_pages` (payload `{citationVolume,citationPageLabel,isCitable:true}`), `case_box_document_page_geometries` (`captured_at`=X), `case_box_anchors` (`geometry_captured_at`=X equal to the geometry; `coordinate_space='page_ratio'`, `origin_ref='DocumentPageGeometry'`), `case_box_evidence_items` — all scoped to the matter's tenant + a SYNTHETIC `documentId` (registerDocument is interactive/headless-blocking; createLink/resolver/export need no case_box_documents row), returning `{anchorId, evidenceId, citationVolume, citationPageLabel}`; (3) a new packaged-Electron test `apps/lawbar-desktop/tests/casebox-link-roundtrip.electron.test.mjs` (mirrors `casebox-ipc.electron.test.mjs`: `launchPackaged` + `--user-data-dir=<temp>` + sentinel) launching with `LAWBAR_CASEBOX_LINK_SEED_TEST_HOOK=true`, creating a matter via REAL `window.lawbar.caseBox.createMatter`, invoking the seed via `app.evaluate((_e, input) => globalThis.__lawbarCaseBoxLinkSeed(input), {matterId, documentId})`, then driving the REAL round-trip `createLink → listLinks → exportLinkCitations` (+ `unlinkLink`/`relinkLink`) and asserting the CLEAN path + negative + containment cases (below); (4) a containment guard test (same file) asserting a launch WITHOUT the env var leaves `globalThis.__lawbarCaseBoxLinkSeed` undefined AND the seed is not exposed via preload/`window.lawbar`/IPC; (5) additive `apps/lawbar-desktop/package.json` registration (`test:link-roundtrip-packaged` mirroring `test:ipc-packaged`). Flip LINK-IPC-T1-D1 `Status` to `closed` in `dev-memo/deferred-audit-findings.md` ONLY AFTER the packaged Electron round-trip passes. NO product `casebox:anchor:*` IPC, NO product seed IPC, NO preload/contextBridge/renderer exposure of the seed, NO persistence/schema/contract/dependency change, NO packaging/tarball/lock/drift-guard change.
Source of truth: docs/adr/ADR-evidence-a3-link-d1-roundtrip-closure.md (rev-1 §11 containment, §12 test plan, §13 closure criteria); apps/lawbar-desktop/electron/main.ts (the tarball-PoC env-gated globalThis-hook precedent to mirror); apps/lawbar-desktop/src/caseBox/caseBoxRuntime.ts (`getCaseBoxRuntime().db` singleton handle); apps/lawbar-desktop/tests/casebox-ipc.electron.test.mjs + tests/_launch-with-pid-log.mjs + tests/smoke.electron.test.mjs (launchPackaged + window/app.evaluate harness); apps/lawbar-desktop/electron/preload.mts (READ-ONLY — the `window.lawbar.caseBox.*` methods); services/case-box-persistence/src/sqlite/{schema,linkStatusResolverQueries,exportCitationQueries,linkRepoQueries}.ts (READ-ONLY — DDL; resolver valid ladder; readCitationIdentity 卷X页Y; createLink checks); services/case-box-persistence/tests/hardening-link-status-resolver.test.mjs (the insertPage/insertGeom/insertAnchor/opEvidence INSERT shapes); apps/lawbar-desktop/scripts/test-packaged-wrapper.mjs (LAWBAR_TEST_FILES); dev-memo/deferred-audit-findings.md (LINK-IPC-T1-D1); .claude/rules/{cc-suite,autonomy,execution-discipline,security-boundary,evidence-genie,staging-hygiene}.md
Allowed files: apps/lawbar-desktop/electron/main.ts, apps/lawbar-desktop/src/caseBox/testSeed/linkRoundtripSeed.ts, apps/lawbar-desktop/tests/casebox-link-roundtrip.electron.test.mjs, apps/lawbar-desktop/package.json, dev-memo/deferred-audit-findings.md, dev-memo/run/queue.md, dev-memo/run/queue.linted, dev-memo/run/queue.reviewed, dev-memo/run/queue.governed, dev-memo/run/reviews/queue-review-103.md
Forbidden files: apps/lawbar-desktop/src/caseBox/** EXCEPT the new src/caseBox/testSeed/linkRoundtripSeed.ts (NO change to caseBox DTOs/handlers/runtime beyond the test-only seed; getCaseBoxRuntime consumed READ-ONLY), apps/lawbar-desktop/electron/** EXCEPT the bounded hook block in main.ts (NO preload/contextBridge change; NO IPC channel), apps/lawbar-desktop/renderer/** (NO renderer/UI change), apps/lawbar-desktop/tests/_*.mjs + scripts/** (READ-ONLY — reuse launcher + wrapper; NO edit; NO drift-guard edit), apps/lawbar-desktop/dist-tarballs/** + package-lock.json (NO packaging/tarball/lock change — the T2 fix is settled), services/** (NO persistence change), services/case-box-persistence/src/sqlite/schema.ts (NO schema/`CURRENT_SCHEMA_VERSION` change), docs/contracts/** (NO contract change), docs/adr/** (NO ADR edit), native/**, .claude/**, .github/**, AGENTS.md, CLAUDE.md, GEMINI.md, migrations/**, committed dev-memo/run/evidence/** marker/ledger/key/evidence-run files, committed keys, dev-memo/run/reports/** (untracked run-state — never staged), dev-memo/run/override-reason.md (untracked residue — never staged), Evidence-Genie-M0-Developer-Handover.md + xiaolai-dev-workflow-study.md (root intake — READ-ONLY); NO product `casebox:anchor:*` IPC, NO product/dev-conditional seed IPC, NO preload/contextBridge/renderer-reachable seed path, NO raw-SQL/arbitrary-path/arbitrary-payload hook, NO persistence/schema/contract/dependency/packaging change, NO T2-AUD-L1 work, NO push/merge/branch-delete
Gates: broker /cc-suite:review-plan REQUIRED before implementation — confirm: the hook is default-off/fail-closed/env-gated, native-loader import, NO IPC/preload/contextBridge/renderer/UI exposure, NO raw-SQL/arbitrary-path/arbitrary-payload (only fixed synthetic rows + scope ids); the seed reuses main's open handle (no 2nd connection); the seed set yields resolver `valid` + CLEAN export; the containment guard test is adequate; NO product anchor IPC needed (stop-point 2); NO persistence/schema/contract/dependency/packaging change needed (stop-points 4/5); A0.7 custody-9b IS required (edits electron/main.ts). If review-plan NEEDS-FIX, fix-forward; if a true blocker (anchor IPC / arbitrary-exec hook / persistence change / packaging change), STOP and re-scope. After implementation: `npm --prefix apps/lawbar-desktop run build` (typecheck) GREEN; `npm --prefix apps/lawbar-desktop run dist` succeeds; `LAWBAR_TEST_FILES=tests/casebox-link-roundtrip.electron.test.mjs npm --prefix apps/lawbar-desktop run test:link-roundtrip-packaged` GREEN (the round-trip reaches `valid`/CLEAN); `npm --prefix apps/lawbar-desktop test` GREEN; `node scripts/workflow/check-internal-tarballs.mjs` still PASSES (packaging untouched); `scripts/workflow/check-contract-integrity.sh` exits 0; broker /cc-suite:audit + /cc-suite:verify on the impl diff (no Critical/High/Medium open before commit); A0.7 (custody 9b) — human mints the marker + runs `A07_REQUIRED=1 check-a07-gate.sh` + `check-gates.sh` (must end GATES OK) BEFORE the impl commit; the agent never receives/echoes/persists the HMAC key and stages NO marker/evidence/key/report file. If `npm run dist` or the packaged harness cannot run in this environment, STOP and report (D1 stays open; stop-point 8).
Acceptance criteria: `electron/main.ts` installs the env-gated `globalThis.__lawbarCaseBoxLinkSeed` hook ONLY when `LAWBAR_CASEBOX_LINK_SEED_TEST_HOOK==="true"`; `src/caseBox/testSeed/linkRoundtripSeed.ts` seeds fixed synthetic anchor/page/geometry/evidence rows via `getCaseBoxRuntime().db` with no raw SQL/arbitrary path/arbitrary payload; `tests/casebox-link-roundtrip.electron.test.mjs` PASSES (packaged) with: createMatter ok; seed returns {anchorId, evidenceId}; `createLink` ok; `listLinks` resolver status === `valid`; `exportLinkCitations` citation `exportFlag === null`, `byFlag.CLEAN === 1`, citationVolume/citationPageLabel match the seed, synthesized `citation.text === "卷<vol>页<label>"`; unlink→relink coherent (relink clears unlinked_at); a negative scope case fails safely (`invalid_argument`/`unknown_matter`/`tenant_mismatch`); a containment case proves a no-env-var launch leaves the hook undefined + not on `window.lawbar`; NO raw evidence content in any assertion/log/snapshot (synthetic only); the test registered additively in `package.json`; `npm --prefix apps/lawbar-desktop test` GREEN; drift guard still PASSES; `check-contract-integrity` exits 0; `CURRENT_SCHEMA_VERSION` remains 12; NO renderer/preload/IPC-channel/services/schema/contract/package-lock/dependency/packaging change; dev-memo/deferred-audit-findings.md LINK-IPC-T1-D1 flipped to `Status | closed` citing this WI's commit (ONLY after the packaged test passes); broker review-plan READY + audit no Critical/High/Medium + verify ALL CLOSED; A0.7 custody-9b gated PASS recorded by the human (GATES OK; key unset; no marker/evidence/key/report staged); committed diff confined to Allowed files.
Risk flags: TEST lane adding a SMALL PRODUCT change (env-gated hook in electron/main.ts + a test-only seed module) over the COURT-FACING audited-link surface — A0.7 COMMIT-GATED (custody 9b). Load-bearing care points: (a) the hook MUST be default-off/fail-closed, native-loader import, NO IPC channel, NO preload/contextBridge, NOT renderer-reachable, NO UI path — proven by the containment test; (b) the seed MUST accept NO raw SQL, NO arbitrary DB path (reuses getCaseBoxRuntime().db), NO arbitrary fixture payload (fixed synthetic rows; only scope ids) — cannot become a general DB-write primitive; (c) NO raw evidence content anywhere; (d) NO product anchor IPC / seed IPC; (e) NO Evidence invariant weakened (synthetic anchor is test data; no Evidence UI added); (f) NO persistence/schema/contract/dependency/PACKAGING change (the T2 packaging fix is settled — do NOT touch tarballs/lock/guard). Heavy-op risk: requires `npm run dist` + packaged Electron run; if packaging cannot run reliably here, STOP (D1 stays open). If closing D1 needs product anchor IPC, an arbitrary-exec hook, or a persistence/schema/packaging change, STOP and re-scope. No push/merge/branch-delete.
Depends on: none
Commit boundary: governance (mark-reviewed + govern) run STANDALONE before the commit (never bundled with git commit); A0.7 custody-9b gated PASS reported by the human BEFORE the impl commit. ONE impl commit — electron/main.ts (hook) + src/caseBox/testSeed/linkRoundtripSeed.ts + tests/casebox-link-roundtrip.electron.test.mjs + package.json (additive) + dev-memo/deferred-audit-findings.md (D1 close, only after the packaged test passes) + governance artifacts (queue.md/.linted/.reviewed/.governed + reviews/queue-review-103.md), staged by exact path; NO renderer/preload/IPC-channel/services/schema/contract/package-lock/dependency/tarball/drift-guard file; NO marker/evidence/key/report file; no push; no merge.
