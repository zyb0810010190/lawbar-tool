## WI-FF1: DTO-sync response-allowlist coverage + forbidden-field table-drives (close CBW-602)
Type: TEST
Scope: TEST-ONLY completion of the corrected, narrowed BS2 work. NO production code, NO behavior change — the IPC boundary is already enforced (BS1 shipped); this widens TEST coverage of the unchanged guards. (1) In `apps/lawbar-desktop/tests/renderer-dto-sync.test.mjs` add the 4 currently-missing canonical `*_RESPONSE_FIELDS` to the RESPONSE_ALLOWLISTS authority-exclusion check: `CREATE_FACT_RESPONSE_FIELDS` excludes [tenant_id, actor_user_id, reviewer_actor_user_id]; `TRANSITION_FACT_RESPONSE_FIELDS` excludes [tenant_id, actor_user_id, reviewer_actor_user_id]; `DOCKET_ENTRY_RESPONSE_FIELDS` excludes [tenant_id, confirmation_actor_user_id, dismissal_actor_user_id] (plus the proposer actor-identity field the docket entity carries, if present — verify the exact name in dto/docket.ts and assert it excluded); `CONFIRM_DOCKET_DEADLINE_RESPONSE_FIELDS` excludes [tenant_id, actor_user_id]. PRESERVE all existing authority-exclusion entries; do NOT weaken/skip any check; do NOT accept an authority field in any allowlist. (2) In `apps/lawbar-desktop/tests/ipc-casebox-handlers.unit.test.mjs` table-drive the forbidden-field tests for `createDocket` (currently single-sample tenant_id, :703), `createFact` (single-sample actor_user_id, :904), and `confirmDocket` (currently NO forbidden test) over their canonical `*_FORBIDDEN_FIELDS` arrays (import CREATE_DOCKET_FORBIDDEN_FIELDS / CREATE_FACT_FORBIDDEN_FIELDS / CONFIRM_DOCKET_FORBIDDEN_FIELDS from the dist dto), mirroring the existing compact dismissDocket loop pattern (:494): for each forbidden field, call the handler with `{ ...validDto, [f]: <value> }` and assert ok:false + error.code "invalid_payload" + error.details.schemaPath == f. Reuse the existing scaffolding (makeDocketProvider/validCreateDto for createDocket; makeFactProvider/validFactDto for createFact; for confirmDocket a provider whose getDocketEntry returns a proposed entry + a valid confirm DTO {matterId, entryId}). Replace the createDocket + createFact single-sample tests with the loops (net-neutral); confirmDocket adds one loop. (3) In `apps/lawbar-desktop/tests/ipc-fact-handlers.unit.test.mjs` replace the two single-sample transitionFact forbidden tests (reviewer_actor_user_id :191, status :203) with ONE loop over TRANSITION_FACT_FORBIDDEN_FIELDS (each field → invalid_payload + schemaPath; keep the calls.transition == 0 not-called assertion where applicable). (4) In `dev-memo/deferred-audit-findings.md` close CBW-602-FORBIDDEN-TESTS iff the createFact table-drive fully satisfies its recorded target (table-driven loop over CREATE_FACT_FORBIDDEN_FIELDS); otherwise update with partial progress and leave open. Do NOT weaken any existing assertion. If `ipc-casebox-handlers.unit.test.mjs` (1140 raw / cap 1200) approaches ~1185 raw or loc-guardian warns, STOP and report a narrower split rather than bloating it (a new test file would need a governed amendment + a package.json test-script entry — do NOT do that silently).
Source of truth: apps/lawbar-desktop/tests/ipc-casebox-handlers.unit.test.mjs (the existing dismissDocket table-driven forbidden loop :494 + the WI-CL1 transitionDeadline table-drive :1050 as the compact precedent; makeDocketProvider/makeFactProvider/validCreateDto/validFactDto/PROPOSED_ENTRY/clock/idFactory scaffolding); apps/lawbar-desktop/tests/ipc-fact-handlers.unit.test.mjs (makeFactProvider + the 2 single transitionFact forbidden tests to replace); apps/lawbar-desktop/src/caseBox/dto/{docket,fact}.ts (the *_FORBIDDEN_FIELDS + *_RESPONSE_FIELDS arrays — READ-ONLY); dev-memo/deferred-audit-findings.md (CBW-602-FORBIDDEN-TESTS row); dev-memo/plan-batch-casebox-forbidden-field-tests-00.md (proposal)
Allowed files: apps/lawbar-desktop/tests/renderer-dto-sync.test.mjs, apps/lawbar-desktop/tests/ipc-casebox-handlers.unit.test.mjs, apps/lawbar-desktop/tests/ipc-fact-handlers.unit.test.mjs, dev-memo/deferred-audit-findings.md
Forbidden files: apps/lawbar-desktop/src/**, apps/lawbar-desktop/renderer/**, apps/lawbar-desktop/electron/**, services/**, docs/contracts/**, .claude/**, scripts/workflow/**, dev-memo/run/**, apps/lawbar-desktop/tests/ipc-list-projection.unit.test.mjs, apps/lawbar-desktop/tests/_view-matter-dom.mjs, apps/lawbar-desktop/tests/ipc-handlers.unit.test.mjs, apps/lawbar-desktop/tests/casebox-ipc.electron.test.mjs, apps/lawbar-desktop/package.json
Gates: npm --prefix apps/lawbar-desktop test
Acceptance criteria: renderer-dto-sync.test.mjs asserts authority-exclusion for ALL 12 canonical `*_RESPONSE_FIELDS` (the 4 added: CREATE_FACT/TRANSITION_FACT/DOCKET_ENTRY/CONFIRM_DOCKET_DEADLINE) with no existing entry weakened; createDocket/createFact/confirmDocket forbidden-field tests are table-driven over their full *_FORBIDDEN_FIELDS arrays (each field → invalid_payload + schemaPath); transitionFact forbidden test is table-driven over all TRANSITION_FACT_FORBIDDEN_FIELDS; no existing assertion weakened; `npm --prefix apps/lawbar-desktop test` passes; ipc-casebox-handlers.unit.test.mjs stays under 1200 raw (STOP+report if approaching ~1185); CBW-602-FORBIDDEN-TESTS closed-if-satisfied or updated-with-partial-progress; NO production/renderer/electron/services/contracts/workflow change; no file outside Allowed touched
Risk flags: TEST-ONLY; no runtime/behavior change; the boundary is unchanged (only its test coverage widens) so per cc-suite §"Low-risk WIs" self-review fallback is permissible (review-plan on the queue still runs); loc-guardian watch on ipc-casebox-handlers.unit.test.mjs (1140 raw / cap 1200) — table-driven loops are compact (import const + reuse scaffolding; createDocket/createFact single→loop net-neutral; confirmDocket +1 loop) so net growth is small, but STOP+report a split if it nears ~1185; no collateral-breakage risk (table-driving ADDS assertions; it changes no response shape)
Depends on: none
Commit boundary: one local commit for the dto-sync coverage + forbidden table-drives + the CBW-602 disposition
