## WI-DPE4: IPC/DTO — docket proposal edit (casebox:docket:edit handler + EditDocketEntryDto)
Type: IMPL
Scope: Expose the merged DPE3 editDocketEntry persistence op to the renderer over a casebox:docket:edit IPC channel, mirroring the existing casebox:docket:confirm/dismiss handlers under the SAME security discipline. HIGH-RISK IPC security boundary (tenant isolation + authority derivation + forbidden-field REJECT (input-side; "strip" is output-projection only) + no-leak error mapping) — broker review-plan BEFORE implementation, broker audit + verify after; tests-first within the WI. NO renderer UI/call sites (DPE5), NO persistence/contract change. Add EditDocketEntryDto + EDIT_DOCKET_DTO_FIELDS (exactly 8: scope matterId, entryId + the six editable content fields proposed_kind, proposed_due_at, proposed_due_at_kind, proposed_due_at_timezone, proposed_owner_user_id, reminder_offsets) + EDIT_DOCKET_FORBIDDEN_FIELDS (the complement, INCLUDING the snake_case scope aliases tenant_id, matter_id, entry_id — the camelCase matterId/entryId are the only accepted scope form; plus actor_user_id, editor_actor_user_id, revised_at, id, source_type, source_rule_citation, extractor_name, extractor_version, extraction_confidence, source_document_id, source_page_number, source_excerpt, confirmation_state, proposed_at, confirmation_actor_user_id, confirmed_at, confirmed_deadline_id, dismissal_actor_user_id, dismissed_at, dismissal_reason, created_at — MUST be disjoint from the eight DTO fields; this is an INPUT-REJECT list, not a strip — "strip" applies ONLY to the output projection) to src/caseBox/dto/docket.ts; add "revised_at" to DOCKET_ENTRY_RESPONSE_FIELDS (the one projection delta; renderer-safe timestamp; input-forbidden but output-exposed); add EditDocketEntryResult type. Add editDocketEntryHandler(payload, provide, now) to src/caseBox/docketHandlers.ts copying confirm/dismiss flow: isPlainJsonObject shape guard → forbidden-field guard (reject, NOT drop) → unknown-field guard (invalid_payload) → field validation (matterId/entryId non-empty strings; content-field types) → SCOPED PREFLIGHT fail-closed (getMatter null → unknown_matter; matter.tenant_id !== getActiveTenantId() → tenant_mismatch; getDocketEntry scoped null → invalid_payload, NO write) → PROPOSED-ONLY fail-closed (existing.confirmation_state !== "proposed" → reject BEFORE write) → server-inject {tenant_id: getActiveTenantId(), matter_id: dto.matterId, entry_id: dto.entryId, editor_actor_user_id: getActiveActorUserId(), + the six content fields} and call persistence.editDocketEntry (revised_at NOT passed — persistence server-derives it) → projectRow(entry, DOCKET_ENTRY_RESPONSE_FIELDS) → on throw mapThrownError(err, {channel: CHANNEL.docketEdit}). Add docketEdit: "casebox:docket:edit" to CHANNEL (handlerShared.ts); register ipcMain.handle in electron/ipc/caseBoxHandlers.ts; add editDocketEntry(dto) to the CaseBoxApi interface + caseBoxApi impl + DTO/Result type imports in electron/preload.mts. DESIGN CLARIFICATIONS (user-directed 2026-06-11): (1) DPE3 persistence is the FINAL authority — the handler scoped-preflight + proposed-only are READ-ONLY defense-in-depth, NOT a second divergent authorization model; persistence enforces tenant/matter/proposed-state/content rules and wins on any disagreement. (2) Preflight failures use the SAME no-leak boundary mapping as persistence errors — a cross-tenant/inaccessible entryId is indistinguishable from non-existent (generic mapped boundary error; no foreign matterId/tenant_id disclosure); fail closed when scope can't be proven. (3) Forbidden/internal fields are REJECTED, not silently dropped; revised_at is explicitly input-forbidden even though output-projected. (4) DTO naming: scope fields camelCase (matterId/entryId, matching confirm/dismiss), content fields snake_case (matching create + the contract); the handler converts matterId→matter_id, entryId→entry_id at the boundary; authority snake_case fields are never in the DTO. (5) Channel + preload only — DPE4 does NOT touch any renderer UI/call site.
Source of truth: docs/adr/docket-proposal-edit.md §3 (six editable fields) / §4 (immutable provenance) / §6 (revised_at) / §9 (DPE4 IPC/DTO spec); dev-memo/plan-batch-casebox-docket-proposal-edit-ipc-00.md (the amended proposal); the merged DPE3 editDocketEntry(opts) persistence op (services/case-box-persistence/src/types.ts EditDocketEntryOpts) + the DPE2 contract; the existing confirm/dismiss pattern (apps/lawbar-desktop/src/caseBox/docketHandlers.ts confirmDocketEntryHandler/dismissDocketEntryHandler), dto/docket.ts (ConfirmDocketEntryDto / CONFIRM_DOCKET_DTO_FIELDS / CONFIRM_DOCKET_FORBIDDEN_FIELDS / DOCKET_ENTRY_RESPONSE_FIELDS), handlerShared.ts (CHANNEL / isPlainJsonObject / shapeGuardFailure / forbiddenFieldFailure / projectRow), errorMap.ts (mapThrownError / makeBoundaryError — no-leak static messages), src/security/activeTenant.ts (getActiveTenantId) + activeActor.ts (getActiveActorUserId), electron/ipc/caseBoxHandlers.ts (registerCaseBoxIpcHandlers), electron/preload.mts (CaseBoxApi); the existing tests ipc-casebox-handlers.unit.test.mjs / dto-contract.test.mjs / ipc-list-projection.unit.test.mjs.
Allowed files: apps/lawbar-desktop/src/caseBox/dto/docket.ts, apps/lawbar-desktop/src/caseBox/docketHandlers.ts, apps/lawbar-desktop/src/caseBox/handlerShared.ts, apps/lawbar-desktop/electron/ipc/caseBoxHandlers.ts, apps/lawbar-desktop/electron/preload.mts, apps/lawbar-desktop/tests/ipc-casebox-handlers.unit.test.mjs, apps/lawbar-desktop/tests/dto-contract.test.mjs, apps/lawbar-desktop/tests/ipc-list-projection.unit.test.mjs, apps/lawbar-desktop/tests/ipc-casebox-docket-edit.unit.test.mjs, apps/lawbar-desktop/package.json
Forbidden files: apps/lawbar-desktop/renderer/** (renderer screens/call sites = DPE5 — a needed renderer edit is STOP-and-split), services/case-box-persistence/** (consume DPE3, never edit), docs/contracts/** (consume the contract), apps/lawbar-desktop/src/security/activeTenant.ts + activeActor.ts (reuse existing session derivation; no auth-provider change), apps/lawbar-desktop/src/caseBox/errorMap.ts (reuse; NO new error code/message unless review reopens it), any IPC channel beyond casebox:docket:edit, any behavior change to confirm/dismiss/create/list handlers, any renderer-facing wire-format break, any forbidden-field-list weakening, any error message leaking tenant/matter/id; DOCKET_ENTRY_RESPONSE_FIELDS may add ONLY revised_at
Gates: broker review-plan via runner (HIGH-RISK IPC security boundary — required pre-impl, not self-review); npm --prefix apps/lawbar-desktop test (handler unit + dto-contract + projection + check-renderer-imports + check-no-real-data all green); loc-guardian scan before + after (source fail 800 pure — watch docketHandlers.ts / dto/docket.ts; test fail 1200 raw — ipc-casebox-handlers.unit.test.mjs is 1167 raw at baseline so the EDIT HANDLER tests go in a NEW sibling tests/ipc-casebox-docket-edit.unit.test.mjs registered in apps/lawbar-desktop/package.json's test script — mechanical loc-split the plan anticipated, user-authorized 2026-06-11; edit PROJECTION tests stay in ipc-list-projection.unit.test.mjs; review-plan review-plan-mqa9a907-cqu5g3 READY stands, no new design); broker audit + verify on the impl scope; batch-end Layer-B closeout. NOTE: DPE4 touches src/caseBox + electron + tests, NOT apps/*/renderer/* — the check-ui-design-artifact gate does NOT apply (no Design artifact line; Type IMPL not UI).
Acceptance criteria: casebox:docket:edit registered (electron/ipc/caseBoxHandlers.ts) + exposed in preload (CaseBoxApi.editDocketEntry) with NO renderer screen file touched; EditDocketEntryDto/EDIT_DOCKET_DTO_FIELDS = exactly the 8 fields; EDIT_DOCKET_FORBIDDEN_FIELDS disjoint from the DTO and covers every authority/provenance/lifecycle/identity/audit field; a renderer payload carrying ANY forbidden or unknown field (incl. tenant_id/editor_actor_user_id/revised_at/id/source_*/snake_case scope keys) is REJECTED (not dropped); an adversarial payload carrying __proto__ (and constructor/prototype) is REJECTED by isPlainJsonObject + the unknown/forbidden guards; trusted fields (tenant_id, editor_actor_user_id, matter-tenant scope check) are server/session-derived and revised_at is server-derived in persistence — none trusted from the renderer; scoped preflight is read-only defense-in-depth (DPE3 persistence remains final authority) and fails closed on unknown_matter/tenant_mismatch/unknown-or-cross-tenant entry WITHOUT writing; proposed-only is enforced fail-closed in the handler BEFORE the write; a cross-tenant/inaccessible entryId is indistinguishable from non-existent via the no-leak boundary mapping (no foreign matterId/tenant_id disclosure); the edit response is projected through DOCKET_ENTRY_RESPONSE_FIELDS + revised_at with NO authority-identity leak and revised_at present; the renderer-facing DTO exposes no snake_case authority field; confirm/dismiss/create/list handlers are behaviorally unchanged; apps test suite green; committed diff touches ONLY the declared allowed files; broker audit returns no Critical/High/Medium and verify confirms ALL CLOSED.
Risk flags: HIGH-RISK — IPC security boundary (tenant isolation, authority derivation, forbidden-field strip, no-leak error mapping) per cc-suite §High-risk + security-boundary.md; full review-plan → tests-first → audit → verify loop. Authority boundary is load-bearing: any caller-supplied authority/provenance/identity/lifecycle/audit value crossing into persistence as trusted, or any error leaking tenant/matter/id, is a hard stop. DPE3 persistence remains the final authority; the handler preflight must NOT become a divergent second authorization model. LOC: ipc-casebox-handlers.unit.test.mjs ~1173 raw near the 1200 test-fail line — split a sibling edit test file as a bounded sub-WI if the edit tests cross it; docketHandlers.ts / dto/docket.ts must stay <800 pure LOC. Renderer-boundary: DPE4 ends at preload; touching any renderer screen file is a hard stop (split to DPE5).
Depends on: WI-DPE2 (contract, merged 52e5b1b) + WI-DPE2-FIX1 (e86747f) + WI-DPE3 (persistence, merged f680d2e). Class-B contract concern (live getters/Proxies) is the deferred WI-DPE2-FIX2 and is OUT of scope here.
Commit boundary: one local commit for the WI-DPE4 IPC/DTO change (EditDocketEntryDto + editDocketEntryHandler + channel/preload wiring + tests), staged by exact path
