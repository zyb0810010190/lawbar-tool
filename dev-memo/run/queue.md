## WI-A3-UNLINK-AUDIT-KINDS: contract support for link unlink/relink audit events (contract-only; NOT A0.7-gated)
Type: SOURCE
A0.7-classification: A3-UNLINK-AUDIT-KINDS is a contract/audit-kind change (the case-box audit-event vocabulary), NOT a geometry-derived A3 behavior; it executes no A0.7-dependent code and reads no A0.7 marker, so it is NOT A0.7-gated and carries NO `Requires-A07:` line. It IS HIGH-RISK (a public contract / audit-chain tamper-evidence surface), so broker /cc-suite:review-plan + /cc-suite:audit + /cc-suite:verify are REQUIRED. If review-plan says this contract/audit change must be A0.7-gated or custody-9b, STOP and report.
Scope: Add the CONTRACT-LEVEL audit support required before the durable unlink/relink OPERATION (WI-A3-UNLINK-T1, a separate lane) can emit a tamper-evident audit event. Add a `link` audit `entity_type` + the `LINK_UNLINKED` and `LINK_RELINKED` audit-event KINDS to the case-box-contract audit vocabulary, additively and consistently across the JSON schema + the TS registry + the regenerated type, governed by the audit-event-kind-preservation ADR (the chain verifier enforces the kind's {action, entity_type, reasonRequired} tuple). CONTRACT-ONLY: this lane does NOT implement the unlink/relink mutating operation, does NOT touch the persistence layer (services/**), does NOT change the SQLite schema or CURRENT_SCHEMA_VERSION, does NOT change resolver/export behavior, and does NOT change UI. The new kinds are DEFINED here but not yet EMITTED (T1 emits them) — defining a kind before its first emitter is the established pattern (see the deferred DOCKET DEADLINE_CONTINUED note in audit-log.ts).
Required decisions to record: (1) ENTITY TYPE — add `link` to the audit `entity_type` enum (case-box-audit-event.schema.json) AND to CASE_BOX_AUDIT_ENTITY_TYPES (audit-log.ts), keeping the schema enum + the TS frozen array in sync (the schema comment requires a coordinated schema + TS update). (2) KINDS — add two entries to CASE_BOX_AUDIT_EVENT_KINDS: `LINK_UNLINKED: { action: "update", entity_type: "link", reasonRequired: true }` (an explicit unlink requires a reason — mirrors the V12 app-layer unlink_reason-required-iff-unlinked invariant) and `LINK_RELINKED: { action: "update", entity_type: "link", reasonRequired: false }` (relink restores the link to active; the V12 unlink_reason is cleared, so the relink event mandates no reason). Flag to review-plan: confirm action `update` (the link row is updated with the marker, not created/deleted) + reasonRequired (true for unlink, false for relink) — if review-plan prefers a different action or reasonRequired for relink, record that. (3) EVENT_KIND ENUM — add `LINK_UNLINKED` + `LINK_RELINKED` to the schema's `event_kind` enum (case-box-audit-event.schema.json), additively, with the existing kinds unchanged (the kind set is append-only per the audit-event-kind ADR). (4) GENERATED TYPE — regenerate docs/contracts/case-box-contract/src/generated/case-box-audit-event.ts via `npm --prefix docs/contracts/case-box-contract run gen:types` (it is AUTO-GENERATED — never hand-edited); commit the regenerated output so the committed state is consistent (loc-guardian exempts the AUTO-GENERATED banner file). (5) TUPLE ENFORCEMENT — the chain verifier (verifyAuditChain) already enforces that a v2 event's kind matches its declared {action, entity_type, reasonRequired}; this lane adds the registry entries so a future LINK_UNLINKED/RELINKED event verifies; no verifier code change unless review-plan identifies one (STOP and ask if the verifier needs new logic beyond the registry addition). (6) NO EMITTER — no audit event is emitted in this lane; no persistence/operation change; the first emitter is WI-A3-UNLINK-T1. (7) ADDITIVE / NON-BREAKING — enum-member + registry additions only; existing entity_types/kinds/canonicalization unchanged; the v1/v2 golden canonical tests stay green (the new kind is not exercised by the existing golden events). (8) NO SCHEMA/RESOLVER/EXPORT/UI/PERSISTENCE — out of scope. (9) DOCS — record the addition in a dev-memo (and an additive note in docs/adr/audit-event-kind-preservation.md ONLY if review-plan recommends it). (10) A0.7 — NOT A0.7-gated; HIGH-RISK contract change (broker required).
Source of truth: docs/adr/audit-event-kind-preservation.md (the governing ADR — kind preservation, the {action, entity_type, reasonRequired} tuple, append-only kinds, regenerate the type); docs/adr/case-box-step-4-audit-log-shape.md (the audit-log shape + hash chain + entity_type enum history); docs/contracts/case-box-contract/schemas/case-box-audit-event.schema.json (the entity_type + event_kind enums — the file edited); docs/contracts/case-box-contract/src/audit-log.ts (CASE_BOX_AUDIT_ENTITY_TYPES + CASE_BOX_AUDIT_EVENT_KINDS — the file edited); docs/contracts/case-box-contract/src/generated/case-box-audit-event.ts (regenerated, not hand-edited); docs/contracts/case-box-contract/tests/ (audit-event-kind-v2.test.mjs + contract/validators/exports tests — the kind/entity assertions to extend); services/case-box-persistence (READ-ONLY — the future consumer/emitter; NOT changed here); docs/adr/ADR-evidence-a3-unlink-break-link-workflow.md + ADR-evidence-a3-durable-unlink-schema.md (A3-UNLINK-00/SCHEMA-00 — the unlink workflow this audit serves); .claude/rules/autonomy.md + .claude/rules/cc-suite.md + .claude/rules/security-boundary.md (this lane is user-authorized; HIGH-RISK contract/audit security; broker required).
Allowed files: docs/contracts/case-box-contract/schemas/case-box-audit-event.schema.json (add `link` to entity_type enum + LINK_UNLINKED/LINK_RELINKED to event_kind enum — additive only), docs/contracts/case-box-contract/src/audit-log.ts (add `link` to CASE_BOX_AUDIT_ENTITY_TYPES + LINK_UNLINKED/LINK_RELINKED to CASE_BOX_AUDIT_EVENT_KINDS — additive only), docs/contracts/case-box-contract/src/generated/case-box-audit-event.ts (REGENERATED via gen:types — not hand-edited), docs/contracts/case-box-contract/tests/audit-event-kind-v2.test.mjs (add LINK_UNLINKED/RELINKED + `link` acceptance + tuple-enforcement cases), docs/contracts/case-box-contract/tests/contract.test.mjs OR validators.test.mjs OR exports.test.mjs (ONLY to update an entity-type/kind-set assertion the additions necessarily change — minimal, additive), dev-memo/run/queue.md, dev-memo/run/queue.linted, dev-memo/run/queue.reviewed, dev-memo/run/queue.governed, dev-memo/run/reviews/queue-review-088.md, dev-memo/study/** (a batch-audit study packet if a window closes), dev-memo/plan-batch-casebox-evidence-a3-unlink-audit-kinds-00.md (a short dev-memo recording the addition), dev-memo/cc-suite-reliability-log.md (only if a TIMEOUT retry occurs), dev-memo/deferred-audit-findings.md (only if a finding is deferred), docs/adr/audit-event-kind-preservation.md (an additive note recording the LINK kinds ONLY if review-plan recommends it AND it stays docs-only)
Forbidden files: services/** (NO persistence/operation change — the unlink/relink op + its emitter are WI-A3-UNLINK-T1; READ-ONLY reference only), services/case-box-persistence/src/sqlite/schema.ts (NO SQLite schema/version change), apps/**, native/**, .claude/**, .github/**, AGENTS.md, CLAUDE.md, GEMINI.md, scripts/workflow/** (NO gate/marker change), scripts/gen-types.mjs (NO generator change — run it, don't edit it), migrations/**, package-lock.json / any dependency manifest, docs/contracts/case-box-contract/package.json (NO change), committed dev-memo/run/evidence/** marker/ledger/key/evidence-run files, committed keys, dev-memo/run/reports/** (untracked local run-state — never staged), dev-memo/run/override-reason.md (untracked residue — never staged), Evidence-Genie-M0-Developer-Handover.md (root intake — READ-ONLY, never moved/deleted), xiaolai-dev-workflow-study.md (root intake — READ-ONLY, never moved/deleted); NO unlink/relink operation, NO audit-event emission, NO persistence change, NO SQLite schema/version change, NO resolver/export/UI change, NO removal/rename of existing entity_types or kinds (append-only), NO canonicalization change, NO verifier logic change (registry-only), NO dependency, NO marker/key/gate change, NO Requires-A07
Gates: broker /cc-suite:review-plan REQUIRED before authoring (confirm NOT-A0.7-gated but HIGH-RISK contract classification; confirm the additive `link` entity_type + LINK_UNLINKED/LINK_RELINKED kinds follow the audit-event-kind-preservation ADR — append-only, schema+TS+generated kept in sync, the {action, entity_type, reasonRequired} tuples correct; confirm action `update` + reasonRequired true(unlink)/false(relink) — or STOP if review-plan wants different; confirm CONTRACT-ONLY — no emitter, no persistence, no SQLite schema/version, no resolver/export/UI; confirm the v1/v2 golden canonical + chain-verify tests stay green); if review-plan returns NEEDS-FIX, STOP and report; if review-plan says this must be A0.7-gated/custody-9b, STOP and report; if review-plan demands an emitter / persistence change / a canonicalization or verifier-logic change / a SQLite schema change / a dependency, STOP and ask; broker /cc-suite:audit + /cc-suite:verify on the implementation diff after authoring; npm --prefix docs/contracts/case-box-contract test passes (the contract package: gen:types + build + the audit-kind/validator/contract tests, incl. the new LINK cases + the golden canonical tests still green); npm --prefix services/case-box-persistence test passes (the downstream consumer stays green — the additive kinds do not break it); scripts/workflow/check-contract-integrity.sh exits 0; scripts/workflow/check-gates.sh returns GATES OK (no Requires-A07 -> A0.7 not required -> pass); npm --prefix apps/lawbar-desktop test 610/610; npm --prefix native/evidence-core test 38/38; no services/apps/native/scripts/.claude/.github/dependency change; no SQLite schema/version/code; no emitter/operation; no committed marker/ledger/key/evidence-run; root intake untouched
Acceptance criteria: docs/contracts/case-box-contract/schemas/case-box-audit-event.schema.json has `link` in the entity_type enum AND LINK_UNLINKED + LINK_RELINKED in the event_kind enum (existing members unchanged); docs/contracts/case-box-contract/src/audit-log.ts has `link` in CASE_BOX_AUDIT_ENTITY_TYPES AND LINK_UNLINKED { action update, entity_type link, reasonRequired true } + LINK_RELINKED { action update, entity_type link, reasonRequired false } in CASE_BOX_AUDIT_EVENT_KINDS (existing entries unchanged); docs/contracts/case-box-contract/src/generated/case-box-audit-event.ts is REGENERATED (via gen:types) to reflect the new enum members (AUTO-GENERATED banner intact, not hand-edited); contract tests prove the new entity_type + kinds are accepted, the {action, entity_type, reasonRequired} tuples are enforced by the chain verifier, and a LINK_UNLINKED event with a reason + a LINK_RELINKED event verify; the existing v1/v2 golden canonical + chain-verify tests remain green (no canonicalization/verifier change); NO entity_type or kind removed/renamed (append-only); NO emitter / persistence / SQLite-schema / resolver / export / UI change; npm --prefix docs/contracts/case-box-contract test passes; npm --prefix services/case-box-persistence test passes; scripts/workflow/check-contract-integrity.sh exits 0; scripts/workflow/check-gates.sh returns GATES OK; npm --prefix apps/lawbar-desktop test 610/610; npm --prefix native/evidence-core test 38/38; broker review-plan READY (classification + kinds confirmed) + audit reports no Critical/High/Medium + verify ALL CLOSED; the committed implementation diff is confined to the Allowed files (the contract schema/src/generated/tests + dev-memo/**)
Risk flags: SOURCE — a CONTRACT / audit-event-kind change over the tamper-evident audit vocabulary — HIGH-RISK (public-API / audit-chain security surface; broker review-plan + audit + verify REQUIRED). NOT A0.7-gated (no geometry-derived behavior; no marker/key). ADDITIVE / NON-BREAKING: adds a `link` entity_type + LINK_UNLINKED/LINK_RELINKED kinds; existing entity_types/kinds/canonicalization UNCHANGED (append-only per the audit-event-kind-preservation ADR). CONTRACT-ONLY: no emitter, no persistence, no SQLite schema/version, no resolver/export/UI — those are out of scope (the unlink/relink OPERATION + the emitter are WI-A3-UNLINK-T1). The schema enum + the TS frozen array + the regenerated type MUST stay in sync. The change MUST NOT weaken the audit-chain tamper-evidence (no canonicalization change; the verifier still enforces the kind tuple) nor any Evidence invariant nor the A3-DB-00 encryption hard stop. No removal/rename of existing kinds. No dependency. No push, no merge.
Depends on: none
Commit boundary: governance commit separate (dev-memo/run/queue.md + queue.linted/reviewed/governed + dev-memo/run/reviews/queue-review-088.md); ONE implementation commit -- docs/contracts/case-box-contract/schemas/case-box-audit-event.schema.json + docs/contracts/case-box-contract/src/audit-log.ts + docs/contracts/case-box-contract/src/generated/case-box-audit-event.ts + docs/contracts/case-box-contract/tests/audit-event-kind-v2.test.mjs (+ a minimal additive update to one of contract/validators/exports tests if an entity/kind-set assertion requires it) (+ dev-memo/plan-batch-casebox-evidence-a3-unlink-audit-kinds-00.md), staged by exact path; no push; no merge
