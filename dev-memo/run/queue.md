## WI-RELEASE-G6-FULL-PROJECT-AUDIT-00: define + govern the gate-6 full-project cc-suite audit lane — the FINAL autonomous full-project sweep against current HEAD; on C0/H0/M0 produce gate-6 evidence + move the gate-6 row; on any C/H/M STOP and report; does NOT clear user-owned gates 4/11/17/21, does NOT decide D-G7-1/D-G7-2, does NOT issue final GO/NO-GO (Type: EVIDENCE, release-governance MEDIUM risk)

## Review packet (compact)
- Active plan summary: gate 6 of the M0 go-live readiness matrix (`docs/release/go-live-readiness-report.md` §1 row 6) is **OPEN** — "Full audit clean (no C/H/M)"; evidence "per-WI cc-suite audit logs; batch-audit closeout chain (all BATCH-PASS C0H0M0L0 …); `dev-memo/deferred-audit-findings.md`"; blocker "Full-project cc-suite audit sweep against current HEAD (legacy WI-12 scope)." All prerequisite lanes are done: the electron runtime advisory (R-G19-1) is remediated, the gate-3 R3 assessment is non-M0/deferred, and the gate-3 screen-count readiness-row hygiene fix is on `main` (so gate 6 does not inherit a stale fact). This WI **governs** (does NOT execute) a FUTURE lane that runs the **final autonomous full-project cc-suite audit sweep against current HEAD**, produces the gate-6 evidence doc, and — ONLY on a clean C0/H0/M0 result — moves the gate-6 row. THIS authoring lane produces the governed queue WI only; it runs NO audit, clears NO gate, and decides NO user go-live hard-stop.
- Grounding note (verified 2026-07-08 — the exec lane MUST re-verify against the real repo state before relying, per `AGENTS.md` §"Authoring discipline"): the current HEAD to audit is the tip of `main` after the gate-3 screen-count hygiene fix. The **full-project surfaces** are: the contract hub `docs/contracts/` (+ `docs/contracts/case-box-contract/`); the services `services/{case-box-persistence,ocr-persistence,ocr-worker,ocr-ingestion,ocr-review}` (+ `ocr-worker-bakeoff`, out of the production graph); the desktop client `apps/lawbar-desktop/`; the native harness `native/{evidence-core,evidence-core-swift}/`; the workflow scaffold + rules under `.claude/`. The release effort has been **continuously batch-audited** — an unbroken chain of Layer-B closeouts (each BATCH-PASS C0 H0 M0 L0/L1) through the current window — and the deferred-findings backlog (`dev-memo/deferred-audit-findings.md`) is **all-Low, all Safe-to-proceed=YES, 0 open Medium+** (reconfirmed at gate 8). These are INPUTS the full-project sweep consolidates + reconfirms; the exec lane MUST re-verify, not assume.
- Exact target files (FUTURE exec lane): a NEW `docs/release/gate6-full-project-audit-00.md` (the full-project audit: scope + method; the per-surface audit results; the batch-audit-chain continuity confirmation; the deferred-findings-backlog reconfirmation; the C/H/M/L tally; the pass/fail conclusion) + the gate-6 evidence ROW of `docs/release/go-live-readiness-report.md` (evidence-row TEXT ONLY — ON a clean C0/H0/M0 result, move gate 6 OPEN → **CLEARED (full-project audit clean at HEAD)** or, if the exec lane prefers the conservative disposition, "CLEARED pending the holistic readiness refresh"; NEVER a go-live implication; MUST NOT clear gates 4/11/17/21 or imply a final GO/NO-GO) + `dev-memo/run/reviews/` (the exec lane's review artifact). NO other file. The exec lane's audit is READ-ONLY over the repo (it inspects/audits; it does NOT modify product source/test/config).
- Exact acceptance criteria: below.
- Exact out-of-scope: NO product source/test/package/config change (the audit is READ-ONLY — it audits the repo, it does NOT fix findings; a fix is a SEPARATE WI); NO fixing of any finding in THIS lane (if the sweep finds a C/H/M, STOP and report — do NOT fix here); NO dependency change; NO schema/migration (`CURRENT_SCHEMA_VERSION` stays 12); NO contract change; NO ADR/brief edit; NO clearing of the USER-OWNED gates 4 (signing/distribution) / 11 (律师法) / 17 (license/business) / 21 (final sign-off); NO D-G7-1 / D-G7-2 gate-7 risk-acceptance decision; NO holistic readiness refresh (that is a SEPARATE lane AFTER gate 6 closes out); NO final GO/NO-GO (gate 21); NO staging of `dev-memo/run/intake/` or untracked residue.
- Requirements the FUTURE full-project audit must define + record (what the exec lane's sweep must specify + do) — at minimum:
  1. **Exact HEAD audited**: record the exact `main` HEAD commit SHA audited + the date. The sweep is against current HEAD (the final autonomous full-project sweep).
  2. **Full-project surface enumeration + audit method**: enumerate the surfaces (contract hub; the 5 production services + the excluded bakeoff; the desktop client; the native harness; the `.claude` scaffold/rules) and the audit method — a cc-suite full-project audit (Path 1 broker) covering the architecture, the critical invariants (contract=vocabulary owner / queue=transport / persistence=source-of-truth; coordinator-owns-lifecycle; stable `OcrQueueError` codes not collapsed; no FK queue→ocr_jobs; the Evidence-Genie M0 invariants), the security boundaries (SSRF/TLS/DNS fetcher; case-box tenant isolation; the offline/local-first posture), and cross-cutting correctness. For a repo too large to inline whole, the method is a STRUCTURED sweep (per-surface audit prompts + the accumulated per-WI/batch audit chain as corroborating evidence), recorded honestly — no silent truncation. **"None omitted" checklist (required):** the doc MUST mark EVERY enumerated surface as exactly one of `freshly audited` (a fresh cc-suite audit this lane), `corroborated by prior audit` (relying on its prior per-WI/batch audit, cited), or `excluded with reason` (e.g. the bakeoff, out of the production graph) — so no surface can silently disappear from the evidence (closes the false-clean path).
  3. **Batch-audit-chain continuity confirmation**: confirm the Layer-B batch-audit closeout chain is unbroken + all BATCH-PASS (C0 H0 M0, L-any) across the release effort to current HEAD (cite the closeout log + the study attestations); record any window that required an M1/H1 in-lane fix + re-audit (e.g. batch-234/238 M1, batch-253 H1, batch-256 M1) and that each re-audited clean.
  4. **Deferred-findings backlog reconfirmation**: reconfirm `dev-memo/deferred-audit-findings.md` is all-Low, all Safe-to-proceed=YES, 0 open Medium+ (the full-project audit does NOT silently ignore the backlog — it confirms every open row is a Low with a documented deferral).
  5. **C/H/M/L tally + the three-way outcome**: record the sweep's Critical/High/Medium/Low tally. **(a) C0/H0/M0** (Lows allowed, per the established discipline) → gate 6's "full audit clean (no C/H/M)" condition is MET; produce the evidence + move the gate-6 row. **(b) ANY C/H/M** → STOP and report before fixing (do NOT fix in this lane; a fix is a separate WI, then re-audit). **(c) only Lows** → apply the established deferred-findings decision discipline: **record every new Low in `gate6-full-project-audit-00.md`**, and a Safe=YES-Low deferral MAY also be appended to `dev-memo/deferred-audit-findings.md` (the only permitted backlog edit — never to fix a finding); gate 6's no-C/H/M condition is still met.
  6. **How gate 6 moves without clearing user-owned gates / implying go-live**: on C0/H0/M0, the gate-6 row moves OPEN → **CLEARED (full-project audit clean at HEAD)** (gate 6's clearance condition is a clean audit, NOT a user decision — unlike gates 4/11/17/21). This move is confined to the **gate-6 evidence/status ROW ONLY** — it MUST NOT touch the §4 roll-up buckets, the readiness summary/launch-posture language, or any other gate row; MUST NOT clear gates 4/11/17/21; MUST NOT decide D-G7-1/D-G7-2; and MUST NOT imply a final GO/NO-GO. The holistic readiness refresh (a SEPARATE later lane) reconciles the §4 roll-up buckets; gate 6's own row clearance is the only readiness-report edit here, recorded on a clean sweep.
  7. **Expected evidence artifact**: the doc records the audited HEAD SHA, the surface enumeration + method, the per-surface + chain + backlog results, the C/H/M/L tally, the pass/fail conclusion, and the environment (date) — captured as text.
  8. **Deterministic pass/fail**: PASS (gate-6 condition met) = the full-project sweep is C0/H0/M0 (Lows allowed) AND the batch-audit chain is confirmed unbroken/BATCH-PASS AND the deferred backlog is all-Low/Safe. FAIL/STOP = any C/H/M in the sweep, a broken/inconsistent audit chain, or an open Medium+ in the backlog → STOP and report, do NOT fix here.
- Dependencies mapped: **gates 4/11/17/21** (USER-OWNED STOP-AND-ASK — NOT cleared by a clean gate-6 audit); **gate 7** (D-G7-1/D-G7-2 user risk-acceptance — NOT decided); the **holistic readiness refresh** (the SEPARATE next lane after gate 6 closes out — reconciles the roll-up; NOT run here); every prior gate's evidence is INPUT to the sweep. These are noted as related, NOT cleared/decided by this WI.
- Essential refs: `docs/release/go-live-readiness-report.md` §1 gate 6 (OPEN) + §4 roll-up (the full-audit + robustness bucket); `dev-memo/batch-closeout-log.md` + the `dev-memo/study/*batch-audit*.md` chain (the batch-audit continuity evidence); `dev-memo/deferred-audit-findings.md` (the backlog to reconfirm); `AGENTS.md` §"Repo brief — lawbar-tool" (the critical invariants) + §"Gates and hard stops" + §"Source hierarchy" + §"Authoring discipline"; `.claude/rules/cc-suite.md` §"Audit remediation policy" (the C/H/M resolution + Low-deferral discipline) + §"High-risk WIs"; `.claude/rules/security-boundary.md` §"Go-live independence"; `.claude/rules/evidence-genie.md` (the Evidence M0 invariants); `.claude/rules/autonomy.md` + `staging-hygiene.md` + `loc-guardian.md`.
- Review questions: (1) Does the WI scope a READ-ONLY full-project cc-suite audit sweep at HEAD (audit + evidence + conditional gate-6 move), NOT a fix and NOT execution in this authoring lane? (2) Does it define the three-way outcome correctly — C0/H0/M0 → gate 6 CLEARED (evidence); any C/H/M → STOP, do NOT fix here; only Lows → deferred-findings discipline? (3) Does it require an honest, no-silent-truncation full-project method (per-surface audit + the batch-chain/backlog corroboration, disclosing any surface relying on prior audits)? (4) Does it keep gates 4/11/17/21 UNCLEARED, D-G7-1/D-G7-2 UNDECIDED, and issue NO final GO/NO-GO, and keep the holistic readiness refresh a SEPARATE later lane? (5) Does it forbid fixing findings in this lane + any product source/test/config change? (6) Is any finding-fix, user-gate clearance, or go-live decision smuggled in?

Type: EVIDENCE
Scope: Govern (do NOT execute) a FUTURE lane that runs the FINAL autonomous full-project cc-suite audit sweep against current `main` HEAD. The FUTURE execution lane produces `docs/release/gate6-full-project-audit-00.md` — defining requirements 1–8 above (exact HEAD SHA; full-project surface enumeration + audit method; batch-audit-chain continuity confirmation; deferred-findings-backlog reconfirmation; the C/H/M/L tally + the three-way outcome; how gate 6 moves without clearing user-owned gates; the evidence artifact; deterministic pass/fail) — then runs the audit sweep **READ-ONLY over the repo audit surfaces** — its WRITE scope is limited to exactly: `docs/release/gate6-full-project-audit-00.md`, the gate-6 row text in `docs/release/go-live-readiness-report.md`, its review artifact, and (optionally, Low-only) a Safe=YES append to `dev-memo/deferred-audit-findings.md`; it does NOT modify product source/test/config and does NOT FIX any finding. It records the result, and — ONLY on a clean **C0/H0/M0** result — updates the gate-6 evidence row of `docs/release/go-live-readiness-report.md` (OPEN → CLEARED [full-project audit clean at HEAD], or the conservative "CLEARED pending readiness refresh"). On **ANY C/H/M** it STOPS and reports (does NOT fix here — a fix is a separate WI + re-audit). On **only Lows** it applies the established deferred-findings discipline. It MUST NOT clear the user-owned gates 4/11/17/21, MUST NOT decide D-G7-1/D-G7-2, MUST NOT run the holistic readiness refresh (a separate later lane), and MUST NOT issue a final GO/NO-GO. THIS authoring lane implements + runs + decides NOTHING — it only governs the gate-6 full-project audit WI. If review asks to FIX a finding, clear a user-owned gate, decide a gate-7 risk acceptance, run the readiness refresh, or issue a go-live decision, REJECT and report.
Source of truth: docs/release/go-live-readiness-report.md §1 gate 6 (OPEN) + §4; dev-memo/batch-closeout-log.md + dev-memo/study/*batch-audit*.md (the batch-audit chain); dev-memo/deferred-audit-findings.md (the backlog); AGENTS.md §"Repo brief — lawbar-tool" + §"Gates and hard stops" + §"Source hierarchy" + §"Authoring discipline"; .claude/rules/cc-suite.md §"Audit remediation policy" + §"High-risk WIs"; .claude/rules/security-boundary.md §"Go-live independence"; .claude/rules/evidence-genie.md; .claude/rules/autonomy.md + staging-hygiene.md + loc-guardian.md
Allowed files: docs/release/gate6-full-project-audit-00.md (the NEW full-project audit evidence doc the FUTURE lane produces) + docs/release/go-live-readiness-report.md (the gate-6 evidence ROW ONLY — evidence-row TEXT ONLY, the conditional OPEN→CLEARED move on a clean sweep; NO other gate row; NO clearing of gates 4/11/17/21; NO go-live implication) + dev-memo/run/reviews/ (the exec lane's own review artifact) + OPTIONALLY dev-memo/deferred-audit-findings.md (ONLY to append newly-surfaced Lows from the sweep with Safe=YES deferrals, if any — never to fix a finding). NOTE: THIS authoring lane commits ONLY the queue governance (queue.md/linted/reviewed/governed) + reviews/queue-review-173.md — NO audit doc, NO report change. The FUTURE exec lane records its evidence (incl. the audit job IDs + the C/H/M/L tally) in the COMMIT MESSAGE + the audit doc + review artifact, and does NOT re-stage this lane's queue governance and does NOT edit product source/test.
Forbidden files: apps/ + services/ + native/ + docs/contracts/ product source/test/config (READ-ONLY — the sweep audits, it does NOT modify or fix), services/*/src/sqlite/schema.ts (CURRENT_SCHEMA_VERSION stays 12), migrations/, package.json/package-lock.json of any package (NO dependency change), docs/product/project-requirements-brief.md (READ-ONLY), docs/adr/ (READ-ONLY), docs/release/ OTHER than the NEW gate6-full-project-audit-00.md + the gate-6 row of go-live-readiness-report.md (the other release docs are READ-ONLY evidence), dev-memo/run/intake/ (raw samples — never staged), dev-memo/ OTHER than run/reviews/ + the OPTIONAL deferred-audit-findings.md Low-append, .claude/ + .github/; NO finding FIX in this lane (any C/H/M → STOP); NO product source/test/package/config change; NO AUDIT RUN in this authoring lane; NO clearing of user-owned gates 4/11/17/21; NO D-G7-1/D-G7-2 decision; NO holistic readiness refresh; NO final GO/NO-GO decision (gate 21). Push is NEVER automatic (autonomy hard-stop): local commit governance is the default; any branch push + PR open happens ONLY under explicit per-invocation user authorization; NO merge; NO branch delete of shared branches
Gates: EVIDENCE/governance lane (MEDIUM release-governance risk — governs the final full-project audit; no code, no finding-fix, no user-gate clearance, no go-live decision). (1) THIS authoring lane: broker /cc-suite:review-plan on this queue WI, Path 1, retrievable, then govern. (2) The FUTURE exec lane: RUN the full-project cc-suite audit sweep (Path 1 broker, structured per-surface + the batch-chain/backlog corroboration), produce the audit doc + the conditional gate-6 row update, then broker a POST-ARTIFACT /cc-suite:review-plan on the produced artifacts. (3) scripts/workflow/check-contract-integrity.sh exit 0; scripts/workflow/check-queue.sh PASS (both lanes). Forbidden-scope scan before commit: CURRENT_SCHEMA_VERSION EQUALS 12; no product source/test/package/config change; no finding fixed; no ADR/brief edit; no clearing of gates 4/11/17/21; no D-G7 decision; no readiness refresh; no GO/NO-GO; no raw sample. STOP-points: review asks to fix a finding, clear a user-owned gate, decide a gate-7 risk acceptance, run the readiness refresh, or issue go-live → REJECT/STOP and report; the sweep finds ANY C/H/M → STOP and report before fixing (do NOT fix in this lane); any forbidden file would change → STOP.
Acceptance criteria: check-queue and check-contract-integrity PASS and CURRENT_SCHEMA_VERSION EQUALS 12 and the diff CONTAINS only the queue governance (queue.md/linted/reviewed/governed) + reviews/queue-review-173.md and no audit-doc/product-source/test/package/config change appears and no finding-fix / no user-gate clearance / no readiness refresh appears. The governed WI must DEFINE the eight full-project-audit requirements (exact HEAD SHA; surface enumeration + method; batch-chain continuity; backlog reconfirmation; C/H/M/L tally + three-way outcome; gate-6 move without clearing user gates; evidence artifact; deterministic pass/fail), require STOP-on-any-C/H/M (no fix in-lane), keep gates 4/11/17/21 uncleared + D-G7-1/D-G7-2 undecided + no final GO/NO-GO + the holistic readiness refresh a separate later lane, and MUST NOT decide a STOP-AND-ASK hard-stop. review-plan (this lane) must be RUN and must PASS (READY, or READY with only Low-risk clarifications applied).
Risk flags: MEDIUM (release-governance — the FINAL full-project audit; risk of a false-clean sweep that silently truncates coverage, of fixing a finding in-lane, of clearing a user-owned gate, or of implying go-live). Care points: (a) NO AUDIT RUN in this authoring lane — this lane only governs the WI; (b) NO SILENT TRUNCATION — the full-project method must be honest: per-surface audit + the batch-chain/backlog corroboration, disclosing any surface relying on prior per-WI/batch audits (no "audited everything" claim without evidence); (c) STOP-ON-C/H/M — the sweep does NOT fix findings; any C/H/M halts the lane for a separate fix WI + re-audit; only Lows follow the deferred-findings discipline; (d) GATE-6 CLEARANCE IS A CLEAN AUDIT, NOT A USER DECISION — a clean sweep legitimately moves gate 6, but MUST NOT clear gates 4/11/17/21, decide D-G7-1/D-G7-2, or imply a final GO/NO-GO; (e) READINESS REFRESH IS SEPARATE — gate 6 closes out, THEN the holistic refresh runs (a later lane); (f) GO-LIVE INDEPENDENCE — a clean full-project audit does NOT imply go-live. If any cannot hold, STOP and report.
Depends on: none
Commit boundary: TWO separate lanes. (1) THIS authoring lane commits ONLY the queue governance (queue.md/linted/reviewed/governed) + reviews/queue-review-173.md — no audit doc, no report change, no implementation, no decision. (2) The FUTURE execution lane commits ONLY the new docs/release/gate6-full-project-audit-00.md + the conditional gate-6 evidence-row update to go-live-readiness-report.md + its own review artifact under dev-memo/run/reviews/ (+ optionally a Low-only append to dev-memo/deferred-audit-findings.md), records its evidence (audited HEAD SHA + audit job IDs + the C/H/M/L tally) in the COMMIT MESSAGE, does NOT re-stage this lane's queue governance, and does NOT edit product source/test or FIX any finding. NO product source/test/package/config file in either lane; NO finding fix (any C/H/M → STOP); NO user-owned-gate clearance; NO D-G7 decision; NO readiness refresh; NO final GO/NO-GO; NO staging of dev-memo/run/intake/ or untracked residue; push is NEVER automatic (autonomy hard-stop) — any branch push + PR open happens ONLY under explicit per-invocation user authorization; no merge; no branch delete of shared branches. THIS authoring lane implements NOTHING, runs NO audit, and decides NOTHING — it only governs the gate-6 full-project audit WI.

## Acceptance criteria (FUTURE execution lane — what the governed full-project audit must satisfy)

These describe the deliverable the FUTURE exec lane must produce; they are NOT preconditions of THIS authoring lane. The authoring-lane acceptance is the structured `Acceptance criteria:` field above (queue governance + review READY only — the audit is NOT run in this lane).

1. A NEW `docs/release/gate6-full-project-audit-00.md` exists defining requirements 1–8 above, recording the exact audited HEAD SHA + date, the full-project surface enumeration + audit method, and the sweep results.
2. The doc records a fresh full-project cc-suite audit sweep (Path 1 broker, structured per-surface) + the batch-audit-chain continuity confirmation (unbroken, all BATCH-PASS, with any in-lane M1/H1-fix-then-re-audit windows noted) + the deferred-findings-backlog reconfirmation (all-Low, Safe=YES, 0 open Medium+), with the C/H/M/L tally and an honest no-silent-truncation disclosure of any surface relying on prior audits.
3. **Three-way outcome:** on **C0/H0/M0** → gate 6's condition met; on **any C/H/M** → STOP + report (no fix in-lane; a separate fix WI + re-audit); on **only Lows** → deferred-findings discipline (record + defer Safe=YES Lows).
4. On a clean C0/H0/M0 result, the exec lane CITES the exact gate-6 row (by its `| 6 | …` line) before editing and updates ONLY that row's evidence/status TEXT — OPEN → CLEARED (full-project audit clean at HEAD) or "CLEARED pending readiness refresh". MUST NOT clear gates 4/11/17/21, decide D-G7-1/D-G7-2, or imply a final GO/NO-GO; no other gate row touched.
5. NO product source/test/package/config/schema/contract change; NO finding fixed in-lane; NO ADR/brief edit; `CURRENT_SCHEMA_VERSION` stays 12; docs-only tracked output (+ optionally a Low-only deferred-findings append).
6. `scripts/workflow/check-queue.sh` PASS + `scripts/workflow/check-contract-integrity.sh` PASS on both lanes. After gate 6 closes out, the holistic readiness refresh is the SEPARATE next lane (NOT run here).
