## WI-D1: casebox:docket:list IPC read handler for pending/confirmed/dismissed docket entries
Type: IMPL
Scope: Add a tenant-scoped read IPC handler `casebox:docket:list` that surfaces durably-persisted docket entries (especially `confirmation_state="proposed"` proposals that are currently invisible after reload because the renderer only lists confirmed deadlines). The handler wraps the EXISTING persistence `listDocketEntries({tenant_id, matter_id, confirmation_state?, source_type?, cursor?, limit?})` and projects each row through the EXISTING `DOCKET_ENTRY_RESPONSE_FIELDS` allowlist (which strips `tenant_id`, `actor_user_id`, `confirmation_actor_user_id`, `dismissal_actor_user_id`). Mirror the established list-handler pattern (shape-guard → forbidden-field guard → unknown-field DTO-allowlist guard → field validation → getMatter existence + active-tenant → persistence call → projectRow over the allowlist). Accept renderer DTO `{matterId, confirmation_state?, source_type?, limit?, cursor?}` only; bound `limit` to MAX_LIST_LIMIT; do NOT accept tenant_id/actor fields. Add a new CHANNEL constant `docketList: "casebox:docket:list"` in handlerShared.ts and wire it in electron/ipc/caseBoxHandlers.ts. NO persistence/contract change (listDocketEntries already exists). NO renderer change in this WI.
Source of truth: services/case-box-persistence/src/types.ts (ListDocketEntriesQuery/Page — already exists); apps/lawbar-desktop/src/caseBox/docketHandlers.ts (existing create/confirm handlers to mirror); apps/lawbar-desktop/src/caseBox/dto.ts (DOCKET_ENTRY_RESPONSE_FIELDS + the *_DTO_FIELDS/*_FORBIDDEN_FIELDS pattern); apps/lawbar-desktop/src/caseBox/handlerShared.ts (CHANNEL map); dev-memo/plan-batch-casebox-docket-lifecycle-00.md (§3 WI-D1)
Allowed files: apps/lawbar-desktop/src/caseBox/docketHandlers.ts, apps/lawbar-desktop/src/caseBox/dto.ts, apps/lawbar-desktop/src/caseBox/handlerShared.ts, apps/lawbar-desktop/electron/ipc/caseBoxHandlers.ts, apps/lawbar-desktop/tests/ipc-casebox-handlers.unit.test.mjs, dev-memo/deferred-audit-findings.md
Forbidden files: apps/lawbar-desktop/renderer/**, services/**, docs/contracts/**, apps/lawbar-desktop/tests/ipc-handlers.unit.test.mjs, apps/lawbar-desktop/tests/renderer-view-matter.test.mjs, apps/lawbar-desktop/tests/_view-matter-dom.mjs, .claude/settings.json
Gates: npm --prefix apps/lawbar-desktop test
Acceptance criteria: a new `listDocketEntriesHandler` returns docket entries for a matter, defaulting to all states and filterable by `confirmation_state` (proposed/confirmed/dismissed) and `source_type`; every returned row is MANDATORILY projected through `projectRow(row, DOCKET_ENTRY_RESPONSE_FIELDS)` on the success path and a test asserts NO returned row contains `tenant_id`, `actor_user_id`, `confirmation_actor_user_id`, or `dismissal_actor_user_id`; a proposed entry created via the create handler is returned by the list handler with `confirmation_state="proposed"` (proving the reload-visibility gap is closeable); the handler rejects an unknown/forbidden field (e.g. tenant_id) with an invalid_payload error; getMatter-missing and cross-tenant cases fail closed; `limit` is bounded to MAX_LIST_LIMIT; CHANNEL.docketList is wired in caseBoxHandlers.ts; `npm --prefix apps/lawbar-desktop test` passes; no renderer/services/contract file modified
Risk flags: IMPL IPC read handler; NO persistence/contract change (listDocketEntries pre-exists); authority-leak risk mitigated by DOCKET_ENTRY_RESPONSE_FIELDS projection (audited); broker review-plan + audit + verify required; not Type UI (no renderer path) so no Design-artifact gate; standard list-handler pattern (not security-boundary/persistence high-risk)
Depends on: none
Commit boundary: one local commit for the docket:list handler + dto/channel/wiring + ipc tests

## WI-D2: casebox:docket:dismiss IPC handler (fail-closed scoped dismiss of a proposal)
Type: IMPL
Scope: Add a tenant-scoped, fail-closed `casebox:docket:dismiss` IPC handler that dismisses a PROPOSED docket entry via the EXISTING persistence `dismissDocketEntry(entryId, {dismissal_actor_user_id, dismissed_at, dismissal_reason})`. This batch dismisses ONLY proposed entries: dismissing a CONFIRMED entry is OUT of scope (it has already materialized a CaseBoxDeadline and dismissing it would orphan that deadline — a separate slice + consistency decision). Mirror `transitionFactHandler`: shape-guard → forbidden-field guard → unknown-field DTO-allowlist guard → require non-empty `dismissal_reason` → getMatter existence + active-tenant → tenant+matter-scoped `getDocketEntry` preflight (FAIL CLOSED if the entry does not exist under this matter/tenant — never call the unscoped persistence dismiss without the scoped existence check) → assert the preflighted entry's `confirmation_state === "proposed"` and REJECT (fail closed, invalid_payload/equivalent) any non-proposed entry BEFORE calling persistence → persistence dismiss → project the returned entry over `DOCKET_ENTRY_RESPONSE_FIELDS` on the success path. The renderer DTO is ONLY `{matterId, entryId, dismissal_reason}`; the handler MUST inject `dismissal_actor_user_id` (active user) and `dismissed_at` (server clock) itself and MUST reject any client-supplied `dismissal_actor_user_id`/`dismissed_at`/`confirmation_*`/status/lifecycle field via a CREATE_*-style FORBIDDEN_FIELDS guard. Add CHANNEL `docketDismiss: "casebox:docket:dismiss"` + wire in caseBoxHandlers.ts. NO persistence/contract change. NO renderer change in this WI.
Source of truth: services/case-box-persistence/src/types.ts (DismissDocketEntryOpts — already exists); apps/lawbar-desktop/src/caseBox/factHandlers.ts (transitionFactHandler — the fail-closed scoped-preflight pattern to mirror); apps/lawbar-desktop/src/caseBox/docketHandlers.ts (confirmDocketEntryHandler scoped preflight); apps/lawbar-desktop/src/caseBox/dto.ts; dev-memo/plan-batch-casebox-docket-lifecycle-00.md (§3 WI-D2)
Allowed files: apps/lawbar-desktop/src/caseBox/docketHandlers.ts, apps/lawbar-desktop/src/caseBox/dto.ts, apps/lawbar-desktop/src/caseBox/handlerShared.ts, apps/lawbar-desktop/electron/ipc/caseBoxHandlers.ts, apps/lawbar-desktop/tests/ipc-casebox-handlers.unit.test.mjs, dev-memo/deferred-audit-findings.md
Forbidden files: apps/lawbar-desktop/renderer/**, services/**, docs/contracts/**, apps/lawbar-desktop/tests/ipc-handlers.unit.test.mjs, apps/lawbar-desktop/tests/renderer-view-matter.test.mjs, apps/lawbar-desktop/tests/_view-matter-dom.mjs, .claude/settings.json
Gates: npm --prefix apps/lawbar-desktop test
Acceptance criteria: a new `dismissDocketEntryHandler` dismisses a PROPOSED entry and returns the projected entry with `confirmation_state="dismissed"`; a dismiss targeting a CONFIRMED (or already-dismissed) entry is REJECTED fail-closed AFTER the scoped preflight and BEFORE any persistence dismiss call (a test asserts a confirmed entry cannot be dismissed via this handler); the handler INJECTS `dismissal_actor_user_id` + `dismissed_at` server-side and REJECTS a client-supplied `dismissal_actor_user_id`, `dismissed_at`, or any confirmation/lifecycle field with invalid_payload (a test asserts each forbidden field is rejected); a missing/empty `dismissal_reason` is rejected; a dismiss targeting an entry NOT under the caller's matter/tenant FAILS CLOSED via the scoped getDocketEntry preflight (does not reach the unscoped persistence call); the success-path response is MANDATORILY projected through `projectRow(entry, DOCKET_ENTRY_RESPONSE_FIELDS)` and a test asserts the returned object contains NONE of `tenant_id`/`actor_user_id`/`confirmation_actor_user_id`/`dismissal_actor_user_id`; CHANNEL.docketDismiss wired; `npm --prefix apps/lawbar-desktop test` passes; no renderer/services/contract file modified
Risk flags: IMPL IPC write handler; PROPOSED-only (confirmed-entry dismissal is OUT of scope — would orphan a materialized deadline; enforced by a post-preflight confirmation_state==="proposed" guard that fails closed); fail-closed tenant/matter scoped preflight mandatory; server-injected actor/timestamp + forbidden-field rejection (authority boundary); mandatory DOCKET_ENTRY_RESPONSE_FIELDS projection on the success path; NO persistence/contract change (dismissDocketEntry pre-exists); broker review-plan + audit + verify required; not Type UI (no renderer path) so no Design-artifact gate
Depends on: none
Commit boundary: one local commit for the docket:dismiss handler + dto/channel/wiring + ipc tests

## WI-D3: design artifact for pending-proposals read surface + dismiss control
Type: ASSET
Scope: Produce the committed design artifact `dev-memo/design/2026-06-06-casebox-docket-pending-dismiss.md` describing the matter-view UX for (a) listing durable PENDING docket proposals after reload (a "Pending proposals" group inside the existing Deadlines disclosure) and (b) a per-proposal Dismiss control (collecting a required dismissal reason). Doc-only: defines layout, states (empty / populated / error / dismissing), which fields render (statement of the proposed deadline: kind, proposed_due_at, owner, proposed_at, source_type), the two consumed channels (casebox:docket:list, casebox:docket:dismiss), and the text-first status rendering convention. This artifact satisfies the renderer-path design-artifact gate for WI-D4. NO source/test/renderer/contract/persistence change.
Source of truth: dev-memo/design/2026-06-05-casebox-deadline-create-confirm.md (sibling design precedent); dev-memo/design/2026-06-03-casebox-deadlines-readonly.md; apps/lawbar-desktop/renderer/screens/viewMatterDeadlines.ts (the section being extended); dev-memo/plan-batch-casebox-docket-lifecycle-00.md (§3 WI-D3)
Allowed files: dev-memo/design/2026-06-06-casebox-docket-pending-dismiss.md
Forbidden files: apps/lawbar-desktop/**, services/**, docs/contracts/**, dev-memo/run/**, .claude/**
Gates: design self-review (doc-only; no code gate)
Acceptance criteria: the file dev-memo/design/2026-06-06-casebox-docket-pending-dismiss.md exists and documents the pending-proposals list + per-proposal dismiss control, naming the two consumed channels (casebox:docket:list, casebox:docket:dismiss), the rendered fields, and the empty/populated/error/dismissing states; it is doc-only and changes no source/test/renderer/contract/persistence file; it is concrete enough to serve as WI-D4's `Design artifact:` reference
Risk flags: ASSET doc-only; no code change; low-risk; produces the design proof WI-D4's renderer path requires; self-review fallback acceptable for a doc-only design asset
Depends on: none
Commit boundary: one local commit for the design artifact only

## WI-D4: renderer bridge + pending-proposals UI + dismiss UI in the matter view
Type: UI
Design artifact: dev-memo/design/2026-06-06-casebox-docket-pending-dismiss.md
Scope: Wire the renderer to the WI-D1/WI-D2 handlers and render durable pending docket proposals + a dismiss control in the matter view. (1) Bridge: add `listDocketEntries` and `dismissDocketEntry` to `apps/lawbar-desktop/electron/preload.mts` (the real renderer IPC bridge; invoke binding only — NO main-process handler / ipc-wiring change), renderer/api.ts (CaseBoxClient/CaseBoxApi methods via stripDtoFields), and renderer/types.ts (DTOs + RENDERER_LIST_DOCKET_DTO_FIELDS + RENDERER_DISMISS_DOCKET_DTO_FIELDS), with renderer-dto-sync PAIRS asserting set-equality vs the canonical *_DTO_FIELDS. (2) UI: in renderer/screens/viewMatterDeadlines.ts render a "Pending proposals" group (loaded on disclosure via casebox:docket:list filtered to confirmation_state="proposed"), each row showing the proposed deadline's human fields + a Dismiss control that collects a required reason and calls casebox:docket:dismiss, then refreshes. Legal actions only (dismiss a proposed entry); NO edit, NO deadline status transition, NO reminders. The renderer forwards only the allowlisted DTO fields; main injects authority. (3) PAGINATION: casebox:docket:list returns `{ rows, next_cursor }`. WI-D4 MUST NOT silently hide proposed entries beyond page one — render the first page and, when `next_cursor` is non-null, render an explicit "Show more" affordance that fetches + appends the next page (mirror the existing facts `next_cursor` "Show more" pattern in viewMatterFacts.ts). Do NOT introduce unbounded eager-fetch. An acceptance test must prove proposals beyond the first page are reachable. (4) LOC: if viewMatterDeadlines.ts (currently 528 raw LOC) approaches the 800 hand-written-source fail cap, EXTRACT a sibling `apps/lawbar-desktop/renderer/screens/viewMatterDocketProposals.ts` (and add it to the renderer-imports lint allowance) rather than letting the file bloat — run loc-guardian:scan before committing. AMENDED (2026-06-07, user-approved): the bridge file is `electron/preload.mts` (NOT renderer/preload.mts, which does not exist); WI-D4 Allowed-files swaps to it and the Forbidden boundary narrows from `electron/**` to `electron/ipc/**` + `electron/main.ts` so the preload bridge is touchable while main-process handler wiring + the entrypoint stay forbidden. Boundary correction only; no product-scope change. AMENDED-2 (2026-06-07, user-approved): loading proposals on disclosure-open means every test that opens the Deadlines disclosure calls api.listDocketEntries; the shared harness `apps/lawbar-desktop/tests/_view-matter-dom.mjs` `makeStubApi` lacks `listDocketEntries`/`dismissDocketEntry`, breaking collateral tests (`renderer-view-matter.test.mjs`, `renderer-deadline-urgency.test.mjs`). Add `_view-matter-dom.mjs` to Allowed-files; the ONLY permitted change there is adding the two default shared-stub methods (`listDocketEntries` default empty success; `dismissDocketEntry` default success, following the harness's success-default convention for mutating methods — WI-D4's own dismiss tests override it and assert the exact `{matterId,entryId,dismissal_reason}` payload). `renderer-view-matter.test.mjs` + `renderer-deadline-urgency.test.mjs` stay Forbidden + untouched (no assertion/fixture change). No renderer guard like `typeof api.listDocketEntries !== "function"` (would mask a missing production bridge).
Source of truth: apps/lawbar-desktop/renderer/screens/viewMatterFacts.ts (the WI-804 review-controls renderer precedent: legal-edges-only controls, text-first status, narrow DTO forward, next_cursor "Show more"); apps/lawbar-desktop/renderer/screens/viewMatterDeadlines.ts (the section to extend); dev-memo/design/2026-06-06-casebox-docket-pending-dismiss.md (WI-D3 design); apps/lawbar-desktop/renderer/types.ts + api.ts + apps/lawbar-desktop/electron/preload.mts (bridge pattern); dev-memo/plan-batch-casebox-docket-lifecycle-00.md (§3 WI-D4)
Allowed files: apps/lawbar-desktop/electron/preload.mts, apps/lawbar-desktop/renderer/api.ts, apps/lawbar-desktop/renderer/types.ts, apps/lawbar-desktop/renderer/screens/viewMatterDeadlines.ts, apps/lawbar-desktop/renderer/screens/viewMatterDocketProposals.ts, apps/lawbar-desktop/tests/renderer-deadline-write.test.mjs, apps/lawbar-desktop/tests/renderer-dto-sync.test.mjs, apps/lawbar-desktop/tests/_view-matter-dom.mjs, dev-memo/deferred-audit-findings.md
Forbidden files: apps/lawbar-desktop/src/**, apps/lawbar-desktop/electron/ipc/**, apps/lawbar-desktop/electron/main.ts, services/**, docs/contracts/**, apps/lawbar-desktop/tests/renderer-view-matter.test.mjs, apps/lawbar-desktop/tests/renderer-deadline-urgency.test.mjs, apps/lawbar-desktop/tests/ipc-handlers.unit.test.mjs, apps/lawbar-desktop/tests/ipc-casebox-handlers.unit.test.mjs, .claude/settings.json
Gates: npm --prefix apps/lawbar-desktop test
Acceptance criteria: the renderer api exposes `listDocketEntries` and `dismissDocketEntry`; renderer-dto-sync PAIRS assert RENDERER_LIST_DOCKET_DTO_FIELDS / RENDERER_DISMISS_DOCKET_DTO_FIELDS set-equal their canonical *_DTO_FIELDS; the Deadlines disclosure renders a pending-proposals group populated from casebox:docket:list (proposed filter) that PERSISTS across reload (a test mounts, lists a proposed entry, and asserts it renders); a per-proposal Dismiss control collects a required reason and calls casebox:docket:dismiss then refreshes (a test asserts the forwarded DTO is exactly {matterId, entryId, dismissal_reason} and that the row disappears/refreshes on success); a dismiss error renders inline role=alert; when casebox:docket:list returns a non-null next_cursor a "Show more" affordance fetches + appends the next page and a test proves proposals beyond page one are reachable (no silent truncation, no unbounded eager-fetch); NO edit/transition/reminder control is added; viewMatterDeadlines.ts stays under the 800 source LOC cap (extract viewMatterDocketProposals.ts if needed) verified by loc-guardian:scan; `npm --prefix apps/lawbar-desktop test` passes; the ONLY non-renderer file touched is the preload bridge `apps/lawbar-desktop/electron/preload.mts` (invoke binding) — no `src/**`, no `electron/ipc/**`, no `electron/main.ts`, no services/contract file modified
Risk flags: Type UI (renderer path) → Design-artifact gate applies and is satisfied by the WI-D3 artifact dev-memo/design/2026-06-06-casebox-docket-pending-dismiss.md; LOC watch on viewMatterDeadlines.ts (528→ near 800 cap) — extract a sibling if needed; legal-actions-only (dismiss); authority injected server-side; broker review-plan + audit + verify required; depends on WI-D1 (list handler) + WI-D2 (dismiss handler) + WI-D3 (design)
Depends on: WI-D1, WI-D2, WI-D3
Commit boundary: one local commit for the renderer bridge + pending-proposals UI + dismiss UI (+ optional sibling extraction)
