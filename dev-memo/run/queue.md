## WI-GATE5-OCR-WORKER-SIGINT-FLAKE-00: deterministic resolution (or justified quarantine) of the ocr-worker SIGINT idle-loop spawn flake (Type: IMPL, test-flake resolution)

## Review packet (compact)
- Active plan summary: close M0 go-live **gate 5** (`docs/release/go-live-readiness-report.md` §1 gate 5 = PARTIAL — "1 pre-existing ocr-worker SIGINT flake (hot-system load)"; required action: "Deterministically resolve OR explicitly quarantine ... documented-acceptance is NOT sufficient for GO"). The flake is precisely located + self-documented: `services/ocr-worker/tests/cli.spawn.test.mjs` header (lines 9-15) + the test `"bin SIGINT during idle loop: exits 0 with stop_reason=stopped"` (line 239). Failure mode: the test spawns the real `bin/ocr-worker.mjs`, waits with a LIVENESS poll (`waitForLiveChild` — explicitly "not a formal readiness protocol; the worker has no child-side ready signal"), then `child.kill("SIGINT")`. Under concurrent host load the child can be live but NOT yet have installed its SIGINT handler / entered the AbortController seam, so the SIGINT hits Node's DEFAULT action (terminate) → the child exits via signal (`signal != null`, `code != 0`) instead of graceful `exitCode=0` / `stop_reason=stopped` → the test fails intermittently.
- Root-cause + fix direction (the FUTURE exec lane decides, review-gated): the residual race is the missing child-side READINESS signal. PRIMARY (deterministic) fix: emit a child-side readiness marker AFTER the SIGINT/SIGTERM handlers are installed AND the worker has entered its idle wait (in `services/ocr-worker/src/cli.ts`, or `src/processSignals.ts` / `src/workerLoop.ts` where the handler-arm + loop-entry happen) — on STDERR so the STDOUT summary JSON the test parses stays clean — and have the test wait for that marker BEFORE sending SIGINT (replacing the `waitForLiveChild` liveness poll). This removes the race deterministically (SIGINT is delivered only once the handler is provably armed). FALLBACK (only if review proves the deterministic fix infeasible/too invasive): explicit quarantine — gate the test behind an env flag with a written justification — which leaves a DOCUMENTED RESIDUAL (gate 5 NOT GO-cleared per the readiness report), never a silent skip.
- Exact target files (future exec lane): `services/ocr-worker/tests/cli.spawn.test.mjs` (the flaky test — replace the liveness poll with a readiness-marker wait) + a MINIMAL `services/ocr-worker/src/cli.ts` (and/or `src/processSignals.ts` / `src/workerLoop.ts`) change to emit the readiness marker. NOTE: `services/ocr-worker/dist/**` is GITIGNORED and rebuilt by the test's own `npm run build` pretest step — it is NEVER staged/committed; the src change is picked up automatically.
- Exact acceptance criteria: below.
- Exact out-of-scope: no OCR product redesign; no broad worker/loop rewrite (the src change is a single readiness-emit, not a refactor); no schema/migration/contract/persistence change; no renderer/UI/IPC/DTO change; no package/dependency change (unless review proves the deterministic fix impossible without one); no native change; no OcrQueueError-code rename; no Forms T4/T5; no product-UI buildout; no signing/notarization; no custody/JS-shim/A8; no release GO/NO-GO decision.
- Essential refs: `docs/release/go-live-readiness-report.md` §1 gate 5 (the flake evidence + required action); `services/ocr-worker/tests/cli.spawn.test.mjs` (the self-documented flake); `services/ocr-worker/src/processSignals.ts` (the SIGINT→AbortSignal seam) + `src/cli.ts` + `src/workerLoop.ts` (handler-arm + idle-loop entry).
- Exec-lane note (folded from review-plan-mr74xvbo-vlbof2 Low): the existing `cli.spawn.test.mjs` header comment anticipates a future readiness marker on STDOUT; the deterministic fix uses STDERR (to protect the stdout summary-JSON parse) — the exec lane MUST update that stale header comment when it edits the test.
- Review questions: (1) Is the deterministic readiness-marker fix correct + minimal, or would it require a broad worker change (→ prefer quarantine)? (2) Should the marker live in `cli.ts`, `processSignals.ts`, or `workerLoop.ts` for the tightest handler-arm-then-emit ordering? (3) Does emitting on STDERR keep the STDOUT summary-JSON assertion intact? (4) Is quarantine-with-justification acceptable as a fallback given "documented-acceptance is NOT sufficient for GO" (i.e. it leaves a residual, does not clear gate 5)? (5) Is any OcrQueueError-code / product-behavior change smuggled in?

Type: IMPL
Scope: Resolve M0 go-live **gate 5** — the single pre-existing ocr-worker SIGINT idle-loop spawn flake (`services/ocr-worker/tests/cli.spawn.test.mjs`, test `"bin SIGINT during idle loop: exits 0 with stop_reason=stopped"`). The FUTURE execution lane: (a) reproduces/characterizes the flake (the test header already characterizes it — a host-load race where SIGINT precedes handler-arm and Node's default action terminates the child before graceful exit); (b) implements the SMALLEST safe fix, PREFERRING the deterministic path — a child-side readiness marker emitted after the SIGINT/SIGTERM handlers are installed and the idle loop is entered, with the test waiting for it before `child.kill("SIGINT")` (replacing the non-deterministic `waitForLiveChild` liveness poll); (c) if and only if review proves the deterministic fix infeasible/too invasive, falls back to an EXPLICIT env-gated quarantine WITH a written justification, recording that this leaves a DOCUMENTED RESIDUAL (gate 5 not GO-cleared) rather than a silent skip; (d) records the evidence — the affected test name/file, the observed failure mode, before/after test results (the flaky test PASSES deterministically across repeated runs after the fix), and whether the outcome CLEARS gate 5 or leaves a documented residual. It changes ONLY the flaky test + a minimal worker readiness-emit; NO OCR product redesign, NO worker rewrite, NO schema/contract/persistence/renderer/dependency/native change, NO OcrQueueError-code change, and NO go-live GO/NO-GO decision. `services/ocr-worker/dist/**` is gitignored (rebuilt by the test's `npm run build` pretest) and is NEVER staged. FORBIDDEN in the exec lane: any OCR engine/pipeline/fetcher/coordinator behavior change; any product-behavior change beyond the readiness marker; any schema/migration/contract/persistence/renderer/IPC/DTO change; any package/dependency change (unless review proves the deterministic fix impossible without it, then STOP and report); any Forms/signing/custody/A8 work; a release GO/NO-GO decision. If review asks to broaden the fix into a worker refactor, REJECT and report.
Source of truth: docs/release/go-live-readiness-report.md §1 gate 5 (PARTIAL — the flake evidence + the "deterministically resolve OR explicitly quarantine; documented-acceptance NOT sufficient for GO" required action) + §3 item 2; dev-memo/plan-go-live-readiness-00.md §1 gate 5; services/ocr-worker/tests/cli.spawn.test.mjs (the self-documented flake — header lines 9-15 + the line-239 test); services/ocr-worker/src/processSignals.ts (SIGINT/SIGTERM → AbortSignal seam) + src/cli.ts + src/workerLoop.ts (handler-arm + idle-loop entry — where the readiness marker would be emitted); AGENTS.md §"Critical invariants" (stable OcrQueueError codes; coordinator owns lifecycle); .claude/rules/security-boundary.md (ocr-worker fetcher is security-sensitive — the fix MUST NOT touch the fetcher/TLS/DNS paths); .claude/rules/autonomy.md + staging-hygiene.md + loc-guardian.md
Allowed files: services/ocr-worker/tests/cli.spawn.test.mjs (the flaky test — deterministic readiness wait OR env-gated quarantine) + services/ocr-worker/src/cli.ts and/or services/ocr-worker/src/processSignals.ts and/or services/ocr-worker/src/workerLoop.ts (the MINIMAL child-side readiness-marker emit — a single stderr line after handler-arm + loop-entry, NOT a refactor) + docs/release/go-live-readiness-report.md (the exec lane updates ONLY the gate-5 evidence row to reflect the resolved/quarantined state) + dev-memo/run/reviews/ (the exec lane's own review artifact). NOTE: THIS authoring lane commits ONLY the queue governance (queue.md/linted/reviewed/governed) + reviews/queue-review-134.md — NO test/src/report change. services/ocr-worker/dist/** is gitignored (rebuilt by the test pretest) and MUST NOT be staged. The future exec lane records its before/after test evidence in the COMMIT MESSAGE + its review artifact.
Forbidden files: services/ocr-worker/src/fetcher/** + any TLS/DNS/SSRF/pinnedLookup path (security-sensitive — NOT this WI's scope), services/ocr-worker/src/engines/** + pipeline/coordinator behavior (no OCR behavior change), services/ocr-worker/dist/** (gitignored — never staged), docs/contracts/ (NO contract change), services/*/src/sqlite/schema.ts (CURRENT_SCHEMA_VERSION stays 12; no DDL), migrations/ (none), native/, package.json/package-lock.json of any package (no dependency unless review-proven-necessary → STOP), apps/lawbar-desktop/ (NO renderer/IPC/DTO/handler change), other services/ (case-box-persistence etc. untouched), dev-memo/run/intake/ (raw samples — never staged), docs/adr/ + docs/product/ + other dev-memo/plan/design docs, docs/release/ OTHER than the gate-5 evidence row of go-live-readiness-report.md, scripts/ + .claude/ + .github/; NO OcrQueueError-code rename/removal/merge; NO OCR product redesign or worker rewrite; NO schema/migration; NO dependency; NO native; NO Forms T4/T5; NO product-UI buildout; NO signing/notarization; NO custody/JS-shim/A8; NO release GO/NO-GO decision. Push is NEVER automatic (autonomy hard-stop): local commit governance is the default; any branch push + PR open happens ONLY under explicit per-invocation user authorization; NO merge; NO branch delete
Gates: IMPL test-flake-resolution lane (LOW-MEDIUM risk — a test-stabilization + a minimal readiness-emit; NOT security/persistence). (1) THIS authoring lane: broker /cc-suite:review-plan on this queue WI, Path 1, retrievable, then govern. (2) The FUTURE exec lane: reproduce the flake (characterize the failure mode), implement the deterministic readiness fix (or the review-justified quarantine), run npm --prefix services/ocr-worker test and confirm the full ocr-worker suite PASSES, and RE-RUN the flaky test repeatedly (e.g. a bounded loop) to show it PASSES deterministically (no signal-terminated exit) rather than by luck; broker /cc-suite:audit on the diff if the fix touches src (a src readiness-emit is product code → audit the diff); /cc-suite:verify if the audit references findings. (3) scripts/workflow/check-contract-integrity.sh exit 0; scripts/workflow/check-queue.sh PASS (both lanes). Forbidden-scope scan before commit: CURRENT_SCHEMA_VERSION EQUALS 12; no fetcher/TLS/DNS/engine/pipeline change; no OcrQueueError-code rename; no dependency; no dist staged; no raw sample. STOP-points: the deterministic fix would require a worker refactor or a dependency → STOP and report (consider quarantine); review/audit asks to broaden scope → REJECT; any forbidden file would change → STOP.
Acceptance criteria: check-queue and check-contract-integrity PASS and CURRENT_SCHEMA_VERSION EQUALS 12 and the diff CONTAINS only the flaky test + a minimal src readiness-emit + the gate-5 evidence row and no fetcher/TLS/DNS/engine/pipeline/schema/contract/dependency/dist change appears and no OcrQueueError-code rename appears. The exec lane must make the flaky test `"bin SIGINT during idle loop..."` PASS deterministically — the child must EXIT with code 0 and signal null (graceful, NOT via the SIGINT default action) and the summary must CONTAIN stop_reason equal to "stopped" — across repeated runs, and the full `npm --prefix services/ocr-worker test` suite must PASS; the exec lane must RECORD the observed failure mode + before/after results and STATE whether gate 5 is CLEARED (deterministic fix) or left a DOCUMENTED RESIDUAL (quarantine). review-plan (this lane) must RETURN READY. (Exec lane, recorded THERE: if the fix touches src, /cc-suite:audit must RETURN no open C/H/M.)
Risk flags: LOW-MEDIUM (test-stabilization + minimal readiness-emit; the ocr-worker is a security-sensitive service BUT this WI must NOT touch the fetcher/TLS/DNS/engine/pipeline paths). Care points: (a) DETERMINISTIC-FIRST — prefer a readiness handshake over retry-masking or a silent skip; quarantine is a review-justified FALLBACK that leaves a documented residual (NOT a GO-clear); (b) STDOUT DISCIPLINE — the readiness marker goes on STDERR so the test's stdout summary-JSON parse is unaffected; (c) NO SECURITY-PATH TOUCH — the fetcher/TLS/DNS/SSRF paths are out of scope; (d) NO BEHAVIOR CHANGE beyond the marker — the graceful-shutdown semantics + OcrQueueError codes + coordinator lifecycle are unchanged; (e) DIST IS GITIGNORED — rebuilt by the test pretest, never staged; (f) GO-LIVE INDEPENDENCE — clearing gate 5 does NOT imply go-live; the final verdict + the three STOP-AND-ASK hard-stops remain the user's. If any cannot hold, STOP and report.
Depends on: none
Commit boundary: TWO separate lanes. (1) THIS authoring lane commits ONLY the queue governance (queue.md/linted/reviewed/governed) + reviews/queue-review-134.md — no test/src/report change, no implementation. (2) The FUTURE execution lane commits ONLY the flaky test change + the minimal src readiness-emit + the gate-5 evidence-row update to go-live-readiness-report.md + its own review/audit artifact under dev-memo/run/reviews/, records its before/after test evidence + any cc-suite audit chain in the COMMIT MESSAGE, and does NOT re-stage this lane's queue governance. services/ocr-worker/dist/** is NEVER staged (gitignored). NO fetcher/TLS/DNS/engine/pipeline/schema/contract/migration/native/dependency file in either lane; NO staging of dev-memo/run/intake/ or untracked residue; push is NEVER automatic (autonomy hard-stop) — local commit governance is the default, any branch push + PR open happens ONLY under explicit per-invocation user authorization; no merge; no branch delete. THIS authoring lane implements NOTHING — it only governs the gate-5 flake WI.
