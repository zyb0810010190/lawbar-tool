## WI-EVIDENCE-A3-T10-ANCHOR-REGRESSION-GATE-00: build native anchor-regression gate (Type: IMPL)
Type: IMPL
Scope: Turn the currently-`not_implemented` A3-T10 `a3-regression` anchor-resolution gate into a REAL, deterministic, fixture-backed native harness in `native/evidence-core-swift/**`, following the A0.7 + A1-T6 pattern (real harness + committed synthetic fixtures + INDEPENDENT oracle + classified verdicts; inconclusive/not_implemented/malformed never pass). NO A8/A10 implementation; NO CanonicalExportModel; NO A8 restore/reproduce; NO JS shim wiring; NO custody/marker; NO schema/contract/persistence/UI change; NO A3 RUNTIME behavior change (this is a harness over synthetic fixtures — the resolver logic is REPLICATED to match the built contract, not edited). Grounding (verified on main): A3-T10 per the TRACKED handover `docs/reference/evidence-genie-m0-developer-handover.md` (line 137 "Regression suite · reopen/freeze identical; quarantine scenarios. CI gate. Verify: a3-regression"; §8 line 103 "anchors round-trip end-to-end identically, incl. quarantine/replacement scenarios"). A3 anchor RESOLUTION (LinkStatus valid|needs_review|broken + the V12 durable `unlinked_at` marker) is DATA-driven (anchor geometry-version, page/geometry presence, document supersession, unlink marker) — NOT PDF geometry, NOT A8 snapshot, NOT A10 export/CanonicalExportModel (A8.6 DEPENDS on A3-T10, not vice versa). The gate exists today ONLY as the JS-shim `not_implemented` (native/evidence-core/lib/commands.mjs) + README; NO Swift harness exists yet. The BUILT resolver contract to MATCH (drift guard) is the strict precedence ladder in `services/case-box-persistence/src/sqlite/linkStatusResolverQueries.ts` (SQL CASE): (0) `unlinked_at` present -> broken; (1) missing anchor target -> broken; (2) missing V9 page identity -> broken; (3) missing V10 geometry record -> broken; (4) `anchor.geometry_captured_at <> current page geometry captured_at` -> needs_review; (5) anchor's document superseded (another document supersedes it) -> needs_review; (6) else -> valid (per ADR `docs/adr/ADR-evidence-a3-resolver-status-transitions.md` §3 + `ADR-evidence-a3-durable-unlink-schema.md`). Approach (mirroring WI-EVIDENCE-A1-T6-CITATION-STABILITY-GATE-00): (1) add `native/evidence-core-swift/Sources/EvidenceCoreSmoke/A3AnchorRegressionHarness.swift` that READS a committed synthetic fixture (JSON: documents{id, supersededBy?}, pages{documentId, physicalPageIndex}, geometries{documentId, physicalPageIndex, capturedAt}, anchors{anchorId, documentId, physicalPageIndex, geometryCapturedAt}, links{linkId, anchorId, unlinkedAt?}) + a committed INDEPENDENT oracle (expected per-link resolved status valid|needs_review|broken + an `unlinked` flag to DISTINGUISH the V12 explicit-unlink from a structural broken — derived from the fixture's KNOWN construction, NOT back-filled), classifies each link by the replicated ladder, and emits a deterministic classified verdict; (2) prove the resolution is byte-identical across >=2 runs (reopen/freeze-identical stability) + deterministic ordering (by linkId) + independence from machine paths/timestamps/locale/renderer metadata; (3) cover the required states — valid, unknown/missing anchor (broken), missing-geometry (broken), unlinked (broken+marker), relinked (unlinkedAt cleared -> re-resolves to valid), version-pinned mismatch (needs_review), document-superseded/replacement (needs_review); (4) add a CLI `a3-regression-cli` (thin, mirroring a07/a1 CLIs) emitting a tab-separated verdict; (5) committed synthetic/public fixtures only; verdict vocabulary mirrors A0.7/A1-T6 (pass / fail-mismatch / inconclusive / fixture_or_oracle_invalid / not_implemented) with inconclusive + malformed fixture/oracle NEVER pass and not_implemented = FAIL. Keep the A0.7 + A1-T6 harnesses/tests/fixtures unchanged (regression). Do NOT wire the JS shim (A07-GATE-00 §6 — native is the gate path).
Source of truth: docs/reference/evidence-genie-m0-developer-handover.md (A3-T10 line 137; A3-T5 resolver line 130; §8 line 103; A3 invariant §3 line 36; LinkStatus data-model); services/case-box-persistence/src/sqlite/linkStatusResolverQueries.ts (the BUILT resolver ladder the Swift harness MUST match — drift guard); docs/adr/ADR-evidence-a3-resolver-status-transitions.md (A3-RESOLVE-00 §3 precedence ladder); docs/adr/ADR-evidence-a3-durable-unlink-schema.md (V12 unlinked_at marker + relink); docs/adr/ADR-evidence-a3-anchor-link-contract.md (INV-A3-1..A3-10); native/evidence-core-swift/Sources/EvidenceCoreSmoke/{A07ConformanceHarness,A1CitationStabilityHarness}.swift + Sources/{A07HarnessCLI,A1CitationStabilityCLI}/main.swift + Package.swift (the harness/CLI/oracle-independence PATTERN to mirror); docs/adr/ADR-evidence-a8-snapshot-integrity-confidentiality.md (A8.6 depends on A3-T10 — must NOT implement A8); native/evidence-core/lib/commands.mjs (a3-regression not_implemented); .claude/rules/{evidence-genie,cc-suite,autonomy,execution-discipline,staging-hygiene,security-boundary,client-local-first}.md
Allowed files: native/evidence-core-swift/Sources/EvidenceCoreSmoke/A3AnchorRegressionHarness.swift, native/evidence-core-swift/Sources/A3RegressionCLI/main.swift, native/evidence-core-swift/Package.swift (ONLY to register the new A3 CLI executable target/product, mirroring a07/a1 — review-plan-approved; else fold into an existing target + leave Package.swift unchanged), native/evidence-core-swift/Tests/EvidenceCoreSmokeTests/A3AnchorRegressionHarnessTests.swift, native/evidence-core-swift/Tests/EvidenceCoreSmokeTests/Fixtures/** (new synthetic anchor/link regression fixtures + independent oracle + README), dev-memo/run/queue.md, dev-memo/run/queue.linted, dev-memo/run/queue.reviewed, dev-memo/run/queue.governed, dev-memo/run/reviews/queue-review-110.md
Forbidden files: native/evidence-core/** (NO JS shim wiring; commands.mjs/cli.mjs UNCHANGED — A07-GATE-00 §6), native/evidence-core-swift/Sources/EvidenceCoreSmoke/{A07ConformanceHarness,A1CitationStabilityHarness,PageBoxProbe,PdfKitProbe,PdfLoadProbe,CoordinateRoundtripProbe,EvidenceCoreSmoke}.swift (A0.7/A1-T6/probe code — NO change; regression only), native/evidence-core-swift/Sources/{A07HarnessCLI,A1CitationStabilityCLI}/** (NO change), native/evidence-core-swift/Tests/EvidenceCoreSmokeTests/{A07ConformanceHarnessTests,A1CitationStabilityHarnessTests}.swift + Fixtures/{messy,a07-renderer-conformance,a1-citation,synthetic-twopage.pdf}/** (A0.7/A1-T6 fixtures — NO change), apps/** + services/** + docs/contracts/** (NO product/schema/contract/persistence/UI/electron change), services/case-box-persistence/src/** (NO resolver/source/schema change — READ-ONLY reference for the drift guard), scripts/** + .claude/** + .github/** + migrations/**, docs/adr/** (READ-ONLY predecessors), dev-memo/design/** (NO separate design artifact unless review-plan requires), dev-memo/run/evidence/** (gitignored custody marker/ledger — never touched/staged), committed keys, dev-memo/run/reports/** + dev-memo/run/override-reason.md + dev-memo/run/autonomous-next.md (untracked run-state — never staged/edited), Evidence-Genie-M0-Developer-Handover.md + xiaolai-dev-workflow-study.md (root intake — READ-ONLY), native/evidence-core-swift/.build/** (gitignored — never staged); NO A8/A10/CanonicalExportModel/restore implementation; NO A3 runtime change; NO custody/marker; NO HMAC/key request/receive/print/persist/stage; NO confidential/client/court fixture; NO push/merge/branch-delete
Gates: broker /cc-suite:review-plan REQUIRED before implementation — confirm: A3-T10 is scopable as a native anchor-resolution regression gate over synthetic JSON WITHOUT inventing A8/A10 (it is — resolution is data-driven); the Swift resolver replication MATCHES the built ladder (linkStatusResolverQueries.ts) without drift, and replicating-vs-referencing is acceptable for the native gate; the V12 unlinked/relink distinction + version-pinning + supersession are modeled correctly; oracle independence; whether registering a new `a3-regression-cli` executable in Package.swift is acceptable (mirrors a07/a1) or should fold into an existing target; NO JS shim, NO schema/contract, NO A8/A10/A3-runtime change. STOP-points: (1) if A3-T10 undefined/contradicted by A3 docs -> STOP; (2) if the only way to pass is to weaken/skip/relabel -> STOP; (3) if the harness cannot exercise a real native deterministic path -> STOP; (4) if A3 regression cannot be tested without A10 CanonicalExportModel -> STOP; (5) if deterministic synthetic fixtures cannot be created -> STOP; (6) if a fixture needs confidential/client/court material -> STOP; (7) if schema/package/tarball/lockfile change becomes necessary WITHOUT review-plan approval (a Package.swift executable-target registration IS contemplated + allowed ONLY with review-plan approval; else STOP) -> STOP; (8) if custody/key handling would expose a key -> STOP; (9) if review-plan says split into design/discovery first -> STOP. If review-plan NEEDS-FIX on approach, fix-forward; if a blocker, STOP. After implementation: `swift test` passes incl. A0.7 + A1-T6 regressions; the A3-T10 gate no longer reports not_implemented on the native path; the A3 CLI against the synthetic fixture(s) returns the expected pass; a repeat-run test proves byte-identical resolution; tests cover valid/broken(missing-anchor & missing-geometry)/unlinked/relinked/version-pinned/superseded; a negative/malformed test proves mismatch/inconclusive/invalid is NOT pass; `scripts/workflow/check-contract-integrity.sh` exits 0; broker /cc-suite:audit + /cc-suite:verify (no Critical/High/Medium open before commit). NO A0.7 custody/marker in this lane. Batch closeout is ALREADY due (3 commits past marker b4f260a) and will be re-due after this commit — run the Layer-B closeout before PR; record any Low per the PR #156 precedent.
Acceptance criteria: `native/evidence-core-swift` `swift test` MUST pass (exit 0, 0 failures) INCLUDING the unchanged A0.7 + A1-T6 tests (regression); a NEW Swift A3 anchor-regression harness exists exercising a REAL native deterministic path that resolves link status from synthetic anchor/link/geometry/document fixture data; the A3-T10 gate no longer reports `not_implemented` on the native path (a real classified verdict is produced; the JS shim stays `not_implemented` until a separate wiring WI); a direct CLI/gate invocation returns a real `pass` verdict on >=1 committed deterministic synthetic fixture + its independent oracle; regression coverage PROVES clean(valid) / unknown-or-missing-anchor(broken) / missing-geometry(broken) / unlinked(broken+marker, distinct from structural broken) / relinked(unlinkedAt cleared -> valid) / version-pinned-mismatch(needs_review) / document-superseded(needs_review); a repeat-run test proves resolution is BYTE-IDENTICAL across >=2 runs and ORDERING is deterministic; tests prove resolution does NOT depend on machine paths / timestamps / locale / renderer metadata; the harness distinguishes pass / mismatch / fixture_or_oracle_invalid / inconclusive / not_implemented, and inconclusive + malformed fixture/oracle are NOT pass; the replicated ladder MATCHES the built resolver (linkStatusResolverQueries.ts); NO A8/A10/CanonicalExportModel/restore implementation; NO A3 runtime/source change; NO confidential fixture; NO change to native/evidence-core/** (JS shim), A0.7/A1-T6 harness/fixtures, schema, contracts, services, apps, scripts, .claude, ADRs; `CURRENT_SCHEMA_VERSION` remains 12; `dev-memo/run/evidence/**` untouched; no A0.7 HMAC/custody key requested/received/printed/persisted/staged; `check-contract-integrity` exits 0; broker review-plan READY + audit no Critical/High/Medium + verify ALL CLOSED; committed diff confined to Allowed files.
Risk flags: HIGH-RISK Evidence-invariant lane (A3-T10 is a permanent CI gate that A8.6 restore-and-reproduce depends on) — but native-harness/test/fixture coverage only; NO product/schema/contract/UI/marker change, NO A3 runtime change. Load-bearing care points: (a) the Swift resolver replication MUST match the built ladder (linkStatusResolverQueries.ts SQL CASE) exactly — precedence broken(unlinked/missing) > needs_review(version-mismatch/superseded) > valid — no drift; (b) preserve the A3 distinction between clean/broken/unlinked/relinked (the resolver status is `broken` for BOTH unlinked + structural; the harness output MUST also carry the `unlinked` marker to distinguish them, mirroring export's UNLINKED); (c) oracle values derived from fixture construction, INDEPENDENT of harness output; (d) inconclusive/not_implemented/malformed NEVER pass; (e) fixtures synthetic/public ONLY (never confidential); (f) NO JS shim wiring (§6), NO marker/custody, NO dev-memo/run/evidence/** touch, NO key handling, NO A8/A10/CanonicalExportModel/A3-runtime change; (g) Package.swift executable registration ONLY with review-plan approval. If any cannot hold, STOP and report. No push/merge/branch-delete.
Depends on: none
Commit boundary: governance (mark-reviewed + govern) run STANDALONE before the commit (never bundled with git commit). ONE implementation commit — native/evidence-core-swift Sources (A3AnchorRegressionHarness.swift [+ A3RegressionCLI/main.swift] [+ Package.swift ONLY if review-plan approved]) + Tests (A3AnchorRegressionHarnessTests.swift) + Fixtures/** (new synthetic anchor/link fixtures + oracle + optional README) + governance artifacts (queue.md/.linted/.reviewed/.governed + reviews/queue-review-110.md), staged by exact path; NO native/evidence-core/** (JS shim); NO A0.7/A1-T6 harness/fixture file; NO services/schema/contract/apps/scripts/.claude file; NO marker/evidence/key/report file; NO .build artifact; no push; no merge; no branch delete.
