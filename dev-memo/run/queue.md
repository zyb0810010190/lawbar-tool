## WI-WH1: test-environment hygiene (postdist native-arch restore) + governance-sequencing note
Type: WORKFLOW
Scope: WORKFLOW-only hygiene; NO product/source/security-hook change. (1) Add `"postdist": "electron-builder install-app-deps"` to the `scripts` block of `apps/lawbar-desktop/package.json` (place it adjacent to the existing `dist`/`postinstall` scripts). Rationale: `npm run dist` runs electron-builder which `@electron/rebuild`s `better-sqlite3` for the `mac.target` arches (`arm64` THEN `x64`), leaving the host `node_modules/better-sqlite3/build/Release/better_sqlite3.node` built x86_64 on an arm64 host; the dev-Electron smoke tests (`tests/smoke.electron.test.mjs`, `tests/main.test.mjs`) then fail with 30s launch timeouts until the host binding is rebuilt. `postdist` is an npm lifecycle hook that auto-runs after `npm run dist`, restoring the host-arch binding via `electron-builder install-app-deps` (the canonical native-rebuild path, already the `postinstall` step); it does NOT run on `npm test` (so normal runs are unaffected) and does NOT alter the already-built dist bundles (it runs AFTER packaging). Do NOT change any dependency, version, packageManager/engines metadata, lockfile, or any other script. (2) Add a CONCISE operational note to `AGENTS.md` (near §"Test commands (per package/service)") documenting: (i) `npm run dist` can leave the dev-Electron `better-sqlite3` native binding on the packaging-target arch; `postdist` (or a manual `electron-builder install-app-deps`) restores the host binding; (ii) the governance-sequencing rule — NEVER bundle `mark-queue-reviewed.sh`/`govern-queue.sh` with the dependent `git commit` in ONE Bash tool call, because `batch-commit-guard.sh` is a PreToolUse hook that checks the PRE-refresh governance state and denies the WHOLE call before govern runs (queue.governed never refreshes); the correct procedure is to run mark-reviewed/govern STANDALONE, verify the content-bound state, then `git commit` in a SEPARATE Bash call. Keep the note operational + concise; do NOT touch any enforcement/gate/hard-stop wording or the commit policy. (3) Record the SEPARATE `block-run-control-bash-write.sh` unanchored-`dev-memo/run/`-substring issue as a tracked Low (finding id BRCBW-SUBSTR) in `dev-memo/deferred-audit-findings.md`, status open/deferred — DO NOT fix it here (it is a 538-line security guard; anchoring needs the full security-WI loop and risks weakening protection; low operator-friction value). If ANY of these requires touching `.claude/hooks/**`, `scripts/workflow/**`, govern-queue.sh, batch-commit-guard.sh, block-run-control-bash-write.sh, governance semantics, dependencies/versions/lockfiles, product/source/renderer/electron/services/contracts, or a test `*.mjs`, STOP and report.
Source of truth: apps/lawbar-desktop/package.json (the scripts block: dist=`npm run build && electron-builder`, postinstall=`electron-builder install-app-deps`, mac.target arch ["arm64","x64"]); dev-memo/plan-desktop-package-architecture-00.md C7 (electron-builder install-app-deps is the proven native-rebuild path); .claude/settings.json (the two PreToolUse Bash hooks block-run-control-bash-write.sh + batch-commit-guard.sh — READ-ONLY context for the note, do NOT edit); AGENTS.md §"Test commands" (insertion anchor); dev-memo/plan-batch-workflow-test-env-hygiene-00.md (proposal)
Allowed files: apps/lawbar-desktop/package.json, AGENTS.md, dev-memo/deferred-audit-findings.md
Forbidden files: .claude/hooks/**, scripts/workflow/**, apps/lawbar-desktop/src/**, apps/lawbar-desktop/renderer/**, apps/lawbar-desktop/electron/**, services/**, docs/contracts/**, dev-memo/run/**, apps/lawbar-desktop/tests/**, .claude/rules/**, .claude/settings.json, package-lock.json, apps/lawbar-desktop/package-lock.json
Gates: npm --prefix apps/lawbar-desktop test
Acceptance criteria: `apps/lawbar-desktop/package.json` has exactly one new `scripts` key `"postdist": "electron-builder install-app-deps"` and NO other change (no dependency/version/packageManager/engines/lockfile/other-script change — verify the diff is the single scripts line); AGENTS.md has a concise note covering the postdist native-arch restoration + the govern/commit-sequencing rule, touching no gate/enforcement/commit-policy wording; BRCBW-SUBSTR recorded as an open deferred Low in dev-memo/deferred-audit-findings.md (NOT fixed); `npm --prefix apps/lawbar-desktop test` passes (525 pass); heavy acceptance run if feasible (`npm run dist` -> better_sqlite3.node restored to arm64 after postdist -> smoke tests green) else documented why; NO .claude/hooks/** , scripts/workflow/**, security-hook, governance-semantics, dependency/version/lockfile, product/source/renderer/electron/services/contracts, or test *.mjs change
Risk flags: WORKFLOW-only (test-harness config + doc + ledger); no product/behavior change; the security hooks (block-run-control-bash-write.sh, batch-commit-guard.sh) are READ-ONLY context and explicitly Forbidden — this WI documents them, never edits them; postdist is an npm lifecycle hook that runs AFTER packaging so it cannot corrupt the built bundles; AGENTS.md edit must not weaken any gate/hard-stop/commit-policy wording (concise operational note only); low-risk WORKFLOW WI -> cc-suite self-review permissible per §"Low-risk WIs", though review-plan on the queue runs; heavy postdist acceptance (npm run dist) is expensive + mutates node_modules (gitignored) — run if feasible, document otherwise
Depends on: none
Commit boundary: one local commit for the postdist script + the AGENTS.md note + the BRCBW-SUBSTR deferred row
