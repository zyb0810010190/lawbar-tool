## WI-ENA7: A0.7 fixture/oracle materialization (data/docs only; not the harness)
Type: ASSET
Scope: Materialize the synthetic/public fixture reference + the independent oracle artifacts the future A0.7 renderer-conformance gate (ADR A07-GATE-00) requires (§2 fixtures, §3 oracle). REUSE the existing synthetic two-page PDF (native/evidence-core-swift/Tests/EvidenceCoreSmokeTests/Fixtures/synthetic-twopage.pdf); do NOT duplicate the PDF unless review-plan explicitly requires a new fixture. Add a manifest + oracle (JSON DATA) + a README under native/evidence-core-swift/Tests/EvidenceCoreSmokeTests/Fixtures/a07-renderer-conformance/ recording: fixture path, provenance, sha256, size, page count, page-box assumptions (mediaBox origin+extent), page-index assumptions (0-based PDFKit), expected values (page count 2; per-page mediaBox 612x792; deterministic sample-point normalize/denormalize expectations using nx=x/width, ny=y/height; roundtrip), tolerance rules (1e-9 absolute), and pass/fail/inconclusive interpretation (inconclusive != pass). The oracle expected values are written down as data/docs BEFORE any harness exists and are INDEPENDENT of future harness code. This lane adds fixture/oracle DATA + documentation ONLY: NO renderer-conformance implementation, NO real A0.7 execution, NO A0.7 marker, NO marker provenance/HMAC implementation, NO tamper/fabrication guard implementation, NO Swift/native executable logic, NO .swift/.mjs/.ts/.js implementation changes, NO JS-shim change, NO anchors, NO page-identity/citation logic, NO persistence, NO PDFView/UI behavior, NO Evidence UI/product behavior, NO dev-memo/run/evidence/**. The artifacts must state: fixtures/oracle only (not production anchor geometry); inconclusive is not pass; not_implemented is fail; no marker can be written from these artifacts alone. Do NOT delete/move the root intake files.
Source of truth: docs/adr/ADR-evidence-a07-renderer-conformance-gate.md (A07-GATE-00 §2 fixtures, §3 oracle, §4 classification, §5 marker, §8 sequencing); docs/adr/ADR-evidence-native-core-a07-feasibility.md (ENA-00); .claude/rules/evidence-genie.md (invariants 3 A0.7-first, 6 anchors page-ratio, 10 not_implemented-fails); native/evidence-core-swift/Tests/EvidenceCoreSmokeTests/Fixtures/synthetic-twopage.pdf (the existing fixture being referenced, sha256 63d91a6c..., 432 bytes, 2 pages, mediaBox 612x792); the ENA3/ENA4/ENA5 probes (page count, mediaBox read, coordinate roundtrip) that established these deterministic values; .claude/rules/autonomy.md (this docs/data lane is user-authorized).
Allowed files: native/evidence-core-swift/Tests/**/Fixtures/** (fixture/oracle DATA files: manifest.json, oracle.json, and a README/docs adjacent), dev-memo/** (queue/governance/audit/study artifacts), docs/adr/** (ONLY for a short cross-reference if needed; prefer not to)
Forbidden files: native/evidence-core-swift/Sources/** and native/evidence-core-swift/Tests/**/*.swift (NO Swift source/test code change — prefer no .swift changes; a docs-only README clarification only if review-plan explicitly requires), native/evidence-core/** (the JS shim — NO change), apps/**, services/**, docs/contracts/**, docs/product/**, .claude/hooks/**, .claude/settings.json, .claude/rules/**, AGENTS.md, CLAUDE.md, GEMINI.md, .github/workflows/** (unless review-plan explicitly requires a docs-only path clarification), dev-memo/run/evidence/**, Evidence-Genie-M0-Developer-Handover.md (root), xiaolai-dev-workflow-study.md, package.json; NO .swift/.mjs/.ts/.js implementation changes; NO renderer-conformance/harness/marker/HMAC/provenance/tamper-guard implementation code
Gates: broker /cc-suite:review-plan REQUIRED before materializing (these artifacts become FUTURE gate authority — the oracle the harness will be measured against; no self-review); if review-plan returns READY but the WI scopes into harness/marker/code IMPLEMENTATION (any .swift/.mjs/.ts/.js executable logic, harness, marker, provenance, tamper guard), STOP and ask; broker /cc-suite:audit + /cc-suite:verify after materializing (the oracle is future gate authority); manifest+oracle are valid JSON; the fixture is REUSED (no duplicate PDF) unless review-plan approves a new one; the oracle records sha256, size, page count, page boxes, page-index convention, expected values, tolerance, and pass/fail/inconclusive (inconclusive!=pass); the artifacts state not-production-anchor-geometry + not_implemented-is-fail + no-marker-from-artifacts-alone; NO .swift/.mjs/.ts/.js change; NO renderer-conformance/harness/marker/provenance/tamper-guard code; NO dev-memo/run/evidence/**; the native/evidence-core JS shim is byte-unchanged; scripts/workflow/check-contract-integrity.sh exits 0; npm --prefix apps/lawbar-desktop test passes (610/610); scripts/workflow/check-gates.sh returns GATES OK
Acceptance criteria: native/evidence-core-swift/Tests/EvidenceCoreSmokeTests/Fixtures/a07-renderer-conformance/ contains manifest.json + oracle.json (valid JSON) + a README; manifest records the reused fixture path (../synthetic-twopage.pdf), provenance, sha256 (63d91a6c...), size (432), page count (2), page-index convention (0-based), and per-page mediaBox (612x792); oracle records expected page count/indexes/mediaBox extents, the normalization formula, deterministic sample-point normalized expectations, tolerance (1e-9), and pass/fail/inconclusive with inconclusive!=pass + not_implemented=fail + no-marker-from-artifacts-alone + not-production-anchor-geometry; the manifest sha256 matches the actual fixture bytes; NO new PDF fixture was added (existing reused); NO .swift/.mjs/.ts/.js implementation code changed; NO renderer-conformance/harness/marker/provenance/tamper-guard code anywhere; NO dev-memo/run/evidence/**; the native/evidence-core JS shim is byte-unchanged; check-contract-integrity PASS; npm desktop test pass 610 fail 0; check-gates GATES OK; broker review-plan READY (scopes data/docs-only, not implementation) and broker audit returns no Critical/High/Medium with verify ALL CLOSED; the committed diff is confined to native/evidence-core-swift/Tests/**/Fixtures/a07-renderer-conformance/** plus dev-memo/** governance artifacts
Risk flags: HIGH-RISK -- the oracle becomes FUTURE A0.7 gate authority (the independent expected-values the harness will be judged against); a wrong/code-derived oracle could later validate a broken harness, so it gets broker review-plan + audit + verify even though it is data/docs-only. Scope strictly limited to fixture/oracle DATA + docs: no Swift/JS executable logic, no harness, no marker, no provenance/HMAC, no tamper guard, no dev-memo/run/evidence/**, JS shim untouched, existing PDF reused (no duplicate). Docs/data-only and revertable as one commit.
Depends on: none (A07-GATE-00 gate design ADR and the ENA3/ENA4/ENA5 probes are already merged to main)
Commit boundary: one local commit for the fixture/oracle artifacts -- native/evidence-core-swift/Tests/EvidenceCoreSmokeTests/Fixtures/a07-renderer-conformance/** (manifest.json, oracle.json, README.md), staged by exact path; governance commit separate; no push
