## WI-RELEASE-G8-DEFERRED-LOWS-RECONFIRM-00: define + govern the M0 gate-8 deferred-Lows reconfirmation (re-confirm each open deferred Low is still Safe=YES + not an M0 blocker; triage still-valid vs closed/duplicate) (Type: EVIDENCE, release-governance MEDIUM risk)

## Review packet (compact)
- Active plan summary: gate 8 of the M0 go-live readiness matrix (`docs/release/go-live-readiness-report.md` §1 row 8) is **PARTIAL** — evidence "`dev-memo/deferred-audit-findings.md`: 37 open, ALL Low, ALL Safe=YES, 0 Medium+"; the outstanding item is "Re-confirm each Safe=YES at gate-close; none are M0-product blockers". This WI **governs** (does NOT execute) a FUTURE lane that runs that reconfirmation as a **read-only triage** of the deferred-findings registry (+ light source cross-reference to confirm any "closed" claim) and records the disposition as evidence. THIS authoring lane produces the governed queue WI only; it IMPLEMENTS NO product change, changes NO source/test/config, clears NO gate, and decides NO user go-live hard-stop.
- Grounding note (verified 2026-07-07 — the exec lane MUST re-verify against the real registry before relying, per `AGENTS.md` §"Authoring discipline"): `dev-memo/deferred-audit-findings.md` is a MIXED-format registry — older WIDE tables (multi-row `| Finding ID | Severity | … | Status | Notes |`, the Phase-A/B SQLite series + scaffold/enforcement sweeps) + newer VERTICAL single-finding tables (`| Finding ID | X |` blocks, ~37 rows incl. `LINK-IPC-T1-D1`, `A1T6-AUD-L1` (already `closed`), `G7-DRILL-AUD-L1`, `G12-STUDY235-AUD-L1`). Every open finding is Low + Safe=YES per the last refresh; the exec lane produces the authoritative recount + per-finding disposition (the gate-8 row's "37" is a snapshot and must be reconfirmed, since new Lows — G7-DRILL-AUD-L1, G12-STUDY235-AUD-L1 — were added after it).
- Exact target files (FUTURE exec lane): a NEW `docs/release/gate8-deferred-lows-reconfirm-00.md` (the reconfirmation disposition table + evidence record) + the gate-8 evidence ROW of `docs/release/go-live-readiness-report.md` (update to reference the reconfirmation; **gate 8 MUST remain `PARTIAL` in this WI** — add only a `[Δ] deferred-Lows reconfirmed: N open, all Safe=YES, 0 M0-blockers` marker using the report's EXISTING vocabulary; a `PARTIAL → CLEARED` move belongs to a SEPARATE holistic readiness-refresh WI, NOT this one — and MUST NOT clear gates 2/6/12/13/16/19/20 or imply any cleared) + `dev-memo/deferred-audit-findings.md` (STATUS-FIELD updates ONLY, and ONLY for a row the reconfirmation demonstrably closes/supersedes with a cited resolution commit — NO wholesale rewrite, NO new finding invented, NO severity change) + `dev-memo/run/reviews/` (the exec lane's review artifact). NO other file.
- Exact acceptance criteria: below.
- Exact out-of-scope: NO implementation of any deferred Low (this is a RECONFIRM, not a fix — fixing a Low is a SEPARATE follow-up WI); NO product source/test/package/config change; NO dependency change; NO schema/migration; NO contract change; NO new deferred finding invented; NO severity change of any finding; NO wholesale registry rewrite (STATUS-field-only updates of demonstrably-closed rows); NO gate-6 full-project audit; NO clearing of gates 2/6/12/13/16/19/20 (or implying any cleared); NO gate-7 D-G7 risk acceptance; NO broad readiness refresh; NO signing/notarization/公开 distribution decision (gate 4); NO 律师法 conclusion (gate 11); NO license/legal/business decision (gate 17); NO final GO/NO-GO (gate 21); NO source/test/package/config/schema/contract change in EITHER lane (docs-only tracked output).
- Requirements the FUTURE reconfirmation doc must define + prove (what the exec lane's reconfirmation must specify + record) — at minimum:
  1. **Read the registry first**: enumerate EVERY open finding in `dev-memo/deferred-audit-findings.md` (both wide-table + vertical formats), by Finding ID. Record the authoritative recount of OPEN Lows (the gate-8 row's "37" is reconfirmed or corrected).
  2. **Per-finding disposition**: map EACH open deferred Low to exactly one of — (a) **still-valid / carry-forward** (a genuine release residual, Safe=YES, not an M0 blocker); (b) **closed-by-later-evidence** (a later commit/WI demonstrably resolved it — cite the resolution commit; closure is confirmed ONLY from the cited commit/WI + a minimal targeted source/docs read, NOT a broad audit or a full fix-verification campaign); (c) **duplicate / superseded** (subsumed by another finding — cite it); (d) **needs-separate-follow-up-WI** (a real residual that warrants its own bounded WI, not fixed here). Record the disposition per Finding ID in a table.
  3. **Preserve evidence links**: for each finding, keep the original audit/review job id(s) + commit where available (do NOT lose provenance).
  4. **Safe=YES + not-an-M0-blocker reconfirmation**: for each still-open Low, reconfirm it is Safe=YES (its deferral does not weaken any current acceptance criterion) AND is NOT an M0-product blocker (it is scaffold/hygiene/test-hardening/doc-precision, not a court-facing correctness/security defect). Any finding that reconfirmation reveals is NOT actually Safe=YES or IS an M0 blocker MUST be surfaced (STOP-and-escalate), not silently carried.
  5. **Registry Status updates (bounded)**: where a finding is demonstrably closed/superseded, update ONLY its `Status` field in the registry to `closed`/`superseded` with the cited resolution commit (per the registry's own header convention) — the row stays visible. NO new finding, NO severity change, NO wholesale rewrite.
  6. **Deterministic pass/fail**: PASS = every open deferred Low is dispositioned; every still-open Low reconfirmed Safe=YES + not-an-M0-blocker; any close/supersede update is evidence-cited; NO open Low is a court-facing correctness/security defect. FAIL = any open Low is NOT Safe=YES, IS an M0 blocker, or cannot be dispositioned — surfaced, not papered over.
  7. **Expected evidence artifact**: the reconfirmation doc records the recount, the per-finding disposition table (Finding ID → category → Safe=YES? → M0-blocker? → evidence link), the count of still-open carry-forward Lows, any status updates made, and the date — captured as text.
  8. **Mode**: **read-only factual reconfirmation** — the ANALYSIS is read-only (read the registry + minimal cited-commit/WI + light source/docs cross-reference to confirm a "closed" claim); the only WRITES are the evidence doc, the gate-8 evidence-row TEXT, and the cited `Status`-field-only registry updates (per requirement 5). It writes NO product code, adds NO dependency, and fixes NO Low.
  9. **How the result feeds gate 8 without clearing unrelated gates**: a PASS supplies the "each Safe=YES re-confirmed; none are M0-product blockers" evidence. **Gate 8 MUST remain `PARTIAL`** — the exec lane adds ONLY a `[Δ] deferred-Lows reconfirmed` marker (existing vocabulary; roll-up bucket unchanged) and MUST NOT make a `PARTIAL → CLEARED` move (that belongs to a SEPARATE holistic readiness-refresh WI). It MUST NOT clear gates 2/6/12/13/16/19/20 or imply any cleared; go-live independence is kept.
  10. **Residual risks + follow-up WIs (if any)**: any finding dispositioned as "needs-separate-follow-up-WI" is listed as a proposed bounded WI (not opened here); the reconfirmation is a snapshot — new Lows accrue and the next gate-close reconfirms again.
- Dependencies mapped: **gate 6** (full-project audit — the deferred-Lows registry is one input to that sweep; LATER, NOT cleared); **gates 2/12/13/16/19/20** (adjacent readiness gates whose evidence may reference deferred Lows — NOT cleared). These are noted as related, NOT cleared by this WI.
- Essential refs: `dev-memo/deferred-audit-findings.md` (the registry — the ground truth; its header defines the row format + close/supersede convention) + `.claude/rules/cc-suite.md` §"Audit remediation policy" (the deferral rules + the durable-backlog-file requirement); `docs/release/go-live-readiness-report.md` §1 gate 8 (PARTIAL) + related gates 2/6/12/13/16/19/20; `AGENTS.md` §"Source hierarchy" + §"Authoring discipline"; `.claude/rules/security-boundary.md` §"Go-live independence".
- Review questions: (1) Does the WI correctly scope a READ-ONLY reconfirmation/triage of the deferred-Lows registry (disposition + evidence), NOT the implementation of any Low and NOT an execution in this authoring lane? (2) Does it require enumerating EVERY open Low (both registry formats) + an authoritative recount (the "37" is a snapshot)? (3) Does it require a per-finding disposition (still-valid / closed / duplicate / needs-follow-up) with preserved evidence links, + a Safe=YES + not-an-M0-blocker reconfirmation that STOPS-and-escalates any finding that fails it? (4) Does it bound registry edits to Status-field-only updates of demonstrably-closed rows (no rewrite, no new finding, no severity change)? (5) Does it keep gates 2/6/12/13/16/19/20 uncleared + gate 8 `PARTIAL`, decide no STOP-AND-ASK (4/11/17/21), and make no GO/NO-GO or readiness refresh? (6) Is any Low implementation/fix, product source/test change, new finding, severity change, or readiness refresh smuggled in?

Type: EVIDENCE
Scope: Govern (do NOT execute) a FUTURE lane that runs the M0 gate-8 deferred-Lows reconfirmation. The FUTURE execution lane produces `docs/release/gate8-deferred-lows-reconfirm-00.md` — defining requirements 1–10 above (read the registry first; per-finding disposition still-valid/closed/duplicate/needs-follow-up; preserve evidence links; Safe=YES + not-an-M0-blocker reconfirmation with STOP-and-escalate on failure; bounded Status-field-only registry updates; deterministic pass/fail; evidence artifact; read-only mode; how it feeds gate 8 without clearing others; residual risks) — then runs the read-only triage (read the registry + light source cross-reference), records the disposition, updates ONLY the gate-8 evidence row (gate 8 MUST remain `PARTIAL`; add a `[Δ] deferred-Lows reconfirmed` marker) + (bounded) the registry Status fields of demonstrably-closed rows. It reconfirms every open Low is Safe=YES + not an M0 blocker (STOP-and-escalate any that is not), fixes NO Low, invents NO finding, changes NO severity, does NO wholesale rewrite, and cites gates 2/6/12/13/16/19/20 WITHOUT clearing them. It PRESERVES every still-non-cleared gate + the STOP-AND-ASK gates (4/11/17/21); it makes NO final GO/NO-GO, resolves NO STOP-AND-ASK, runs NO gate-6 audit, IMPLEMENTS NO Low/product change, adds NO dependency, runs NO readiness refresh, and touches NO product source/test/schema/contract. THIS authoring lane implements + runs + decides NOTHING — it only governs the gate-8 reconfirmation WI. If review asks to implement/fix a Low, change product source/test, invent a finding, change a severity, clear a dependent gate, run gate 6, or run a readiness refresh, REJECT and report.
Source of truth: dev-memo/deferred-audit-findings.md (the registry — ground truth; header defines row format + close/supersede convention); .claude/rules/cc-suite.md §"Audit remediation policy" (deferral rules + durable-backlog-file requirement + deferred-finding recording fields); docs/release/go-live-readiness-report.md §1 gate 8 (PARTIAL) + gates 2/6/12/13/16/19/20; AGENTS.md §"Source hierarchy" + §"Authoring discipline"; .claude/rules/security-boundary.md §"Go-live independence"; .claude/rules/autonomy.md + staging-hygiene.md + loc-guardian.md
Allowed files: docs/release/gate8-deferred-lows-reconfirm-00.md (the NEW reconfirmation doc the FUTURE lane produces) + docs/release/go-live-readiness-report.md (the gate-8 evidence ROW ONLY — evidence-row TEXT ONLY, NO roll-up/status-bucket edit; gate 8 stays PARTIAL, its roll-up bucket unchanged) + dev-memo/deferred-audit-findings.md (STATUS-FIELD-only updates of demonstrably-closed/superseded rows, with cited resolution commit — NO new finding, NO severity change, NO wholesale rewrite) + dev-memo/run/reviews/ (the exec lane's own review artifact). NOTE: THIS authoring lane commits ONLY the queue governance (queue.md/linted/reviewed/governed) + reviews/queue-review-158.md — NO reconfirmation doc, NO registry edit, NO report change. The FUTURE exec lane records its evidence in the COMMIT MESSAGE + review artifact and does NOT re-stage this lane's queue governance.
Forbidden files: docs/release/ OTHER than the NEW gate8-deferred-lows-reconfirm-00.md + the gate-8 row of go-live-readiness-report.md (the other reports are READ-ONLY evidence — NOT edited), docs/adr/ + docs/product/ (READ-ONLY), docs/contracts/, apps/ + services/ (NO product source/test/renderer/IPC change — reconfirmation does NOT implement/fix any Low; a Low fix is a SEPARATE follow-up WI), services/*/src/sqlite/schema.ts (CURRENT_SCHEMA_VERSION stays 12), migrations/, native/, package.json/package-lock.json of any package (NO dependency change), dev-memo/run/intake/ (raw samples — never staged), dev-memo/ OTHER than run/reviews/ + the bounded Status-field updates to deferred-audit-findings.md (no other plan/design edit), .claude/ + .github/; NO Low IMPLEMENTATION/fix; NO new finding invented; NO severity change; NO wholesale registry rewrite; NO reconfirmation RUN in this authoring lane; NO gate-6 full-project audit; NO broad readiness refresh; NO clearing of gates 2/6/12/13/16/19/20; NO signing/notarization/public-distribution decision (gate 4); NO 律师法 conclusion (gate 11); NO license/legal/business decision (gate 17); NO gate-7 D-G7 risk acceptance; NO final GO/NO-GO decision (gate 21). Push is NEVER automatic (autonomy hard-stop): local commit governance is the default; any branch push + PR open happens ONLY under explicit per-invocation user authorization; NO merge; NO branch delete of shared branches
Gates: EVIDENCE/governance lane (MEDIUM release-governance risk — governs a deferred-Lows reconfirmation WI; no code, no Low fix, no go-live decision). (1) THIS authoring lane: broker /cc-suite:review-plan on this queue WI, Path 1, retrievable, then govern. (2) The FUTURE exec lane: RUN the read-only reconfirmation (read the registry + disposition), produce the reconfirmation doc + the gate-8 row update + bounded registry Status updates, then broker /cc-suite:review-plan on the produced artifacts. (3) scripts/workflow/check-contract-integrity.sh exit 0; scripts/workflow/check-queue.sh PASS (both lanes). Forbidden-scope scan before commit: CURRENT_SCHEMA_VERSION EQUALS 12; no product source/test/package/schema/contract change; no Low fix; no new finding; no severity change; no wholesale rewrite; no gate-6 run; no readiness refresh; no gate 2/6/12/13/16/19/20 clear; no GO/NO-GO; no raw sample. STOP-points: review asks to implement/fix a Low, change product source/test, invent a finding, change a severity, clear a dependent gate, run gate 6, or run a readiness refresh → REJECT/STOP and report; a reconfirmation reveals an open Low is NOT Safe=YES or IS an M0 blocker → STOP-and-escalate; any forbidden file would change → STOP.
Acceptance criteria: check-queue and check-contract-integrity PASS and CURRENT_SCHEMA_VERSION EQUALS 12 and the diff CONTAINS only the queue governance (queue.md/linted/reviewed/governed) + reviews/queue-review-158.md and no docs/source/test/package/schema/contract change appears and no Low fix / no new finding / no severity change / no readiness refresh appears. The governed WI must DEFINE the ten reconfirmation requirements (read registry first; per-finding disposition; preserve evidence links; Safe=YES + not-M0-blocker reconfirm with STOP-and-escalate; bounded Status-only registry updates; deterministic pass/fail; evidence artifact; read-only mode; feeds-gate-8-without-clearing-others; residual risks), keep gates 2/6/12/13/16/19/20 uncleared, keep gate 8 `PARTIAL`, and MUST NOT decide a STOP-AND-ASK hard-stop or assert go-live. review-plan (this lane) must be RUN and must PASS (READY, or READY with only Low-risk clarifications applied).
Risk flags: MEDIUM (release-governance — a reconfirmation WI that could over-claim readiness, drift into fixing Lows, invent findings, or clear a dependent gate). Care points: (a) NO RECONFIRMATION RUN in this authoring lane — this lane only governs the WI; (b) RECONFIRM, NOT FIX — the exec lane triages + reconfirms; fixing a Low is a SEPARATE follow-up WI (no product source/test change); (c) BOUNDED REGISTRY EDIT — only Status-field updates of demonstrably-closed rows (cited commit), NO new finding, NO severity change, NO wholesale rewrite; (d) STOP-AND-ESCALATE — any open Low that reconfirmation reveals is NOT Safe=YES or IS an M0 blocker is surfaced, not carried; (e) DEPENDENT GATES UNCLEARED — gates 2/6/12/13/16/19/20 stay uncleared; gate 6 (full-project audit) consumes the registry later; (f) GO-LIVE INDEPENDENCE — a reconfirmation does NOT imply go-live; the final verdict + the STOP-AND-ASK hard-stops (4/11/17/21) remain the user's; (g) NO READINESS REFRESH — the holistic roll-up reconciliation is a separate lane. If any cannot hold, STOP and report.
Depends on: none
Commit boundary: TWO separate lanes. (1) THIS authoring lane commits ONLY the queue governance (queue.md/linted/reviewed/governed) + reviews/queue-review-158.md — no reconfirmation doc, no registry edit, no report change, no implementation, no decision. (2) The FUTURE execution lane commits ONLY the new docs/release/gate8-deferred-lows-reconfirm-00.md + the gate-8 evidence-row update to go-live-readiness-report.md + (bounded) Status-field updates to dev-memo/deferred-audit-findings.md + its own review artifact under dev-memo/run/reviews/, records its evidence (incl. the disposition table) in the COMMIT MESSAGE, and does NOT re-stage this lane's queue governance. NO product source/test/package/schema/contract file in either lane; NO Low fix; NO new finding; NO severity change; NO wholesale registry rewrite; NO dependency change; NO staging of dev-memo/run/intake/ or untracked residue; push is NEVER automatic (autonomy hard-stop) — local commit governance is the default, any branch push + PR open happens ONLY under explicit per-invocation user authorization; no merge; no branch delete of shared branches. THIS authoring lane implements NOTHING, runs NO reconfirmation, and decides NOTHING — it only governs the gate-8 deferred-Lows reconfirmation WI.

## Acceptance criteria (FUTURE execution lane — what the governed reconfirmation doc must satisfy)

These describe the deliverable the FUTURE exec lane must produce; they are NOT preconditions of THIS authoring lane. The authoring-lane acceptance is the structured `Acceptance criteria:` field above (queue governance + review READY only — the reconfirmation doc does NOT exist yet and MUST NOT be created in this lane).

1. A NEW `docs/release/gate8-deferred-lows-reconfirm-00.md` exists defining requirements 1–10 above and recording the reconfirmation disposition table + result.
2. The reconfirmation enumerated EVERY open deferred Low (both registry formats), recorded the authoritative recount, and mapped each to one disposition (still-valid / closed / duplicate / needs-follow-up) with preserved evidence links.
3. Each still-open Low is reconfirmed Safe=YES + not an M0-product blocker; any finding that fails this is STOPPED-and-escalated (not silently carried). Registry Status updates (if any) are Status-field-only on demonstrably-closed rows with a cited resolution commit — no new finding, no severity change, no wholesale rewrite.
4. The gate-8 ROW references the reconfirmation; gate 8 **MUST remain `PARTIAL`** — add ONLY a `[Δ] deferred-Lows reconfirmed` marker (roll-up bucket unchanged); any `PARTIAL → CLEARED` move belongs to a SEPARATE holistic readiness-refresh WI. MUST NOT clear gates 2/6/12/13/16/19/20 or imply any cleared. Roll-up-line semantics kept consistent.
5. The doc defers gate 6 (later) + keeps go-live independence (no sign-off; gates 4/11/17/21 remain user STOP-AND-ASK).
6. NO product source/test/package/config/schema/contract change in either lane; NO Low fix; NO new finding; NO severity change; NO wholesale registry rewrite; `CURRENT_SCHEMA_VERSION` stays 12; docs-only tracked output.
7. `scripts/workflow/check-queue.sh` PASS + `scripts/workflow/check-contract-integrity.sh` PASS on both lanes.
