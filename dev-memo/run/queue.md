## WI-EVIDENCE-A1-T6-CITATION-STABILITY-GATE-00: build native citation-stability gate (Type: IMPL)
Type: IMPL
Scope: Turn the currently-`not_implemented` A1-T6 `citation-stability-gate` into a REAL, deterministic, fixture-backed native harness in `native/evidence-core-swift/**`, following the A0.7 pattern (real harness, committed deterministic synthetic fixtures + INDEPENDENT oracle, honest classified verdicts, no fake green). NO A8/A10/A3-T10 implementation; NO CanonicalExportModel; NO JS shim wiring; NO custody/marker; NO schema/contract/persistence/UI change. Grounding (verified on main): A1-T6 per the TRACKED handover `docs/reference/evidence-genie-m0-developer-handover.md` (§A1 ticket section line 120; §8 CI gate) = "Citation-stability gate · citation identity · **byte-identical across close/reopen/export; only from DocumentPage**". A1 citation identity derives SOLELY from `DocumentPage` data (`{documentId, physicalPageIndex, citationVolume?, citationPageLabel, citationPageSortKey?, isCitable}`; UNIQUE(documentId, physicalPageIndex); labels non-unique) via the A1-T5 generator (卷X页Y from DocumentPage; ambiguity→disambiguate-or-refuse+warn; non-citable handling) — NOT from PDF geometry, NOT from CanonicalExportModel (so A1-T6 is independent of A10; A10-T1 will LATER wrap this primitive into the export contract). The `citation-stability-gate` exists today ONLY as the JS-shim `not_implemented` (native/evidence-core/lib/commands.mjs) + README; NO Swift harness exists yet. Approach (mirroring WI-EVIDENCE-A0.7-FIXTURE-COVERAGE-00): (1) add a Swift A1 citation primitive + stability-gate harness `native/evidence-core-swift/Sources/EvidenceCoreSmoke/A1CitationStabilityHarness.swift` that READS a committed DocumentPage citation-map fixture (JSON) + a committed INDEPENDENT oracle (expected 卷X页Y citations + expected ambiguity/non-citable handling — derived from the fixture's known construction, NOT back-filled from harness output, per A07-GATE-00 §3 oracle-independence applied to A1), renders citations deterministically from DocumentPage ONLY, and emits a classified verdict; (2) prove citation identity is byte-identical across ≥2 independent runs over the same fixture (close/reopen stability) + deterministic ordering (by citationPageSortKey/physicalPageIndex) + independence from machine paths/current timestamps/locale/renderer metadata; (3) detect the A1 ambiguity invariant (a citation label mapping to >1 physical page in scope → refuse+warn, never guess) + isCitable=false → non-citable, as classified non-pass outcomes; (4) add a CLI `a1-citation-stability-cli` (thin, mirroring a07-harness-cli) emitting a deterministic tab-separated verdict; (5) committed synthetic/public fixtures only; verdict vocabulary mirrors A0.7 (pass / fail-mismatch / inconclusive / fixture_or_oracle_invalid / not_implemented) with `inconclusive` and malformed fixture/oracle NEVER pass and `not_implemented` = FAIL. Keep the A0.7 harness + tests unchanged (regression). Do NOT wire the JS shim (A07-GATE-00 §6 posture — native is the gate path).
Source of truth: docs/reference/evidence-genie-m0-developer-handover.md (A1 invariant §3 line 35; A1-T2 DocumentPage schema line 117; A1-T3 index↔label rules line 118; A1-T5 citation generator line 119; A1-T6 citation-stability gate line 120; §8 CI-gate list line 101; data-model DocumentPage block); native/evidence-core-swift/Sources/EvidenceCoreSmoke/A07ConformanceHarness.swift + Tests/EvidenceCoreSmokeTests/A07ConformanceHarnessTests.swift + Sources/A07HarnessCLI/main.swift (the A0.7 harness/CLI/oracle-independence PATTERN to mirror); services/case-box-persistence/src/sqlite/exportCitationQueries.ts (the BUILT TS 卷X页Y citation contract the Swift primitive must MATCH — `卷${citationVolume}页${citationPageLabel}`); native/evidence-core/lib/commands.mjs (citation-stability-gate not_implemented); docs/adr/ADR-evidence-a10-court-fileable-export.md (A10-T1 will later wrap this primitive — A1-T6 must not pre-empt CanonicalExportModel); .claude/rules/{evidence-genie,cc-suite,autonomy,execution-discipline,staging-hygiene,security-boundary,client-local-first}.md
Allowed files: native/evidence-core-swift/Sources/EvidenceCoreSmoke/A1CitationStabilityHarness.swift, native/evidence-core-swift/Sources/A1CitationStabilityCLI/main.swift, native/evidence-core-swift/Package.swift (ONLY to register the new A1 CLI executable target/product, mirroring the a07-harness-cli registration — if review-plan approves; else fold the gate into an existing target and leave Package.swift unchanged), native/evidence-core-swift/Tests/EvidenceCoreSmokeTests/A1CitationStabilityHarnessTests.swift, native/evidence-core-swift/Tests/EvidenceCoreSmokeTests/Fixtures/** (new synthetic DocumentPage citation-map fixtures + independent oracles + README), dev-memo/run/queue.md, dev-memo/run/queue.linted, dev-memo/run/queue.reviewed, dev-memo/run/queue.governed, dev-memo/run/reviews/queue-review-109.md
Forbidden files: native/evidence-core/** (NO JS shim wiring; commands.mjs/cli.mjs UNCHANGED — A07-GATE-00 §6), native/evidence-core-swift/Sources/EvidenceCoreSmoke/{A07ConformanceHarness,PageBoxProbe,PdfKitProbe,PdfLoadProbe,CoordinateRoundtripProbe,EvidenceCoreSmoke}.swift (A0.7/probe code — NO change; regression only), native/evidence-core-swift/Sources/A07HarnessCLI/** (NO change), native/evidence-core-swift/Tests/EvidenceCoreSmokeTests/A07ConformanceHarnessTests.swift + Fixtures/messy/** + Fixtures/synthetic-twopage.pdf + Fixtures/a07-renderer-conformance/** (A0.7 fixtures — NO change), apps/** + services/** + docs/contracts/** (NO product/schema/contract/persistence/UI/electron change), services/case-box-persistence/src/sqlite/{schema.ts,exportCitationQueries.ts} (NO schema/CURRENT_SCHEMA_VERSION/export-code change — READ-ONLY reference), scripts/** + .claude/** + .github/** + migrations/**, docs/adr/** (READ-ONLY predecessors), dev-memo/design/** (NO separate design artifact unless review-plan requires), dev-memo/run/evidence/** (gitignored custody marker/ledger — never touched/staged), committed keys, dev-memo/run/reports/** + dev-memo/run/override-reason.md + dev-memo/run/autonomous-next.md (untracked run-state — never staged/edited), Evidence-Genie-M0-Developer-Handover.md + xiaolai-dev-workflow-study.md (root intake — READ-ONLY), native/evidence-core-swift/.build/** (gitignored — never staged); NO CanonicalExportModel/A10/A8/A3-T10 implementation; NO custody/marker; NO HMAC/key request/receive/print/persist/stage; NO confidential/client/court fixture; NO push/merge/branch-delete
Gates: broker /cc-suite:review-plan REQUIRED before implementation — confirm: A1-T6 is scopable as a native citation-identity stability gate WITHOUT inventing A10 CanonicalExportModel (it is — citations derive from DocumentPage data); the Swift citation primitive (A1-T5 generator: 卷X页Y from DocumentPage; ambiguity refuse+warn; non-citable) MATCHES the BUILT TS contract (`卷${citationVolume}页${citationPageLabel}`) without drift, and whether re-implementing it in Swift vs referencing it is acceptable for the native gate surface; oracle independence (expected values from fixture construction, not harness output); whether registering a new `a1-citation-stability-cli` executable in Package.swift is acceptable (mirrors a07-harness-cli) or should be folded into an existing target; NO JS shim, NO schema/contract, NO A10/A8 pre-emption. STOP-points: (1) if A1-T6 is undefined/contradicted by A1 docs → STOP; (2) if the only way to pass is to weaken/skip/relabel the gate → STOP; (3) if the harness cannot exercise a real native path → STOP; (4) if citation stability cannot be tested without A10 CanonicalExportModel → STOP; (5) if deterministic synthetic fixtures cannot be created → STOP; (6) if a fixture needs confidential/client/court material → STOP; (7) if schema/package/tarball/lockfile change becomes necessary WITHOUT review-plan approval (note: a Package.swift executable-target registration IS contemplated above and is allowed ONLY if review-plan approves; otherwise STOP) → STOP; (8) if custody/key handling would expose a key → STOP; (9) if review-plan says split into design/discovery first → STOP. If review-plan NEEDS-FIX on approach, fix-forward; if a blocker, STOP. After implementation: `swift test` (native/evidence-core-swift) passes incl. the A0.7 regression; the A1-T6 gate no longer reports not_implemented; the A1 CLI/gate against the synthetic fixture(s) returns the expected pass; a repeat-run test proves byte-identical citation identity across ≥2 runs; a negative/malformed/ambiguous test proves mismatch/inconclusive/invalid is NOT pass; `scripts/workflow/check-contract-integrity.sh` exits 0; broker /cc-suite:audit + /cc-suite:verify (no Critical/High/Medium open before commit). NO A0.7 custody/marker in this lane.
Acceptance criteria: `native/evidence-core-swift` `swift test` MUST pass (exit 0, 0 failures) INCLUDING the unchanged A0.7 tests (regression); a NEW Swift A1 citation-stability harness exists exercising a REAL native path that renders citations from DocumentPage data ONLY; the A1-T6 gate no longer reports `not_implemented` (a real classified verdict is produced); a direct CLI/gate invocation returns a real `pass` verdict on ≥1 committed deterministic synthetic DocumentPage citation-map fixture + its independent oracle; a repeat-run test proves citation identity is BYTE-IDENTICAL across ≥2 runs over the same fixture (close/reopen stability) and ORDERING is deterministic; tests prove citation identity does NOT depend on machine paths / current timestamps / locale / renderer metadata; the harness distinguishes pass / mismatch / fixture_or_oracle_invalid / inconclusive / not_implemented, and `inconclusive` + malformed fixture/oracle + the ambiguity-refuse case are NOT treated as pass; the rendered citation MATCHES the built contract `卷X页Y` (`卷${citationVolume}页${citationPageLabel}`); NO CanonicalExportModel invented; NO confidential fixture; NO change to native/evidence-core/** (JS shim), A0.7 harness/fixtures, schema, contracts, services, apps, scripts, .claude, ADRs; `CURRENT_SCHEMA_VERSION` remains 12; `dev-memo/run/evidence/**` untouched; no A0.7 HMAC/custody key requested/received/printed/persisted/staged; `check-contract-integrity` exits 0; broker review-plan READY + audit no Critical/High/Medium + verify ALL CLOSED; committed diff confined to Allowed files.
Risk flags: HIGH-RISK Evidence-invariant lane (A1-T6 is a permanent CI gate that A8.6 + A10 reproducibility depend on) — but native-harness/test/fixture coverage only; NO product/schema/contract/UI/marker change. Load-bearing care points: (a) citations rendered from DocumentPage DATA only (never physicalPageIndex arithmetic, never CanonicalExportModel, never geometry) — preserve A1 invariant + A10-not-pre-empted; (b) the Swift 卷X页Y primitive MUST match the BUILT TS contract (no format drift) — review-plan to confirm re-impl-vs-reference; (c) oracle values derived from fixture construction, INDEPENDENT of harness output (A07-GATE-00 §3 applied to A1); (d) `inconclusive`/`not_implemented`/ambiguity-refuse/malformed NEVER pass; (e) fixtures synthetic/public ONLY (never confidential — AGENTS hard-stop); (f) NO JS shim wiring (§6), NO marker/custody, NO `dev-memo/run/evidence/**` touch, NO key handling, NO A10/A8/A3-T10 work; (g) a Package.swift executable-target registration is allowed ONLY with review-plan approval (else fold into existing target / STOP). If any cannot hold, STOP and report. No push/merge/branch-delete.
Depends on: none
Commit boundary: governance (mark-reviewed + govern) run STANDALONE before the commit (never bundled with git commit). ONE implementation commit — native/evidence-core-swift Sources (A1CitationStabilityHarness.swift [+ A1CitationStabilityCLI/main.swift] [+ Package.swift ONLY if review-plan approved]) + Tests (A1CitationStabilityHarnessTests.swift) + Fixtures/** (new synthetic citation-map fixtures + oracles + optional README) + governance artifacts (queue.md/.linted/.reviewed/.governed + reviews/queue-review-109.md), staged by exact path; NO native/evidence-core/** (JS shim); NO A0.7 harness/fixture file; NO schema/contract/services/apps/scripts/.claude file; NO marker/evidence/key/report file; NO .build artifact; no push; no merge; no branch delete.
