## WI-RELEASE-G19-SUPPLY-CHAIN-POSTURE-00: define + govern the M0 gate-19 supply-chain posture evidence lane (inventory + risk posture for v1 runtime/build dependencies, native binaries, committed tarballs, the OCR engine, and license/security-audit surfaces — a read-only `npm audit` + provenance sweep, NO dependency change, preserving the gate-17 legal/business decision boundary) (Type: EVIDENCE, release-governance MEDIUM risk)

## Review packet (compact)
- Active plan summary: gate 19 of the M0 go-live readiness matrix (`docs/release/go-live-readiness-report.md` §1 row 19) is **OPEN [Δ]** — its evidence already names `better-sqlite3` (native), `docx@9.7.1` (MIT/pure-JS; a prior dependency-risk-review found ACCEPTABLE, job `audit-mr63d6y7`), + two internal committed tarballs; its blocker is "Dependency inventory + license review + `npm audit` sweep + native-binary provenance for `better-sqlite3` + OCR engine; SBOM optional." This WI **governs** (does NOT execute) a FUTURE lane that RUNS that supply-chain inventory/audit as a **read-only** sweep and records the result as evidence. THIS authoring lane produces the governed queue WI only; it changes NO dependency/lockfile, changes NO source/test/config, clears NO gate, and decides NO user go-live / legal-business hard-stop.
- Grounding note (verified 2026-07-08 — the exec lane MUST re-verify against the real files before relying, per `AGENTS.md` §"Authoring discipline"): (a) **10 package.json manifests** (non-node_modules): `apps/lawbar-desktop`, `docs/contracts`, `docs/contracts/case-box-contract`, `native/evidence-core`, `services/case-box-persistence`, `services/ocr-{ingestion,persistence,review,worker,worker-bakeoff}`; **9 package-lock.json** (all but `native/evidence-core`). (b) **v1 Mac-client runtime deps** (`apps/lawbar-desktop/package.json`): `better-sqlite3 ^12.9.0` (native), `docx 9.7.1` (exact-pinned; MIT/pure-JS), `case-box-contract` + `case-box-persistence` as `file:dist-tarballs/*.tgz` (two committed internal tarballs under `apps/lawbar-desktop/dist-tarballs/`); devDeps `electron ^34`, `electron-builder ^25`, `@playwright/test ^1.50`, `playwright`, `@types/node ^22`, `typescript ^5.6`. (c) **`better-sqlite3 ^12.9.0`** in three manifests (`apps/lawbar-desktop`, `services/ocr-persistence`, `services/case-box-persistence`); `ocr-persistence` runs an `abi-smoke` pretest (native ABI check). (d) **OCR engine**: `services/ocr-worker` runtime dep `@gutenye/ocr-node ^1.4.8` + internal `file:` refs; `services/ocr-worker-bakeoff` (tesseract / paddleocr-onnx test harness) is **OUT of the production dependency graph** (ADR-11A.1) and excluded. (e) other services use workspace-internal `file:` path refs (not registry deps). (f) real-PaddleOCR is model-gated / post-v1 (gate-2 residual R-G2-3); the OCR worker is NOT wired into the v1 Mac client.
- Exact target files (FUTURE exec lane): a NEW `docs/release/gate19-supply-chain-posture-00.md` (the inventory + `npm audit` results + native-binary/tarball provenance + license inventory + pass/fail) + the gate-19 evidence ROW of `docs/release/go-live-readiness-report.md` (evidence-row TEXT ONLY; **gate 19 MUST remain `OPEN` in this WI** — enrich the row with a `[Δ] supply-chain inventory + audit + provenance recorded` marker using the report's EXISTING vocabulary; movement, if any, belongs to a SEPARATE holistic readiness-refresh WI — NO roll-up/status-bucket edit; gate 19 stays OPEN, its roll-up bucket unchanged — and MUST NOT clear gates 4/6/12/13/17/20 or imply any cleared) + `dev-memo/run/reviews/` (the exec lane's review artifact). NO other file. The exec lane's sweep is READ-ONLY: it MAY run `npm audit` / `npm ls` / read manifests + lockfiles + tarball metadata, but it MUST NOT run `npm install` / `npm audit fix` / `npm update` or otherwise mutate any `package.json` or `package-lock.json`.
- Exact acceptance criteria: below.
- Exact out-of-scope: NO dependency change (no add/remove/bump); NO `package-lock.json` mutation (the sweep is read-only — `npm audit` reads, it does NOT `install`/`fix`); NO product source/test/package/config change; NO OCR/Forms implementation; NO schema/migration (`CURRENT_SCHEMA_VERSION` stays 12); NO contract change; NO ADR edit; NO brief edit; NO gate-6 full-project audit; NO broad readiness refresh; NO clearing of gates 4/6/12/13/17/20 (or implying any cleared); NO **license/legal/business DECISION** (gate 17 — a license *inventory* is allowed; a *decision* about license acceptability/compliance is user-owned STOP-AND-ASK); NO gate-7 D-G7 risk acceptance; NO signing/notarization/公开 distribution decision (gate 4); NO 律师法 conclusion (gate 11); NO final GO/NO-GO (gate 21); NO staging of `dev-memo/run/intake/` or untracked residue.
- Requirements the FUTURE supply-chain posture doc must define + record (what the exec lane's sweep must specify + record) — at minimum:
  1. **Exact package manifests + lockfiles to inspect**: the 10 `package.json` + 9 `package-lock.json` enumerated in the grounding note (state which, note `native/evidence-core` has no lockfile, and the tool version/date read). Distinguish the **v1 production surface** (the Mac client `apps/lawbar-desktop` + its transitive tarballs) from the OCR services (not wired into v1) + the excluded bakeoff.
  2. **Exact runtime dependency inventory**: enumerate the v1-client runtime deps (`better-sqlite3 ^12.9.0`, `docx 9.7.1`, the two `file:dist-tarballs/*.tgz` internal tarballs) + the OCR-service engine (`@gutenye/ocr-node ^1.4.8`) + the Electron/Vite/build-tool devDeps (`electron ^34`, `electron-builder ^25`, `playwright`, `typescript`), each with its declared version + resolved version from the lockfile. Mark `docx@9.7.1` as carrying the prior ACCEPTABLE dependency-risk-review (`audit-mr63d6y7`, MIT/pure-JS) — cite it, do not re-decide it.
  3. **Native-binary / provenance checks**: for `better-sqlite3` — record the resolved version, the prebuilt-vs-compiled provenance, and the `services/ocr-persistence` `abi-smoke` pretest as the ABI-integrity guard; for the OCR engine (`@gutenye/ocr-node`) — record its provenance + that real-PaddleOCR is model-gated/post-v1 and not wired into the v1 client; for the two committed internal tarballs (`apps/lawbar-desktop/dist-tarballs/case-box-{contract,persistence}-0.1.0.tgz`) — record their provenance (built from in-repo `docs/contracts/case-box-contract` + `services/case-box-persistence`) so a committed binary artifact is traceable to source.
  4. **`npm audit` (or equivalent) commands, scoped by workspace/package**: the exact read-only commands (e.g. `npm --prefix apps/lawbar-desktop audit --omit=dev`, and per OCR service) + their recorded output (advisory counts by severity). READ-ONLY — no `npm audit fix`, no `npm install`, no lockfile mutation. If the environment cannot run `npm audit` offline, record that limitation honestly (a `not_run`/`offline` note is a recorded limitation, NOT a silent pass).
  5. **License inventory expectations (preserving the gate-17 boundary)**: record the declared license of each production dependency (e.g. `better-sqlite3` MIT, `docx` MIT, the internal tarballs = in-repo) as an **inventory**. This is descriptive only — it MUST NOT make a legal/business **decision** about license acceptability, compliance obligations, or attribution requirements (that is gate 17, user-owned STOP-AND-ASK). State explicitly that the inventory does not clear gate 17.
  6. **Supply-chain exclusions / post-v1 items**: `services/ocr-worker-bakeoff` (out of the production graph, ADR-11A.1) is excluded; the OCR services are not wired into the v1 Mac client (post-v1 surface); an SBOM is optional/post-v1; `npm audit` of transitive dev-only chains may be recorded as advisory-only. State each exclusion + why.
  7. **Expected evidence artifact**: the doc records the manifest/lockfile enumeration, the runtime-dependency inventory + resolved versions, the native-binary/tarball provenance, the `npm audit` command set + severity-bucketed results (or a recorded limitation), the license inventory, and the environment (date, npm/node version) — captured as text.
  8. **Deterministic pass/fail criteria**: PASS = the inventory is complete (all v1-production deps enumerated with versions + provenance) AND `npm audit` was run (or its non-run recorded as a limitation) with any Critical/High advisory in a v1-production dependency surfaced + dispositioned (fixed-later / accepted-with-reason / escalated) AND the license inventory is recorded AND no gate-17 legal/business decision was made. **Disposition discipline:** "accepted-with-reason" here means the exec lane RECORDS a *prior explicit* acceptance (e.g. the existing `docx@9.7.1` ACCEPTABLE review) or a *proposed/temporary* posture for the user to ratify — it is NOT a NEW go-live / security risk-acceptance made by the agent. Any *blocking* Critical/High in a v1-production dependency stays SURFACED / escalated to the user; it does NOT clear gate 19 or advance final readiness. FAIL = an incomplete inventory, an unrecorded/hidden audit result, an unsurfaced Critical/High in a production dep, or a legal/business decision smuggled in. An unfixed Critical/High in a v1-production dependency is SURFACED (and may be escalated to the user), not silently passed.
  9. **How gate 19 is evidence-advanced without clearing gate 17 / gate 4 / gate 6 / final go-live**: a completed inventory+audit+provenance supplies the "supply-chain posture recorded" evidence. **Gate 19 MUST remain `OPEN`** — the exec lane adds ONLY a `[Δ] supply-chain inventory + audit + provenance recorded` marker (existing vocabulary; roll-up bucket unchanged) and MUST NOT make an OPEN → PARTIAL/CLEARED move (that belongs to a SEPARATE holistic readiness-refresh WI). It MUST NOT clear gate 17 (license/business decision, user-owned), gate 4 (signing/distribution), gate 6 (full-project audit — consumes this later), or imply go-live. It MAY reference gate 13 (operator/runbook) + gate 20 (no-telemetry) context WITHOUT clearing them.
  10. **Residual risks + follow-up WIs (if any)**: e.g. an SBOM generation WI (post-v1); a gate-17 license-decision WI (user-owned); any Critical/High advisory disposition; a periodic re-audit cadence; the OCR-service supply-chain re-audit when/if OCR is wired into the client (post-v1).
- Dependencies mapped: **gate 17** (license/legal/business — a license *inventory* here is allowed; the *decision* is user-owned, NOT cleared); **gate 4** (signing/distribution — native-binary packaging is gate-4 territory, NOT cleared); **gate 6** (full-project audit — consumes the supply-chain posture later, NOT started here); **gate 13** (operator/runbook — the dependency-audit context is adjacent, referenced NOT cleared); **gate 20** (no-telemetry — the "deps clean" observation is adjacent, referenced NOT cleared); **gate 2** (OCR pipeline — the OCR engine inventory is adjacent, NOT cleared). These are noted as related/referenced, NOT cleared by this WI.
- Essential refs: `docs/release/go-live-readiness-report.md` §1 gate 19 (OPEN [Δ]) + gates 4/6/13/17/20; `apps/lawbar-desktop/package.json` + `package-lock.json` (the v1 production surface); `services/{ocr-worker,ocr-persistence,case-box-persistence}/package.json` (native + engine deps); the prior `docx@9.7.1` dependency-risk-review (job `audit-mr63d6y7`, ACCEPTABLE); `.claude/rules/client-local-first.md` (v1 posture) + `.claude/rules/security-boundary.md` §"Go-live independence"; `AGENTS.md` §"Source hierarchy" + §"Authoring discipline" + §"Repo brief — lawbar-tool" (Node 22+, better-sqlite3 ABI smoke); `.claude/rules/autonomy.md` + `staging-hygiene.md` + `loc-guardian.md`.
- Review questions: (1) Does the WI correctly scope a READ-ONLY supply-chain inventory/audit/provenance sweep (inventory + `npm audit` + provenance), NOT a dependency change and NOT an execution in this authoring lane? (2) Does it forbid ALL dependency/lockfile mutation (no `npm install`/`audit fix`/`update`) and keep the sweep read-only? (3) Does it keep a **license inventory** but forbid a **license/legal/business decision** (gate 17 user-owned)? (4) Does it keep gates 4/6/12/13/17/20 uncleared + gate 19 `OPEN`, and surface (not silently pass) any Critical/High advisory in a v1-production dep? (5) Does it decide no STOP-AND-ASK (4/11/17/21) hard-stop and assert no go-live? (6) Is any dependency change, lockfile mutation, license decision, gate clearance, or readiness refresh smuggled in?

Type: EVIDENCE
Scope: Govern (do NOT execute) a FUTURE lane that RUNS the M0 gate-19 supply-chain posture sweep. The FUTURE execution lane produces `docs/release/gate19-supply-chain-posture-00.md` — defining requirements 1–10 above (manifests+lockfiles to inspect; runtime-dependency inventory; native-binary/tarball/OCR-engine provenance; scoped read-only `npm audit` commands + results; license inventory preserving the gate-17 boundary; exclusions/post-v1; evidence artifact; deterministic pass/fail; evidence-advance-without-clearing-gate-17/4/6; residual risks) — then runs the READ-ONLY sweep (read manifests + lockfiles + tarball metadata; run `npm audit`/`npm ls`; NEVER `npm install`/`audit fix`/`update` and NEVER mutate a `package.json`/`package-lock.json`), records the result, and updates ONLY the gate-19 evidence row of `docs/release/go-live-readiness-report.md`, where **gate 19 MUST remain `OPEN`** (add only a `[Δ] supply-chain inventory + audit + provenance recorded` marker; NO roll-up/status-bucket edit; a movement belongs to a SEPARATE holistic readiness-refresh WI). It keeps a license **inventory** but makes NO license/legal/business **decision** (gate 17 user-owned), surfaces (not silently passes) any Critical/High advisory in a v1-production dependency, and preserves every still-non-cleared gate + the STOP-AND-ASK gates (4/11/17/21). It makes NO final GO/NO-GO, resolves NO STOP-AND-ASK, runs NO gate-6 audit, changes NO dependency/lockfile, EDITS NO ADR/brief, does NO OCR/Forms implementation, and runs NO readiness refresh. THIS authoring lane implements + runs + decides NOTHING — it only governs the gate-19 sweep WI. If review asks to change a dependency, mutate a lockfile, make a license/legal decision, clear a dependent gate, run gate 6, or run a readiness refresh, REJECT and report.
Source of truth: docs/release/go-live-readiness-report.md §1 gate 19 (OPEN [Δ]) + gates 4/6/13/17/20; apps/lawbar-desktop/package.json + package-lock.json (v1 production surface) + dist-tarballs/*.tgz; services/{ocr-worker,ocr-persistence,case-box-persistence}/package.json (native + OCR-engine deps); the prior docx@9.7.1 dependency-risk-review (job audit-mr63d6y7, ACCEPTABLE); .claude/rules/client-local-first.md + security-boundary.md §"Go-live independence"; AGENTS.md §"Source hierarchy" + §"Authoring discipline" + §"Repo brief — lawbar-tool"; .claude/rules/autonomy.md + staging-hygiene.md + loc-guardian.md
Allowed files: docs/release/gate19-supply-chain-posture-00.md (the NEW supply-chain posture evidence doc the FUTURE lane produces) + docs/release/go-live-readiness-report.md (the gate-19 evidence ROW ONLY — evidence-row TEXT ONLY, NO roll-up/status-bucket edit; gate 19 stays OPEN, its roll-up bucket unchanged) + dev-memo/run/reviews/ (the exec lane's own review artifact). NOTE: THIS authoring lane commits ONLY the queue governance (queue.md/linted/reviewed/governed) + reviews/queue-review-164.md — NO posture doc, NO report change. The FUTURE exec lane records its evidence (incl. the npm-audit command set + results) in the COMMIT MESSAGE + the posture doc + review artifact, does NOT re-stage this lane's queue governance, and does NOT mutate any package.json / package-lock.json / dependency.
Forbidden files: any package.json + any package-lock.json (READ-ONLY — NO dependency add/remove/bump, NO lockfile mutation, NO `npm install`/`audit fix`/`update`), apps/ + services/ + native/ source/test (NO product source/test/renderer/IPC/OCR/Forms change), services/*/src/sqlite/schema.ts (CURRENT_SCHEMA_VERSION stays 12), migrations/, dist-tarballs/*.tgz (READ metadata only — never rebuild/replace a committed tarball in this lane), docs/product/project-requirements-brief.md (READ-ONLY — NO brief edit), docs/adr/ (READ-ONLY — NO ADR edit), docs/release/ OTHER than the NEW gate19-supply-chain-posture-00.md + the gate-19 row of go-live-readiness-report.md (the other gate evidence docs are READ-ONLY — referenced, NOT edited), docs/contracts/, dev-memo/run/intake/ (raw samples — never staged), dev-memo/ OTHER than run/reviews/ (no other plan/design edit), .claude/ + .github/; NO dependency change; NO lockfile mutation; NO license/legal/business DECISION (gate 17); NO OCR/Forms implementation; NO supply-chain SWEEP RUN in this authoring lane; NO gate-6 full-project audit; NO broad readiness refresh; NO clearing of gates 4/6/12/13/17/20; NO signing/notarization/public-distribution decision (gate 4); NO 律师法 conclusion (gate 11); NO gate-7 D-G7 risk acceptance; NO final GO/NO-GO decision (gate 21). Push is NEVER automatic (autonomy hard-stop): local commit governance is the default; any branch push + PR open happens ONLY under explicit per-invocation user authorization; NO merge; NO branch delete of shared branches
Gates: EVIDENCE/governance lane (MEDIUM release-governance risk — governs a supply-chain inventory/audit WI; no code, no dependency change, no license decision, no go-live decision). (1) THIS authoring lane: broker /cc-suite:review-plan on this queue WI, Path 1, retrievable, then govern. (2) The FUTURE exec lane: RUN the read-only sweep (read manifests+lockfiles+tarball metadata; run `npm audit`/`npm ls`), produce the posture doc + the gate-19 row update, then broker a POST-ARTIFACT /cc-suite:review-plan on the PRODUCED artifacts (the posture doc + the gate-19 row diff, inlined — the docs-evidence review convention used by the other exec lanes, i.e. review OF the produced evidence, not a fresh plan review). (3) scripts/workflow/check-contract-integrity.sh exit 0; scripts/workflow/check-queue.sh PASS (both lanes). Forbidden-scope scan before commit: CURRENT_SCHEMA_VERSION EQUALS 12; no dependency change; no package.json/package-lock.json mutation; no product source/test/schema/contract change; no ADR/brief edit; no OCR/Forms impl; no gate-6 run; no readiness refresh; no gate 4/6/12/13/17/20 clear; no license/legal decision; no GO/NO-GO; no raw sample. STOP-points: review asks to change a dependency, mutate a lockfile, make a license/legal/business decision, clear a dependent gate, run gate 6, or run a readiness refresh → REJECT/STOP and report; the sweep reveals a Critical/High advisory in a v1-production dependency → SURFACE it (and escalate to the user if it blocks) — never silently pass; any forbidden file would change → STOP.
Acceptance criteria: check-queue and check-contract-integrity PASS and CURRENT_SCHEMA_VERSION EQUALS 12 and the diff CONTAINS only the queue governance (queue.md/linted/reviewed/governed) + reviews/queue-review-164.md and no posture-doc/source/test/package/lockfile/schema/contract change appears and no ADR/brief edit / no dependency change / no lockfile mutation / no license decision / no readiness refresh appears. The governed WI must DEFINE the ten sweep requirements (manifests+lockfiles; runtime-dependency inventory; native-binary/tarball/OCR-engine provenance; scoped read-only npm audit; license inventory preserving the gate-17 boundary; exclusions/post-v1; evidence artifact; deterministic pass/fail; evidence-advance-without-clearing-gate-17/4/6; residual risks), keep gates 4/6/12/13/17/20 uncleared, keep gate 19 `OPEN`, forbid a dependency/lockfile change + a license decision, surface any Critical/High advisory, and MUST NOT decide a STOP-AND-ASK hard-stop or assert go-live. review-plan (this lane) must be RUN and must PASS (READY, or READY with only Low-risk clarifications applied).
Risk flags: MEDIUM (release-governance — a supply-chain audit WI that could drift into mutating a lockfile / bumping a dependency, making a license/legal decision, over-claiming readiness, or clearing a referenced gate). Care points: (a) NO SWEEP RUN in this authoring lane — this lane only governs the WI; (b) READ-ONLY — the sweep reads manifests/lockfiles + runs `npm audit`/`npm ls`; it NEVER `npm install`/`audit fix`/`update` and NEVER mutates a package.json/package-lock.json/tarball; (c) LICENSE INVENTORY, NOT DECISION — a descriptive license inventory is allowed; a legal/business decision about license acceptability/compliance is gate 17, user-owned STOP-AND-ASK; (d) ADVISORY DISCIPLINE — any Critical/High `npm audit` advisory in a v1-production dependency is SURFACED (escalated if blocking), never silently passed; a non-runnable audit is a RECORDED limitation, not a silent pass; (e) DEPENDENT GATES UNCLEARED — gates 4/6/12/13/17/20 stay uncleared; gate 6 consumes this later; gate 19 stays OPEN; (f) GO-LIVE INDEPENDENCE — a supply-chain posture does NOT imply go-live; the final verdict + the STOP-AND-ASK hard-stops (4/11/17/21) remain the user's. If any cannot hold, STOP and report.
Depends on: none
Commit boundary: TWO separate lanes. (1) THIS authoring lane commits ONLY the queue governance (queue.md/linted/reviewed/governed) + reviews/queue-review-164.md — no posture doc, no report change, no implementation, no decision. (2) The FUTURE execution lane commits ONLY the new docs/release/gate19-supply-chain-posture-00.md + the gate-19 evidence-row update to go-live-readiness-report.md + its own review artifact under dev-memo/run/reviews/, records its evidence (incl. the npm-audit command set + severity-bucketed results) in the COMMIT MESSAGE, does NOT re-stage this lane's queue governance, and does NOT mutate any package.json/package-lock.json/dependency/tarball. NO product source/test/package/lockfile/schema/contract file in either lane; NO dependency change; NO lockfile mutation; NO license/legal decision; NO OCR/Forms implementation; NO staging of dev-memo/run/intake/ or untracked residue; push is NEVER automatic (autonomy hard-stop) — local commit governance is the default, any branch push + PR open happens ONLY under explicit per-invocation user authorization; no merge; no branch delete of shared branches. THIS authoring lane implements NOTHING, runs NO sweep, and decides NOTHING — it only governs the gate-19 supply-chain posture WI.

## Acceptance criteria (FUTURE execution lane — what the governed sweep doc must satisfy)

These describe the deliverable the FUTURE exec lane must produce; they are NOT preconditions of THIS authoring lane. The authoring-lane acceptance is the structured `Acceptance criteria:` field above (queue governance + review READY only — the posture doc does NOT exist yet and MUST NOT be created in this lane).

1. A NEW `docs/release/gate19-supply-chain-posture-00.md` exists defining requirements 1–10 above and recording the sweep findings + result.
2. The doc enumerates the 10 manifests + 9 lockfiles, inventories the v1-client runtime deps (`better-sqlite3 ^12.9.0`, `docx 9.7.1`, the two `dist-tarballs/*.tgz`) + the OCR engine (`@gutenye/ocr-node ^1.4.8`) + Electron/build devDeps with declared + resolved versions, and cites the prior `docx@9.7.1` ACCEPTABLE review (`audit-mr63d6y7`) without re-deciding it.
3. The doc records native-binary/tarball provenance (`better-sqlite3` + the `abi-smoke` guard; the OCR engine + model-gated/post-v1 note; the two committed tarballs traced to their in-repo source) and the scoped read-only `npm audit` command set + severity-bucketed results (or a recorded non-run limitation) — NO lockfile mutation, NO `audit fix`.
4. The doc records a **license inventory** (descriptive) and explicitly does NOT make a gate-17 legal/business **decision** (user-owned); any Critical/High advisory in a v1-production dep is surfaced + dispositioned.
5. The exec lane CITES the exact gate-19 row (by its `| 19 | …` line) before editing, and edits ONLY that row's evidence/blocker TEXT — NO adjacent roll-up/status-bucket line touched. Gate 19 **MUST remain `OPEN`** — add ONLY a `[Δ] supply-chain inventory + audit + provenance recorded` marker (roll-up bucket unchanged); movement belongs to a SEPARATE holistic readiness-refresh WI. MUST NOT clear gates 4/6/12/13/17/20 or imply any cleared.
6. NO dependency change, NO `package.json`/`package-lock.json` mutation, NO product source/test/config/schema/contract change; NO ADR/brief edit; NO license decision; `CURRENT_SCHEMA_VERSION` stays 12; docs-only tracked output.
7. `scripts/workflow/check-queue.sh` PASS + `scripts/workflow/check-contract-integrity.sh` PASS on both lanes.
