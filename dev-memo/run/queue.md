## WI-EVIDENCE-A10-T1-CITATION-RENDER-CONTRACT-00: implement A10 citation-render contract (Type: IMPL)
Type: IMPL
Scope: Implement the A10-T1 citation-render CONTRACT as the first concrete A10 slice, per `docs/adr/ADR-evidence-a10-court-fileable-export.md` (A10-DESIGN-00 §6, §13). NO full `CanonicalExportModel`, NO A10-T6 golden export, NO T3/T4/T5 forms, NO export-preview hashing, NO .docx/PDF generation, NO A8 implementation, NO schema/native/UI/IPC change. Grounding (verified on main): the citation RENDER already exists in `services/case-box-persistence` — `buildExportCitations` produces `ExportCitationResult` with `ExportCitation.citation.text = 卷${citationVolume}页${citationPageLabel}` (from DocumentPage) + `ExportCitationFlag = NEEDS_REVIEW|BROKEN|NON_CITABLE|AMBIGUOUS|UNLINKED` (the A3-EXPORT-00 degradation; `UNLINKED` is the V12 durable marker). The apps layer (`apps/lawbar-desktop/src/caseBox/linkHandlers.ts` exportLinkCitationsHandler + `dto/link.ts`) only CONSUMES it via IPC. There is NO apps-layer citation-render/format/version module today. A10-T1 (ADR §6) = "the ONLY code producing citation strings; renders from DocumentPage via A1-T5; pins citationFormatVersion (syntax) + exportTemplateVersion (layout); the ExportCitationFlag enum is part of the contract." Because the services renderer is FORBIDDEN to edit in this lane, the in-scope A10-T1 slice is a NEW apps-layer contract module (`apps/lawbar-desktop/src/caseBox/**`, e.g. `export/a10CitationContract.ts`) that: (1) defines the CANONICAL `A10ExportCitationFlag` enum with 中文_ENGLISH labels — RETAIN `UNLINKED` (V12, do not erase), treat `REPLACED` as spec-pending (NOT emitted until replacement-lifecycle support exists; documented as a deferred reconciliation per ADR §6); (2) pins `CITATION_FORMAT_VERSION` (卷X页Y syntax) + `EXPORT_TEMPLATE_VERSION` (layout) constants; (3) provides a DETERMINISTIC, pure mapper from the DocumentPage-derived services `ExportCitation` to a versioned `A10RenderedCitation` shape (carries structured citationVolume/citationPageLabel + the rendered text + the canonical flag + the pinned versions) — SINGLE-SOURCE discipline: it REUSES the DocumentPage-derived 卷X页Y text (does NOT invent a second/alternate citation format, ADR §3 "no module formats citations independently"), and asserts the text equals `卷{citationVolume}页{citationPageLabel}` for clean citations; (4) makes clean/broken/unlinked/needs-review/non-citable/ambiguous states EXPLICIT enough for later A10-T6 golden verification (every input maps to exactly one rendered-citation-or-flag — total + bijective, ADR §6 A10-T2 alignment). It is a STANDALONE contract module + unit tests; it does NOT wire into the live exportLinkCitationsHandler IPC path (that is a later A10 slice) and does NOT implement CanonicalExportModel. Preserve A1 DocumentPage-only citation identity, A3 export degradation incl. UNLINKED, A8 canonical-not-raw-bytes, and the A0.7/A1-T6/A3-T10 native gates (regression). If A10-T1 cannot be implemented without editing services or inventing CanonicalExportModel, STOP and report.
Source of truth: docs/adr/ADR-evidence-a10-court-fileable-export.md (A10-DESIGN-00 §3 single-citation-source, §6 citation/flag representation + UNLINKED/REPLACED reconciliation, §13 T1 sequence, §14 acceptance); docs/adr/ADR-evidence-a3-export-degradation.md (A3-EXPORT-00 degradation precedence + ExportCitationFlag); docs/reference/evidence-genie-m0-developer-handover.md (A1 invariant §3; A10-T1 line 149; ExportCitationFlag data-model); services/case-box-persistence/src/sqlite/exportCitationQueries.ts (READ-ONLY: the built ExportCitation/ExportCitationFlag/text 卷X页Y the contract maps + must not drift from); apps/lawbar-desktop/src/caseBox/{linkHandlers.ts,dto/link.ts} (READ-ONLY: how ExportCitationResult is consumed); apps/lawbar-desktop/src/caseBox/dto (DTO module pattern); apps/lawbar-desktop/tests (test pattern); native/evidence-core-swift/Sources/EvidenceCoreSmoke/A1CitationStabilityHarness.swift (the A1-T6 卷X页Y/ambiguity/non-citable contract the apps contract must stay consistent with); .claude/rules/{evidence-genie,cc-suite,autonomy,execution-discipline,staging-hygiene,security-boundary,client-local-first}.md
Allowed files: apps/lawbar-desktop/src/caseBox/export/a10CitationContract.ts (NEW A10-T1 contract module; exact filename may adjust per review-plan within apps/lawbar-desktop/src/caseBox/**), apps/lawbar-desktop/tests/a10-citation-contract.unit.test.mjs (NEW unit tests; exact name may adjust), apps/lawbar-desktop/package.json (ONLY if a new test file must be registered in the test script — minimal, review-plan to confirm; else leave unchanged), dev-memo/run/queue.md, dev-memo/run/queue.linted, dev-memo/run/queue.reviewed, dev-memo/run/queue.governed, dev-memo/run/reviews/queue-review-111.md
Forbidden files: services/** (NO edit to buildExportCitations / exportCitationQueries / any services source/schema — READ-ONLY reference; if A10-T1 needs a services change, STOP), services/case-box-persistence/src/sqlite/schema.ts (NO schema/CURRENT_SCHEMA_VERSION change), docs/contracts/** (NO contract-package change), native/** (NO native/Swift change; A0.7/A1-T6/A3-T10 gates are regression-only), apps/lawbar-desktop/renderer/** (NO renderer UI change), apps/lawbar-desktop/electron/** (NO Electron IPC/preload change), apps/lawbar-desktop/src/caseBox/linkHandlers.ts + dto/link.ts (NO change to the live export handler/DTO — A10-T1 is a STANDALONE contract module, not wired this lane), apps/lawbar-desktop/{package-lock.json,dist-tarballs/**} (NO dependency/tarball change), scripts/** + .claude/** + .github/** + migrations/**, docs/adr/** (READ-ONLY), dev-memo/design/** (NO separate artifact unless review-plan requires), dev-memo/run/evidence/** (gitignored custody marker/ledger — never touched/staged), committed keys, dev-memo/run/reports/** + dev-memo/run/override-reason.md + dev-memo/run/autonomous-next.md (untracked run-state — never staged/edited), Evidence-Genie-M0-Developer-Handover.md + xiaolai-dev-workflow-study.md (root intake — READ-ONLY); NO full CanonicalExportModel / golden-export / forms / preview-hash / docx-pdf / A8 / restore implementation; NO custody/marker; NO HMAC/key request/receive/print/persist/stage; NO confidential/client/court fixture; NO REPLACED emission; NO push/merge/branch-delete
Gates: broker /cc-suite:review-plan REQUIRED before implementation — confirm: A10-T1 is implementable as an APPS-LAYER citation-render contract module WITHOUT editing services (the built renderer) or inventing CanonicalExportModel; the single-source discipline is satisfied (the contract REUSES the DocumentPage-derived 卷X页Y text rather than introducing a second format) and does not duplicate/drift from the services render; pinning citationFormatVersion + exportTemplateVersion + owning the canonical ExportCitationFlag enum (UNLINKED retained, REPLACED spec-pending) is the right T1 slice; standalone-module (not wiring the live IPC handler) is acceptable for T1; NO schema/services/native/UI/IPC change. STOP-points: (1) if A10-T1 is undefined/contradicted by the ADR/current impl -> STOP; (2) if the only way to implement A10-T1 is to invent full CanonicalExportModel/A10-T6 -> STOP; (3) if T3/T4/T5 forms / product-legal form-field spec / jurisdictional filing rules are required first -> STOP; (4) if schema/renderer-UI/Electron-IPC/package/native/services change becomes necessary without review-plan approval -> STOP (note: a minimal package.json test-script registration is contemplated + allowed ONLY with review-plan approval); (5) if the design would weaken A1/A3/A8 or the A0.7/A1-T6/A3-T10 native gates -> STOP; (6) if custody/key handling would expose a key -> STOP. If review-plan NEEDS-FIX on approach, fix-forward; if a blocker, STOP. After implementation: targeted tests pass (`npm --prefix apps/lawbar-desktop test` GREEN, or the targeted node --test for the new file); `scripts/workflow/check-contract-integrity.sh` exits 0; broker /cc-suite:audit + /cc-suite:verify (no Critical/High/Medium open before commit). No A0.7 custody. Batch was closed (190, marker now 222016b); a closeout will be due before PR after this commit.
Acceptance criteria: a NEW apps-layer A10-T1 citation-render contract module exists under `apps/lawbar-desktop/src/caseBox/**` that (a) defines the canonical `A10ExportCitationFlag` enum RETAINING `UNLINKED` and treating `REPLACED` as spec-pending/NOT-emitted (with 中文_ENGLISH labels); (b) pins `CITATION_FORMAT_VERSION` + `EXPORT_TEMPLATE_VERSION`; (c) deterministically maps a DocumentPage-derived `ExportCitation` to a versioned `A10RenderedCitation` (structured volume/label + rendered text + canonical flag + pinned versions), REUSING the 卷X页Y text (single-source; asserts text == `卷{vol}页{label}` for clean; never invents an alternate format; never emits REPLACED); (d) every input maps to exactly one rendered-citation-or-flag (total + bijective). Tests PROVE: clean linked citation renders deterministically (stable ordering, stable ids, no current timestamps, no machine paths, no locale-dependent formatting); broken/missing/unlinked/needs-review/non-citable/ambiguous degrade deterministically to the correct flag; `UNLINKED` retained + covered; `REPLACED` is NOT emitted; determinism (repeat run byte-identical). Existing A1-T6 + A3-T10 native gates + the desktop suite remain GREEN (regression). NO full CanonicalExportModel/golden-export/forms/preview-hash/docx-pdf/A8 implementation; NO services/schema/native/renderer/electron/dependency change; NO confidential fixture; `CURRENT_SCHEMA_VERSION` remains 12; `dev-memo/run/evidence/**` untouched; no HMAC/custody key; `check-contract-integrity` exits 0; broker review-plan READY + audit no Critical/High/Medium + verify ALL CLOSED; committed diff confined to Allowed files.
Risk flags: HIGH-RISK Evidence-invariant lane (A10 court-fileable export prerequisite; citation fidelity) — but a STANDALONE apps-layer contract module + tests only; NO services/schema/native/UI/IPC change, NO live-pipeline wiring, NO CanonicalExportModel. Load-bearing care points: (a) SINGLE-SOURCE — the contract MUST reuse the DocumentPage-derived 卷X页Y render (assert text == 卷{vol}页{label}), NOT introduce a second/alternate citation format (ADR §3); it must not drift from the services render or the A1-T6 native gate; (b) UNLINKED retained, REPLACED spec-pending/NOT-emitted (ADR §6 reconciliation); (c) total+bijective degradation (every input -> one rendered-citation-or-flag); (d) deterministic (stable order/ids, no timestamps/machine-paths/locale); (e) do NOT invent CanonicalExportModel / golden-export / forms / A8; (f) do NOT edit services (the renderer) — if needed, STOP; (g) do NOT touch custody/marker/evidence/key. If any cannot hold, STOP and report. No push/merge/branch-delete.
Depends on: none
Commit boundary: governance (mark-reviewed + govern) run STANDALONE before the commit (never bundled with git commit). ONE implementation commit — apps/lawbar-desktop/src/caseBox/export/a10CitationContract.ts (+ apps/lawbar-desktop/tests/a10-citation-contract.unit.test.mjs) [+ apps/lawbar-desktop/package.json ONLY if review-plan approved a test-script registration] + governance artifacts (queue.md/.linted/.reviewed/.governed + reviews/queue-review-111.md), staged by exact path; NO services/schema/native/renderer/electron/contract/dependency/tarball file; NO marker/evidence/key/report file; no push; no merge; no branch delete.
