## WI-A3-LINK-UI-DESIGN-00: design the renderer UI for audited evidence links (design-only; NOT A0.7-gated)
Type: PLAN
A0.7-classification: WI-A3-LINK-UI-DESIGN-00 is a DESIGN-ONLY UI design artifact (it authorizes no code, executes no A0.7-dependent code, reads no A0.7 marker), so it is NOT A0.7-gated and carries NO `Requires-A07:` line. It governs the eventual renderer UI impl (the future WI-A3-LINK-UI-T1, whose A0.7 classification its own review-plan decides), so broker /cc-suite:review-plan + /cc-suite:audit + /cc-suite:verify are REQUIRED on the design. If review-plan says the design itself must be A0.7-gated, STOP and report.
Scope: Produce a DESIGN-ONLY UI design artifact (`dev-memo/design/2026-06-26-audited-evidence-links-ui.md`, the established Claude-Design-export format that the check-ui-design-artifact gate references) for the renderer UI of the audited link lifecycle (view/create/unlink/relink/export; status valid/needs_review/broken + export flags), grounded in the existing renderer conventions (the fact create + fact review two-step + deadline transition two-step write flows; status-label+data-attribute rendering; the i18n catalog; the a11y patterns; the renderer api.ts/types.ts bridge + RENDERER_*_DTO_FIELDS strip allowlists). No renderer code. The artifact records: the UI state model for link rows + actions; component/surface placement (a per-matter "Evidence links" disclosure); the create/unlink(reason-required)/relink user-facing flows; A0.7-gating/confirmation behavior (A0.7 is dev/commit-time NOT runtime — no runtime marker UI; confirmation = the two-step unlink reason); the error/empty/loading/gate-failure display model; auditability without chain internals; confidentiality (identifiers/status/flags/timestamps/reasons only — no raw evidence content; no real data in fixtures/screenshots); a11y + i18n; the renderer authority-boundary/bridge prerequisite; the test plan; and the deferred-item handling + sequencing. DESIGN-ONLY: no renderer/** change, no IPC/preload change, no persistence change, no schema or CURRENT_SCHEMA_VERSION change, no audit-contract change, no package/dependency/native/Electron-harness change. If the design proves any of those are REQUIRED before the UI design can complete, STOP and report.
Required decisions to record: (1) PLACEMENT — a per-matter "Evidence links" disclosure (Facts-disclosure pattern) + a new renderer/screens/viewMatterLinks.ts (future). (2) STATE MODEL — per-row lifecycle active vs unlinked + resolver status (valid/needs_review/broken) + export flag (UNLINKED/BROKEN/NEEDS_REVIEW/NON_CITABLE/AMBIGUOUS/clean); render = visible label + data-status/data-flag + CSS class (never color-only); show created_at + unlinked_at + unlink_reason where appropriate. (3) ACTION FLOWS — create form (sourceType 5-enum select + sourceId + anchorId -> createLink); unlink = two-step REQUIRED reason -> unlinkLink (warns it breaks the citation, recorded in audit); relink = one-click no-reason -> relinkLink; export = exportLinkCitations -> read-only { citations, byFlag } display (export-document rendering OUT of scope). (4) A0.7/CONFIRM — A0.7 is dev/commit-time, NOT runtime (ADR D4); NO runtime A0.7 marker UI invented; confirmation = the two-step unlink reason + the create form. (5) ERROR/EMPTY/LOADING/GATE — loading text banner; empty heading+paragraph+CTA; inline role=alert server-safe message; the envelope codes (invalid_payload/unknown_matter/tenant_mismatch/illegal_transition/invalid_argument/duplicate_id) mapped to user copy; stale-load guard. (6) AUDITABILITY — lifecycle (created/unlinked+reason/relinked) + timestamps only; NO chain internals (no hashes/sequence/event_kind). (7) CONFIDENTIALITY — identifiers/status/flags/timestamps/reasons only; no raw evidence body/PDF/OCR; no real data anywhere. (8) A11Y/I18N — data-status (not color-only), label-for + required + focus for the reason input, Enter/Escape, role=alert/aria-live; all copy in catalog.ts (new linkStatus.*/linkFlag.*/links.* keys; i18n-guard forbids hardcoded strings). (9) BRIDGE PREREQ — the impl lane must add the 5 link methods to renderer/api.ts + the renderer DTO types/allowlists to renderer/types.ts + EXTEND the renderer-dto-sync PAIRS list. (10) DEFERRED + SEQUENCING — D1 (real-db list/export IPC round-trip): RECOMMEND folding the Electron integration round-trip into the UI impl lane (close D1 there), no separate predecessor required; D2 (stale errorMap governance prose): a separate tiny docs-only cleanup lane, NOT edited here; NEXT lane = the UI implementation WI-A3-LINK-UI-T1 (Type: UI, citing this artifact as its Design artifact), which carries the bridge + screens + i18n + tests + D1.
Source of truth: apps/lawbar-desktop/renderer/{router,index,api,types,dom,format}.ts + screens/{viewMatterFacts,viewMatterDeadlines,listMatters}.ts + i18n/{t,catalog}.ts (READ-ONLY — the renderer conventions the design mirrors); apps/lawbar-desktop/src/caseBox/dto/link.ts + linkHandlers.ts (READ-ONLY — the link DTOs + IPC the UI calls); docs/adr/ADR-evidence-a3-link-ipc-surface.md (the IPC contract + the A0.7-is-dev-time model); UI-GATES.md + dev-memo/design/2026-06-05-casebox-fact-review.md (READ-ONLY — the design-artifact gate + format); dev-memo/deferred-audit-findings.md (D1/D2); .claude/rules/{client-local-first,autonomy,cc-suite,evidence-genie}.md (local-first; user-authorized design lane).
Allowed files: dev-memo/design/2026-06-26-audited-evidence-links-ui.md (the DESIGN ARTIFACT — the only deliverable), dev-memo/run/queue.md, dev-memo/run/queue.linted, dev-memo/run/queue.reviewed, dev-memo/run/queue.governed, dev-memo/run/reviews/queue-review-095.md, dev-memo/study/** (a batch-audit study packet if a window closes), dev-memo/cc-suite-reliability-log.md (only if a TIMEOUT retry occurs), dev-memo/deferred-audit-findings.md (only if a finding is deferred)
Forbidden files: apps/** (NO renderer/IPC/preload/test code — the UI impl is the future WI-A3-LINK-UI-T1), apps/lawbar-desktop/renderer/** (NO renderer code/types/bridge change), apps/lawbar-desktop/electron/** (NO IPC/preload change), services/** (NO persistence change), services/case-box-persistence/src/sqlite/schema.ts (NO schema/version change), docs/contracts/** (NO contract change), apps/lawbar-desktop/package.json + any package manifest (NO change), package-lock.json / any dependency manifest, native/**, .claude/**, .github/**, AGENTS.md, CLAUDE.md, GEMINI.md, scripts/workflow/**, migrations/**, committed dev-memo/run/evidence/** marker/ledger/key/evidence-run files, committed keys, dev-memo/run/reports/** (untracked local run-state — never staged), dev-memo/run/override-reason.md (untracked residue — never staged), Evidence-Genie-M0-Developer-Handover.md (root intake — READ-ONLY, never moved/deleted), xiaolai-dev-workflow-study.md (root intake — READ-ONLY, never moved/deleted); NO renderer/product/IPC code, NO schema/version change, NO contract change, NO persistence change, NO package/dependency change, NO implementation, NO Electron-harness change, NO editing the D2 governance prose here, NO runtime A0.7 mechanism designed-in
Gates: broker /cc-suite:review-plan REQUIRED before/at authoring (confirm DESIGN-ONLY + NOT-A0.7-gated classification; confirm the placement/state-model/action-flows/error-model match the renderer conventions + the IPC ADR; confirm the A0.7-is-dev-time/no-runtime-marker-UI posture — or STOP if review-plan wants a runtime A0.7 gate; confirm the confidentiality posture (identifiers/status/flags only, no evidence content) + auditability-without-chain-internals; confirm a11y/i18n match the renderer conventions; confirm the bridge-prerequisite + the renderer-dto-sync PAIRS extension note; confirm the D1-fold-into-UI-lane + D2-separate-docs-cleanup sequencing + UI-impl-next); if review-plan returns NEEDS-FIX, STOP and report; if review-plan says the design must be A0.7-gated, STOP and report; broker /cc-suite:audit + /cc-suite:verify on the artifact diff after authoring (no Critical/High/Medium open before commit); scripts/workflow/check-contract-integrity.sh exits 0 (sanity — always-loaded contract docs untouched); no apps/services/schema/contract/package/dependency change; no committed marker/ledger/key/evidence-run; root intake untouched
Acceptance criteria: dev-memo/design/2026-06-26-audited-evidence-links-ui.md exists in the established design-artifact format and records sections 1-12 — placement (per-matter Evidence-links disclosure), the link-row UI state model (active/unlinked + status valid/needs_review/broken + export flags, rendered as label+data-attribute not color-only), the create/unlink(reason-required two-step)/relink/export action flows, the A0.7-dev-time/no-runtime-marker confirmation model, the error/empty/loading/gate-failure display model, auditability-without-chain-internals, the identifiers/status/flags-only confidentiality + no-real-data constraint, a11y + i18n (catalog keys; i18n-guard), the renderer bridge prerequisite + the renderer-dto-sync PAIRS extension, the test plan, and the deferred-item handling (D1 fold into the UI lane; D2 separate docs cleanup) + sequencing (UI impl next, citing this artifact as its Design artifact); DESIGN-ONLY (no renderer/product/IPC code, no schema/contract/persistence/package change); broker review-plan READY (design + classification + the A0.7-no-runtime-marker posture confirmed) + audit reports no Critical/High/Medium + verify ALL CLOSED; scripts/workflow/check-contract-integrity.sh exits 0; the committed diff is confined to the design artifact + the governance artifacts (dev-memo/**)
Risk flags: PLAN — a DESIGN-ONLY UI design artifact (no product code). NOT A0.7-gated (no geometry-derived behavior; no marker/key). Doc-only, but it GOVERNS the eventual renderer UI impl over court-facing link presentation, so broker review-plan + audit + verify are REQUIRED on the design. The artifact MUST NOT implement renderer/IPC code, MUST NOT design a runtime A0.7 mechanism (A0.7 is dev/commit-time per the IPC ADR D4), MUST NOT change schema/CURRENT_SCHEMA_VERSION/contract/persistence/package, MUST NOT expose raw evidence content in the link UI design or any fixture, and MUST NOT weaken any Evidence invariant (offline/local-first; main-injected identity; identifiers-only confidentiality). The D2 governance-prose cleanup is NOT done here. If the design proves an IPC/persistence/schema/contract/package change is required before the UI design can complete, STOP and report. No push, no merge.
Depends on: none
Commit boundary: governance (mark-reviewed + govern) run STANDALONE before the commit (never bundled with git commit). ONE commit -- dev-memo/design/2026-06-26-audited-evidence-links-ui.md + dev-memo/run/queue.md + dev-memo/run/queue.linted + dev-memo/run/queue.reviewed + dev-memo/run/queue.governed + dev-memo/run/reviews/queue-review-095.md, staged by exact path; no product code; no push; no merge
