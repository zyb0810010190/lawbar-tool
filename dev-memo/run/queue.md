## WI-A3-LINK-UI-T1: renderer UI implementation for audited evidence links (Type: UI; A0.7 commit-gated)
Type: UI
Design artifact: dev-memo/design/2026-06-26-audited-evidence-links-ui.md
A0.7-classification: WI-A3-LINK-UI-T1 is the renderer UI implementation for the court-facing audited-link lifecycle. The renderer code executes NO A0.7-dependent code (no geometry, no marker read; it only calls the merged `casebox:link:*` IPC and renders the `{ok,error}` envelope). Per the design artifact §12 + the lane authorization, the lane is nonetheless treated as A0.7 COMMIT-GATED (custody 9b): `Requires-A07: yes`. review-plan MAY downgrade this only by recording a concrete contrary reason (that no A0.7-dependent action is involved); absent that, the impl commit is blocked until a human mints the marker and reports a gated `A07_REQUIRED=1 check-gates.sh` PASS. NO runtime A0.7 marker UI is designed or implemented (a runtime A0.7 mechanism would be a separate-ADR STOP).
Requires-A07: yes
Scope: Implement the renderer UI described by dev-memo/design/2026-06-26-audited-evidence-links-ui.md (sections 1-12) for the audited evidence-link lifecycle, consuming the ALREADY-MERGED IPC/preload link API (5 `casebox:link:*` channels + 5 `CaseBoxApi` preload methods, PR #142) — do NOT redesign or modify the IPC/preload/persistence/contract. Deliver: (1) the renderer link bridge — add the 5 link methods (createLink/unlinkLink/relinkLink/listLinks/exportLinkCitations) to `renderer/api.ts` (`CaseBoxClient` + `CaseBoxApi` + `createCaseBoxApi` wrapping via `stripDtoFields`), the renderer DTO request/response types + the `RENDERER_CREATE_LINK_DTO_FIELDS` / `RENDERER_UNLINK_LINK_DTO_FIELDS` / `RENDERER_RELINK_LINK_DTO_FIELDS` / `RENDERER_LIST_LINKS_DTO_FIELDS` / `RENDERER_EXPORT_LINK_CITATIONS_DTO_FIELDS` strip allowlists in `renderer/types.ts`, and EXTEND the `renderer-dto-sync.test.mjs` `PAIRS` list with the 5 new RENDERER↔canonical link pairs (the canonical `*_LINK_DTO_FIELDS` arrays ALREADY EXIST in `src/caseBox/dto/link.ts` — read-only; the lane adds only the renderer mirrors). (2) a new `renderer/screens/viewMatterLinks.ts` screen (a per-matter "Evidence links" disclosure mirroring `renderFactsDisclosure`, lazy-loaded) wired into `renderer/screens/viewMatter.ts` alongside the existing disclosures; the create form, the per-row resolver status + lifecycle rendering (label + `data-status`, never color-only; `created_at`; `unlinked_at`+`unlink_reason` when unlinked), the unlink two-step required-reason flow, the one-click relink flow, and the section-header "Export citations" trigger that renders the `exportLinkCitations` `{citations, byFlag}` read-only (export flags shown ONLY here, joined by `linkId`). The renderer MUST define renderer-LOCAL export-citation types/shapes (in `renderer/types.ts`) — it MUST NOT import `ExportCitationResult`/`ExportCitation` (or any type) from `case-box-persistence` or `src/caseBox` in renderer code (`check-renderer-imports` forbids it; review-plan round-3 Low). (3) the i18n catalog keys in `renderer/i18n/catalog.ts` (`linkStatus.*`, `linkFlag.*`, `links.*`). (4) renderer tests (§10): `renderer-links-list/create/unlink/relink/export.test.mjs` (MockDoc + stub api, plain node) + extend `renderer-api.test.mjs` + `renderer-dto-sync.test.mjs` + (if needed) `renderer-i18n-guard.test.mjs`, all registered additively in `apps/lawbar-desktop/package.json`'s curated `test` script. (4b) the i18n drift-guard allowlist `apps/lawbar-desktop/renderer/i18n/ui-strings-allowlist.json` is REGENERATED (`scanAll()` → exact set) to reflect the new screen. The lexical drift-guard (`tests/_i18n-ui-scan.mjs`) flags EVERY string literal in array (`[`) position as kind `text`, so the regeneration (a) updates the 3 PRE-EXISTING `viewMatter.ts` literals whose line numbers SHIFTED from the 2-line disclosure wiring (231→232, 251→252, 422→424) and (b) ADDS the new `viewMatterLinks.ts` NON-PROSE scanner artifacts: the 5 `sourceType` enum constants (`evidence`/`note`/`question`/`calcTerm`/`claimElement`), the 6 export-flag iteration constants (`CLEAN`/`UNLINKED`/`BROKEN`/`NEEDS_REVIEW`/`NON_CITABLE`/`AMBIGUOUS`), 3 separator strings (`" "`), and 6 dynamic interpolated templates (the row/summary `${t(...)}`/field scaffolding). These are the SAME class of non-copy scanner candidates already allowlisted for the facts/deadlines/docket screens (e.g. the facts `FACT_PURPOSES` enum + separators). NO fixed user-facing PROSE literal is allowlisted — ALL prose resolves through `t()`/the catalog; the allowlisted entries are scanner artifacts (enum identifiers, separators, interpolation scaffolding), not hardcoded copy. If the regeneration would add a fixed PROSE literal, STOP (that literal must move to the catalog instead). (5) LINK-IPC-T1-D1 REMAINS DEFERRED — NOT closed in this lane (review-plan round-1 High, confirmed): `createLink` requires a scoped `case_box_anchors` row (`services/case-box-persistence/src/sqlite/linkRepoQueries.ts:211` → `invalid_argument` "unknown anchor"); there is NO `casebox:anchor:*` IPC channel to seed one (registered casebox channels = matter/document/audit/deadline/docket/fact/link), and a direct test-DB seed is Electron-ABI-blocked (the plain-node launcher cannot load the desktop's Electron-ABI `better-sqlite3`). Closing D1 would require forbidden scope (a new anchor IPC / a main-process seed hook / an `electron/**`|`src/caseBox/**` change) or new infrastructure — out of scope here. D1 is carried as an integration-test infrastructure prerequisite for a separate, properly-scoped FUTURE WI (likely either a minimal audited/authorized anchor IPC surface, OR a sanctioned Electron integration-test anchor-seed capability). UI-T1 proceeds WITHOUT closing D1; `tests/casebox-ipc.electron.test.mjs` is NOT edited in this lane. THIS QUEUE SUPERSEDES the design artifact §11-12 recommendation to "fold D1 into the UI impl lane" — that recommendation predates the round-1 anchor-seed finding (no anchor IPC + ABI-blocked seed); where this queue and the design artifact's D1-fold wording conflict, this queue governs (the design artifact §11-12 D1-fold text is corrected later in the same docs-only cleanup lane as D2/D3/D4, NOT here). (6) the OPTIONAL error-code→catalog map (review-plan round-1 Low): IF the renderer adds a small code→catalog mapping for friendlier copy, a renderer test MUST prove every UNMAPPED code falls back to the server safe message (`env.error.message`) — never a blank or guessed message; the default remains rendering the server safe message verbatim. PRESERVE the corrected data contract: list rows are `RendererLink` ONLY (status + lifecycle, NO export flags); `LINK_RESPONSE_FIELDS` MUST NOT gain `exportFlag`; export flags live only in the export-citations panel. The renderer is NOT the security boundary: it strips outgoing DTOs, never forwards `tenantId`/`actorUserId` (main injects identity), never bypasses IPC DTO projection or safe-message handling; the error model renders the server safe message verbatim by default with an optional code→catalog mapping carrying a MANDATORY fallback. If any of the following proves REQUIRED before the UI can complete, STOP and report: a persistence/schema/`CURRENT_SCHEMA_VERSION`/audit-contract change; an IPC/preload-handler change beyond consuming the merged API; an `LINK_RESPONSE_FIELDS` `exportFlag` addition; package-lock/dependency churn; a runtime A0.7 mechanism.
Source of truth: dev-memo/design/2026-06-26-audited-evidence-links-ui.md (THE design; sections 1-12); apps/lawbar-desktop/renderer/screens/{viewMatterFacts,viewMatterDeadlines,viewMatter}.ts (READ for the disclosure/create/two-step/status-render precedents); apps/lawbar-desktop/renderer/{api,types}.ts (the bridge + strip-allowlist precedents); apps/lawbar-desktop/renderer/i18n/{catalog,t,labels}.ts (i18n); apps/lawbar-desktop/src/caseBox/dto/link.ts (READ-ONLY — the canonical link DTOs + `*_LINK_DTO_FIELDS` + `RendererLink` + `LINK_RESPONSE_FIELDS` the UI mirrors); apps/lawbar-desktop/src/caseBox/linkHandlers.ts + electron/preload.mts (READ-ONLY — the 5 channels + preload methods the UI calls); docs/adr/ADR-evidence-a3-link-ipc-surface.md (the IPC contract + A0.7-is-dev-time D4); UI-GATES.md (the Design-artifact UI gate); dev-memo/deferred-audit-findings.md (LINK-IPC-T1-D1 stays DEFERRED — carried to a future WI, NOT closed here; D2/D3/D4 stay deferred); .claude/rules/{client-local-first,autonomy,cc-suite,evidence-genie,loc-guardian,execution-discipline}.md
Allowed files: apps/lawbar-desktop/renderer/screens/viewMatterLinks.ts, apps/lawbar-desktop/renderer/screens/viewMatter.ts, apps/lawbar-desktop/renderer/api.ts, apps/lawbar-desktop/renderer/types.ts, apps/lawbar-desktop/renderer/i18n/catalog.ts, apps/lawbar-desktop/renderer/i18n/ui-strings-allowlist.json, apps/lawbar-desktop/tests/renderer-links-list.test.mjs, apps/lawbar-desktop/tests/renderer-links-create.test.mjs, apps/lawbar-desktop/tests/renderer-links-unlink.test.mjs, apps/lawbar-desktop/tests/renderer-links-relink.test.mjs, apps/lawbar-desktop/tests/renderer-links-export.test.mjs, apps/lawbar-desktop/tests/renderer-api.test.mjs, apps/lawbar-desktop/tests/renderer-dto-sync.test.mjs, apps/lawbar-desktop/tests/renderer-i18n-guard.test.mjs, apps/lawbar-desktop/package.json, dev-memo/run/queue.md, dev-memo/run/queue.linted, dev-memo/run/queue.reviewed, dev-memo/run/queue.governed, dev-memo/run/reviews/queue-review-096.md, dev-memo/study/**, dev-memo/cc-suite-reliability-log.md, dev-memo/deferred-audit-findings.md
Forbidden files: apps/lawbar-desktop/src/caseBox/** (READ-ONLY — NO IPC/handler/DTO change; the canonical link DTOs + `LINK_RESPONSE_FIELDS` are merged and must NOT change; NO `exportFlag` on `LINK_RESPONSE_FIELDS`), apps/lawbar-desktop/electron/** (NO IPC registration/preload change — consume the merged channels only), services/** (NO persistence change), services/case-box-persistence/src/sqlite/schema.ts (NO schema/`CURRENT_SCHEMA_VERSION` change), docs/contracts/** (NO contract change), docs/adr/** (NO ADR edit — D3 is a separate docs lane), package-lock.json + dist-tarballs/** (NO dependency/tarball churn — never staged), native/**, .claude/**, .github/**, AGENTS.md, CLAUDE.md, GEMINI.md, scripts/workflow/**, migrations/**, committed dev-memo/run/evidence/** marker/ledger/key/evidence-run files, committed keys, dev-memo/run/reports/** (untracked local run-state — never staged), dev-memo/run/override-reason.md (untracked residue — never staged), Evidence-Genie-M0-Developer-Handover.md (root intake — READ-ONLY, never moved/deleted), xiaolai-dev-workflow-study.md (root intake — READ-ONLY, never moved/deleted); NO persistence/schema/contract/IPC/preload change, NO `LINK_RESPONSE_FIELDS` exportFlag addition, NO runtime A0.7 mechanism, NO D2/D3/D4 docs cleanup here, NO dependency/package-lock change, NO push/merge/branch-delete
Gates: broker /cc-suite:review-plan REQUIRED before implementation (confirm the UI consumes the merged IPC with no IPC/persistence/schema/contract change; confirm the `RendererLink`-only list-row + export-flags-only-in-panel data contract; confirm `LINK_RESPONSE_FIELDS` is NOT extended; confirm the renderer-is-not-the-security-boundary posture (DTO strip; no actor/tenant from renderer; server-safe-message + fallback-guarded optional catalog map); confirm the A0.7-commit-gated/no-runtime-marker posture — or record a concrete reason to downgrade `Requires-A07`; confirm LINK-IPC-T1-D1 stays DEFERRED (NOT closed here; unseedable in-lane without forbidden scope/new infrastructure) and is correctly carried to a future WI; confirm a11y/i18n match the renderer conventions; confirm the package.json additive test-list edit is the right mechanism for the new test files); if review-plan returns NEEDS-FIX, STOP and report; A0.7 (custody 9b) — human mints the marker + runs `A07_REQUIRED=1 check-a07-gate.sh` and `A07_REQUIRED=1 check-gates.sh` (must end GATES OK) BEFORE the impl commit; the agent never receives/echoes/persists the HMAC key and stages NO marker/evidence/key/report files; npm --prefix apps/lawbar-desktop test (all renderer + ipc-unit + the 5 new link tests GREEN); scripts/workflow/check-contract-integrity.sh exits 0; broker /cc-suite:audit + /cc-suite:verify on the impl diff (no Critical/High/Medium open before commit); no apps/src-caseBox/electron/services/schema/contract/package-lock/dependency change; CURRENT_SCHEMA_VERSION remains 12; no committed marker/ledger/key/evidence-run; root intake untouched
Acceptance criteria: the renderer exposes the 5 link methods with typed DTOs (`renderer/api.ts` + `renderer/types.ts`); the `RENDERER_*_LINK_DTO_FIELDS` allowlists strip outgoing DTOs and `renderer-dto-sync.test.mjs` PAIRS prove renderer↔canonical link-field parity (set-equal); the renderer strips `tenantId`/`actorUserId` from link DTOs (`renderer-api.test.mjs`); a per-matter "Evidence links" disclosure (`viewMatterLinks.ts` wired into `viewMatter.ts`) lazy-loads the matter's links; list rows render resolver status (valid/needs_review/broken) + lifecycle (active/unlinked + `unlink_reason`) as a visible label + `data-status` (never color-only) and DO NOT show export flags; the export-citations panel renders `{citations, byFlag}` with export flags shown only there; create/unlink/relink call the correct API methods; unlink requires a non-empty reason (client + server); the gate/error state is visible + safe (inline `role="alert"`, server safe message verbatim by default with a fallback-guarded optional catalog map); no evidence content leaks through the link DTOs/UI/fixtures (`check-no-real-data` GREEN; synthetic ids only); IF an optional error-code→catalog map is added, a renderer test proves unmapped codes fall back to the server safe message (`env.error.message`); LINK-IPC-T1-D1 REMAINS DEFERRED (NOT closed here; `tests/casebox-ipc.electron.test.mjs` NOT edited) and is explicitly recorded as carried-forward in `dev-memo/deferred-audit-findings.md` (a separate future WI: a minimal anchor IPC surface OR a sanctioned Electron integration-test anchor-seed capability); the 5 new renderer test files are registered in `package.json`'s curated `test` script (additive); `npm --prefix apps/lawbar-desktop test` GREEN; existing renderer tests remain GREEN; `scripts/workflow/check-contract-integrity.sh` exits 0; `CURRENT_SCHEMA_VERSION` remains 12; broker review-plan READY + audit no Critical/High/Medium + verify ALL CLOSED; A0.7 custody-9b gated PASS recorded by the human (GATES OK; key unset; no marker/evidence/key/report staged); the committed diff is confined to the Allowed files
Risk flags: UI — renderer implementation over a COURT-FACING audited-link presentation (status/citation/lifecycle). A0.7 COMMIT-GATED (custody 9b) per the design + lane authorization, though the renderer executes no A0.7-dependent code (no geometry/marker). HIGH-RISK adjacency: it presents citation/export-flag/audit-lifecycle data, so the data-contract integrity (RendererLink-only rows; export flags only in the panel; no `LINK_RESPONSE_FIELDS` exportFlag; no chain internals; identifiers-only confidentiality) and the renderer-is-not-the-security-boundary posture (DTO strip; main-injected identity; server-safe-message) are load-bearing — broker review-plan + audit + verify REQUIRED. The lane MUST NOT change persistence/schema/`CURRENT_SCHEMA_VERSION`/contract/IPC/preload, MUST NOT add `exportFlag` to `LINK_RESPONSE_FIELDS`, MUST NOT design a runtime A0.7 mechanism, MUST NOT expose raw evidence content in the UI or any fixture, MUST NOT churn package-lock/dependencies, and MUST NOT weaken any Evidence invariant (offline/local-first; main-injected identity; identifiers-only confidentiality; A0.7-dev-time). If the impl proves an IPC/persistence/schema/contract/`LINK_RESPONSE_FIELDS`/dependency change is REQUIRED, STOP and report. LINK-IPC-T1-D1 REMAINS DEFERRED by decision (review-plan round-1 High, confirmed unseedable in-lane without forbidden scope/new infrastructure) — NOT closed here; carried to a separate future WI. No push, no merge, no branch delete; D1/D2/D3/D4 stay deferred.
Depends on: none
Commit boundary: governance (mark-reviewed + govern) run STANDALONE before the commit (never bundled with git commit); A0.7 custody-9b gated PASS reported by the human BEFORE the impl commit. ONE impl commit -- the renderer source + tests + package.json test-list (Allowed files), staged by exact path; governance + deferred-findings (the D1 carry-forward/deferral record — NOT a D1 closure) may be in the same commit or a separate governance commit, exact-path staged; NO src/caseBox, electron, services, schema, contract, package-lock, or dependency file; NO marker/evidence/key/report file; no push; no merge
