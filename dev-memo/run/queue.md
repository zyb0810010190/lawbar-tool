## WI-FORMS-T3-S3-DOCX-EXPORT-00: T3 证据目录及说明 DOCX export via the `docx` library (Type: IMPL)
Type: IMPL
Design artifact: dev-memo/adr-forms-t3-s3-docx-export.md (S3 DOCX decision ADR; §2 decision, §5 tests, §6 minimal UI trigger — the design reference the future PR's check-ui-design-artifact line points to)
Scope: Implement ONLY the T3 证据目录及说明 DOCX export as decided in `dev-memo/adr-forms-t3-s3-docx-export.md` (BINDING). Mechanics: (a) ADD the maintained `docx` npm package as a runtime dependency of `apps/lawbar-desktop` (exact pinned version + lockfile integrity), gated by a MANDATORY dependency-risk review (see Gates) — NO other new dependency; reject any transitive native binding / node-gyp step / network I/O / template-execution engine; (b) a NEW main-process / export-layer builder module (e.g. `apps/lawbar-desktop/src/caseBox/export/t3DocxExport.ts`) that takes a `T3CatalogModel` (the merged S1 shape) and builds a `docx` `Document` — one table with DR-00's four columns 序号/证据名称/证明内容/页码 (one row per S1 model row, S1 order, 序号 1..n), a header (提交人诉讼地位 + 名称/姓名), reviewNeeded cells rendered as an explicit needs-review marker, Chinese/multi-clause text verbatim (NFC); it consumes the merged `buildT3CatalogModel` output and MUST NOT re-implement it, MUST NOT read persistence/contract/documents directly, MUST NOT attach a 卷X页Y/citation column, MUST NOT couple to the A10 CanonicalExportModel; (c) a single export path delivering the `.docx` via a MAIN-PROCESS SAVE DIALOG (ADR §6 decision) — the renderer NEVER handles raw `.docx` bytes: a new export operation (an IPC channel e.g. `casebox:t3:exportDocx` and/or main save-dialog wiring in electron/main.ts) builds the `T3CatalogModel` from the SAME drained-accepted-evidence + `buildT3CatalogModel` source as the S2 preview (mirroring the S2 handler's tenant/matter scoping), packs the `docx` Document to a buffer, and writes it via `dialog.showSaveDialog`, returning ONLY a structured status result — `{ written: true }` on save, `{ written: false }` on user cancel (a no-op success, NOT an error), a structured error on build/write failure, and NO document for a `{kind:"refusal"}` model; default filename ends `.docx`, dialog filter is DOCX, overwrite is owned by the OS dialog; (d) the minimal S2 UI trigger per ADR §6 — a `<button type="button" data-test-id="view-t3-export-docx">导出 DOCX` inside the existing `renderer/screens/viewMatterT3Catalog.ts` disclosure that invokes the export path and reports success/error inline (`role="alert"` on error); NO new screen/route/redesign; strings via the i18n catalog; (e) tests + package.json test registration. FORBIDDEN in this lane: PDF generation of any kind; any T4/T5 (证明对象/三性/质证) fields or behavior; any evidence WRITE path; any `CURRENT_SCHEMA_VERSION`/schema/migration/DDL/contract/persistence-source change; modification or re-implementation of `t3CatalogModel.ts` or any a10* module; hashing raw `.docx`/PDF bytes as the reproducibility unit (use a normalized-OOXML / logical-structure golden per A10 invariant 9); a 卷X页Y/citation column; custody/marker/seal/JS-shim/A8 work; broad renderer/UI redesign; any new dependency beyond `docx` + its reviewed transitive set; raw client samples or tracked intake fixtures. If a schema/contract/persistence change or a second dependency appears necessary, STOP and report (do not expand scope). If review/audit asks for PDF/T4-T5/court-fileable-A10-coupling/broader forms work, REJECT as out of scope and report.
Source of truth: dev-memo/adr-forms-t3-s3-docx-export.md (BINDING S3 decision: `docx` library, render S1 model only, no 卷X页Y, no PDF, dependency controls, normalized-OOXML golden, minimal trigger); apps/lawbar-desktop/src/caseBox/export/t3CatalogModel.ts (S1 model — consumed, never modified/re-implemented); apps/lawbar-desktop/src/caseBox/t3Handlers.ts + renderer/screens/viewMatterT3Catalog.ts (S2 surface the trigger attaches to; scoping precedent); dev-memo/plan-forms-t3-evidence-catalog-00.md §2/§4 (S3 double-gate now resolved); dev-memo/forms-spec-a10-t3-t5-sample-adr-00.md §H DR-00; docs/adr/ADR-evidence-a10-court-fileable-export.md (invariant 9 — never hash raw .docx bytes); .claude/rules/autonomy.md (new-runtime-dependency hard stop) + cc-suite.md + client-local-first.md + evidence-genie.md + execution-discipline.md + staging-hygiene.md + loc-guardian.md
Allowed files: apps/lawbar-desktop/package.json (add `docx` dependency + register new test files), apps/lawbar-desktop/package-lock.json (docx + reviewed transitive lockfile entries), apps/lawbar-desktop/src/caseBox/export/t3DocxExport.ts (NEW builder), apps/lawbar-desktop/src/caseBox/ (new export IPC handler + registration wiring if an IPC channel is used), apps/lawbar-desktop/src/caseBox/handlerShared.ts (channel-name constant only, if used), apps/lawbar-desktop/src/caseBox/dto/t3.ts (export request DTO addition, if an IPC channel is used), apps/lawbar-desktop/electron/ipc/caseBoxHandlers.ts (register the export channel, if used), apps/lawbar-desktop/electron/preload.mts (bridge the export channel, if used), apps/lawbar-desktop/electron/main.ts (main-process save-dialog wiring, if the delivery uses a dialog), apps/lawbar-desktop/renderer/api.ts (expose the export path, if used), apps/lawbar-desktop/renderer/types.ts (export DTO/result type), apps/lawbar-desktop/renderer/screens/viewMatterT3Catalog.ts (the minimal export trigger only), apps/lawbar-desktop/renderer/i18n/ (new UI strings via allowlist), apps/lawbar-desktop/tests/ (new export builder + IPC/renderer/golden tests). NOTE: the S3 decision ADR + this queue governance + reviews/queue-review-125.md are committed by the SEPARATE authoring lane (this WI's authoring PR), NOT re-staged in the future implementation commit — the implementation commit is code-only and records its cc-suite chain in the commit message (per the S2 precedent). The implementation lane MUST NOT modify the ADR or queue governance artifacts.
Forbidden files: apps/lawbar-desktop/src/caseBox/export/t3CatalogModel.ts (S1 model — consumed READ-ONLY, never modified), apps/lawbar-desktop/src/caseBox/export/a10CanonicalExportModel.ts + a10CitationContract.ts + a10HyperlinkDegradation.ts + a10LivePipeline.ts (untouched), docs/contracts/ (NO contract change), services/ (NO persistence-source change), services/case-box-persistence/src/sqlite/schema.ts (CURRENT_SCHEMA_VERSION stays 12), migrations/ (none; none created), native/, dependency/version fields of any package OTHER than the single `docx` addition to apps/lawbar-desktop, dev-memo/run/intake/ (raw samples — never staged), docs/adr/ + docs/product/ + other dev-memo design docs (READ-ONLY), scripts/ + .claude/ + .github/, Evidence-Genie-M0-Developer-Handover.md + xiaolai-dev-workflow-study.md; NO PDF; NO T4/T5; NO evidence write; NO 卷X页Y citation column; NO custody/marker/seal; NO JS shim; NO A8; NO second new dependency; push limited to this WI's scoped branch + PR open; NO merge; NO branch delete
Gates: FULL chain + DEPENDENCY review (new runtime dependency = HIGH-RISK): (1) broker /cc-suite:review-plan on this queue WI + the S3 decision ADR BEFORE implementation; (2) MANDATORY dependency-risk review — run `/codex-dependency-review` (dependency_risk_reviewer) on the actual apps/lawbar-desktop package.json + package-lock.json diff, clearing for `docx` AND its full transitive set: exact pinned version + recorded integrity hash; license compatibility (MIT/permissive; flag copyleft/unknown); NO install/preinstall/postinstall lifecycle scripts running arbitrary code; optional/peer-dependency surface reviewed; maintainer/release provenance; known advisories (`npm audit`/GHSA clean or each triaged); package size / packaged-Electron bundle impact; Node/Electron packaging compatibility (pure-JS, Node 22.x/24.x + electron-builder, no arch-specific rebuild); no native binding, no network, no template-execution engine; (3) implement per the ADR; (4) tests — `npm --prefix apps/lawbar-desktop test` PASS (full suite incl. new DOCX export builder + IPC/renderer + normalized-OOXML golden tests), `scripts/workflow/check-contract-integrity.sh` exit 0, `scripts/workflow/check-queue.sh` PASS; regression — `npm --prefix docs/contracts/case-box-contract test` + `npm --prefix services/case-box-persistence test` PASS; (5) broker /cc-suite:audit on the implementation diff; (6) fix Critical/High/Medium findings in-scope; (7) broker /cc-suite:verify consuming the audit artifact; (8) staged-file check (exact paths only; raw samples + residue never staged). Forbidden-scope scan before commit: no CURRENT_SCHEMA_VERSION change; no migration/DDL; no native; no raw-sample/client-fixture commit; no PDF; no T4/T5; no 卷X页Y column; no S1-model/a10 change; no contract/persistence change; no second new dependency; no raw .docx-byte hashing as the golden. UI design-artifact: the S3 impl PR body MUST carry `Design artifact: dev-memo/adr-forms-t3-s3-docx-export.md` (the ADR §6 trigger reference) because it touches renderer UI paths (check-ui-design-artifact). STOP-points: dependency review flags an unacceptable risk → STOP and report; schema/contract/persistence change or a second dependency appears necessary → STOP; review/audit requests out-of-scope work → REJECT and report; any forbidden file would change → STOP. NO swiftpm-smoke unless native is unexpectedly touched (it must not be).
Acceptance criteria: the `docx` dependency is added to apps/lawbar-desktop with an exact pinned version + lockfile integrity and PASSES the dependency-risk review (no native/network/template-exec); a T3CatalogModel builds a `.docx` Document with a header (提交人诉讼地位 + 名称) and a four-column table (序号/证据名称/证明内容/页码), one row per S1 model row in S1 order, 序号 1..n; reviewNeeded cells render an explicit marker (never blank-as-data); a {kind:"refusal"} model produces NO document and surfaces the refusal; Chinese multi-clause proof text renders verbatim (NFC); notes/filename/party_side never appear as 证据名称/证明内容/页码; no 卷X页Y/citation column exists and 页码 equals exhibit_page_range; only accepted rows appear; the reproducibility golden includes BOTH a structural assertion (header text + ordered 4-column rows + 序号 1..n + reviewNeeded markers) AND a focused normalized-OOXML assertion on the extracted main document part (table header + row text), and NEVER a raw .docx-byte hash (A10 invariant 9); the export delivers via the main-process save dialog with the renderer never handling raw bytes (cancel → {written:false} no-op success; refusal → no document; error → inline role=alert); the minimal 导出 DOCX trigger invokes the export path and reports success/error inline; the full desktop suite passes; check-contract-integrity + check-queue pass; CURRENT_SCHEMA_VERSION remains 12 and no migration/contract/persistence change; committed diff contains ONLY Allowed files; raw samples remain untracked. review-plan READY, dependency-review acceptable, audit C/H/M all closed, verify ALL CLOSED.
Risk flags: HIGH — introduces the first new runtime dependency of the forms track (`docx`, an autonomy hard-stop the user has authorized), plus a DOCX generation surface + a UI trigger. Care points: (a) DEPENDENCY — exact pin + lockfile integrity + mandatory dependency-risk review; no native/network/template-exec; no second dep; (b) SINGLE SOURCE OF TRUTH — render the S1 model only; never re-implement buildT3CatalogModel or couple to A10; (c) DETERMINISM/GOLDEN — normalized OOXML / logical-structure assertion, NEVER raw .docx-byte hashing (A10 invariant 9); (d) NON-PROMOTION + reviewNeeded fidelity + accepted-only filter carried from S1; (e) NO 卷X页Y, NO PDF, NO T4/T5, NO evidence write; (f) MINIMAL UI — one export button, no redesign; (g) LOC — new files under thresholds; (h) INPUT-ONLY SAMPLES — never staged; synthetic golden/QA data only. If any cannot hold, STOP and report.
Depends on: none
Commit boundary: TWO separate lanes. (1) THIS authoring lane commits ONLY the decision ADR (dev-memo/adr-forms-t3-s3-docx-export.md) + queue governance (queue.md/linted/reviewed/governed) + reviews/queue-review-125.md — no implementation, no dependency addition. (2) The FUTURE implementation lane commits ONE code-only commit staging the Allowed-files code by exact path (docx dependency in package.json + lockfile + t3DocxExport.ts + export handler/save-dialog wiring + minimal trigger in viewMatterT3Catalog.ts + api/preload/types/i18n as needed + new tests + package.json test registration); it does NOT re-stage the ADR / queue governance / queue-review-125.md (those are already committed by lane 1) and records its cc-suite review-plan/dependency-review/audit/verify chain in the COMMIT MESSAGE (per the S2 precedent). NO S1-model/a10/contract/persistence/schema/migration/native file; NO staging of dev-memo/run/intake/ or untracked residue; the impl PR body carries `Design artifact: dev-memo/adr-forms-t3-s3-docx-export.md`; push of the scoped WI branch + PR open; no merge; no branch delete.
