## WI-A3-PAGE-T2: DocumentPageGeometry schema V10 (case-box-persistence; A0.7-gated)
Type: IMPL
Requires-A07: yes
A07 custody mode: 9b. Human runs marker mint + A0.7-gated check-gates directly; agent does not receive the HMAC key. The implementation commit is BLOCKED until human-run gated verification reports PASS (A07_REQUIRED=1 check-a07-gate + A07_REQUIRED=1 check-gates both PASS with LAWBAR_A07_MARKER_HMAC_KEY in the human's shell, and no marker/ledger/key/evidence-run staged or committed). The agent never exports/echoes/commits/mints the key, never weakens or drops Requires-A07, never bypasses the gate.
Scope: Add SCHEMA-ONLY version V10 to services/case-box-persistence for the DocumentPageGeometry geometry-version owner, per A3-PAGE-00 decision 6 + A3-SCHEMA-00 §6 (reconciled, no-FK). The SECOND foundation implementation lane: captured page-geometry provenance, AFTER DocumentPage (V9), BEFORE anchors (A3-T1-IMPL). NO anchor/link tables, NO resolver/status, NO export, NO UI/product, NO production evidence ingestion, NO encryption/key management, NO new dependency, NO package.json/lock change, NO SQLite FK constraints, NO marker/key/gate change, NO geometry model broader than anchors need. Changes confined to services/case-box-persistence/src/sqlite/schema.ts (add DDL_STATEMENTS_V10 + register [10, DDL_STATEMENTS_V10] in DDL_BY_VERSION + bump CURRENT_SCHEMA_VERSION 9->10, via the existing idempotent applySchema/one-BEGIN/COMMIT/forward-only convention; no down-migration, no destructive reset) and services/case-box-persistence/tests/hardening-schema.test.mjs (extend the EXISTING in-script schema test with V10 cases — no package.json test-list change).
Schema requirements (geometry-version owner; columns from A3-PAGE-00 decision 6 / handover §10): table case_box_document_page_geometries with columns: id TEXT PRIMARY KEY; tenant_id TEXT NOT NULL; matter_id TEXT NOT NULL; document_id TEXT NOT NULL (app-layer invariant -> case_box_documents.id, NO FK); physical_page_index INTEGER NOT NULL CHECK (physical_page_index >= 0) (0-based, consistent with V9 DocumentPage); resolved_box TEXT NOT NULL CHECK (resolved_box IN ('cropBox','mediaBox')); bounds_x / bounds_y / bounds_width / bounds_height (the resolved page box in PDF page space; numeric STORAGE TYPE to be RESOLVED by review-plan — see TWO OPEN DECISIONS below); rotation INTEGER NOT NULL CHECK (rotation IN (0,90,180,270)); captured_at TEXT NOT NULL COLLATE BINARY (the geometry VERSION key = the anchor's geometryCapturedAt); created_at TEXT NOT NULL COLLATE BINARY; payload_json TEXT NOT NULL (canonical record; pdf_kit_version? and any non-lifted fields live here). No viewport/screen columns; no anchor/link tables; no FK. The app-layer identity invariant (A3-PAGE-00 §"App-layer identity invariant"): a geometry row's (document_id, physical_page_index) references the SAME canonical page identity owned by V9 case_box_document_pages — one page-identity owner, no parallel path.
TWO OPEN DECISIONS (review-plan MUST resolve; do NOT guess — stop-and-ask if review-plan leaves either ambiguous): (1) BOUNDS NUMERIC STORAGE TYPE — the ADR does not specify it. RECOMMEND fixed-decimal TEXT (byte-stable, consistent with A3-SCHEMA-00's page_ratio 12-dp TEXT decision; PDF box coords can be fractional, so REAL would reintroduce float drift and INTEGER points would lose fractional precision). review-plan picks one of {fixed-decimal TEXT (+ a precision), INTEGER micro-units, REAL}; if it does not clearly resolve it, STOP and report options. (2) UNIQUENESS MODEL — the merged A3-PAGE-00 ADR + handover §10 specify UNIQUE(documentId, physicalPageIndex) (SINGLE-CURRENT geometry per page; captured_at is that row's current version; re-capture updates the row; an anchor with a different geometryCapturedAt -> mismatch -> needs_review, matching INV-A3-6). The lane authorization suggested UNIQUE(document_id, physical_page_index, captured_at) (version-history; multiple versions per page), which DEVIATES from the merged ADR and would need a current-version marker. RECOMMEND the ADR's UNIQUE(document_id, physical_page_index) single-current model (the merged contract, simpler, sufficient for anchor mismatch detection, and "no geometry model broader than anchors need"). review-plan confirms UNIQUE(document_id, physical_page_index) OR explicitly approves a different equivalent; if it recommends the version-history captured_at variant (an ADR deviation) OR leaves it ambiguous, STOP and report.
Source of truth: docs/adr/ADR-evidence-a3-page-geometry-foundation.md (A3-PAGE-00 decision 6 DocumentPageGeometry owner fields + UNIQUE(documentId, physicalPageIndex) + the app-layer identity invariant; no viewport; no production evidence); Evidence-Genie-M0-Developer-Handover.md (root intake; READ-ONLY) §10 DocumentPageGeometry shape (resolvedBox cropBox|mediaBox, boundsX/Y/W/H, rotation, pdfKitVersion?, capturedAt; UNIQUE(documentId, physicalPageIndex); capturedAt = version), §5 A0.7 geometry stability; docs/adr/ADR-evidence-a3-schema-persistence-contract.md (A3-SCHEMA-00 — byte-stable fixed-decimal TEXT precedent for page_ratio; §6 no-FK reconciliation); docs/adr/ADR-evidence-a3-persistence-substrate.md (A3-DB-00 — encryption hard stop stands); services/case-box-persistence/src/sqlite/schema.ts (the V9 case_box_document_pages + the applySchema/DDL_BY_VERSION/CHECK/column convention) + tests/hardening-schema.test.mjs (the schema-test pattern to extend); docs/adr/ADR-evidence-a07-key-custody-operating-model.md (A07-KEY-00 — custody 9b). User decision 2026-06-24: physical_page_index is 0-based.
Allowed files: services/case-box-persistence/src/sqlite/schema.ts (DDL_STATEMENTS_V10 + DDL_BY_VERSION registration + CURRENT_SCHEMA_VERSION bump + a version-10 comment), services/case-box-persistence/tests/hardening-schema.test.mjs (extend with V10 cases), dev-memo/** (queue/governance/review/study/report artifacts)
Forbidden files: services/case-box-persistence/package.json + package-lock.json (NO dependency/test-list change), any other services/** file, docs/contracts/** (NO change), apps/**, native/**, scripts/workflow/** (NO gate/marker change), .claude/**, .github/**, AGENTS.md, CLAUDE.md, GEMINI.md, committed dev-memo/run/evidence/** marker/ledger/key/evidence-run files, committed keys, Evidence-Genie-M0-Developer-Handover.md (root intake), xiaolai-dev-workflow-study.md (root intake), dev-memo/run/override-reason.md (untracked residue — never staged); NO anchor/link tables, NO viewport/screen columns, NO SQLite FK constraints, NO new dependency, NO package.json change, NO resolver/status/export/UI/product, NO production evidence ingestion, NO encryption/key management, NO marker/key/gate change, NO migration beyond schema V10, NO geometry model broader than anchors need
Gates: broker /cc-suite:review-plan REQUIRED before impl (RESOLVE the two open decisions — bounds numeric storage type + the UNIQUE model; confirm the geometry columns match A3-PAGE-00 decision 6; confirm 0-based index, no-FK app-layer invariant, the page-identity invariant to V9 case_box_document_pages; if review-plan leaves the bounds numeric type ambiguous OR recommends the version-history captured_at UNIQUE deviation OR leaves UNIQUE ambiguous, STOP and report; if review-plan asks for a dependency, encryption/key-mgmt, anchors/links, UI/product, FK constraints, marker/key/gate changes, or a broader geometry model, STOP and ask); broker /cc-suite:audit + /cc-suite:verify after impl AND after the human-run gated PASS; A0.7-gated under custody 9b — the implementation commit is BLOCKED until the human reports A07_REQUIRED=1 check-a07-gate PASS + A07_REQUIRED=1 check-gates PASS (human shell holds the key) with no marker/ledger/key/evidence-run staged or committed; agent-side (non-secret): npm --prefix services/case-box-persistence test passes (incl. the new V10 cases + all existing tests green incl. V9); scripts/workflow/check-contract-integrity.sh exits 0; npm --prefix apps/lawbar-desktop test 610/610; npm --prefix native/evidence-core test 38/38; the A0.7 gate is intact (A07_REQUIRED=1 no marker/key fails closed; not-required passes); the agent's own check-gates correctly FAILS CLOSED at the A0.7 gate (queue Requires-A07, agent has no key — expected under 9b); no package.json/dependency change; no anchor/link/viewport/FK; no apps/native/scripts/.claude/.github change; no committed marker/ledger/key/evidence-run
Acceptance criteria: services/case-box-persistence/src/sqlite/schema.ts adds DDL_STATEMENTS_V10 (case_box_document_page_geometries + index) registered as [10, DDL_STATEMENTS_V10] with CURRENT_SCHEMA_VERSION = 10, using the review-plan-approved bounds numeric storage type and the review-plan-approved UNIQUE (default: ADR's UNIQUE(document_id, physical_page_index) single-current); services/case-box-persistence/tests/hardening-schema.test.mjs PASSES new V10 cases proving: V10 applies from empty; V9 -> V10 upgrade preserves existing DocumentPage data; schema_version becomes 10; case_box_document_page_geometries exists with the expected columns; document_id + physical_page_index + captured_at are required (NOT NULL); resolved_box CHECK rejects values outside cropBox/mediaBox; rotation CHECK rejects values outside 0/90/180/270; physical_page_index = 0 accepted; negative physical_page_index rejected; the approved UNIQUE is enforced; NO viewport/screen columns; NO anchor/link tables; NO SQLite FK (PRAGMA foreign_key_list empty); the V9 DocumentPage tests + all existing case-box tests remain green; npm --prefix services/case-box-persistence test exits 0; scripts/workflow/check-contract-integrity.sh exits 0; npm --prefix apps/lawbar-desktop test 610/610; npm --prefix native/evidence-core test 38/38; no package.json/package-lock change; the human-run A0.7-gated verification (custody 9b) reports PASS before the implementation commit; broker review-plan READY (both open decisions resolved) + audit no Critical/High/Medium + verify ALL CLOSED; the committed diff is confined to services/case-box-persistence/src/sqlite/schema.ts + services/case-box-persistence/tests/hardening-schema.test.mjs + dev-memo/** governance artifacts
Risk flags: PERSISTENCE / MIGRATION IMPL — A0.7-DEPENDENT (Requires-A07: yes), custody mode 9b — the implementation commit is BLOCKED until the human runs the A0.7-gated check-gates with the HMAC key and reports PASS; the agent never receives/echoes/commits/mints the key, never weakens/drops Requires-A07, never bypasses the gate. TWO open decisions (bounds numeric type; UNIQUE model) routed to review-plan with recommendations (fixed-decimal TEXT; ADR's UNIQUE(document_id, physical_page_index) single-current) — STOP and report if review-plan leaves either ambiguous or recommends deviating from the merged ADR. Schema-only V10 DocumentPageGeometry: NO anchor/link, NO viewport columns, NO FK (app-layer invariant), NO new dependency, NO package.json change, NO UI/product, NO production evidence ingestion, NO encryption/key management, NO broader geometry model. Migration forward-only (no down-migration; rollback via git revert + a new forward version). broker review-plan + audit + verify REQUIRED. Must not weaken any Evidence invariant nor the A3-DB-00 encryption hard stop. No push, no merge.
Depends on: none
Commit boundary: governance commit separate (dev-memo/run/queue.md + queue.linted/reviewed/governed + dev-memo/run/reviews/queue-review-075.md); one implementation commit -- services/case-box-persistence/src/sqlite/schema.ts + services/case-box-persistence/tests/hardening-schema.test.mjs, staged by exact path, ONLY after human-run gated PASS + broker audit/verify; no push; no merge
