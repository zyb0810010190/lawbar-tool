## WI-RELEASE-ELECTRON-RUNTIME-ADVISORY-BUMP-00: define + govern the Electron runtime-advisory remediation lane (bump the v1 Mac-client Electron runtime off 34.5.8 to the minimal npm-audit-verified clean version to resolve the gate-19 surfaced R-G19-1 — 2+ HIGH advisories on the shipped runtime — with a full desktop test/build gate, a `better-sqlite3` native-rebuild step, and a rollback plan; signing/notarization/distribution stays gate-4 user-owned) (Type: IMPL, framework/runtime-dependency HIGH risk)

## Review packet (compact)
- Active plan summary: gate 19's supply-chain sweep (`docs/release/gate19-supply-chain-posture-00.md`, WI-RELEASE-G19-SUPPLY-CHAIN-POSTURE-00) SURFACED **R-G19-1** — the v1 Mac client's shipped Electron runtime `electron 34.5.8` (declared a devDependency but bundled as the runtime by electron-builder) carries HIGH advisories (ASAR integrity bypass; macOS AppleScript injection; + additional use-after-free / command-line-switch-injection HIGHs in the cumulative audit range). This WI **governs** (does NOT execute) a FUTURE lane that performs the minimal safe Electron bump to a `npm audit`-verified clean version, re-verifies with `npm audit --omit=dev` + the desktop test/build suite, rebuilds the `better-sqlite3` native binding for the host, and records the result. This is an **engineering/security remediation** (the runtime advisory is fixable now), NOT merely a gate-4 distribution decision — but signing/notarization/public distribution + the final GO/NO-GO stay user-owned (gates 4/21). THIS authoring lane produces the governed queue WI only; it changes NO dependency/lockfile, runs NO bump, clears NO gate, and decides NO user hard-stop.
- Grounding note (verified 2026-07-08 via read-only `npm audit` on `apps/lawbar-desktop` — the exec lane MUST re-verify before relying, per `AGENTS.md` §"Authoring discipline"): declared `electron: ^34.0.0` (`apps/lawbar-desktop/package.json` devDeps), resolved **34.5.8**. `npm audit` reports the Electron advisory **vulnerable range `<=39.8.4`** and a **`fixAvailable: { name: electron, version: 43.1.0, isSemVerMajor: true }`** — i.e. the audit-resolved clean target is a **semver-MAJOR jump (34 → 43)**, not a 34.x patch. The cumulative Electron advisory list at this range includes multiple HIGH items (use-after-free in permission callbacks / PowerMonitor / offscreen paint; renderer command-line-switch injection; ASAR integrity bypass; AppleScript injection) plus moderates/lows. A major jump across ~9 Electron majors carries real breaking-change risk to the Electron-34 app (main-process API, BrowserWindow/webPreferences defaults, native ABI) — so the bump MUST be gated on the full desktop test/build suite + a `better-sqlite3` native rebuild, with a rollback plan. The exec lane MUST NOT hard-code `43.1.0` as gospel: it re-runs `npm audit` to confirm the minimal version that clears the HIGH advisories, prefers the lowest such version that also passes the test/build gate, and records the actual chosen version + its re-audit evidence.
- Exact target files (FUTURE exec lane): `apps/lawbar-desktop/package.json` (the `electron` devDependency version range ONLY — the minimal bump; NO other dependency touched) + `apps/lawbar-desktop/package-lock.json` (regenerated by the governed `npm install`/update of electron ONLY) + a NEW `docs/release/electron-runtime-advisory-bump-00.md` (the remediation evidence: before/after version, re-audit output, test/build results, native-rebuild note, rollback plan) + the gate-19 evidence ROW of `docs/release/go-live-readiness-report.md` (evidence-row TEXT ONLY — record R-G19-1 remediation status; **gate 19 stays `OPEN`**, gate 4 stays user-owned; NO roll-up/status-bucket edit) + `dev-memo/run/reviews/` (the exec lane's review artifact). NO other file. The lockfile change is confined to the electron subtree (+ its transitive electron-only deps); the exec lane MUST show `git diff` proves no unrelated dependency moved.
- Exact acceptance criteria: below.
- Exact out-of-scope: NO unrelated dependency upgrade (electron devDep ONLY — NOT electron-builder, NOT playwright/typescript/docx/better-sqlite3-version). **If the Electron bump strictly requires an `electron-builder` upgrade, that is a HARD STOP: roll back and open/amend a SEPARATE explicitly-authorized WI (with its own expanded allowed-files + gates) — never widen THIS WI.** NO schema/migration (`CURRENT_SCHEMA_VERSION` stays 12); NO contract/persistence CODE change (a `better-sqlite3` native REBUILD artifact is allowed ONLY as the governed post-bump ABI step, not a version bump or code change); NO OCR/Forms/custody/A8 work; NO product source/test LOGIC change (test/build are RUN, not rewritten). **If the Electron major bump strictly requires a product source/test compatibility change, that is a HARD STOP: roll back and open/amend a SEPARATE explicitly-authorized WI naming the concrete files — never a "minimal shim" folded into THIS WI.** NO ADR/brief edit; NO gate-6 full-project audit; NO broad readiness refresh; NO clearing of gates 4/6/12/13/17/19/20 (gate 19's R-G19-1 is *remediated/updated*, not *cleared*); NO signing/notarization/公开 distribution decision (gate 4, user-owned); NO 律师法 conclusion (gate 11); NO license/legal/business decision (gate 17); NO final GO/NO-GO (gate 21); NO staging of `dev-memo/run/intake/` or untracked residue.
- Requirements the FUTURE remediation must define + record (what the exec lane must specify + do) — at minimum:
  1. **Current Electron version + advisories (from gate 19)**: record the before-state — `electron 34.5.8`, the R-G19-1 HIGH advisories (ASAR integrity bypass; macOS AppleScript injection; + the cumulative HIGH set), and the `npm audit` vulnerable range `<=39.8.4`. Cite `docs/release/gate19-supply-chain-posture-00.md`.
  2. **Target version selection method (NOT hard-coded) — explicit candidate procedure**: (i) re-run `npm audit` and read the current vulnerable **range** (`<=39.8.4` at authoring time) + the `fixAvailable` (`electron@43.1.0`, semver-major) — re-verify both, do not trust the authoring snapshot; (ii) the minimal advisory-clean candidate is the LOWEST published Electron version STRICTLY ABOVE the vulnerable range (i.e. `> 39.8.4`) that `npm audit` confirms clears the HIGH advisories — test that candidate FIRST, not `43.1.0` by default; (iii) install the candidate, re-audit to confirm clean, and run the desktop test/build gate; (iv) only if the lowest candidate is NOT test/build-green, step up to the next clean candidate (up to the `fixAvailable` `43.1.0`), recording WHY each lower candidate was rejected (audit-not-clean OR test/build-fail with the concrete failure); (v) the chosen version is the LOWEST version that is BOTH advisory-clean AND test/build-green, recorded with its audit + test evidence — never a bare assertion, never `43.1.0` without proving no lower clean version passes. **Record the SOURCE used to enumerate candidate versions** (e.g. `npm view electron versions` against the registry) + the EXACT candidate versions tested and each one's outcome, so the minimality claim is auditable.
  3. **Exact package/lock files allowed to change + lockfile-confinement PROOF**: `apps/lawbar-desktop/package.json` (the `electron` devDep range only) + `apps/lawbar-desktop/package-lock.json` (regenerated for the electron subtree only). **Confinement proof (required, recorded in the evidence doc):** run `git diff apps/lawbar-desktop/package.json` (only the `electron` line changed) and enumerate EVERY package whose version changed in the lockfile diff, JUSTIFYING each as Electron-owned or an Electron-only transitive; if ANY non-Electron package moved (e.g. `electron-builder`, playwright, better-sqlite3), that is a HARD STOP → roll back and open/amend a SEPARATE explicitly-authorized WI, never silently widen THIS WI.
  4. **Required install/update command**: the exact command (e.g. `npm --prefix apps/lawbar-desktop install electron@<chosen> --save-dev` or `npm --prefix apps/lawbar-desktop update electron`), run so it mutates ONLY the electron subtree; record the command + its output.
  5. **Required `npm audit --omit=dev` (or equivalent) verification**: re-run the production audit AND the dev-inclusive audit after the bump; record that the R-G19-1 HIGH advisories on the runtime are CLEARED (or, if residual advisories remain, surface them honestly). The gate is: the shipped-runtime HIGH advisories are resolved.
  6. **Required desktop test/build checks**: run the desktop UI gate `npm --prefix apps/lawbar-desktop test` (incl. the Electron smoke/main/IPC tests `tests/smoke.electron.test.mjs`, `tests/main.test.mjs`, `test:ipc-packaged`) AND `npm --prefix apps/lawbar-desktop run dist` (electron-builder packaging) to prove the major bump does not break launch/packaging — if `run dist` is infeasible in-environment, that is allowed ONLY if the doc records the CONCRETE environment reason (e.g. no codesign identity available offline) AND the smoke/main/IPC launch tests still pass. Record pass/fail with counts. A test/build FAIL that cannot be fixed within this WI's minimal scope is a STOP (rollback per requirement 8), not a silent ship.
  7. **`better-sqlite3` native rebuild after the Electron bump**: an Electron major bump changes the Electron ABI, so the `better-sqlite3` native binding MUST be rebuilt for the new Electron (via `electron-builder install-app-deps` / the `postdist` restore per `AGENTS.md` §"Test-environment notes") and re-validated by the `abi-smoke` check (`services/ocr-persistence/scripts/abi-smoke.mjs` pattern). Record the rebuild + that the dev-Electron tests launch (no ~30s ABI launch timeout). This is the governed native-rebuild artifact step — it is NOT a `better-sqlite3` version change.
  8. **Rollback plan if the bump breaks build/tests**: the exact revert path — restore `apps/lawbar-desktop/package.json` + `package-lock.json` to the pre-bump state (`git restore` the two files, or `git revert` if committed), rebuild the native binding for Electron 34, re-run the desktop gate to confirm green. **Post-rollback verification (required):** confirm `apps/lawbar-desktop/package.json` `electron` is back to `^34.0.0`, the lockfile resolved `electron` is back to `34.5.8`, `git diff` of the two files is empty vs the pre-bump state, and the desktop gate is green again. If the bump cannot be made green within this WI's minimal scope, the lane STOPS, rolls back (with the verification above), and records R-G19-1 as "remediation attempted; blocked — needs a larger Electron-migration WI"; it does NOT ship a broken build and does NOT silently accept the advisory.
  9. **Expected evidence artifact**: `docs/release/electron-runtime-advisory-bump-00.md` recording before/after Electron version, the target-selection audit evidence, the install command + output, the `npm audit` before/after (HIGH advisories cleared), the desktop test/build results, the `better-sqlite3` native-rebuild + abi-smoke result, the rollback plan, and the environment (date, node/npm/electron versions).
  10. **Deterministic pass/fail + how it feeds gate 19 / gate 4 without clearing gate 4/17/6/go-live**: PASS = Electron bumped to a `npm audit`-verified version with the R-G19-1 HIGH advisories CLEARED AND the desktop test/build suite green AND the `better-sqlite3` native binding rebuilt + abi-smoke green AND the lockfile diff confined to the electron subtree. FAIL/STOP = advisories not cleared, a test/build regression not fixable in-scope, or an unrelated dependency dragged in → rollback + record. On PASS: the gate-19 R-G19-1 residual is UPDATED to "remediated" (evidence-row text; **gate 19 stays OPEN**, not cleared) and the gate-4 signing/notarization/distribution decision + gate-17 license + gate-21 GO/NO-GO stay user-owned; gate 6 is NOT run.
- Dependencies mapped: **gate 19** (R-G19-1 is this WI's input; the residual is *remediated/updated*, NOT the gate *cleared*); **gate 4** (signing/notarization/distribution — the packaged runtime is gate-4 territory; the bump reduces the runtime's advisory surface but does NOT make the gate-4 decision, user-owned); **gate 17** (license — electron stays MIT; no license decision); **gate 6** (full-project audit — LATER, not run); **gate 20** (no-telemetry — the bump must not introduce telemetry/crashReporter; re-affirm gate-20's posture holds). These are noted as related, NOT cleared by this WI.
- Essential refs: `docs/release/gate19-supply-chain-posture-00.md` §4 + §9 (R-G19-1, the surfaced advisory + follow-up); `apps/lawbar-desktop/package.json` + `package-lock.json` (the electron devDep + lockfile); `AGENTS.md` §"Test-environment & governance-sequencing notes" (the electron-builder `mac.target` arch rebuild + `postdist` `install-app-deps` host restore + the `better-sqlite3` ABI smoke); `AGENTS.md` §"Repo brief — lawbar-tool" (Node 22+/24.x; ocr-persistence abi-smoke); `.claude/rules/cc-suite.md` §"High-risk WIs" (framework/runtime dependency + native module = HIGH risk → broker review required); `.claude/rules/security-boundary.md` §"Go-live independence"; `.claude/rules/client-local-first.md`; `.claude/rules/autonomy.md` (dependency change is normally a hard-stop — this WI is the explicit user authorization for the electron bump) + `staging-hygiene.md` + `loc-guardian.md`.
- Review questions: (1) Does the WI correctly scope the MINIMAL Electron bump (electron devDep + lockfile only) to resolve R-G19-1, NOT an execution in this authoring lane and NOT an unrelated dependency sweep? (2) Does it define a target-version selection METHOD (npm-audit-verified minimal clean version, lowest test/build-green) rather than hard-coding `43.1.0`, and honestly flag that the audit-resolved fix is a semver-MAJOR jump (34→43) with breaking-change risk? (3) Does it gate the bump on the full desktop test/build suite + the `better-sqlite3` native rebuild + abi-smoke, with a concrete rollback plan? (4) Does it keep gate 19 OPEN (R-G19-1 remediated/updated, not cleared) and gates 4/6/17/20 uncleared, with signing/distribution + GO/NO-GO user-owned? (5) Does it forbid unrelated dependency upgrades, schema/contract changes, OCR/Forms work, and any license/legal decision? (6) Is any gate clearance, unrelated upgrade, or go-live decision smuggled in?

Type: IMPL
Scope: Govern (do NOT execute) a FUTURE lane that performs the MINIMAL Electron runtime-advisory remediation. The FUTURE execution lane: re-runs `npm audit` to select the minimal advisory-clean Electron version (the audit's `fixAvailable` is currently `electron@43.1.0`, semver-major — NOT hard-coded; the exec lane verifies + prefers the lowest test/build-green clean version); bumps ONLY the `electron` devDependency in `apps/lawbar-desktop/package.json` + regenerates `apps/lawbar-desktop/package-lock.json` for the electron subtree; re-runs `npm audit --omit=dev` + dev-inclusive audit to confirm the R-G19-1 HIGH advisories are CLEARED; rebuilds the `better-sqlite3` native binding for the new Electron ABI (`electron-builder install-app-deps` / `postdist` restore) + re-validates via abi-smoke; runs the desktop test/build gate (`npm --prefix apps/lawbar-desktop test` incl. the Electron smoke/main/IPC tests, and `run dist` — infeasibility allowed ONLY with a recorded concrete environment reason); records a NEW `docs/release/electron-runtime-advisory-bump-00.md` + updates ONLY the gate-19 evidence row (R-G19-1 → remediated; **gate 19 stays OPEN**). It has a concrete ROLLBACK plan (restore the two package files + rebuild for Electron 34 + re-run the gate) and STOPS+rolls-back rather than ship a broken build or silently accept the advisory. It preserves signing/notarization/public distribution as gate-4 user-owned, gate-17 license as user-owned, gate-6 as not-run, and the final GO/NO-GO as gate-21 user-owned. THIS authoring lane implements + runs + decides NOTHING — it only governs the remediation WI. If review asks to upgrade unrelated dependencies, change schema/contract, do OCR/Forms work, make a license/distribution decision, clear a dependent gate, or run gate 6, REJECT and report.
Source of truth: docs/release/gate19-supply-chain-posture-00.md §4+§9 (R-G19-1); apps/lawbar-desktop/package.json + package-lock.json (the electron devDep + lockfile); AGENTS.md §"Test-environment & governance-sequencing notes" (electron-builder mac.target arch rebuild + postdist install-app-deps + better-sqlite3 abi-smoke) + §"Repo brief — lawbar-tool"; .claude/rules/cc-suite.md §"High-risk WIs"; .claude/rules/security-boundary.md §"Go-live independence"; .claude/rules/client-local-first.md; .claude/rules/autonomy.md + staging-hygiene.md + loc-guardian.md
Allowed files: apps/lawbar-desktop/package.json (the `electron` devDependency version ONLY) + apps/lawbar-desktop/package-lock.json (regenerated for the electron subtree ONLY) + docs/release/electron-runtime-advisory-bump-00.md (NEW remediation evidence doc) + docs/release/go-live-readiness-report.md (the gate-19 evidence ROW ONLY — R-G19-1 remediation status; gate 19 stays OPEN, roll-up bucket unchanged) + dev-memo/run/reviews/ (the exec lane's review artifact) + the `better-sqlite3` native binding REBUILD ARTIFACT (host node_modules binding, NOT tracked/committed — a build step, not a source change). NOTE: THIS authoring lane commits ONLY the queue governance (queue.md/linted/reviewed/governed) + reviews/queue-review-166.md — NO package/lock change, NO remediation doc, NO report change. The FUTURE exec lane records its evidence in the COMMIT MESSAGE + the remediation doc + review artifact, and does NOT re-stage this lane's queue governance.
Forbidden files: any package.json OTHER than apps/lawbar-desktop/package.json + any package-lock.json OTHER than apps/lawbar-desktop/package-lock.json (NO other package's deps touched), and even in apps/lawbar-desktop ONLY the `electron` devDep may change (NO better-sqlite3/docx/playwright/typescript/electron-builder version bump — an `electron-builder` upgrade strictly required by the Electron bump is a HARD STOP → roll back + separate authorized WI, NEVER folded into THIS WI), services/ + native/ + docs/contracts/ source (NO product source/test LOGIC change — test/build are RUN not rewritten; a product source/test compatibility change required by the Electron major bump is a HARD STOP → roll back + separate authorized WI naming the concrete files, NEVER a "minimal shim" inside THIS WI), services/*/src/sqlite/schema.ts (CURRENT_SCHEMA_VERSION stays 12), migrations/, docs/adr/ (READ-ONLY), docs/product/project-requirements-brief.md (READ-ONLY), docs/release/ OTHER than the NEW electron-runtime-advisory-bump-00.md + the gate-19 row of go-live-readiness-report.md, dev-memo/run/intake/ (raw samples — never staged), dev-memo/ OTHER than run/reviews/, .claude/ + .github/; NO unrelated dependency upgrade; NO schema/migration; NO contract/persistence CODE change (native REBUILD artifact only); NO OCR/Forms/custody/A8 work; NO ADR/brief edit; NO Electron BUMP RUN in this authoring lane; NO gate-6 full-project audit; NO broad readiness refresh; NO clearing of gates 4/6/12/13/17/19/20; NO signing/notarization/public-distribution decision (gate 4); NO 律师法 conclusion (gate 11); NO license/legal/business decision (gate 17); NO final GO/NO-GO decision (gate 21). Push is NEVER automatic (autonomy hard-stop): local commit governance is the default; any branch push + PR open happens ONLY under explicit per-invocation user authorization; NO merge; NO branch delete of shared branches
Gates: IMPL/remediation lane (HIGH risk — framework/runtime dependency + native module; per `.claude/rules/cc-suite.md` §"High-risk WIs" the broker review is REQUIRED, self-review forbidden). (1) THIS authoring lane: broker /cc-suite:review-plan on this queue WI, Path 1, retrievable, then govern. (2) The FUTURE exec lane: RUN the bump + re-audit + desktop test/build + native rebuild, produce the remediation doc + gate-19 row update, then broker a POST-ARTIFACT /cc-suite:audit + /cc-suite:verify on the diff (HIGH-risk IMPL → audit+verify, not just review-plan). (3) scripts/workflow/check-contract-integrity.sh exit 0; scripts/workflow/check-queue.sh PASS (both lanes); loc-guardian scan (the bump is dep/doc, low LOC, but run the gate). Forbidden-scope scan before commit: CURRENT_SCHEMA_VERSION EQUALS 12; only the electron devDep + its lockfile subtree changed; no unrelated dependency moved (verified by lockfile diff); no product source/test logic change; no schema/contract change; no ADR/brief edit; no gate-6 run; no readiness refresh; no gate 4/6/12/13/17/19/20 clear; no license/distribution decision; no GO/NO-GO; no raw sample. STOP-points: review asks to upgrade unrelated deps, change schema/contract, do OCR/Forms work, make a license/distribution decision, clear a dependent gate, or run gate 6 → REJECT/STOP and report; the bump breaks the desktop test/build suite and cannot be fixed in this WI's minimal scope → ROLLBACK + record (R-G19-1 blocked, needs a larger Electron-migration WI), never ship broken; the electron bump drags an incompatible electron-builder / any non-Electron dep, OR requires a product source/test compatibility change → HARD STOP: ROLLBACK + open/amend a SEPARATE explicitly-authorized WI, never widen THIS WI; any forbidden file would change → STOP.
Acceptance criteria: check-queue and check-contract-integrity PASS and CURRENT_SCHEMA_VERSION EQUALS 12 and the diff CONTAINS only the queue governance (queue.md/linted/reviewed/governed) + reviews/queue-review-166.md and no package/lock/source/test/schema/contract change appears and no ADR/brief edit / no unrelated dependency change / no readiness refresh appears. The governed WI must DEFINE the ten remediation requirements (current version+advisories; target-selection method not hard-coded; exact package/lock files; install command; re-audit verification; desktop test/build checks; better-sqlite3 native rebuild; rollback plan; evidence artifact; deterministic pass/fail + feeds-gate-19/4-without-clearing), keep gate 19 OPEN (R-G19-1 remediated not cleared) and gates 4/6/12/13/17/20 uncleared, forbid unrelated dependency upgrades + a license/distribution decision, require the desktop test/build gate + native rebuild + rollback plan, and MUST NOT decide a STOP-AND-ASK hard-stop or assert go-live. review-plan (this lane) must be RUN and must PASS (READY, or READY with only Low-risk clarifications applied).
Risk flags: HIGH (framework/runtime dependency + native module — an Electron MAJOR bump 34→43 with real breaking-change + native-ABI risk). Care points: (a) NO BUMP RUN in this authoring lane — this lane only governs the WI; (b) MAJOR-JUMP HONESTY — the audit-resolved fix is semver-major (34→43); the WI flags the breaking-change risk + gates on the full desktop test/build suite, and STOPS+rolls-back rather than ship a broken build; (c) MINIMAL SCOPE — ONLY the electron devDep + its lockfile subtree; NO unrelated dependency upgrade; a dragged-in incompatible `electron-builder` OR a required product source/test compatibility change is a HARD STOP → roll back + open/amend a SEPARATE explicitly-authorized WI, NEVER widened into THIS WI; (d) NATIVE ABI — the `better-sqlite3` binding MUST be rebuilt for the new Electron + abi-smoke re-validated (the ~30s launch-timeout failure mode per `AGENTS.md` is the signal of a stale binding); (e) GATE INDEPENDENCE — R-G19-1 is *remediated*, gate 19 stays OPEN; signing/notarization/distribution (gate 4) + license (gate 17) + GO/NO-GO (gate 21) stay user-owned; gate 6 NOT run; (f) HIGH-RISK BROKER — the exec lane runs cc-suite audit+verify (not self-review). If any cannot hold, STOP and report.
Depends on: none
Commit boundary: TWO separate lanes. (1) THIS authoring lane commits ONLY the queue governance (queue.md/linted/reviewed/governed) + reviews/queue-review-166.md — no package/lock change, no remediation doc, no report change, no implementation, no decision. (2) The FUTURE execution lane commits ONLY the bumped apps/lawbar-desktop/package.json (electron devDep) + apps/lawbar-desktop/package-lock.json (electron subtree) + the NEW docs/release/electron-runtime-advisory-bump-00.md + the gate-19 evidence-row update to go-live-readiness-report.md + its own review artifact under dev-memo/run/reviews/, records its evidence (chosen version + re-audit + test/build + native-rebuild + rollback plan) in the COMMIT MESSAGE, and does NOT re-stage this lane's queue governance. The `better-sqlite3` native rebuild is a host build artifact (node_modules binding), NOT a committed file. NO unrelated dependency change; NO other package's lockfile; NO schema/contract change; NO OCR/Forms work; NO license/distribution decision; NO staging of dev-memo/run/intake/ or untracked residue; push is NEVER automatic (autonomy hard-stop) — any branch push + PR open happens ONLY under explicit per-invocation user authorization; no merge; no branch delete of shared branches. THIS authoring lane implements NOTHING, runs NO bump, and decides NOTHING — it only governs the Electron runtime-advisory remediation WI.

## Acceptance criteria (FUTURE execution lane — what the governed remediation must satisfy)

These describe the deliverable the FUTURE exec lane must produce; they are NOT preconditions of THIS authoring lane. The authoring-lane acceptance is the structured `Acceptance criteria:` field above (queue governance + review READY only — the bump is NOT run in this lane).

1. `electron` in `apps/lawbar-desktop/package.json` is bumped to a `npm audit`-verified version that CLEARS the R-G19-1 HIGH advisories, chosen as the lowest advisory-clean + test/build-green version (the audit's `fixAvailable electron@43.1.0` is the resolved candidate; the exec lane records the actual chosen version + why). `apps/lawbar-desktop/package-lock.json` is regenerated for the electron subtree ONLY.
2. `npm audit --omit=dev` (+ dev-inclusive) re-run after the bump shows the R-G19-1 HIGH runtime advisories CLEARED (any residual advisory surfaced honestly).
3. The desktop test/build gate is green: `npm --prefix apps/lawbar-desktop test` (incl. the Electron smoke/main/IPC tests) passes with counts recorded; `run dist` packaging attempted if feasible, result recorded.
4. The `better-sqlite3` native binding is rebuilt for the new Electron ABI (`electron-builder install-app-deps` / `postdist` restore) and re-validated by abi-smoke; the dev-Electron tests launch without the ~30s ABI timeout.
5. A NEW `docs/release/electron-runtime-advisory-bump-00.md` records before/after version, target-selection audit evidence, install command + output, before/after `npm audit`, test/build results, the native-rebuild + abi-smoke result, and the rollback plan.
6. The exec lane CITES the exact gate-19 row before editing and updates ONLY its evidence text — R-G19-1 → remediated; **gate 19 stays `OPEN`** (roll-up bucket unchanged). MUST NOT clear gates 4/6/12/13/17/19/20; signing/notarization/distribution (gate 4) + license (gate 17) + GO/NO-GO (gate 21) stay user-owned.
7. The lockfile diff is confined to the electron subtree — every changed lockfile package is enumerated + justified as Electron-owned/transitive; if any non-Electron package (electron-builder, playwright, better-sqlite3, …) moved, that is a HARD STOP → rollback + separate authorized WI. `CURRENT_SCHEMA_VERSION` stays 12; no product source/test logic change (a required compatibility change is a HARD STOP → separate authorized WI, NOT folded in here); no ADR/brief edit. `scripts/workflow/check-queue.sh` + `check-contract-integrity.sh` PASS; the HIGH-risk exec lane runs cc-suite audit + verify. If the bump cannot be made green in minimal scope, the lane ROLLS BACK and records R-G19-1 as blocked (needs a larger Electron-migration WI) — it does NOT ship a broken build.
