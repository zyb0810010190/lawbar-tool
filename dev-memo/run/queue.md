## WI-BS1: project the 5 raw CaseBox IPC channels (matter create/get/list/archive + document:register)
Type: IMPL
Scope: Close the live authority-field leaks on the 5 channels that still return raw persistence objects (matter:create/get/list/archive + document:register), mirroring the shipped FACTS-AUD-3/GET-AUD-1/AUDIT-AUD-1 projection. SECURITY-ADJACENT. In `apps/lawbar-desktop/src/caseBox/dto/matter.ts`: import `CaseBoxMatter` from `case-box-contract`; add ONE shared `MATTER_RESPONSE_FIELDS` = `id, name, matter_type, jurisdiction, parties, confidentiality_class, status, created_at, archived_at, retainer_scope, case_type_text, case_progress_text, court_contact_text, contention_summary_text` (the renderer-consumed display fields, verified in listMatters/viewMatter/createMatter/archiveMatter; `id` is REQUIRED — routing/`data-matter-id`/nested API calls — a stable renderer identifier, not authority); add `RendererMatter = Pick<CaseBoxMatter, (typeof MATTER_RESPONSE_FIELDS)[number]>` + a renderer-safe `RendererMattersPage { readonly rows: ReadonlyArray<RendererMatter>; readonly next_cursor: string | null }`; retype `CreateMatterResult = IpcEnvelope<RendererMatter>`, `GetMatterResult = IpcEnvelope<RendererMatter | null>`, `ListMattersResult = IpcEnvelope<RendererMattersPage>`, `ArchiveMatterResult = IpcEnvelope<RendererMatter>` (drop now-unused persistence page import if any). In `apps/lawbar-desktop/src/caseBox/dto/document.ts`: add `REGISTER_DOCUMENT_RESPONSE_FIELDS` mirroring `GET_DOCUMENT_RESPONSE_FIELDS` (renderer only null-checks the register result; excludes `tenant_id`/`actor_user_id`/`custody_chain`) + `RendererRegisteredDocument = Pick<CaseBoxDocument, (typeof REGISTER_DOCUMENT_RESPONSE_FIELDS)[number]>`; retype `RegisterDocumentResult = IpcEnvelope<RendererRegisteredDocument>`. In `matterHandlers.ts`: apply `projectRow<RendererMatter>` in createMatterHandler + archiveMatterHandler; null-safe `projectRow` in getMatterHandler (PRESERVE `if (value === null) return { ok: true, value: null }`); `projectPage<RendererMatter>` in listMattersHandler (project every row, preserve next_cursor). In `documentHandlers.ts`: `projectRow<RendererRegisteredDocument>` in registerDocumentHandler. EXCLUDE from every new allowlist: `tenant_id`, `actor_user_id`, the open `[k: string]: unknown` (projectRow copies only allowlisted keys), and for register also `custody_chain`. Do NOT change request DTOs, persistence calls, contract types, renderer UI, electron wiring, handlerShared.ts, or handlers.ts. If a renderer-consumed matter/document field is found OUTSIDE the allowlist that cannot be served without exposing an authority field, STOP and surface. Tests: extend `tests/ipc-list-projection.unit.test.mjs` with matter create/get/list/archive + register blocks (raw row includes authority + open-index extra; response omits tenant_id/actor_user_id/open-index [+custody_chain for register]; consumed fields retained; matter:list ≥2 rows + next_cursor preserved; getMatter null path preserved); extend `tests/renderer-dto-sync.test.mjs` RESPONSE_ALLOWLISTS with `MATTER_RESPONSE_FIELDS` + `REGISTER_DOCUMENT_RESPONSE_FIELDS` authority-exclusion entries. Record + close MATTER-AUD-1 (create/get/list/archive) + REGDOC-AUD-1 in `dev-memo/deferred-audit-findings.md`.
Source of truth: apps/lawbar-desktop/src/caseBox/dto/deadline.ts (LIST/TRANSITION response + RendererRow + page precedent); apps/lawbar-desktop/src/caseBox/dto/document.ts (LIST_DOCUMENTS_RESPONSE_FIELDS + GET_DOCUMENT_RESPONSE_FIELDS to mirror for register); apps/lawbar-desktop/src/caseBox/deadlineHandlers.ts + documentHandlers.ts getDocumentHandler (projectRow/projectPage + null-safe precedent); apps/lawbar-desktop/src/caseBox/handlerShared.ts (projectRow/projectPage, READ-ONLY); docs/contracts/case-box-contract/src/generated/case-box-matter.ts (CaseBoxMatter shape — READ-ONLY); apps/lawbar-desktop/renderer/screens/{listMatters,viewMatter,createMatter,archiveMatter}.ts (the consumed matter fields, READ-ONLY); apps/lawbar-desktop/tests/ipc-list-projection.unit.test.mjs (projection-test pattern); dev-memo/plan-batch-casebox-ipc-boundary-sweep-00.md (proposal)
Allowed files: apps/lawbar-desktop/src/caseBox/dto/matter.ts, apps/lawbar-desktop/src/caseBox/dto/document.ts, apps/lawbar-desktop/src/caseBox/matterHandlers.ts, apps/lawbar-desktop/src/caseBox/documentHandlers.ts, apps/lawbar-desktop/tests/ipc-list-projection.unit.test.mjs, apps/lawbar-desktop/tests/renderer-dto-sync.test.mjs, apps/lawbar-desktop/tests/ipc-handlers.unit.test.mjs, dev-memo/deferred-audit-findings.md
Forbidden files: apps/lawbar-desktop/renderer/**, apps/lawbar-desktop/electron/**, services/**, docs/contracts/**, .claude/**, scripts/workflow/**, dev-memo/run/**, apps/lawbar-desktop/src/caseBox/handlerShared.ts, apps/lawbar-desktop/src/caseBox/handlers.ts, apps/lawbar-desktop/src/caseBox/auditHandlers.ts, apps/lawbar-desktop/src/caseBox/deadlineHandlers.ts, apps/lawbar-desktop/src/caseBox/docketHandlers.ts, apps/lawbar-desktop/src/caseBox/factHandlers.ts, apps/lawbar-desktop/tests/ipc-casebox-handlers.unit.test.mjs, apps/lawbar-desktop/tests/ipc-fact-handlers.unit.test.mjs
AMENDMENT-1 (2026-06-07, user-approved): added `apps/lawbar-desktop/tests/ipc-handlers.unit.test.mjs` to BS1 Allowed-files. Reason: WI-BS1's getMatter response projection (intended behavior) broke the pre-existing assertion `ipc-handlers.unit.test.mjs:209` `assert.equal(result.value.tenant_id, "default-tenant")`, which was asserting the PRE-FIX leak. Updating that one assertion (tenant_id present -> absent; id still present) belongs in BS1 (BS1 must keep the suite green and prove the projection it introduces). Scope strictly limited to the affected getMatter assertion; no other assertion/fixture in that file may be touched; no test weakened.
Gates: npm --prefix apps/lawbar-desktop test
Acceptance criteria: createMatter/getMatter/listMatters/archiveMatter + registerDocument responses are projected through their allowlists; the IPC response OMITS tenant_id, actor_user_id, and open-index extras on every row (+ custody_chain for register) while INCLUDING every renderer-consumed field; matter:list projects every row and preserves next_cursor; getMatter null path unchanged (value:null); result types retyped to renderer-safe types; request DTOs/persistence/contract/renderer/electron unchanged; `npm --prefix apps/lawbar-desktop test` passes (new projection blocks + dto-sync MATTER + REGISTER_DOCUMENT authority-exclusion; existing matter/document UI tests stay green); MATTER-AUD-1 + REGDOC-AUD-1 recorded + closed in dev-memo/deferred-audit-findings.md with the resolution commit
Risk flags: SECURITY BOUNDARY (IPC authority-field leak on the oldest channels) → cc-suite review-plan + audit + verify REQUIRED, self-review NOT acceptable; mirrors the shipped FACTS-AUD-3 fix; persistence/contract READ-ONLY; MATTER_RESPONSE_FIELDS must be exactly the renderer-consumed display set (surface before broadening; never include tenant_id/actor_user_id); getMatter null path must be preserved; projectRow allowlist drops the open-index extras; no renderer path (no design gate); wire-safety only (renderer reads only display fields); long cc-suite calls via runner native --background + state-file polling
Depends on: none
Commit boundary: one local commit for the 5-channel projection + tests + MATTER-AUD-1/REGDOC-AUD-1 closures

## WI-BS2: harden IPC-boundary test depth (all response allowlists + write-handler projection + table-driven forbidden fields)
Type: TEST
Scope: TEST-ONLY boundary-depth hardening; no source change. (1) Extend `apps/lawbar-desktop/tests/renderer-dto-sync.test.mjs` RESPONSE_ALLOWLISTS so EVERY canonical `*_RESPONSE_FIELDS` participates in the authority-exclusion check — add the 4 currently-missing: `CREATE_FACT_RESPONSE_FIELDS` (exclude tenant_id/actor_user_id/reviewer_actor_user_id), `TRANSITION_FACT_RESPONSE_FIELDS` (same), `DOCKET_ENTRY_RESPONSE_FIELDS` (exclude tenant_id/actor_user_id), `CONFIRM_DOCKET_DEADLINE_RESPONSE_FIELDS` (exclude tenant_id/actor_user_id). (The BS1 MATTER + REGISTER_DOCUMENT entries are added by BS1.) (2) Add projection regression tests in `tests/ipc-list-projection.unit.test.mjs` for the already-projected write handlers lacking them — createFact, transitionFact, createDocket, dismissDocket (raw provider row includes authority + open-index extra; handler response omits them; consumed fields retained). (3) Table-drive the shallow/zero forbidden-field tests over their full *_FORBIDDEN_FIELDS arrays: confirmDocketEntry (currently 0 coverage) + dismissDocketEntry (0) + createDocketEntry (1/18) in `tests/ipc-casebox-handlers.unit.test.mjs`; createFactHandler (1/15) in `tests/ipc-casebox-handlers.unit.test.mjs`; transitionFactHandler (2/13) in `tests/ipc-fact-handlers.unit.test.mjs` — each asserting ok:false + invalid_payload + details.schemaPath == the field. Do NOT weaken any existing test. (4) Close `CBW-602-FORBIDDEN-TESTS` in `dev-memo/deferred-audit-findings.md` ONLY if the createFact table-drive fully satisfies it; otherwise update the row with partial progress and leave open. If table-driving pushes `ipc-casebox-handlers.unit.test.mjs` (985 pure / cap 1200) toward the cap, STOP and report a narrower follow-up split rather than bloating the file.
Source of truth: apps/lawbar-desktop/tests/ipc-list-projection.unit.test.mjs (projection-test pattern); apps/lawbar-desktop/tests/ipc-casebox-handlers.unit.test.mjs (the WI-CL1 transitionDeadline table-driven forbidden-field test as the precedent to mirror); apps/lawbar-desktop/src/caseBox/dto/{fact,docket}.ts (the *_RESPONSE_FIELDS + *_FORBIDDEN_FIELDS arrays — READ-ONLY); dev-memo/deferred-audit-findings.md (CBW-602-FORBIDDEN-TESTS row)
Allowed files: apps/lawbar-desktop/tests/ipc-list-projection.unit.test.mjs, apps/lawbar-desktop/tests/renderer-dto-sync.test.mjs, apps/lawbar-desktop/tests/ipc-casebox-handlers.unit.test.mjs, apps/lawbar-desktop/tests/ipc-fact-handlers.unit.test.mjs, dev-memo/deferred-audit-findings.md
Forbidden files: apps/lawbar-desktop/src/**, apps/lawbar-desktop/renderer/**, apps/lawbar-desktop/electron/**, services/**, docs/contracts/**, .claude/**, scripts/workflow/**, dev-memo/run/**
Gates: npm --prefix apps/lawbar-desktop test
Acceptance criteria: every canonical `*_RESPONSE_FIELDS` allowlist is checked for authority-field exclusion in renderer-dto-sync.test.mjs; createFact/transitionFact/createDocket/dismissDocket have explicit raw-vs-projected regression tests; confirmDocket/dismissDocket/createDocket/createFact/transitionFact forbidden-field tests are table-driven over their full *_FORBIDDEN_FIELDS arrays (each field → invalid_payload + schemaPath); no existing test weakened; `npm --prefix apps/lawbar-desktop test` passes; ipc-casebox-handlers.unit.test.mjs stays under the 1200 LOC fail cap (STOP + report if approaching); CBW-602-FORBIDDEN-TESTS closed-if-satisfied or updated-with-partial-progress; NO src/** or product change
Risk flags: TEST-ONLY; no runtime/behavior change; depends on BS1 (the dto-sync all-allowlist coverage includes the BS1-added matter/register allowlists); loc-guardian watch on ipc-casebox-handlers.unit.test.mjs (985 pure / cap 1200) — table-driven loops are compact (replace shallow single tests; confirm/dismiss add ~1 loop each) so net growth is small, but STOP + report a split if it nears the cap; low-risk (cc-suite per workflow, self-review fallback acceptable for a test-only WI)
Depends on: WI-BS1
Commit boundary: one local commit for the test-depth hardening + the CBW-602-FORBIDDEN-TESTS disposition
