## WI-RELEASE-G14-BACKUP-RECOVERY-00: define + govern the M0 gate-14 backup + recovery procedure (the operator-facing data-survival runbook) (Type: EVIDENCE, release-governance MEDIUM risk)

## Review packet (compact)
- Active plan summary: gate 14 of the M0 go-live readiness matrix (`docs/release/go-live-readiness-report.md` §1 row 14) requires a documented **backup + recovery procedure** for the lawyer's local data (currently `OPEN`, evidence pointer "brief §14", note "Document SQLite-file backup + audit-chain recovery"). This WI **governs** (does NOT execute) a FUTURE lane that authors that procedure as a release runbook and records how it was verified. The robustness bar is v1 **local-first data survival + recovery** on a single Mac (`.claude/rules/client-local-first.md`), NOT distributed/replicated backup. THIS authoring lane produces the governed queue WI only; it runs NO backup, changes NO source/test/config, clears NO gate, edits NO brief, and decides NO user go-live hard-stop.
- Grounding note (load-bearing — the manifest must match REALITY, not a static doc): the FUTURE doc MUST describe the **actual shipped v1 data-directory layout** as read from source, per `AGENTS.md` §"Source hierarchy" ("Generated local output beats static documentation when they conflict — trust the file a tool actually produces"). The real layout (verified 2026-07-07 from `apps/lawbar-desktop/src/caseBox/caseBoxRuntime.ts:14,42` + `apps/lawbar-desktop/electron/main.ts:25-26,116`): productName `lawbar` → `app.getPath("userData")` = `~/Library/Application Support/lawbar/`, containing (a) **one** `case-box.sqlite` (WAL mode → transient `case-box.sqlite-wal` + `case-box.sqlite-shm` sidecars; the audit tables `case_box_audit_events` / `case_box_audit_chain_heads` live INSIDE this single DB, confirmed by the gate-7 crash-recovery drill finding R-DRILL-1), (b) a `case-box-documents/` directory (the document/blob store, `main.ts:116`), and (c) `theme-preference.json` (non-critical UI state). This **diverges from brief §14's two-file model** (`case-box.sqlite` + `audit-log.sqlite` + `blobs/`). The FUTURE doc MUST document reality AND log the divergence as a `RECONCILIATION-NEEDED` item proposing a bounded brief-reconciliation follow-up WI — it MUST NOT silently adopt the brief's manifest and it MUST NOT edit the brief.
- Exact target files (FUTURE exec lane): a NEW `docs/release/gate14-backup-recovery-00.md` (the backup + recovery procedure + verification record) + the gate-14 evidence ROW of `docs/release/go-live-readiness-report.md` (update to reference the procedure doc; status may move `OPEN` → `PARTIAL / procedure documented` but MUST NOT become `CLEARED` — gate-14 clearance is not this lane's to grant and a documented procedure is not a go-live sign-off) + `dev-memo/run/reviews/` (the exec lane's review artifact). NO other file.
- Exact acceptance criteria: below.
- Exact out-of-scope: NO actual backup/restore RUN in this authoring lane; NO NEW backup/restore/export SOFTWARE, script, or product harness; NO data-export FORMAT certification (that is **gate 18** — a separate lane); NO rollback dry-run beyond CITING the already-executed gate-7 crash-recovery drill (that is **gate 15** — a separate lane); NO audit-chain operational-recovery deep-dive beyond the `event_count==COUNT==MAX(sequence)` verification step (that is **gate 12** — a separate lane); NO gate-6 full-project audit; NO EDIT of `docs/product/project-requirements-brief.md` (only flag the §14 divergence as `RECONCILIATION-NEEDED`); NO signing/notarization/公开 distribution decision; NO 律师法 conclusion; NO final GO/NO-GO; NO gate-14 `CLEARED`; NO source/test/package/config/schema/contract change in EITHER lane (docs-only).
- Requirements the FUTURE procedure doc must define (what the exec lane's doc must specify + how it was verified) — at minimum:
  1. **Real data-directory manifest**: the exact backup set as read from source (per the Grounding note) — `~/Library/Application Support/lawbar/case-box.sqlite` (+ its `-wal`/`-shm` sidecars) + `case-box-documents/` (the content/document store) + `theme-preference.json` (non-critical) — with a one-line criticality tag per item (critical vs regenerable/non-critical). The exec lane MUST **re-verify the resolved macOS `userData` path from the actual app/source behavior at exec time** (the productName `lawbar` → `app.getPath("userData")` resolution in `apps/lawbar-desktop/electron/main.ts` and the `CASE_BOX_DB_FILENAME` + `case-box-documents` joins), NOT merely restate the path from this WI or the brief. Record the tool version + date the layout was read (per `AGENTS.md`). Flag the brief §14 two-file divergence as `RECONCILIATION-NEEDED`.
  2. **Backup procedure**: the backup-as-directory model — the safe path is **app-closed** (quiesced) directory copy (`cp -R` / a Time-Machine-equivalent snapshot) so `case-box.sqlite` is self-contained (WAL already checkpointed at clean shutdown); if a live/hot copy is ever documented, it MUST include the `-wal` + `-shm` sidecars or perform a `wal_checkpoint(TRUNCATE)` first, else the copy risks losing the last-committed transactions. Enumerate the exact commands / manual Finder steps. NO real lawyer data in any transcript (fixture/placeholder paths only).
  3. **Restore procedure**: the "drop the directory back" model per brief §14 — replace the data directory, then the app's startup schema-integrity check + audit-chain verification runs on next launch. Enumerate the exact steps and what a clean restore looks like.
  4. **Verification / recovery checks**: post-restore `PRAGMA integrity_check` on `case-box.sqlite` (expected `ok`); the audit `event_count == COUNT(*) == MAX(sequence)` invariant holds (overlaps gate 12 — cited, not deep-dived); the `case-box-documents/` blob set referenced by DB rows is present. State the exact query/assertion and the expected result.
  5. **Verification evidence — cite the executed drill**: the gate-7 crash-recovery drill (`docs/release/gate7-crash-recovery-drill-00.md`, PASS) already EXERCISED backup-as-directory + restore + `integrity_check=ok` + the audit invariant across a crash+restore round-trip on a disposable fixture. The procedure doc MUST cite that drill as the verification that this procedure works, rather than re-running a live backup in the exec lane. Note the drill's disclosed residuals (R-DRILL-1 single-file layout; R-DRILL-2 process-kill vs storage-power-loss boundary) as they bear on backup completeness.
  6. **Scope boundaries + honest status**: the doc states explicitly what it does NOT cover — export-format certification (gate 18), rollback dry-run (gate 15), audit-chain operational recovery (gate 12) — and keeps the gate-14 row honest (procedure documented + drill-verified, but NOT a go-live sign-off; the three STOP-AND-ASK hard-stops and the final GO/NO-GO remain the user's).
- Dependencies mapped: **gate 12** (audit-chain `event_count==COUNT==MAX(sequence)` recovery — requirement 4 cites it, does not close it); **gate 15** (rollback dry-run — the crash-recovery drill is a related exercised recovery, cited not closed); **gate 18** (export/backup FORMAT certification — the backup manifest here is the input, format cert is separate); **gate 7** (the crash-recovery drill that verifies this procedure — informs, does not clear); **gate 6** (a procedure gap would be a finding for that sweep). These are noted as related, NOT cleared by this WI. Reconciliation dependency: the brief §14 two-file-manifest divergence (surface as `RECONCILIATION-NEEDED`, propose a bounded follow-up; do not edit the brief here).
- Essential refs: `docs/product/project-requirements-brief.md` §14 (backup manifest + drop-directory restore + startup schema-integrity + audit-chain verification — the divergence source); `docs/release/gate7-crash-recovery-drill-00.md` (the executed verification evidence + R-DRILL-1/2); `docs/release/go-live-readiness-report.md` §1 gate 14 (+ related gates 12/15/18/6/7); `apps/lawbar-desktop/src/caseBox/caseBoxRuntime.ts` + `apps/lawbar-desktop/electron/main.ts` (the real data-directory layout — the ground truth for the manifest); `.claude/rules/client-local-first.md` (v1 local-first/offline posture); `AGENTS.md` §"Source hierarchy" (generated output beats static doc) + §"Critical invariants" (audit-chain invariant); `.claude/rules/security-boundary.md` §"Go-live independence".
- Review questions: (1) Does the WI correctly scope a backup + recovery PROCEDURE doc (operator runbook + verification record), NOT new backup software or a data-export format certification? (2) Does it require the manifest to be grounded in the REAL shipped layout (single `case-box.sqlite` + `case-box-documents/` + `theme-preference.json`), and to flag the brief §14 two-file divergence as `RECONCILIATION-NEEDED` without editing the brief? (3) Does it correctly cite the executed gate-7 crash-recovery drill as the verification evidence rather than re-running a live backup? (4) Are gates 18 (export format), 15 (rollback dry-run), and 12 (audit-chain operational recovery) properly DEFERRED to their own lanes, and gates 6/7 mapped without clearing? (5) Does it keep gate 14 NON-CLEARED (procedure documented ≠ go-live sign-off), decide no STOP-AND-ASK hard-stop (4/11/21), and make no GO/NO-GO? (6) Is any product source/test/package/schema/contract change, new dependency, brief edit, or new backup software smuggled in?

Type: EVIDENCE
Scope: Govern (do NOT execute) a FUTURE lane that authors the M0 gate-14 backup + recovery procedure. The FUTURE execution lane produces `docs/release/gate14-backup-recovery-00.md` — defining requirements 1–6 above (real data-directory manifest grounded in source, not brief §14; backup-as-directory procedure with WAL/sidecar handling; drop-directory restore; PRAGMA integrity_check + audit event_count==COUNT==MAX(sequence) verification; citation of the executed gate-7 crash-recovery drill as verification evidence; explicit scope boundaries + honest status) — then updates ONLY the gate-14 evidence row of `docs/release/go-live-readiness-report.md` to reference the procedure doc while keeping gate 14 NON-CLEARED (a documented procedure is evidence, not a go-live sign-off; status may move OPEN → PARTIAL / procedure documented). It grounds the backup manifest in the ACTUAL shipped layout (per AGENTS.md source hierarchy — generated output beats static doc), flags the brief §14 two-file divergence as RECONCILIATION-NEEDED proposing a bounded follow-up (WITHOUT editing the brief), cites the executed gate-7 drill as verification, and DEFERS export-format cert (gate 18) + rollback dry-run (gate 15) + audit-chain operational recovery (gate 12) to their own lanes. It PRESERVES every still-non-cleared gate + the STOP-AND-ASK gates (4/11/21); it makes NO final GO/NO-GO, resolves NO STOP-AND-ASK, runs NO gate-6 audit, writes NO new backup/restore/export software, adds NO new runtime dependency, edits NO brief/ADR, and changes NO product source/test/schema/contract. THIS authoring lane implements + runs + decides NOTHING — it only governs the gate-14 procedure WI. If review asks to run a live backup, write backup software, certify an export format, run gate 6, unilaterally clear gate 14, edit the brief, or add a dependency, REJECT and report.
Source of truth: docs/product/project-requirements-brief.md §14 (backup manifest + restoration = drop-directory-back + startup schema-integrity + audit-chain verification — the divergence source, READ-ONLY here); docs/release/gate7-crash-recovery-drill-00.md (the executed verification evidence, PASS, + residuals R-DRILL-1/2); docs/release/go-live-readiness-report.md §1 gate 14 (OPEN) + gates 12/15/18/6/7 (dependencies); apps/lawbar-desktop/src/caseBox/caseBoxRuntime.ts + apps/lawbar-desktop/electron/main.ts (the REAL data-directory layout — ground truth for the manifest); .claude/rules/client-local-first.md (v1 local-first/offline posture); AGENTS.md §"Source hierarchy" (generated output beats static doc) + §"Critical invariants" (audit-chain event_count==COUNT==MAX(sequence)); .claude/rules/security-boundary.md §"Go-live independence"; .claude/rules/autonomy.md + staging-hygiene.md + loc-guardian.md
Allowed files: docs/release/gate14-backup-recovery-00.md (the NEW procedure doc the FUTURE lane produces) + docs/release/go-live-readiness-report.md (the gate-14 evidence ROW ONLY) + dev-memo/run/reviews/ (the exec lane's own review artifact). NOTE: THIS authoring lane commits ONLY the queue governance (queue.md/linted/reviewed/governed) + reviews/queue-review-146.md — NO procedure doc, NO backup run, NO report change. The future exec lane records its evidence in the COMMIT MESSAGE + review artifact and does NOT re-stage this lane's queue governance.
Forbidden files: docs/release/ OTHER than the NEW gate14-backup-recovery-00.md + the gate-14 row of go-live-readiness-report.md (the other audit/assessment/policy/drill reports are READ-ONLY evidence — NOT edited; the gate-7 drill doc is READ-ONLY here), docs/adr/ + docs/product/ (READ-ONLY — NO brief/ADR edit; the brief §14 divergence is only FLAGGED as RECONCILIATION-NEEDED, never edited), docs/contracts/, apps/ + services/ (NO product source/test/renderer/IPC/persistence/contract change — NO new backup/restore/export software or harness), services/*/src/sqlite/schema.ts (CURRENT_SCHEMA_VERSION stays 12), migrations/, native/, package.json/package-lock.json of any package (NO new dependency), dev-memo/run/intake/ (raw samples — never staged), dev-memo/ OTHER than run/reviews/ (no other plan/design edit), .claude/ + .github/; NO backup/restore RUN in this authoring lane; NO gate-6 full-project audit; NO data-export FORMAT certification (gate 18); NO rollback dry-run implementation (gate 15); NO audit-chain operational-recovery deep-dive (gate 12); NO signing/notarization/public-distribution decision; NO 律师法 conclusion; NO final GO/NO-GO decision; NO unilateral gate-14 CLEARED. Push is NEVER automatic (autonomy hard-stop): local commit governance is the default; any branch push + PR open happens ONLY under explicit per-invocation user authorization; NO merge; NO branch delete
Gates: EVIDENCE/governance lane (MEDIUM release-governance risk — governs a backup/recovery procedure WI; no code, no backup run, no go-live decision). (1) THIS authoring lane: broker /cc-suite:review-plan on this queue WI, Path 1, retrievable, then govern. (2) The FUTURE exec lane: produce the procedure doc + the gate-14 row update, then broker /cc-suite:review-plan on the produced artifacts. (3) scripts/workflow/check-contract-integrity.sh exit 0; scripts/workflow/check-queue.sh PASS (both lanes). Forbidden-scope scan before commit: CURRENT_SCHEMA_VERSION EQUALS 12; no product source/test/package/schema/contract change; no backup run; no gate-6 run; no new dependency; no brief edit; no export-format cert; no GO/NO-GO; no unilateral gate-14 clear; no raw sample. STOP-points: review asks to run a live backup, write backup software, certify an export format, run gate 6, unilaterally clear gate 14, edit the brief, or add a dependency → REJECT/STOP and report; any forbidden file would change → STOP.
Acceptance criteria: check-queue and check-contract-integrity PASS and CURRENT_SCHEMA_VERSION EQUALS 12 and the diff CONTAINS only the queue governance (queue.md/linted/reviewed/governed) + reviews/queue-review-146.md and no docs/source/test/package/schema/contract change appears and no backup run / no new backup software / no new dependency appears. The governed WI must DEFINE requirements 1–6 (real data-directory manifest grounded in source; backup-as-directory procedure; drop-directory restore; PRAGMA integrity_check + audit event_count==COUNT==MAX(sequence) verification; citation of the executed gate-7 crash-recovery drill; scope boundaries + honest status), must GROUND the manifest in the real layout + flag the brief §14 divergence as RECONCILIATION-NEEDED (without editing the brief), must DEFER gates 18/15/12 to their own lanes + MAP gates 6/7, must keep gate 14 NON-CLEARED, and MUST NOT assert go-live or decide a STOP-AND-ASK hard-stop. review-plan (this lane) must RETURN READY.
Risk flags: MEDIUM (release-governance — a procedure WI that could over-claim readiness, smuggle a live backup run, write new backup software, edit the brief, or add a dependency). Care points: (a) NO BACKUP RUN in this authoring lane — this lane only governs the WI; (b) NO UNILATERAL CLEAR — a documented procedure is evidence; gate 14's clear is not this lane's to grant and does not imply go-live; (c) MANIFEST GROUNDED IN REALITY — the backup set is read from source (single case-box.sqlite + case-box-documents/ + theme-preference.json), NOT copied from brief §14's two-file model; the divergence is FLAGGED as RECONCILIATION-NEEDED, never silently adopted and never resolved by editing the brief; (d) NO NEW SOFTWARE / NO DEPENDENCY — the procedure is a runbook, not new backup/restore/export code, and adds no runtime dependency (that would be a hard-stop); (e) SCOPE DEFERRALS — export-format cert (gate 18), rollback dry-run (gate 15), audit-chain operational recovery (gate 12) are DEFERRED to their own lanes, not folded in; gates 6/7 mapped, not cleared; (f) GO-LIVE INDEPENDENCE — a procedure doc does NOT imply go-live; the final verdict + the three STOP-AND-ASK hard-stops remain the user's; (g) FIXTURE-ONLY TRANSCRIPTS — any command example uses fixture/placeholder paths, never real lawyer data. If any cannot hold, STOP and report.
Depends on: none
Commit boundary: TWO separate lanes. (1) THIS authoring lane commits ONLY the queue governance (queue.md/linted/reviewed/governed) + reviews/queue-review-146.md — no procedure doc, no backup run, no report change, no implementation, no decision. (2) The FUTURE execution lane commits ONLY the new docs/release/gate14-backup-recovery-00.md + the gate-14 evidence-row update to go-live-readiness-report.md + its own review artifact under dev-memo/run/reviews/, records its evidence in the COMMIT MESSAGE, and does NOT re-stage this lane's queue governance. NO product source/test/package/schema/contract/backup-software file in either lane; NO new dependency; NO brief edit; NO staging of dev-memo/run/intake/ or untracked residue; push is NEVER automatic (autonomy hard-stop) — local commit governance is the default, any branch push + PR open happens ONLY under explicit per-invocation user authorization; no merge; no branch delete. THIS authoring lane implements NOTHING, runs NO backup, and decides NOTHING — it only governs the gate-14 backup + recovery procedure WI.

## Acceptance criteria (FUTURE execution lane — what the governed procedure doc must satisfy)

These describe the deliverable the FUTURE exec lane must produce; they are NOT preconditions of THIS authoring lane. The authoring-lane acceptance is the structured `Acceptance criteria:` field above (queue governance + review READY only — the procedure doc does NOT exist yet and MUST NOT be created in this lane).

1. A NEW `docs/release/gate14-backup-recovery-00.md` exists defining requirements 1–6 above.
2. The data-directory manifest is grounded in the REAL shipped layout — the exec lane re-verifies the `userData` path + filenames from source (`caseBoxRuntime.ts` + `main.ts`) at exec time rather than restating this WI/the brief — records the tool version + date, and flags the brief §14 two-file divergence as `RECONCILIATION-NEEDED` with a proposed bounded follow-up WI — the brief itself is NOT edited.
3. The procedure cites the executed gate-7 crash-recovery drill (`gate7-crash-recovery-drill-00.md`, PASS) as the verification that backup-as-directory + restore + integrity + audit-invariant hold, and notes the drill's residuals as they bear on backup completeness.
4. The gate-14 ROW of `docs/release/go-live-readiness-report.md` is updated to reference the procedure doc; status may move `OPEN` → `PARTIAL / procedure documented`, and MUST NOT become `CLEARED`. No other row / no roll-up line semantics broken.
5. The doc explicitly defers export-format cert (gate 18), rollback dry-run (gate 15), and audit-chain operational recovery (gate 12) to their own gates, and keeps go-live independence (no sign-off, no user hard-stop decided).
6. NO product source/test/package/config/schema/contract change in either lane; `CURRENT_SCHEMA_VERSION` stays 12 (untouched); docs-only.
7. `scripts/workflow/check-queue.sh` PASS + `scripts/workflow/check-contract-integrity.sh` PASS on both lanes.
