## WI-EVIDENCE-A1T6-AUD-L1-DEFERRED-HARDENING-00: close the A1T6-AUD-L1 deferred Low (Type: IMPL)
Type: IMPL
Scope: Close ONLY the existing deferred Low `A1T6-AUD-L1` (recorded in `dev-memo/deferred-audit-findings.md`; source audit `audit-mr05ozbe-0ynd3b`, study 188). The finding: `EvidenceCoreA1CitationGate.evaluate` (`native/evidence-core-swift/Sources/EvidenceCoreSmoke/A1CitationStabilityHarness.swift`) does NOT validate oracle COMPLETENESS (a partial oracle covering only some derived pages still passes) nor reject a non-clean expected row (`ambiguous`/`non_citable`) carrying a stray `text`. Add the NARROWEST native-Swift hardening: before comparing outcomes, (1) ROW-VALIDITY — each oracle `expected` row must name a known outcome (`clean`/`non_citable`/`ambiguous`) and carry `text` IFF `clean` (a non-clean row with a stray `text`, or a clean row missing `text`, is `fixture_or_oracle_invalid`); (2) COMPLETENESS — the oracle's page-key set must EQUAL the derived page set with no duplicate keys (partial / over-covering / duplicate oracle is `fixture_or_oracle_invalid`, never a false-green subset pass). Add targeted regression tests in `A1CitationStabilityHarnessTests.swift`. Update the A1T6-AUD-L1 row Status open->closed with a resolution reference (per `dev-memo/deferred-audit-findings.md` update rules). This is a STANDALONE A1-T6 native-gate hardening — it MUST NOT reopen A10, fold into A10-T1, start T3/T4/T5 forms, touch A8, mutate schema, touch custody/marker, confidential fixtures, the JS shim, or unrelated export-pipeline work; it MUST NOT broaden into general A1-T6 refactoring (only the two named gaps). Preserve all existing A1-T6 behavior outside oracle-input strictness (the committed complete+clean oracle MUST still pass). If the finding is stale (code already changed) or the fix needs larger architecture/schema work, STOP and report with evidence instead of inventing a fix.
Source of truth: dev-memo/deferred-audit-findings.md (the A1T6-AUD-L1 row — the exact finding to close); native/evidence-core-swift/Sources/EvidenceCoreSmoke/A1CitationStabilityHarness.swift (the A1-T6 gate to harden); native/evidence-core-swift/Tests/EvidenceCoreSmokeTests/A1CitationStabilityHarnessTests.swift (the A1-T6 test pattern); native/evidence-core-swift/Tests/EvidenceCoreSmokeTests/Fixtures/a1-citation/{citation-map.json,citation-map.oracle.json} (the committed fixture+oracle — READ-ONLY, must still pass); native/evidence-core-swift/Sources/EvidenceCoreSmoke/A3AnchorRegressionHarness.swift (the analogous oracle-completeness pattern); dev-memo/study/2026-06-30-batch-audit-188.md (the source audit); .claude/rules/{evidence-genie,cc-suite,autonomy,execution-discipline,staging-hygiene}.md
Allowed files: native/evidence-core-swift/Sources/EvidenceCoreSmoke/A1CitationStabilityHarness.swift (the two oracle-strictness checks only), native/evidence-core-swift/Tests/EvidenceCoreSmokeTests/A1CitationStabilityHarnessTests.swift (regression tests), dev-memo/deferred-audit-findings.md (flip the A1T6-AUD-L1 row status open->closed + resolution note ONLY), dev-memo/run/queue.md, dev-memo/run/queue.linted, dev-memo/run/queue.reviewed, dev-memo/run/queue.governed, dev-memo/run/reviews/queue-review-117.md
Forbidden files: native/evidence-core-swift/Sources/EvidenceCoreSmoke/{A07ConformanceHarness,A3AnchorRegressionHarness,A10GoldenExportHarness}.swift + their CLIs/tests (the A0.7/A3/A10 gates — READ-ONLY/regression-only; NO edit), native/evidence-core-swift/Tests/EvidenceCoreSmokeTests/Fixtures/a1-citation/** (the committed A1 fixture+oracle — READ-ONLY; NO rewrite), native/evidence-core/** (the JS shim — READ-ONLY), apps/** (NO application/export/A10/renderer/electron/test/package change), apps/lawbar-desktop/src/caseBox/export/** (the A10 modules — READ-ONLY; NO reopen), dev-memo/evidence-a10-closeout-00.md (the A10 closeout doc — READ-ONLY), services/** + docs/contracts/** (NO services/schema/contract change), services/case-box-persistence/src/sqlite/schema.ts (NO schema change), scripts/** + .claude/** + .github/** + migrations/**, docs/adr/** + docs/reference/** (READ-ONLY), dev-memo/run/evidence/** (gitignored custody marker/ledger — never touched/staged), committed keys, dev-memo/run/reports/** + dev-memo/run/override-reason.md + dev-memo/run/autonomous-next.md (untracked run-state — never staged/edited), Evidence-Genie-M0-Developer-Handover.md + xiaolai-dev-workflow-study.md (root intake — READ-ONLY); NO A10 reopen; NO T3/T4/T5 forms; NO A8; NO schema mutation; NO custody/marker; NO HMAC/key; NO confidential fixture; NO JS shim; NO unrelated export-pipeline work; NO broad A1-T6 refactor; NO push/merge/branch-delete
Gates: broker /cc-suite:review-plan REQUIRED before considering the fix reviewed — confirm: the two checks (oracle-row validity `text` iff clean + known outcome; oracle completeness set-equality + no duplicate keys) are the correct narrowest closure of A1T6-AUD-L1; they REJECT as `fixture_or_oracle_invalid` (never a false-green); the committed complete+clean oracle still PASSES; this does not broaden A1-T6 or reopen A10. STOP-points: (1) if the finding is stale (the harness already validates this) -> STOP and report; (2) if closing it requires schema/architecture/contract change -> STOP; (3) if it requires editing the committed A1 fixture/oracle, A10 files, services, or another gate -> STOP; (4) if it would change A1-T6 behavior beyond oracle-input strictness (e.g. alter a valid oracle's verdict) -> STOP. If review-plan NEEDS-FIX, fix-forward within A1T6-AUD-L1 scope. After: `swift build`/`swift test --package-path native/evidence-core-swift` GREEN (the swiftpm-smoke gate); the A1-T6 gate (`a1-citation-stability-cli`) on the committed fixture+oracle still prints `pass` exit 0; `scripts/workflow/check-contract-integrity.sh` exits 0; broker /cc-suite:audit + /cc-suite:verify (no Critical/High/Medium open before commit). No A0.7 custody. Batch is 1/3 (marker 2f80288); NOT due — no closeout this lane.
Acceptance criteria: `A1CitationStabilityHarness.swift` `evaluate` REJECTS (returns `fixture_or_oracle_invalid`, status fail) an incomplete oracle (missing a derived page), an over-covering oracle (a non-derived page), a duplicate oracle page key, a non-clean (`ambiguous`/`non_citable`) expected row carrying a `text`, a clean expected row MISSING `text`, and an unknown outcome string; the committed complete+clean oracle still PASSES (`a1-citation-stability-cli` outputs `pass`); new regression tests in `A1CitationStabilityHarnessTests.swift` PROVE each of those reject cases and the still-pass case; the `A1T6-AUD-L1` row Status is flipped to `closed` with a resolution reference; `swift build`+`swift test` PASS; `check-contract-integrity` PASSES (exits 0); broker review-plan returns READY + audit no Critical/High/Medium + verify ALL CLOSED; the committed diff CONTAINS only the A1 harness + A1 tests + the A1T6-AUD-L1 row + governance (NO A10/forms/A8/schema/custody/JS-shim/export/other-gate/fixture change; existing A1-T6 valid-oracle behavior unchanged).
Risk flags: LOW/MEDIUM-RISK native-gate hardening (Evidence A1-T6 citation-stability gate; closing a deferred Low) — a STANDALONE oracle-input-strictness change + tests + a backlog-row status flip; NO A10 reopen, NO forms, NO A8, NO schema/services/app/export change, NO edit to the committed A1 fixture/oracle or other gates. Load-bearing care points: (a) NARROW — only the two named gaps (completeness + row validity); do NOT refactor A1-T6 broadly; (b) NO-FALSE-GREEN — reject classes must be `fixture_or_oracle_invalid` (never pass); (c) NON-REGRESSION — the committed complete+clean oracle MUST still pass (a1-citation-stability-cli `pass`); (d) do NOT touch A10/forms/A8/schema/custody/JS-shim/export or fold into A10-T1. If any cannot hold, STOP and report. No push/merge/branch-delete.
Depends on: none
Commit boundary: governance (mark-reviewed + govern) run STANDALONE before the commit (never bundled with git commit). ONE implementation commit — native/evidence-core-swift/Sources/EvidenceCoreSmoke/A1CitationStabilityHarness.swift + native/evidence-core-swift/Tests/EvidenceCoreSmokeTests/A1CitationStabilityHarnessTests.swift + dev-memo/deferred-audit-findings.md (A1T6-AUD-L1 row status flip + resolution) + governance artifacts (queue.md/.linted/.reviewed/.governed + reviews/queue-review-117.md), staged by exact path; NO apps/services/schema/A10/other-gate/fixture/package file; NO marker/evidence/key/report file; no push; no merge; no branch delete.
