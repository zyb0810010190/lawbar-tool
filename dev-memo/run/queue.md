## WI-FORMS-T4-S0-SPEC-FOUNDATION-00: T4 举证质证表 proof-model + scope-reconciliation DECISION ADR (design-only) (Type: PLAN)
Type: PLAN
Scope: Produce ONE design/decision ADR (`docs/adr/` or `dev-memo/`) that frames the T4 举证质证表 (proof / cross-examination matrix) so a future implementation lane can proceed ONLY AFTER the user resolves the blockers — per `dev-memo/forms-t4-spec-00.md` (BINDING). This is a DESIGN/DECISION-FOUNDATION lane: it writes ONE ADR + queue governance; it implements NOTHING, changes no schema/contract/persistence, and makes NO product-scope decision on the agent's own authority. The ADR MUST: (a) **surface Blocker B1 (M0-vs-post-v1 STOP-AND-ASK)** — the `READY` project brief (`docs/product/project-requirements-brief.md` §12/§14) lists the **proof matrix among post-v1 export candidates, "each a separate STOP-AND-ASK ADR"**, while the PRD/forms-spec treated the forms as an M0 promise (forms-spec §F Q6 `RECONCILIATION-NEEDED`); the ADR records this as an unresolved user STOP-AND-ASK + a `RECONCILIATION-NEEDED` between the brief and the PRD, and does NOT decide it; (b) **frame Blocker B2 (proof-model product/legal decision)** — the 争议焦点/证明对象/evidence↔object-linkage/对方质证-三性(真实性·合法性·关联性 accept/reject/partial + reasons)/proof-gap model, presented as OPTIONS (structured matrix vs narrative, overlapping the T5 §F Q5 fork) for a user/legal decision, NOT designed as final (designing them absent the decision is invention, forbidden by ADR-evidence-a10 §10 + forms-spec §E); (c) **conditionally specify Blocker B3 (schema/contract additions)** — the new persisted fields these imply, described as a LATER additive schema/contract ADR + explicit approval (S0-style precedent), explicitly gated behind B1+B2 and NOT performed here; (d) record the smallest-safe first slice + the reuse baseline (the completed forms-T3 S0–S3 chain) WITHOUT changing any T3 behavior. FORBIDDEN in this lane and in the ADR it produces: any T4 implementation (preview/table/DOCX/PDF code); any product-scope decision made by the agent (B1 is the user's STOP-AND-ASK); any final proof-model field design; any schema/migration/DDL/`CURRENT_SCHEMA_VERSION`/contract/persistence change; any T5 work; native/custody/marker/seal/JS-shim/A8 work; new runtime dependency; raw client samples or tracked intake fixtures; broad renderer/UI redesign; any change to completed T3 behavior. If the future lane finds it cannot produce the ADR without the user first resolving B1 (post-v1 STOP-AND-ASK) or B2 (proof model), it STOPS and asks the user — it does NOT invent the scope or the proof model. If review/audit asks for T4/T5 implementation, schema work, or a product-scope decision, REJECT as out of scope and report.
Source of truth: dev-memo/forms-t4-spec-00.md (BINDING T4 framing: target, source-data gaps, candidate structure decision-pending, hard blockers B1/B2/B3, smallest-safe first slice); dev-memo/forms-spec-a10-t3-t5-sample-adr-00.md §C/§E/§F (T4 proof-model gaps; non-decisions; Q6 RECONCILIATION-NEEDED; Q7 new fields); docs/product/project-requirements-brief.md status:READY §12/§14 (proof matrix = post-v1 STOP-AND-ASK export candidate; AUTHORITATIVE product direction); dev-memo/plan-forms-t3-evidence-catalog-00.md + dev-memo/adr-forms-t3-s0-schema.md + dev-memo/adr-forms-t3-s3-docx-export.md (the S0→S3 ADR/schema/impl chain pattern T4 echoes one step later); .claude/rules/autonomy.md (product-direction + new-dep hard stops) + project-brief.md (READY brief governs product direction) + evidence-genie.md (manual-truth invariant) + client-local-first.md + cc-suite.md + staging-hygiene.md + loc-guardian.md
Allowed files: docs/adr/ADR-forms-t4-proof-model-scope.md (or dev-memo/adr-forms-t4-*.md — the ONE new T4 decision-foundation ADR the FUTURE design lane produces) + that future lane's own review artifact under dev-memo/run/reviews/. NOTE: the T4 spec note (dev-memo/forms-t4-spec-00.md) + THIS queue governance (queue.md/linted/reviewed/governed) + reviews/queue-review-126.md are committed by the SEPARATE authoring lane (this WI's authoring PR) and are NOT re-staged in the future ADR lane; the future ADR lane commits the ADR (+ its own review artifact) and records its review-plan in the COMMIT MESSAGE (per the S3 two-lane precedent). The future ADR lane MUST NOT modify the spec note or this lane's queue governance.
Forbidden files: apps/lawbar-desktop/ (NO app/renderer/IPC/DTO/export code — no T4 implementation), docs/contracts/ (NO contract change), services/ (NO persistence change), services/case-box-persistence/src/sqlite/schema.ts (CURRENT_SCHEMA_VERSION stays 12), migrations/ (none), native/, package.json/package-lock.json of any package (no dependency), dev-memo/run/intake/ (raw samples — never staged), docs/product/project-requirements-brief.md (READ-ONLY — the brief is not edited here; a RECONCILIATION is surfaced, not performed), other docs/adr/ + dev-memo design docs (READ-ONLY except the ONE new T4 ADR), scripts/ + .claude/ + .github/, Evidence-Genie-M0-Developer-Handover.md + xiaolai-dev-workflow-study.md; NO T4/T5 implementation; NO schema/contract/persistence change; NO product-scope decision; NO PDF; NO A8; NO custody/marker; NO JS shim; NO new dependency; push limited to this WI's scoped branch + PR open; NO merge; NO branch delete
Gates: DESIGN/PLAN lane (docs-only; the ADR is a design/decision record, not high-risk product code): (1) broker /cc-suite:review-plan on this queue WI + the T4 spec note BEFORE the future ADR is written, and again on the produced ADR; (2) `scripts/workflow/check-contract-integrity.sh` exit 0; `scripts/workflow/check-queue.sh` PASS; (3) NO product code, so no desktop/contract/persistence test run is required unless a code file is unexpectedly touched (it must not be). Forbidden-scope scan before commit: no CURRENT_SCHEMA_VERSION change; no migration/DDL; no apps/services/contracts/native change; no dependency; no raw-sample commit; no T4/T5 code; no product-scope decision recorded as decided. STOP-points: the ADR cannot be written without the user resolving B1 (post-v1 STOP-AND-ASK) or B2 (proof model) → STOP and ask the user; review/audit requests implementation/schema/scope-decision → REJECT and report; any forbidden file would change → STOP. NO swiftpm-smoke (native untouched).
Acceptance criteria: the ADR exists as ONE new design/decision record that surfaces B1 as an unresolved user STOP-AND-ASK + RECONCILIATION-NEEDED (brief vs PRD) without deciding it, frames B2 proof-model options (structured matrix vs narrative; 争议焦点/证明对象/三性/proof-gap) as user/legal decisions without designing final fields, conditionally specifies B3 schema additions as a gated later ADR, records the smallest-safe first slice + the forms-T3 reuse baseline, and states explicitly that no T4 implementation / schema change / product-scope decision is authorized; the FUTURE ADR lane's committed diff is ONLY the ONE new T4 ADR + its own future review artifact under dev-memo/run/reviews/ (docs-only) and EXCLUDES the spec note, this queue governance, and queue-review-126.md (which are committed by THIS separate authoring lane); CURRENT_SCHEMA_VERSION remains 12; no apps/services/contracts/native/dependency change; no raw sample committed; check-queue + check-contract-integrity pass; review-plan READY (or the lane STOPS and asks the user if the reviewer finds T4 too product-blocked to govern even a design-foundation ADR).
Risk flags: MEDIUM (governance/product-direction sensitivity, not code risk) — T4 sits behind a `READY`-brief post-v1 STOP-AND-ASK + a RECONCILIATION-NEEDED; the danger is an artifact that silently presumes M0 scope or invents the proof model. Care points: (a) SURFACE-NOT-DECIDE B1 — the M0-vs-post-v1 scope is the user's STOP-AND-ASK; the ADR records it, never resolves it; (b) NO INVENTION of the proof model (B2) — options only, per forms-spec §E/ADR-a10 §10; (c) NO schema change (B3 is a later gated ADR); (d) NO T4/T5 code, no dependency, no T3 behavior change; (e) DOCS-ONLY diff; (f) INPUT-ONLY SAMPLES never staged. If the reviewer judges T4 too product-blocked to govern even this design-foundation ADR, STOP and ask the user (do not force governance).
Depends on: none
Commit boundary: TWO separate lanes. (1) THIS authoring lane commits ONLY the T4 spec note (`dev-memo/forms-t4-spec-00.md`) + queue governance (queue.md/linted/reviewed/governed) + reviews/queue-review-126.md — no implementation, no schema, no product-scope decision. (2) The FUTURE design lane commits ONLY the ONE new T4 decision ADR (+ its own review artifact), records its review-plan in the commit message, and does NOT re-stage this lane's spec/queue/review artifacts. NO apps/services/contracts/schema/migration/native/dependency file in either lane; NO staging of dev-memo/run/intake/ or untracked residue; push of the scoped WI branch + PR open; no merge; no branch delete. THIS authoring lane makes NO product-scope decision and writes NO ADR — it only frames T4 and governs the design-foundation WI.
