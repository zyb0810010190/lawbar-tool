## WI-RELEASE-G3-SCREEN-COUNT-READINESS-HYGIENE-00: define + govern a bounded document-consistency fix that corrects the stale gate-3 readiness-report evidence count "12 renderer screens" → "11 renderer screens + `auditEventLabels.ts` (a non-screen label map)" — a narrow release-matrix hygiene fix so gate 6 does not inherit a known-inaccurate fact; NOT a holistic readiness refresh, NOT gate-3 clearance, NOT R3 implementation (Type: EVIDENCE, release-governance LOW risk)

## Review packet (compact)
- Active plan summary: the gate-3 row of `docs/release/go-live-readiness-report.md` §1 (row 3) still states "**12 renderer screens** (`renderer/screens/`: … + auditEventLabels)" in its evidence column — but `apps/lawbar-desktop/renderer/screens/` has **12 `.ts` files = 11 renderer screens + 1 label map**; `auditEventLabels.ts` is a renderer-only `event_kind`→human-label lookup, NOT a screen. This exact miscount was already corrected inside the gate-3 R3 assessment WI (batch-256 M1) + surfaced in the R3 disposition text, which flags the evidence-column count as "a documentation-lag to reconcile at a readiness refresh." This WI **governs** (does NOT execute) a FUTURE lane that makes the narrow correction so gate 6 does not inherit a known-inaccurate release-matrix fact. THIS authoring lane produces the governed queue WI only; it changes NO doc, clears NO gate, and decides NO user go-live hard-stop.
- Grounding note (verified 2026-07-08 — the exec lane MUST re-verify against the real files before relying, per `AGENTS.md` §"Authoring discipline"): `apps/lawbar-desktop/renderer/screens/` = 12 `.ts` files: `listMatters, createMatter, archiveMatter, viewMatter, viewMatterAudit, viewMatterDeadlines, viewMatterDocketProposals, viewMatterDocuments, viewMatterFacts, viewMatterLinks, viewMatterT3Catalog` (the **11 screens**) + `auditEventLabels.ts` (the **label map, not a screen**). `docs/release/gate3-r3-polish-assessment-00.md` §2 already records the accurate 11-screen framing.
- Exact target files (FUTURE exec lane): `docs/release/go-live-readiness-report.md` — the gate-3 row EVIDENCE-COLUMN wording ONLY (correct "12 renderer screens (…+ auditEventLabels)" → "11 renderer screens (…) + `auditEventLabels.ts` (a non-screen label map)"; and, since the count is now reconciled inline, tidy the R3-disposition clause's now-redundant "documentation-lag to reconcile at a readiness refresh" note to "reconciled here") + `dev-memo/run/reviews/` (the exec lane's review artifact). NO other file, NO other gate row, NO status/roll-up/bucket change.
- Exact acceptance criteria: below.
- Exact out-of-scope: NO gate-3 CLEARANCE (gate 3 stays PARTIAL); NO change to gate-3 STATUS or the roll-up/status bucket; NO change to any OTHER gate row/classification; NO R1/R2/R3 disposition change beyond the count-reconcile tidy (R1 stays gate-4-blocked; R2 stays SHIPPED; R3 stays ASSESSED non-M0/deferred); NO R3 IMPLEMENTATION; NO UI/source/test/package/config change; NO dependency change; NO schema/migration (`CURRENT_SCHEMA_VERSION` stays 12); NO contract change; NO ADR/brief edit; NO gate-4 signing/notarization/公开 distribution decision (gate 4); NO gate-6 full-project audit; NO holistic readiness refresh (this is a single-fact hygiene fix, NOT a refresh); NO gate-7 D-G7 risk acceptance; NO 律师法 conclusion (gate 11); NO license/legal/business decision (gate 17); NO final GO/NO-GO (gate 21); NO staging of `dev-memo/run/intake/` or untracked residue.
- Requirements the FUTURE correction must satisfy (what the exec lane must do) — at minimum:
  1. **Exact stale phrase to correct**: the gate-3 row evidence-column "**12 renderer screens** (`renderer/screens/`: … + auditEventLabels)". Replace the count with **11 renderer screens** (the enumerated list) + `auditEventLabels.ts` explicitly labelled a **non-screen label map**.
  2. **Consistency sweep**: after the edit, `grep '12 renderer screens' docs/release/go-live-readiness-report.md` returns NOTHING (no stale count remains in the readiness report). Any remaining "12" is only the accurate "12 `.ts` files = 11 screens + 1 label map" decomposition if used, or an unrelated count (e.g. schema version 12) — never "12 renderer screens".
  3. **Reconcile the R3-disposition note (narrow)**: the R3-disposition clause currently says the evidence-column count is "a documentation-lag to reconcile at a readiness refresh"; since this WI reconciles it, tidy ONLY that dependent phrase (to "reconciled here"). This edit is STRICTLY limited to the documentation-lag phrase — it MUST NOT alter the R3 disposition itself (R3 stays "ASSESSED non-M0 / deferred") and MUST NOT imply R3 completion/implementation.
  4. **Preserve every disposition**: gate 3 stays **PARTIAL**; R1 stays gate-4 STOP-AND-ASK (user-owned); R2 stays SHIPPED; R3 stays ASSESSED non-M0/deferred. NO gate cleared, NO status/roll-up change, NO other gate row touched.
  5. **Expected artifact**: the corrected gate-3 evidence-column wording in `go-live-readiness-report.md` + the exec review artifact. This is a documentation-consistency fix, NOT new evidence and NOT a readiness refresh.
  6. **Deterministic pass/fail**: PASS = the count reads "11 renderer screens + `auditEventLabels.ts` (non-screen label map)", no residual "12 renderer screens" in the readiness report, gate 3 still PARTIAL, all R-dispositions + other gate rows unchanged. FAIL = a residual "12 renderer screens", a gate-3 status/roll-up change, an other-gate-row change, or any out-of-scope edit.
- Dependencies mapped: **gate 6** (the correction ensures gate 6 does not inherit the inaccurate count; gate 6 NOT run here); **gate 3** (row corrected, NOT cleared, stays PARTIAL); **gate 4** (R1 STOP-AND-ASK, untouched). These are noted as related, NOT cleared by this WI.
- Essential refs: `docs/release/go-live-readiness-report.md` §1 gate-3 row (the evidence-column count to correct); `docs/release/gate3-r3-polish-assessment-00.md` §2 (the accurate 11-screen framing); `apps/lawbar-desktop/renderer/screens/*.ts` (READ-ONLY verification — 11 screens + the auditEventLabels label map); `AGENTS.md` §"Source hierarchy" + §"Authoring discipline"; `.claude/rules/security-boundary.md` §"Go-live independence"; `.claude/rules/autonomy.md` + `staging-hygiene.md` + `loc-guardian.md`.
- Review questions: (1) Is the WI a NARROW single-fact document-consistency fix (correct "12 renderer screens" → "11 screens + label map" in the gate-3 evidence column), NOT a readiness refresh and NOT execution in this authoring lane? (2) Does it preserve gate 3 PARTIAL + R1 gate-4-blocked + R2 shipped + R3 non-M0/deferred, and forbid any status/roll-up/other-gate change? (3) Does it forbid gate-3 clearance, R3 implementation, UI/source change, gate-4/6 decisions, and any go-live decision? (4) Is any gate clearance, holistic refresh, or out-of-scope edit smuggled in?

Type: EVIDENCE
Scope: Govern (do NOT execute) a FUTURE lane that makes a bounded document-consistency correction to `docs/release/go-live-readiness-report.md`: the gate-3 row evidence-column count "12 renderer screens (…+ auditEventLabels)" → "11 renderer screens (…) + `auditEventLabels.ts` (a non-screen label map)", plus the small dependent tidy of the R3-disposition clause's now-redundant "documentation-lag to reconcile" note (the one directly-supporting edit). It changes ONLY the gate-3 row wording (evidence-column count + the dependent R3-note tidy); it makes NO gate-3 status/roll-up/bucket change, NO other-gate-row change, NO R1/R2/R3 disposition change (beyond the count-reconcile tidy), NO R3 implementation, NO UI/source/test/config change, NO gate-4 decision, NO gate-6 audit, NO holistic readiness refresh, and NO go-live decision. THIS authoring lane implements + runs + decides NOTHING — it only governs the hygiene WI. If review asks to clear gate 3, change a gate status, run a readiness refresh, implement R3, change source/UI, make a gate-4 decision, or run gate 6, REJECT and report.
Source of truth: docs/release/go-live-readiness-report.md §1 gate-3 row (evidence-column count); docs/release/gate3-r3-polish-assessment-00.md §2 (accurate 11-screen framing); apps/lawbar-desktop/renderer/screens/*.ts (READ-ONLY — 11 screens + auditEventLabels label map); AGENTS.md §"Source hierarchy" + §"Authoring discipline"; .claude/rules/security-boundary.md §"Go-live independence"; .claude/rules/autonomy.md + staging-hygiene.md + loc-guardian.md
Allowed files: docs/release/go-live-readiness-report.md (the gate-3 row wording ONLY — the evidence-column count correction + the dependent R3-note tidy; NO status/roll-up/bucket edit; NO other gate row) + dev-memo/run/reviews/ (the exec lane's own review artifact). NOTE: THIS authoring lane commits ONLY the queue governance (queue.md/linted/reviewed/governed) + reviews/queue-review-171.md — NO readiness-report change. The FUTURE exec lane records its evidence in the COMMIT MESSAGE + review artifact and does NOT re-stage this lane's queue governance.
Forbidden files: docs/release/ OTHER than go-live-readiness-report.md (the other release docs — incl. gate3-r3-polish-assessment-00.md + gate3-client-release-readiness-00.md — are READ-ONLY source, NOT edited), docs/product/project-requirements-brief.md (READ-ONLY), docs/adr/ (READ-ONLY), docs/contracts/, apps/ + services/ + native/ source/test (NO UI/source/test change), services/*/src/sqlite/schema.ts (CURRENT_SCHEMA_VERSION stays 12), migrations/, package.json/package-lock.json of any package (NO dependency change), dev-memo/run/intake/ (raw samples — never staged), dev-memo/ OTHER than run/reviews/ (no other plan/design edit), .claude/ + .github/; NO gate-3 clearance; NO gate-3 status/roll-up/bucket change; NO other-gate-row change; NO R3 implementation; NO UI/source/test/package/config change; NO DOC CORRECTION RUN in this authoring lane; NO gate-4 signing/distribution decision; NO gate-6 full-project audit; NO holistic readiness refresh; NO gate-7 D-G7 risk acceptance; NO 律师法 conclusion (gate 11); NO license/legal/business decision (gate 17); NO final GO/NO-GO decision (gate 21). Push is NEVER automatic (autonomy hard-stop): local commit governance is the default; any branch push + PR open happens ONLY under explicit per-invocation user authorization; NO merge; NO branch delete of shared branches
Gates: EVIDENCE/governance lane (LOW release-governance risk — a narrow single-fact document-consistency fix; no code, no UI change, no gate clearance, no go-live decision). (1) THIS authoring lane: broker /cc-suite:review-plan on this queue WI, Path 1, retrievable, then govern. (2) The FUTURE exec lane: make the narrow correction, then broker a POST-ARTIFACT /cc-suite:review-plan on the produced gate-3 row diff. (3) scripts/workflow/check-contract-integrity.sh exit 0; scripts/workflow/check-queue.sh PASS (both lanes). Forbidden-scope scan before commit: CURRENT_SCHEMA_VERSION EQUALS 12; no UI/source/test/package/config change; no ADR/brief edit; no gate-3 clearance; no gate-3 status/roll-up change; no other gate row change; no gate-4 decision; no gate-6 run; no holistic readiness refresh; no GO/NO-GO; no raw sample; after the exec edit, no "12 renderer screens" remains in the readiness report. STOP-points: review asks to clear gate 3, change a gate status, run a readiness refresh, implement R3, change source/UI, make a gate-4 decision, or run gate 6 → REJECT/STOP and report; any forbidden file would change → STOP.
Acceptance criteria: check-queue and check-contract-integrity PASS and CURRENT_SCHEMA_VERSION EQUALS 12 and the diff CONTAINS only the queue governance (queue.md/linted/reviewed/governed) + reviews/queue-review-171.md and no readiness-report/source/test/package/config change appears and no gate-3 clearance / no gate-status change / no other-gate change / no R3 impl / no readiness refresh appears. The governed WI must DEFINE the six correction requirements (exact stale phrase; consistency sweep to zero "12 renderer screens"; the R3-note reconcile tidy; preserve every disposition; expected artifact; deterministic pass/fail), keep gate 3 `PARTIAL`, forbid a gate clearance / status change / other-gate change / R3 implementation / readiness refresh, and MUST NOT decide a STOP-AND-ASK hard-stop or assert go-live. review-plan (this lane) must be RUN and must PASS (READY, or READY with only Low-risk clarifications applied).
Risk flags: LOW (release-governance — a narrow single-fact document-consistency fix that could over-reach into a gate-status change, an other-gate edit, or a readiness refresh). Care points: (a) NO CORRECTION RUN in this authoring lane — this lane only governs the WI; (b) SINGLE-FACT SCOPE — ONLY the gate-3 evidence-column count + the dependent R3-note tidy; NO gate-3 status/roll-up change, NO other gate row; (c) PRESERVE DISPOSITIONS — gate 3 PARTIAL, R1 gate-4-blocked, R2 shipped, R3 non-M0/deferred all unchanged; (d) NOT A REFRESH — this is a single stale-fact fix, explicitly NOT a holistic readiness refresh; (e) GO-LIVE INDEPENDENCE — a doc-hygiene fix does NOT imply go-live; the final verdict + the STOP-AND-ASK hard-stops (4/11/17/21) remain the user's. If any cannot hold, STOP and report.
Depends on: none
Commit boundary: TWO separate lanes. (1) THIS authoring lane commits ONLY the queue governance (queue.md/linted/reviewed/governed) + reviews/queue-review-171.md — no readiness-report change, no implementation, no decision. (2) The FUTURE execution lane commits ONLY the gate-3 row correction in docs/release/go-live-readiness-report.md + its own review artifact under dev-memo/run/reviews/, records its evidence (the before/after count + the grep-zero proof) in the COMMIT MESSAGE, and does NOT re-stage this lane's queue governance. NO source/test/UI/package/config file in either lane; NO gate-3 clearance; NO gate-status/other-gate change; NO R3 implementation; NO readiness refresh; NO staging of dev-memo/run/intake/ or untracked residue; push is NEVER automatic (autonomy hard-stop) — any branch push + PR open happens ONLY under explicit per-invocation user authorization; no merge; no branch delete of shared branches. THIS authoring lane implements NOTHING, runs NO correction, and decides NOTHING — it only governs the gate-3 screen-count hygiene WI.

## Acceptance criteria (FUTURE execution lane — what the governed correction must satisfy)

These describe the deliverable the FUTURE exec lane must produce; they are NOT preconditions of THIS authoring lane. The authoring-lane acceptance is the structured `Acceptance criteria:` field above (queue governance + review READY only — the correction is NOT made in this lane).

1. `docs/release/go-live-readiness-report.md` gate-3 row evidence column reads "**11 renderer screens** (`renderer/screens/`: the 11 enumerated screens) + `auditEventLabels.ts` (a non-screen label map)" — the stale "12 renderer screens" is gone.
2. `grep '12 renderer screens' docs/release/go-live-readiness-report.md` returns NOTHING; any remaining "12" is the accurate "12 `.ts` files = 11 screens + 1 label map" decomposition or an unrelated count (schema version), never "12 renderer screens".
3. The R3-disposition clause's "documentation-lag to reconcile at a readiness refresh" note is tidied (to "reconciled here") STRICTLY on that dependent phrase — the R3 disposition itself ("ASSESSED non-M0 / deferred") is unchanged and R3 completion/implementation is NOT implied.
4. Gate 3 stays **PARTIAL**; R1 gate-4 STOP-AND-ASK, R2 SHIPPED, R3 non-M0/deferred all preserved; NO gate-3 status/roll-up/bucket change; NO other gate row touched.
5. NO UI/source/test/package/config/schema/contract change; NO R3 implementation; NO ADR/brief edit; NO holistic readiness refresh; `CURRENT_SCHEMA_VERSION` stays 12; docs-only tracked output (a single release-matrix hygiene fix).
6. `scripts/workflow/check-queue.sh` PASS + `scripts/workflow/check-contract-integrity.sh` PASS on both lanes.
