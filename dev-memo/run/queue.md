## WI-A3-LINK-T2-AUD-L1: harden renderer i18n event-kind coverage test (Type: TEST)
Type: TEST
Scope: Close the deferred Low `T2-AUD-L1` — TEST-COMPLETENESS hardening only, NO product behavior change. `apps/lawbar-desktop/tests/renderer-i18n.test.mjs` has a hand-maintained `ALL_EVENT_KINDS` array (line 62) that omits the 3 A3-link audit kinds (`LINK_CREATED`/`LINK_UNLINKED`/`LINK_RELINKED`), so the runtime "eventKindLabel resolves a non-empty zh-CN label for every audit event kind" loop (line 78) does not iterate them. Fix DURABLY: replace the hardcoded array with a derivation from the canonical contract source — add `import { CASE_BOX_AUDIT_EVENT_KINDS } from "case-box-contract";` (the exact form already used by `tests/renderer-audit-labels.test.mjs:8`) and set `const ALL_EVENT_KINDS = Object.keys(CASE_BOX_AUDIT_EVENT_KINDS);`. This makes the per-kind label-coverage loop exhaustive over ALL contract audit kinds (53, incl. the 3 LINK_*) and self-maintaining for any future kind, while staying NON-tautological (it still asserts `eventKindLabel(kind)` returns a non-empty string via the renderer's labels/catalog). Do NOT touch the separate count assertion ("holds all 53 eventKind.* keys") — it is already correct + an independent check. NO other test edit. Flip `T2-AUD-L1` to `closed` in `dev-memo/deferred-audit-findings.md` ONLY AFTER the strengthened test passes (now iterating the 3 LINK_* kinds). Keep `LINK-IPC-T1-D1` + `DESKTOP-DEPS-STALE-LOCK-01` closed.
Source of truth: apps/lawbar-desktop/tests/renderer-i18n.test.mjs (the ALL_EVENT_KINDS array + the per-kind label test); apps/lawbar-desktop/tests/renderer-audit-labels.test.mjs:8 (the precedent `import { CASE_BOX_AUDIT_EVENT_KINDS } from "case-box-contract"` + `Object.keys` derivation); apps/lawbar-desktop/node_modules/case-box-contract/dist/audit-log.js (`CASE_BOX_AUDIT_EVENT_KINDS` — 53 keys incl. LINK_CREATED/LINK_UNLINKED/LINK_RELINKED); dev-memo/deferred-audit-findings.md (T2-AUD-L1); .claude/rules/{cc-suite,autonomy,execution-discipline,staging-hygiene,evidence-genie}.md
Allowed files: apps/lawbar-desktop/tests/renderer-i18n.test.mjs, dev-memo/deferred-audit-findings.md, dev-memo/run/queue.md, dev-memo/run/queue.linted, dev-memo/run/queue.reviewed, dev-memo/run/queue.governed, dev-memo/run/reviews/queue-review-104.md
Forbidden files: apps/lawbar-desktop/tests/** EXCEPT renderer-i18n.test.mjs (NO other test edit; NO D1 test/hook change), apps/lawbar-desktop/renderer/** (NO renderer/UI/i18n-catalog/labels change — the labels already exist + pass), apps/lawbar-desktop/electron/** (NO change; NO D1 hook), apps/lawbar-desktop/src/** (NO source change), apps/lawbar-desktop/package.json + package-lock.json + dist-tarballs/** (NO manifest/dependency/tarball change), scripts/workflow/check-internal-tarballs.mjs + check-gates.sh (NO drift-guard/gate change), services/** + docs/contracts/** (NO source/schema/contract change), services/case-box-persistence/src/sqlite/schema.ts (NO schema/`CURRENT_SCHEMA_VERSION` change), docs/adr/** + dev-memo/design/** (NO ADR/design edit), native/**, .claude/**, .github/**, AGENTS.md, CLAUDE.md, GEMINI.md, migrations/**, committed dev-memo/run/evidence/** marker/ledger/key/evidence-run files, committed keys, dev-memo/run/reports/** (untracked run-state — never staged), dev-memo/run/override-reason.md (untracked residue — never staged), Evidence-Genie-M0-Developer-Handover.md + xiaolai-dev-workflow-study.md (root intake — READ-ONLY); NO product behavior change, NO renderer/electron/src/persistence/schema/contract/dependency/packaging change, NO D1 work, NO marking D1/DESKTOP-DEPS reopened, NO branch delete, NO push/merge
Gates: broker /cc-suite:review-plan REQUIRED before implementation — confirm: the change is test-only + closes exactly the T2-AUD-L1 gap (the runtime loop now iterates LINK_CREATED/LINK_UNLINKED/LINK_RELINKED); deriving from `CASE_BOX_AUDIT_EVENT_KINDS` is the right durable approach (mirrors renderer-audit-labels.test.mjs) and stays non-tautological; NO product/renderer/dependency/schema change; the hardening is NOT broader than the L1 gap. STOP-points: if review-plan says this needs a design/UI artifact or broader renderer work (stop-point 1); if the fix requires product behavior changes (stop-point 2); if it requires package/dependency/schema/contract/persistence changes (stop-point 3); if the hardening grows beyond the coverage gap (stop-point 4). If review-plan NEEDS-FIX, fix-forward; if a blocker, STOP. After implementation: prove the runtime list now includes the 3 LINK_* kinds; `node --test apps/lawbar-desktop/tests/renderer-i18n.test.mjs` GREEN; `npm --prefix apps/lawbar-desktop test` GREEN; `node scripts/workflow/check-internal-tarballs.mjs` PASSES; `scripts/workflow/check-contract-integrity.sh` exits 0; broker /cc-suite:audit + /cc-suite:verify (no Critical/High/Medium open before commit). No A0.7 custody (test-only; no electron/geometry/marker/product code; review-plan MAY say otherwise but none expected).
Acceptance criteria: `apps/lawbar-desktop/tests/renderer-i18n.test.mjs` imports `CASE_BOX_AUDIT_EVENT_KINDS` from `case-box-contract` and derives `ALL_EVENT_KINDS = Object.keys(CASE_BOX_AUDIT_EVENT_KINDS)`; the per-kind `eventKindLabel` coverage loop now iterates all 53 kinds INCLUDING `LINK_CREATED`/`LINK_UNLINKED`/`LINK_RELINKED` (provable: a temporary assertion or a grep that the derived list contains them + the test passes); the count assertion (53 eventKind.* keys) is UNCHANGED; `node --test apps/lawbar-desktop/tests/renderer-i18n.test.mjs` exits 0; `npm --prefix apps/lawbar-desktop test` GREEN; drift guard PASSES; check-contract-integrity exits 0; `CURRENT_SCHEMA_VERSION` remains 12; NO renderer/electron/src/services/schema/contract/package/dependency/packaging change; NO other test file changed; `dev-memo/deferred-audit-findings.md` flips T2-AUD-L1 to `Status | closed` (citing this WI) ONLY AFTER the test passes, with `LINK-IPC-T1-D1` + `DESKTOP-DEPS-STALE-LOCK-01` left `closed`; broker review-plan READY + audit no Critical/High/Medium + verify ALL CLOSED; committed diff confined to Allowed files.
Risk flags: LOW-RISK test-only hardening — closes the deferred Low T2-AUD-L1. NO product behavior change. Load-bearing care points: (a) the only product-adjacent move is importing `case-box-contract` in a test (precedent exists in renderer-audit-labels.test.mjs); (b) the derivation must keep the assertion meaningful (eventKindLabel non-empty per kind) — NOT a tautology; (c) do not touch the count assertion or any other test; (d) NO renderer/catalog/labels change (labels already exist); (e) D1 + DESKTOP-DEPS stay closed. If the fix would need renderer/product/dependency/schema/contract change or a design artifact, STOP (stop-points 1/2/3). No push/merge/branch-delete.
Depends on: none
Commit boundary: governance (mark-reviewed + govern) run STANDALONE before the commit (never bundled with git commit). ONE test commit — apps/lawbar-desktop/tests/renderer-i18n.test.mjs + dev-memo/deferred-audit-findings.md (T2-AUD-L1 close, only after the test passes) + governance artifacts (queue.md/.linted/.reviewed/.governed + reviews/queue-review-104.md), staged by exact path; NO renderer/electron/src/services/schema/contract/package/dependency/tarball file; NO marker/evidence/key/report file; no push; no merge.
