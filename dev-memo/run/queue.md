## WI-EVIDENCE-A10-T6-GOLDEN-CANONICAL-EXPORT-00: implement A10 golden CanonicalExportModel (Type: IMPL)
Type: IMPL
Scope: Implement the A10-T6 golden CanonicalExportModel slice, per `docs/adr/ADR-evidence-a10-court-fileable-export.md` (§4 CanonicalExportModel transcription, §5 layering, §8 A10-T6 golden-export, §13 step-6). This lane MAY introduce the apps-layer `CanonicalExportModel` type + a deterministic serializer + golden fixture, but NOTHING ELSE: NO live export-pipeline wiring, NO native `golden-export` harness / JS shim (`native/**`), NO T3/T4/T5 forms, NO A8 snapshot/seal/ExportPreview, NO schema/native/UI/IPC/services change, NO custody/marker, NO confidential fixtures, NO A1T6-AUD-L1 hardening. Builds OVER the merged A10-T1 (`apps/lawbar-desktop/src/caseBox/export/a10CitationContract.ts`: `A10RenderedCitation` { citation 卷X页Y | flag }) + A10-T2 (`apps/lawbar-desktop/src/caseBox/export/a10HyperlinkDegradation.ts`: `toA10HyperlinkCitations` text-XOR-flag, `internalHref` non-authoritative). Add a NEW sibling apps-layer module `apps/lawbar-desktop/src/caseBox/export/a10CanonicalExportModel.ts` exporting a `CanonicalExportModel` type (per ADR §4: `{ exportType, citationFormatVersion, exportTemplateVersion, rows[], citations[], linkDegradations[], flags[], warnings[], sourceObjectIds[], generatedFromSnapshotId? }`), `buildCanonicalExportModel({exportType, rendered, warnings?, generatedFromSnapshotId?})` over `A10RenderedCitation[]`, a deterministic `serializeCanonicalExportModel(model)` (ADR §4 rule: stable key/row order, NFC-normalized strings, NO timestamps/machine-paths/renderer-metadata), and `canonicalModelSha256(model)` (SHA-256 hex of the serialization — the reproducibility unit). CRITICAL: the court-fileable AUTHORITY is the per-row 卷X页Y `citationText` OR the `flag`; the A10-T2 `internalHref` (non-authoritative nav metadata) is DELIBERATELY EXCLUDED from the canonical model — it MUST NOT appear in any row/serialization/hash. Hash the LOGICAL model ONLY — NEVER raw `.docx`/PDF bytes (ADR §3, evidence-genie invariant 9). Every input citation maps to EXACTLY one row (text XOR flag), never dropped; arrays in stable content-derived order (rows/citations/degradations by linkId; flags/sourceObjectIds/warnings sorted) so the serialization is byte-identical across runs/machines. Standalone module + a golden fixture (`apps/lawbar-desktop/tests/fixtures/a10-golden-canonical-export.json`) + unit tests; NOT wired into any live export IPC path. If A10-T6 cannot be implemented without wiring the live pipeline / building the native harness / inventing A8 / changing schema, STOP and report.
Source of truth: docs/adr/ADR-evidence-a10-court-fileable-export.md (A10-DESIGN-00 §4 CanonicalExportModel transcription + determinism rule, §5 canonical-model-vs-rendered-artifact layering, §8 A10-T6 golden-export byte-identical, §13 step-6 stop-points); docs/reference/evidence-genie-m0-developer-handover.md (§"Data model" CanonicalExportModel/ExportPreview, §"Two-layer export reproducibility", §"What not to do"); apps/lawbar-desktop/src/caseBox/export/a10CitationContract.ts (A10-T1 — A10RenderedCitation, CITATION_FORMAT_VERSION/EXPORT_TEMPLATE_VERSION); apps/lawbar-desktop/src/caseBox/export/a10HyperlinkDegradation.ts (A10-T2 — toA10HyperlinkCitations); apps/lawbar-desktop/tests/a10-hyperlink-degradation.unit.test.mjs (test pattern); .claude/rules/{evidence-genie,cc-suite,autonomy,execution-discipline,staging-hygiene,security-boundary,client-local-first,loc-guardian}.md
Allowed files: apps/lawbar-desktop/src/caseBox/export/a10CanonicalExportModel.ts (NEW; exact name may adjust per review-plan within apps/lawbar-desktop/src/caseBox/export/), apps/lawbar-desktop/tests/a10-canonical-export.unit.test.mjs (NEW), apps/lawbar-desktop/tests/fixtures/a10-golden-canonical-export.json (NEW golden fixture), apps/lawbar-desktop/package.json (ONLY a one-line test-script registration — surgical Edit, NO reformat), dev-memo/run/queue.md, dev-memo/run/queue.linted, dev-memo/run/queue.reviewed, dev-memo/run/queue.governed, dev-memo/run/reviews/queue-review-113.md
Forbidden files: apps/lawbar-desktop/src/caseBox/export/a10CitationContract.ts + a10HyperlinkDegradation.ts (the A10-T1/A10-T2 modules — READ-ONLY/imported; NO edit), apps/lawbar-desktop/src/caseBox/linkHandlers.ts + dto/link.ts (live export handler/DTO — NO wiring this lane), services/** + docs/contracts/** (NO services/schema/contract change), services/case-box-persistence/src/sqlite/schema.ts (NO schema/CURRENT_SCHEMA_VERSION change), native/** (NO native/Swift/JS-shim change incl. the `golden-export` harness; A0.7/A1-T6/A3-T10 gates regression-only), apps/lawbar-desktop/renderer/** (NO renderer UI change), apps/lawbar-desktop/electron/** (NO Electron IPC/preload change), apps/lawbar-desktop/{package-lock.json,dist-tarballs/**} (NO dependency/tarball change), scripts/** + .claude/** + .github/** + migrations/**, docs/adr/** + docs/reference/** (READ-ONLY), dev-memo/design/** (NO separate artifact unless review-plan requires), dev-memo/run/evidence/** (gitignored custody marker/ledger — never touched/staged), committed keys, dev-memo/run/reports/** + dev-memo/run/override-reason.md + dev-memo/run/autonomous-next.md (untracked run-state — never staged/edited), Evidence-Genie-M0-Developer-Handover.md + xiaolai-dev-workflow-study.md (root intake — READ-ONLY), apps/lawbar-desktop/dist/** (gitignored build output — never staged); NO live-pipeline/IPC wiring; NO A8 snapshot/seal/manifest/ExportPreview; NO T3/T4/T5 forms; NO custody/marker; NO HMAC/key request/receive/print/persist/stage; NO confidential/client/court fixture; NO A1T6-AUD-L1 hardening; NO push/merge/branch-delete
Gates: broker /cc-suite:review-plan REQUIRED before implementation — confirm: A10-T6 implementable as a NEW apps-layer module over the A10-T1/A10-T2 contracts WITHOUT wiring the live pipeline / building the native harness / inventing A8 / changing schema; the CanonicalExportModel shape matches ADR §4; the deterministic-serialization rule (stable key/row order, NFC, no timestamps/machine-paths/renderer-metadata) is sound; hashing the LOGICAL serialization (never raw .docx/PDF bytes) is correct; excluding internalHref from the canonical model is correct (authority = text/flag); the golden fixture + one-line package.json test registration are acceptable; NO schema/services/native/UI/IPC change. STOP-points: (1) if the CanonicalExportModel shape is undefined/contradicted by ADR/handover -> STOP; (2) if it requires wiring the live export pipeline or editing services/linkHandlers -> STOP; (3) if it requires building the native `golden-export` harness / JS shim -> STOP; (4) if it requires A8 snapshot/seal/ExportPreview -> STOP; (5) if schema/renderer-UI/electron-IPC/package(beyond test-script)/native/dependency change becomes necessary -> STOP; (6) if it would weaken A1/A3/A8 or the A0.7/A1-T6/A3-T10 native gates or the A10-T1/A10-T2 contracts -> STOP; (7) if raw-.docx/PDF-byte hashing seems required -> STOP (forbidden by default). If review-plan NEEDS-FIX, fix-forward; if a blocker, STOP. After implementation: targeted `node --test apps/lawbar-desktop/tests/a10-canonical-export.unit.test.mjs` GREEN; A10-T1+A10-T2 targeted tests GREEN (regression); `npm --prefix apps/lawbar-desktop test` GREEN; `scripts/workflow/check-contract-integrity.sh` exits 0; broker /cc-suite:audit + /cc-suite:verify (no Critical/High/Medium open before commit). No A0.7 custody. Batch is 1/3 (marker 725c017); NOT due — no closeout this lane.
Acceptance criteria: a NEW apps-layer module `apps/lawbar-desktop/src/caseBox/export/a10CanonicalExportModel.ts` exports `CanonicalExportModel` (ADR §4 shape), `buildCanonicalExportModel`, `serializeCanonicalExportModel`, `canonicalModelSha256`, `CANONICAL_EXPORT_MODEL_VERSION`; a golden fixture `apps/lawbar-desktop/tests/fixtures/a10-golden-canonical-export.json` locks one representative model's serialization + sha256; tests PROVE: (a) GOLDEN — building from the fixture input yields a serialization + sha256 byte-identical to the locked fixture; (b) DETERMINISM — rebuild AND input reordering yield the identical hash; (c) VALID — clean citations carry verbatim 卷X页Y (citations[] + clean rows); (d) DEGRADED stays visible/reviewable — flagged links appear as rows with their flag, never dropped (rows count == input count); (e) AUTHORITY — every row has EXACTLY one of {citationText} XOR {flag} and NO `internalHref`/`href` field; (f) NO-HREF-LEAK — the serialized model contains no `lawbar:`/`http(s):`/`javascript:`/`data:`/`file:`/`mailto:`/`vbscript:`/`internalHref`/`href` substring; (g) UNSAFE-TARGET — a hostile documentId never enters the model (no traversal payload / scheme in the serialization; row stays text-only); (h) LOGICAL-NOT-RAW — the hash is SHA-256 of the logical serialization (64-hex), never raw artifact bytes; (i) REGRESSION — A10-T1 + A10-T2 behavior unchanged (their tests still pass; the contract mappers still drive the model). NO live-pipeline/IPC wiring; NO native/JS-shim; NO A8; NO forms; NO services/schema/renderer/electron/dependency change; NO edit to a10CitationContract.ts/a10HyperlinkDegradation.ts; `CURRENT_SCHEMA_VERSION` unchanged; `dev-memo/run/evidence/**` untouched; no HMAC/custody key; no confidential fixture; `check-contract-integrity` exits 0; broker review-plan READY + audit no Critical/High/Medium + verify ALL CLOSED; committed diff confined to Allowed files (package.json diff = exactly the one-line test registration).
Risk flags: HIGH-RISK Evidence-invariant lane (A10 court-fileable export reproducibility; the CanonicalExportModel is the A8.6 reproducibility target) — but a STANDALONE apps-layer model + serializer + golden fixture + tests only; NO services/schema/native/UI/IPC change, NO live-pipeline wiring, NO native harness, NO A8. Load-bearing care points: (a) DETERMINISM — stable key/row order + NFC + no timestamps/machine-paths/renderer-metadata so serialization is byte-identical across runs/machines; (b) LOGICAL-NOT-RAW — hash the canonical serialization, NEVER raw .docx/PDF bytes; (c) AUTHORITY — rows carry text-or-flag; internalHref is EXCLUDED (never a court-fileable value, never in the hash); (d) NEVER-DROPPED + total — every input -> exactly one row; (e) SINGLE-SOURCE — reuse the A10-T1 卷X页Y verbatim (do NOT re-render); (f) do NOT wire the live pipeline, edit services/linkHandlers/A10-T1/A10-T2, build the native harness, or touch A8/forms/schema/custody/confidential-fixtures. If any cannot hold, STOP and report. No push/merge/branch-delete.
Depends on: none
Commit boundary: governance (mark-reviewed + govern) run STANDALONE before the commit (never bundled with git commit). ONE implementation commit — apps/lawbar-desktop/src/caseBox/export/a10CanonicalExportModel.ts + apps/lawbar-desktop/tests/a10-canonical-export.unit.test.mjs + apps/lawbar-desktop/tests/fixtures/a10-golden-canonical-export.json + apps/lawbar-desktop/package.json (one-line test registration) + governance artifacts (queue.md/.linted/.reviewed/.governed + reviews/queue-review-113.md), staged by exact path; NO services/schema/native/renderer/electron/contract/dependency/tarball file; NO edit to a10CitationContract.ts/a10HyperlinkDegradation.ts/linkHandlers/dto-link; NO marker/evidence/key/report file; no push; no merge; no branch delete.
