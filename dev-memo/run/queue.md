## WI-PKG1-TEST1: test — refresh the packaged smoke to assert the current case-box shell (retire stale token-fixture expectations)
Type: TEST
Scope: Update the packaged-binary Playwright smoke `apps/lawbar-desktop/tests/smoke.packaged.electron.test.mjs` so its assertions match the CURRENT case-box product UI instead of the obsolete "token fixture" UI. The test became dormant-stale because `npm run dist` failed on the >4.2GB asar bloat (now fixed by the uncommitted WI-PKG1 packaging-config change), so it never ran to completion; its bodies still assert the retired token-fixture shell (window title "lawbar (token fixture)"; exactly 12 `article.panel[data-token=...]` panels; `button[data-mode='dark'|'light']` theme buttons), none of which the shipped app renders. The fix MIRRORS the already-passing dev Electron smoke `tests/smoke.electron.test.mjs` (the canonical current-UI spec): assert window title === "lawbar"; `main#app` mounts; the #/matters list screen renders `<h1>lawbar — case-box</h1>`; the empty-state marker `[data-test-id="list-empty"]` is present with its local-storage copy; the `button.list-new-btn` is visible. For the theme test, drive the flip the SAME way the dev smoke does — `app.evaluate(({ nativeTheme }) => { nativeTheme.themeSource = "dark"|"light"; })` then wait for `<html data-theme>` to toggle — instead of clicking the retired `button[data-mode]`. KEEP the packaged-binary harness intact: `findPackagedBinary()` (probes both dist/ and release/ output roots, host-arch priority), `launchPackaged()` from `_launch-with-pid-log.mjs`, `clearPref()`/userData hygiene, the CI wrapper-sentinel guard, and the "packaged .app exists + is executable" test. Rename the second test's title from "...12 panels render with data-token attrs" to "...renders case-box list shell". NO product runtime change, NO packaging-config change (the WI-PKG1 package.json fix is a SEPARATE uncommitted WI and is NOT part of this commit), NO other test file.
Source of truth: apps/lawbar-desktop/tests/smoke.electron.test.mjs (the dev Electron smoke — the canonical current case-box-shell assertions: title "lawbar", main#app, h1 "lawbar — case-box", [data-test-id="list-empty"], button.list-new-btn, nativeTheme themeSource flip → <html data-theme>); the failing packaged run (smoke.packaged.electron.test.mjs:134 asserted "lawbar (token fixture)" while the actual title is "lawbar"; the 12-panel + button[data-mode] assertions reference a UI the product replaced with the case-box shell); apps/lawbar-desktop/tests/_launch-with-pid-log.mjs (launchPackaged harness); dev-memo/plan-packaging-smoke-00.md + plan-packaged-probe-verification-00.md (the packaged-smoke design + the Class-B shutdown caveat, unchanged); the case-box UI in renderer/screens/listMatters.ts (h1 + list-empty + list-new-btn render).
Allowed files: apps/lawbar-desktop/tests/smoke.packaged.electron.test.mjs
Forbidden files: apps/lawbar-desktop/package.json (the WI-PKG1 packaging fix is a separate WI — do NOT stage it in this commit), apps/lawbar-desktop/tests/smoke.electron.test.mjs (the dev smoke is the reference, not edited), apps/lawbar-desktop/tests/_launch-with-pid-log.mjs (harness reused, not edited), any other tests/** file, apps/lawbar-desktop/src/**, apps/lawbar-desktop/electron/**, apps/lawbar-desktop/renderer/** (no product runtime change), services/**, docs/contracts/**, any packaging-config change, any product behavior change, WI-DPE2-FIX2
Gates: broker review-plan via runner on the queue (TEST WI matching shipped UI — low-risk but governed); npm --prefix apps/lawbar-desktop run test:packaged (the updated packaged smoke MUST pass against the already-built release/ app: window opens, title "lawbar", case-box shell + list-empty + list-new-btn render, theme flip toggles <html data-theme>); npm --prefix apps/lawbar-desktop run test:smoke (dev smoke still green); npm --prefix apps/lawbar-desktop test (full suite still green; the new test file is not added to the default `test` script — packaged smoke runs only via the wrapper, unchanged); loc-guardian scan before + after (single test file, well under the 1200 raw test-fail line); broker audit + verify on the impl scope; batch-end Layer-B closeout.
Acceptance criteria: smoke.packaged.electron.test.mjs no longer asserts the string "lawbar (token fixture)", no longer asserts "article.panel"/"data-token"/"12 panels", and no longer clicks "button[data-mode"; instead it asserts title equals "lawbar", waits for "main#app", asserts the h1 text equals "lawbar — case-box", asserts the [data-test-id=\"list-empty\"] marker is present, and asserts button.list-new-btn is visible, mirroring tests/smoke.electron.test.mjs; the theme test drives nativeTheme.themeSource via app.evaluate and asserts <html data-theme> toggles dark then light; the packaged-binary harness (findPackagedBinary/launchPackaged/clearPref/CI-sentinel/"exists+executable" test) is retained unchanged; `npm --prefix apps/lawbar-desktop run test:packaged` PASSES (all packaged smoke tests green against the existing release/ build); `npm --prefix apps/lawbar-desktop run test:smoke` PASSES; `npm --prefix apps/lawbar-desktop test` PASSES (no regression); the committed diff touches ONLY apps/lawbar-desktop/tests/smoke.packaged.electron.test.mjs (plus governance metadata); broker audit returns no Critical/High/Medium and verify confirms ALL CLOSED.
Risk flags: LOW-RISK — test-only fixture sync to the already-shipped case-box UI, mirroring the passing dev smoke; no product runtime, packaging, dependency, or security change. R1: the packaged theme flip must use the same nativeTheme.themeSource path as the dev smoke (the retired button[data-mode] does not exist in the case-box shell) — proven by test:packaged going green. R2: test:packaged validates against the EXISTING release/ build produced by the uncommitted WI-PKG1 fix (the wrapper does NOT rebuild); if release/ is absent the gate cannot run — rebuild via the WI-PKG1 dist is a prerequisite, not part of this commit. R3: keep the packaged-binary Class-B late-shutdown caveat + the CI wrapper sentinel intact (do not weaken crash detection). Touching any file beyond smoke.packaged.electron.test.mjs is a hard stop.
Depends on: none
Commit boundary: one local commit for the WI-PKG1-TEST1 packaged-smoke refresh (apps/lawbar-desktop/tests/smoke.packaged.electron.test.mjs + the governance-metadata refresh), staged by exact path; the WI-PKG1 package.json packaging fix is NOT included (separate WI, committed afterward)
