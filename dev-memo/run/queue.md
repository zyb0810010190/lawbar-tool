## WI-A3-DB-00: A3 persistence substrate decision (design-only; NOT A0.7-gated)
Type: PLAN
A0.7-classification: A3-DB-00 is design-only and intentionally NOT A0.7-gated. This docs-only ADR DECIDES the storage substrate for the future A3 schema implementation; it adds no dependency, no migration, no schema, and changes no gate/marker behavior, so it needs no live provenance-valid A0.7 marker. Therefore this WI carries NO `Requires-A07:` line. The FUTURE A3-T1-IMPL schema WI MUST declare `Requires-A07: yes` and pass the A0.7 hard gate (custody mode 9b). review-plan must confirm this classification; if review-plan says this docs-only WI must be A0.7-gated, STOP and report.
Scope: Decide the persistence substrate for the A3 anchor/link schema implementation, as an ADR + a dev-memo sequencing note, BEFORE any schema/migration code. Answer: (1) what local-persistence storage already exists in the repo; (2) can A3-T1 schema reuse an existing dependency/path; (3) if a new dependency is needed, which + why; (4) whether SQLCipher/encryption-at-rest is required now or deferred; (5) whether schema-only impl can proceed WITHOUT encryption given local-only storage + no production evidence ingested yet; (6) migration file location + naming convention; (7) rollback / forward-only policy; (8) how schema tests run headlessly; (9) how A0.7 gating applies to the impl WI; (10) the exact next implementation WI authorized. Design/ADR ONLY — no dependency, no package change, no migration, no schema implementation, no app/UI/product behavior, no marker/key/gate change. Output: docs/adr/ADR-evidence-a3-persistence-substrate.md (A3-DB-00) + dev-memo/plan-batch-casebox-evidence-a3-db-substrate-00.md; an optional narrow cross-reference line in docs/adr/ADR-evidence-a3-schema-persistence-contract.md only if review-plan recommends it.
Decision constraints: prefer NO new dependency if the repo already has a suitable local SQLite layer; do NOT introduce cloud/auth/network/external-DB/sync; do NOT weaken confidentiality assumptions; do NOT invent production key management for evidence encryption in this lane; if encryption-at-rest is unresolved, record it as a HARD STOP before production evidence ingestion (not a hidden assumption); keep A3-T1 implementation schema-only (no resolver/status/product/UI).
Grounded facts (verified read-only, to record in the ADR): services/case-box-persistence + services/ocr-persistence already use better-sqlite3 ^12.9.0 (native binding; Node 22.x/24.x engines pin; pretest abi-smoke); migrations are versioned-DDL arrays in src/sqlite/schema.ts (CURRENT_SCHEMA_VERSION; DDL_STATEMENTS_Vn; idempotent applySchema(db); schema_version table; forward-only, no down-migrations; single BEGIN/COMMIT per version); case-box-persistence is at V8 and already manages case_box_documents (+ supersedes_document_id replacement) / facts / evidence_items / OCR links / audit chain — the natural home for Anchor/Link (DocumentPage/DocumentPageGeometry are case-box domain); headless tests run node --test against :memory: (+ temp-file via mkdtempSync); the Node services are PLAINTEXT SQLite today (WAL/synchronous/foreign_keys pragmas only, no cipher/key); GRDB/SQLCipher are the macOS app's eventual concern per the handover, NOT the Node services.
Required ADR conclusions: select the persistence substrate (recommend: REUSE services/case-box-persistence's existing better-sqlite3 SQLite layer — NO new dependency — adding the A3 Anchor/Link tables as schema V9 via the existing versioned-DDL/applySchema convention); record the encryption-at-rest decision (recommend: DEFER — Node services are plaintext today; A3-T1 schema-only proceeds without encryption for local-only synthetic M0 data with no production evidence ingested + no network; encryption-at-rest, the macOS GRDB/SQLCipher store, and production key management are a HARD STOP before production evidence ingestion, recorded not hidden); record the migration convention (versioned-DDL array in src/sqlite/schema.ts, bump CURRENT_SCHEMA_VERSION, idempotent applySchema, schema_version row, forward-only / no down-migrations; rollback via git revert + a new forward version, never a destructive reset); record headless test approach (:memory: + temp-file, pretest abi-smoke); record that A3-T1-IMPL is A0.7-gated (Requires-A07: yes, custody 9b) and is a MIGRATION (schema-version bump) but NOT a NEW dependency under the reuse decision; name the exact next WI (WI-A3-T1-IMPL: schema-only V9 in case-box-persistence, A0.7-gated).
Source of truth: docs/adr/ADR-evidence-a3-schema-persistence-contract.md (A3-SCHEMA-00 — the engine-neutral schema contract this substrate hosts; §2 storage type 12-dp string, §5 storage-engine deferred); services/case-box-persistence/{package.json,src/sqlite/schema.ts,src/sqlite/openSqliteCaseBoxPersistence.ts,scripts/abi-smoke.mjs,tests/hardening-schema.test.mjs} (the existing SQLite layer; READ-ONLY reference, no change); services/ocr-persistence/src/sqlite/schema.ts (same convention); Evidence-Genie-M0-Developer-Handover.md (root intake; READ-ONLY) — GRDB/SQLCipher for the macOS app, manual-truth M0, synthetic fixtures only; docs/adr/ADR-evidence-a07-key-custody-operating-model.md (A07-KEY-00); .claude/rules/client-local-first.md (local-first, no cloud/sync default-on, tenant_id forward-compat only); .claude/rules/security-boundary.md (no silent confidentiality weakening); .claude/rules/autonomy.md (new dependency + migration + cloud/auth + key custody are hard stops).
Allowed files: docs/adr/ADR-evidence-a3-persistence-substrate.md, dev-memo/plan-batch-casebox-evidence-a3-db-substrate-00.md, dev-memo/** (queue/governance/review/study/plan artifacts), docs/adr/ADR-evidence-a3-schema-persistence-contract.md ONLY a narrow cross-reference line if review-plan explicitly recommends it
Forbidden files: services/** (case-box-persistence + ocr-persistence + all — NO code/schema/package change; READ-ONLY reference only), apps/**, native/**, docs/contracts/**, .claude/**, .github/**, AGENTS.md, CLAUDE.md, GEMINI.md, scripts/workflow/** (NO gate/marker change), any package.json / package-lock / dependency manifest, any migration files, committed dev-memo/run/evidence/** marker/ledger/key/evidence-run files, committed keys, Evidence-Genie-M0-Developer-Handover.md (root intake), xiaolai-dev-workflow-study.md (root intake), dev-memo/run/override-reason.md (untracked residue — never staged); NO dependency/package change, NO migration/schema code, NO SQLCipher/encryption/key-management implementation, NO cloud/auth/network/external-DB/sync, NO Evidence UI/product, NO marker/key/gate change, NO Requires-A07 on this WI
Gates: broker /cc-suite:review-plan REQUIRED before authoring (confirm design-only / NOT-A0.7-gated; the decision governs eventual HIGH-RISK persistence/migration work; confirm the reuse-existing-SQLite recommendation is sound + the encryption-deferral is correctly framed as a hard stop before production evidence, not a hidden assumption); if review-plan returns NEEDS-FIX, STOP and report; if review-plan says this docs-only WI must be A0.7-gated, STOP and report; if review-plan returns READY but asks for implementation, migration, a dependency addition, key-management implementation, UI, or marker/key/gate changes, STOP and ask; broker /cc-suite:audit + /cc-suite:verify on the design packet after authoring; scripts/workflow/check-contract-integrity.sh exits 0; scripts/workflow/check-gates.sh returns GATES OK (queue has no Requires-A07, no markers -> A0.7 gate not required -> pass); npm --prefix apps/lawbar-desktop test 610/610; npm --prefix native/evidence-core test 38/38; no dependency/package/migration/code change; no services/apps/native/scripts/.claude/.github change; no marker/writer/validator/gate change; no committed marker/ledger/key/evidence-run; root intake untouched
Acceptance criteria: docs/adr/ADR-evidence-a3-persistence-substrate.md EXISTS and answers all 10 required questions and records the conclusions (selected substrate = reuse case-box-persistence better-sqlite3, NO new dependency, A3 tables as schema V9 via the existing versioned-DDL convention; encryption-at-rest DEFERRED with an explicit HARD STOP before production evidence ingestion; migration convention = versioned-DDL/applySchema/schema_version, forward-only/no down-migrations, rollback via git revert + forward version; headless tests via :memory: + temp-file + pretest abi-smoke; A3-T1-IMPL is A0.7-gated + a migration but NOT a new dependency; the next WI = WI-A3-T1-IMPL schema-only V9); dev-memo/plan-batch-casebox-evidence-a3-db-substrate-00.md records the next-WI boundary + the unresolved hard stops (encryption-at-rest, macOS GRDB/SQLCipher store, production key management — all before production evidence ingestion); the confidentiality/local-first constraints are preserved (no cloud/auth/network/sync; no invented key management); scripts/workflow/check-contract-integrity.sh exits 0; scripts/workflow/check-gates.sh returns GATES OK; npm --prefix apps/lawbar-desktop test 610/610; npm --prefix native/evidence-core test 38/38; broker review-plan READY (classification confirmed) + audit reports no Critical/High/Medium + verify ALL CLOSED; the committed design diff is confined to docs/adr/** + dev-memo/** (plus an optional A3-SCHEMA-00 cross-reference line only if review-plan approved)
Risk flags: DESIGN-ONLY (Type PLAN). Explicitly NOT A0.7-gated (no Requires-A07) — a substrate DECISION ADR adds no dependency/migration/code and needs no live marker. The decision INFLUENCES eventual HIGH-RISK persistence + (deferred) encryption/key-management work, which are autonomy hard stops handled in their own authorizations; this lane invents NO key management and weakens NO confidentiality assumption. The reuse-existing-better-sqlite3 recommendation means the future A3-T1-IMPL is a MIGRATION (schema-version bump) but NOT a new-dependency hard stop. No code, no dependency, no migration, no UI, no marker/key/gate change. broker review-plan + audit + verify REQUIRED. Encryption-at-rest is recorded as a HARD STOP before production evidence ingestion, never assumed. No push, no merge.
Depends on: none
Commit boundary: governance commit separate (dev-memo/run/queue.md + queue.linted/reviewed/governed + dev-memo/run/reviews/queue-review-072.md); one design commit -- docs/adr/ADR-evidence-a3-persistence-substrate.md + dev-memo/plan-batch-casebox-evidence-a3-db-substrate-00.md (+ an optional A3-SCHEMA-00 cross-reference line only if review-plan approved), staged by exact path; no push; no merge
