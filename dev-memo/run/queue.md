## WI-EVIDENCE-A8-DESIGN-00: snapshot integrity & confidentiality architecture gate (Type: PLAN)
Type: PLAN
Scope: DESIGN/ADR ONLY — author ONE Evidence-Genie A8 architecture decision record, `docs/adr/ADR-evidence-a8-snapshot-integrity-confidentiality.md`. NO implementation, NO crypto/export/freeze stubs, NO source/schema/native/contract/test change. A8 ("Snapshot integrity & confidentiality") is the next CRITICAL-PATH hard-invariant architecture gate after A3 (the four hard invariants are A1/A3/A8/A10 per the TRACKED handover `docs/reference/evidence-genie-m0-developer-handover.md`; A5/A6/A9 are deliberately off-critical-path epic-level forms/search/audit work and are NOT this lane). The ADR consolidates the handover's A8 ticket-depth spec (A8.1 autosave/crash-recovery, A8.2 freeze-service, A8.3 manifest+seal, A8.4 export-bundle, A8.5 two-key Argon2id+AES-256-GCM, A8.6 restore-and-reproduce, A8.7 snapshot-versioning; order T2→T3→T5→T4→T6→T7→T1) into a decided architecture, and MODELS its prerequisites as explicit sequencing + stop-points rather than assuming they exist. Grounding facts (verified read-only on main): A8 surface = ZERO on main (no ADR/code); A1 citation identity = PARTIAL (persistence built — `case_box_document_pages` V9 + `buildExportCitations` + ambiguity refusal + byte-stable 卷X页Y; A1-T6 native citation-stability gate `not_implemented`); A10 court-fileable export / `CanonicalExportModel` = SPEC-ONLY (no canonical-model type; only A3 precursor `ExportCitationResult`); A0.7 native `renderer-conformance` = `not_implemented` (FAIL, NOT green); A0.7 marker key-custody (env-only HMAC) exists. The ADR MUST NOT invent A10's `CanonicalExportModel`; A8.6's "reproduce byte-identical canonical model" acceptance is SEQUENCED behind A10 design/build (A10-T6), and A8 IMPLEMENTATION is gated behind: A0.7 green, encryption-at-rest user authorization (A3-DB-00 §5 HARD STOP), and the A10 canonical model. Preserve all A0.7/A1/A3 invariants (esp. INV-A3-1..A3-10 anchor/citation-from-DocumentPage, geometry-version pinning, manifest/seal anti-circularity — manifest hashes the deterministic LOGICAL payload, NOT the encrypted DB, with a separate SnapshotSeal).
Source of truth: docs/reference/evidence-genie-m0-developer-handover.md (TRACKED authoritative handover; A8 invariant + A8.1-A8.7 tickets + verify harnesses + order T2->T3->T5->T4->T6->T7->T1 + "do not migrate frozen snapshots in place"; the untracked root-copy `Evidence-Genie-M0-Developer-Handover.md` is NOT cited as authoritative committed source); docs/adr/ADR-evidence-a07-{renderer-conformance-gate,key-custody-operating-model,marker-provenance}.md (A0.7 + key custody A8 composes with); docs/adr/ADR-evidence-a3-{anchor-link-contract,persistence-substrate,export-degradation,durable-unlink-schema}.md (A3 invariants A8 freeze must preserve; A3-DB-00 §5 encryption-at-rest HARD STOP); services/case-box-persistence/src/sqlite/{schema.ts,exportCitationQueries.ts} (A1 built state); native/evidence-core/lib/commands.mjs (gate shim: snapshot-verify/golden-export not_implemented); .claude/rules/{evidence-genie,cc-suite,autonomy,execution-discipline,staging-hygiene,security-boundary,client-local-first}.md
Allowed files: docs/adr/ADR-evidence-a8-snapshot-integrity-confidentiality.md, dev-memo/run/queue.md, dev-memo/run/queue.linted, dev-memo/run/queue.reviewed, dev-memo/run/queue.governed, dev-memo/run/reviews/queue-review-106.md
Forbidden files: services/** (NO source/schema/contract change; NO snapshot/freeze/crypto code), docs/contracts/** (NO contract/schema change), services/case-box-persistence/src/sqlite/schema.ts (NO schema/CURRENT_SCHEMA_VERSION change), native/** (NO native/Swift crypto/harness code or stub), apps/** (NO renderer/electron/UI/package/tarball/lockfile change), scripts/** + .claude/** + .github/** + migrations/**, AGENTS.md + CLAUDE.md + GEMINI.md, docs/adr/ADR-evidence-a07-** + docs/adr/ADR-evidence-a3-** (READ-ONLY predecessors — do NOT edit), docs/adr/ADR-evidence-a5-** (NO A5 work — out of scope), dev-memo/design/** (NO separate design artifact in this lane — single ADR), dev-memo/run/evidence/** (marker/ledger/key — never staged), dev-memo/run/reports/** + dev-memo/run/override-reason.md + dev-memo/run/autonomous-next.md (untracked run-state — never staged/edited), Evidence-Genie-M0-Developer-Handover.md + xiaolai-dev-workflow-study.md (root intake — READ-ONLY); NO implementation/crypto/export/freeze stub; NO A5 scope; NO weakening of A0.7/A1/A3 invariants; NO push/merge/branch-delete
Gates: broker /cc-suite:review-plan REQUIRED before authoring the ADR — confirm: A8 is correctly framed as the next critical-path hard-invariant gate after A3 (NOT A5); the A1-PARTIAL / A10-SPEC-ONLY / A0.7-not-green dependencies are modeled as explicit sequencing + stop-points (NOT assumed built, NOT invented); the ADR does not weaken A0.7/A1/A3 invariants; the design needs no schema/source/native change in THIS lane; A8.6 reproduce-acceptance is sequenced behind A10. STOP-points: (1) if review-plan finds A8 cannot be scoped without unresolved A1/A10 implementation facts, STOP; (2) if review-plan says a prerequisite A1/A10 discovery/design lane must come first, STOP and report; (3) if cloud/auth/PIPL/China-filing/local-first/product-ownership decisions are required before architecture can be safely written, STOP; (4) if a separate UI design artifact is required, STOP; (5) if any implementation/source/schema/package/native file must change, STOP; (6) if review-plan says this must be split into multiple design WIs, STOP. If review-plan NEEDS-FIX on wording/scope, fix-forward; if a blocker, STOP. After authoring: `scripts/workflow/check-contract-integrity.sh` exits 0; broker /cc-suite:audit + /cc-suite:verify (no Critical/High/Medium open before commit). No A0.7 custody (docs/ADR-only; no electron/geometry/marker/product code).
Acceptance criteria: `docs/adr/ADR-evidence-a8-snapshot-integrity-confidentiality.md` MUST exist and MUST contain sections that answer: what A8 is; why A8 is the next critical-path gate after A3 (and why NOT A5); what A8 must not do; freeze/snapshot semantics (A8.2 Online-Backup-API default, VACUUM-INTO compaction-only, atomic, audited); manifest/seal semantics (SHA-256 over deterministic LOGICAL payload + role-tagged files + citation map + anchor set + export previews + optimized renditions; separate SnapshotSeal; anti-circularity — manifest MUST NOT hash the encrypted DB that holds the seal); confidentiality + key-custody model (LocalDatabaseKey Keychain/Secure-Enclave vs BundlePassphrase user-held/never-stored/zeroized; pinned Argon2id 19MiB/2/1 floor; AES-256-GCM; forgotten-passphrase = unrecoverable) and how it composes with the A0.7 marker HMAC custody; restore/reproduce semantics (decrypt→verify-manifest→read-only; reproduce byte-identical canonical model + citations + anchors; newer-app opens read-only, no in-place migration); explicit interaction/sequencing with A0.7, A1, A3, A10; whether schema changes are needed (and that none happen in THIS lane); whether native/Swift crypto work is needed; whether renderer UI is needed; whether cloud/public-deployment/auth-provider/China-filing/PIPL/local-first are blockers or explicitly out-of-scope; the implementation WI sequence (mapping A8.1-A8.7, honoring order T2→T3→T5→T4→T6→T7→T1); acceptance tests + verify harnesses per WI (freeze-service, snapshot-verify CI-gate, two-key-crypto CI-gate, export-bundle, restore-and-reproduce, snapshot-versioning, autosave-recovery); per-lane stop-points (incl. A0.7-green precondition, encryption-at-rest user authorization, A10 CanonicalExportModel prerequisite for A8.6). The ADR MUST explicitly record A1=PARTIAL, A10=SPEC-ONLY, A0.7=not-green and sequence A8 implementation behind them — it MUST NOT invent a CanonicalExportModel nor claim A0.7 green. NO schema/source/native/contract/test/UI file changed; `CURRENT_SCHEMA_VERSION` still equals 12; `scripts/workflow/check-contract-integrity.sh` passes (exit 0); broker review-plan returns READY + audit returns no Critical/High/Medium + verify returns ALL CLOSED; committed diff contained within Allowed files; no A0.7/A1/A3 invariant weakened.
Risk flags: HIGH-RISK court-facing Evidence design lane (snapshot integrity, confidentiality, encryption-at-rest, key custody) — but DOCS/ADR-ONLY (no code, no crypto impl, no schema). Load-bearing care points: (a) frame A8 as the next CRITICAL-PATH gate after A3, NOT A5 (A5 is off-path forms work; do NOT scope A5); (b) model A1=PARTIAL / A10=SPEC-ONLY / A0.7=not-green as explicit sequencing + stop-points — do NOT assume built, do NOT invent A10's CanonicalExportModel; (c) preserve manifest/seal anti-circularity (manifest hashes logical payload, not the encrypted DB) and all INV-A3-1..A3-10 + A1 citation-from-DocumentPage; (d) A8.5 encryption-at-rest design records the approach but IMPLEMENTATION stays behind the A3-DB-00 §5 HARD STOP + explicit user authorization; (e) A8 implementation gated behind A0.7-green (Evidence invariant: nothing builds on A0.7 until green). If the design would need any source/schema/native/crypto change, or product/cloud/auth/PIPL/local-first decisions, STOP. No push/merge/branch-delete.
Depends on: none
Commit boundary: governance (mark-reviewed + govern) run STANDALONE before the commit (never bundled with git commit). ONE design commit — docs/adr/ADR-evidence-a8-snapshot-integrity-confidentiality.md + governance artifacts (queue.md/.linted/.reviewed/.governed + reviews/queue-review-106.md), staged by exact path; NO source/schema/native/contract/test/UI/package file; NO marker/evidence/key/report file; no push; no merge; no branch delete.
