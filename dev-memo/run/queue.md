## WI-A3-UNLINK-RESOLVE: resolver/export durable-unlink marker-awareness (A0.7-gated; custody 9b)
Type: IMPL
Requires-A07: yes
A07 custody mode: 9b. Human runs marker mint + A0.7-gated check-gates directly; agent does not receive the HMAC key. Implementation commit is blocked until human-run gated verification reports PASS.
A0.7-classification: WI-A3-UNLINK-RESOLVE changes the resolver (resolveLinkStatuses) + export builder (buildExportCitations) behavior over court-facing evidence link status + citation presentation — it affects legal-record presentation + unlink semantics, so it is A0.7-gated (the same files WI-A3-T5-RESOLVE + WI-A3-EXPORT-T1 gated under custody 9b). It carries `Requires-A07: yes` and runs under A07-KEY-00 custody mode 9b: the human mints the local marker + runs the A0.7-gated `check-gates.sh` with the HMAC key; the agent never receives the key; the implementation commit is blocked until the human reports gated PASS. If review-plan says this WI must NOT be A0.7-gated, STOP and report.
Scope: Implement READ/EXPORT marker-awareness ONLY for the V12 durable-unlink marker (case_box_links.unlinked_at / unlink_reason, already shipped). Make resolveLinkStatuses treat a row with unlinked_at IS NOT NULL as inactive/unlinked — a HIGHEST-precedence non-clean status that is NEVER recomputed to valid (the marker is durable; A3-UNLINK-SCHEMA-00 §5). Make buildExportCitations read the marker (not only status) and flag an unlinked link distinctly — a deterministic non-clean exportFlag distinguishable from structural broken, never a clean citation, never silently dropped (A10 no-drop; A3-UNLINK-SCHEMA-00 §6). Preserve historical row durability (NO row deletion). NO schema change, NO CURRENT_SCHEMA_VERSION bump, NO FK/cascade/index/CHECK, NO unlink/relink MUTATING operation (writing/clearing the marker is the separate WI-A3-UNLINK-T1), NO dependency, NO marker/key/gate change. Do NOT change LinkStatus (valid|needs_review|broken) unless review-plan proves it necessary for read-DTO semantics (STOP and ask if so). Do NOT touch UI unless resolver/export tests prove an existing UI boundary must be adjusted — STOP before any UI scope expansion.
Required decisions to record: (1) RESOLVER MARKER RUNG — add a HIGHEST-precedence branch to the resolver's status computation: when case_box_links.unlinked_at IS NOT NULL, the link resolves to a non-clean status REGARDLESS of structural inputs (anchor/page/geometry/supersession) and is NEVER recomputed to valid. PROPOSED status value: `broken` (an explicit unlink severs the link target, like a missing target — the highest non-clean rung). Flag to review-plan: broken vs needs_review for an explicitly-unlinked link; if review-plan prefers needs_review, record that. The marker is DURABLE: a re-run keeps the unlinked link non-clean (idempotent). (2) EXPORT MARKER-AWARENESS — buildExportCitations reads case_box_links.unlinked_at (add it to the link SELECT) and, when set, emits a DISTINCT non-clean exportFlag (best-effort link/source identity; NO clean citation; A10 no-drop — the link is NOT dropped, it gets exactly one object). PROPOSED: add a NARROW additive ExportCitationFlag member `UNLINKED` (distinct from structural BROKEN), mirroring the anchor_referenced additive-code precedent; update the byFlag summary record. Flag to review-plan: a new UNLINKED flag vs reuse BROKEN with a distinguishing field; if review-plan prefers reuse, record that. The marker check is the HIGHEST-precedence export branch (it overrides the status-derived branches). (3) PRECEDENCE — an unlinked link's marker takes precedence over structural broken/needs_review in BOTH the resolver (status) and the export (flag); the marker is the durable reason. (4) DURABILITY / NO DELETE — neither the resolver nor the export deletes or mutates rows except the resolver's existing case_box_links.status write (now also writing the unlinked->non-clean status); the marker columns themselves are NOT written by this WI (that is WI-A3-UNLINK-T1). Historical rows persist. (5) LEGACY COMPAT — rows with unlinked_at IS NULL (all existing/legacy rows) behave EXACTLY as before (active; the resolver ladder + export flags unchanged for them). (6) NO SCHEMA — no schema/migration/version change; CURRENT_SCHEMA_VERSION stays 12; no FK/cascade/index/CHECK. (7) DETERMINISM + IDEMPOTENCE — same inputs -> same statuses/flags; re-running the resolver/export on an unlinked row writes no spurious change (the unlinked->broken status is stable). (8) LINKSTATUS UNCHANGED — the LinkStatus enum (valid|needs_review|broken) is NOT extended; the unlinked distinction lives in the export FLAG + the durable marker, not in LinkStatus (unless review-plan proves a read-DTO necessity -> STOP and ask). (9) AUDIT — read/export marker-awareness is non-mutating (no new audit events; the unlink OPERATION's audit is WI-A3-UNLINK-T1). (10) A0.7 — A0.7-gated (custody 9b).
Source of truth: docs/adr/ADR-evidence-a3-durable-unlink-schema.md (A3-UNLINK-SCHEMA-00 §5 resolver respects the marker / no recompute to valid; §6 export reads the marker as a hard clean-export override, distinguishable from structural broken; §9 terminal-until-relink); docs/adr/ADR-evidence-a3-unlink-break-link-workflow.md (A3-UNLINK-00 — explicit unlink is distinct from structural broken); docs/adr/ADR-evidence-a3-resolver-status-transitions.md (A3-RESOLVE-00 — the ladder this WI prepends the marker rung to); docs/adr/ADR-evidence-a3-export-degradation.md (A3-EXPORT-00 — A10 no-drop / degraded non-clean export); services/case-box-persistence/src/sqlite/schema.ts (READ-ONLY — V12 case_box_links.unlinked_at/unlink_reason; NO edit); services/case-box-persistence/src/sqlite/linkStatusResolverQueries.ts + exportCitationQueries.ts (the two files edited — the resolver COMPUTE_SQL ladder + the export derivation); docs/adr/ADR-evidence-a07-key-custody-operating-model.md (A07-KEY-00 — custody 9b); .claude/rules/autonomy.md + .claude/rules/cc-suite.md + .claude/rules/loc-guardian.md (user-authorized; A0.7-gated; broker required).
Allowed files: services/case-box-persistence/src/sqlite/linkStatusResolverQueries.ts (add the highest-precedence unlinked_at marker rung to COMPUTE_SQL; no recompute-to-valid for a marked link), services/case-box-persistence/src/sqlite/exportCitationQueries.ts (read unlinked_at in the link SELECT; emit a distinct non-clean exportFlag for an unlinked link; the ExportCitationFlag union + byFlag summary gain the marker flag), services/case-box-persistence/tests/hardening-link-status-resolver.test.mjs (extend with unlinked-marker cases — already wired into the package test script), services/case-box-persistence/tests/hardening-export-citation.test.mjs (extend with unlinked-marker cases — already wired), dev-memo/run/queue.md, dev-memo/run/queue.linted, dev-memo/run/queue.reviewed, dev-memo/run/queue.governed, dev-memo/run/reviews/queue-review-087.md, dev-memo/study/** (a batch-audit study packet if a window closes), dev-memo/cc-suite-reliability-log.md (only if a TIMEOUT retry occurs), dev-memo/deferred-audit-findings.md (only if a finding is deferred)
Forbidden files: services/case-box-persistence/src/sqlite/schema.ts (NO schema/DDL/version change — V12 is already shipped; this WI reads the columns), services/case-box-persistence/src/sqlite/anchorDeleteGuardQueries.ts (NO guard change), services/case-box-persistence/src/errors.ts (NO error change unless review-plan proves a needed code — STOP and ask), services/case-box-persistence/src/index.ts (NO change unless a NEW exported type is introduced — the ExportCitationFlag union member is transparent through the existing export; if review-plan requires a new exported type, STOP and ask), any other services/** path not listed in Allowed files, migrations/**, apps/**, native/**, docs/contracts/**, .claude/**, .github/**, AGENTS.md, CLAUDE.md, GEMINI.md, scripts/workflow/** (NO gate/marker change), package-lock.json / any dependency manifest, services/case-box-persistence/package.json (NO change — both test files already wired), committed dev-memo/run/evidence/** marker/ledger/key/evidence-run files, committed keys, dev-memo/run/reports/** (untracked local run-state — never staged), dev-memo/run/override-reason.md (untracked residue — never staged), Evidence-Genie-M0-Developer-Handover.md (root intake — READ-ONLY, never moved/deleted), xiaolai-dev-workflow-study.md (root intake — READ-ONLY, never moved/deleted); NO schema/migration/version change, NO FK/cascade/index/CHECK, NO row deletion, NO unlink/relink MUTATING operation (no writing/clearing the marker), NO LinkStatus enum change (unless review-plan proves a read-DTO necessity), NO UI change, NO dependency, NO marker/key/gate change, NO audit-event emission
Gates: broker /cc-suite:review-plan REQUIRED before authoring code (confirm A0.7-gated classification; confirm the resolver highest-precedence unlinked_at rung + the chosen non-clean status (broken vs needs_review); confirm the export distinct unlinked flag (new UNLINKED member vs reuse BROKEN) + that export reads the marker not only status; confirm precedence (marker overrides structural) in both resolver + export; confirm legacy NULL-marker rows are unchanged; confirm no schema/version change, no row deletion, no mutating unlink op, no LinkStatus change, no UI); if review-plan returns NEEDS-FIX, STOP and report; if review-plan demands a schema/version change, a LinkStatus enum change, a mutating unlink op, a dependency, UI, or marker/key/gate changes, STOP and ask; broker /cc-suite:audit + /cc-suite:verify on the implementation diff AFTER the human-run gated PASS, BEFORE the implementation commit; A0.7 GATE (custody 9b) — human runs marker mint + A07_REQUIRED=1 scripts/workflow/check-gates.sh -> GATES OK with the HMAC key (agent never receives key); npm --prefix services/case-box-persistence test passes (existing resolver/export/V9-V12 + all case-box tests stay green AND the new unlinked-marker tests pass); scripts/workflow/check-contract-integrity.sh exits 0; npm --prefix apps/lawbar-desktop test 610/610; npm --prefix native/evidence-core test 38/38; loc-guardian fail thresholds respected (resolver/export source < 800 LOC each); no schema/version/dependency/UI change; CURRENT_SCHEMA_VERSION stays 12; no committed marker/ledger/key/evidence-run; root intake untouched
Acceptance criteria: services/case-box-persistence/src/sqlite/linkStatusResolverQueries.ts resolves a link with unlinked_at IS NOT NULL to a non-clean status (the proposed `broken`) as the HIGHEST-precedence rung, NEVER recomputing it to valid even when anchor/page/geometry are structurally valid + the document is not superseded; services/case-box-persistence/src/sqlite/exportCitationQueries.ts reads unlinked_at and emits a DISTINCT non-clean exportFlag for an unlinked link (the proposed `UNLINKED`), never a clean citation, never dropped (exactly one export object per in-scope link incl. the unlinked one), distinguishable from a structurally-broken link's BROKEN flag; the new tests in tests/hardening-link-status-resolver.test.mjs + tests/hardening-export-citation.test.mjs PASS for: an unlinked link with otherwise-valid structure -> non-clean status (not valid) + the distinct unlinked export flag (not clean); a re-run is idempotent (the unlinked status is stable, no spurious write); the unlinked row REMAINS in storage (not deleted) after resolve/export; a legacy row with unlinked_at IS NULL behaves exactly as before (valid->clean when structure resolves); unlinked precedence over structural broken/needs_review; NO schema version bump (CURRENT_SCHEMA_VERSION stays 12); NO new table/column/FK/cascade/index; NO LinkStatus enum change; NO row deletion; NO mutating unlink operation; existing resolver/export tests remain green; npm --prefix services/case-box-persistence test passes; scripts/workflow/check-contract-integrity.sh exits 0; npm --prefix apps/lawbar-desktop test 610/610; npm --prefix native/evidence-core test 38/38; human-run A0.7-gated check-gates returns GATES OK; broker review-plan READY (classification + status/flag choices confirmed) + audit reports no Critical/High/Medium + verify ALL CLOSED; the committed implementation diff is confined to the Allowed files (resolver + export + their two test files + dev-memo/**)
Risk flags: IMPL over court-facing link status + citation export — HIGH-RISK (Evidence A3 invariant surface; broker review-plan + audit + verify REQUIRED). A0.7-GATED (custody mode 9b — human runs marker mint + gated check-gates with the HMAC key; agent never receives the key; impl commit blocked until human reports gated PASS). READ/EXPORT marker-awareness ONLY: no schema/version change, no row deletion, no mutating unlink op, no LinkStatus enum change, no UI. The marker is a HIGHEST-precedence non-clean override in both resolver + export; export reads the marker (not only status) so an unlinked link is distinguishable from a structurally-broken one (A3-UNLINK-SCHEMA-00 §6). Legacy NULL-marker rows are unchanged. The change MUST NOT weaken any Evidence invariant (no unlinked link reads valid/exports clean; no row deleted; A10 no-drop; LinkStatus no-default; resolver determinism/idempotence) nor the A3-DB-00 encryption hard stop. NO dependency, NO marker/key/gate change. No push, no merge.
Depends on: none
Commit boundary: governance commit separate (dev-memo/run/queue.md + queue.linted + queue.reviewed + queue.governed + dev-memo/run/reviews/queue-review-087.md); ONE implementation commit -- services/case-box-persistence/src/sqlite/linkStatusResolverQueries.ts + services/case-box-persistence/src/sqlite/exportCitationQueries.ts + services/case-box-persistence/tests/hardening-link-status-resolver.test.mjs + services/case-box-persistence/tests/hardening-export-citation.test.mjs, staged by exact path; the implementation commit is gated on the human-reported A0.7 gated PASS + broker audit/verify clean; no push; no merge
