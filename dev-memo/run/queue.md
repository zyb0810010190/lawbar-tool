## WI-ENA2: PDFKit compile/import probe (second authorized implementation hard-stop)
Type: SCAFFOLD
Scope: Extend the existing SwiftPM skeleton under native/evidence-core-swift/ with a minimal PDFKit availability/compile probe. It may import PDFKit and expose a deterministic capability value (e.g. pdfKitAvailable == true) when built/run on macOS. Add/extend Swift tests to prove PDFKit import + availability on macOS. Update the macOS CI smoke job ONLY if a narrow test-command change is needed. This is a compile/import/capability probe ONLY: NO PDF loading or parsing, NO PDF fixtures, NO geometry extraction, NO renderer-conformance, NO A0.7 harness logic, NO A0.7 marker, NO marker provenance/HMAC, NO EVW5 hard hooks, NO Evidence UI/product behavior. Do NOT change the existing native/evidence-core JS shim behavior. Do NOT delete/move the root intake files. The README/status note states: PDFKit import proven; no geometry yet; no A0.7; no marker; no product behavior. CI stays macOS-only for the Swift/PDFKit job; the package still builds/tests locally on macOS.
Source of truth: dev-memo/plan-batch-casebox-evidence-native-a07-feasibility-00.md (ENA0 feasibility plan, §1-§3 toolchain/CI/boundary — PDFKit availability is the next feasibility probe); docs/adr/ADR-evidence-native-core-a07-feasibility.md (ENA-00); native/evidence-core-swift/** (the merged WI-ENA1 skeleton this WI extends); native/evidence-core/README.md (the JS shim contract, read-only reference); .github/workflows/ui-design-artifact.yml (the existing ubuntu check to preserve); .claude/rules/autonomy.md (this step is a user-authorized hard-stop); local Swift toolchain (Apple Swift 5.9.2, arm64-apple-macosx, available).
Allowed files: native/evidence-core-swift/** (extend Sources/**, Tests/**, README.md / a status note; Package.swift only if a target/test change requires it), .github/workflows/evidence-core-swift-smoke.yml (ONLY if a narrow test-command update is needed to run the new test; otherwise byte-unchanged), dev-memo/** (queue/governance/audit/study artifacts)
Forbidden files: apps/**, native/evidence-core/** (the JS shim — NO behavior change), services/**, docs/contracts/**, docs/product/**, .claude/hooks/**, .claude/settings.json, .claude/rules/**, AGENTS.md, CLAUDE.md, GEMINI.md, dev-memo/run/evidence/**, Evidence-Genie-M0-Developer-Handover.md (root), xiaolai-dev-workflow-study.md, package.json; NO PDF loading/parsing; NO PDF fixtures; NO geometry-extraction code; NO renderer-conformance; NO A0.7 harness logic; NO marker / HMAC / provenance code
Gates: broker /cc-suite:review-plan REQUIRED before impl (HIGH-RISK: introduces a PDFKit system-framework dependency on the macOS CI path — no self-review); if review-plan returns READY but scopes broader than a PDFKit compile/import probe, STOP and ask; broker /cc-suite:audit + /cc-suite:verify after impl (HIGH-RISK); swift build and swift test pass for native/evidence-core-swift (Swift available locally); the probe imports PDFKit and exposes only a deterministic capability value (no PDF loading/parsing/geometry); the macOS CI workflow runs only the SwiftPM smoke build/test and stays macOS-only; the existing ubuntu ui-design-artifact.yml is byte-unchanged; PDFKit import appears ONLY inside native/evidence-core-swift/** (grep); no PDF fixtures (grep); no geometry/renderer-conformance/marker/HMAC/provenance code (grep); nothing under dev-memo/run/evidence/ (grep); the native/evidence-core JS shim is byte-unchanged; README/status note states PDFKit-import-proven/no-geometry/no-A0.7/no-marker/no-product-behavior; scripts/workflow/check-contract-integrity.sh exits 0; npm --prefix apps/lawbar-desktop test passes (610/610); scripts/workflow/check-gates.sh returns GATES OK
Acceptance criteria: native/evidence-core-swift/ now contains a PDFKit availability/compile probe (a source symbol that imports PDFKit and yields a deterministic capability value such as pdfKitAvailable == true on macOS) plus a Swift test proving PDFKit import/availability; swift test --package-path native/evidence-core-swift passes (skeleton smoke + new PDFKit probe test); grep shows PDFKit import only under native/evidence-core-swift/** and no PDF-load/parse/geometry/renderer-conformance/marker/HMAC/provenance code and nothing under dev-memo/run/evidence/; the macOS CI workflow is either byte-unchanged or changed only by a narrow test-command line and remains macOS-only; .github/workflows/ui-design-artifact.yml is byte-unchanged; the native/evidence-core JS shim is byte-unchanged; the README/status note states PDFKit-import-proven/no-geometry/no-A0.7/no-marker/no-product-behavior; npm desktop test shows pass 610 fail 0; check-gates GATES OK; broker audit returns no Critical/High/Medium and verify confirms ALL CLOSED; the committed diff is confined to native/evidence-core-swift/** plus (at most) the one macOS smoke workflow file
Risk flags: HIGH-RISK -- introduces a PDFKit system-framework import on the Swift/macOS-CI path (a new framework dependency surface; autonomy hard-stop, EXPLICITLY user-authorized for this narrow PDFKit-compile/import-probe scope ONLY). broker review-plan REQUIRED + broker audit+verify after. Scope strictly limited to a compile/import/capability probe; NO PDF load/parse, NO geometry, NO renderer-conformance, NO A0.7 harness, NO marker/provenance/HMAC, NO hooks/UI/product behavior; the JS shim is untouched. Swift toolchain confirmed available locally (5.9.2).
Depends on: none (the ENA1 SwiftPM skeleton is already merged to main)
Commit boundary: one local commit -- native/evidence-core-swift/** plus (at most) the one macOS smoke workflow file, staged by exact path; no push
