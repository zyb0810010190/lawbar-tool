## WI-RELEASE-G16-BRIEF-READY-RECONCILIATION-VERIFY-00: define + govern the M0 gate-16 brief-READY + reconciliation-log verification (confirm the brief is READY, the reconciliation log has no unresolved M0-blocking entry, and accepted divergences incl. the T4 proof-model are documented) (Type: EVIDENCE, release-governance MEDIUM risk)

## Review packet (compact)
- Active plan summary: gate 16 of the M0 go-live readiness matrix (`docs/release/go-live-readiness-report.md` §1 row 16) is **PARTIAL (verify)** — evidence "brief `status: READY`; the T4 brief-vs-PRD reconciliation is now RESOLVED as accepted divergence (`docs/adr/ADR-forms-t4-proof-model-scope.md`)"; the outstanding item is "Inspect the brief's reconciliation log for any residual unresolved entries." This WI **governs** (does NOT execute) a FUTURE lane that runs that verification as a **read-only inspection** of the brief + reconciliation log + the T4 ADR and records the result as evidence. THIS authoring lane produces the governed queue WI only; it EDITS NO brief/ADR, changes NO source/test/config, clears NO gate, and decides NO user go-live hard-stop.
- Grounding note (verified 2026-07-07 — the exec lane MUST re-verify against the real files before relying, per `AGENTS.md` §"Authoring discipline"): `docs/product/project-requirements-brief.md` frontmatter line 2 reads `status: READY` (revision 5); its **§"Reconciliation log"** (≈§346) contains an **R-1/R-2/R-3 grouped resolution** ("Post-v1 SYNC reconciliation program") + a promotion-safety checklist ("Each RECONCILIATION-NEEDED entry has either been resolved …"). `docs/adr/ADR-forms-t4-proof-model-scope.md` (FORMS-T4-S0-00) is **Status: DECIDED** for B1 (scope) + B2 (proof model) on explicit user authority (2026-07-04), B3 NOT authorized — this is the accepted-divergence record for the T4 举证质证表 proof-model. The exec lane confirms the brief is still READY, enumerates every reconciliation-log entry, and confirms none is an UNRESOLVED M0-blocking entry.
- Exact target files (FUTURE exec lane): a NEW `docs/release/gate16-brief-ready-reconciliation-verify-00.md` (the verification procedure + findings + evidence record) + the gate-16 evidence ROW of `docs/release/go-live-readiness-report.md` (update to reference the result — evidence-row TEXT ONLY; **gate 16 MUST remain `PARTIAL` in this WI** — add only a `[Δ] brief READY + reconciliation log clean` marker using the report's EXISTING vocabulary; a `PARTIAL → CLEARED` move belongs to a SEPARATE holistic readiness-refresh WI, NOT this one — NO roll-up/status-bucket edit; gate 16 stays PARTIAL, its roll-up bucket unchanged — and MUST NOT clear gates 2/6/8/12/13/19/20 or imply any cleared) + `dev-memo/run/reviews/` (the exec lane's review artifact). NO other file. The exec lane's inspection is READ-ONLY; it EDITS NO brief, ADR, or product file.
- Exact acceptance criteria: below.
- Exact out-of-scope: NO project-brief EDIT/rewrite (the brief is inspected READ-ONLY; a brief change/amendment is a SEPARATE governed WI per `.claude/rules/project-brief.md`); NO ADR edit; NO Forms implementation; NO product source/test/package/config change; NO dependency change; NO schema/migration; NO contract change; NO gate-6 full-project audit; NO clearing of gates 2/6/8/12/13/19/20 (or implying any cleared); NO gate-7 D-G7 risk acceptance; NO broad readiness refresh; NO signing/notarization/公开 distribution decision (gate 4); NO 律师法 conclusion (gate 11); NO license/legal/business decision (gate 17); NO final GO/NO-GO (gate 21); NO source/test/package/config/schema/contract change in EITHER lane (docs-only tracked output).
- Requirements the FUTURE verification doc must define + prove (what the exec lane's verification must specify + record) — at minimum:
  1. **Exact brief file(s) to inspect**: `docs/product/project-requirements-brief.md` — the frontmatter `status:` field + the `## Status banner` + the `## Reconciliation log` section (state the exact section/line references + the tool version/date read).
  2. **Exact reconciliation-log section(s) to inspect**: the brief's `## Reconciliation log` (≈§346) INCLUDING the R-1/R-2/R-3 grouped resolution (Post-v1 SYNC program) + the promotion-safety checklist. Enumerate EVERY entry.
  3. **Status field / markers proving READY**: record that the frontmatter `status: READY` (and that the `authoritative_after` condition is met — RECORD this from an EXISTING artifact, i.e. cite the prior `/cc-suite:review-plan` READY that promoted the brief per `.claude/rules/project-brief.md` §"Status lifecycle"; do NOT re-decide or re-run the promotion review). Cite the exact `status:` frontmatter line.
  4. **Accepted-divergence records to verify (incl. the T4 proof-model ADR)**: for each reconciliation-log entry marked resolved/accepted, confirm the resolution is documented — specifically the **T4 举证质证表 proof-model** divergence resolved by `docs/adr/ADR-forms-t4-proof-model-scope.md` (Status DECIDED). Confirm the Forms T3/T4/T5 release boundary (T3 COMPLETE M0; T4 DECIDED post-v1; T5 design-gated) is consistent with the brief + the readiness evidence. Record each accepted divergence + its ADR/record reference.
  5. **Unresolved-entry search criteria + per-entry classification**: grep/scan the reconciliation log for any `RECONCILIATION-NEEDED` without a `RESOLUTION:` (accepted-as-known-divergence OR resolved-via-ADR/WI), and any `NEEDS-RECONCILIATION` / open-conflict marker. Record the exact search patterns + results. **Classify EACH reconciliation-log entry explicitly as one of `resolved` / `accepted-divergence` / `post-v1` / `unresolved-M0-blocking`** in a per-entry table, so "no unresolved M0-blocking" is AUDITABLE (a per-entry verdict), not only a summary claim.
  6. **What counts as M0-blocking vs post-v1/non-blocking**: an unresolved entry is **M0-blocking** ONLY if it is an unresolved conflict that would block a v1 day-one surface (a contradiction the brief itself flags as blocking) — NOT a documented accepted divergence, NOT a post-v1 item (e.g. the R-1/R-2/R-3 SYNC program is explicitly post-v1), NOT a resolved-via-ADR entry (e.g. T4). Any genuinely-unresolved M0-blocking entry MUST be surfaced (STOP-and-escalate), not silently passed.
  7. **Expected evidence artifact**: the doc records the brief `status:` line, the reconciliation-log entry enumeration + each entry's resolution state, the accepted-divergence records (incl. the T4 ADR), the unresolved-entry search patterns + results (expected: none M0-blocking), and the environment (date) — captured as text.
  8. **Deterministic pass/fail criteria**: PASS = brief `status: READY` AND the reconciliation log has NO unresolved M0-blocking entry AND every accepted divergence (incl. T4) is documented with its ADR/record. FAIL = brief not READY, OR an unresolved M0-blocking reconciliation entry, OR an accepted divergence lacking a documented record — surfaced, not papered over.
  9. **How the result feeds gate 16 without clearing unrelated gates**: a PASS supplies the "brief READY + reconciliation log clean" evidence. **Gate 16 MUST remain `PARTIAL`** — the exec lane adds ONLY a `[Δ] brief READY + reconciliation log clean` marker (existing vocabulary; roll-up bucket unchanged) and MUST NOT make a `PARTIAL → CLEARED` move (that belongs to a SEPARATE holistic readiness-refresh WI). It MUST NOT clear gates 2/6/8/12/13/19/20 or imply any cleared; go-live independence is kept.
  10. **Residual risks + follow-up WIs (if any)**: e.g. the R-1/R-2/R-3 SYNC reconciliation program is post-v1 (tracked, not resolved for v1 — a future post-v1 reconciliation WI); any brief AMENDMENT that later becomes necessary is a SEPARATE governed project-brief WI (not this lane).
- Dependencies mapped: **gate 6** (full-project audit — the brief + reconciliation log are inputs to that sweep; LATER, NOT cleared); **gates 2/8/12/13/19/20** (adjacent readiness gates — NOT cleared); the Forms T3/T4/T5 boundary (T3 COMPLETE, T4 DECIDED-post-v1, T5 design-gated) is cited as consistent context, NOT re-decided. These are noted as related, NOT cleared by this WI.
- Essential refs: `docs/product/project-requirements-brief.md` (frontmatter `status:` + `## Reconciliation log` — the ground truth, READ-ONLY); `docs/adr/ADR-forms-t4-proof-model-scope.md` (the T4 accepted-divergence record, Status DECIDED); `.claude/rules/project-brief.md` §"Authority hierarchy" + §"Status lifecycle" + §"Conflict handling" (the READY + reconciliation rules); `docs/release/go-live-readiness-report.md` §1 gate 16 (PARTIAL) + related gates 2/6/8/12/13/19/20; `AGENTS.md` §"Source hierarchy" + §"Authoring discipline"; `.claude/rules/security-boundary.md` §"Go-live independence".
- Review questions: (1) Does the WI correctly scope a READ-ONLY brief-READY + reconciliation-log VERIFICATION (inspection + evidence), NOT a brief edit/rewrite and NOT an execution in this authoring lane? (2) Does it require inspecting the brief `status:` + the FULL reconciliation log (incl. the R-1/R-2/R-3 grouped resolution) + the T4 accepted-divergence ADR? (3) Does it define M0-blocking vs post-v1/accepted-divergence correctly (SYNC program = post-v1; T4 = resolved-via-ADR) + STOP-and-escalate any genuinely-unresolved M0-blocking entry? (4) Does it keep gates 2/6/8/12/13/19/20 uncleared + gate 16 `PARTIAL`, forbid a brief edit (a brief amendment is a separate governed WI), and make no GO/NO-GO or readiness refresh? (5) Does it decide no STOP-AND-ASK (4/11/17/21) hard-stop? (6) Is any brief rewrite, ADR edit, Forms implementation, product source/test change, or readiness refresh smuggled in?

Type: EVIDENCE
Scope: Govern (do NOT execute) a FUTURE lane that runs the M0 gate-16 brief-READY + reconciliation-log verification. The FUTURE execution lane produces `docs/release/gate16-brief-ready-reconciliation-verify-00.md` — defining requirements 1–10 above (exact brief file + reconciliation-log sections; the READY status markers; the accepted-divergence records incl. the T4 proof-model ADR; the unresolved-entry search criteria; M0-blocking vs post-v1/accepted definition; evidence artifact; deterministic pass/fail; how it feeds gate 16 without clearing others; residual risks) — then runs the READ-ONLY inspection (read the brief + reconciliation log + the T4 ADR), records the result, and updates ONLY the gate-16 evidence row of `docs/release/go-live-readiness-report.md`, where **gate 16 MUST remain `PARTIAL`** (add only a `[Δ] brief READY + reconciliation log clean` marker; NO roll-up/status-bucket edit; a `PARTIAL → CLEARED` move belongs to a SEPARATE holistic readiness-refresh WI). It EDITS NO brief/ADR (a brief amendment is a SEPARATE governed project-brief WI), confirms the brief is READY + the reconciliation log has no unresolved M0-blocking entry + the accepted divergences (incl. T4) are documented, STOP-and-escalates any genuinely-unresolved M0-blocking entry, and cites gates 2/6/8/12/13/19/20 WITHOUT clearing them. It PRESERVES every still-non-cleared gate + the STOP-AND-ASK gates (4/11/17/21); it makes NO final GO/NO-GO, resolves NO STOP-AND-ASK, runs NO gate-6 audit, EDITS NO brief/ADR, does NO Forms implementation, adds NO dependency, runs NO readiness refresh, and changes NO product source/test/schema/contract. THIS authoring lane implements + runs + decides NOTHING — it only governs the gate-16 verification WI. If review asks to edit the brief/ADR, implement Forms, change product source/test, clear a dependent gate, run gate 6, or run a readiness refresh, REJECT and report.
Source of truth: docs/product/project-requirements-brief.md (frontmatter status: + ## Reconciliation log — READ-ONLY ground truth); docs/adr/ADR-forms-t4-proof-model-scope.md (T4 accepted-divergence record, Status DECIDED); .claude/rules/project-brief.md §"Authority hierarchy" + §"Status lifecycle" + §"Conflict handling" + §"Downstream consumption rules"; docs/release/go-live-readiness-report.md §1 gate 16 (PARTIAL) + gates 2/6/8/12/13/19/20; AGENTS.md §"Source hierarchy" + §"Authoring discipline"; .claude/rules/security-boundary.md §"Go-live independence"; .claude/rules/autonomy.md + staging-hygiene.md + loc-guardian.md
Allowed files: docs/release/gate16-brief-ready-reconciliation-verify-00.md (the NEW verification evidence doc the FUTURE lane produces) + docs/release/go-live-readiness-report.md (the gate-16 evidence ROW ONLY — evidence-row TEXT ONLY, NO roll-up/status-bucket edit; gate 16 stays PARTIAL, its roll-up bucket unchanged) + dev-memo/run/reviews/ (the exec lane's own review artifact). NOTE: THIS authoring lane commits ONLY the queue governance (queue.md/linted/reviewed/governed) + reviews/queue-review-160.md — NO verification doc, NO brief/ADR edit, NO report change. The FUTURE exec lane's inspection is READ-ONLY; it records its evidence in the COMMIT MESSAGE + review artifact and does NOT re-stage this lane's queue governance and does NOT edit the brief/ADR.
Forbidden files: docs/product/project-requirements-brief.md (READ-ONLY — NO brief edit/rewrite/amendment; a brief change is a SEPARATE governed project-brief WI), docs/adr/ (READ-ONLY — NO ADR edit), docs/release/ OTHER than the NEW gate16-brief-ready-reconciliation-verify-00.md + the gate-16 row of go-live-readiness-report.md (the other reports are READ-ONLY evidence — NOT edited), docs/contracts/, apps/ + services/ (NO product source/test/renderer/IPC/Forms change — NO Forms implementation), services/*/src/sqlite/schema.ts (CURRENT_SCHEMA_VERSION stays 12), migrations/, native/, package.json/package-lock.json of any package (NO dependency change), dev-memo/run/intake/ (raw samples — never staged), dev-memo/ OTHER than run/reviews/ (no other plan/design edit), .claude/ + .github/; NO brief EDIT; NO ADR edit; NO Forms implementation; NO verification RUN in this authoring lane; NO gate-6 full-project audit; NO broad readiness refresh; NO clearing of gates 2/6/8/12/13/19/20; NO signing/notarization/public-distribution decision (gate 4); NO 律师法 conclusion (gate 11); NO license/legal/business decision (gate 17); NO gate-7 D-G7 risk acceptance; NO final GO/NO-GO decision (gate 21). Push is NEVER automatic (autonomy hard-stop): local commit governance is the default; any branch push + PR open happens ONLY under explicit per-invocation user authorization; NO merge; NO branch delete of shared branches
Gates: EVIDENCE/governance lane (MEDIUM release-governance risk — governs a brief-READY + reconciliation-log verification WI; no code, no brief edit, no go-live decision). (1) THIS authoring lane: broker /cc-suite:review-plan on this queue WI, Path 1, retrievable, then govern. (2) The FUTURE exec lane: RUN the read-only inspection (read the brief + reconciliation log + T4 ADR), produce the verification doc + the gate-16 row update, then broker /cc-suite:review-plan on the produced artifacts. (3) scripts/workflow/check-contract-integrity.sh exit 0; scripts/workflow/check-queue.sh PASS (both lanes). Forbidden-scope scan before commit: CURRENT_SCHEMA_VERSION EQUALS 12; no product source/test/package/schema/contract change; no brief/ADR edit; no Forms impl; no dependency change; no gate-6 run; no readiness refresh; no gate 2/6/8/12/13/19/20 clear; no GO/NO-GO; no raw sample. STOP-points: review asks to edit the brief/ADR, implement Forms, change product source/test, clear a dependent gate, run gate 6, or run a readiness refresh → REJECT/STOP and report; a verification reveals the brief is NOT READY or an unresolved M0-blocking reconciliation entry → STOP-and-escalate; any forbidden file would change → STOP.
Acceptance criteria: check-queue and check-contract-integrity PASS and CURRENT_SCHEMA_VERSION EQUALS 12 and the diff CONTAINS only the queue governance (queue.md/linted/reviewed/governed) + reviews/queue-review-160.md and no docs/source/test/package/schema/contract change appears and no brief/ADR edit / no Forms impl / no dependency change / no readiness refresh appears. The governed WI must DEFINE the ten verification requirements (brief file + reconciliation-log sections; READY markers; accepted-divergence records incl. T4 ADR; unresolved-entry search; M0-blocking-vs-post-v1 definition; evidence artifact; deterministic pass/fail; feeds-gate-16-without-clearing-others; residual risks), keep gates 2/6/8/12/13/19/20 uncleared, keep gate 16 `PARTIAL`, forbid a brief edit, and MUST NOT decide a STOP-AND-ASK hard-stop or assert go-live. review-plan (this lane) must be RUN and must PASS (READY, or READY with only Low-risk clarifications applied).
Risk flags: MEDIUM (release-governance — a verification WI that could over-claim readiness, drift into editing the brief, or clear a dependent gate). Care points: (a) NO VERIFICATION RUN in this authoring lane — this lane only governs the WI; (b) BRIEF READ-ONLY — the brief + ADRs are inspected, NEVER edited; a brief amendment is a SEPARATE governed project-brief WI (`.claude/rules/project-brief.md`); (c) M0-BLOCKING DISCIPLINE — the R-1/R-2/R-3 SYNC program is post-v1 (not M0-blocking); the T4 divergence is resolved-via-ADR (accepted); only a genuinely-unresolved v1-day-one conflict is M0-blocking → STOP-and-escalate; (d) DEPENDENT GATES UNCLEARED — gates 2/6/8/12/13/19/20 stay uncleared; gate 6 consumes the brief later; (e) GO-LIVE INDEPENDENCE — a brief/reconciliation verification does NOT imply go-live; the final verdict + the STOP-AND-ASK hard-stops (4/11/17/21) remain the user's; (f) NO READINESS REFRESH / NO FORMS WORK. If any cannot hold, STOP and report.
Depends on: none
Commit boundary: TWO separate lanes. (1) THIS authoring lane commits ONLY the queue governance (queue.md/linted/reviewed/governed) + reviews/queue-review-160.md — no verification doc, no brief/ADR edit, no report change, no implementation, no decision. (2) The FUTURE execution lane commits ONLY the new docs/release/gate16-brief-ready-reconciliation-verify-00.md + the gate-16 evidence-row update to go-live-readiness-report.md + its own review artifact under dev-memo/run/reviews/, records its evidence (incl. the reconciliation-log enumeration) in the COMMIT MESSAGE, and does NOT re-stage this lane's queue governance and does NOT edit the brief/ADR. NO product source/test/package/schema/contract file in either lane; NO brief/ADR edit; NO Forms implementation; NO dependency change; NO staging of dev-memo/run/intake/ or untracked residue; push is NEVER automatic (autonomy hard-stop) — local commit governance is the default, any branch push + PR open happens ONLY under explicit per-invocation user authorization; no merge; no branch delete of shared branches. THIS authoring lane implements NOTHING, runs NO verification, and decides NOTHING — it only governs the gate-16 brief-READY + reconciliation-log verification WI.

## Acceptance criteria (FUTURE execution lane — what the governed verification doc must satisfy)

These describe the deliverable the FUTURE exec lane must produce; they are NOT preconditions of THIS authoring lane. The authoring-lane acceptance is the structured `Acceptance criteria:` field above (queue governance + review READY only — the verification doc does NOT exist yet and MUST NOT be created in this lane).

1. A NEW `docs/release/gate16-brief-ready-reconciliation-verify-00.md` exists defining requirements 1–10 above and recording the inspection findings + result.
2. The verification cites the brief `status: READY` frontmatter line, enumerates every `## Reconciliation log` entry (incl. the R-1/R-2/R-3 grouped resolution), and confirms the T4 accepted-divergence ADR (`ADR-forms-t4-proof-model-scope.md`, Status DECIDED) — the brief is NOT edited.
3. The evidence records the unresolved-entry search patterns + results (expected: no unresolved M0-blocking entry), each accepted divergence + its record, and an explicit PASS/FAIL — read-only inspection, no brief edit.
4. The exec lane CITES the exact gate-16 row (by its `| 16 | …` line) before editing, and edits ONLY that row's evidence/blocker TEXT — NO adjacent roll-up/status-bucket line touched. Gate 16 **MUST remain `PARTIAL`** — add ONLY a `[Δ] brief READY + reconciliation log clean` marker (roll-up bucket unchanged); any `PARTIAL → CLEARED` move belongs to a SEPARATE holistic readiness-refresh WI. MUST NOT clear gates 2/6/8/12/13/19/20 or imply any cleared. Roll-up-line semantics kept consistent.
5. The doc cites gate 6 (consumes the brief later) + keeps go-live independence (no sign-off; gates 4/11/17/21 remain user STOP-AND-ASK); the R-1/R-2/R-3 SYNC program is recorded as post-v1 (not M0-blocking).
6. NO product source/test/package/config/schema/contract change in either lane; NO brief/ADR edit; NO Forms implementation; NO dependency change; `CURRENT_SCHEMA_VERSION` stays 12; docs-only tracked output.
7. `scripts/workflow/check-queue.sh` PASS + `scripts/workflow/check-contract-integrity.sh` PASS on both lanes.
