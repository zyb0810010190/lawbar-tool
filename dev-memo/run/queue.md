## WI-A3-UNLINK-SCHEMA-01: durable-unlink V12 additive migration (A0.7-gated; custody 9b)
Type: IMPL
Requires-A07: yes
A07 custody mode: 9b. Human runs marker mint + A0.7-gated check-gates directly; agent does not receive the HMAC key. Implementation commit is blocked until human-run gated verification reports PASS.
A0.7-classification: WI-A3-UNLINK-SCHEMA-01 is the durable-unlink SCHEMA IMPLEMENTATION (the V12 additive migration) foreseen by A3-UNLINK-SCHEMA-00 §11 + the plan WI-A3-UNLINK-SCHEMA-T1; it changes the persistence schema over the V11 A3 anchor/link tables, so it MUST NOT build on unproven A0.7 geometry and is HIGH-RISK (persistence/migration). It carries `Requires-A07: yes` and runs under A07-KEY-00 custody mode 9b: the human mints the local marker + runs the A0.7-gated `check-gates.sh` with the HMAC key; the agent never receives the key; the implementation commit is blocked until the human reports gated PASS. If review-plan says this WI must NOT be A0.7-gated, STOP and report.
Scope: Implement ONLY the durable-unlink schema mechanism chosen by A3-UNLINK-SCHEMA-00 §2 (the binding design source / PR #133): a forward-only ADDITIVE V12 migration that adds two nullable marker columns to case_box_links — `unlinked_at TEXT COLLATE BINARY` and `unlink_reason TEXT` — so explicit-unlink state is durable + schema-backed. CURRENT_SCHEMA_VERSION 11 -> 12; DDL_STATEMENTS_V12 (ALTER TABLE case_box_links ADD COLUMN ...) added to DDL_BY_VERSION; applySchema upgrades V11 -> V12 additively; existing rows default unlinked_at NULL (not unlinked); no data rewrite. anchor_id stays NOT NULL; LinkStatus stays valid|needs_review|broken (UNCHANGED). This is SCHEMA-ONLY: NO resolver/export marker-awareness (deferred to WI-A3-UNLINK-RESOLVE), NO unlink/relink OPERATION / set-clear write path (deferred to WI-A3-UNLINK-T1), NO audit-event emission, NO new src module beyond schema.ts, NO FK/ON DELETE CASCADE, NO UI, NO dependency, NO marker/key/gate change. The "reason required iff unlinked" + terminal-until-relink are RECORDED app-layer invariants enforced by the future operation WI, NOT by this migration.
Required decisions to record: (1) MIGRATION SHAPE — DDL_STATEMENTS_V12 = `ALTER TABLE case_box_links ADD COLUMN unlinked_at TEXT COLLATE BINARY;` + `ALTER TABLE case_box_links ADD COLUMN unlink_reason TEXT;` (two additive, nullable columns; no NOT NULL, no default — existing rows get NULL). (2) VERSION BUMP — CURRENT_SCHEMA_VERSION 11 -> 12; DDL_BY_VERSION gains `[12, DDL_STATEMENTS_V12]`; applySchema's existing current+1..CURRENT loop runs V12 exactly once (version-gated, so the ALTER never double-runs). (3) NO V11 EDIT — the V11 DDL_STATEMENTS_V11 CREATE TABLE is NOT edited (each VN is a delta; V12 is the ALTER delta). (4) COLLATION/STYLE — unlinked_at is a byte-stable timestamp TEXT COLLATE BINARY (matching created_at/captured_at); unlink_reason is plain TEXT. (5) NO SQLITE CHECK FOR THE iff — the "unlink_reason required iff unlinked_at IS NOT NULL" invariant is an APP-LAYER invariant per the case-box convention (cross-column iff cannot be added via ALTER; enforced by the future operation WI), NOT a SQLite CHECK; RECORD it, do not enforce in DDL. (6) BACKWARD COMPAT — V11 -> V12 forward-only additive; a planted V11 DB upgrades additively to 12; existing V11 rows read unlinked_at IS NULL; no data rewrite; applySchema still refuses a future-version (>CURRENT) DB. (7) NO INDEX — minimal migration adds no index on the marker columns (a query/index is a future WI if needed). (8) TESTS — extend tests/hardening-schema.test.mjs (already wired into the package test script) with V12 cases (mirroring the V9/V10/V11 pattern). (9) NO RESOLVER/EXPORT/OPERATION — schema-only; the resolver/export marker-awareness + the unlink/relink operation are separate future A0.7-gated WIs. (10) A0.7 — A0.7-gated (custody 9b).
Source of truth: docs/adr/ADR-evidence-a3-durable-unlink-schema.md (A3-UNLINK-SCHEMA-00 — the binding design / PR #133: §2 chosen mechanism = the two marker columns; §4 anchor_id NOT NULL + LinkStatus unchanged; §9 app-layer invariants; §10 V11->V12 forward-only additive backward-compat; §11 A0.7-gated); dev-memo/plan-batch-casebox-evidence-a3-unlink-schema-00.md (WI-A3-UNLINK-SCHEMA-T1 — additive V12, schema-only; resolver/export + operation deferred; test strategy); services/case-box-persistence/src/sqlite/schema.ts (the file edited — V11 case_box_links; CURRENT_SCHEMA_VERSION; DDL_STATEMENTS_VN; DDL_BY_VERSION; applySchema forward-only/refuse-future); services/case-box-persistence/tests/hardening-schema.test.mjs (the V9/V10/V11 hardening-test pattern to mirror for V12); docs/adr/ADR-evidence-a3-schema-persistence-contract.md (A3-SCHEMA-00 — additive-migration + no-FK + COLLATE-BINARY conventions); .claude/rules/autonomy.md + .claude/rules/cc-suite.md + .claude/rules/security-boundary.md + .claude/rules/loc-guardian.md (user-authorized; A0.7-gated; schema migration is HIGH-RISK so broker review-plan + audit + verify REQUIRED).
Allowed files: services/case-box-persistence/src/sqlite/schema.ts (the V12 migration: DDL_STATEMENTS_V12 + DDL_BY_VERSION entry + CURRENT_SCHEMA_VERSION 11->12 + a V12 doc-comment block; NO edit to V1-V11 DDL), services/case-box-persistence/tests/hardening-schema.test.mjs (extend with V12 cases — already wired into the package test script), dev-memo/run/queue.md, dev-memo/run/queue.linted, dev-memo/run/queue.reviewed, dev-memo/run/queue.governed, dev-memo/run/reviews/queue-review-085.md, dev-memo/study/** (a batch-audit study packet if a window closes), dev-memo/cc-suite-reliability-log.md (only if a TIMEOUT retry occurs), dev-memo/deferred-audit-findings.md (only if a finding is deferred)
Forbidden files: services/case-box-persistence/src/sqlite/linkStatusResolverQueries.ts (NO resolver change — schema-only), services/case-box-persistence/src/sqlite/exportCitationQueries.ts (NO export change), services/case-box-persistence/src/sqlite/anchorDeleteGuardQueries.ts (NO guard change), services/case-box-persistence/src/errors.ts (NO error change), any other services/** path not listed in Allowed files (NO new operation module, NO repo-query change, NO index.ts change — applySchema/CURRENT_SCHEMA_VERSION already exported), migrations/**, apps/**, native/**, docs/contracts/**, .claude/**, .github/**, AGENTS.md, CLAUDE.md, GEMINI.md, scripts/workflow/** (NO gate/marker/check change), package-lock.json / any dependency manifest, services/case-box-persistence/package.json (NO change — hardening-schema.test.mjs already wired), committed dev-memo/run/evidence/** marker/ledger/key/evidence-run files, committed keys, dev-memo/run/reports/** (untracked local run-state — never staged), dev-memo/run/override-reason.md (untracked residue — never staged), Evidence-Genie-M0-Developer-Handover.md (root intake — READ-ONLY, never moved/deleted), xiaolai-dev-workflow-study.md (root intake — READ-ONLY, never moved/deleted); NO resolver/export/operation/unlink/relink/set-clear implementation, NO physical anchor/link delete, NO FK/ON DELETE CASCADE, NO SQLite CHECK for the reason-iff-unlinked invariant, NO edit to V1-V11 DDL, NO index on the marker columns, NO audit-event emission, NO UI, NO dependency, NO marker/key/gate change
Gates: broker /cc-suite:review-plan REQUIRED before authoring code (confirm A0.7-gated classification for the V12 migration; confirm the additive ALTER shape + version bump 11->12 + DDL_BY_VERSION entry is correct + idempotent (version-gated, no double-ALTER); confirm anchor_id stays NOT NULL + LinkStatus unchanged; confirm schema-only — no resolver/export/operation; confirm the reason-iff-unlinked stays an app-layer invariant (not a DDL CHECK); confirm V11->V12 backward-compat); if review-plan returns NEEDS-FIX, STOP and report; if review-plan demands resolver/export/operation code, a dependency, UI, a SQLite CHECK that needs a table rebuild, or any non-additive/destructive migration, STOP and ask; broker /cc-suite:audit + /cc-suite:verify on the implementation diff AFTER the human-run gated PASS, BEFORE the implementation commit; A0.7 GATE (custody 9b) — human runs marker mint + A07_REQUIRED=1 scripts/workflow/check-gates.sh -> GATES OK with the HMAC key (agent never receives key); npm --prefix services/case-box-persistence test passes (existing V9/V10/V11 + resolver/export/guard + all case-box tests stay green AND the new V12 tests pass; the migration rebuilds dist); scripts/workflow/check-contract-integrity.sh exits 0; npm --prefix apps/lawbar-desktop test 610/610; npm --prefix native/evidence-core test 38/38; loc-guardian fail thresholds respected (schema.ts stays a generated-style large file — additive change only; if schema.ts is observed over a hard threshold, surface it but the additive delta is small); no resolver/export/guard/dependency change; no committed marker/ledger/key/evidence-run; root intake untouched; override-reason/reports never staged
Acceptance criteria: services/case-box-persistence/src/sqlite/schema.ts has CURRENT_SCHEMA_VERSION == 12; DDL_STATEMENTS_V12 adds the two nullable columns (unlinked_at TEXT COLLATE BINARY, unlink_reason TEXT) to case_box_links via additive ALTER TABLE; DDL_BY_VERSION contains [12, DDL_STATEMENTS_V12]; V1-V11 DDL UNCHANGED; tests/hardening-schema.test.mjs has V12 cases and ALL of these observable cases PASS: applySchema on an empty :memory: DB returns 12 and schema_version contains 1..12; applySchema is idempotent at v12 (second call no-op, no duplicate-column error); a planted V11 DB upgrades additively to 12 (the two columns added; existing case_box_links rows read unlinked_at IS NULL); the two columns exist on case_box_links + are nullable (a row inserted WITHOUT them succeeds; a row WITH unlinked_at + unlink_reason set persists those values across a DB close+reopen — durability); applySchema still refuses a future-version (13) DB before mutation; anchor_id stays NOT NULL (insert with NULL anchor_id still rejected) + LinkStatus CHECK unchanged (status='unlinked' still rejected — no enum change); NO new table; NO FK/cascade; NO index added on the marker columns; the existing resolver/export/guard tests remain green (their behavior is unchanged by the additive columns); npm --prefix services/case-box-persistence test passes; scripts/workflow/check-contract-integrity.sh exits 0; npm --prefix apps/lawbar-desktop test 610/610; npm --prefix native/evidence-core test 38/38; human-run A0.7-gated check-gates returns GATES OK; broker review-plan READY (classification + additive-migration confirmed) + audit reports no Critical/High/Medium + verify ALL CLOSED; the committed implementation diff is confined to the Allowed files (schema.ts + hardening-schema.test.mjs + dev-memo/**)
Risk flags: IMPL — a SCHEMA MIGRATION (V11 -> V12) over court-facing evidence-link persistence — HIGH-RISK (persistence/migration; broker review-plan + audit + verify REQUIRED). A0.7-GATED (custody mode 9b — human runs marker mint + gated check-gates with the HMAC key; agent never receives the key; impl commit blocked until human reports gated PASS). Forward-only ADDITIVE: two nullable columns; existing rows default not-unlinked; no data rewrite; idempotent (version-gated). SCHEMA-ONLY: no resolver/export/operation change (deferred). The "reason required iff unlinked" + terminal-until-relink are RECORDED app-layer invariants, NOT DDL CHECKs (a cross-column iff cannot be added via ALTER without a table rebuild — avoid the rebuild). The migration MUST NOT weaken any Evidence invariant (anchor_id stays NOT NULL; LinkStatus unchanged; no-FK convention; existing resolver/export/guard behavior unchanged) nor the A3-DB-00 encryption hard stop. NO non-additive/destructive migration, NO table rebuild, NO resolver/export/operation, NO dependency, NO marker/key/gate change. No push, no merge.
Depends on: none
Commit boundary: governance commit separate (dev-memo/run/queue.md + queue.linted + queue.reviewed + queue.governed + dev-memo/run/reviews/queue-review-085.md); ONE implementation commit -- services/case-box-persistence/src/sqlite/schema.ts + services/case-box-persistence/tests/hardening-schema.test.mjs, staged by exact path; the implementation commit is gated on the human-reported A0.7 gated PASS + broker audit/verify clean; no push; no merge
