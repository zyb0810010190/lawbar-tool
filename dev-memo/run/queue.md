## WI-EVIDENCE-A10-NATIVE-GOLDEN-EXPORT-HARNESS-00: implement the real native golden-export gate (Type: IMPL)
Type: IMPL
Scope: Implement the REAL native `golden-export` CI gate in the Swift package `native/evidence-core-swift/` (where the A0.7 / A1-T6 / A3-T10 real gates live), replacing the `not_implemented`/failing placeholder with a deterministic, truthful gate that validates the EXISTING A10-T6 apps-layer golden artifact (`apps/lawbar-desktop/tests/fixtures/a10-golden-canonical-export.json`). It MUST NOT touch the JS shim `native/evidence-core/**` (that contract shim deliberately keeps `golden-export` `not_implemented`), MUST NOT wire the live export pipeline, MUST NOT render `.docx`/PDF, MUST NOT re-implement the apps-layer serializer (a second source of truth), and MUST NOT start A8/forms/schema/custody/confidential-fixture/JS-shim/A1T6-AUD-L1 work. Add a NEW harness `native/evidence-core-swift/Sources/EvidenceCoreSmoke/A10GoldenExportHarness.swift` (`EvidenceCoreA10GoldenExport.run(fixtureURL:)` -> a stable verdict { status: pass|fail|inconclusive; classification: ok|integrity_mismatch|shape_mismatch|authority_violation|semantic_mismatch|fixture_or_oracle_invalid|not_implemented|inconclusive_no_checkable_assertions; observedRowCount; detail }, mirroring EvidenceCoreA3Regression), a thin CLI `native/evidence-core-swift/Sources/A10GoldenExportCLI/main.swift` (`a10-golden-export-cli <golden.json>` -> `<status>\t<classification>\t<rowCount>`, exit 0 iff pass), register both in `native/evidence-core-swift/Package.swift`, and add tests `native/evidence-core-swift/Tests/EvidenceCoreSmokeTests/A10GoldenExportHarnessTests.swift`. The gate VALIDATES the golden artifact (single source of truth — read by repo-relative path, no copy into the Swift package): (1) loadable + parseable; (2) INTEGRITY — SHA-256 of `expectedSerialization` equals `expectedSha256` (pure-Swift SHA-256, no CryptoKit/platform floor); (3) SHAPE — required ADR §4 keys present, NO invented `canonicalModelVersion`, top-level keys in canonical ascending order, no `null` sentinels in rows; (4) NO-HREF-LEAK — no `lawbar:`/`http(s):`/`javascript:`/`data:`/`file:`/`mailto:`/`vbscript:`/`internalHref`/`href` token in the serialization; (5) ROWS authority — each row is key-presence-XOR (EXACTLY one of citationText/flag) with a non-empty linkId and no href/nav metadata; (6) SEMANTIC — rows are consistent with the `rendered` input (same link-id set; clean citations -> citationText row carrying the exact 卷X页Y; flagged -> flag row). `not_implemented` MUST NEVER be the result. The gate writes NO marker, performs NO provenance/HMAC, makes NO network call. It MUST NOT falsely claim live-pipeline/.docx/PDF support. If the A10-T6 contract is internally inconsistent (so the gate cannot validate it without rewriting the contract), STOP and report rather than broadening the model.
Source of truth: docs/adr/ADR-evidence-a10-court-fileable-export.md (A10-DESIGN-00 §4 CanonicalExportModel, §8 A10-T6 golden-export byte-identical, §13 step-6, §"native golden-export harness not_implemented = fail"); .claude/rules/evidence-genie.md (invariant 9 canonical-not-raw-bytes, invariant 10 not_implemented is a FAILURE); apps/lawbar-desktop/tests/fixtures/a10-golden-canonical-export.json (the A10-T6 golden artifact to validate); apps/lawbar-desktop/src/caseBox/export/a10CanonicalExportModel.ts (the A10-T6 contract the golden encodes — READ-ONLY reference); native/evidence-core-swift/Sources/EvidenceCoreSmoke/A3AnchorRegressionHarness.swift + Sources/A3RegressionCLI/main.swift + Tests/EvidenceCoreSmokeTests/A3AnchorRegressionHarnessTests.swift (the harness/CLI/test pattern to mirror); native/evidence-core-swift/Package.swift (CLI registration pattern); native/evidence-core/lib/commands.mjs (the JS shim — READ-ONLY; stays not_implemented); .claude/rules/{evidence-genie,cc-suite,autonomy,execution-discipline,staging-hygiene,security-boundary,client-local-first,loc-guardian}.md
Allowed files: native/evidence-core-swift/Sources/EvidenceCoreSmoke/A10GoldenExportHarness.swift (NEW), native/evidence-core-swift/Sources/A10GoldenExportCLI/main.swift (NEW), native/evidence-core-swift/Tests/EvidenceCoreSmokeTests/A10GoldenExportHarnessTests.swift (NEW), native/evidence-core-swift/Package.swift (register the new executable target + product — exact-name may adjust per review-plan), dev-memo/run/queue.md, dev-memo/run/queue.linted, dev-memo/run/queue.reviewed, dev-memo/run/queue.governed, dev-memo/run/reviews/queue-review-114.md
Forbidden files: native/evidence-core/** (the JS shim — READ-ONLY; golden-export stays not_implemented there), apps/lawbar-desktop/src/caseBox/export/a10CanonicalExportModel.ts + a10CitationContract.ts + a10HyperlinkDegradation.ts (A10-T1/T2/T6 modules — READ-ONLY/referenced; NO edit), apps/lawbar-desktop/tests/fixtures/a10-golden-canonical-export.json (the golden — READ-ONLY; NO edit/regeneration), apps/lawbar-desktop/** except read access (NO app/renderer/electron/package/test change), native/evidence-core-swift/Sources/EvidenceCoreSmoke/{A07ConformanceHarness,A1CitationStabilityHarness,A3AnchorRegressionHarness}.swift + the a07/a1/a3 CLIs + their tests (READ-ONLY — regression-only; NO edit), services/** + docs/contracts/** (NO services/schema/contract change), services/case-box-persistence/src/sqlite/schema.ts (NO schema/CURRENT_SCHEMA_VERSION change), scripts/** + .claude/** + .github/** + migrations/**, docs/adr/** + docs/reference/** (READ-ONLY), dev-memo/run/evidence/** (gitignored custody marker/ledger — never touched/staged), committed keys, dev-memo/run/reports/** + dev-memo/run/override-reason.md + dev-memo/run/autonomous-next.md (untracked run-state — never staged/edited), Evidence-Genie-M0-Developer-Handover.md + xiaolai-dev-workflow-study.md (root intake — READ-ONLY); NO live-pipeline/IPC wiring; NO .docx/PDF rendering; NO A8 snapshot/seal/manifest/ExportPreview; NO T3/T4/T5 forms; NO custody/marker; NO HMAC/key request/receive/print/persist/stage; NO confidential/client/court fixture; NO A1T6-AUD-L1 hardening; NO push/merge/branch-delete
Gates: broker /cc-suite:review-plan REQUIRED before implementation — confirm: the native golden-export gate belongs in the Swift package (not the forbidden JS shim); validating the existing apps-layer golden artifact (single source of truth) rather than re-deriving it is the correct truthful scope; the verdict vocabulary (pass/fail/inconclusive + classifications; not_implemented NEVER returned) mirrors A0.7/A1-T6/A3-T10; integrity (SHA-256) + shape + no-href-leak + rows-authority + semantic-consistency are the right checks; a pure-Swift SHA-256 (no CryptoKit/platform floor) is acceptable; reading the apps fixture by repo-relative path is acceptable; NO JS-shim/apps-source/services/schema change. STOP-points: (1) if the gate cannot validate the golden without re-implementing the apps serializer (second source of truth) -> STOP; (2) if it requires editing the JS shim / apps a10*.ts / the golden fixture -> STOP; (3) if it requires wiring the live pipeline / rendering .docx/PDF / A8 -> STOP; (4) if the A10-T6 contract is internally inconsistent -> STOP and report (do not rewrite the contract); (5) if schema/services/forms/custody change becomes necessary -> STOP; (6) if it would weaken A1/A3/A8/A10 or the A0.7/A1-T6/A3-T10 gates -> STOP. If review-plan NEEDS-FIX, fix-forward; if a blocker, STOP. After implementation: `swift build --package-path native/evidence-core-swift` + `swift test --package-path native/evidence-core-swift` GREEN (the swiftpm-smoke gate); the gate run against the real golden prints `pass` and exits 0 (NOT not_implemented); targeted A10-T6 + A10-T1 + A10-T2 apps tests GREEN (regression); `scripts/workflow/check-contract-integrity.sh` exits 0; broker /cc-suite:audit + /cc-suite:verify (no Critical/High/Medium open before commit). No A0.7 custody. Batch is 1/3 (marker 0742464); NOT due — no closeout this lane.
Acceptance criteria: a NEW Swift harness `A10GoldenExportHarness.swift` (`EvidenceCoreA10GoldenExport.run/evaluate`), a NEW thin CLI `a10-golden-export-cli`, registered in `Package.swift`, with tests in `A10GoldenExportHarnessTests.swift`; the gate run against `apps/lawbar-desktop/tests/fixtures/a10-golden-canonical-export.json` returns status=pass, classification=ok, observedRowCount=5 and the CLI exits 0; tests PROVE: (a) the real apps-layer golden PASSES; (b) result schema shape (status/classification/observedRowCount/isMarker=false/detail); (c) `not_implemented` is NEVER the result; (d) a missing fixture -> fixture_or_oracle_invalid (fail); (e) a tampered serialization (hash no longer matches) -> integrity_mismatch (fail); (f) an injected href/internalHref (even with a matching re-hash) -> authority_violation (fail); (g) an invented `canonicalModelVersion` -> shape_mismatch (fail); (h) a reintroduced null-sentinel row -> shape_mismatch (fail); (i) a rendered/row mismatch -> semantic_mismatch (fail); (j) top-level keys are canonical-ascending; (k) pure-Swift SHA-256 is stable + 64-hex. NO JS-shim/apps-source/services/schema/forms/A8/live-pipeline/custody change; NO edit to a10*.ts or the golden fixture or the a07/a1/a3 harnesses; `CURRENT_SCHEMA_VERSION` unchanged; `dev-memo/run/evidence/**` untouched; no HMAC/custody key; `swift build`+`swift test` GREEN; `check-contract-integrity` exits 0; broker review-plan READY + audit no Critical/High/Medium + verify ALL CLOSED; committed diff confined to Allowed files.
Risk flags: HIGH-RISK Evidence-invariant lane (A10 court-fileable export; the golden-export gate is an A8.6 reproducibility prerequisite) — but a STANDALONE native validator over the EXISTING apps-layer golden; NO JS-shim/apps-source/services/schema change, NO live-pipeline wiring, NO .docx/PDF, NO A8, NO second source of truth. Load-bearing care points: (a) TRUTHFUL — not_implemented MUST NEVER be the result; integrity (SHA-256) + shape + no-href-leak + rows-authority + semantic checks must all genuinely fail-closed (negative tests prove each class fails, never pass); (b) SINGLE-SOURCE — validate the existing golden artifact by repo-relative path; do NOT copy it into the Swift package or re-implement the serializer; (c) NO-OVERCLAIM — the gate must not pretend to support the live pipeline or .docx/PDF; (d) DETERMINISM — pure-Swift SHA-256 + canonical-key-order check; (e) do NOT touch the JS shim, apps a10*.ts, the golden fixture, the a07/a1/a3 harnesses, A8, forms, schema, or custody. If any cannot hold, STOP and report. No push/merge/branch-delete.
Depends on: none
Commit boundary: governance (mark-reviewed + govern) run STANDALONE before the commit (never bundled with git commit). ONE implementation commit — native/evidence-core-swift/Sources/EvidenceCoreSmoke/A10GoldenExportHarness.swift + native/evidence-core-swift/Sources/A10GoldenExportCLI/main.swift + native/evidence-core-swift/Tests/EvidenceCoreSmokeTests/A10GoldenExportHarnessTests.swift + native/evidence-core-swift/Package.swift + governance artifacts (queue.md/.linted/.reviewed/.governed + reviews/queue-review-114.md), staged by exact path; NO JS-shim/apps/services/schema/contract/forms file; NO edit to a10*.ts / the golden fixture / the a07/a1/a3 harnesses; NO marker/evidence/key/report file; no push; no merge; no branch delete.
