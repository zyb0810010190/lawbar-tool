## WI-GOLIVE-READINESS-REFRESH-00: refresh the M0 go-live readiness gate status against current main (Type: EVIDENCE)
Type: EVIDENCE
Scope: Produce a docs/evidence-only refresh of the M0 go-live readiness picture against current `main`. The FUTURE execution lane: (a) refreshes the 21-gate readiness matrix in `dev-memo/plan-go-live-readiness-00.md` §1 against `main` HEAD — for each of the 21 gates, update `State today` + `Evidence` to the CURRENT reality (case-box persistence Phase B, the completed forms-T3 S0→S3 track, WI-01..03d HTTPS/SSRF sign-off, etc.) and classify each as CLEARED / PARTIALLY-CLEARED (residual) / OPEN (needs new WI) / STOP-AND-ASK, with a refreshed roll-up count. For the desktop-client gate specifically, the refresh MUST perform an EVIDENCE INVENTORY of the current app on main — enumerate the actual renderer screens + IPC handlers + channels that exist (matter/document/deadline/fact/docket/link/audit + the forms-T3 preview/export) — and classify the client evidence-bound as "release-incomplete to the extent proven" (naming the specific product-grade gaps), NOT prescribe a "token fixture" verdict; the `package.json` self-description is ONE input, but the blueprint's stale "NOT STARTED" and any "token fixture" framing MUST be corrected against what is actually built; (b) populates `docs/release/go-live-readiness-report.md` with an EXPLICITLY-INTERIM readiness status snapshot (as of `main` @ the refresh commit) using the report template's sections — the refresh MUST REPLACE the report's top status line ("Final v1 gate … Empty until then") and its GO/NO-GO Verdict section with an explicit "INTERIM SNAPSHOT / NO FINAL GO-LIVE VERDICT / blueprint gate 21 blocked" frame, so the file's own header does not fight the interim scope; it MUST NOT emit the FINAL GO/NO-GO verdict (that is blueprint gate 21, authored only after all v1-blocking gates clear; emitting a final GO here would be false). The refresh MUST: (i) flag the THREE user hard-stops as unresolved STOP-AND-ASK — 1. desktop framework confirm + PUBLIC distribution + code-sign/notarization for public release (Apple Developer ID); note the brief's distinction: dev-mode signing is acceptable-deferred for v1 dev use, but distribution/public release cannot clear without the user-owned signing/distribution decision — do NOT frame it as "signing blocks all v1 readiness"; 2. 律师法 confidentiality/legal-compliance sign-off; 3. final go-live sign-off (engineering + legal); (ii) distinguish true M0 blockers (evidence-bound — e.g. the desktop client's release-incompleteness to the extent proven by the inventory, open release-docs gates, test-matrix + full-audit closure) from NON-BLOCKING deferred Lows (`dev-memo/deferred-audit-findings.md`: 37 open, all Low, all Safe=YES, 0 Medium+); (iii) record the forms-track release status — T3 (证据目录及说明) COMPLETE (M0 feature), T4 (举证质证表) DECIDED post-v1 per `docs/adr/ADR-forms-t4-proof-model-scope.md`, T5 design-gated; (iv) restate the post-v1 / design-gated set (sync/mini-program, LLM, text-extraction engine, auto-update, multi-user, redaction, backup tooling incl. proof-matrix). It writes ONLY those two docs + governance; it changes NO code, NO product UI, NO schema, NO dependency, and makes NO go-live decision on the agent's own authority (the three hard-stops stay the user's). FORBIDDEN in this lane and the future exec lane: any code change; any product-UI/Mac-client buildout; any Electron signing/notarization implementation; any package/dependency change; any schema/migration/contract/persistence change; any native change; any Forms T4/T5 implementation; any security-boundary audit EXECUTION (this WI only records that gate #10 is open); any OCR-flake fix; any custody/marker/JS-shim/A8 work; and — critically — emitting a FINAL GO/NO-GO go-live verdict (gate #21 is blocked; only an interim status is authorized). If the refresh would require actually clearing a STOP-AND-ASK gate or making a go-live decision, STOP and report (record the gate as open/stop-and-ask; do not resolve it).
Source of truth: dev-memo/plan-go-live-readiness-00.md (the READY Go-Live Readiness Blueprint — the 21-gate matrix §1 + 23 STOP-AND-ASK §4 + roll-up; the authoritative gate list to refresh); docs/release/go-live-readiness-report.md (the report template to populate as an interim snapshot); docs/release/go-live-plan.md (legacy predecessor — WI-00..WI-13 obsolete/in-scope map; defers to the blueprint); docs/release/wi-03-security-signoff.md (WI-01..03d HTTPS/SSRF sign-off — gate #9 CLEARED evidence); docs/release/operator-checklist.md + test-and-audit-report.md (operational + test/audit gate evidence); docs/product/project-requirements-brief.md status:READY §18/§20 (v1 must-haves + STOP-AND-ASK); dev-memo/deferred-audit-findings.md (37 open Lows, Safe=YES — non-blocking classification); docs/adr/ADR-forms-t4-proof-model-scope.md (T4 post-v1); the forms-T3 S0–S3 artifacts on main; .claude/rules/autonomy.md (hard-stop list) + project-brief.md (READY brief governs) + cc-suite.md + staging-hygiene.md + loc-guardian.md
Allowed files: dev-memo/plan-go-live-readiness-00.md (gate-status refresh), docs/release/go-live-readiness-report.md (interim snapshot population) + the future exec lane's own review artifact under dev-memo/run/reviews/. NOTE: THIS authoring lane commits ONLY the queue governance (queue.md/linted/reviewed/governed) + reviews/queue-review-128.md; the future execution lane commits the two refreshed release docs (+ its own review artifact) and records its review-plan in the COMMIT MESSAGE (two-lane boundary, S3/T4 precedent). The future exec lane MUST NOT modify this lane's queue governance.
Forbidden files: apps/lawbar-desktop/ (NO code/UI/product buildout), services/ (NO persistence/OCR change), docs/contracts/ (NO contract change), services/case-box-persistence/src/sqlite/schema.ts (CURRENT_SCHEMA_VERSION stays 12), migrations/ (none), native/, package.json/package-lock.json of any package (no dependency; no signing/notarization config), dev-memo/run/intake/ (raw samples — never staged), docs/adr/ + docs/product/ + other dev-memo design docs (READ-ONLY — the brief/ADRs are evidence, not edited here), dev-memo/plan-batch-casebox-* + other plan docs (READ-ONLY evidence), scripts/ + .claude/ + .github/, Evidence-Genie-M0-Developer-Handover.md + xiaolai-dev-workflow-study.md; NO product UI; NO Electron signing/notarization; NO Forms T4/T5 impl; NO security-boundary audit execution; NO OCR-flake fix; NO A8/custody/JS-shim; NO FINAL GO/NO-GO verdict; NO new dependency; push limited to this WI's scoped branch + PR open; NO merge; NO branch delete
Gates: EVIDENCE/docs lane (docs-only; no product code): (1) broker /cc-suite:review-plan on this queue WI BEFORE the refresh, and again on the produced refresh; (2) `scripts/workflow/check-contract-integrity.sh` exit 0; `scripts/workflow/check-queue.sh` PASS; (3) NO product code, so no desktop/contract/persistence test run is required unless a code file is unexpectedly touched (it must not be). Forbidden-scope scan before commit: no CURRENT_SCHEMA_VERSION change; no migration/DDL; no apps/services/contracts/native change; no dependency; no signing/notarization config; no raw-sample commit; no Forms/security/OCR implementation; no FINAL go-live verdict emitted. STOP-points: the refresh would require clearing a STOP-AND-ASK gate or making a go-live decision → STOP and report (record open, do not resolve); review/audit requests implementation/gate-clearing/final-verdict → REJECT and report; any forbidden file would change → STOP. NO swiftpm-smoke (native untouched).
Acceptance criteria: check-queue and check-contract-integrity PASS and the committed diff CONTAINS only docs/evidence + governance files (no apps/services/contracts/native/dependency change) and CURRENT_SCHEMA_VERSION EQUALS 12 and no raw sample appears. The refreshed gate matrix in dev-memo/plan-go-live-readiness-00.md must RENDER each of the 21 gates re-classified (cleared/partial/open/STOP-AND-ASK) with current evidence + a refreshed roll-up; docs/release/go-live-readiness-report.md must CONTAIN an EXPLICITLY-INTERIM snapshot and MUST NOT emit a final GO/NO-GO verdict (blueprint gate 21 stays blocked); the three user hard-stops (framework/distribution/signing; 律师法 compliance; final go-live sign-off) must appear as unresolved STOP-AND-ASK; the report must DISTINGUISH true M0 blockers (the client's evidence-inventoried release-incompleteness, open release-docs gates, test/audit closure) from the 37 non-blocking deferred Lows (all Low, Safe=YES); forms-T3 COMPLETE + T4 post-v1 + T5 design-gated must appear; review-plan must RETURN READY.
Risk flags: LOW-MEDIUM (release-governance sensitivity, not code risk) — the danger is an artifact that OVER-CLAIMS readiness (emits a false GO, or silently clears a STOP-AND-ASK gate the user owns). Care points: (a) INTERIM-NOT-FINAL — no GO/NO-GO verdict; gate #21 stays blocked; (b) SURFACE-NOT-CLEAR the three hard-stops — they remain the user's STOP-AND-ASK; (c) EVIDENCE-BOUND blocker classification — the desktop client is classified release-incomplete ONLY to the extent an evidence inventory of current app surfaces/handlers proves (NOT a prescribed "token fixture" verdict; the blueprint's stale "NOT STARTED" is corrected against what is built); deferred Lows are non-blocking; (d) EVIDENCE-BOUND — every gate status cites a concrete artifact/commit; (e) DOCS-ONLY diff, no code/dependency/schema; (f) READ-ONLY brief/ADRs (evidence, not edited). If any cannot hold, STOP and report.
Depends on: none
Commit boundary: TWO separate lanes. (1) THIS authoring lane commits ONLY the queue governance (queue.md/linted/reviewed/governed) + reviews/queue-review-128.md — no release-doc edit, no code, no go-live decision. (2) The FUTURE execution lane commits ONLY the two refreshed release docs (dev-memo/plan-go-live-readiness-00.md + docs/release/go-live-readiness-report.md) + its own review artifact under dev-memo/run/reviews/, and records its cc-suite review-plan in the COMMIT MESSAGE; it does NOT re-stage this lane's queue governance / queue-review-128.md. NO apps/services/contracts/schema/migration/native/dependency file in either lane; NO staging of dev-memo/run/intake/ or untracked residue; push of the scoped WI branch + PR open; no merge; no branch delete. THIS authoring lane produces NO release-doc refresh and makes NO go-live decision — it only governs the readiness-refresh WI.
