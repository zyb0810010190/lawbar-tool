## WI-A3-T2: page-ratio normalization math (pure headless; A0.7-gated)
Type: IMPL
Requires-A07: yes
A07 custody mode: 9b. Human runs marker mint + A0.7-gated check-gates directly; agent does not receive the HMAC key. The implementation commit is BLOCKED until human-run gated verification reports PASS (A07_REQUIRED=1 check-a07-gate + A07_REQUIRED=1 check-gates both PASS with LAWBAR_A07_MARKER_HMAC_KEY in the human's shell, and no marker/ledger/key/evidence-run staged or committed). The agent never exports/echoes/commits the key, never mints a marker, never weakens or drops Requires-A07, never bypasses the gate.
Scope: Implement PURE headless page-ratio normalization/denormalization MATH only, per docs/adr/ADR-evidence-a3-anchor-link-contract.md §4 + INV-A3-4/INV-A3-5/INV-A3-7. Add a new dependency-free ESM module native/evidence-core/lib/page-ratio.mjs (pure functions; no I/O; no clock; no randomness; side-effect-free) and its unit tests native/evidence-core/tests/page-ratio.test.mjs (node:test + node:assert/strict). NO persistence, NO schema, NO migration, NO GRDB/SQLCipher, NO new dependency, NO native/evidence-core-swift change (no PDFKit/Swift), NO harness command change (cli.mjs/commands.mjs unchanged), NO apps/UI/product behavior, NO anchor resolver, NO link-status engine, NO replacement/quarantine behavior, NO export behavior, NO marker/key/gate behavior change, NO OCR/AI/cloud/auth/network. native/evidence-core is the pure-JS, dependency-free domain home (NOT the Swift/PDFKit package); review-plan must confirm this is the correct home for the pure math + tests; if review-plan says the math must live in native/evidence-core-swift (Swift/PDFKit package) or anywhere else, STOP and ask.
Required behavior (per ADR A3-CONTRACT-00 §4 + INV-A3-4/5/7): (1) normalizePageSpaceToRatio(rect, geometry) -> page_ratio rect: subtract the resolved page-box origin (boundsX/boundsY), divide by boundsWidth/boundsHeight, rotation-aware (supported rotations 0/90/180/270 per the ADR), in PDF page space (lower-left origin, 72 dpi); (2) denormalizeRatioToPageSpace(ratioRect, geometry) -> page-space rect, the exact inverse; (3) ratios derived ONLY from the persisted resolved box/bounds/rotation inputs (never viewport/screen coordinates); (4) canonical page_ratio components persisted at EXACTLY 12 fractional decimal places, round-half-to-even, serialized as a canonical fixed-point decimal string (always 12 fractional digits, leading 0. for <1, no exponent, no trailing-zero trim, -0 normalized to 0); (5) denormalize(normalize(rect)) round-trip within ABSOLUTE tolerance <= 1e-9 in page_ratio units; (6) FAIL-CLOSED (throw a typed error, never return a guessed value) on: absent geometry provenance, ambiguous geometry provenance, zero/negative page width or height, NaN/Infinity in any input, unsupported rotation, and out-of-representable-bounds rect if the ADR treats out-of-bounds as invalid; (7) never accept viewport/screen coordinates as a persistence source.
Source of truth: docs/adr/ADR-evidence-a3-anchor-link-contract.md (A3-CONTRACT-00 §4 headless API contracts; INV-A3-4 ratio derivation; INV-A3-5 deterministic 12-dp precision + 1e-9 roundtrip tolerance; INV-A3-7 fail-closed on absent/ambiguous provenance); dev-memo/plan-batch-casebox-evidence-a3-anchor-contract-00.md (A3-T2 is the first lane: pure math before persistence; §3 fixtures); docs/adr/ADR-evidence-a07-key-custody-operating-model.md (A07-KEY-00 — custody mode 9b + A3-T2 preconditions §4); native/evidence-core/{package.json,lib/commands.mjs,tests/cli.test.mjs} (the pure-JS module + test style to mirror); .claude/rules/evidence-genie.md (inv.6 anchors page-ratio vs captured geometry, never viewport); .claude/rules/autonomy.md (this impl lane is user-authorized under custody mode 9b).
Allowed files: native/evidence-core/lib/page-ratio.mjs (new pure module), native/evidence-core/tests/page-ratio.test.mjs (new tests), dev-memo/** (queue/governance/review/study/report artifacts)
Forbidden files: native/evidence-core/cli.mjs + native/evidence-core/lib/commands.mjs + native/evidence-core/package.json (NO harness/command/manifest change — pure new module + tests only), native/evidence-core-swift/** (NO Swift/PDFKit change), scripts/workflow/check-a07-gate.sh + a07_marker.py + a07-marker-write.sh + check-marker-guard.sh + check-gates.sh (NO gate/writer/validator behavior change), apps/**, services/**, docs/contracts/**, .claude/**, .github/**, AGENTS.md, CLAUDE.md, GEMINI.md, committed dev-memo/run/evidence/** marker/ledger/key/evidence-run files, committed keys, Evidence-Genie-M0-Developer-Handover.md (root intake), xiaolai-dev-workflow-study.md (root intake), dev-memo/run/override-reason.md (untracked residue — never staged); NO persistence/schema/migration, NO GRDB/SQLCipher or any new dependency, NO anchor resolver / link-status / replacement / export behavior, NO marker/key handling or key exposure, NO Evidence UI/product, NO OCR/AI/cloud/auth/network
Gates: broker /cc-suite:review-plan REQUIRED before impl (confirm native/evidence-core is the correct pure-math home; if it says the math must live in native/evidence-core-swift/Swift-PDFKit or asks for UI/persistence/DB-dependency/app-behavior/marker-key/gate changes, STOP and ask); broker /cc-suite:audit + /cc-suite:verify after impl AND after the human-run gated PASS; A0.7-gated under custody 9b — the implementation commit is BLOCKED until the human reports A07_REQUIRED=1 check-a07-gate PASS + A07_REQUIRED=1 check-gates PASS (human shell holds LAWBAR_A07_MARKER_HMAC_KEY) with no marker/ledger/key/evidence-run staged or committed; agent-side (non-secret) verification: npm --prefix native/evidence-core test passes (incl. the new page-ratio tests); scripts/workflow/check-contract-integrity.sh exits 0; npm --prefix apps/lawbar-desktop test passes 610/610; the A0.7 gate is intact (A07_REQUIRED=1 with no marker/key fails closed; not-required logic passes); the agent's own check-gates correctly FAILS CLOSED at the A0.7 gate because the governed queue declares Requires-A07: yes and the agent has no key (expected under 9b — the human's gated run is authoritative); no native/evidence-core-swift / apps / services / .claude / .github change; no committed marker/ledger/key/evidence-run; the harness cli.mjs/commands.mjs are byte-unchanged
Acceptance criteria: native/evidence-core/lib/page-ratio.mjs EXISTS and exports normalizePageSpaceToRatio + denormalizeRatioToPageSpace as pure functions; native/evidence-core/tests/page-ratio.test.mjs EXISTS and the suite PASSES (npm --prefix native/evidence-core test exits 0) covering: exact cases at the A0.7 synthetic page size 612x792; box-origin subtraction (non-zero origin); center rect; edge/corner rect; fractional rect; 12-dp canonicalization; round-half-to-even tie case; denormalize(normalize(rect)) round-trip within <= 1e-9; invalid dimensions (zero/negative) REJECTED (throws); NaN/Infinity REJECTED; absent geometry provenance REJECTED; ambiguous geometry provenance REJECTED; unsupported rotation REJECTED; viewport/screen coordinate input not accepted as a persistence source; rotation cases (0/90/180/270) matching the ADR transforms; the canonical ratio is a fixed-point 12-dp string with round-half-to-even; the module performs NO I/O and writes nothing under dev-memo/run/evidence/; scripts/workflow/check-contract-integrity.sh exits 0; npm --prefix apps/lawbar-desktop test passes 610/610; broker review-plan READY + audit no Critical/High/Medium + verify ALL CLOSED; the human-run A0.7-gated verification (custody 9b) reports PASS before the implementation commit; the committed diff is confined to native/evidence-core/lib/page-ratio.mjs + native/evidence-core/tests/page-ratio.test.mjs + dev-memo/** governance artifacts
Risk flags: A0.7-DEPENDENT IMPL (Requires-A07: yes), custody mode 9b — the implementation commit is BLOCKED until the human runs the A0.7-gated check-gates with the HMAC key and reports PASS; the agent never receives/echoes/commits the key, never mints a marker, never weakens/drops Requires-A07, never bypasses the gate. Pure math only: no persistence/schema/migration, no new dependency, no Swift/PDFKit change, no UI/product, no anchor resolver/link-status/replacement/export. broker review-plan + audit + verify REQUIRED (first A3 implementation lane; the coordinate contract is load-bearing). Must not weaken any Evidence invariant (page-ratio vs captured geometry, no viewport in persistence, deterministic precision, fail-closed provenance). No push, no merge.
Depends on: none
Commit boundary: governance commit separate (dev-memo/run/queue.md + queue.linted/reviewed/governed + dev-memo/run/reviews/queue-review-069.md); one implementation commit -- native/evidence-core/lib/page-ratio.mjs + native/evidence-core/tests/page-ratio.test.mjs, staged by exact path, ONLY after human-run gated PASS + broker audit/verify; no push; no merge
