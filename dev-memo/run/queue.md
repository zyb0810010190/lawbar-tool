## WI-ENA11-FIX1: bind + check isMarker in the A0.7 marker provenance (BATCH-FAIL remediation)
Type: IMPL
Scope: Fix the WI-ENA11-window BATCH-FAIL (audit-mqqlaozw-jns64v, M1): in scripts/workflow/a07_marker.py the marker field isMarker is written (true) but is NOT part of the HMAC-bound canonical payload and is NOT checked by validation, so editing isMarker: true -> false survives validation (violates "any touched marker fails"). Narrow fix (option B, user-chosen): bind isMarker into the canonical payload (add to PAYLOAD_FIELDS so it is covered by provenancePayloadHash + the HMAC) and make validate REQUIRE isMarker is true (boolean true). Editing only isMarker: true -> false must then fail validation (HMAC mismatch and/or the explicit check). Add a regression test (in scripts/workflow/a07-marker.test.sh): a genuine marker validates, then flipping ONLY isMarker true -> false fails validation. Keep the change minimal: NO marker-schema expansion beyond binding/checking the existing isMarker field; NO key-custody change; NO A0.7 harness-semantics change; NO EVW5 hooks; NO Evidence UI/product/apps change. The writer continues to set isMarker = true (now bound). The canonical serialization stays single-line fixed-order; isMarker is appended in a fixed position so writer + validator agree byte-for-byte. Do NOT delete/move the root intake files. Do NOT commit any marker/ledger/key/evidence-run file.
Source of truth: dev-memo/study reports + the batch-audit finding audit-mqqlaozw-jns64v (M1: isMarker unbound/unchecked); docs/adr/ADR-evidence-a07-marker-provenance.md (A07-MARK-00 §2 schema, §4 validation — any tamper must fail; isMarker=false cannot satisfy validity); scripts/workflow/a07_marker.py (the file being fixed) + scripts/workflow/a07-marker.test.sh (regression home); scripts/workflow/check-marker-guard.sh (delegates to validate; unchanged); .claude/rules/evidence-genie.md (invariants 3/10); .claude/rules/autonomy.md (BATCH-FAIL remediation, user-authorized fix-first with a single-use human.override per direct commit).
Allowed files: scripts/workflow/a07_marker.py (bind + check isMarker), scripts/workflow/a07-marker.test.sh (regression test), dev-memo/** (queue/governance/audit/study artifacts)
Forbidden files: scripts/workflow/check-marker-guard.sh (no change needed — it delegates to a07_marker.py validate), scripts/workflow/check-a07-gate.sh + check-a07-gate.test.sh (EVW5 scratch — NOT part of this WI, must not be committed), native/evidence-core-swift/** (NO change — no harness-semantics change), native/evidence-core/** (JS shim — NO change), .github/**, .claude/hooks/**, .claude/settings.json, .claude/rules/**, apps/**, services/**, docs/contracts/**, docs/product/**, AGENTS.md, CLAUDE.md, GEMINI.md, dev-memo/run/evidence/**, Evidence-Genie-M0-Developer-Handover.md (root), xiaolai-dev-workflow-study.md, package.json; NO key custody change; NO EVW5 hooks; NO marker-schema expansion beyond isMarker
Gates: broker /cc-suite:review-plan REQUIRED before impl (fixes the apex marker provenance — no self-review); if review-plan returns READY but scope expands beyond the isMarker bind/check + regression test, STOP and ask; broker /cc-suite:audit + /cc-suite:verify after impl; isMarker is in the canonical PAYLOAD_FIELDS (HMAC-bound); validate requires isMarker is true; a genuine marker validates; flipping ONLY isMarker true -> false fails validation; existing touched/copied/schema-only/wrong-key/duplicate-runId/traversal tests still pass; check-marker-guard.test.sh still passes; the native/evidence-core JS shim is byte-unchanged; no native change; scripts/workflow/check-contract-integrity.sh exits 0; swift test passes; npm --prefix apps/lawbar-desktop test passes (610/610); scripts/workflow/check-gates.sh returns GATES OK; commits use a single-use human.override (authorized) — one for governance, one for impl
Acceptance criteria: scripts/workflow/a07_marker.py has isMarker in PAYLOAD_FIELDS (so provenancePayloadHash + HMAC cover it) and cmd_validate requires isMarker is true (boolean); the writer still sets isMarker=true; a07-marker.test.sh adds a regression case proving a genuine marker validates and that flipping ONLY isMarker true->false fails validation; all prior marker + guard tests still pass; no committed marker/ledger/key; no EVW5 scratch staged; the native package + JS shim are byte-unchanged; check-contract-integrity PASS; swift test pass; npm desktop 610/610; check-gates GATES OK; broker audit no Critical/High/Medium and verify ALL CLOSED; the committed diff is confined to scripts/workflow/a07_marker.py + scripts/workflow/a07-marker.test.sh plus dev-memo/** governance artifacts
Risk flags: HIGH-RISK -- repairs the apex A0.7 marker provenance (a BATCH-FAIL Medium); narrow option-B fix (bind + check isMarker). broker review-plan + audit + verify required. Commits land via a single-use, logged human.override (user-authorized; one for governance, one for impl) because the BATCH-FAIL window cannot be closed (no BATCH-PASS) and the count gate blocks direct commits. After the fix, re-run the batch audit over the failed window INCLUDING the fix; only a BATCH-PASS re-run authorizes the closeout/marker advance. No key-custody change; no harness change; no EVW5; no UI/product.
Depends on: none (ENA11 is merged to main; this repairs the merged defect)
Commit boundary: two local commits via single-use human.override -- (1) governance: dev-memo/run/queue.* + the review artifact; (2) impl: scripts/workflow/a07_marker.py + scripts/workflow/a07-marker.test.sh; staged by exact path; no push
