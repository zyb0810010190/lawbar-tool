## WI-GATE10-CASEBOX-PERSISTENCE-SECURITY-BOUNDARY-AUDIT-00: full-scope security-boundary audit of case-box persistence (Type: EVIDENCE)
Type: EVIDENCE
Scope: Produce a READ-ONLY, review/audit-only security-boundary audit of the case-box persistence layer + its IPC/handler reach, to close M0 go-live **gate 10** (`docs/release/go-live-readiness-report.md` §1 gate 10 = OPEN; `dev-memo/plan-go-live-readiness-00.md` §1). The FUTURE execution lane: (a) runs the broker security audit (cc-suite `/cc-suite:audit` security persona, or `codex-security-review`, Path 1) over the case-box persistence boundary — `services/case-box-persistence/src/**` (the ~20 in-memory modules + the ~17 `sqlite/*RepoQueries.ts` + `openSqliteCaseBoxPersistence.ts` + `schema.ts` + `auditChain.ts` + `errors.ts`/`resolveTarget.ts`) AND the desktop boundary that reaches persistence / holds server authority (READ-ONLY): `apps/lawbar-desktop/src/caseBox/*Handlers.ts` + `handlerShared.ts` + the `dto/**` allowlists + `t3CatalogSource.ts` (persistence-reaching + tenant preflight) + `caseBoxRuntime.ts` (in-memory-vs-SQLite runtime selection) + `errorMap.ts` (safe error projection / message stripping) + `apps/lawbar-desktop/src/security/activeTenant.ts` + `activeActor.ts` (the server-authority tenant/actor injection sources) + `handlers.ts` (the handler barrel) + `electron/ipc/caseBoxHandlers.ts` + the case-box registration slice of `electron/main.ts` (`getCaseBoxRuntime()` → `registerCaseBoxIpcHandlers()` — the ipcMain registration seam); (b) verifies the boundary invariants: tenant/matter scoping (every read/write scoped by `tenant_id` + `matter_id`; `tenant_mismatch` / `unknown_matter` enforced BEFORE any cross-scope read; no cross-tenant / cross-matter leak; `party_side`/`litigation_position` not a scope bypass), the IPC/handler boundary (renderer never supplies `tenant_id`/`actor_user_id` — server injects; forbidden-field rejection → `invalid_payload`; the response projection/allowlist strips server-authority fields like `tenant_id`/`actor_user_id`/`custody_chain` before crossing IPC), the SQLite access patterns (parameterized queries — no injection; `payload_json` is the canonical row source; no read-layer re-sort masking a persistence bug; no FK from queue rows to `ocr_jobs`; the no-cross-column-CHECK repository-validation convention), and the boundary TEST coverage (the conformance + hardening suites actually exercise tenant/matter isolation + forbidden-field rejection + refusal codes); (c) classifies every finding Critical/High/Medium/Low; (d) produces a **security-boundary audit report** at `docs/release/casebox-persistence-security-audit-00.md` (per `.claude/rules/security-boundary.md` §7 sign-off convention) recording scope, the broker job id(s) + rawOutput sha256 (11-field cc-suite recording), each finding with severity + evidence (file:line), and — for any Critical/High/Medium — an EXPLICIT ESCALATION treated as a go-live blocker: the report PROPOSES a bounded follow-up security WI (a suggestion, NOT a self-authorized/executed queue item — the user authorizes it separately) per `.claude/rules/cc-suite.md` §"Audit remediation policy" (Medium+ are fixed-or-escalated, NEVER silently deferred into a self-authored next lane); a clean result records "gate 10 CLEARED pending user go-live approval" WITHOUT itself asserting go-live readiness (`.claude/rules/security-boundary.md` §"Go-live independence"). It writes ONLY that report + governance; it changes NO code, NO schema, NO contract, NO dependency, and makes NO go-live GO/NO-GO decision. If the audit surfaces a Critical/High that needs a code fix, that fix is a SEPARATE bounded security WI (plan-review → tests → implement → audit → verify per `.claude/rules/security-boundary.md` §"Required loop"), NOT performed in this audit lane. FORBIDDEN in this lane and the future exec lane: any persistence/contract/schema/migration/renderer/IPC/DTO implementation change; any `CURRENT_SCHEMA_VERSION` change; any OcrQueueError-code rename/removal/merge; any package/dependency change; any native change; Forms T4/T5 work; OCR-flake fix; product-UI buildout; signing/notarization; custody/marker/JS-shim/A8 work; a release GO/NO-GO decision. If review/audit asks for an implementation fix in this lane, REJECT (open a separate security WI) and report.
Source of truth: docs/release/go-live-readiness-report.md §1 gate 10 (OPEN — the audit target) + dev-memo/plan-go-live-readiness-00.md §1 gate 10; .claude/rules/security-boundary.md (§"In-scope work" tenant isolation/auth/sandbox; §"Required loop"; §"No silent surface changes" — stable OcrQueueError codes; §"Go-live independence"); .claude/rules/cc-suite.md (§"High-risk WIs" security = required broker; §"Audit remediation policy" — Mediums+ escalate not defer; 11-field recording); services/case-box-persistence/src/** (the audit target — READ-ONLY); apps/lawbar-desktop/src/caseBox/*Handlers.ts + handlerShared.ts + dto/** (the IPC boundary that reaches persistence — READ-ONLY); AGENTS.md §"Critical invariants" (contract=vocabulary owner, coordinator owns lifecycle, queue dedupe key, stable OcrQueueError codes, no queue→ocr_jobs FK, no read-layer re-sort, payload_json canonical); dev-memo/deferred-audit-findings.md (existing open Lows — the audit confirms none are boundary blockers); .claude/rules/autonomy.md + staging-hygiene.md + loc-guardian.md
Allowed files: docs/release/casebox-persistence-security-audit-00.md (the NEW security-boundary audit report the FUTURE lane produces) + that future lane's own review/audit artifact under dev-memo/run/reviews/. NOTE: THIS authoring lane commits ONLY the queue governance (queue.md/linted/reviewed/governed) + reviews/queue-review-130.md; the future execution lane commits the audit report (+ its own review artifact) and records its cc-suite audit/verify chain in the report + COMMIT MESSAGE (two-lane boundary, S3/T4/readiness precedent). The future exec lane MUST NOT modify this lane's queue governance and MUST NOT edit any persistence/contract/app source.
Forbidden files: services/ (READ-ONLY audit target — NO persistence/contract/OCR source change), services/case-box-persistence/src/sqlite/schema.ts (CURRENT_SCHEMA_VERSION stays 12; no DDL), docs/contracts/ (NO contract change), apps/lawbar-desktop/ (READ-ONLY audit target — NO renderer/IPC/DTO/handler code change), migrations/ (none), native/, package.json/package-lock.json of any package (no dependency), dev-memo/run/intake/ (raw samples — never staged), docs/adr/ + docs/product/ + other dev-memo/plan/design docs (READ-ONLY evidence), docs/release/ OTHER than the ONE new casebox-persistence-security-audit-00.md report, scripts/ + .claude/ + .github/, Evidence-Genie-M0-Developer-Handover.md + xiaolai-dev-workflow-study.md; NO persistence/contract/app implementation; NO schema/migration; NO OcrQueueError-code change; NO dependency; NO OCR-flake fix; NO Forms T4/T5; NO signing/notarization; NO custody/A8; NO release GO/NO-GO decision; push limited to this WI's scoped branch + PR open; NO merge; NO branch delete
Gates: EVIDENCE/security-audit lane (security = HIGH-RISK broker category per cc-suite; but read-only, no code): (1) broker /cc-suite:review-plan on this queue WI BEFORE the audit; (2) broker /cc-suite:audit (security persona) over the case-box persistence boundary + IPC handlers, Path 1, retrievable, 11-field recording; (3) if the audit is clean → record gate 10 CLEARED-pending-user-approval; if Critical/High/Medium → ESCALATE as a go-live blocker + PROPOSE a bounded follow-up security WI in the report for the user to authorize separately (do NOT fix in this lane, do NOT self-authorize/execute the follow-up); (4) broker /cc-suite:verify if the audit references prior findings; (5) scripts/workflow/check-contract-integrity.sh exit 0; scripts/workflow/check-queue.sh PASS. NO product test run required (read-only audit; no code change). Forbidden-scope scan before commit: no CURRENT_SCHEMA_VERSION change; no persistence/contract/app/native/dependency change; no OcrQueueError-code change; no raw-sample commit; no implementation; no GO/NO-GO decision. STOP-points: the audit finds a Critical/High requiring a code fix → do NOT fix here; ESCALATE + open a separate bounded security WI and report; review/audit asks for implementation → REJECT; any forbidden file would change → STOP. NO swiftpm-smoke (native untouched).
Acceptance criteria: check-queue and check-contract-integrity PASS and the committed diff CONTAINS only the audit report + governance/review files and CURRENT_SCHEMA_VERSION EQUALS 12 and no persistence/contract/app/native/dependency change appears and no raw sample appears. The audit report must RENDER: the exact READ-ONLY audit scope (persistence modules + IPC handlers), the broker audit job id(s) + rawOutput sha256, a per-finding list with severity + file:line evidence, tenant/matter-scoping + IPC-boundary + SQLite-access + test-coverage verification results, and — for any Critical/High/Medium — a concrete bounded follow-up security WI; a clean audit must STATE "gate 10 CLEARED pending user go-live approval" and MUST NOT itself assert go-live readiness; review-plan must RETURN READY.
Risk flags: MEDIUM-HIGH (security-boundary sensitivity; but read-only, no code). Care points: (a) READ-ONLY — the audit inspects + reports; any fix is a SEPARATE security WI (plan-review→tests→implement→audit→verify); (b) MEDIUMS+ ESCALATE, never silently defer, per cc-suite audit-remediation policy; (c) NO surface change — OcrQueueError codes + thrown shapes + `tenant_mismatch`/`unknown_matter` semantics are stable (an audit finding proposing to rename/collapse them needs an ADR + user approval, not a silent fix); (d) GO-LIVE INDEPENDENCE — clearing gate 10 does NOT imply go-live; the final verdict stays the user's; (e) DOCS-ONLY diff (the report), no code/dependency/schema; (f) INPUT-ONLY samples never staged. If any cannot hold, STOP and report.
Depends on: none
Commit boundary: TWO separate lanes. (1) THIS authoring lane commits ONLY the queue governance (queue.md/linted/reviewed/governed) + reviews/queue-review-130.md — no audit report, no code, no go-live decision. (2) The FUTURE execution lane commits ONLY the ONE new security-boundary audit report (docs/release/casebox-persistence-security-audit-00.md) + its own review artifact under dev-memo/run/reviews/, and records its cc-suite audit/verify chain in the report + COMMIT MESSAGE; it does NOT re-stage this lane's queue governance / queue-review-130.md and does NOT edit any persistence/contract/app source. NO services/apps/contracts/schema/migration/native/dependency file in either lane; NO staging of dev-memo/run/intake/ or untracked residue; push of the scoped WI branch + PR open; no merge; no branch delete. THIS authoring lane produces NO audit report and makes NO go-live decision — it only governs the gate-10 security-boundary audit WI.
