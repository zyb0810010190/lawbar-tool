## WI-FORMS-T3-S0-CONTRACT-FIELDS-IMPLEMENTATION-00: implement the S0 Option A payload-only contract fields (Type: IMPL)
Type: IMPL
Scope: Implement EXACTLY the ADR-approved S0 Option A additive payload-only contract fields (`dev-memo/adr-forms-t3-s0-schema.md` §4 Option A, BINDING): evidence-item optional properties `evidence_title` (string, minLength 1), `proof_statement` (string, minLength 1; whitespace-only/empty input NORMALIZED TO ABSENT at the persistence append path before validation), `display_order` (integer, minimum 0, absence-only — no null branch); matter optional property `litigation_position` (enum plaintiff|defendant). Mechanics: (a) add the four optional properties to the two contract schema JSONs (NOT added to `required`; no other schema text changed); (b) regenerate the committed generated types via the contract package's gen:types; (c) ADD (never mutate) contract fixtures — valid: evidence item with all three new fields, matter with litigation_position; invalid: evidence with empty-string proof_statement, evidence with empty-string evidence_title, evidence with null display_order, evidence with negative display_order, evidence with non-integer display_order, matter with out-of-enum litigation_position; (d) add explicit fixture tests to `tests/contract.test.mjs` (NOT `tests/validators.test.mjs`, which is a pre-existing 1333-LOC over-threshold file this WI must not grow); (e) add the whitespace-only proof_statement -> absent normalization in `services/case-box-persistence/src/inMemoryEvidence.ts` `prepareAppendEvidenceItem` (before `validateEvidenceItem`; own-property check via `Object.prototype.hasOwnProperty.call`, structured-clone-then-delete so caller input is never mutated; non-empty values preserved VERBATIM — no trimming of content; non-string values left for AJV rejection); (e2) classify `litigation_position` in the desktop matter DTO boundary `apps/lawbar-desktop/src/caseBox/dto/matter.ts` — ADD it to `CREATE_MATTER_FORBIDDEN_FIELDS` ONLY (not renderer-suppliable and NOT added to `MATTER_RESPONSE_FIELDS`; no renderer exposure in M0), the minimal additive classification the schema-drift catcher `tests/dto-contract.test.mjs` requires for every new matter schema property, PLUS the matching single addition to that test's hard-coded expected CREATE_MATTER_FORBIDDEN_FIELDS list in `apps/lawbar-desktop/tests/dto-contract.test.mjs` (no other test change; no renderer exposure; no MATTER_RESPONSE_FIELDS addition); (e3) VERSIONING RESOLUTION (supersedes the ADR §6 "bump the contract package version (additive minor)" line for this repo): the contract package version REMAINS `0.1.0` — the package is a private committed tarball pinned by the DESKTOP-DEPS-00 convention (docs/adr/ADR-evidence-desktop-internal-deps-packaging.md); drift protection comes from the payload-hash tarball guard, not semver; NO package.json version-field edit; (f) add targeted persistence tests to the existing `tests/hardening-evidence.test.mjs` + `tests/impl-parity-matter.test.mjs` (or equivalent existing files): new-field round-trips (both in-memory and SQLite), normalization behavior, legacy rows without the fields stay valid, invalid litigation_position rejected, CURRENT_SCHEMA_VERSION remains 12, no T4/T5 fields introduced, filename/notes not promoted into display truth; (g) refresh the desktop internal tarballs (`npm --prefix apps/lawbar-desktop run pack:internal`) + package-lock integrity if changed, because contract+persistence source are bundled and `check-internal-tarballs` is a drift gate. FORBIDDEN in this lane: any SQLite DDL/column/table change, any `CURRENT_SCHEMA_VERSION` change, any migration file, any T3 export/catalog-model generation, DOCX/PDF/renderer/UI-preview work, T4/T5 fields or behavior, native/custody/JS-shim/A8 work, mutation of existing fixtures, and staging anything under `dev-memo/run/intake/**`. If the current code CANNOT support the fields without DDL/migration/version change, STOP and report (do not expand scope). If review/audit asks for renderer/DOCX/UI/migration/T4-T5/broader forms work, REJECT as out of scope and report.
Source of truth: dev-memo/adr-forms-t3-s0-schema.md (BINDING Option A design: field names, shapes, absence-only display_order, empty-as-absent proof_statement, enum litigation_position, no-DDL/no-migration/no-version-bump); dev-memo/plan-forms-t3-evidence-catalog-00.md (parent plan §5 test expectations); dev-memo/forms-spec-a10-t3-t5-sample-adr-00.md §H DR-00 (页码 = exhibit_page_range physical range; 卷X页Y supporting-only; T4/T5 gated); docs/adr/ADR-evidence-desktop-internal-deps-packaging.md (tarball repack duty); services/case-box-persistence/src/sqlite/schema.ts (READ-ONLY — proves no DDL needed; CURRENT_SCHEMA_VERSION stays 12); .claude/rules/{evidence-genie,cc-suite,autonomy,execution-discipline,staging-hygiene,loc-guardian}.md
Allowed files: docs/contracts/case-box-contract/schemas/case-box-evidence-item.schema.json, docs/contracts/case-box-contract/schemas/case-box-matter.schema.json, docs/contracts/case-box-contract/src/generated/case-box-evidence-item.ts (regen), docs/contracts/case-box-contract/src/generated/case-box-matter.ts (regen), docs/contracts/case-box-contract/fixtures/valid/*.json (NEW files only), docs/contracts/case-box-contract/fixtures/invalid/*.json (NEW files only), docs/contracts/case-box-contract/tests/contract.test.mjs, services/case-box-persistence/src/inMemoryEvidence.ts, services/case-box-persistence/tests/hardening-evidence.test.mjs, services/case-box-persistence/tests/impl-parity-matter.test.mjs, apps/lawbar-desktop/src/caseBox/dto/matter.ts (ONLY the additive litigation_position entry in CREATE_MATTER_FORBIDDEN_FIELDS), apps/lawbar-desktop/tests/dto-contract.test.mjs (ONLY the matching litigation_position addition to the hard-coded expected forbidden list), apps/lawbar-desktop/dist-tarballs/case-box-contract-0.1.0.tgz, apps/lawbar-desktop/dist-tarballs/case-box-persistence-0.1.0.tgz, apps/lawbar-desktop/dist-tarballs/manifest.json, apps/lawbar-desktop/package-lock.json (tarball integrity refresh only), dev-memo/run/queue.md, dev-memo/run/queue.linted, dev-memo/run/queue.reviewed, dev-memo/run/queue.governed, dev-memo/run/reviews/queue-review-122.md
Forbidden files: services/case-box-persistence/src/sqlite/schema.ts (NO DDL/CURRENT_SCHEMA_VERSION change), services/case-box-persistence/src/sqlite/*RepoQueries.ts (wholesale payload_json writes unchanged), migrations/** (none exist; none created), docs/contracts/case-box-contract/src/{ajv-instance,validateEvidenceItem,validateMatter,loadSchemas,invariants,matter-type-invariants}.ts (validators are pure AJV — unchanged), docs/contracts/case-box-contract/tests/validators.test.mjs (pre-existing 1333-LOC over-threshold file — do not grow), docs/contracts/case-box-contract/fixtures/**(existing files — never mutated), package.json manifests of any package (no script/dependency/version-field change), apps/lawbar-desktop/src/** + renderer/** EXCEPT the single allowed dto/matter.ts classification line (NO other UI/IPC/DTO change — litigation_position is NOT added to MATTER_RESPONSE_FIELDS and not exposed to the renderer in M0), native/**, dev-memo/run/intake/** (raw samples — never staged), dev-memo/run/evidence/**, docs/adr/** + docs/product/** + dev-memo/*.md design docs (READ-ONLY), scripts/** + .claude/** + .github/**, Evidence-Genie-M0-Developer-Handover.md + xiaolai-dev-workflow-study.md; NO A8; NO T3 export generation; NO T4/T5; NO DOCX/PDF/renderer; NO custody/marker; NO JS shim; push limited to this WI's scoped branch + PR open; NO merge; NO branch delete
Gates: FULL chain (persistence/contract implementation = HIGH-RISK category): (1) broker /cc-suite:review-plan on this queue WI + the ADR binding BEFORE implementation; (2) implement; (3) tests — `npm --prefix docs/contracts/case-box-contract test` PASS, `npm --prefix services/case-box-persistence test` PASS, `npm --prefix apps/lawbar-desktop test` PASS (tarball/package registration changed -> full desktop suite), `npm --prefix apps/lawbar-desktop run check:internal-tarballs` PASS, `scripts/workflow/check-contract-integrity.sh` exit 0, `scripts/workflow/check-queue.sh` PASS; (4) broker /cc-suite:audit on the implementation diff; (5) fix Critical/High/Medium findings in-scope; (6) broker /cc-suite:verify consuming the audit artifact; (7) staged-file check (exact paths only; raw samples + residue never staged). STOP-points: DDL/migration/version change appears necessary -> STOP and report; review/audit requests out-of-scope work -> REJECT and report; any forbidden file would change -> STOP. Batch is 1/3 (marker 9a59d39); NOT due — no closeout this lane. No swiftpm-smoke (native untouched).
Acceptance criteria: the two contract schemas contain the four new OPTIONAL properties exactly as the ADR specifies (evidence_title minLength 1; proof_statement minLength 1; display_order integer minimum 0 with no null branch; litigation_position enum plaintiff|defendant) with `required` lists unchanged; generated types regenerated and committed; NEW valid+invalid fixtures added (no existing fixture mutated) and explicit tests for them added to contract.test.mjs; `prepareAppendEvidenceItem` normalizes whitespace-only/empty proof_statement to ABSENT before validation without mutating caller input and preserves non-empty values verbatim; persistence tests prove round-trip of all four fields (in-memory + SQLite), legacy-row validity, empty-proof normalization, invalid-enum rejection, CURRENT_SCHEMA_VERSION === 12, and no T4/T5 field leakage; desktop tarballs repacked and `check:internal-tarballs` PASSES; ALL package test suites listed in Gates PASS; broker review-plan READY, audit findings C/H/M all closed, verify ALL CLOSED; committed diff contains ONLY Allowed files; raw samples remain untracked.
Risk flags: MEDIUM — additive-only contract + one normalization guard, but it is the first product-code lane of the forms track and touches the contract package consumed by persistence + desktop (tarball repack required, full desktop suite required). Care points: (a) ADDITIVE ONLY — required lists, existing properties, validators, and repo queries unchanged; (b) NORMALIZE-BEFORE-VALIDATE placement so empty proof_statement becomes absent rather than a minLength rejection, and VERBATIM preservation otherwise; (c) FIXTURE DISCIPLINE — add, never mutate; schema+semantic tests updated together per AGENTS.md invariant; (d) LOC — do not grow validators.test.mjs (pre-existing 1333-LOC violation; new tests go to contract.test.mjs at 778 LOC); (e) TARBALL DRIFT — repack after source change or check-internal-tarballs fails; (f) INPUT-ONLY SAMPLES — never staged. If any cannot hold, STOP and report.
Depends on: none
Commit boundary: governance (mark-reviewed + govern) run STANDALONE before the commit (never bundled with git commit). ONE implementation commit — the Allowed files above staged by exact path (contract schemas + regenerated types + new fixtures + contract tests + inMemoryEvidence normalization + persistence tests + repacked tarballs/manifest + package-lock integrity if changed + queue governance artifacts + reviews/queue-review-122.md); NO schema.ts/migration/manifest-script/UI/native file; NO staging of dev-memo/run/intake/** or untracked residue; push of the scoped WI branch + PR open authorized by the user's directive; no merge; no branch delete.
