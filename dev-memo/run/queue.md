## WI-EVIDENCE-A10-LIVE-PIPELINE-WIRING-00: wire the live export path to the A10 contract (Type: IMPL)
Type: IMPL
Scope: Wire the LIVE export path (`apps/lawbar-desktop/src/caseBox/linkHandlers.ts` `exportLinkCitationsHandler`) to CONSUME the already-merged A10 export contract (A10-T1 citation-render, A10-T2 hyperlink degradation, A10-T6 CanonicalExportModel + golden + the native golden-export gate) — without REDEFINING any of it. Add a NEW narrow apps-layer adapter `apps/lawbar-desktop/src/caseBox/export/a10LivePipeline.ts` exporting `buildLiveCanonicalExport(result: ExportCitationResult, exportType?) -> { canonicalModel: CanonicalExportModel; canonicalModelSha256: string }` that is PURE COMPOSITION of the existing helpers: `toA10RenderedCitations(result)` (A10-T1) -> `buildCanonicalExportModel({exportType, rendered})` (A10-T6, which internally drives A10-T2 `toA10HyperlinkCitations` for the text-or-flag rows and excludes `internalHref`) -> `canonicalModelSha256(model)` (A10-T6). It adds NO citation-rendering / href-validation / serialization logic of its own. Wire it into `exportLinkCitationsHandler`: after `buildExportCitations`, return `{ ...result, canonicalExport: buildLiveCanonicalExport(result) }` — ADDITIVE (the pre-existing `citations` / `byFlag` fields of the services `ExportCitationResult` are preserved verbatim for existing renderer/test consumers). Extend the apps-layer IPC DTO `ExportLinkCitationsResult` (`apps/lawbar-desktop/src/caseBox/dto/link.ts`) additively to carry `canonicalExport`. This is the A10 live-pipeline wiring; NO new IPC channel, NO renderer/electron change, NO `.docx`/PDF rendering, NO A8, NO forms T3/T4/T5, NO schema/persistence/services change, NO custody/marker, NO confidential fixtures, NO JS shim, NO A1T6-AUD-L1 hardening, NO golden-fixture rewrite. The live canonical model is byte-identical to what the native A10 golden-export gate validates (same A10-T6 builder), so the live path is compatible with that gate by construction. If wiring the live path requires editing the A10-T1/T2/T6 modules / the golden fixture / services / schema / inventing new citation logic, STOP and report.
Source of truth: docs/adr/ADR-evidence-a10-court-fileable-export.md (A10-DESIGN-00 §3 single-source/never-dropped, §4 CanonicalExportModel, §6 degradation, §8 reproducibility/A10-T6); apps/lawbar-desktop/src/caseBox/export/{a10CitationContract,a10HyperlinkDegradation,a10CanonicalExportModel}.ts (the A10-T1/T2/T6 modules to CONSUME — READ-ONLY); apps/lawbar-desktop/tests/fixtures/a10-golden-canonical-export.json (the golden — READ-ONLY); apps/lawbar-desktop/src/caseBox/linkHandlers.ts (the live export handler entry point); apps/lawbar-desktop/src/caseBox/dto/link.ts (the IPC result DTO to extend additively); services/case-box-persistence (buildExportCitations / ExportCitationResult — READ-ONLY consumed type); native/evidence-core-swift (the A10 golden-export gate — READ-ONLY, regression-only); .claude/rules/{evidence-genie,cc-suite,autonomy,execution-discipline,staging-hygiene,security-boundary,client-local-first,loc-guardian}.md
Allowed files: apps/lawbar-desktop/src/caseBox/export/a10LivePipeline.ts (NEW; exact name may adjust per review-plan within apps/lawbar-desktop/src/caseBox/export/), apps/lawbar-desktop/tests/a10-live-pipeline.unit.test.mjs (NEW), apps/lawbar-desktop/tests/casebox-link-roundtrip.electron.test.mjs (ADDITIVE assertion only — lock the live handler's canonicalExport attachment in the real export round-trip; per review-plan-mr0qskbj Low), apps/lawbar-desktop/src/caseBox/linkHandlers.ts (wire the adapter into exportLinkCitationsHandler — additive return only), apps/lawbar-desktop/src/caseBox/dto/link.ts (extend ExportLinkCitationsResult additively to carry canonicalExport), apps/lawbar-desktop/package.json (ONLY a one-line test-script registration — surgical Edit, NO reformat), dev-memo/run/queue.md, dev-memo/run/queue.linted, dev-memo/run/queue.reviewed, dev-memo/run/queue.governed, dev-memo/run/reviews/queue-review-115.md
Forbidden files: apps/lawbar-desktop/src/caseBox/export/{a10CitationContract,a10HyperlinkDegradation,a10CanonicalExportModel}.ts (the A10-T1/T2/T6 modules — READ-ONLY/imported; NO edit), apps/lawbar-desktop/tests/fixtures/a10-golden-canonical-export.json (the golden — READ-ONLY; NO rewrite), native/** (A10 golden-export + a07/a1/a3 gates — READ-ONLY/regression-only), native/evidence-core/** (JS shim — READ-ONLY), services/** + docs/contracts/** (NO services/schema/contract change; ExportCitationResult/buildExportCitations consumed READ-ONLY), services/case-box-persistence/src/sqlite/schema.ts (NO schema/CURRENT_SCHEMA_VERSION change), apps/lawbar-desktop/renderer/** (NO renderer UI change — the new field is additive + ignored), apps/lawbar-desktop/electron/** (NO Electron IPC/preload/channel change), apps/lawbar-desktop/src/caseBox/handlerShared.ts (NO new channel), apps/lawbar-desktop/{package-lock.json,dist-tarballs/**} (NO dependency/tarball change), scripts/** + .claude/** + .github/** + migrations/**, docs/adr/** + docs/reference/** (READ-ONLY), dev-memo/run/evidence/** (gitignored custody marker/ledger — never touched/staged), committed keys, dev-memo/run/reports/** + dev-memo/run/override-reason.md + dev-memo/run/autonomous-next.md (untracked run-state — never staged/edited), Evidence-Genie-M0-Developer-Handover.md + xiaolai-dev-workflow-study.md (root intake — READ-ONLY), apps/lawbar-desktop/dist/** (gitignored build output — never staged); NO A8 snapshot/seal/manifest/ExportPreview; NO T3/T4/T5 forms; NO .docx/PDF rendering; NO new IPC channel; NO custody/marker; NO HMAC/key; NO confidential/client/court fixture; NO A1T6-AUD-L1 hardening; NO push/merge/branch-delete
Gates: broker /cc-suite:review-plan REQUIRED before implementation — confirm: the live wiring CONSUMES A10-T1/T2/T6 (pure composition) without editing them / the golden / services / schema; attaching the canonical model ADDITIVELY (preserving citations/byFlag) is non-breaking for existing consumers; the IPC DTO extension is apps-layer (not a schema/persistence change); reusing buildCanonicalExportModel guarantees compatibility with the native golden-export gate; the one-line package.json test registration is acceptable; NO renderer/electron/new-channel/services/schema change. STOP-points: (1) if wiring requires editing the A10-T1/T2/T6 modules or the golden fixture -> STOP; (2) if it requires a services/schema/persistence change -> STOP; (3) if it requires a new IPC channel / renderer / electron / preload change -> STOP; (4) if it requires .docx/PDF rendering, A8, or forms -> STOP; (5) if it would weaken A1/A3/A8/A10 or the A0.7/A1-T6/A3-T10/A10-golden-export native gates -> STOP; (6) if custody/key handling would be exposed -> STOP. If review-plan NEEDS-FIX, fix-forward; if a blocker, STOP. After implementation: targeted `node --test apps/lawbar-desktop/tests/a10-live-pipeline.unit.test.mjs` GREEN; A10-T1/T2/T6 targeted regression GREEN; the native A10 golden-export gate still prints `pass` exit 0 (compatibility); `npm --prefix apps/lawbar-desktop test` GREEN (incl. existing link/export suites); `scripts/workflow/check-contract-integrity.sh` exits 0; native surfaces untouched (else swiftpm-smoke); broker /cc-suite:audit + /cc-suite:verify (no Critical/High/Medium open before commit). No A0.7 custody. Batch is 1/3 (marker 033e1cb); NOT due — no closeout this lane.
Acceptance criteria: a NEW adapter `a10LivePipeline.ts` exports `buildLiveCanonicalExport` (pure composition of A10-T1 toA10RenderedCitations + A10-T6 buildCanonicalExportModel + canonicalModelSha256) and `LIVE_EXPORT_TYPE`; `exportLinkCitationsHandler` returns `{ ...result, canonicalExport: { canonicalModel, canonicalModelSha256 } }` (additive — citations/byFlag preserved); `ExportLinkCitationsResult` additively carries `canonicalExport`; tests PROVE: (a) a valid citation flows through to a 卷X页Y canonical row + 64-hex sha256; (b) a missing hyperlink target degrades to a text-only/reviewable row (no internalHref/href in the model, citation NOT dropped); (c) a flagged/unsafe link yields a flag row with no fake/empty/unsafe href; (d) no href/nav metadata leaks into the live canonical serialization; (e) deterministic (same services result -> identical model + sha); (f) total + order-preserving (every services citation -> exactly one row); (g) the adapter output is byte-identical to calling A10-T6 directly (redefines nothing); (h) custom exportType honored. The native A10 golden-export gate still passes (compatibility). NO edit to a10CitationContract.ts/a10HyperlinkDegradation.ts/a10CanonicalExportModel.ts/the golden fixture/native/services/schema; NO new IPC channel/renderer/electron change; NO A8/forms/.docx/PDF/custody/confidential; `CURRENT_SCHEMA_VERSION` unchanged; `dev-memo/run/evidence/**` untouched; `check-contract-integrity` exits 0; broker review-plan READY + audit no Critical/High/Medium + verify ALL CLOSED; committed diff confined to Allowed files (package.json diff = exactly the one-line test registration).
Risk flags: HIGH-RISK Evidence-invariant lane (A10 court-fileable export; live export now produces the court-facing canonical model) — but a STANDALONE additive adapter + handler wiring + DTO extension + tests only; NO edit to the A10 contract modules / golden fixture / native gate / services / schema, NO new IPC channel, NO renderer/electron change, NO .docx/PDF, NO A8, NO forms. Load-bearing care points: (a) CONSUME-NOT-REDEFINE — pure composition of A10-T1/T2/T6; add no citation/href/serialization logic; (b) ADDITIVE — preserve the existing citations/byFlag result fields so existing renderer/test consumers do not break; (c) COMPATIBLE — reuse buildCanonicalExportModel so the live model is byte-identical to the native golden-export gate's input; (d) AUTHORITY/NEVER-DROPPED/NO-HREF preserved by reuse (text-or-flag rows, internalHref excluded, total+deterministic); (e) do NOT edit the A10 modules / golden fixture / native / services / schema / add a channel / touch A8/forms/custody. If any cannot hold, STOP and report. No push/merge/branch-delete.
Depends on: none
Commit boundary: governance (mark-reviewed + govern) run STANDALONE before the commit (never bundled with git commit). ONE implementation commit — apps/lawbar-desktop/src/caseBox/export/a10LivePipeline.ts + apps/lawbar-desktop/tests/a10-live-pipeline.unit.test.mjs + apps/lawbar-desktop/tests/casebox-link-roundtrip.electron.test.mjs (additive assertion) + apps/lawbar-desktop/src/caseBox/linkHandlers.ts + apps/lawbar-desktop/src/caseBox/dto/link.ts + apps/lawbar-desktop/package.json (one-line test registration) + governance artifacts (queue.md/.linted/.reviewed/.governed + reviews/queue-review-115.md), staged by exact path; NO services/schema/native/renderer/electron/contract/dependency/tarball file; NO edit to the A10-T1/T2/T6 modules / golden fixture; NO marker/evidence/key/report file; no push; no merge; no branch delete.
