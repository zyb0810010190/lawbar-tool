## WI-EVIDENCE-A10-T2-HYPERLINK-DEGRADATION-00: implement A10 hyperlink degradation (Type: IMPL)
Type: IMPL
Scope: Implement the A10-T2 hyperlink-degradation slice, per `docs/adr/ADR-evidence-a10-court-fileable-export.md` (§3, §6, §13 step 2). NO full CanonicalExportModel / A10-T6 golden, NO live export-pipeline wiring, NO T3/T4/T5 forms, NO A8, NO schema/native/UI/IPC/services change, NO custody/marker, NO A1T6-AUD-L1 hardening. Builds on the merged A10-T1 contract (`apps/lawbar-desktop/src/caseBox/export/a10CitationContract.ts`: `A10RenderedCitation` { citation 卷X页Y | flag } + canonical `A10ExportCitationFlag`). A10-T2 (ADR §6/§24): "every in-app link → textual 卷X页Y OR the matching ExportCitationFlag; bijection link↔citation/flag; never dropped/silently-wrong." Add a NEW sibling apps-layer module `apps/lawbar-desktop/src/caseBox/export/a10HyperlinkDegradation.ts` that maps an `A10RenderedCitation` to an `A10HyperlinkCitation` { linkId, citationFormatVersion, exportTemplateVersion, text: string|null, internalHref: string|null, flag: A10ExportCitationFlag|null }. CRITICAL (review-plan-mr09ymaa fix #1): the court-fileable AUTHORITY is the textual `text` (卷X页Y) OR the `flag` — NEVER the href. `internalHref` is APP-INTERNAL, NON-AUTHORITATIVE navigation metadata ONLY (named `internalHref` to signal this); it MUST NOT become the court-fileable authority and MUST NOT be rendered into final court `.docx`/PDF artifacts (a later WI must explicitly authorize any such rendering). Rules: (a) a CLEAN citation -> `text` = the A10-T1 卷X页Y (reused verbatim, single-source) + `internalHref` = a SAFE internal nav href minted ONLY from the citation's own documentId+physicalPageIndex, flag null; (b) a clean citation MISSING a hyperlink target (documentId or physicalPageIndex null, or an unmintable/unsafe target) -> `text` present, `internalHref` null (text-only degradation — citation NOT dropped, still human-readable + court-fileable via text), flag null; (c) a FLAGGED citation -> `text` null, `internalHref` null, `flag` set (the explicit, reviewable degraded state); (d) a malformed/unsafe href is NEVER emitted. Mint via a MINT-ONLY helper `mintInternalCitationHref(documentId, physicalPageIndex)` -> `lawbar://citation/<encodeURIComponent(documentId)>/<physicalPageIndex>` returning null when documentId is empty or physicalPageIndex is not a NON-NEGATIVE SAFE INTEGER; validate via `isSafeCitationHref(href)` with STRICT rules (review-plan-mr09ymaa fix #2 — NOT prefix-alone): reject null/empty/whitespace/control-chars; reject any non-internal scheme (http(s)/javascript/data/file/mailto/vbscript/etc., case-insensitively); accept ONLY `lawbar://citation/<docSeg>/<idx>` where docSeg is a single encoded path segment that round-trips (encodeURIComponent(decodeURIComponent(seg))===seg), contains NO `.`/`..`/`/`/`\\` after decode and no encoded-traversal (`%2e`/`%2f`/`%5c`), and idx is `^[0-9]+$`. The mapper emits `internalHref` ONLY if the minted value passes `isSafeCitationHref` (else null). Invariants: total + bijective (every input -> EXACTLY one of {text, optionally with internalHref} XOR {flag}); `internalHref` non-null ONLY when `text` non-null AND the minted href is safe; existing A10-T1 behavior unchanged (regression). Standalone module + unit tests; NOT wired into the live exportLinkCitationsHandler IPC path (a later slice). Deterministic (pure; no timestamps/machine-paths/locale). If A10-T2 cannot be implemented without editing services / inventing CanonicalExportModel / wiring the live pipeline, STOP and report.
Source of truth: docs/adr/ADR-evidence-a10-court-fileable-export.md (A10-DESIGN-00 §3 single-source + never-dropped, §6 degradation total+bijective, §13 step-2 A10-T2); apps/lawbar-desktop/src/caseBox/export/a10CitationContract.ts (the merged A10-T1 contract to extend — A10RenderedCitation, A10ExportCitationFlag, CITATION_FORMAT_VERSION/EXPORT_TEMPLATE_VERSION, A10CitationContractError); apps/lawbar-desktop/tests/a10-citation-contract.unit.test.mjs (the A10-T1 test pattern); docs/adr/ADR-evidence-a3-export-degradation.md (A3-EXPORT-00 degradation the flags come from); apps/lawbar-desktop/package.json (test-script registration pattern); .claude/rules/{evidence-genie,cc-suite,autonomy,execution-discipline,staging-hygiene,security-boundary,client-local-first}.md
Allowed files: apps/lawbar-desktop/src/caseBox/export/a10HyperlinkDegradation.ts (NEW; exact name may adjust per review-plan within apps/lawbar-desktop/src/caseBox/export/), apps/lawbar-desktop/tests/a10-hyperlink-degradation.unit.test.mjs (NEW), apps/lawbar-desktop/package.json (ONLY a one-line test-script registration — surgical Edit, NO reformat; review-plan to confirm), dev-memo/run/queue.md, dev-memo/run/queue.linted, dev-memo/run/queue.reviewed, dev-memo/run/queue.governed, dev-memo/run/reviews/queue-review-112.md
Forbidden files: apps/lawbar-desktop/src/caseBox/export/a10CitationContract.ts (the A10-T1 module — READ-ONLY/imported; NO edit unless review-plan explicitly approves a minimal additive export), apps/lawbar-desktop/src/caseBox/linkHandlers.ts + dto/link.ts (live export handler/DTO — NO wiring this lane), services/** + docs/contracts/** (NO services/schema/contract change — services renderer READ-ONLY), services/case-box-persistence/src/sqlite/schema.ts (NO schema/CURRENT_SCHEMA_VERSION change), native/** (NO native/Swift change; A0.7/A1-T6/A3-T10 gates regression-only), apps/lawbar-desktop/renderer/** (NO renderer UI change), apps/lawbar-desktop/electron/** (NO Electron IPC/preload change), apps/lawbar-desktop/{package-lock.json,dist-tarballs/**} (NO dependency/tarball change), scripts/** + .claude/** + .github/** + migrations/**, docs/adr/** (READ-ONLY), dev-memo/design/** (NO separate artifact unless review-plan requires), dev-memo/run/evidence/** (gitignored custody marker/ledger — never touched/staged), committed keys, dev-memo/run/reports/** + dev-memo/run/override-reason.md + dev-memo/run/autonomous-next.md (untracked run-state — never staged/edited), Evidence-Genie-M0-Developer-Handover.md + xiaolai-dev-workflow-study.md (root intake — READ-ONLY), apps/lawbar-desktop/dist/** (gitignored build output — never staged); NO CanonicalExportModel/golden/forms/preview-hash/docx-pdf/A8/live-pipeline implementation; NO custody/marker; NO HMAC/key request/receive/print/persist/stage; NO confidential/client/court fixture; NO push/merge/branch-delete
Gates: broker /cc-suite:review-plan REQUIRED before implementation — confirm: A10-T2 implementable as a NEW apps-layer module over the A10-T1 contract WITHOUT editing services / a10CitationContract.ts / linkHandlers / inventing CanonicalExportModel / wiring the live pipeline; the safe-href rule (mint ONLY the internal `lawbar://citation/` scheme; never external/empty/fake; `isSafeCitationHref` rejects unsafe) is sound; total+bijective + never-dropped + href-only-when-clean-and-safe are the right A10-T2 semantics; the one-line package.json test registration is acceptable; NO schema/services/native/UI/IPC change. STOP-points: (1) if A10-T2 undefined/contradicted by ADR/A10-T1 -> STOP; (2) if it requires inventing CanonicalExportModel/A10-T6 -> STOP; (3) if it requires wiring the live export pipeline or editing services/linkHandlers -> STOP; (4) if schema/renderer-UI/electron-IPC/package(beyond test-script)/native change becomes necessary without review-plan approval -> STOP; (5) if it would weaken A1/A3/A8 or the A0.7/A1-T6/A3-T10 native gates or the A10-T1 contract -> STOP; (6) if custody/key handling would expose a key -> STOP. If review-plan NEEDS-FIX, fix-forward; if a blocker, STOP. After implementation: targeted `node --test apps/lawbar-desktop/tests/a10-hyperlink-degradation.unit.test.mjs` GREEN; `npm --prefix apps/lawbar-desktop test` GREEN (incl. the A10-T1 + existing suites); `scripts/workflow/check-contract-integrity.sh` exits 0; broker /cc-suite:audit + /cc-suite:verify (no Critical/High/Medium open before commit). No A0.7 custody. Batch is 1/3 (marker 13f10eb); NOT due — no closeout this lane.
Acceptance criteria: a NEW apps-layer module `apps/lawbar-desktop/src/caseBox/export/a10HyperlinkDegradation.ts` maps `A10RenderedCitation` -> `A10HyperlinkCitation` { text, internalHref, flag, ... } where the court-fileable AUTHORITY is text/flag and `internalHref` is non-authoritative internal nav metadata only; `isSafeCitationHref` ACCEPTS only a STRICT internal `lawbar://citation/<docSeg>/<idx>` form (docSeg encodeURIComponent round-trips + no `.`/`..`/slash/backslash/encoded-traversal after decode; idx `^[0-9]+$`) and REJECTS null/empty/whitespace/control-char/http(s)/javascript/data/file/mailto/vbscript/other-scheme/dot-segment/encoded-traversal/case-prefix-trick; `mintInternalCitationHref` returns null for empty documentId or non-negative-non-integer physicalPageIndex; tests PROVE: (a) valid clean citation with documentId+physicalPageIndex -> text 卷X页Y + a safe `lawbar://citation/<doc>/<idx>` internalHref, flag null; (b) clean citation missing documentId or physicalPageIndex -> text present, internalHref null (text-only, NOT dropped); (c) flagged citation -> text null, internalHref null, flag set (visible/reviewable, NOT dropped); (d) no empty/fake/unsafe internalHref is ever emitted (mapper internalHref is always either a strict-valid internal URL or null; isSafeCitationHref rejects the unsafe set incl. `lawbar://citation/../0`, `%2e%2e`, encoded slash, controls, negative/non-integer idx, scheme-case tricks); (e) total+bijective (every input -> exactly one of text/flag); (f) existing A10-T1 contract behavior unchanged (regression — A10-T1 tests still pass); (g) determinism (repeat run deep-equal; no machine paths/timestamps/locale); (h) internalHref is documented + named as non-authoritative (never the court-fileable authority; never auto-rendered into court artifacts). NO CanonicalExportModel/golden/forms/A8/live-pipeline; NO services/schema/native/renderer/electron/dependency change; NO edit to a10CitationContract.ts (unless review-plan approved an additive export) / linkHandlers / dto-link; `CURRENT_SCHEMA_VERSION` remains 12; `dev-memo/run/evidence/**` untouched; no HMAC/custody key; `check-contract-integrity` exits 0; broker review-plan READY + audit no Critical/High/Medium + verify ALL CLOSED; committed diff confined to Allowed files (package.json diff = exactly the one-line test registration).
Risk flags: HIGH-RISK Evidence-invariant lane (A10 court-fileable export; citation/hyperlink fidelity) — but a STANDALONE apps-layer module + tests only; NO services/schema/native/UI/IPC change, NO live-pipeline wiring, NO CanonicalExportModel. Load-bearing care points: (a) SINGLE-SOURCE — reuse the A10-T1 卷X页Y text verbatim (do NOT re-render); (b) SAFE-HREF — mint ONLY the internal `lawbar://citation/` scheme; NEVER emit external/javascript/data/empty/fake hrefs; degrade unsafe/missing to text-only; (c) NEVER-DROPPED + total+bijective — every input yields exactly one of {text(±href)} XOR {flag}; a flagged or missing-target citation stays visible (flag or text-only), never silently omitted; (d) href non-null ONLY when text non-null AND safe; (e) deterministic; (f) do NOT wire the live pipeline, edit services/linkHandlers, or invent CanonicalExportModel; (g) do NOT touch custody/marker/evidence/key. If any cannot hold, STOP and report. No push/merge/branch-delete.
Depends on: none
Commit boundary: governance (mark-reviewed + govern) run STANDALONE before the commit (never bundled with git commit). ONE implementation commit — apps/lawbar-desktop/src/caseBox/export/a10HyperlinkDegradation.ts + apps/lawbar-desktop/tests/a10-hyperlink-degradation.unit.test.mjs + apps/lawbar-desktop/package.json (one-line test registration) + governance artifacts (queue.md/.linted/.reviewed/.governed + reviews/queue-review-112.md), staged by exact path; NO services/schema/native/renderer/electron/contract/dependency/tarball file; NO edit to a10CitationContract.ts/linkHandlers/dto-link (unless review-plan approved); NO marker/evidence/key/report file; no push; no merge; no branch delete.
