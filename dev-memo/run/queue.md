## WI-A3-LINK-CREATE-AUDIT-KIND: contract support for the audited link-create event kind (contract-only; NOT A0.7-gated)
Type: SOURCE
A0.7-classification: WI-A3-LINK-CREATE-AUDIT-KIND is a contract/audit-kind change (the case-box audit-event vocabulary), NOT a geometry-derived A3 behavior; it executes no A0.7-dependent code and reads no A0.7 marker, so it is NOT A0.7-gated and carries NO `Requires-A07:` line. It IS HIGH-RISK (a public contract / audit-chain tamper-evidence surface), so broker /cc-suite:review-plan + /cc-suite:audit + /cc-suite:verify are REQUIRED. If review-plan says this contract/audit change must be A0.7-gated or custody-9b, STOP and report.
Scope: Add the CONTRACT-LEVEL audit support the link-create ADR (docs/adr/ADR-evidence-a3-link-create-operation.md §D1/§8) requires BEFORE the durable link-CREATE OPERATION (WI-A3-LINK-CREATE-T1, a separate later A0.7-gated lane) can emit a tamper-evident audit event. Add the `LINK_CREATED` audit-event KIND to the case-box-contract audit vocabulary, additively and consistently across the JSON schema event_kind enum + the TS registry + the regenerated type, governed by the audit-event-kind-preservation ADR (the chain verifier enforces the kind's {action, entity_type, reasonRequired} tuple). The `link` entity_type already exists (added by WI-A3-UNLINK-AUDIT-KINDS, PR #136), so this lane adds ONLY the new event_kind. CONTRACT-ONLY: this lane does NOT implement the createLink mutating operation, does NOT touch the persistence layer (services/**), does NOT change the SQLite schema or CURRENT_SCHEMA_VERSION, does NOT change resolver/export behavior, and does NOT change UI/IPC/export rendering. The new kind is DEFINED here but not yet EMITTED (T1 emits it) — defining a kind before its first emitter is the established pattern (WI-A3-UNLINK-AUDIT-KINDS).
Required decisions to record: (1) KIND — add ONE entry to CASE_BOX_AUDIT_EVENT_KINDS (audit-log.ts): `LINK_CREATED: { action: "create", entity_type: "link", reasonRequired: false }` (the ADR-defined tuple — creating a link is action create, entity_type link, and needs no reason; mirrors DOCUMENT_REGISTERED/MATTER_REGISTERED create kinds). Flag to review-plan: confirm action `create` (a link row is created) + reasonRequired `false` — if review-plan prefers different, record/STOP. (2) EVENT_KIND ENUM — add `LINK_CREATED` to the schema's event_kind enum (case-box-audit-event.schema.json), additively, with the existing kinds unchanged (the kind set is append-only per the audit-event-kind ADR). (3) ENTITY_TYPE — `link` already exists in the entity_type enum + CASE_BOX_AUDIT_ENTITY_TYPES (PR #136); NO entity_type change needed. (4) GENERATED TYPE — regenerate docs/contracts/case-box-contract/src/generated/case-box-audit-event.ts via `npm --prefix docs/contracts/case-box-contract run gen:types` (it is AUTO-GENERATED — never hand-edited); commit the regenerated output so the committed state is consistent (loc-guardian exempts the AUTO-GENERATED banner file). (5) TUPLE ENFORCEMENT — the chain verifier (verifyAuditChain) already enforces that a v2 event's kind matches its declared {action, entity_type, reasonRequired}; this lane adds the registry entry so a future LINK_CREATED event verifies; no verifier code change unless review-plan identifies one (STOP and ask if the verifier needs new logic beyond the registry addition). (6) NO EMITTER — no audit event is emitted in this lane; no persistence/operation change; the first emitter is WI-A3-LINK-CREATE-T1. (7) ADDITIVE / NON-BREAKING — enum-member + registry addition only; existing entity_types/kinds/canonicalization unchanged; the v1/v2 golden canonical tests stay green (the new kind is not exercised by the existing golden events). (8) NO SCHEMA/RESOLVER/EXPORT/UI/PERSISTENCE — out of scope. (9) DOCS — the governing ADR is already merged (PR #138); no ADR edit needed unless review-plan recommends an additive note. (10) A0.7 — NOT A0.7-gated; HIGH-RISK contract change (broker required).
Source of truth: docs/adr/ADR-evidence-a3-link-create-operation.md (the link-create design ADR — D1 requires LINK_CREATED as the predecessor; §8 sequencing); docs/adr/audit-event-kind-preservation.md (the governing ADR — kind preservation, the {action, entity_type, reasonRequired} tuple, append-only kinds, regenerate the type); docs/adr/case-box-step-4-audit-log-shape.md (the audit-log shape + hash chain + entity_type enum history); docs/contracts/case-box-contract/schemas/case-box-audit-event.schema.json (the event_kind enum — the file edited); docs/contracts/case-box-contract/src/audit-log.ts (CASE_BOX_AUDIT_EVENT_KINDS — the file edited; the existing LINK_UNLINKED/LINK_RELINKED entries are the immediate precedent); docs/contracts/case-box-contract/src/generated/case-box-audit-event.ts (regenerated, not hand-edited); docs/contracts/case-box-contract/tests/audit-event-kind-v2.test.mjs (the kind tests to extend); services/case-box-persistence (READ-ONLY — the future consumer/emitter; NOT changed here); .claude/rules/autonomy.md + .claude/rules/cc-suite.md + .claude/rules/security-boundary.md (this lane is user-authorized; HIGH-RISK contract/audit security; broker required).
Allowed files: docs/contracts/case-box-contract/schemas/case-box-audit-event.schema.json (add LINK_CREATED to the event_kind enum — additive only), docs/contracts/case-box-contract/src/audit-log.ts (add LINK_CREATED to CASE_BOX_AUDIT_EVENT_KINDS — additive only), docs/contracts/case-box-contract/src/generated/case-box-audit-event.ts (REGENERATED via gen:types — not hand-edited), docs/contracts/case-box-contract/tests/audit-event-kind-v2.test.mjs (add LINK_CREATED acceptance + tuple-enforcement cases), docs/contracts/case-box-contract/tests/contract.test.mjs OR validators.test.mjs OR state-machine.test.mjs (ONLY to update a kind-set assertion the addition necessarily changes — minimal, additive), dev-memo/run/queue.md, dev-memo/run/queue.linted, dev-memo/run/queue.reviewed, dev-memo/run/queue.governed, dev-memo/run/reviews/queue-review-091.md, dev-memo/plan-batch-casebox-evidence-a3-link-create-audit-kind-00.md (a short dev-memo recording the addition), dev-memo/study/** (a batch-audit study packet if a window closes), dev-memo/cc-suite-reliability-log.md (only if a TIMEOUT retry occurs), dev-memo/deferred-audit-findings.md (only if a finding is deferred)
Forbidden files: services/** (NO persistence/operation change — createLink + its emitter are WI-A3-LINK-CREATE-T1; READ-ONLY reference only), services/case-box-persistence/src/sqlite/schema.ts (NO SQLite schema/version change), apps/**, native/**, .claude/**, .github/**, AGENTS.md, CLAUDE.md, GEMINI.md, scripts/workflow/**, scripts/gen-types.mjs (NO generator change — run it, don't edit it), migrations/**, package-lock.json / any dependency manifest, docs/contracts/case-box-contract/package.json (NO change), committed dev-memo/run/evidence/** marker/ledger/key/evidence-run files, committed keys, dev-memo/run/reports/** (untracked local run-state — never staged), dev-memo/run/override-reason.md (untracked residue — never staged), Evidence-Genie-M0-Developer-Handover.md (root intake — READ-ONLY, never moved/deleted), xiaolai-dev-workflow-study.md (root intake — READ-ONLY, never moved/deleted); NO createLink operation, NO audit-event emission, NO persistence change, NO SQLite schema/version change, NO resolver/export/UI/IPC/export-rendering change, NO removal/rename of existing entity_types or kinds (append-only), NO entity_type change (link already exists), NO canonicalization change, NO verifier logic change (registry-only), NO dependency, NO marker/key/gate change, NO Requires-A07
Gates: broker /cc-suite:review-plan REQUIRED before implementation (confirm NOT-A0.7-gated but HIGH-RISK contract classification; confirm the additive LINK_CREATED kind follows the audit-event-kind-preservation ADR — append-only, schema+TS+generated kept in sync, the {action: create, entity_type: link, reasonRequired: false} tuple correct per the link-create ADR; confirm CONTRACT-ONLY — no emitter, no persistence, no SQLite schema/version, no resolver/export/UI; confirm the v1/v2 golden canonical + chain-verify tests stay green); if review-plan returns NEEDS-FIX, STOP and report; if review-plan says this must be A0.7-gated/custody-9b, STOP and report; if review-plan demands an emitter / persistence change / a canonicalization or verifier-logic change / a SQLite schema change / a dependency, STOP and ask; broker /cc-suite:audit + /cc-suite:verify on the implementation diff after authoring; npm --prefix docs/contracts/case-box-contract test passes (the contract package: gen:types + build + the audit-kind/validator/contract tests, incl. the new LINK_CREATED cases + the golden canonical tests still green); npm --prefix services/case-box-persistence test passes (the downstream consumer stays green — the additive kind does not break it); scripts/workflow/check-contract-integrity.sh exits 0; no services/apps/native/scripts/.claude/.github/dependency change; no SQLite schema/version/code; no emitter/operation; no committed marker/ledger/key/evidence-run; root intake untouched
Acceptance criteria: docs/contracts/case-box-contract/schemas/case-box-audit-event.schema.json has LINK_CREATED in the event_kind enum (existing members unchanged); docs/contracts/case-box-contract/src/audit-log.ts has LINK_CREATED { action create, entity_type link, reasonRequired false } in CASE_BOX_AUDIT_EVENT_KINDS (existing entries unchanged); docs/contracts/case-box-contract/src/generated/case-box-audit-event.ts is REGENERATED (via gen:types) to reflect the new enum member (AUTO-GENERATED banner intact, not hand-edited); contract tests prove LINK_CREATED is accepted, its {action create, entity_type link, reasonRequired false} tuple is enforced by the chain verifier, and a LINK_CREATED event (no reason) verifies; the existing LINK_UNLINKED/LINK_RELINKED + v1/v2 golden canonical + chain-verify tests remain green (no canonicalization/verifier change); NO entity_type or kind removed/renamed (append-only); NO emitter / persistence / SQLite-schema / resolver / export / UI change; npm --prefix docs/contracts/case-box-contract test passes; npm --prefix services/case-box-persistence test passes; scripts/workflow/check-contract-integrity.sh exits 0; broker review-plan READY (classification + kind confirmed) + audit reports no Critical/High/Medium + verify ALL CLOSED; the committed implementation diff is confined to the Allowed files (the contract schema/src/generated/tests + dev-memo/**)
Risk flags: SOURCE — a CONTRACT / audit-event-kind change over the tamper-evident audit vocabulary — HIGH-RISK (public-API / audit-chain security surface; broker review-plan + audit + verify REQUIRED). NOT A0.7-gated (no geometry-derived behavior; no marker/key). ADDITIVE / NON-BREAKING: adds ONE LINK_CREATED kind; existing entity_types/kinds/canonicalization UNCHANGED (append-only per the audit-event-kind-preservation ADR); the `link` entity_type already exists (#136). CONTRACT-ONLY: no emitter, no persistence, no SQLite schema/version, no resolver/export/UI — those are out of scope (the createLink OPERATION + the emitter are WI-A3-LINK-CREATE-T1). The schema enum + the TS registry + the regenerated type MUST stay in sync. The change MUST NOT weaken the audit-chain tamper-evidence (no canonicalization change; the verifier still enforces the kind tuple) nor any Evidence invariant. No removal/rename of existing kinds. No dependency. No push, no merge.
Depends on: none
Commit boundary: governance (mark-reviewed + govern) run STANDALONE before the commit (never bundled with git commit). ONE commit -- docs/contracts/case-box-contract/schemas/case-box-audit-event.schema.json + docs/contracts/case-box-contract/src/audit-log.ts + docs/contracts/case-box-contract/src/generated/case-box-audit-event.ts + docs/contracts/case-box-contract/tests/audit-event-kind-v2.test.mjs (+ a minimal additive update to one of contract/validators/state-machine tests if a kind-set assertion requires it) + dev-memo/plan-batch-casebox-evidence-a3-link-create-audit-kind-00.md + dev-memo/run/queue.md + dev-memo/run/queue.linted + dev-memo/run/queue.reviewed + dev-memo/run/queue.governed + dev-memo/run/reviews/queue-review-091.md, staged by exact path; no push; no merge
