## WI-RELEASE-G13-OPERATOR-CHECKLIST-RUNBOOK-00: define + govern the M0 gate-13 operator checklist + runbook completion (fill the two scaffold docs — `docs/release/operator-checklist.md` + `docs/release/ocr-worker-runbook.md` — with a Mac-client operator section + case-box local-first operating procedures, cross-referencing the gate 14/15/2/20 evidence WITHOUT clearing them, reconciling the stale OCR-worker *service* framing against the locked v1 Mac-desktop local-first posture) (Type: EVIDENCE, release-governance MEDIUM risk)

## Review packet (compact)
- Active plan summary: gate 13 of the M0 go-live readiness matrix (`docs/release/go-live-readiness-report.md` §1 row 13) is **OPEN** — evidence "`docs/release/operator-checklist.md` (scaffold); `ocr-worker-runbook.md`", blocker "Documentation WIs (OCR runbook content + NEW Mac-client operator section)". Both docs are **scaffolds** (headers + `_To be filled in WI-09b …_` placeholders) pinned to an OLD round-4 `docs/release/go-live-plan.md` WI-09b that never landed. This WI **governs** (does NOT execute) a FUTURE lane that COMPLETES the two operator docs — adding the required Mac-client operator section + the case-box local-first operating procedures, and cross-referencing the already-landed gate 14 (backup/recovery), gate 15 (rollback), gate 2 (OCR), gate 20 (telemetry) evidence WITHOUT clearing any of them. THIS authoring lane produces the governed queue WI only; it EDITS NO operator doc, changes NO source/test/config, clears NO gate, and decides NO user go-live hard-stop.
- Grounding note (verified 2026-07-07 — the exec lane MUST re-verify against the real files before relying, per `AGENTS.md` §"Authoring discipline"): (a) `docs/release/operator-checklist.md` (25 lines) = `Status: scaffold` + 8 section headers (Node version check; dependency audit; production config preflight; fail-closed probes; queue+persistence checks; rollback procedure; commit/diff-hash evidence; audit checks), each a `_To be filled in WI-09b …_` placeholder. (b) `docs/release/ocr-worker-runbook.md` (28 lines) = `Status: scaffold` + 10 section headers (HTTPS source policy; production profile env vars; production fail-closed; retry/dead-letter observability; pending-retry recovery; source-kind policy; day-0 operations; no-PDF-in-v1; proxy/pooling ADR) — a **server/service** production-profile framing (`NODE_ENV=production`, `OCR_WORKER=paddleocr-onnx`, HTTPS host allowlist, S3-rejected/pre-signed-URL, healthcheck, shutdown signals). (c) That server framing PREDATES the locked v1 posture in `.claude/rules/client-local-first.md` — v1 is a **Mac desktop app, local-first**; the OCR worker + HTTP gateway is an **in-process / local-loopback transport for the Mac client**, NOT a public/deployed service. So the runbook's service-profile content is stale relative to the locked direction and MUST be reconciled (per §"Reconciliation duty"), NOT completed as if a public OCR service ships in v1.
- Exact target files (FUTURE exec lane): `docs/release/operator-checklist.md` (COMPLETE the scaffold — add the Mac-client operator section + case-box local-first operating procedures + the gate 14/15/2/20 cross-references; the operator-facing release checklist) + `docs/release/ocr-worker-runbook.md` (COMPLETE + RECONCILE — fill the local-first-appropriate operating content; reframe or defer the stale *service* production-profile sections per `client-local-first.md`, logging the divergence as `RECONCILIATION-NEEDED` rather than silently rewriting the locked direction) + the gate-13 evidence ROW of `docs/release/go-live-readiness-report.md` (evidence-row TEXT ONLY; **gate 13 MUST remain `OPEN` in this WI** — enrich the row with a `[Δ] operator checklist + runbook docs completed — gate remains OPEN pending a holistic readiness-refresh WI` marker using the report's EXISTING vocabulary; a `OPEN → PARTIAL/CLEARED` move belongs to a SEPARATE holistic readiness-refresh WI, NOT this one — NO roll-up/status-bucket edit; gate 13 stays OPEN, its roll-up bucket unchanged — and MUST NOT clear gates 2/6/12/14/15/18/19/20 or imply any cleared) + `dev-memo/run/reviews/` (the exec lane's review artifact). NO other file. The exec lane EDITS the two operator docs + the one gate-13 row; it EDITS NO other release doc, NO brief, NO ADR, NO product source/test/config.
- Exact acceptance criteria: below.
- Exact out-of-scope: NO product source/test/package/config change (docs-only completion of the two operator docs + the gate-13 row); NO OCR/Forms implementation; NO dependency change; NO schema/migration (`CURRENT_SCHEMA_VERSION` stays 12); NO contract change; NO ADR edit (the OCR-worker/service reconciliation is LOGGED as `RECONCILIATION-NEEDED` for a future ADR/reconciliation WI, NOT decided here); NO brief edit; NO gate-6 full-project audit; NO broad readiness refresh; NO clearing of gates 2/6/12/14/15/18/19/20 (or implying any cleared); NO gate-7 D-G7 risk acceptance; NO signing/notarization/公开 distribution decision (gate 4); NO 律师法 conclusion (gate 11); NO license/legal/business decision (gate 17); NO final GO/NO-GO (gate 21); NO staging of `dev-memo/run/intake/` or untracked residue.
- Requirements the FUTURE operator-doc completion must define + satisfy (what the exec lane's docs must specify + record) — at minimum:
  1. **Exact existing runbook/checklist files to inspect + update**: `docs/release/operator-checklist.md` + `docs/release/ocr-worker-runbook.md` (both scaffolds today). State the exact section headers present + which are completed vs reconciled-as-post-v1, and the tool version/date read. No THIRD operator file is created.
  2. **Required Mac-client operator section**: a concrete operator-facing section for the v1 **Mac desktop app** — install/launch, where case data lives (`~/Library/Application Support/lawbar/` — single `case-box.sqlite` with audit tables inside + `case-box-documents/` blob dir + `theme-preference.json`, grounded in the shipped layout read from `caseBoxRuntime.ts`/`main.ts`), first-run, normal operation, quit/restart, and the offline/local-first operating assumption (App-Sandbox no-network posture per `client-local-first.md`). This is the "NEW Mac-client operator section" the gate-13 blocker names.
  3. **Required case-box local-first operating procedures**: the day-to-day local-first procedures — opening/working a matter, where documents/audit live, the single-user local-first assumption, no cloud/sync by default (opt-in per document/matter is post-v1), and the "data does not leave the Mac unless the user takes a deliberate action" invariant.
  4. **Backup/recovery references to Gate 14 evidence WITHOUT clearing Gate 14**: the checklist/runbook CITES `docs/release/gate14-backup-recovery-00.md` (the backup + recovery runbook, drill-verified) as the authoritative backup/recovery procedure — a REFERENCE, not a re-derivation; gate 14 stays OPEN (its own row unchanged, NOT cleared).
  5. **Rollback references to Gate 15 evidence WITHOUT clearing Gate 15**: CITES `docs/release/gate15-rollback-dry-run-00.md` + `dev-memo/rollback-00.md` (git-revert-never-reset discipline, dry-run PASS) for the rollback procedure — a REFERENCE; gate 15 stays PARTIAL (NOT cleared). The scaffold's own "Rollback Procedure" section is completed by pointing at this evidence.
  6. **OCR operational references to Gate 2 evidence WITHOUT clearing Gate 2**: CITES `docs/release/gate2-ocr-pipeline-verify-00.md` (the OCR test-sweep PASS) for OCR operational behavior; RECONCILE the runbook's stale *service* production-profile (S3/HTTPS-allowlist/healthcheck/production env vars) against the locked v1 in-process/local-loopback OCR posture (`client-local-first.md`) — reframe for the local worker OR mark post-v1 server-mode, logging `RECONCILIATION-NEEDED`; gate 2 stays PARTIAL (NOT cleared). The `RECONCILIATION-NEEDED` note MUST be logged in ALL THREE places: (i) IN the runbook itself (an explicit reconciliation note preserving an AUDIT TRAIL of exactly WHICH stale service-mode content was reframed/deferred and WHY — so the old service posture is visibly superseded, NOT silently erased); (ii) the exec-lane COMMIT MESSAGE; (iii) the exec-lane REVIEW ARTIFACT. Any retained post-v1 server-mode section MUST be EXPLICITLY labeled `post-v1 / deferred` (not left as a generic `_To be filled_` placeholder). A conflict between `client-local-first.md` and a release doc that CANNOT be resolved by deferring server-mode content without changing locked direction is a HARD STOP (STOP-and-escalate for a future ADR WI), never a silent rewrite.
  7. **Telemetry/crash-reporting references to Gate 20 evidence WITHOUT clearing Gate 20**: CITES `docs/release/gate20-telemetry-crash-reporting-verify-00.md` (no telemetry; crash reporting OFF by default; no external egress) for the operating posture — a REFERENCE; gate 20 stays PARTIAL (NOT cleared).
  8. **What must be in the final operator checklist before Gate 13 can move**: an explicit "gate-13 completion checklist" enumerating what a COMPLETE operator checklist requires (Mac-client operator section present; case-box local-first procedures present; gate 14/15/2/20 cross-references present; the OCR service-framing reconciled or deferred; no dangling `_To be filled_` placeholder in a v1-relevant section) — so a future readiness-refresh WI has an auditable "gate 13 ready to move" condition. (This WI does NOT move gate 13.)
  9. **Expected evidence artifact / doc updates**: the two completed operator docs + the gate-13 evidence-row enrichment (`[Δ]` marker) — captured as text; the completion IS the evidence (no separate `gate13-*.md` doc is required, though the exec lane MAY ALSO add a `docs/release/gate13-operator-checklist-runbook-00.md` summary if it prefers — allowed only within the target-file list, and the two scaffold docs MUST STILL themselves be completed).
  10. **Deterministic pass/fail criteria + residual risks**: PASS = both operator docs completed (no dangling placeholder in the v1-relevant sections) AND the Mac-client + case-box local-first sections present AND the gate 14/15/2/20 references present-without-clearing AND the OCR service-framing reconciled/deferred (RECONCILIATION-NEEDED logged) AND gate 13 kept OPEN. FAIL = a placeholder left in a v1-relevant section, a missing required section, a gate wrongly cleared, or the stale server-framing silently rewritten against locked direction. Residual risks: the OCR-service reconciliation may need a follow-up ADR/reconciliation WI (post-v1); a full operator manual/screenshots is post-v1.
- Dependencies mapped: **gate 14** (backup/recovery evidence — REFERENCED, NOT cleared); **gate 15** (rollback evidence — REFERENCED, NOT cleared); **gate 2** (OCR evidence — REFERENCED, NOT cleared); **gate 20** (telemetry evidence — REFERENCED, NOT cleared); **gate 6** (full-project audit — LATER, consumes the runbook; NOT cleared); **gate 19** (supply-chain — the checklist's dependency-audit section is adjacent but gate 19 is a SEPARATE lane, NOT cleared); **gate 18** (export/backup format — adjacent, NOT cleared). These are noted as related/referenced, NOT cleared by this WI. `client-local-first.md` is the locked v1 posture the reconciliation obeys.
- Essential refs: `.claude/rules/client-local-first.md` (locked v1 Mac-desktop local-first posture + §"HTTP gateway is not a website" + §"Reconciliation duty" — the OCR-worker service-framing reconciliation obeys this); `docs/release/go-live-readiness-report.md` §1 gate 13 (OPEN) + gates 2/14/15/20 (the referenced evidence rows); `docs/release/gate14-backup-recovery-00.md` + `gate15-rollback-dry-run-00.md` + `gate2-ocr-pipeline-verify-00.md` + `gate20-telemetry-crash-reporting-verify-00.md` (the cross-referenced evidence); `docs/release/operator-checklist.md` + `ocr-worker-runbook.md` (the two scaffolds to complete); `AGENTS.md` §"Source hierarchy" + §"Authoring discipline" + §"Repo brief — lawbar-tool"; `.claude/rules/security-boundary.md` §"Go-live independence"; `.claude/rules/autonomy.md` + `staging-hygiene.md` + `loc-guardian.md`.
- Review questions: (1) Does the WI correctly scope COMPLETING the two operator scaffold docs (Mac-client operator section + case-box local-first procedures + gate 14/15/2/20 cross-references), NOT an execution in this authoring lane and NOT a product change? (2) Does it require the stale OCR-worker *service* production-profile framing to be RECONCILED against the locked v1 Mac-desktop local-first posture (`client-local-first.md` §"Reconciliation duty") — reframed/deferred + `RECONCILIATION-NEEDED` logged, NOT silently rewritten and NOT completed as if a public OCR service ships? (3) Does it keep gates 2/6/12/14/15/18/19/20 uncleared (references-not-clearances) + gate 13 `OPEN`? (4) Does it forbid product source/test/config/ADR/brief edits + Forms/OCR implementation + dependency change + readiness refresh? (5) Does it decide no STOP-AND-ASK (4/11/17/21) hard-stop and assert no go-live? (6) Is any gate clearance, ADR decision, product change, or readiness refresh smuggled in?

Type: EVIDENCE
Scope: Govern (do NOT execute) a FUTURE lane that COMPLETES the M0 gate-13 operator checklist + runbook. The FUTURE execution lane fills the two scaffold docs — `docs/release/operator-checklist.md` (add the Mac-client operator section + case-box local-first operating procedures + the gate 14/15/2/20 cross-references + the "what must be present before gate 13 can move" completion checklist) + `docs/release/ocr-worker-runbook.md` (complete the local-first-appropriate operating content; RECONCILE the stale *service* production-profile sections against the locked v1 Mac-desktop local-first posture per `.claude/rules/client-local-first.md` — reframe for the in-process/local-loopback worker OR mark post-v1 server-mode, logging `RECONCILIATION-NEEDED`, NOT silently rewriting locked direction) — defining requirements 1–10 above, then updates ONLY the gate-13 evidence row of `docs/release/go-live-readiness-report.md`, where **gate 13 MUST remain `OPEN`** (add only a `[Δ] operator checklist + runbook docs completed — gate remains OPEN pending a holistic readiness-refresh WI` marker; NO roll-up/status-bucket edit; an `OPEN → PARTIAL/CLEARED` move belongs to a SEPARATE holistic readiness-refresh WI). It CROSS-REFERENCES the gate 14/15/2/20 evidence WITHOUT clearing them (references, not clearances), CITES gate 6 as the later consumer, and preserves every still-non-cleared gate + the STOP-AND-ASK gates (4/11/17/21). It makes NO final GO/NO-GO, resolves NO STOP-AND-ASK, runs NO gate-6 audit, EDITS NO ADR/brief, does NO Forms/OCR implementation, adds NO dependency, runs NO readiness refresh, and changes NO product source/test/schema/contract. THIS authoring lane implements + runs + decides NOTHING — it only governs the gate-13 completion WI. If review asks to clear a dependent gate, edit an ADR/brief, implement OCR/Forms, change product source/test, run gate 6, run a readiness refresh, or silently rewrite the locked OCR/service posture, REJECT and report.
Source of truth: .claude/rules/client-local-first.md (locked v1 Mac-desktop local-first posture + §"HTTP gateway is not a website" + §"Reconciliation duty" — the OCR-worker service reconciliation); docs/release/go-live-readiness-report.md §1 gate 13 (OPEN) + gates 2/14/15/20; docs/release/gate14-backup-recovery-00.md + gate15-rollback-dry-run-00.md + gate2-ocr-pipeline-verify-00.md + gate20-telemetry-crash-reporting-verify-00.md (cross-referenced evidence); docs/release/operator-checklist.md + ocr-worker-runbook.md (the scaffolds to complete); AGENTS.md §"Source hierarchy" + §"Authoring discipline" + §"Repo brief — lawbar-tool"; .claude/rules/security-boundary.md §"Go-live independence"; .claude/rules/autonomy.md + staging-hygiene.md + loc-guardian.md
Allowed files: docs/release/operator-checklist.md (COMPLETE the scaffold) + docs/release/ocr-worker-runbook.md (COMPLETE + RECONCILE the scaffold) + docs/release/go-live-readiness-report.md (the gate-13 evidence ROW ONLY — evidence-row TEXT ONLY, NO roll-up/status-bucket edit; gate 13 stays OPEN, its roll-up bucket unchanged) + OPTIONALLY a NEW docs/release/gate13-operator-checklist-runbook-00.md (a summary evidence doc, allowed only IF the exec lane prefers — the two scaffold docs MUST STILL be completed) + dev-memo/run/reviews/ (the exec lane's own review artifact). NOTE: THIS authoring lane commits ONLY the queue governance (queue.md/linted/reviewed/governed) + reviews/queue-review-162.md — NO operator-doc edit, NO report change, NO summary doc. The FUTURE exec lane records its evidence in the COMMIT MESSAGE + review artifact and does NOT re-stage this lane's queue governance and does NOT edit any ADR/brief.
Forbidden files: docs/product/project-requirements-brief.md (READ-ONLY — NO brief edit), docs/adr/ (READ-ONLY — NO ADR edit; the OCR/service reconciliation is LOGGED as RECONCILIATION-NEEDED for a future WI, NOT decided here), docs/release/ OTHER than operator-checklist.md + ocr-worker-runbook.md + the gate-13 row of go-live-readiness-report.md + the optional gate13-operator-checklist-runbook-00.md (the other gate evidence docs are READ-ONLY — referenced, NOT edited), docs/contracts/, apps/ + services/ (NO product source/test/renderer/IPC/OCR/Forms change), services/*/src/sqlite/schema.ts (CURRENT_SCHEMA_VERSION stays 12), migrations/, native/, package.json/package-lock.json of any package (NO dependency change), dev-memo/run/intake/ (raw samples — never staged), dev-memo/ OTHER than run/reviews/ (no other plan/design edit), .claude/ + .github/; NO ADR edit; NO brief edit; NO OCR/Forms implementation; NO operator-doc COMPLETION in this authoring lane; NO gate-6 full-project audit; NO broad readiness refresh; NO clearing of gates 2/6/12/14/15/18/19/20; NO signing/notarization/public-distribution decision (gate 4); NO 律师法 conclusion (gate 11); NO license/legal/business decision (gate 17); NO gate-7 D-G7 risk acceptance; NO final GO/NO-GO decision (gate 21). Push is NEVER automatic (autonomy hard-stop): local commit governance is the default; any branch push + PR open happens ONLY under explicit per-invocation user authorization; NO merge; NO branch delete of shared branches
Gates: EVIDENCE/governance lane (MEDIUM release-governance risk — governs an operator-doc completion WI; no code, no ADR edit, no go-live decision). (1) THIS authoring lane: broker /cc-suite:review-plan on this queue WI, Path 1, retrievable, then govern. (2) The FUTURE exec lane: COMPLETE the two operator docs + the gate-13 row update, then broker /cc-suite:review-plan on the produced artifacts. (3) scripts/workflow/check-contract-integrity.sh exit 0; scripts/workflow/check-queue.sh PASS (both lanes). Forbidden-scope scan before commit: CURRENT_SCHEMA_VERSION EQUALS 12; no product source/test/package/schema/contract change; no ADR/brief edit; no OCR/Forms impl; no dependency change; no gate-6 run; no readiness refresh; no gate 2/6/12/14/15/18/19/20 clear; no GO/NO-GO; no raw sample. STOP-points: review asks to clear a dependent gate, edit an ADR/brief, implement OCR/Forms, change product source/test, run gate 6, run a readiness refresh, or silently rewrite the locked OCR/service posture → REJECT/STOP and report; the OCR-service reconciliation reveals a genuine conflict needing an ADR decision → LOG RECONCILIATION-NEEDED and STOP for a future ADR WI (do NOT decide it here); any forbidden file would change → STOP.
Acceptance criteria: check-queue and check-contract-integrity PASS and CURRENT_SCHEMA_VERSION EQUALS 12 and the diff CONTAINS only the queue governance (queue.md/linted/reviewed/governed) + reviews/queue-review-162.md and no operator-doc/source/test/package/schema/contract change appears and no ADR/brief edit / no OCR-Forms impl / no dependency change / no readiness refresh appears. The governed WI must DEFINE the ten completion requirements (files to inspect+update; Mac-client operator section; case-box local-first procedures; gate 14/15/2/20 references-without-clearing; the OCR-service local-first reconciliation; the "before gate 13 can move" completion checklist; expected doc updates; deterministic pass/fail; residual risks), keep gates 2/6/12/14/15/18/19/20 uncleared, keep gate 13 `OPEN`, require the stale OCR-service framing to be RECONCILED (not silently rewritten), forbid an ADR/brief edit, and MUST NOT decide a STOP-AND-ASK hard-stop or assert go-live. review-plan (this lane) must be RUN and must PASS (READY, or READY with only Low-risk clarifications applied).
Risk flags: MEDIUM (release-governance — an operator-doc completion WI that could over-claim readiness, drift into clearing a referenced gate, silently rewrite the locked OCR/service posture, or smuggle in an ADR decision). Care points: (a) NO DOC COMPLETION in this authoring lane — this lane only governs the WI; (b) REFERENCES-NOT-CLEARANCES — the gate 14/15/2/20 evidence is CITED, never cleared; each stays at its current status; (c) LOCKED LOCAL-FIRST POSTURE — the stale OCR-worker *service* production-profile framing is RECONCILED against `client-local-first.md` (reframed/deferred + RECONCILIATION-NEEDED logged), NEVER silently rewritten and NEVER completed as if a public OCR service ships in v1; a genuine conflict → STOP for a future ADR WI; (d) GATE 13 STAYS OPEN — add only a `[Δ]` marker; an OPEN→PARTIAL/CLEARED move belongs to a separate readiness-refresh WI; (e) GO-LIVE INDEPENDENCE — an operator runbook does NOT imply go-live; the final verdict + the STOP-AND-ASK hard-stops (4/11/17/21) remain the user's; (f) NO READINESS REFRESH / NO OCR-FORMS WORK / NO DEPENDENCY CHANGE. If any cannot hold, STOP and report.
Depends on: none
Commit boundary: TWO separate lanes. (1) THIS authoring lane commits ONLY the queue governance (queue.md/linted/reviewed/governed) + reviews/queue-review-162.md — no operator-doc edit, no report change, no implementation, no decision. (2) The FUTURE execution lane commits ONLY the completed docs/release/operator-checklist.md + docs/release/ocr-worker-runbook.md + the gate-13 evidence-row update to go-live-readiness-report.md + the OPTIONAL docs/release/gate13-operator-checklist-runbook-00.md + its own review artifact under dev-memo/run/reviews/, records its evidence (incl. the OCR-service RECONCILIATION-NEEDED note) in the COMMIT MESSAGE, and does NOT re-stage this lane's queue governance and does NOT edit any ADR/brief. NO product source/test/package/schema/contract file in either lane; NO ADR/brief edit; NO OCR/Forms implementation; NO dependency change; NO staging of dev-memo/run/intake/ or untracked residue; push is NEVER automatic (autonomy hard-stop) — local commit governance is the default, any branch push + PR open happens ONLY under explicit per-invocation user authorization; no merge; no branch delete of shared branches. THIS authoring lane implements NOTHING, completes NO doc, and decides NOTHING — it only governs the gate-13 operator checklist + runbook completion WI.

## Acceptance criteria (FUTURE execution lane — what the governed completion must satisfy)

These describe the deliverable the FUTURE exec lane must produce; they are NOT preconditions of THIS authoring lane. The authoring-lane acceptance is the structured `Acceptance criteria:` field above (queue governance + review READY only — the operator docs are NOT completed in this lane).

1. `docs/release/operator-checklist.md` is COMPLETED (no dangling `_To be filled_` in a v1-relevant section) with a concrete Mac-client operator section + case-box local-first operating procedures + the gate 14/15/2/20 cross-references + a "what must be present before gate 13 can move" completion checklist.
2. `docs/release/ocr-worker-runbook.md` is COMPLETED + RECONCILED: the stale *service* production-profile sections (S3/HTTPS-allowlist/healthcheck/production env vars) are reframed for the v1 in-process/local-loopback OCR worker OR marked post-v1 server-mode, with the divergence logged as `RECONCILIATION-NEEDED` per `.claude/rules/client-local-first.md` §"Reconciliation duty" in ALL THREE places (the runbook itself — with an audit trail of which stale content was deferred and why; the commit message; the review artifact), any retained server-mode section EXPLICITLY labeled `post-v1 / deferred` — NOT silently rewritten, NOT completed as if a public OCR service ships. An unresolvable client-local-first-vs-release-doc conflict is a HARD STOP for a future ADR WI.
3. The docs CITE `gate14-backup-recovery-00.md` (backup/recovery), `gate15-rollback-dry-run-00.md` + `dev-memo/rollback-00.md` (rollback), `gate2-ocr-pipeline-verify-00.md` (OCR), `gate20-telemetry-crash-reporting-verify-00.md` (telemetry) as authoritative REFERENCES — gates 14/15/2/20 each stay at their current status (NOT cleared).
4. The exec lane CITES the exact gate-13 row (by its `| 13 | …` line) before editing, and edits ONLY that row's evidence/blocker TEXT — NO adjacent roll-up/status-bucket line touched. Gate 13 **MUST remain `OPEN`** — add ONLY a `[Δ] operator checklist + runbook docs completed — gate remains OPEN pending a holistic readiness-refresh WI` marker (roll-up bucket unchanged); any `OPEN → PARTIAL/CLEARED` move belongs to a SEPARATE holistic readiness-refresh WI. MUST NOT clear gates 2/6/12/14/15/18/19/20 or imply any cleared.
5. The docs keep go-live independence (no sign-off; gates 4/11/17/21 remain user STOP-AND-ASK); gate 6 is cited as the later consumer of the runbook; the OCR-service reconciliation + any full operator manual are recorded as residual/post-v1 follow-ups.
6. NO product source/test/package/config/schema/contract change in either lane; NO ADR/brief edit; NO OCR/Forms implementation; NO dependency change; `CURRENT_SCHEMA_VERSION` stays 12; docs-only tracked output.
7. `scripts/workflow/check-queue.sh` PASS + `scripts/workflow/check-contract-integrity.sh` PASS on both lanes.
