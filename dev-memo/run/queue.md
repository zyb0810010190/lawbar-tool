## WI-A3-LINK-IPC-T1: audited link IPC/API implementation (A0.7-gated, custody 9b; IPC-only, no UI)
Type: IMPL
Requires-A07: yes
A0.7-classification: WI-A3-LINK-IPC-T1 implements the IPC/API surface that exposes the audited court-facing link lifecycle (create/unlink/relink + resolve/export) to the app via trusted main-process handlers — mutating IPC over legal-record evidence-link state. It is A0.7-gated (the same A3 evidence-link surface the persistence ops gated under custody 9b). It carries `Requires-A07: yes` and runs under A07-KEY-00 custody mode 9b: the human mints the local marker + runs the A0.7-gated `check-gates.sh` with the HMAC key; the agent never receives the key; the implementation commit is blocked until the human reports gated PASS. NOTE (ADR D4): A0.7 is a DEV/COMMIT-time gate, NOT a runtime feature flag — this lane MUST NOT invent a runtime A0.7 marker check (that would be a new mechanism needing its own ADR). If review-plan says this WI must NOT be A0.7-gated, STOP and report.
Scope: Implement the IPC/API surface defined by docs/adr/ADR-evidence-a3-link-ipc-surface.md (D1-D11) — IPC/API ONLY, NO renderer UI. Add 5 channels (casebox:link:create/unlink/relink/list/export) as trusted main-process handlers in a new linkHandlers.ts module that delegate to the live persistence lifecycle (createLink/unlinkLink/relinkLink on the concrete SqliteCaseBoxPersistence; resolveLinkStatuses/buildExportCitations over the runtime db). Mirror the factHandlers.ts pattern exactly: shape-guard -> forbidden-field rejection (invalid_payload) -> unknown-field rejection -> camelCase-DTO validation -> matter+active-tenant preflight -> the concrete link call -> LINK_RESPONSE_FIELDS-projected response (tenant_id + payload_json excluded) -> mapThrownError. Actor/tenant are main-injected (getActiveActorUserId/getActiveTenantId); renderer-supplied actor/tenant are FORBIDDEN. Link DTOs expose identifiers/status/flags only — NO evidence content/PDF/OCR crosses the bridge. Wire a concrete LinkPersistenceProvider from the runtime (extend Runtime to expose the concrete sqlite + db; the InMemory unit-fallback returns a safe boundary error for link channels). Add the link methods to the preload CaseBoxApi ONLY (do NOT touch renderer/api.ts or renderer/types.ts — the renderer-dto-sync gate's hard-coded PAIRS list forces no renderer parity for new link DTOs; the renderer typed wrapper is the future UI lane). Register the new desktop unit test file in apps/lawbar-desktop/package.json (the curated test-list; explicitly allowed for THIS lane — do NOT orphan the test file).
Required decisions to record: (1) HANDLERS — 5 handlers in linkHandlers.ts mirroring factHandlers (createLinkHandler/unlinkLinkHandler/relinkLinkHandler/listLinksHandler/exportLinkCitationsHandler). (2) PROVIDER — a concrete LinkPersistenceProvider () => { persistence: SqliteCaseBoxPersistence; db } (ADR M1; NOT the shared CaseBoxPersistence interface); Runtime extended to expose sqlite+db; InMemory fallback -> safe boundary error for link channels. (3) DTOs — camelCase request (CreateLinkDto { matterId, sourceType, sourceId, anchorId }; UnlinkLinkDto { matterId, linkId, unlinkReason }; RelinkLinkDto { matterId, linkId }; ListLinksDto/ExportLinkCitationsDto { matterId }); main-injected tenant_id+actor_user_id; *_FORBIDDEN_FIELDS reject renderer tenantId/actorUserId + snake_case authority fields; LINK_RESPONSE_FIELDS = projected snake_case row (id, matter_id, source_type, source_id, anchor_id, status, created_at, unlinked_at, unlink_reason) excluding tenant_id + payload_json; export returns the verbatim { citations, byFlag } ExportCitationResult. NOTE (review High#1): unlinkLink/relinkLink persistence ops are UNSCOPED (linkId only), so UnlinkLinkDto/RelinkLinkDto MUST carry matterId and the handler does a SCOPED link preflight before mutating (see decision 5) — fail-closed against a guessed foreign linkId; this EXTENDS ADR §2 (which omitted matterId for unlink/relink — a docs reconciliation may fold it back, optional). (4) ERRORS — reuse IpcEnvelope + mapThrownError + the existing SAFE_MESSAGES (link-surfaced codes only: invalid_argument/unknown_matter/tenant_mismatch/duplicate_id/illegal_transition/invalid_payload; anchor_referenced NOT surfaced — no errorMap change). (5) BOUNDARIES — matter+active-tenant preflight (getMatter) -> unknown_matter/tenant_mismatch; for unlink/relink ALSO a SCOPED link-existence preflight via the runtime db (SELECT 1 FROM case_box_links WHERE id=? AND tenant_id=getActiveTenantId() AND matter_id=matterId) -> if null, invalid_payload ("linkId does not reference a link in this matter") WITHOUT calling the unscoped mutation (fail-closed, mirrors the fact-transition/docket-confirm scoped preflight, review High#1); evidence/anchor existence enforced by persistence on create; relink rejects a passed reason; unlink requires non-blank reason. (6) A0.7 — A0.7-gated custody 9b at COMMIT time; NO runtime A0.7 marker invented (ADR D4); user test 10 ("mutating calls fail closed without a valid marker") is satisfied at the dev/commit gate. (7) CONFIDENTIALITY — identifiers/status/flags only; synthetic fixtures; mapThrownError keeps detail main-side. (8) TESTS — a new ipc-link-handlers.unit.test.mjs (mock provider) registered in package.json; covers create/unlink/relink reaching persistence + DTO shape, list/export no-content, camelCase, forbidden-field rejection, actor/tenant injection (renderer rejected), tenant/matter mismatch, missing evidence/anchor, no schema bump, no evidence-content leakage. (9) renderer/api.ts (Q1 RESOLVED — review High#2) — do NOT touch renderer/api.ts or renderer/types.ts. The renderer-dto-sync gate uses a HARD-CODED PAIRS list (it does NOT iterate new canonical DTOs), so new link DTOs/preload methods force NO renderer parity; the renderer typed wrapper (api.ts/types.ts + their sync tests) is the future UI lane's job. This lane is IPC + preload(CaseBoxApi) only. (10) NO UI / NO renderer wrapper / NO PERSISTENCE-SEMANTIC / NO SCHEMA / NO CONTRACT change.
Source of truth: docs/adr/ADR-evidence-a3-link-ipc-surface.md (D1-D11 — the IPC contract); apps/lawbar-desktop/src/caseBox/{factHandlers,handlerShared,errorMap,caseBoxRuntime}.ts + dto/fact.ts + dto/shared.ts (READ-ONLY pattern — the handler/DTO/error/runtime shape to mirror); apps/lawbar-desktop/electron/ipc/caseBoxHandlers.ts + electron/preload.mts (READ-ONLY pattern — registration + bridge); services/case-box-persistence/src/sqlite/{linkRepoQueries,linkStatusResolverQueries,exportCitationQueries}.ts + src/errors.ts + src/index.ts (READ-ONLY — the link ops + the CreateLinkInput/UnlinkLinkOptions/RelinkLinkOptions/CaseBoxLinkRow types + resolve/export functions the IPC calls); .claude/rules/{autonomy,cc-suite,security-boundary,evidence-genie,client-local-first,loc-guardian}.md (user-authorized lane; HIGH-RISK IPC over court-facing state; broker required; A0.7 invariants; local-first).
Allowed files: apps/lawbar-desktop/src/caseBox/linkHandlers.ts (NEW), apps/lawbar-desktop/src/caseBox/dto/link.ts (NEW), apps/lawbar-desktop/src/caseBox/dto.ts (barrel — add ./dto/link.js export), apps/lawbar-desktop/src/caseBox/handlerShared.ts (add CHANNEL.link* + the LinkPersistenceProvider type), apps/lawbar-desktop/src/caseBox/caseBoxRuntime.ts (expose concrete sqlite + db on Runtime), apps/lawbar-desktop/electron/ipc/caseBoxHandlers.ts (register the 5 link channels), apps/lawbar-desktop/electron/preload.mts (add the 5 link CaseBoxApi methods), apps/lawbar-desktop/src/caseBox/errorMap.ts (ONE additive exhaustiveness-sync line ONLY — add the EXISTING persistence error code `anchor_referenced` to the total SAFE_MESSAGES Record so errorMap.ts compiles against the current CaseBoxPersistenceErrorCode union; NOT a new error code, NOT a taxonomy change, NO refactor; the desktop build is otherwise broken against current persistence — review High#1/amendment 2026-06-26, 1a), apps/lawbar-desktop/tests/ipc-link-handlers.unit.test.mjs (NEW — the link handler unit tests), apps/lawbar-desktop/package.json (register the NEW test file in the node --test file list — the curated-list requirement; additive test-registration only, NO dependency change), dev-memo/plan-batch-casebox-evidence-a3-link-ipc-t1-00.md (this plan), dev-memo/run/queue.md, dev-memo/run/queue.linted, dev-memo/run/queue.reviewed, dev-memo/run/queue.governed, dev-memo/run/reviews/queue-review-094.md, dev-memo/study/** (a batch-audit study packet if a window closes), dev-memo/cc-suite-reliability-log.md (only if a TIMEOUT retry occurs), dev-memo/deferred-audit-findings.md (only if a finding is deferred)
Forbidden files: apps/lawbar-desktop/renderer/** including renderer/api.ts + renderer/types.ts (NO renderer UI components/views/styles AND no renderer typed-wrapper/api-type change — the renderer-dto-sync gate's hard-coded PAIRS list does NOT force renderer parity for new link DTOs, so the renderer wrapper is the separate future UI lane; this lane is IPC + preload only), services/** (NO persistence change — the link ops are live on main; READ-ONLY), services/case-box-persistence/src/sqlite/schema.ts (NO schema/version change), docs/contracts/** (NO contract change), apps/lawbar-desktop/src/caseBox/errorMap.ts EXCEPT the one additive anchor_referenced exhaustiveness-sync line (NO other change — no new error code beyond the additive existing-code sync, no taxonomy change, no refactor; the single line is moved to Allowed per the 1a amendment), package-lock.json + apps/lawbar-desktop/package-lock.json (NO commit of the local internal-tarball integrity churn — dist-tarballs is gitignored + rebuilt via bootstrap; no CI consumes the desktop lock; restore to main), apps/lawbar-desktop/src/caseBox/handlers.ts (the barrel — NOT touched; link handlers imported directly like docket/fact, per the established barrel-reconciliation-deferred pattern), package-lock.json / any dependency manifest (NO dependency change — package.json edit is test-list-registration ONLY), native/**, .claude/**, .github/**, AGENTS.md, CLAUDE.md, GEMINI.md, scripts/workflow/**, migrations/**, committed dev-memo/run/evidence/** marker/ledger/key/evidence-run files, committed keys, dev-memo/run/reports/** (untracked local run-state — never staged), dev-memo/run/override-reason.md (untracked residue — never staged), Evidence-Genie-M0-Developer-Handover.md (root intake — READ-ONLY, never moved/deleted), xiaolai-dev-workflow-study.md (root intake — READ-ONLY, never moved/deleted); NO renderer UI, NO persistence-semantic change, NO schema/version change, NO contract change, NO new error code, NO dependency, NO native/export-rendering, NO runtime A0.7 mechanism invented, NO orphan test file
Gates: broker /cc-suite:review-plan REQUIRED before implementation (confirm A0.7-gated custody-9b classification; confirm Q1 — whether renderer/api.ts must be edited for renderer-dto-sync parity and that it is the IPC API type not UI; confirm Q2 the concrete LinkPersistenceProvider + Runtime sqlite/db exposure; confirm Q3 NO runtime A0.7 marker (dev/commit-gate only); confirm the camelCase-DTO + main-injected-actor/tenant + LINK_RESPONSE_FIELDS projection + the export { citations, byFlag } shape + the link-surfaced error codes match the ADR + the app conventions; confirm IPC-ONLY no UI); if review-plan returns NEEDS-FIX, STOP and report; if review-plan says this must NOT be A0.7-gated, STOP and report; if review-plan demands renderer UI / a schema or contract or persistence-semantic change / a dependency / a runtime A0.7 mechanism, STOP and ask; A0.7-gated check-gates.sh -> GATES OK under custody mode 9b (human mints the marker + runs check-gates.sh with the HMAC key; the agent never receives the key; the agent's own check-gates.sh fails closed exit nonzero without the key — expected); broker /cc-suite:audit + /cc-suite:verify on the implementation diff after authoring (no Critical/High/Medium open before commit); npm --prefix apps/lawbar-desktop test passes (the new link IPC unit suite + all existing desktop suites green, incl. renderer-dto-sync/renderer-api); npm --prefix services/case-box-persistence test passes (unchanged — no persistence edit); scripts/workflow/check-contract-integrity.sh exits 0; loc-guardian: no hand-written source file over the 800 fail threshold (linkHandlers.ts new well under; the test file under 1200); no services/schema/contract/dependency/native change; no renderer UI; no committed marker/ledger/key/evidence-run; root intake untouched
Acceptance criteria: createLink/unlinkLink/relinkLink IPC handlers reach the audited persistence ops and return the ADR DTO (RendererLink projected, tenant_id + payload_json excluded); listLinks runs resolveLinkStatuses then returns RendererLink[] (status/flags, no evidence content); exportLinkCitations returns the verbatim { citations, byFlag } result; camelCase request DTOs; forbidden/extra/renderer-authority fields (tenantId/actorUserId) rejected with invalid_payload (forbiddenFieldFailure/unknown-field), the trusted getActiveActorUserId/getActiveTenantId used; tenant/matter mismatch -> unknown_matter/tenant_mismatch; missing evidence/anchor -> invalid_argument; re-unlink/re-relink -> illegal_transition; a new ipc-link-handlers.unit.test.mjs (registered in package.json) proves the above incl. no evidence-content leakage in DTO snapshots; existing desktop (incl. renderer-dto-sync/renderer-api), persistence, and contract tests stay green; no schema version bump; no renderer UI; fixtures synthetic-only; the committed diff is confined to the Allowed files; npm --prefix apps/lawbar-desktop test passes; npm --prefix services/case-box-persistence test passes; scripts/workflow/check-contract-integrity.sh exits 0; A0.7-gated check-gates.sh returns GATES OK (human custody 9b); broker review-plan READY + audit no Critical/High/Medium + verify ALL CLOSED
Risk flags: IMPL — mutating IPC over court-facing audited evidence-link state — HIGH-RISK (IPC boundary + audit lifecycle exposure; broker review-plan + audit + verify REQUIRED) and A0.7-gated (custody 9b; Requires-A07: yes). MUST NOT trust renderer-supplied actor/tenant (main-injected; renderer-forbidden). MUST NOT expose evidence content/PDF/OCR over the bridge (identifiers/status/flags only). MUST NOT invent a runtime A0.7 marker (ADR D4 — A0.7 is dev/commit-time). MUST NOT add renderer UI, change persistence semantics, schema/version, the contract, a new error code, a dependency, or native code. The package.json edit is test-list-registration ONLY (no dependency change). MUST NOT weaken any Evidence invariant (offline/local-first; main-injected identity). SQLite-only link surface (concrete provider; InMemory fallback returns a safe boundary error). No push, no merge.
Depends on: none
Commit boundary: governance commit separate (dev-memo/run/queue.md + queue.linted/reviewed/governed + dev-memo/run/reviews/queue-review-094.md); ONE implementation commit -- apps/lawbar-desktop/src/caseBox/linkHandlers.ts + apps/lawbar-desktop/src/caseBox/dto/link.ts + apps/lawbar-desktop/src/caseBox/dto.ts + apps/lawbar-desktop/src/caseBox/handlerShared.ts + apps/lawbar-desktop/src/caseBox/caseBoxRuntime.ts + apps/lawbar-desktop/electron/ipc/caseBoxHandlers.ts + apps/lawbar-desktop/electron/preload.mts + apps/lawbar-desktop/src/caseBox/errorMap.ts (the one anchor_referenced line) + apps/lawbar-desktop/tests/ipc-link-handlers.unit.test.mjs + apps/lawbar-desktop/package.json (test-list line ONLY — the cosmetic mac.target reformat restored; NOT package-lock.json) (+ dev-memo/plan-batch-casebox-evidence-a3-link-ipc-t1-00.md), staged by exact path; the human-gated A0.7 marker/key/evidence files are NEVER staged; if the batch guard requires a closeout before the implementation commit, run the Layer-B closeout first; no push; no merge
