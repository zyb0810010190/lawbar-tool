## WI-A3-T1: anchor/link schema + persistence contract design (design-only; NOT A0.7-gated)
Type: PLAN
A0.7-classification: A3-T1-DESIGN is design-only and intentionally NOT A0.7-gated. This docs-only ADR/plan defines the persistence CONTRACT for A3 anchors/links; it implements no schema, no migration, no persistence code, and changes no gate/marker behavior, so it needs no live provenance-valid A0.7 marker. Therefore this WI carries NO `Requires-A07:` line. The FUTURE A3-T1 implementation/migration WI (the one that actually adds schema/persistence) MUST declare `Requires-A07: yes` and pass the A0.7 hard gate with a provenance-valid local marker. review-plan must confirm this classification; if review-plan says this docs-only WI must be A0.7-gated, STOP and report.
Scope: Author the A3 anchor/link SCHEMA + PERSISTENCE contract as an ADR + a dev-memo implementation-sequencing plan, BEFORE any migration or persistence code. Define: (1) the anchor storage model (identity, canonical page_ratio rect storage, no viewport/screen coords); (2) geometry-version binding (captured DocumentPageGeometry provenance, geometryCapturedAt as the version key, fail-closed on absent/ambiguous); (3) the link model (source/target references, LinkStatus enum valid|needs_review|broken, mismatch/replacement -> needs_review, missing target -> broken); (4) replacement/quarantine effects on link status (deterministic, no silently-stale valid coordinate); (5) deletion/cascade left UNRESOLVED per A3-CONTRACT-00 (do not invent; future A3-T6-equivalent stops-and-asks); (6) migration boundaries (first impl WI = schema only, no UI/product; later WI = resolver/status transitions; later WI = export degradation); (7) the A0.7-dependence declaration for the future implementation WIs; (8) the test strategy. Design/ADR ONLY — no code, no migration, no dependency, no app/UI/product behavior, no marker/key/gate change. Output: docs/adr/ADR-evidence-a3-schema-persistence-contract.md (A3-SCHEMA-00) + dev-memo/plan-batch-casebox-evidence-a3-schema-contract-00.md; an optional narrow cross-reference line in docs/adr/ADR-evidence-a3-anchor-link-contract.md only if review-plan recommends it.
Required decisions to record: (1) anchor storage — identity binds documentId+physicalPageIndex; stores the canonical page_ratio rect values produced by A3-T2 (12-dp fixed-point strings or the chosen exact storage type); NO viewport/screen coordinate persists; (2) geometry binding — anchor stores captured DocumentPageGeometry provenance; geometryCapturedAt is the current version key unless a stronger immutable key is introduced by a later WI; absent/ambiguous geometry provenance fails closed; (3) link model — link references source anchor / target anchor or document/page target per the A3 contract; LinkStatus includes at least valid, needs_review, broken; geometry mismatch / document replacement cannot remain valid (becomes needs_review); missing target becomes explicit broken; (4) replacement/quarantine — document replacement affects link status deterministically; no stale coordinate remains silently valid; (5) deletion/cascade — preserve the UNRESOLVED policy from A3-CONTRACT-00; do not invent delete/cascade behavior; future A3-T6 or equivalent must stop-and-ask; (6) migration boundaries — first implementation WI adds schema only (no UI/product); a later WI adds resolver/status transitions; a later WI adds export degradation; (7) A0.7 dependency — this design-only WI is NOT A0.7-gated; the future A3-T1 implementation/migration WI MUST declare Requires-A07: yes; (8) test strategy — schema tests prove canonical ratio values are stored as fixed 12-dp strings (or the chosen exact storage type); tests prove invalid geometry provenance cannot create a valid persisted anchor; tests prove replacement/mismatch is not silently valid once the resolver/status WI exists; tests prove no marker/key/evidence files enter commits.
Source of truth: docs/adr/ADR-evidence-a3-anchor-link-contract.md (A3-CONTRACT-00 — §3 invariants INV-A3-1..10, §4 headless API, §5 data-model dependencies, decision 9 cascade UNRESOLVED, decision 3 geometry-version key); native/evidence-core/lib/page-ratio.mjs (A3-T2, merged — the canonical page_ratio producer this schema persists); Evidence-Genie-M0-Developer-Handover.md (root intake; READ-ONLY) §10 entities (Anchor/Link/LinkStatus/rect/DocumentPage/DocumentPageGeometry/Document.status), §11 anchor-delete cascade undecided, A3 ticket set; docs/adr/ADR-evidence-a07-key-custody-operating-model.md (A07-KEY-00 — A3 impl WIs are A0.7-gated under custody mode 9b); .claude/rules/evidence-genie.md (inv.5/6/7); .claude/rules/client-local-first.md (local-first; tenant_id forward-compat only); .claude/rules/autonomy.md (this design lane is user-authorized; migration + new dependency are hard stops for the future impl WI).
Allowed files: docs/adr/ADR-evidence-a3-schema-persistence-contract.md, dev-memo/plan-batch-casebox-evidence-a3-schema-contract-00.md, dev-memo/** (queue/governance/review/study/plan artifacts), docs/adr/ADR-evidence-a3-anchor-link-contract.md ONLY a narrow cross-reference line if review-plan explicitly recommends it
Forbidden files: apps/**, native/** (native/evidence-core + native/evidence-core-swift — NO change), services/**, docs/contracts/**, .claude/**, .github/**, AGENTS.md, CLAUDE.md, GEMINI.md, scripts/workflow/** (NO gate/marker/script change), any migration files, committed dev-memo/run/evidence/** marker/ledger/key/evidence-run files, committed keys, Evidence-Genie-M0-Developer-Handover.md (root intake), xiaolai-dev-workflow-study.md (root intake), dev-memo/run/override-reason.md (untracked residue — never staged); NO persistence/schema/migration code, NO GRDB/SQLCipher or any new dependency, NO Evidence UI/product, NO anchor resolver/link-status/export implementation, NO marker/key handling or gate behavior change, NO OCR/AI/cloud/auth/network, NO Requires-A07 on this WI
Gates: broker /cc-suite:review-plan REQUIRED before authoring (confirm design-only / NOT-A0.7-gated; the contract governs eventual HIGH-RISK persistence/migration work); if review-plan returns NEEDS-FIX, STOP and report; if review-plan says this docs-only WI must be A0.7-gated, STOP and report; if review-plan returns READY but asks for implementation, migration, a dependency, UI, or marker/key/gate changes, STOP and ask; broker /cc-suite:audit + /cc-suite:verify on the design packet after authoring; scripts/workflow/check-contract-integrity.sh exits 0; scripts/workflow/check-gates.sh returns GATES OK (queue has no Requires-A07, no markers -> A0.7 gate not required -> pass); npm --prefix apps/lawbar-desktop test 610/610; npm --prefix native/evidence-core test 38/38 (A3-T2 stays green); no apps/native/.claude/.github/services change; no marker/writer/validator/gate change; no committed marker/ledger/key/evidence-run; root intake untouched
Acceptance criteria: docs/adr/ADR-evidence-a3-schema-persistence-contract.md EXISTS and records all 8 decision groups (anchor storage incl. canonical page_ratio storage type + no-viewport; geometry-version binding + fail-closed provenance; link model incl. LinkStatus valid|needs_review|broken + mismatch/replacement->needs_review + missing->broken; deterministic replacement/quarantine with no silently-stale valid; cascade PRESERVED unresolved; migration boundaries schema-first then resolver then export; the A0.7-dependence rule for future impl WIs; the test strategy); dev-memo/plan-batch-casebox-evidence-a3-schema-contract-00.md defines the future A3-T1 implementation WI sequence (schema-only first, A0.7-gated) + the test fixtures/strategy; the anchor-delete cascade rule is PRESERVED as explicitly unresolved (not invented); scripts/workflow/check-contract-integrity.sh exits 0; scripts/workflow/check-gates.sh returns GATES OK; npm --prefix apps/lawbar-desktop test 610/610; npm --prefix native/evidence-core test 38/38; broker review-plan READY (classification confirmed) + audit reports no Critical/High/Medium + verify ALL CLOSED; the committed design diff is confined to docs/adr/** + dev-memo/** (plus an optional A3-CONTRACT-00 cross-reference line only if review-plan approved)
Risk flags: DESIGN-ONLY (Type PLAN). Explicitly NOT A0.7-gated (no Requires-A07) — a schema/persistence CONTRACT ADR implements no migration/persistence and needs no live marker; the future A3-T1 implementation/migration WI is A0.7-gated AND is itself a persistence/migration + possible new-dependency autonomy hard stop (handled in its own authorization). No code, no migration, no dependency (no GRDB/SQLCipher), no UI/product, no resolver/export, no marker/key/gate change. broker review-plan + audit + verify REQUIRED (the contract governs eventual HIGH-RISK persistence work). The design must not weaken any Evidence invariant (page-ratio-from-A3-T2, no viewport in persistence, fail-closed provenance, no-stale-valid-on-mismatch, optimized-never-canonical). No push, no merge.
Depends on: none
Commit boundary: governance commit separate (dev-memo/run/queue.md + queue.linted/reviewed/governed + dev-memo/run/reviews/queue-review-071.md); one design commit -- docs/adr/ADR-evidence-a3-schema-persistence-contract.md + dev-memo/plan-batch-casebox-evidence-a3-schema-contract-00.md (+ an optional A3-CONTRACT-00 cross-reference line only if review-plan approved), staged by exact path; no push; no merge
