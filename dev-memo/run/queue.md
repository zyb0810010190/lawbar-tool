## WI-DC1: audit-event schema description cleanup — close DT-V1-L1
Type: SOURCE
Scope: Doc-wording-only cleanup of the audit-event schema, closing deferred Low DT-V1-L1. In docs/contracts/case-box-contract/schemas/case-box-audit-event.schema.json, edit ONLY the `event_kind` property's `description` string: replace the inaccurate "The TS validator additionally enforces that the kind's declared {action, entity_type, reasonRequired} matches the event." with language naming the chain verifier, e.g. "The chain verifier (verifyAuditChain) additionally enforces that the kind's declared {action, entity_type, reasonRequired} matches the event." (rationale: validateAuditEvent is schema-only; the cross-field consistency invariant is enforced in verifyAuditChain per Batch-1 WI-V1). Then run `npm run gen:types` so the regenerated docs/contracts/case-box-contract/src/generated/case-box-audit-event.ts comment updates to match — do NOT hand-edit the generated file. Confirm the ENTIRE diff is description/comment-only: NO change to `required`, `enum`, `additionalProperties`, `dependentRequired`, any property type/shape, validator behaviour, or canonical/golden/hash output. Flip the DT-V1-L1 row in dev-memo/deferred-audit-findings.md to `status: closed`, citing WI-DC1 (BATCH-CASEBOX-AUDIT-DOC-CLEANUP-00) + the unchanged-behaviour note in the row — do NOT embed a self-referential commit hash (the resolution commit is the one that lands this closure; its hash is reported post-commit in dev-memo/run/log.md / the PR, mirroring how prior deferred-finding closures cite "resolved-in this WI's commit"). Note `npm run gen:types` regenerates ALL generated types (it iterates every schema), but ONLY case-box-audit-event.ts must show a VCS delta — the other generated files are idempotent and must stay byte-unchanged. If gen:types changes any generated file OTHER than case-box-audit-event.ts, or changes anything in case-box-audit-event.ts beyond the comment delta, or any schema-shape diff appears, STOP and report.
Source of truth: dev-memo/deferred-audit-findings.md (DT-V1-L1 row — the deferred finding being closed); docs/contracts/case-box-contract/schemas/case-box-audit-event.schema.json:69 (the event_kind description with the "TS validator" wording); docs/contracts/case-box-contract/src/audit-log.ts verifyAuditChain (READ-ONLY — confirms the consistency check lives there, NOT in validateAuditEvent which is schema-only); scripts/gen-types.mjs (`npm run gen:types`); dev-memo/plan-batch-casebox-audit-doc-cleanup-00.md (proposal)
Allowed files: docs/contracts/case-box-contract/schemas/case-box-audit-event.schema.json, docs/contracts/case-box-contract/src/generated/case-box-audit-event.ts, dev-memo/deferred-audit-findings.md
Forbidden files: docs/contracts/case-box-contract/src/audit-log.ts, docs/contracts/case-box-contract/src/!(generated)/**, docs/contracts/case-box-contract/tests/**, docs/contracts/case-box-contract/package.json, services/**, apps/**, .claude/**, scripts/workflow/**
Gates: npm --prefix docs/contracts/case-box-contract test (393 still pass — no test change; the golden v1 + v2 canonical-hash tests are description-independent); loc-guardian scan (trivial); cc-suite review-plan + audit + verify (contract-touching → broker)
Acceptance criteria: the schema event_kind description no longer says "TS validator" and instead names the chain verifier (verifyAuditChain); the regenerated case-box-audit-event.ts comment matches (regenerated via gen:types, NOT hand-edited); the full diff is description/comment-only — NO required/enum/additionalProperties/dependentRequired/property-shape/validator-behaviour/canonical-hash change (verify with git diff: only the description string in the schema + its mirror comment in the generated type changed); `npm --prefix docs/contracts/case-box-contract test` passes (393, unchanged); gen:types produced a VCS delta in ONLY case-box-audit-event.ts (all other generated files idempotent/byte-unchanged); DT-V1-L1 is flipped to status: closed in dev-memo/deferred-audit-findings.md citing WI-DC1 + the unchanged-behaviour note (the resolution commit hash is reported post-commit in the log/PR, NOT embedded self-referentially in the row); NO audit-log.ts / services / apps / package.json / hooks / workflow change
Risk flags: contract-touching (schema + regenerated type) → cc-suite review-plan + audit + verify via broker per cc-suite §High-risk + security-boundary.md; but a PURE description-string change with zero behaviour/hash/validation impact (the description is neither hashed nor tested; golden + v2 canonical tests unaffected); gen:types regenerates ALL generated types (it iterates every schema) but only case-box-audit-event.ts should show a VCS delta (the others are idempotent/unchanged); the generated file is regenerated, never hand-edited; STOP if any non-comment generated delta or any schema-shape diff appears
Depends on: none
Commit boundary: one local commit for the schema description + regenerated comment + the DT-V1-L1 closure
