## WI-RELEASE-G12-AUDIT-CHAIN-RECOVERY-00: define + govern the M0 gate-12 audit-chain operational recovery + tamper-evidence closure (prove the hash-chained audit trail detects tampering and recovers, on a disposable fixture) (Type: EVIDENCE, release-governance MEDIUM risk)

## Review packet (compact)
- Active plan summary: gate 12 of the M0 go-live readiness matrix (`docs/release/go-live-readiness-report.md` §1 row 12) is **PARTIAL** — evidence `auditChain.ts` + the case-box-step-4-audit-log-shape ADR; the outstanding operational item is **audit-chain recovery + tamper-evidence closure** (the row notes "Operational: backup-format certification + recovery procedure (gate 14)"). This WI **governs** (does NOT execute) a FUTURE lane that runs the operational audit-chain recovery + tamper-evidence exercise on a DISPOSABLE fixture and records the transcript as evidence. The exercise proves the EXISTING hash-chained audit trail (a) detects tampering and (b) recovers a verifying chain after a restore — WITHOUT changing the audit-chain implementation. THIS authoring lane produces the governed queue WI only; it runs NO recovery exercise, changes NO source/test/contract/config, clears NO gate, and decides NO user go-live hard-stop.
- Audit-chain grounding (the mechanism the exercise proves — verify against the real source/contract before relying): the audit trail is hash-chained — each event's `eventHashFn` = SHA-256 of the contract's `canonicalAuditEventHashInput(event)` (`services/case-box-persistence/src/auditChain.ts`), linked via `prev_event_hash`; tables `case_box_audit_events` + `case_box_audit_chain_heads`. Verification is the CONTRACT function `verifyAuditChain` (`docs/contracts/case-box-contract/src/audit-log.ts`), reused by SQLite via `verifyAuditChainForMatterSqlite(db, matterId)` (`services/case-box-persistence/src/sqlite/auditRepoQueries.ts:126`) exposed as `verifyAuditChainForMatter(matterId)` → `VerifyAuditChainResult` = `ChainVerifyOk` | `ChainVerifyErr { verified:false, errorReason }`. The tamper-evidence failure modes are the contract's `ChainVerifyErrorReason` enum: `prev_event_hash_mismatch`, `prev_event_hash_non_null_for_first_event`, `before_state_hash_not_null_on_create`, `missing_after_state_hash`, `event_schema_invalid`, `event_kind_inconsistent`, `tenant_id_mismatch`, `matter_id_mismatch`. The audit invariant is `event_count == COUNT(*) == MAX(sequence)` per matter (`AGENTS.md` §"Critical invariants"). The exercise RUNS these existing verifiers against a tampered + restored fixture; it does NOT modify them.
- Exact target files (FUTURE exec lane): a NEW `docs/release/gate12-audit-chain-recovery-00.md` (the exercise procedure + transcript + result/evidence record) + the gate-12 evidence ROW of `docs/release/go-live-readiness-report.md` (update to reference the result; **gate 12 MUST remain `PARTIAL` in this WI** — add only a `[Δ] audit-chain recovery + tamper-evidence PASS` (or FAIL) marker; any `PARTIAL → CLEARED` move belongs to a SEPARATE holistic readiness-refresh WI, NOT this one — and MUST NOT clear gates 14/18/15 and MUST NOT imply gate 7 cleared) + `dev-memo/run/reviews/` (the exec lane's review artifact). NO other file. If the exec lane makes the exercise scripted, the harness is a DRILL ARTIFACT under `/tmp` or a drill-specific dir — NOT committed, NOT a product source/test change, NOT a new dependency.
- Exact acceptance criteria: below.
- Exact out-of-scope: NO recovery-exercise EXECUTION in this authoring lane; NO change to the audit-chain implementation (auditChain.ts / auditRepoQueries.ts / the contract verifier are READ-ONLY — run, not edited); NO product source/test/package/config change; NO schema/migration; NO contract/persistence change; NO gate-6 full-project audit; NO clearing of gate 14 (backup+recovery — CITED not cleared), gate 18 (export/backup-format — CITED not cleared), or gate 15 (rollback — CITED not cleared); NO implication that gate 7 is cleared; NO release-doc implementation beyond the exercise evidence doc + the gate-12 row; NO signing/notarization/公开 distribution decision (gate 4); NO 律师法 conclusion (gate 11); NO license/legal/business decision (gate 17); NO final GO/NO-GO (gate 21); NO real user data; NO new dependency (a scripted drill harness adds none); NO source/test/package/config/schema/contract change in EITHER lane (docs-only tracked output).
- Requirements the FUTURE exercise doc must define + prove (what the exec lane's exercise must specify + record) — at minimum:
  1. **Exact audit-chain fixture / disposable data directory**: a disposable `/tmp` `case-box.sqlite` (NEVER the real `~/Library/Application Support/lawbar/`), seeded via the real persistence API with a known set (N matters + documents + audit events) so the pre/post chain state is deterministic. No real case data.
  2. **Backup/restore source (if any)**: the backup-as-directory model from the gate-14 runbook (`docs/release/gate14-backup-recovery-00.md`) — CITED as supporting context (NOT clearing gate 14); the exercise backs up the disposable data dir, then restores it after a simulated tamper to demonstrate recovery of a verifying chain.
  3. **Audit-chain invariant checks**: `event_count == COUNT(*) == MAX(sequence)` per matter holds on the clean fixture and after restore; the `case_box_audit_chain_heads` anchor matches the last event's hash. State the exact query/assertion + expected result.
  4. **Tamper-evidence checks**: the exec lane FIRST verifies the exact verifier import/API names + the `ChainVerifyErrorReason` values against the real source/contract (`verifyAuditChainForMatter` in `SqliteCaseBoxPersistence.ts` / `verifyAuditChainForMatterSqlite` in `auditRepoQueries.ts` / the contract `verifyAuditChain` + enum in `audit-log.ts`) before authoring the transcript (per `AGENTS.md` §"Authoring discipline" — verify every named thing). Then it mutates a persisted audit-event row in the DISPOSABLE fixture directly (e.g. alter an `after_state_hash` / `prev_event_hash` / reorder a `sequence`), and `verifyAuditChainForMatter(matterId)` MUST return `ChainVerifyErr` with the EXPECTED `ChainVerifyErrorReason` (e.g. `prev_event_hash_mismatch` / `missing_after_state_hash`) — proving tampering is DETECTED, not silently accepted. Record the exact mutation + the returned `errorReason`.
  5. **Recovery procedure to follow**: after the detected tamper, restore the disposable data dir from the backup (gate-14 drop-directory model), then re-run `verifyAuditChainForMatter` → `ChainVerifyOk` + the invariant holds again — proving operational recovery of a verifying, tamper-evident chain.
  6. **Expected evidence artifact**: the doc records the fixture, the seed, the clean-chain verify result (`ChainVerifyOk`), the invariant results, the exact tamper mutation + detected `errorReason` (`ChainVerifyErr`), the restore steps, the post-restore verify (`ChainVerifyOk`) + invariant, and the environment (OS, node, better-sqlite3 version, date) — captured as text (NO real case data; fixture-only).
  7. **Explicit pass/fail criteria**: PASS = (clean chain verifies `ChainVerifyOk`) AND (the invariant holds pre + post) AND (the injected tamper is DETECTED as `ChainVerifyErr` with the expected `errorReason`) AND (after restore the chain verifies `ChainVerifyOk` + invariant holds). FAIL = a tamper NOT detected (false `ChainVerifyOk`), OR an invariant break, OR an unrecoverable chain. A FAIL is an audit-integrity finding (surface, do not paper over).
  8. **Manual / scripted / hybrid**: the doc DECIDES + states its mode. Default recommendation (non-binding on the exec lane): a SCRIPTED harness (a disposable `/tmp` driver, like the gate-7 crash-recovery drill) that seeds + tampers + verifies + restores; the harness is a DRILL ARTIFACT (NOT committed, NOT a product test, NOT a new dependency).
  9. **How the result feeds gate 12 WITHOUT clearing unrelated gates**: a PASS supplies the operational audit-chain recovery + tamper-evidence closure evidence. **Gate 12 MUST remain `PARTIAL` in this WI** — the exec lane adds ONLY a `[Δ] audit-chain recovery + tamper-evidence PASS` marker (using the report's existing vocabulary; roll-up bucket unchanged) and MUST NOT make a `PARTIAL → CLEARED` move, which belongs to a SEPARATE holistic readiness-refresh WI. It MUST NOT clear gates 14/18/15 and MUST NOT imply gate 7 cleared; go-live independence is kept.
  10. **Residual risks + follow-up WIs (if any)**: e.g. the exercise covers row-level tamper of a single event — cross-matter chain-head tampering or WAL-level corruption may warrant a deeper follow-up; a real (non-fixture) operational-recovery runbook step could be folded into gate 14; the tamper-injection is a disposable-fixture SQL mutation, NOT a product surface.
- Dependencies mapped: **gate 14** (backup+recovery — the restore step reuses the backup-as-directory model; CITED as supporting context, NOT cleared); **gate 18** (export/backup-format certification — related recovery/format context; CITED, NOT cleared); **gate 15** (rollback dry-run PASS — adjacent operational evidence; CITED, does NOT clear gate 12); **gate 7** (crash-recovery drill PASS — adjacent robustness evidence; informs, gate 7 stays user-decision-dependent D-G7-1/D-G7-2, NOT cleared); **gate 6** (full-project audit — LATER; a tamper-evidence FAIL would be a finding there). These are noted as related, NOT cleared by this WI.
- Essential refs: `services/case-box-persistence/src/auditChain.ts` (eventHashFn + hash-chain) + `services/case-box-persistence/src/sqlite/auditRepoQueries.ts` (verifyAuditChainForMatterSqlite) + `docs/contracts/case-box-contract/src/audit-log.ts` (verifyAuditChain + ChainVerifyErrorReason enum) — the audit-chain surface (READ-ONLY, the ground truth); `AGENTS.md` §"Critical invariants" (audit-chain `event_count==COUNT==MAX(sequence)`; stable error codes); `docs/release/gate14-backup-recovery-00.md` (backup-as-directory restore — supporting context) + `docs/release/gate7-crash-recovery-drill-00.md` (the crash-recovery drill that also checked the invariant across crash+restore — adjacent evidence); `docs/release/go-live-readiness-report.md` §1 gate 12 (PARTIAL) (+ related gates 14/18/15/7/6); the case-box-step-4-audit-log-shape ADR; `.claude/rules/client-local-first.md`; `AGENTS.md` §"Source hierarchy" + §"Authoring discipline"; `.claude/rules/security-boundary.md` §"Go-live independence" + §"No silent surface changes" (do NOT rename/remove error codes).
- Review questions: (1) Does the WI correctly scope an operational audit-chain recovery + tamper-evidence EXERCISE (procedure + transcript evidence) on a DISPOSABLE fixture, NOT a change to the audit-chain implementation and NOT an execution in this authoring lane? (2) Are the tamper-evidence checks grounded in the real verifier (`verifyAuditChainForMatter` → `ChainVerifyErr` with a real `ChainVerifyErrorReason`) and the invariant (`event_count==COUNT==MAX(sequence)`), run against the existing code (not re-implemented)? (3) Does the recovery step reuse the gate-14 backup-as-directory model + cite gates 14/18/15/7 as supporting/adjacent context WITHOUT clearing them? (4) Does it keep gate 12 PARTIAL (existing vocabulary), not clear gates 14/18/15, not imply gate 7 cleared, and defer gate 6 later? (5) Does it keep fixture-only (no real data), add no dependency, and change no source/test/contract/schema? (6) Is any recovery-exercise execution, audit-chain implementation change, error-code rename, product test, or dependency smuggled in?

Type: EVIDENCE
Scope: Govern (do NOT execute) a FUTURE lane that runs the M0 gate-12 audit-chain operational recovery + tamper-evidence exercise. The FUTURE execution lane produces `docs/release/gate12-audit-chain-recovery-00.md` — defining requirements 1–10 above (disposable audit-chain fixture; backup/restore source; audit-chain invariant checks `event_count==COUNT==MAX(sequence)`; tamper-evidence checks via `verifyAuditChainForMatter` → `ChainVerifyErr` with a real `ChainVerifyErrorReason`; recovery procedure = restore + re-verify `ChainVerifyOk`; expected evidence artifact; explicit pass/fail; manual/scripted/hybrid mode; how the result feeds gate 12 without clearing unrelated gates; residual risks) — then runs the exercise on a DISPOSABLE `/tmp` fixture (never real case data; any scripted harness is a non-committed drill artifact, no new dependency), records the transcript, and updates ONLY the gate-12 evidence row of `docs/release/go-live-readiness-report.md` to reference the result, where **gate 12 MUST remain `PARTIAL` in this WI** — the exec lane adds ONLY a `[Δ] audit-chain recovery + tamper-evidence PASS` (or FAIL) marker (existing vocabulary; roll-up bucket unchanged); any `PARTIAL → CLEARED` move belongs to a SEPARATE holistic readiness-refresh WI, NOT this one — and it MUST NOT clear gates 14/18/15 and MUST NOT imply gate 7 cleared. It RUNS the EXISTING audit-chain verifier + invariant (does NOT re-implement or edit them, does NOT rename/remove any `ChainVerifyErrorReason` / error code), cites gates 14/18/15/7 as supporting/adjacent context WITHOUT clearing them, and DEFERS gate 6 (later). It PRESERVES every still-non-cleared gate + the STOP-AND-ASK gates (4/11/17/21); it makes NO final GO/NO-GO, resolves NO STOP-AND-ASK, runs NO gate-6 audit, changes NO audit-chain implementation/contract/schema, touches NO real user data, adds NO dependency, edits NO brief/ADR, and changes NO product source/test. THIS authoring lane implements + runs + decides NOTHING — it only governs the gate-12 exercise WI. If review asks to run the exercise, change the audit-chain implementation, rename an error code, clear gate 14/18/15, imply gate 7 cleared, run gate 6, or add a dependency, REJECT and report.
Source of truth: services/case-box-persistence/src/auditChain.ts + services/case-box-persistence/src/sqlite/auditRepoQueries.ts + docs/contracts/case-box-contract/src/audit-log.ts (verifyAuditChain + ChainVerifyErrorReason — the audit-chain surface, READ-ONLY ground truth); AGENTS.md §"Critical invariants" (event_count==COUNT==MAX(sequence); stable OcrQueueError/audit error codes — do NOT rename/remove); docs/release/gate14-backup-recovery-00.md + docs/release/gate7-crash-recovery-drill-00.md (backup-as-directory restore + crash-recovery-invariant evidence — supporting/adjacent context); docs/release/gate15-rollback-dry-run-00.md (adjacent operational evidence); docs/release/go-live-readiness-report.md §1 gate 12 (PARTIAL) + gates 14/18/15/7/6; the case-box-step-4-audit-log-shape ADR; .claude/rules/security-boundary.md §"Go-live independence" + §"No silent surface changes"; .claude/rules/client-local-first.md; AGENTS.md §"Source hierarchy" + §"Authoring discipline"; .claude/rules/autonomy.md + staging-hygiene.md + loc-guardian.md
Allowed files: docs/release/gate12-audit-chain-recovery-00.md (the NEW exercise evidence doc the FUTURE lane produces) + docs/release/go-live-readiness-report.md (the gate-12 evidence ROW ONLY + any narrow roll-up-consistency edit strictly required by a gate-12 status move) + dev-memo/run/reviews/ (the exec lane's own review artifact). NOTE: THIS authoring lane commits ONLY the queue governance (queue.md/linted/reviewed/governed) + reviews/queue-review-152.md — NO exercise doc, NO recovery run, NO report change. The FUTURE exec lane's scripted harness (if any) is a DISPOSABLE /tmp drill artifact (NOT committed); it records its evidence in the COMMIT MESSAGE + review artifact and does NOT re-stage this lane's queue governance.
Forbidden files: docs/release/ OTHER than the NEW gate12-audit-chain-recovery-00.md + the gate-12 row of go-live-readiness-report.md (the other audit/assessment/policy/drill/procedure/cert/dry-run reports are READ-ONLY evidence — NOT edited; the gate-14 runbook + gate-7 drill + gate-15 dry-run + gate-18 cert are READ-ONLY, CITED not cleared), docs/adr/ + docs/product/ (READ-ONLY — NO brief/ADR edit), docs/contracts/ (READ-ONLY — the audit-log contract + verifier are run, NOT edited; NO error-code rename/removal), apps/ + services/ (NO product source/test/renderer/IPC/persistence change — auditChain.ts / auditRepoQueries.ts are READ-ONLY, run not edited), services/*/src/sqlite/schema.ts (CURRENT_SCHEMA_VERSION stays 12), migrations/, native/, package.json/package-lock.json of any package (NO new dependency), dev-memo/run/intake/ (raw samples — never staged), dev-memo/ OTHER than run/reviews/ (no other plan/design edit), .claude/ + .github/; NO recovery-exercise RUN in this authoring lane; NO audit-chain implementation change; NO ChainVerifyErrorReason / error-code rename or removal; NO gate-6 full-project audit; NO clearing of gate 14/18/15; NO implying gate 7 cleared; NO signing/notarization/public-distribution decision (gate 4); NO 律师法 conclusion (gate 11); NO license/legal/business decision (gate 17); NO final GO/NO-GO decision (gate 21); NO real user data. Push is NEVER automatic (autonomy hard-stop): local commit governance is the default; any branch push + PR open happens ONLY under explicit per-invocation user authorization; NO merge; NO branch delete of shared branches
Gates: EVIDENCE/governance lane (MEDIUM release-governance risk — governs an audit-chain recovery + tamper-evidence exercise WI; no code, no recovery run, no audit-chain change, no go-live decision). (1) THIS authoring lane: broker /cc-suite:review-plan on this queue WI, Path 1, retrievable, then govern. (2) The FUTURE exec lane: RUN the exercise on a disposable fixture (seed + tamper + verify + restore), produce the evidence doc + the gate-12 row update, then broker /cc-suite:review-plan on the produced artifacts. (3) scripts/workflow/check-contract-integrity.sh exit 0; scripts/workflow/check-queue.sh PASS (both lanes). Forbidden-scope scan before commit: CURRENT_SCHEMA_VERSION EQUALS 12; no product source/test/package/schema/contract change; no audit-chain change; no error-code rename; no recovery run; no gate-6 run; no new dependency; no gate 14/18/15 clear; no gate-7 clear implication; no GO/NO-GO; no raw sample. STOP-points: review asks to run the exercise, change the audit-chain implementation, rename an error code, clear gate 14/18/15, imply gate 7 cleared, run gate 6, or add a dependency → REJECT/STOP and report; any forbidden file would change → STOP.
Acceptance criteria: check-queue and check-contract-integrity PASS and CURRENT_SCHEMA_VERSION EQUALS 12 and the diff CONTAINS only the queue governance (queue.md/linted/reviewed/governed) + reviews/queue-review-152.md and no docs/source/test/package/schema/contract change appears and no recovery run / no audit-chain change / no error-code rename / no new dependency appears. The governed WI must DEFINE the ten exercise requirements (disposable fixture; backup/restore source; invariant checks event_count==COUNT==MAX(sequence); tamper-evidence via verifyAuditChainForMatter → ChainVerifyErr with a real ChainVerifyErrorReason; recovery = restore + re-verify ChainVerifyOk; evidence artifact; explicit pass/fail; manual/scripted/hybrid; feeds-gate-12-without-clearing-others; residual risks), GROUND the tamper-evidence + invariant in the real verifier (run, not re-implemented), keep gates 14/18/15 uncleared + not imply gate 7 cleared + defer gate 6, keep gate 12 PARTIAL after governance, and MUST NOT decide a STOP-AND-ASK hard-stop or assert go-live. review-plan (this lane) must RETURN READY.
Risk flags: MEDIUM (release-governance — an audit-chain exercise WI that could over-claim readiness, drift into changing the audit-chain implementation, rename an error code, or clear a dependent gate). Care points: (a) NO EXERCISE RUN in this authoring lane — this lane only governs the WI; (b) IMPLEMENTATION READ-ONLY — auditChain.ts / auditRepoQueries.ts / the contract verifier + its ChainVerifyErrorReason enum are RUN, not edited/renamed (a code rename/removal is a security-boundary + contract hard-stop); (c) FIXTURE-ONLY / NO REAL DATA — the tamper is a SQL mutation on a disposable /tmp fixture, never real case data; (d) TAMPER MUST BE DETECTED — PASS requires the injected tamper to surface as ChainVerifyErr with the expected errorReason (a false ChainVerifyOk is a FAIL, surfaced not papered over); (e) DEPENDENT GATES UNCLEARED — gates 14/18/15 CITED as supporting/adjacent context not cleared, gate 7 not implied cleared, gate 6 later; (f) GO-LIVE INDEPENDENCE — an audit-chain exercise does NOT imply go-live; the final verdict + the STOP-AND-ASK hard-stops (4/11/17/21) remain the user's; (g) NO NEW DEPENDENCY / NO PRODUCT TEST — a scripted harness is a disposable drill artifact, not committed. If any cannot hold, STOP and report.
Depends on: none
Commit boundary: TWO separate lanes. (1) THIS authoring lane commits ONLY the queue governance (queue.md/linted/reviewed/governed) + reviews/queue-review-152.md — no exercise doc, no recovery run, no report change, no implementation, no decision. (2) The FUTURE execution lane commits ONLY the new docs/release/gate12-audit-chain-recovery-00.md + the gate-12 evidence-row update to go-live-readiness-report.md (+ any narrow roll-up-consistency edit strictly required by a gate-12 status move) + its own review artifact under dev-memo/run/reviews/, records its evidence (incl. the tamper mutation + detected errorReason + post-restore verify) in the COMMIT MESSAGE, and does NOT re-stage this lane's queue governance. Any scripted harness is a DISPOSABLE /tmp drill artifact (NOT committed). NO product source/test/package/schema/contract file in either lane; NO audit-chain implementation change; NO error-code rename; NO new dependency; NO staging of dev-memo/run/intake/ or untracked residue; push is NEVER automatic (autonomy hard-stop) — local commit governance is the default, any branch push + PR open happens ONLY under explicit per-invocation user authorization; no merge; no branch delete of shared branches. THIS authoring lane implements NOTHING, runs NO exercise, and decides NOTHING — it only governs the gate-12 audit-chain recovery + tamper-evidence WI.

## Acceptance criteria (FUTURE execution lane — what the governed exercise doc must satisfy)

These describe the deliverable the FUTURE exec lane must produce; they are NOT preconditions of THIS authoring lane. The authoring-lane acceptance is the structured `Acceptance criteria:` field above (queue governance + review READY only — the exercise doc does NOT exist yet and MUST NOT be created in this lane).

1. A NEW `docs/release/gate12-audit-chain-recovery-00.md` exists defining requirements 1–10 above and recording the exercise transcript + result.
2. The exercise ran against the EXISTING audit-chain verifier (`verifyAuditChainForMatter` / the contract `verifyAuditChain`) on a DISPOSABLE `/tmp` fixture — the clean chain verifies `ChainVerifyOk`, the invariant `event_count==COUNT==MAX(sequence)` holds, an injected tamper is DETECTED as `ChainVerifyErr` with the expected `ChainVerifyErrorReason`, and after restore the chain verifies `ChainVerifyOk` + invariant holds. No audit-chain implementation/contract/schema change; no error-code rename.
3. The evidence records the fixture, seed, clean verify, invariant, the exact tamper mutation + detected `errorReason`, the restore steps, the post-restore verify, the environment, and an explicit PASS/FAIL — fixture/disposable only, no real data; any scripted harness is a non-committed drill artifact (no new dependency).
4. The gate-12 ROW references the exercise; gate 12 **MUST remain `PARTIAL`** in this WI — add ONLY a `[Δ] audit-chain recovery + tamper-evidence PASS` marker (roll-up bucket unchanged); any `PARTIAL → CLEARED` move belongs to a SEPARATE holistic readiness-refresh WI. MUST NOT clear gates 14/18/15 and MUST NOT imply gate 7 cleared. Roll-up-line semantics kept consistent.
5. The doc cites gates 14/18/15/7 as supporting/adjacent context (not cleared), defers gate 6 (later), and keeps go-live independence (no sign-off; gates 4/11/17/21 remain user STOP-AND-ASK).
6. NO product source/test/package/config/schema/contract change in either lane; NO audit-chain implementation change; NO error-code rename; `CURRENT_SCHEMA_VERSION` stays 12; docs-only tracked output.
7. `scripts/workflow/check-queue.sh` PASS + `scripts/workflow/check-contract-integrity.sh` PASS on both lanes.
