## WI-EVW3: least-privilege .claude/agents scaffolding for the Evidence/feature workflow
Type: SCAFFOLD
Scope: Create six least-privilege agent definitions under .claude/agents/ (new directory) that the future /feature-workflow and /evidence-workflow commands (EVW4/EVW6) will chain. Definition files only (YAML frontmatter + role prompt); no command/hook/rule wiring, no product code, no Evidence behavior. Separation of duties is carried by each agent's tools: frontmatter (Claude Code grants a subagent only its listed tools — hard-enforced tool presence/absence); path-scoped write and commit-conditionality are documented as soft/policy surfaces enforced elsewhere (reviewer + existing commit hooks), NOT claimed as agent-level hard guarantees.
Source of truth: docs/adr/ADR-evidence-m0-xiaolai-workflow-composition.md (EVW-00, decisions D1-D7); dev-memo/plan-batch-casebox-evidence-workflow-port-00.md sections 4.2 (Lawbar .claude plan agent table) and 5 (WI-EVW3); AGENTS.md section "Project layout" (.claude/ holds agents) and section "Evidence-Genie M0 workflow composition".
Allowed files: .claude/agents/planner.md, .claude/agents/test-designer.md, .claude/agents/implementer.md, .claude/agents/reviewer.md, .claude/agents/evidence-invariant-reviewer.md, .claude/agents/release-steward.md
Forbidden files: .claude/settings.json, .claude/commands/**, .claude/hooks/**, .claude/rules/**, AGENTS.md, CLAUDE.md, GEMINI.md, services/**, docs/contracts/**, apps/**, native/**, package.json
Gates: each agent .md has valid YAML frontmatter containing name, description, and tools; read-only agents (planner, test-designer, reviewer, evidence-invariant-reviewer) contain a tools line equal to "Read, Grep, Glob" and do NOT contain Write/Edit/Bash in frontmatter; implementer frontmatter contains Write and Edit; release-steward frontmatter contains Bash AND the body states git operations require explicit human.ack / user instruction and exact staging; a static structural grep check passes (no harness load command is asserted because none exists in-repo); npm --prefix apps/lawbar-desktop test passes (610/610); scripts/workflow/check-gates.sh returns GATES OK
Acceptance criteria: ls .claude/agents returns exactly the six named .md files; grep shows planner, test-designer, reviewer, and evidence-invariant-reviewer each contain a tools line equal to "Read, Grep, Glob" and do NOT contain Write or Edit or Bash; grep shows implementer contains Write and Edit; grep shows release-steward contains Bash and contains human.ack / explicit-user-instruction language; npm desktop test output shows pass 610 fail 0; check-gates returns GATES OK; the committed diff touches only the six .claude/agents files
Risk flags: orchestration scaffolding only; inert markdown (no behavior until EVW4/EVW6 wire commands); NOT an autonomy hard-stop; NOT high-risk per cc-suite (no persistence/security-code/cloud/public-API/runtime/migration/LLM). Enforcement distinction (recorded, not hidden): tools: presence/absence is HARD-enforced by Claude Code agent tool grants; path-scoped write is NOT expressible in tools: and stays SOFT/policy; release-steward conditional git use is SOFT at the agent level and HARD only at the commit hooks (batch-commit-guard human.ack + block-git-add-all + staging-hygiene)
Depends on: WI-EVW2 (AGENTS.md composition section committed 6e0cb87)
Commit boundary: one local commit -- the six .claude/agents files only, staged by exact path; no push
