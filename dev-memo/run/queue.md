## WI-EVW7: scaffold native/evidence-core deterministic-JSON harness contract shim (JS-only)
Type: SCAFFOLD
Scope: Scaffold a dependency-free Node deterministic-JSON CONTRACT SHIM under native/evidence-core/ exposing the ten harness commands. version and healthcheck are utility commands that return status "passed" (exit 0); the eight gate commands (renderer-conformance, geometry-roundtrip, citation-stability-gate, coordinate-roundtrip, a3-regression, snapshot-verify, golden-export, compress-readability-fixtures) return status "not_implemented" which FAILS (exit non-zero), never passes. This is NOT the real native Evidence Core: NO Swift/SwiftPM/PDFKit, NO new npm dependency, NO real A0.7/PDFKit behavior, NO Evidence UI, NO hooks, NO apps changes, NO OCR/AI-VLM/cloud/auth/network/anchors/export/snapshot/compression. NO marker is created and nothing is written under dev-memo/run/evidence/. Also apply the minimal broker-requested stale-wording fix to the port plan §5 EVW7 line (8 commands -> 10-command surface; clarify 8 gates return not_implemented failures and version/healthcheck are utility passes).
Source of truth: docs/adr/ADR-evidence-m0-xiaolai-workflow-composition.md (EVW-00 D5/D7); dev-memo/plan-batch-casebox-evidence-workflow-port-00.md (§4.4 + §5 EVW7); AGENTS.md (§"Evidence-Genie M0 workflow composition"; Node 22+ runtime); .claude/rules/evidence-genie.md (not_implemented-fails, A0.7-first, no fabricated marker); .claude/commands/evidence-geometry-gate.md (wraps renderer-conformance).
Allowed files: native/evidence-core/** (new tree: cli.mjs, lib/**, package.json, README.md, tests/**), dev-memo/plan-batch-casebox-evidence-workflow-port-00.md (ONLY the §5 EVW7 stale-wording fix: 8 commands -> 10-command surface + the 8-gates/2-utility clarification, per broker review-plan-mqp9a4cd-d2mhnz Low #1)
Forbidden files: apps/**, .claude/hooks/**, .claude/settings.json, .claude/rules/**, .claude/commands/**, .claude/agents/**, AGENTS.md, CLAUDE.md, GEMINI.md, services/**, docs/contracts/**, dev-memo/run/evidence/**, dev-memo/run/queue.md, root intake files; NO Package.swift / *.swift; NO new npm dependency
Gates: broker /cc-suite:review-plan READY before impl (review-plan-mqp9a4cd-d2mhnz; HIGH-RISK new package tree) — no self-review; broker /cc-suite:audit + /cc-suite:verify after impl (HIGH-RISK); each of the eight gate commands returns status not_implemented and exits non-zero (capture stdout/stderr AND exit code separately, parse JSON separately — no pipe masking); version and healthcheck return status passed and exit 0; output is deterministic JSON (no timestamps/randomness) with a stable schemaVersion and fields ok/command/status/diagnostics and result-or-error; an unknown/malformed command returns deterministic error JSON and exits non-zero; NO command writes a marker or any path under dev-memo/run/evidence/ (pre/post content comparison if the dir exists); no *.swift / Package.swift added; no new npm dependency added; the package-local test passes; scripts/workflow/check-contract-integrity.sh exits 0; npm --prefix apps/lawbar-desktop test passes (610/610); scripts/workflow/check-gates.sh GATES OK
Acceptance criteria: native/evidence-core/ exists with a Node CLI exposing the ten commands; node native/evidence-core/cli.mjs version returns ok true status passed with a stable schemaVersion and exits 0; healthcheck returns status passed and exits 0; renderer-conformance returns status not_implemented with ok false and a non-zero exit; the other seven gate commands likewise return not_implemented + non-zero exit; an unknown command returns deterministic error JSON with a non-zero exit; the README states this is NOT the Swift/PDFKit A0.7 implementation and names the 8 gate commands as not_implemented failures and version/healthcheck as utility passes; grep shows no command emits a green A0.7 marker and a pre/post check shows nothing written under dev-memo/run/evidence/; no *.swift or Package.swift; no new npm dependency; the port plan §5 EVW7 line reads "10-command surface" not "8 commands"; npm desktop test shows pass 610 fail 0; check-gates GATES OK; broker audit returns no Critical/High/Medium and verify confirms ALL CLOSED; the committed diff is confined to native/evidence-core/** plus the single port-plan wording fix
Risk flags: HIGH-RISK — new package tree + harness command surface; broker review-plan READY + broker audit+verify after. AUTONOMY ASSESSMENT (broker-confirmed): the JS-only shim is dependency-free (existing Node 22+) and is NOT an autonomy hard-stop; introducing Swift/SwiftPM/PDFKit, any new npm dependency, or any write outside native/evidence-core/** IS a hard-stop and is DEFERRED to a separate explicitly-authorized WI.
Depends on: WI-EVW1 (ADR + port plan committed c8dc19d)
Commit boundary: one local commit -- native/evidence-core/** plus the single port-plan §5 wording fix, staged by exact path; no push
