## WI-DTO1: Split src/caseBox/dto.ts into per-entity modules behind a barrel (closes DTO-LOC-797)
Type: IMPL
Scope: Behavior-preserving SOURCE refactor to resolve DTO-LOC-797 (`apps/lawbar-desktop/src/caseBox/dto.ts` at 797/800 raw LOC, one DTO addition from breaching the loc-guardian 800 hand-source fail cap). Split `dto.ts` into per-entity modules under a NEW `apps/lawbar-desktop/src/caseBox/dto/` directory and turn `dto.ts` into a thin BARREL that re-exports them (`export * from "./dto/<entity>.js"`), so all existing importers keep `import ... from "./dto.js"` UNCHANGED. Module layout: `dto/shared.ts` (IpcEnvelope + MAX_LIST_LIMIT + MAX_CURSOR_LENGTH), `dto/matter.ts`, `dto/audit.ts`, `dto/document.ts`, `dto/deadline.ts`, `dto/docket.ts`, `dto/fact.ts`. Each entity module carries that entity's DTO interfaces + `*_DTO_FIELDS` + `*_FORBIDDEN_FIELDS` + `*_RESPONSE_FIELDS` allowlists + renderer-row Pick types + Result types + enum-validation constants, importing ONLY type-only `case-box-persistence`/`case-box-contract` + `dto/shared.ts`. Entity modules MUST NOT import the barrel `dto.ts` (circular). PRESERVE every exported symbol name + every DTO field, forbidden field, response allowlist, enum-validation constant, limit, and result type VERBATIM (move, not rewrite). NO DTO field add/remove, NO behavior change, NO IPC-channel change, NO renderer-UX change, NO persistence/contract change, NO opportunistic cleanup. Also update `apps/lawbar-desktop/tests/renderer-dto-sync.test.mjs` so its `extractFieldArrays` canonical reader reads the split `src/caseBox/dto/*.ts` modules instead of the single `dto.ts` (the barrel no longer contains the `Object.freeze` blocks) — WITHOUT weakening the assertions: an empty extraction must NOT pass, and the renderer↔canonical set-equality + `*_RESPONSE_FIELDS` authority-exclusion checks must hold. Close DTO-LOC-797 in dev-memo/deferred-audit-findings.md only after the split is implemented and all gates pass. If the split forces ANY change to the 9 importers / electron/preload.mts / dto-contract.test.mjs, STOP and report.
Source of truth: apps/lawbar-desktop/src/caseBox/dto.ts (the 797-LOC file to split + its section structure); apps/lawbar-desktop/tests/renderer-dto-sync.test.mjs (the regex-coupled canonical reader); apps/lawbar-desktop/tests/dto-contract.test.mjs (symbol-import consumer — must keep resolving from the barrel, untouched); dev-memo/deferred-audit-findings.md (DTO-LOC-797 row); dev-memo/plan-batch-casebox-dto-split-00.md (proposal); apps/lawbar-desktop/tsconfig.json (NodeNext, no verbatimModuleSyntax — export * re-exports types+values)
Allowed files: apps/lawbar-desktop/src/caseBox/dto.ts, apps/lawbar-desktop/src/caseBox/dto/**, apps/lawbar-desktop/tests/renderer-dto-sync.test.mjs, dev-memo/deferred-audit-findings.md
Forbidden files: apps/lawbar-desktop/src/caseBox/matterHandlers.ts, apps/lawbar-desktop/src/caseBox/auditHandlers.ts, apps/lawbar-desktop/src/caseBox/documentHandlers.ts, apps/lawbar-desktop/src/caseBox/deadlineHandlers.ts, apps/lawbar-desktop/src/caseBox/docketHandlers.ts, apps/lawbar-desktop/src/caseBox/factHandlers.ts, apps/lawbar-desktop/src/caseBox/handlerShared.ts, apps/lawbar-desktop/src/caseBox/errorMap.ts, apps/lawbar-desktop/electron/preload.mts, apps/lawbar-desktop/electron/ipc/**, apps/lawbar-desktop/renderer/**, services/**, docs/contracts/**, apps/lawbar-desktop/tests/dto-contract.test.mjs, apps/lawbar-desktop/tests/_view-matter-dom.mjs, apps/lawbar-desktop/tests/ipc-handlers.unit.test.mjs, apps/lawbar-desktop/tests/ipc-casebox-handlers.unit.test.mjs, apps/lawbar-desktop/tests/renderer-deadline-write.test.mjs, apps/lawbar-desktop/tests/renderer-view-matter.test.mjs, .claude/settings.json
Gates: npm --prefix apps/lawbar-desktop test
Acceptance criteria: `dto.ts` is a barrel that re-exports the per-entity `dto/*.ts` modules and drops to barrel size (well under 800; target <= ~40 LOC); each `dto/*.ts` module is well below the 800 hand-source fail cap; the public exported-symbol set of the barrel is UNCHANGED vs pre-split (a re-export-completeness check / the build + dto-contract.test.mjs symbol imports prove every DTO/forbidden/response/result symbol still resolves from `./dto.js`); NO importer (`{matter,audit,document,deadline,docket,fact}Handlers.ts`, `handlerShared.ts`, `errorMap.ts`, `electron/preload.mts`) is modified; `dto-contract.test.mjs` is untouched and still passes; `renderer-dto-sync.test.mjs` reads the split modules and its renderer↔canonical set-equality + `*_RESPONSE_FIELDS` authority-exclusion assertions still pass (no empty-extraction pass-through); no entity module imports the barrel (no circular import); `npm --prefix apps/lawbar-desktop test` passes with the SAME total test count as before the split; loc-guardian scan clean; DTO-LOC-797 recorded closed citing this WI; no behavior/DTO-field/IPC-channel/renderer-UX/persistence/contract change
Risk flags: behavior-preserving SOURCE refactor of src/caseBox (no services/contract change → not persistence/security high-risk); the one required coupling change is renderer-dto-sync.test.mjs's canonical reader (must not weaken the assertion / must reject empty extraction); circular-import hazard avoided by entity->shared->contract layering (no entity imports the barrel); barrel completeness verified by build + dto-contract symbol imports + a symbol-set check; broker review-plan + audit + verify required; not Type UI (no renderer path) so no Design-artifact gate
Depends on: none
Commit boundary: one local commit for the dto.ts barrel + dto/*.ts modules + renderer-dto-sync.test.mjs reader update + DTO-LOC-797 ledger closure
