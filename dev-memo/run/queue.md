## WI-A3-LINK-D1-DESIGN-00: design closure path for the real-db Electron link round-trip (Type: PLAN)
Type: PLAN
Scope: DESIGN-ONLY. Author ONE architecture decision record, docs/adr/ADR-evidence-a3-link-d1-roundtrip-closure.md, that chooses + justifies the closure path for deferred finding LINK-IPC-T1-D1 (the real-db Electron `casebox:link:*` create→list→export→unlink→relink round-trip is uncovered: `createLink` requires a scoped `case_box_anchors` row via `existsScoped` at `services/case-box-persistence/src/sqlite/linkRepoQueries.ts:211`; there is NO anchor write path anywhere in `services/case-box-persistence/src` and NO `casebox:anchor:*` IPC; a plain-`node --test` direct DB seed is Electron-ABI-blocked by the desktop `better-sqlite3` binding). The ADR MUST: (1) compare at least three options — Option 1 a minimal PRODUCT `casebox:anchor:*` IPC surface (real anchor creation), Option 2 a SANCTIONED Electron integration-test anchor-seed capability (test-only), Option 3 keep D1 deferred; (2) for Option 1, analyze auditability, commit-time A0.7 gating, main-process actor/tenant injection, tenant/matter/document/page/anchor scope checks, DTO projection, product-visibility — AND the dependency on the A0.7 geometry gate, because `case_box_anchors` rows carry page-ratio geometry (`coordinate_space='page_ratio'`, `origin_ref='DocumentPageGeometry'`, rect_x/y/width/height, geometry_captured_at, page_rotation; schema.ts:579) so real anchor creation is geometry-capture work governed by `.claude/rules/evidence-genie.md` invariants #3 (A0.7 is the first real Evidence architecture gate; nothing builds on it until green) + #4 (no Evidence UI before A0.7 green) + #6 (anchors ratio-based against captured page geometry); (3) for Option 2, prove the seed cannot be invoked by renderer/product code, cannot ship as a user-facing backdoor, and cannot bypass audit/security in production — grounded in the already-proven repo mechanism (Playwright `_electron` + `electronApp.evaluate()` runs code INSIDE the Electron main process from the test driver, used today in `tests/smoke.electron.test.mjs:139` / `tests/smoke.packaged.electron.test.mjs:181`), so a test-DRIVER-injected direct DB seed adds ZERO product/IPC/preload surface; (4) recommend ONE path; (5) define the EXACT next implementation WI (file scope + test strategy); (6) define the precise acceptance criteria under which D1 may be marked closed (a real-db Electron round-trip proving: anchor seeded → createLink succeeds → listLinks/resolve sees the link with the right resolver status → export/read observes the correct resolver/export-flag state → unlink/relink lifecycle coherent if included → NO evidence content leaks into logs/fixtures/UI snapshots); (7) include a `## Review packet (compact)` section (this ADR governs eventual high-risk IPC/test-harness/security work, so review-plan is REQUIRED per .claude/rules/cc-suite.md §"High-risk WIs"). Also update dev-memo/deferred-audit-findings.md to CLARIFY D1's planned closure path ONLY (add a "Planned closure path" note pointing at this ADR) — D1 MUST NOT be marked closed (closure happens only after the future impl WI proves the round-trip). NO product/runtime/test code is written in this lane; the ADR authorizes nothing by existing.
Source of truth: services/case-box-persistence/src/sqlite/linkRepoQueries.ts (createLink anchor/evidence existence checks); services/case-box-persistence/src/sqlite/schema.ts §case_box_anchors (the geometry-bearing anchor table, V11, lines 550-599); services/case-box-persistence/tests/hardening-{anchor-delete-guard,link-status-resolver,export-citation}.test.mjs (the existing direct-SQL `insertAnchor`/`opEvidence` seed pattern the test-seed option mirrors); apps/lawbar-desktop/tests/casebox-ipc.electron.test.mjs + apps/lawbar-desktop/tests/_launch-with-pid-log.mjs + apps/lawbar-desktop/tests/smoke.electron.test.mjs (the Playwright `_electron` launcher + `electronApp.evaluate()` precedent); apps/lawbar-desktop/tests/ipc-link-handlers.unit.test.mjs:27-35 (the recorded ABI-block + deferral); docs/adr/ADR-evidence-a3-link-ipc-surface.md §4 (A0.7 is dev/commit-time, NOT runtime); docs/adr/ADR-evidence-a3-anchor-link-contract.md §3-4 (the headless `createAnchor` geometry contract — design-only, unimplemented); docs/adr/ADR-evidence-a07-renderer-conformance-gate.md (the A0.7 gate, itself a future gate); .claude/rules/evidence-genie.md (invariants #3/#4/#6/#10); .claude/rules/{cc-suite,autonomy,execution-discipline,client-local-first,security-boundary,staging-hygiene}.md; dev-memo/deferred-audit-findings.md (LINK-IPC-T1-D1)
Allowed files: docs/adr/ADR-evidence-a3-link-d1-roundtrip-closure.md, dev-memo/deferred-audit-findings.md, dev-memo/run/queue.md, dev-memo/run/queue.linted, dev-memo/run/queue.reviewed, dev-memo/run/queue.governed, dev-memo/run/reviews/queue-review-098.md
Forbidden files: apps/lawbar-desktop/** (NO renderer/electron/test/harness/seed code — this lane writes NO product or test code; the Electron harness + anchor seed are DESIGNED here, implemented in a future WI), services/** (NO persistence/anchor-write/schema code), services/case-box-persistence/src/sqlite/schema.ts (NO schema/`CURRENT_SCHEMA_VERSION` change), docs/contracts/** (NO contract change), docs/adr/*.md EXCEPT the one new ADR-evidence-a3-link-d1-roundtrip-closure.md (NO edits to existing ADRs), dev-memo/design/** (this is an ADR-class decision; no design-artifact edit), package.json + package-lock.json + dist-tarballs/** + native/** (NO manifest/dependency/native/lockfile change), .claude/**, .github/**, AGENTS.md, CLAUDE.md, GEMINI.md, scripts/workflow/**, migrations/**, committed dev-memo/run/evidence/** marker/ledger/key/evidence-run files, committed keys, dev-memo/run/reports/** (untracked local run-state — never staged), dev-memo/run/override-reason.md (untracked residue — never staged), Evidence-Genie-M0-Developer-Handover.md + xiaolai-dev-workflow-study.md (root intake — READ-ONLY, never moved/deleted); NO anchor IPC implementation, NO test seed hook implementation, NO Electron harness edit, NO renderer/persistence/schema/audit-contract/dependency change, NO marking D1 closed, NO push/merge/branch-delete
Gates: broker /cc-suite:review-plan REQUIRED before the design commit (the ADR governs eventual high-risk IPC/test-harness/security work) — it MUST confirm: the three options are fairly compared; the recommended path's security analysis is sound (renderer is not the security boundary; actor/tenant trusted-main-derived; tenant/matter/document/page/anchor scope enforced; no production seed backdoor); the A0.7-first dependency for any real anchor-creation path is correctly surfaced (evidence-genie #3/#4/#6); D1 is NOT marked closed; the next-WI file scope + acceptance criteria are concrete + testable. If review-plan says the design must be SPLIT into a product-anchor-IPC design vs a test-harness design, STOP and report (stop-point 1). If review-plan returns NEEDS-FIX, STOP and report. After authoring: broker /cc-suite:audit + /cc-suite:verify on the diff (no Critical/High/Medium open before commit); scripts/workflow/check-contract-integrity.sh exits 0 (the new ADR is not a contract doc, but run it to prove no contract doc was disturbed). No A0.7 custody required (DESIGN-only; no geometry/marker/renderer code; review-plan MAY say otherwise).
Acceptance criteria: docs/adr/ADR-evidence-a3-link-d1-roundtrip-closure.md EXISTS and (a) compares Option 1 / Option 2 / Option 3 with explicit pros/cons; (b) contains an explicit single decision line naming the chosen path; (c) contains a Security analysis section covering renderer-not-the-boundary, trusted-main actor/tenant, tenant/matter/document/page/anchor scope, no-production-backdoor; (d) contains a Test plan for the impl WI; (e) contains exact D1-closure acceptance criteria (the round-trip proof list); (f) contains a next-WI file-scope proposal; (g) contains a `## Review packet (compact)` section; dev-memo/deferred-audit-findings.md LINK-IPC-T1-D1 shows a "Planned closure path" clarification pointing to the ADR and is NOT marked closed (no `Status | closed` row added to the D1 block); scripts/workflow/check-contract-integrity.sh exits 0; broker review-plan READY + audit no Critical/High/Medium + verify ALL CLOSED; the committed diff is confined to the Allowed files; no apps/services/schema/contract/dependency/manifest file changed.
Risk flags: DESIGN-ONLY doc lane, but HIGH-RISK-ADJACENT — the ADR governs eventual security-sensitive work (a new IPC surface OR a test seed that can write a real DB), so review-plan is REQUIRED (cc-suite §"High-risk WIs"). Load-bearing care points: (a) the security analysis MUST hold the line that a test-seed path is test-only, renderer-unreachable, and not a production backdoor — the recommended mechanism is a test-DRIVER-injected direct DB seed via Playwright `electronApp.evaluate()` adding ZERO product/IPC/preload surface; (b) any PRODUCT anchor-creation path (Option 1) MUST be surfaced as geometry-capture work that is BLOCKED by the A0.7-first invariant (evidence-genie #3/#4) and is a separate, much larger future A-phase — NOT what the D1 test gap needs; (c) D1 stays OPEN (not closed) — only the future impl WI's proven round-trip closes it; (d) NO product/runtime/test code is written; (e) NO Evidence invariant weakened (offline/local-first; main-injected identity; A0.7-dev-time; anchors ratio-based). If review-plan directs a design split (product-IPC vs test-harness), STOP. No push, no merge, no branch delete; D1 stays deferred.
Depends on: none
Commit boundary: governance (mark-reviewed + govern) run STANDALONE before the commit (never bundled with git commit). ONE design commit — the new ADR + the deferred-audit-findings.md D1 planned-closure clarification + governance artifacts (queue.md/.linted/.reviewed/.governed + reviews/queue-review-098.md), staged by exact path; NO apps/services/schema/contract/dependency/manifest/native/lockfile file; NO existing-ADR edit; NO marker/evidence/key/report file; no push; no merge.
