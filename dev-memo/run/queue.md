## WI-EVW5-FIX1: A0.7 hard-gate self-test env isolation (workflow fix; NOT A0.7-gated)
Type: WORKFLOW
A0.7-classification: NOT A0.7-gated. This is a narrow workflow self-test isolation fix; it does not implement Evidence behavior, does not mint/validate a marker, and does not change production gate behavior. Therefore it carries NO `Requires-A07:` line. The production A0.7 gate (check-a07-gate.sh) MUST keep reading ambient A07_REQUIRED (that is intended); only the self-test must become hermetic.
Scope: Fix the EVW5 A0.7 hard-gate SELF-TEST environment isolation defect: scripts/workflow/check-a07-gate.test.sh inherits ambient A07_REQUIRED (and would inherit an ambient LAWBAR_A07_MARKER_HMAC_KEY), so when the OUTER scripts/workflow/check-gates.sh is run with A07_REQUIRED=1 (the human-run mode-9b gated verification), the self-test's not-required cases (esp. case 1 "not-required clean tree should pass") see the leaked A07_REQUIRED=1 and fail closed, breaking the gate battery. Reproduced: `A07_REQUIRED=1 bash scripts/workflow/check-a07-gate.test.sh` -> 1 FAIL / 11 PASS (case 1); clean env -> ALL 12 PASS. Fix: make every gate invocation inside check-a07-gate.test.sh hermetic w.r.t. A07_REQUIRED and LAWBAR_A07_MARKER_HMAC_KEY — each test case explicitly clears both ambient vars and sets ONLY the required/key state it intends (not-required cases clear A07_REQUIRED; required cases set it; queue-driven cases derive required-ness from the temp queue fixture only, with ambient A07_REQUIRED cleared). Production gate behavior is UNCHANGED. NO change to check-a07-gate.sh production logic, the marker writer/validator/schema, or any Evidence behavior.
Required behavior: after the fix — `bash scripts/workflow/check-a07-gate.test.sh` passes ALL cases; `A07_REQUIRED=1 bash scripts/workflow/check-a07-gate.test.sh` ALSO passes ALL cases (the regression that currently fails case 1); each test case explicitly controls required/not-required mode and does NOT depend on ambient A07_REQUIRED or an ambient HMAC key; the not-required clean-tree case clears A07_REQUIRED and uses a temp queue with no Requires-A07 line; required-mode cases set A07_REQUIRED=1 or a temp-queue Requires-A07 line explicitly; the production gate still FAILS CLOSED when A07_REQUIRED=1 and no marker/key exists (unchanged); a genuine marker + matching key still PASSES (unchanged); fabricated/committed/isMarker-tampered markers still fail (unchanged). With a human-provided key + genuine marker (later), `A07_REQUIRED=1 bash scripts/workflow/check-gates.sh` ends GATES OK.
Source of truth: scripts/workflow/check-a07-gate.test.sh (the self-test with the env-leak defect — the file under repair); scripts/workflow/check-a07-gate.sh (production gate — behavior UNCHANGED, read-only reference); scripts/workflow/check-gates.sh (the battery that runs the self-test under ambient A07_REQUIRED — change only if review-plan says the wiring must sanitize env around self-tests); docs/adr/ADR-evidence-a07-renderer-conformance-gate.md (A07-GATE-00) + docs/adr/ADR-evidence-a07-marker-provenance.md (A07-MARK-00) + docs/adr/ADR-evidence-a07-key-custody-operating-model.md (A07-KEY-00, mode-9b gated check-gates is the scenario that exposed this) — gate semantics that must be preserved; .claude/rules/evidence-genie.md (inv.3/4 — gate must not be weakened); .claude/rules/autonomy.md (workflow self-improvement is a WORKFLOW WI).
Allowed files: scripts/workflow/check-a07-gate.test.sh (the fix), scripts/workflow/check-gates.sh (ONLY a narrow env-sanitization around the self-tests IF review-plan explicitly says the wiring must do it — prefer test-only), dev-memo/** (queue/governance/review/study/report artifacts)
Forbidden files: scripts/workflow/check-a07-gate.sh (production gate logic — NO behavior change), scripts/workflow/a07_marker.py + a07-marker-write.sh + check-marker-guard.sh (marker writer/validator/guard — NO change), native/evidence-core/** + native/evidence-core-swift/** (NO change), apps/**, services/**, docs/contracts/**, .claude/**, .github/**, AGENTS.md, CLAUDE.md, GEMINI.md, committed dev-memo/run/evidence/** marker/ledger/key/evidence-run files, committed keys, Evidence-Genie-M0-Developer-Handover.md (root intake), xiaolai-dev-workflow-study.md (root intake), dev-memo/run/override-reason.md (untracked residue — never staged); NO production gate-behavior change, NO marker schema/writer/validator change, NO Requires-A07 drop or gate weakening, NO key material/mint/exposure, NO A3 implementation, NO Evidence UI/product, NO OCR/AI/cloud/auth/network, NO new dependency
Gates: broker /cc-suite:review-plan REQUIRED before impl (confirm the fix is test-only env-hermeticism and does NOT weaken production gate behavior; confirm whether check-gates.sh wiring also needs env sanitization or the test-only fix suffices; if review-plan asks to change production gate logic / marker writer/validator/schema / drop Requires-A07 / weaken the gate, STOP and ask); broker /cc-suite:audit + /cc-suite:verify after impl; agent-side: `bash scripts/workflow/check-a07-gate.test.sh` ALL PASS AND `A07_REQUIRED=1 bash scripts/workflow/check-a07-gate.test.sh` ALL PASS (the regression); `bash scripts/workflow/check-marker-guard.test.sh` 17 PASS; `bash scripts/workflow/a07-marker.test.sh` PASS; `A07_REQUIRED=1 bash scripts/workflow/check-a07-gate.sh` fails closed (no key/marker, unchanged); not-required `bash scripts/workflow/check-a07-gate.sh` passes; `scripts/workflow/check-contract-integrity.sh` exits 0; `bash scripts/workflow/check-gates.sh` returns GATES OK in not-required mode; `npm --prefix apps/lawbar-desktop test` 610/610; no native/apps/.claude/.github/services change; no production gate-logic change; no committed marker/ledger/key/evidence-run
Acceptance criteria: scripts/workflow/check-a07-gate.test.sh PASSES all cases under BOTH a clean env AND ambient A07_REQUIRED=1 (the current case-1 regression is fixed); each gate invocation in the self-test explicitly clears ambient A07_REQUIRED + LAWBAR_A07_MARKER_HMAC_KEY and sets only the intended state; the production gate check-a07-gate.sh is BYTE-UNCHANGED (or, only if review-plan approved, check-gates.sh gains a narrow env-sanitization wrapper around the self-tests with no production-path change); A07_REQUIRED=1 check-a07-gate.sh still FAILS CLOSED without a marker/key; check-marker-guard.test.sh (17) + a07-marker.test.sh still PASS; check-contract-integrity exits 0; check-gates.sh returns GATES OK in not-required mode; npm --prefix apps/lawbar-desktop test 610/610; broker review-plan READY + audit no Critical/High/Medium + verify ALL CLOSED; the committed diff is confined to scripts/workflow/check-a07-gate.test.sh (+ check-gates.sh only if review-plan approved) + dev-memo/** governance artifacts
Risk flags: WORKFLOW self-test fix (the EVW5 hard-gate battery). Must NOT weaken production gate behavior — the production gate keeps reading ambient A07_REQUIRED; only the self-test becomes hermetic. NO marker schema/writer/validator change, NO Requires-A07 drop, NO gate weakening, NO key handling, NO A3 implementation, NO new dependency. broker review-plan + audit + verify REQUIRED (the gate battery is the apex A0.7 enforcement). No push, no merge.
Depends on: none
Commit boundary: governance commit separate (dev-memo/run/queue.md + queue.linted/reviewed/governed + dev-memo/run/reviews/queue-review-070.md); one implementation commit -- scripts/workflow/check-a07-gate.test.sh (+ scripts/workflow/check-gates.sh only if review-plan approved), staged by exact path; no push; no merge
