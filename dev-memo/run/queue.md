## WI-A3-T1-IMPL: Anchor/Link schema V11 (case-box-persistence; A0.7-gated)
Type: IMPL
Requires-A07: yes
A07 custody mode: 9b. Human runs marker mint + A0.7-gated check-gates directly; agent does not receive the HMAC key. The implementation commit is BLOCKED until human-run gated verification reports PASS (A07_REQUIRED=1 check-a07-gate + A07_REQUIRED=1 check-gates both PASS with LAWBAR_A07_MARKER_HMAC_KEY in the human's shell, and no marker/ledger/key/evidence-run staged or committed). The agent never exports/echoes/commits/mints the key, never weakens or drops Requires-A07, never bypasses the gate.
Scope: Add SCHEMA-ONLY version V11 to services/case-box-persistence for the Anchor + Link tables, per A3-SCHEMA-00 §3 (reconciled, no-FK, §6). The FINAL A3 foundation implementation lane (the original A3-T1, unblocked now that DocumentPage V9 + DocumentPageGeometry V10 exist). NO resolver/status transition behavior, NO replacement/quarantine resolver behavior, NO export behavior, NO production evidence ingestion, NO encryption/key management, NO new dependency, NO package.json/lock change, NO SQLite FK constraints, NO UI/app behavior, NO ON DELETE cascade (the anchor-delete cascade policy is UNRESOLVED — A3-SCHEMA-00 decision 5 / A3-CONTRACT-00 decision 9; do NOT invent it). Changes confined to services/case-box-persistence/src/sqlite/schema.ts (add DDL_STATEMENTS_V11 + register [11, DDL_STATEMENTS_V11] in DDL_BY_VERSION + bump CURRENT_SCHEMA_VERSION 10->11, via the existing idempotent applySchema/one-BEGIN/COMMIT/forward-only convention; no down-migration, no destructive reset) and services/case-box-persistence/tests/hardening-schema.test.mjs (extend the EXISTING in-script schema test with V11 cases — no package.json test-list change).
Schema requirements (per A3-SCHEMA-00 §3, reconciled to the case-box no-FK convention): TABLE case_box_anchors with columns: id TEXT PRIMARY KEY; tenant_id TEXT NOT NULL; matter_id TEXT NOT NULL; document_id TEXT NOT NULL (app-layer invariant -> the V9 case_box_document_pages page identity; NO FK); physical_page_index INTEGER NOT NULL CHECK (physical_page_index >= 0) (0-based, consistent with V9/V10); geometry_captured_at TEXT NOT NULL COLLATE BINARY (the geometry VERSION the anchor was normalized against; provenance required = NOT NULL; immutable per stored anchor by app-layer invariant; app-layer invariant -> a V10 case_box_document_page_geometries.captured_at for the same page); rect_x / rect_y / rect_width / rect_height TEXT NOT NULL COLLATE BINARY (canonical page_ratio components — fixed 12-dp decimal strings, identical storage form as the V10 geometry bounds + A3-T2 page_ratio; NO REAL/float columns; the 12-dp canonical format + [0,1] domain are APP-LAYER invariants, not numeric SQLite CHECKs, consistent with the V10 bounds decision); coordinate_space TEXT NOT NULL CHECK (coordinate_space = 'page_ratio'); origin_ref TEXT NOT NULL CHECK (origin_ref = 'DocumentPageGeometry'); page_rotation INTEGER NOT NULL CHECK (page_rotation IN (0,90,180,270)); created_at TEXT NOT NULL COLLATE BINARY; payload_json TEXT NOT NULL (canonical Anchor record; optional label? lives here). NO UNIQUE beyond the PK (multiple anchors per page are allowed). NO viewport/screen columns. TABLE case_box_links with columns: id TEXT PRIMARY KEY; tenant_id TEXT NOT NULL; matter_id TEXT NOT NULL; source_type TEXT NOT NULL CHECK (source_type IN ('evidence','note','question','calcTerm','claimElement')); source_id TEXT NOT NULL; anchor_id TEXT NOT NULL (app-layer invariant -> case_box_anchors.id; NO FK); status TEXT NOT NULL CHECK (status IN ('valid','needs_review','broken')) (NOT NULL, NO DEFAULT — no implicit valid); created_at TEXT NOT NULL COLLATE BINARY; payload_json TEXT NOT NULL. Add one by-document index on case_box_anchors and one by-source (and/or by-anchor) index on case_box_links, following the existing index convention. NO ON DELETE cascade; NO SQLite FK on either table.
Required behavior: applySchema applies V11 idempotently; upgrades from V10 without dropping existing DocumentPage/DocumentPageGeometry data; schema_version becomes 11; case_box_anchors + case_box_links exist with the columns above; rect components stored as 12-dp TEXT round-trip byte-identically; geometry_captured_at NOT NULL (an anchor without geometry provenance is rejected); Link.status has NO implicit valid (a row without an explicit status fails; the CHECK rejects values outside valid/needs_review/broken); source_type CHECK rejects unknown values; coordinate_space/origin_ref/page_rotation CHECKs enforce the consts/enum; physical_page_index = 0 accepted, negative rejected; no viewport/screen columns; no SQLite FK on either table (PRAGMA foreign_key_list empty); no ON DELETE cascade.
Source of truth: docs/adr/ADR-evidence-a3-schema-persistence-contract.md (A3-SCHEMA-00 §3 Anchor/Link entities + columns, decision-Medium page_ratio 12-dp TEXT, decision 3 LinkStatus no-default, decision 5 cascade UNRESOLVED, §6 no-FK reconciliation); docs/adr/ADR-evidence-a3-anchor-link-contract.md (A3-CONTRACT-00 §4 PageRatioRect coordinateSpace/originRef/pageRotation; INV-A3-1..10; decision 9 cascade undecided); docs/adr/ADR-evidence-a3-page-geometry-foundation.md (A3-PAGE-00 — the V9/V10 foundations + the app-layer identity invariant); services/case-box-persistence/src/sqlite/schema.ts (the V9 case_box_document_pages + V10 case_box_document_page_geometries + the applySchema/DDL_BY_VERSION/CHECK/COLLATE-BINARY/12-dp-TEXT/no-FK convention) + tests/hardening-schema.test.mjs (the schema-test pattern to extend); docs/adr/ADR-evidence-a07-key-custody-operating-model.md (A07-KEY-00 — custody 9b). User decisions: physical_page_index 0-based; bounds/page_ratio as 12-dp TEXT.
Allowed files: services/case-box-persistence/src/sqlite/schema.ts (DDL_STATEMENTS_V11 + DDL_BY_VERSION registration + CURRENT_SCHEMA_VERSION bump + a version-11 comment), services/case-box-persistence/tests/hardening-schema.test.mjs (extend with V11 cases), dev-memo/** (queue/governance/review/study/report artifacts)
Forbidden files: services/case-box-persistence/package.json + package-lock.json (NO dependency/test-list change), any other services/** file, docs/contracts/** (NO change), apps/**, native/**, scripts/workflow/** (NO gate/marker change), .claude/**, .github/**, AGENTS.md, CLAUDE.md, GEMINI.md, committed dev-memo/run/evidence/** marker/ledger/key/evidence-run files, committed keys, Evidence-Genie-M0-Developer-Handover.md (root intake), xiaolai-dev-workflow-study.md (root intake), dev-memo/run/override-reason.md (untracked residue — never staged); NO resolver/status/replacement/export behavior, NO ON DELETE cascade, NO SQLite FK constraints, NO new dependency, NO package.json change, NO viewport/screen columns, NO production evidence ingestion, NO encryption/key management, NO UI/product, NO marker/key/gate change, NO migration beyond schema V11
Gates: broker /cc-suite:review-plan REQUIRED before impl (confirm the V11 Anchor/Link schema matches A3-SCHEMA-00 §3 + A3-CONTRACT-00 §4: page_ratio rect as 12-dp TEXT, LinkStatus CHECK no-default, coordinate_space/origin_ref consts, page_rotation/source_type CHECKs, no-FK app-layer invariants, 0-based index, NO ON DELETE cascade; if review-plan introduces scope expansion, an unresolved cascade/delete policy, SQLite FK constraints, marker/key/gate changes, or ambiguous ratio constraints, STOP and report; if review-plan asks for resolver/status/replacement/export/UI/dependency, STOP and ask); broker /cc-suite:audit + /cc-suite:verify after impl AND after the human-run gated PASS; A0.7-gated under custody 9b — the implementation commit is BLOCKED until the human reports A07_REQUIRED=1 check-a07-gate PASS + A07_REQUIRED=1 check-gates PASS (human shell holds the key) with no marker/ledger/key/evidence-run staged or committed; agent-side (non-secret): npm --prefix services/case-box-persistence test passes (incl. the new V11 cases + all existing tests green incl. V9/V10); scripts/workflow/check-contract-integrity.sh exits 0; npm --prefix apps/lawbar-desktop test 610/610; npm --prefix native/evidence-core test 38/38; the A0.7 gate is intact (A07_REQUIRED=1 no marker/key fails closed; not-required passes); the agent's own check-gates correctly FAILS CLOSED at the A0.7 gate (queue Requires-A07, agent has no key — expected under 9b); no package.json/dependency change; no FK/viewport/cascade; no apps/native/scripts/.claude/.github change; no committed marker/ledger/key/evidence-run
Acceptance criteria: services/case-box-persistence/src/sqlite/schema.ts adds DDL_STATEMENTS_V11 (case_box_anchors + case_box_links + indices) registered as [11, DDL_STATEMENTS_V11] with CURRENT_SCHEMA_VERSION = 11; V1-V10 DDL unmodified; services/case-box-persistence/tests/hardening-schema.test.mjs PASSES new V11 cases proving: V11 applies from empty; V10 -> V11 upgrade preserves existing DocumentPage + DocumentPageGeometry data; schema_version becomes 11; case_box_anchors + case_box_links exist with the expected columns; geometry_captured_at NOT NULL (anchor without provenance rejected); rect_x/y/width/height stored as 12-dp TEXT byte-identical round-trip; coordinate_space/origin_ref/page_rotation CHECKs enforce consts/enum; Link.status CHECK rejects unknown values AND a row without explicit status fails (no DEFAULT 'valid'); source_type CHECK rejects unknown values; physical_page_index 0 accepted / negative rejected; NO viewport/screen columns; NO SQLite FK on either table (PRAGMA foreign_key_list empty for both); NO ON DELETE cascade; the V9/V10 tests + all existing case-box tests remain green; npm --prefix services/case-box-persistence test exits 0; scripts/workflow/check-contract-integrity.sh exits 0; npm --prefix apps/lawbar-desktop test 610/610; npm --prefix native/evidence-core test 38/38; no package.json/package-lock change; the human-run A0.7-gated verification (custody 9b) reports PASS before the implementation commit; broker review-plan READY + audit no Critical/High/Medium + verify ALL CLOSED; the committed diff is confined to services/case-box-persistence/src/sqlite/schema.ts + services/case-box-persistence/tests/hardening-schema.test.mjs + dev-memo/** governance artifacts
Risk flags: PERSISTENCE / MIGRATION IMPL — A0.7-DEPENDENT (Requires-A07: yes), custody mode 9b — the implementation commit is BLOCKED until the human runs the A0.7-gated check-gates with the HMAC key and reports PASS; the agent never receives/echoes/commits/mints the key, never weakens/drops Requires-A07, never bypasses the gate. Schema-only V11 Anchor+Link: NO resolver/status/replacement/export, NO ON DELETE cascade (unresolved policy — not invented), NO SQLite FK (app-layer invariants), NO new dependency, NO package.json change, NO viewport columns, NO production evidence ingestion, NO encryption/key management, NO UI/product. Migration forward-only (no down-migration; rollback via git revert + a new forward version). page_ratio rects stored as byte-stable 12-dp TEXT (no float); LinkStatus has no implicit valid. broker review-plan + audit + verify REQUIRED. Must not weaken any Evidence invariant nor the A3-DB-00 encryption hard stop. No push, no merge.
Depends on: none
Commit boundary: governance commit separate (dev-memo/run/queue.md + queue.linted/reviewed/governed + dev-memo/run/reviews/queue-review-076.md + dev-memo/cc-suite-reliability-log.md + dev-memo/batch-closeout-log.md + dev-memo/study/2026-06-24-batch-audit-154.md from the closeout); one implementation commit -- services/case-box-persistence/src/sqlite/schema.ts + services/case-box-persistence/tests/hardening-schema.test.mjs, staged by exact path, ONLY after human-run gated PASS + broker audit/verify; no push; no merge
