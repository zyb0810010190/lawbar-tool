## WI-A3-LINK-CREATE-DESIGN-00: governed design for the audited link-create operation (design-only; NOT A0.7-gated)
Type: PLAN
A0.7-classification: WI-A3-LINK-CREATE-DESIGN-00 is a DESIGN-ONLY ADR (it authorizes no code, executes no A0.7-dependent code, reads no A0.7 marker), so it is NOT A0.7-gated and carries NO `Requires-A07:` line. It governs eventual HIGH-RISK work (a persistence mutation + a public audit-contract kind), so broker /cc-suite:review-plan + /cc-suite:audit + /cc-suite:verify are REQUIRED on the design. If review-plan says the design itself must be A0.7-gated/custody-9b, STOP and report.
Scope: Produce a DESIGN-ONLY ADR (`docs/adr/ADR-evidence-a3-link-create-operation.md`) for the still-missing audited link-CREATE operation — the prerequisite before any UI/IPC unlink/relink wiring (links today exist only via tests/raw SQL). No product code. The ADR records: (a) whether link creation needs a NEW audit kind or an existing kind covers it; (b) the repository/service API boundary for create; (c) the required validation (tenant/matter/source/anchor identity boundaries, duplicate handling, structural validity, confidentiality/synthetic-fixture constraints, transactional audit-chain append); (d) the initial `status` + resolver/export expectations after create; (e) the explicit separation of contract/audit-kind work vs persistence impl vs IPC/UI wiring vs export rendering; (f) the WI sequencing. DESIGN-ONLY: no services/** change, no docs/contracts/** change, no SQLite schema or CURRENT_SCHEMA_VERSION change, no UI/IPC, no implementation. If the design proves a genuine schema gap, STOP and report (do NOT implement a schema change).
Required decisions to record: (1) AUDIT KIND — determine whether link creation needs a new `LINK_CREATED` kind or an existing kind covers it. The existing `link` kinds (LINK_UNLINKED/LINK_RELINKED) are both `action: update`; creation is `action: create`, so the ADR concludes a NEW `LINK_CREATED { action: create, entity_type: link, reasonRequired: false }` kind is required, added by a SEPARATE predecessor contract WI (mirroring WI-A3-UNLINK-AUDIT-KINDS) — the impl WI must not define the kind inline. If review-plan finds an existing kind suffices, record/STOP. (2) API BOUNDARY — `createLink(input)` on the CONCRETE SqliteCaseBoxPersistence (SQLite-only; NOT the shared interface; InMemory untouched), input { tenant_id, matter_id, source_type, source_id, anchor_id, actor_user_id }; id generated (ULID); single `stamp = deps.nowIso()` for created_at + the audit timestamp; backed by applyCreateLinkSqlite + prepareCreateLink in linkRepoQueries.ts; reuses the existing audit-chain helpers. (3) INITIAL STATUS — insert a provisional non-valid `needs_review` (A3-RESOLVE: valid is never a default); the caller runs resolveLinkStatuses to compute the authoritative status; create does NOT run the resolver. (4) VALIDATION — matter exists + tenant matches (unknown_matter/tenant_mismatch); source_type in the 5-enum + non-empty source_id + non-empty actor_user_id (invalid_argument); anchor existence (RECOMMEND require-exists at create -> invalid_argument; ALTERNATIVE allow-dangling -> resolver broken; review-plan confirms); duplicate id -> duplicate_id; NO logical-duplicate (source+anchor) uniqueness (the V11 schema has none; dedup would be a schema change). (5) TRANSACTIONAL — one BEGIN IMMEDIATE: build the LINK_CREATED event, INSERT the row, append the event; before_state_hash null (create); after_state_hash via entityStateHash over the persisted state excluding the resolver-derived status; exactly one event per create. (6) RESOLVER/EXPORT — unchanged behavior; integration-tested: a valid fixture resolves valid + clean citation, a missing-anchor fixture resolves broken. (7) SEPARATION — contract/audit-kind WI (predecessor) vs persistence impl WI vs IPC/UI wiring vs export rendering are independent governed WIs. (8) SCHEMA — NO change; CURRENT_SCHEMA_VERSION stays 12; the only thing that would force a schema change (a logical-duplicate UNIQUE index) is DECLINED; if the impl lane finds a real gap, STOP. (9) NEXT LANE — the audit-kind contract WI (WI-A3-LINK-CREATE-AUDIT-KIND), NOT the persistence impl, because the impl emits LINK_CREATED which must exist in the contract first. (10) TESTS — enumerate the impl-lane test set; tests extend an already-wired test file (the package test runner is a curated list, no glob; package.json forbidden — the WI-A3-UNLINK-T1 lesson).
Source of truth: docs/adr/audit-event-kind-preservation.md (the kind-preservation ADR — append-only kinds, the {action, entity_type, reasonRequired} tuple); docs/adr/ADR-evidence-a3-unlink-break-link-workflow.md + ADR-evidence-a3-durable-unlink-schema.md (the unlink chain this create-op precedes/parallels); docs/adr/ADR-evidence-a3-resolver-status-transitions.md (the status ladder — valid is never a default); docs/contracts/case-box-contract/src/audit-log.ts (READ-ONLY — CASE_BOX_AUDIT_EVENT_KINDS; the existing link kinds); services/case-box-persistence/src/sqlite/{linkRepoQueries,schema}.ts (READ-ONLY — the unlink/relink pattern + the V11/V12 case_box_links columns); .claude/rules/{autonomy,cc-suite,evidence-genie,client-local-first}.md (user-authorized design lane; the design governs HIGH-RISK persistence + audit-contract work).
Allowed files: docs/adr/ADR-evidence-a3-link-create-operation.md (the DESIGN ADR — the only deliverable), dev-memo/run/queue.md, dev-memo/run/queue.linted, dev-memo/run/queue.reviewed, dev-memo/run/queue.governed, dev-memo/run/reviews/queue-review-090.md, dev-memo/study/** (a batch-audit study packet if a window closes), dev-memo/cc-suite-reliability-log.md (only if a TIMEOUT retry occurs), dev-memo/deferred-audit-findings.md (only if a finding is deferred)
Forbidden files: services/** (NO product code — the createLink operation is the future WI-A3-LINK-CREATE-T1), services/case-box-persistence/src/sqlite/schema.ts (NO schema/version change), docs/contracts/** (NO contract change — the LINK_CREATED kind is the future WI-A3-LINK-CREATE-AUDIT-KIND, NOT this lane), docs/contracts/case-box-contract/src/generated/** (NO regenerate), apps/**, native/**, .claude/**, .github/**, AGENTS.md, CLAUDE.md, GEMINI.md, scripts/workflow/**, scripts/gen-types.mjs, migrations/**, package-lock.json / any dependency manifest, services/case-box-persistence/package.json, committed dev-memo/run/evidence/** marker/ledger/key/evidence-run files, committed keys, dev-memo/run/reports/** (untracked local run-state — never staged), dev-memo/run/override-reason.md (untracked residue — never staged), Evidence-Genie-M0-Developer-Handover.md (root intake — READ-ONLY, never moved/deleted), xiaolai-dev-workflow-study.md (root intake — READ-ONLY, never moved/deleted); NO product code, NO schema/version change, NO contract change, NO audit-kind addition (deferred to the predecessor WI), NO implementation, NO UI/IPC, NO export rendering, NO dependency
Gates: broker /cc-suite:review-plan REQUIRED before/at authoring (confirm DESIGN-ONLY + NOT-A0.7-gated classification; confirm D1 — link creation needs a new LINK_CREATED kind, not an existing one — or STOP if review-plan finds an existing kind suffices; confirm D7 — no schema change, CURRENT_SCHEMA_VERSION stays 12 — or STOP if a schema gap is real; confirm the API boundary, the provisional-needs_review initial status, the validation set, the transactional audit append, and the WI sequencing audit-kind-first; confirm CONTRACT/PERSISTENCE/IPC/EXPORT separation); if review-plan returns NEEDS-FIX, STOP and report; if review-plan says the design must be A0.7-gated, STOP and report; broker /cc-suite:audit + /cc-suite:verify on the ADR diff after authoring (no Critical/High/Medium open before commit); scripts/workflow/check-contract-integrity.sh exits 0 (sanity — the always-loaded contract docs are untouched); no services/docs-contracts/schema/apps/native/scripts/dependency change; no committed marker/ledger/key/evidence-run; root intake untouched
Acceptance criteria: docs/adr/ADR-evidence-a3-link-create-operation.md exists and records decisions D1-D10 — it concludes a new LINK_CREATED { action create, entity_type link, reasonRequired false } kind is REQUIRED (added by a separate predecessor contract WI), specifies createLink on the concrete SqliteCaseBoxPersistence with the validation + transactional-audit + provisional-needs_review + resolver/export-after-create design, declines a schema change (CURRENT_SCHEMA_VERSION stays 12; STOP if a gap is real), and names the NEXT LANE as the audit-kind contract WI (WI-A3-LINK-CREATE-AUDIT-KIND) NOT the persistence impl; DESIGN-ONLY (no product code, no contract change, no schema change); broker review-plan READY (design + classification + D1 + D7 confirmed) + audit reports no Critical/High/Medium + verify ALL CLOSED; scripts/workflow/check-contract-integrity.sh exits 0; the committed diff is confined to the ADR + the governance artifacts (dev-memo/run/**)
Risk flags: PLAN — a DESIGN-ONLY ADR (no product code). NOT A0.7-gated (no geometry-derived behavior; no marker/key). Doc-only, but it GOVERNS eventual HIGH-RISK work (a persistence mutation + a public audit-contract kind), so broker review-plan + audit + verify are REQUIRED on the design. The ADR MUST NOT add the LINK_CREATED kind (that is the predecessor contract WI), MUST NOT change schema/CURRENT_SCHEMA_VERSION, MUST NOT implement createLink, MUST NOT touch UI/IPC/export-rendering, and MUST NOT weaken any Evidence invariant or the audit-chain tamper-evidence. If the design proves a schema gap, STOP and report rather than implement. No push, no merge.
Depends on: none
Commit boundary: governance (mark-reviewed + govern) run STANDALONE before the commit (never bundled with git commit). ONE commit -- docs/adr/ADR-evidence-a3-link-create-operation.md + dev-memo/run/queue.md + dev-memo/run/queue.linted + dev-memo/run/queue.reviewed + dev-memo/run/queue.governed + dev-memo/run/reviews/queue-review-090.md, staged by exact path; no product code; no push; no merge
