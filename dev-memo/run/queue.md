## WI-ENA9: A0.7 marker provenance DESIGN (docs/ADR lane; design only, non-authorizing)
Type: PLAN
Scope: Produce a design ADR for FUTURE A0.7 marker provenance at docs/adr/ADR-evidence-a07-marker-provenance.md (A07-MARK-00), extending A07-GATE-00 §5/§8. DESIGN/GOVERNANCE ONLY, authorizes no implementation: define (1) marker objective (durable evidence that a real harness run produced a classified pass against the reviewed fixture/oracle; a marker is NOT a source file existing; a harness pass is not a marker without provenance-valid material); (2) marker schema proposal (schemaVersion, gateId, harnessImplCommit, fixturePath+sha256, oraclePath+sha256, resultStatus/classification, observedPageCount, tolerance, producedAt, repoCommit+treeHash, command, platform, provenancePayloadHash, provenance HMAC/signature field — proposed not implemented); (3) evidence binding rules (bind to exact fixture bytes, oracle bytes, harness commit/tree, classified result, command invocation, local/offline context; invalid if any bound artifact changes); (4) validation rules (schema-valid != provenance-valid; provenance-valid requires recomputing bindings + verifying tamper-evidence; a manually touched/copied marker MUST fail; missing/invalid HMAC/signature MUST fail; an isMarker=false harness result cannot satisfy marker validity); (5) tamper/fabrication guard requirements (separately authorized WI; MUST land before marker-write; rejects fabricated markers + writes without valid provenance; protects dev-memo/run/evidence/** before marker write); (6) marker path policy (propose dev-memo/run/evidence/** but do NOT create it; path stays ABSENT in ENA9); (7) failure semantics (only status=pass + classification=ok marker-eligible; fail/inconclusive/not_implemented/Class-1/Class-2/fixture-or-oracle-invalid NOT eligible; Class-2 STOP never marker-eligible); (8) sequencing (ENA9 authorizes no implementation; future WIs: tamper/fabrication guard impl, marker write impl, EVW5 hooks; marker write MUST NOT precede the guard). This lane is docs/governance only: NO native/** code, NO marker, NO HMAC/provenance implementation, NO tamper/fabrication guard implementation, NO EVW5 hooks, NO harness result-semantics change, NO JS-shim change, NO Evidence UI/product behavior, NO dev-memo/run/evidence/**. Do NOT delete/move the root intake files.
Source of truth: docs/adr/ADR-evidence-a07-renderer-conformance-gate.md (A07-GATE-00 §5 marker policy, §8 sequencing guard-before-write); docs/adr/ADR-evidence-native-core-a07-feasibility.md (ENA-00); .claude/rules/evidence-genie.md (invariants 3 A0.7-first, 8 snapshot manifest/seal anti-circularity, 10 not_implemented-fails); AGENTS.md §"Evidence-Genie M0 workflow composition" + hard stops (key custody / secret material is Stop-and-Ask); native/evidence-core-swift/Tests/EvidenceCoreSmokeTests/Fixtures/a07-renderer-conformance/{manifest.json,oracle.json} (WI-ENA7 fixture/oracle the marker binds to); native/evidence-core-swift/Sources/EvidenceCoreSmoke/A07ConformanceHarness.swift (WI-ENA8 harness emitting isMarker=false); .claude/rules/autonomy.md (this docs/ADR lane is user-authorized).
Allowed files: docs/adr/** (the new A07-MARK-00 ADR), dev-memo/** (queue/governance/audit/study artifacts; any supporting dev-memo), docs/product/** (ONLY for a short cross-reference if necessary; prefer not to)
Forbidden files: native/** (NO code — harness/marker/provenance/guard), .github/** (NO CI/hook change), .claude/hooks/**, .claude/settings.json, .claude/rules/**, apps/**, services/**, docs/contracts/**, AGENTS.md, CLAUDE.md, GEMINI.md, dev-memo/run/evidence/**, Evidence-Genie-M0-Developer-Handover.md (root), xiaolai-dev-workflow-study.md, package.json; NO marker / HMAC / provenance / tamper-guard implementation code; NO marker files
Gates: broker /cc-suite:review-plan REQUIRED before writing the ADR (it defines FUTURE marker authority; no self-review); if review-plan returns READY but the WI scopes into IMPLEMENTATION (marker-write / HMAC / provenance / tamper-guard / hooks / UI / product behavior), STOP and ask; broker /cc-suite:audit + /cc-suite:verify after writing the ADR (it creates future marker authority); the ADR is design-only/non-authorizing and explicitly authorizes none of the §8 future WIs; no native/** change; no .github/** change; no dev-memo/run/evidence/**; no marker files; no marker/HMAC/provenance/tamper-guard implementation code; the native/evidence-core JS shim is byte-unchanged; the ADR states schema-valid != provenance-valid, manually-touched markers fail, only pass+ok is marker-eligible, Class-2 never marker-eligible, and guard-before-write; scripts/workflow/check-contract-integrity.sh exits 0; npm --prefix apps/lawbar-desktop test passes (610/610); scripts/workflow/check-gates.sh returns GATES OK
Acceptance criteria: docs/adr/ADR-evidence-a07-marker-provenance.md exists, Status "Proposed — design only, non-authorizing", containing the 8 required design sections (marker objective; schema proposal; evidence binding rules; validation rules incl. schema-valid!=provenance-valid + manually-touched-fails + missing-HMAC-fails + isMarker=false-not-sufficient; tamper/fabrication guard requirements incl. guard-before-write; marker path policy proposing dev-memo/run/evidence/** but keeping it absent; failure semantics only pass+ok eligible + Class-2 never eligible; sequencing listing future WIs with marker-write-not-before-guard); the ADR cites A07-GATE-00, ENA-00, and evidence-genie.md; NO native/** or .github/** change; NO dev-memo/run/evidence/**; NO marker/HMAC/provenance/tamper-guard implementation code anywhere; the native/evidence-core JS shim is byte-unchanged; check-contract-integrity PASS; npm desktop test pass 610 fail 0; check-gates GATES OK; broker review-plan READY (scopes design-only, not implementation) and broker audit returns no Critical/High/Medium with verify ALL CLOSED; the committed diff is confined to docs/adr/** plus dev-memo/** governance artifacts
Risk flags: HIGH-RISK -- the ADR defines FUTURE A0.7 marker authority (the durable court-facing A0.7 claim); a weak design could later allow a fabricated/forged marker (the EVW5 forgery class), so it gets broker review-plan + audit + verify even though it is docs-only. Scope strictly limited to DESIGN: no native code, no marker, no HMAC/provenance, no tamper guard, no dev-memo/run/evidence/**, JS shim untouched; the ADR authorizes none of the §8 future implementation WIs and binds marker-write-not-before-guard. Key custody (HMAC/signing key) is flagged as a Stop-and-Ask for the future marker-write WI, not resolved here. Docs-only and revertable as one commit.
Depends on: none (A07-GATE-00, the WI-ENA7 fixture/oracle, and the WI-ENA8 harness are already merged to main)
Commit boundary: one local commit for the ADR -- docs/adr/ADR-evidence-a07-marker-provenance.md (plus any supporting dev-memo), staged by exact path; governance commit separate; no push
