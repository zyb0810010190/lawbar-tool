## WI-ENA1: SwiftPM skeleton + macOS CI smoke (first authorized implementation hard-stop)
Type: SCAFFOLD
Scope: Add a minimal SwiftPM package under native/evidence-core-swift/ that BUILDS and whose tests PASS, exposing only a version/smoke value (no behavior). Add a GitHub Actions macOS CI job that runs ONLY the SwiftPM smoke build/test, preserving the existing ubuntu ui-design-artifact check. This is the narrow toolchain step the user explicitly authorized: SwiftPM skeleton + macOS CI smoke ONLY. NO PDFKit (default no PDFKit import unless review-plan explicitly approves a compile-only import), NO real A0.7 harness logic, NO A0.7 marker, NO marker provenance/HMAC, NO fixtures, NO EVW5 hard hooks, NO Evidence UI/product behavior. Do NOT change the existing native/evidence-core JS shim behavior. Do NOT connect the Swift package to the JS shim unless review-plan explicitly approves a no-behavior reference only. Do NOT delete/move the root intake files. The package name + README make clear it is a native smoke/skeleton (not A0.7-complete): README states skeleton-only / no-A0.7 / no-marker / no-PDF-geometry / no-product-behavior.
Source of truth: dev-memo/plan-batch-casebox-evidence-native-a07-feasibility-00.md (ENA0 feasibility plan, §1-§3 toolchain/CI/boundary); docs/adr/ADR-evidence-native-core-a07-feasibility.md (ENA-00); native/evidence-core/README.md (the JS shim contract, read-only reference); .github/workflows/ui-design-artifact.yml (the existing ubuntu check to preserve); .claude/rules/autonomy.md (this step is a user-authorized hard-stop); local Swift toolchain (Apple Swift 5.9.2, arm64-apple-macosx, available).
Allowed files: native/evidence-core-swift/** (new tree: Package.swift, Sources/**, Tests/**, README.md), .github/workflows/** (a NEW minimal macOS SwiftPM smoke workflow/job; the existing ui-design-artifact.yml stays unchanged), dev-memo/** (queue/governance/audit/study artifacts), and a narrow status note in dev-memo/plan-batch-casebox-evidence-native-a07-feasibility-00.md OR docs/adr/ADR-evidence-native-core-a07-feasibility.md ONLY if review-plan requires it
Forbidden files: apps/**, native/evidence-core/** (the JS shim — NO behavior change), services/**, docs/contracts/**, docs/product/**, .claude/hooks/**, .claude/settings.json, .claude/rules/**, AGENTS.md, CLAUDE.md, GEMINI.md, dev-memo/run/evidence/**, Evidence-Genie-M0-Developer-Handover.md (root), xiaolai-dev-workflow-study.md, package.json; NO PDFKit import (default; only if review-plan explicitly approves a compile-only import); NO A0.7 harness logic; NO marker / HMAC / provenance code
Gates: broker /cc-suite:review-plan REQUIRED before impl (HIGH-RISK: new Swift toolchain + macOS CI — no self-review); if review-plan returns READY but scopes broader than SwiftPM smoke, STOP and ask; broker /cc-suite:audit + /cc-suite:verify after impl (HIGH-RISK); swift build and swift test pass for native/evidence-core-swift (Swift is available locally); the package exposes only a version/smoke value (no behavior); the macOS CI workflow runs ONLY the SwiftPM smoke build/test and the existing ubuntu ui-design-artifact.yml is unchanged; no PDFKit import (grep); no A0.7 marker and nothing under dev-memo/run/evidence/; the native/evidence-core JS shim is unchanged; README states skeleton/no-A0.7/no-marker/no-PDF-geometry/no-product-behavior; scripts/workflow/check-contract-integrity.sh exits 0; npm --prefix apps/lawbar-desktop test passes (610/610); scripts/workflow/check-gates.sh returns GATES OK
Acceptance criteria: native/evidence-core-swift/ exists with Package.swift + Sources + Tests + README; swift test --package-path native/evidence-core-swift passes; the package exposes only a version/smoke value; a new .github/workflows macOS job runs only the SwiftPM smoke build/test; .github/workflows/ui-design-artifact.yml is byte-unchanged; grep shows no PDFKit import and no A0.7-marker/dev-memo-run-evidence reference; the native/evidence-core JS shim is byte-unchanged; the README states skeleton/no-A0.7/no-marker/no-PDF-geometry/no-product-behavior; npm desktop test shows pass 610 fail 0; check-gates GATES OK; broker audit returns no Critical/High/Medium and verify confirms ALL CLOSED; the committed diff is confined to native/evidence-core-swift/** plus the one new macOS workflow file
Risk flags: HIGH-RISK — introduces a new Swift/SwiftPM toolchain AND a new macOS CI job (both autonomy hard-stops, EXPLICITLY user-authorized for this narrow SwiftPM-skeleton + macOS-CI-smoke scope ONLY). broker review-plan REQUIRED + broker audit+verify after. Scope strictly limited to a buildable/testable skeleton + CI smoke; NO PDFKit/A0.7/marker/provenance/hooks/UI/product behavior; the JS shim is untouched. Swift toolchain confirmed available locally (5.9.2).
Depends on: none (the ENA0 feasibility proposal is already merged to main)
Commit boundary: one local commit -- native/evidence-core-swift/** plus the one new macOS smoke workflow file, staged by exact path; no push
