## WI-ENA5: coordinate transform roundtrip probe (fifth authorized implementation hard-stop; first geometry-adjacent transform)
Type: SCAFFOLD
Scope: Extend the merged native/evidence-core-swift package with a minimal, isolated coordinate-transform ROUNDTRIP probe that loads the EXISTING synthetic fixture (Tests/EvidenceCoreSmokeTests/Fixtures/synthetic-twopage.pdf), reads page 1's mediaBox width/height only, converts deterministic sample PDF-space point(s) into a normalized [0,1] representation and back to PDF-space, and asserts roundtrip equality within a tiny tolerance. The transform is a probe/test helper, NOT a product API (kept internal, exercised via @testable import). This is a coordinate transform ROUNDTRIP probe ONLY: NO PDF rendering/drawing, NO PDFView/UI coordinate conversion, NO thumbnail/pixel inspection, NO renderer-conformance, NO geometry pass/fail classification, NO anchors, NO citation/page-identity logic, NO persistence/storage, NO A0.7 harness logic, NO A0.7 marker, NO A0.7 green claim, NO marker provenance/HMAC, NO EVW5 hard hooks, NO Evidence UI/product behavior. Reuse the EXISTING synthetic fixture (NO new fixture unless review-plan explicitly requires one). Do NOT wire to the JS shim. Do NOT change the existing native/evidence-core JS shim behavior. Keep it a local Swift test / macOS CI smoke only. Naming says "probe"/"roundtrip probe", not "anchor"/"citation"/"A0.7". README/status note states: coordinate roundtrip probe proven; no renderer-conformance; no A0.7; no marker; no anchors/page-identity/citation; no product behavior.
Source of truth: dev-memo/plan-batch-casebox-evidence-native-a07-feasibility-00.md (ENA0 feasibility plan, §1-§3 toolchain/boundary — a coordinate roundtrip is the next feasibility probe after the page-box read); docs/adr/ADR-evidence-native-core-a07-feasibility.md (ENA-00); native/evidence-core-swift/** (the merged WI-ENA1..ENA4 package this WI extends, incl. the existing synthetic fixture + PageBoxProbe); native/evidence-core/README.md (the JS shim contract, read-only reference); .github/workflows/evidence-core-swift-smoke.yml (the existing macOS smoke job); .github/workflows/ui-design-artifact.yml (the existing ubuntu check to preserve); .claude/rules/autonomy.md (this step is a user-authorized hard-stop); .claude/rules/evidence-genie.md (A0.7 is the first real Evidence architecture gate; PRODUCTION anchors persist page-RATIO geometry against captured page geometry, rotation-aware, version-pinned — a probe-only invertibility check is NOT production anchor geometry and makes no such claim); local Swift toolchain (Apple Swift 5.9.2, arm64-apple-macosx, available).
Allowed files: native/evidence-core-swift/** (extend Sources/**, Tests/**, README/status note; Package.swift only if a target/test change requires it; reuse the EXISTING Fixtures/synthetic-twopage.pdf), .github/workflows/evidence-core-swift-smoke.yml (ONLY if a narrow test-command update is needed; otherwise byte-unchanged), dev-memo/** (queue/governance/audit/study artifacts)
Forbidden files: apps/**, native/evidence-core/** (the JS shim — NO behavior change), services/**, docs/contracts/**, docs/product/**, .claude/hooks/**, .claude/settings.json, .claude/rules/**, AGENTS.md, CLAUDE.md, GEMINI.md, dev-memo/run/evidence/**, Evidence-Genie-M0-Developer-Handover.md (root), xiaolai-dev-workflow-study.md, package.json; NO new PDF fixtures (reuse the existing one unless review-plan explicitly requires a new one); NO PDF rendering/drawing; NO PDFView/UI coordinate conversion; NO thumbnail/pixel-render code; NO renderer-conformance; NO geometry pass/fail classification; NO anchor code; NO citation/page-identity logic; NO persistence/storage; NO A0.7 harness logic; NO marker / HMAC / provenance code
Gates: broker /cc-suite:review-plan REQUIRED before impl (HIGH-RISK: first coordinate-transform step on the Evidence native-core path — no self-review); if review-plan returns READY but scopes broader than a coordinate transform roundtrip probe, STOP and ask; broker /cc-suite:audit + /cc-suite:verify after impl (HIGH-RISK, first coordinate transform); swift build and swift test pass for native/evidence-core-swift (Swift available locally); the probe is an isolated internal helper (not a product API) that normalizes a PDF-space point to [0,1] against page 1's mediaBox extent and inverts it, asserting roundtrip equality within a tiny tolerance on the existing synthetic fixture; the macOS CI smoke job runs the test and stays macOS-only; the existing ubuntu ui-design-artifact.yml is byte-unchanged; coordinate-transform logic appears ONLY in the ENA5 probe/test under native/evidence-core-swift/** (grep); no renderer-conformance impl (grep); no anchor/citation/page-identity/persistence code (grep); no PDFView/drawing/thumbnail/pixel-render code (grep); no A0.7 marker and nothing under dev-memo/run/evidence/ (grep); no HMAC/provenance (grep); no new fixture; the native/evidence-core JS shim is byte-unchanged; README/status note states coordinate-roundtrip-probe-proven/no-renderer-conformance/no-A0.7/no-marker/no-anchors-page-identity-citation/no-product-behavior; scripts/workflow/check-contract-integrity.sh exits 0; npm --prefix apps/lawbar-desktop test passes (610/610); scripts/workflow/check-gates.sh returns GATES OK
Acceptance criteria: native/evidence-core-swift/ now contains an isolated coordinate-transform roundtrip probe (internal EvidenceCoreCoordinateRoundtripProbe with normalize/denormalize + roundtripOnFixture(url:samples:) -> {loaded,width,height,maxAbsError}) plus a Swift test that loads the EXISTING synthetic fixture, reads page 1 mediaBox extent (612 x 792), roundtrips deterministic sample PDF-space points, and asserts maxAbsError < a tiny tolerance (e.g. 1e-9); swift test --package-path native/evidence-core-swift passes (skeleton + PDFKit + PDF-load + page-box + coordinate-roundtrip probes); grep shows coordinate-transform logic only in the ENA5 probe/test under native/evidence-core-swift/** and NO renderer-conformance/anchor/citation/page-identity/persistence/PDFView/drawing/thumbnail/pixel/marker/HMAC/provenance code and nothing under dev-memo/run/evidence/; NO new PDF fixture was added; the macOS CI workflow is either byte-unchanged or changed only by a narrow test-command line and remains macOS-only; .github/workflows/ui-design-artifact.yml is byte-unchanged; the native/evidence-core JS shim is byte-unchanged; the README/status note states coordinate-roundtrip-probe-proven/no-renderer-conformance/no-A0.7/no-marker/no-anchors-page-identity-citation/no-product-behavior; npm desktop test shows pass 610 fail 0; check-gates GATES OK; broker audit returns no Critical/High/Medium and verify confirms ALL CLOSED; the committed diff is confined to native/evidence-core-swift/** plus (at most) the one macOS smoke workflow file
Risk flags: HIGH-RISK -- first coordinate-transform step on the Evidence native-core path (autonomy hard-stop, EXPLICITLY user-authorized for this narrow coordinate-transform-roundtrip-probe scope ONLY). broker review-plan REQUIRED + broker audit+verify after. Scope strictly limited to an isolated, deterministic, reversible PDF-space<->normalized[0,1] roundtrip probe on the existing synthetic fixture; NO rendering, NO PDFView/UI conversion, NO renderer-conformance, NO geometry classification, NO anchors, NO citation/page-identity, NO persistence, NO A0.7 harness, NO marker/provenance/HMAC, NO hooks/UI/product behavior; the JS shim is untouched; no new dependency; no new fixture; the transform is probe-only (internal, not a product API). Swift toolchain confirmed available locally (5.9.2).
Depends on: none (the ENA4 page-box inspection probe is already merged to main)
Commit boundary: one local commit -- native/evidence-core-swift/** plus (at most) the one macOS smoke workflow file, staged by exact path; no push
