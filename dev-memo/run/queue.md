## WI-U1: project event_kind to the audit-list response DTO
Type: IMPL
Scope: Surface the Batch-1 `event_kind` across the IPC boundary for the audit panel. (1) Add `"event_kind"` to `LIST_AUDIT_EVENTS_RESPONSE_FIELDS` in `apps/lawbar-desktop/src/caseBox/dto/audit.ts` (the array `RendererAuditEventRow = Pick<CaseBoxAuditEvent, …>` derives from). Do NOT add `audit_schema_version` (it stays internal — hash-format selector, not display). (2) Do NOT edit `auditHandlers.ts`: its `projectPage<RendererAuditEventRow>(page, LIST_AUDIT_EVENTS_RESPONSE_FIELDS)` is array-driven and picks up the allowlist automatically (verify by test, not by editing the handler; if review proves a hard need to touch the handler, STOP and report). (3) Update `apps/lawbar-desktop/tests/ipc-list-projection.unit.test.mjs`: add `event_kind` to the audit consumed-field expectation (`AUDIT_CONSUMED`); add `event_kind` (a valid CASE_BOX_AUDIT_EVENT_KINDS key, e.g. "DEADLINE_MET") to the `rawAuditRow()` fixture; prove `event_kind` crosses the IPC boundary on every projected row WHILE the authority/internal fields (tenant_id, actor_user_id, id, matter_id) and the open-index extra remain stripped. (4) Update `apps/lawbar-desktop/tests/renderer-dto-sync.test.mjs` ONLY if explicit coverage is needed (its RESPONSE_ALLOWLISTS no-leak test auto-validates that event_kind is non-authority — preserve that behavior; optionally add an assertion that LIST_AUDIT_EVENTS_RESPONSE_FIELDS now includes event_kind). If closing this WI appears to require touching auditHandlers.ts, the renderer, services, contract, electron, or package.json, STOP and report.
Source of truth: apps/lawbar-desktop/src/caseBox/dto/audit.ts (LIST_AUDIT_EVENTS_RESPONSE_FIELDS, RendererAuditEventRow); apps/lawbar-desktop/src/caseBox/auditHandlers.ts (line 131 projectPage — READ-ONLY, confirms no handler change); apps/lawbar-desktop/tests/ipc-list-projection.unit.test.mjs (AUDIT_CONSUMED/AUDIT_AUTHORITY/rawAuditRow at ~271-310); docs/adr/audit-event-kind-preservation.md (event_kind is display metadata, non-authority); dev-memo/plan-batch-casebox-audit-event-kind-v2-ui-00.md (proposal)
Allowed files: apps/lawbar-desktop/src/caseBox/dto/audit.ts, apps/lawbar-desktop/tests/ipc-list-projection.unit.test.mjs, apps/lawbar-desktop/tests/renderer-dto-sync.test.mjs, dev-memo/deferred-audit-findings.md
Forbidden files: apps/lawbar-desktop/src/caseBox/auditHandlers.ts, apps/lawbar-desktop/renderer/**, apps/lawbar-desktop/electron/**, apps/lawbar-desktop/src/caseBox/dto/!(audit).ts, services/**, docs/contracts/**, apps/lawbar-desktop/package.json, .claude/**, scripts/workflow/**
Gates: npm --prefix apps/lawbar-desktop test (ipc-list-projection proves event_kind projected + authority stripped; renderer-dto-sync no-leak green); loc-guardian scan; cc-suite review-plan (batch) + audit + verify (broker)
Acceptance criteria: LIST_AUDIT_EVENTS_RESPONSE_FIELDS includes "event_kind" and NOT "audit_schema_version"; auditHandlers.ts UNCHANGED (the array-driven projection picks up event_kind automatically); ipc-list-projection.unit.test.mjs asserts event_kind crosses the IPC boundary on every audit row while tenant_id/actor_user_id/id/matter_id + the open-index extra remain stripped (rawAuditRow carries event_kind); renderer-dto-sync no-authority-leak test still passes; `npm --prefix apps/lawbar-desktop test` passes; NO renderer/handler/services/contract/electron/package.json change
Risk flags: IMPL (DTO allowlist + projection tests); event_kind is emitter-chosen display metadata, NOT a server-authority field (the renderer-dto-sync RESPONSE_ALLOWLISTS authority set excludes it); array-driven projection means no handler edit; cc-suite review/audit/verify via broker (UI lane); audit_schema_version deliberately NOT projected
Depends on: none
Commit boundary: one local commit for the DTO allowlist + projection tests

## WI-U2: design artifact — audit event-kind humanized label table
Type: ASSET
Scope: Author ONE design artifact at dev-memo/design/2026-06-08-audit-event-kind-labels.md specifying the renderer humanized-label mapping for WI-U3. No source edits. The artifact must contain: (1) the FULL `event_kind → humanized label` table for ALL current CASE_BOX_AUDIT_EVENT_KINDS keys (the 49 keys: matter registered/archived/unarchived; document registered/ocr-submitted/ocr-complete/ocr-failed/triaged/tagged/reviewed/soft-deleted/accessed/printed/shared; ocr_link snapshotted/refreshed; deadline registered/met/missed/withdrawn/missed-to-met; evidence proposed/accepted/rejected/superseded; fact proposed/reviewed/accepted/rejected/replacement-accepted; privilege_marker proposed/confirmed/dismissed/waived; external-ocr authorized/revoked; sync-grant granted/revoked; llm-extraction opt-in/opt-out; privilege-log-exported; case-data-exported; classification set/upgraded/downgraded/reset-to-unclassified; docket_entry proposed/confirmed/dismissed) — at minimum deadline met/missed/withdrawn/missed-to-met, docket proposal created/confirmed/dismissed, fact proposed/reviewed/accepted/rejected, document registered, matter created/archived/unarchived, plus the evidence/privilege/classification/ocr/sync/llm/export/access kinds; (2) the fallback rule — null/legacy event_kind → `action · entity_type`, and unknown future event_kind (not in the map) → `action · entity_type`; (3) the rule that the humanized label must appear in the ACCESSIBLE row text (not color/title only) and that the existing audit list `aria-live="polite"` announces it; (4) the constraint that labels MUST NOT imply facts not stored in the event (honest wording — e.g. "Deadline marked missed" not a value judgement); (5) whether the label REPLACES or AUGMENTS the existing `view-audit-action`/entity display (recommend one). Cite CASE_BOX_AUDIT_EVENT_KINDS (docs/contracts/case-box-contract/src/audit-log.ts:66-118 — READ-ONLY reference) and viewMatterAudit.ts renderAuditEventRow (current action·entity_type rendering). If authoring requires editing any source/test file, STOP and report.
Source of truth: docs/contracts/case-box-contract/src/audit-log.ts CASE_BOX_AUDIT_EVENT_KINDS (the 49 keys + their {action,entity_type} — READ-ONLY); apps/lawbar-desktop/renderer/screens/viewMatterAudit.ts renderAuditEventRow (current rendering); docs/adr/audit-event-kind-preservation.md §8 (renderer maps kind→label, fallback for null/unknown); dev-memo/plan-batch-casebox-audit-event-kind-v2-ui-00.md (proposal)
Allowed files: dev-memo/design/2026-06-08-audit-event-kind-labels.md
Forbidden files: apps/lawbar-desktop/**, services/**, docs/contracts/**, .claude/**, scripts/workflow/**, dev-memo/run/**
Gates: design-artifact self-review (low-risk ASSET, doc-only); no automated test gate
Acceptance criteria: dev-memo/design/2026-06-08-audit-event-kind-labels.md exists and contains the full event_kind→label table covering all 49 CASE_BOX_AUDIT_EVENT_KINDS keys (incl. the deadline/docket/fact/document/matter/evidence/privilege/classification/ocr/sync/llm/export/access kinds), the null+unknown → `action · entity_type` fallback rule, the accessible-row-text + aria-live requirement, the honest-wording constraint, and the replace-vs-augment decision; no source/test file changed
Risk flags: doc-only ASSET; no product/behavior change; low risk; cc-suite self-review permissible per cc-suite §Low-risk
Depends on: none
Commit boundary: one local commit for the design artifact

## WI-U3: render humanized audit event-kind labels (implement WI-U2)
Type: UI
Design artifact: dev-memo/design/2026-06-08-audit-event-kind-labels.md
Scope: Implement the WI-U2 design artifact in the audit renderer, using the WI-U1-projected `event_kind` (existing IPC; no DTO/IPC change here). (1) Add `readonly event_kind?: string;` to the renderer-local `AuditEventRow` interface in apps/lawbar-desktop/renderer/screens/viewMatterAudit.ts (line ~18). (2) Add a renderer-local label map in a NEW file apps/lawbar-desktop/renderer/screens/auditEventLabels.ts exporting `EVENT_KIND_LABELS` (covering all current event kinds per the artifact) + a small `auditEventLabel(ev)` helper that returns the humanized label for a known event_kind and the existing `action · entity_type` string for null/missing/unknown event_kind. (3) In renderAuditEventRow, render the humanized label (per the artifact's replace-vs-augment decision) in the accessible row text (a `view-audit-kind`-labelled span or the existing action span), keeping the `action · entity_type` fallback for legacy/null/unknown; the existing audit list `aria-live="polite"` must still announce it. Do NOT infer deadline met/missed/withdrawn from action/entity_type (honest fallback only). Do NOT display or project `audit_schema_version`. (4) Add `event_kind` to the `auditEvent()` fixture in apps/lawbar-desktop/tests/_view-matter-dom.mjs ONLY if needed for new tests (else pass via overrides). (5) Add renderer tests in a NEW file apps/lawbar-desktop/tests/renderer-audit-labels.test.mjs (registered by appending it to the `test` script's explicit file list in apps/lawbar-desktop/package.json — that ONE line only; no dependency/version/lockfile/other-script change) proving: a known event_kind renders the expected humanized label; a null/missing event_kind falls back to `action · entity_type`; an unknown future event_kind falls back to `action · entity_type`; the label is present in the accessible row text; existing audit-panel behavior is intact. Adjust renderer-view-matter.test.mjs ONLY if a minimal existing audit assertion needs updating (avoid growing it toward the 1200 cap). If closing this WI appears to require touching src/**, services/**, docs/contracts/**, electron/**, a dependency/lockfile, or any package.json key other than the one test-script line, STOP and report.
Source of truth: dev-memo/design/2026-06-08-audit-event-kind-labels.md (WI-U2 artifact); WI-U1's projected event_kind; apps/lawbar-desktop/renderer/screens/viewMatterAudit.ts (AuditEventRow + renderAuditEventRow); apps/lawbar-desktop/tests/_view-matter-dom.mjs (auditEvent fixture); apps/lawbar-desktop/package.json (explicit test list — append the new file)
Allowed files: apps/lawbar-desktop/renderer/screens/viewMatterAudit.ts, apps/lawbar-desktop/renderer/screens/auditEventLabels.ts, apps/lawbar-desktop/tests/renderer-audit-labels.test.mjs, apps/lawbar-desktop/tests/renderer-view-matter.test.mjs, apps/lawbar-desktop/tests/_view-matter-dom.mjs, apps/lawbar-desktop/package.json, dev-memo/deferred-audit-findings.md
Forbidden files: apps/lawbar-desktop/src/**, apps/lawbar-desktop/electron/**, services/**, docs/contracts/**, .claude/**, scripts/workflow/**, apps/lawbar-desktop/renderer/screens/!(viewMatterAudit|auditEventLabels).ts, package-lock.json, apps/lawbar-desktop/package-lock.json
Gates: npm --prefix apps/lawbar-desktop test (the new renderer-audit-labels.test.mjs MUST execute); loc-guardian scan (viewMatterAudit.ts + auditEventLabels.ts under 800; renderer-view-matter.test.mjs not grown over 1200; new test file under 1200); UI design-artifact gate (PR body cites the WI-U2 artifact); cc-suite review-plan (batch) + audit + verify (broker, UI lane)
Acceptance criteria: AuditEventRow has an optional event_kind; auditEventLabels.ts exports EVENT_KIND_LABELS (covering the current event kinds) + a label helper; renderAuditEventRow renders the humanized label for a known event_kind and falls back to `action · entity_type` for null/missing/unknown, in the accessible row text, with the audit list aria-live intact and no audit_schema_version display; the new renderer-audit-labels.test.mjs exists, is registered in package.json (exactly that one test-line), and proves known-label / null-fallback / unknown-fallback / accessible-text / existing-behavior-intact; `npm --prefix apps/lawbar-desktop test` passes with the new file executing; loc-guardian reports no over-limit file; NO src/services/contract/electron/dependency change; package.json diff is exactly the one test-script registration
Risk flags: UI/renderer only; uses existing IPC (WI-U1 projection); honest fallback (no inference of deadline transitions from action/entity_type); viewMatterAudit.ts 367/800 + a ~49-entry map in a sibling file (keeps the screen lean); renderer-view-matter.test.mjs 1101/1200 — label tests go in the NEW file to avoid the cap; package.json change is a single test-registration line (verify the staged diff); cc-suite review/audit/verify via broker; audit_schema_version NOT displayed
Depends on: WI-U1, WI-U2
Commit boundary: one local commit for the renderer label rendering + the label map + the new renderer test + the package.json test-line
