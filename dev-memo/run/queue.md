## WI-SEC-CASEBOX-TENANT-SCOPING-DEFENSE-00: row-level tenant scoping for case-box child reads + id-addressed mutations (Type: IMPL, security-boundary HIGH-RISK)

## Review packet (compact)
- Active plan summary: close the two Medium defense-in-depth findings from the gate-10 security-boundary audit (`docs/release/casebox-persistence-security-audit-00.md`, findings C0 H0 M2 L1, gate 10 NOT CLEARED). M-1: the case-box child-entity list/history SQLite reads run a `requireMatterTenant` matter-tenant preflight but the child-row SELECT lacks a row-level `tenant_id` predicate. M-2: the id-addressed mutations resolve+`UPDATE ... WHERE id = ?` with tenant asserted by the surrounding resolve/preflight, not atomically in the statement. The FUTURE execution lane adds `tenant_id` (+ `matter_id` where applicable) predicates to those SQL statements, following the ONE existing scoped-*mutation* precedent in the tree (`linkStatusResolverQueries.ts:157-158` — a real `UPDATE case_box_links SET status = ? WHERE id = ? AND tenant_id = ? AND matter_id = ?`); the several `WHERE id = ? AND tenant_id = ? AND matter_id = ?` occurrences in `deadlineRepoQueries.ts:157` / `docketRepoQueries.ts:424` / `evidenceRepoQueries.ts:292` / `factsRepoQueries.ts:345` are scoped SELECTs (read-side existence checks), NOT mutation precedents. Keeps in-memory/SQLite parity, and extends the tenant-mismatch regression tests (L-1). NO schema change (the `tenant_id` lifted columns already exist; adding a predicate is not DDL; `CURRENT_SCHEMA_VERSION` stays 12).
- Exact target files (future exec lane): SQLite reads M-1 — `services/case-box-persistence/src/sqlite/evidenceRepoQueries.ts` (`listEvidenceItemsSqlite`), `docketRepoQueries.ts` (`listDocketEntriesSqlite`), `auditRepoQueries.ts` (`listAuditEventsSqlite`), `classificationRepoQueries.ts` (`getEffectiveClassificationSqlite`, `listConfidentialityClassificationsSqlite`), `privilegeRepoQueries.ts` (`getPrivilegeStatusSqlite`, `listPrivilegeMarkersSqlite`), `ocrLinkRepoQueries.ts` (`listOcrLinksSqlite`); SQLite mutations M-2 (PRINCIPLED RULE: the COMPLETE set of id-addressed CHILD-entity mutation `UPDATE` statements in the persistence SQLite layer whose WHERE clause is currently `WHERE id = ?` only) — `updateFactRow` (facts transition), `updateDeadlineRow` (deadline transition), `updateDocketEntryRow` (docket confirm/dismiss) + `updateDocketEntryEditRow` (docket edit), `updateEvidenceItemRow` (evidence transition), `updateLinkMarkerRow` (link unlink/relink), `updatePrivilegeMarkerRow` (privilege transition). EXCLUDED with reason: `updateMatterRow` — matter is the tenant-OWNING parent (its PK *is* the tenant scope boundary and the archive handler already tenant-checks), not a child entity, so it is out of the audit's M-2 child-scoping class. FEASIBILITY + MECHANISM: these mutation APIs (`transitionFact`/`transitionDeadline`/`confirmDocketEntry`/`dismissDocketEntry`/`editDocketEntry`/`transitionEvidenceItem`/`transitionPrivilegeMarker`/link unlink-relink) take an entity id + opts and receive NO caller `tenant_id` (`types.ts:163`), so the by-id resolve/shadow-state loaders CANNOT gain a caller-tenant predicate without a FORBIDDEN API/signature change. The fix is therefore ATOMIC-CONSISTENCY hardening, NOT caller-tenant authorization: scope each `UPDATE` to `WHERE id = ? AND tenant_id = ? AND matter_id = ?` binding the RESOLVED ROW's OWN `tenant_id`+`matter_id` (read during the existing by-id resolve step), AND assert the better-sqlite3 `RunResult.changes === 1` BEFORE writing the audit event — so a scope-drift / concurrent-modification / corruption between the read and the write makes the UPDATE match zero rows and RAISES (a stable existing error, no new code), rather than writing an audit event for an unmodified row; the in-memory mirrors ONLY where a matching gap exists (`inMemoryEvidence.ts`, `inMemoryDocket.ts`, `inMemoryAudit.ts`, `inMemoryClassification.ts`, `inMemoryPrivilege.ts`, `inMemoryOcrLink.ts`, `inMemoryFact.ts`, `inMemoryDeadline.ts`); tests — `services/case-box-persistence/tests/tenant-filter-lists.test.mjs` (+ the affected `hardening-*.test.mjs` / `impl-parity-*.test.mjs` / conformance fixtures ONLY as needed for coverage + parity).
- Exact acceptance criteria: below (see Acceptance criteria).
- Exact out-of-scope: no schema/migration/DDL; no `CURRENT_SCHEMA_VERSION` change; no contract-package change; no `CaseBoxPersistenceError`/`OcrQueueError` code rename/removal/merge; no renderer/UI/IPC/DTO change (except test-harness wiring IF review justifies); no dependency; no native; no Forms T4/T5; no OCR-flake; no signing/notarization; no custody/JS-shim/A8; no release GO/NO-GO.
- Essential refs: `docs/release/casebox-persistence-security-audit-00.md` (M-1/M-2/L-1 evidence); `.claude/rules/security-boundary.md` (§"Required loop", §"No silent surface changes"); `AGENTS.md` §"Critical invariants" (stable error codes, payload_json canonical, no read-layer re-sort).
- Review questions: (1) Is the target-file set complete for M-1 + M-2 (any affected read/mutation path missed)? (2) Can the fix be done with NO schema change (predicate-only) given the `tenant_id` lifted columns already exist? (3) Does adding a row-level `tenant_id` predicate preserve the stable `tenant_mismatch`/`unknown_matter` error surface + in-memory/SQLite parity? (4) Is any renderer/contract/schema change smuggled in? (5) Is the L-1 test extension scoped to the affected paths only?

Type: IMPL
Scope: FIX the two Medium defense-in-depth findings the gate-10 audit escalated (`docs/release/casebox-persistence-security-audit-00.md`, gate 10 NOT CLEARED, C0 H0 M2 L1), via the FULL security-WI loop, so gate 10 can be cleared. The FUTURE execution lane: (a) M-1 — adds a row-level `tenant_id = ?` predicate (binding the caller's `query.tenant_id`) to the child-row SELECT WHERE clause of the 8 M-1 SQLite list/history reads (`listEvidenceItemsSqlite`, `listDocketEntriesSqlite`, `listAuditEventsSqlite`, `getEffectiveClassificationSqlite`, `listConfidentialityClassificationsSqlite`, `getPrivilegeStatusSqlite`, `listPrivilegeMarkersSqlite`, `listOcrLinksSqlite`), KEEPING the existing `requireMatterTenant` preflight (belt-and-suspenders, not a replacement); (b) M-2 — ATOMIC-CONSISTENCY hardening (NOT caller-tenant authorization — the APIs supply no caller tenant): scopes the mutating `UPDATE` of the COMPLETE child-entity mutation set to `WHERE id = ? AND tenant_id = ? AND matter_id = ?` binding the RESOLVED ROW's OWN `tenant_id`+`matter_id`, AND asserts `RunResult.changes === 1` before the audit-event write — `updateFactRow`, `updateDeadlineRow`, `updateDocketEntryRow` (confirm/dismiss) + `updateDocketEntryEditRow` (edit), `updateEvidenceItemRow`, `updateLinkMarkerRow`, `updatePrivilegeMarkerRow` — following the EXISTING scoped-mutation precedent (`linkStatusResolverQueries.ts:157-158` — a real scoped `UPDATE case_box_links SET status = ? WHERE id = ? AND tenant_id = ? AND matter_id = ?`). The by-id resolve/shadow-state loaders stay by-id (the mutation API supplies NO caller `tenant_id` — `types.ts:163` — so a loader caller-tenant predicate is not addable without a FORBIDDEN API-signature change). `updateMatterRow` is EXCLUDED (matter is the tenant-owning parent, PK-scoped, archive-handler tenant-checked — out of the audit's child-scoping class); (c) keeps in-memory/SQLite parity (mirror any predicate the in-memory path is missing so `impl-parity-*` stays green); (d) L-1 — extends `tenant-filter-lists.test.mjs` (+ the affected hardening/impl-parity/conformance suites only as needed) with regression tests proving a cross-tenant/cross-matter row is EXCLUDED from each affected read and REJECTED/untouched by each affected mutation (tests must fail before the fix, pass after). It PRESERVES: the matter-tenant preflight, the write-time `child.tenant_id === matter.tenant_id` invariant, the stable `CaseBoxPersistenceError`/`OcrQueueError` code surface + thrown shapes (`tenant_mismatch`/`unknown_matter`/`unknown_document`/`matter_id_mismatch` semantics UNCHANGED), the IPC forbidden-field + response-projection behavior, `payload_json` canonicality, the no-read-layer-re-sort invariant, and the existing public API/DTO behavior (no wire-format change). NO schema/DDL (the `tenant_id` lifted columns already exist — a predicate is not a migration; `CURRENT_SCHEMA_VERSION` stays 12). If review proves a target path cannot be scoped without a schema or contract change, STOP and report (do NOT smuggle it). Full loop: plan-review READY (this authoring lane) then the exec lane does tests-first (red) then minimal implement (green) then `/cc-suite:audit` (security persona) then `/cc-suite:verify` ALL CLOSED then a sign-off append to `docs/release/casebox-persistence-security-audit-00.md` recording gate 10 CLEARED-pending-user-approval (which still does NOT assert go-live).
Source of truth: docs/release/casebox-persistence-security-audit-00.md (the M-1/M-2/L-1 findings this WI closes — the authoritative fix spec, incl. the exact affected functions + file:line evidence); .claude/rules/security-boundary.md (§"In-scope work" tenant isolation; §"Required loop" plan-review then tests then implement then audit then verify then sign-off; §"No silent surface changes" — stable OcrQueueError codes, no thrown-shape change without an ADR; §"Go-live independence"); .claude/rules/cc-suite.md (§"High-risk WIs" security = required broker; §"Audit remediation policy"); AGENTS.md §"Critical invariants" (stable OcrQueueError codes, payload_json canonical, no queue→ocr_jobs FK, no read-layer re-sort, contract=vocabulary owner); services/case-box-persistence/src/sqlite/*RepoQueries.ts + inMemory*.ts (the fix targets); services/case-box-persistence/tests/ (the regression targets); linkStatusResolverQueries.ts:157-158 (the ONLY existing scoped-mutation UPDATE precedent to mirror — the id+tenant_id+matter_id occurrences in deadline/docket/evidence/facts RepoQueries are scoped SELECTs, not mutations); .claude/rules/autonomy.md + staging-hygiene.md + loc-guardian.md
Allowed files: services/case-box-persistence/src/sqlite/evidenceRepoQueries.ts, docketRepoQueries.ts, auditRepoQueries.ts, classificationRepoQueries.ts, privilegeRepoQueries.ts, ocrLinkRepoQueries.ts, factsRepoQueries.ts, deadlineRepoQueries.ts, linkRepoQueries.ts (the M-1 read + M-2 mutation targets — predicate-only edits, NO schema.ts); services/case-box-persistence/src/inMemoryEvidence.ts, inMemoryDocket.ts, inMemoryAudit.ts, inMemoryClassification.ts, inMemoryPrivilege.ts, inMemoryOcrLink.ts, inMemoryFact.ts, inMemoryDeadline.ts (parity mirrors — ONLY where a matching gap exists); services/case-box-persistence/tests/tenant-filter-lists.test.mjs + the affected hardening-*.test.mjs / impl-parity-*.test.mjs / conformance fixtures (regression + parity coverage for L-1 + the M-1/M-2 paths); docs/release/casebox-persistence-security-audit-00.md (the exec lane appends a sign-off section ONLY — it does NOT rewrite the findings); dev-memo/run/reviews/ (the exec lane's own review/audit artifact). NOTE: THIS authoring lane commits ONLY the queue governance (queue.md/linted/reviewed/governed) + reviews/queue-review-132.md — NO persistence/test/report code. The future execution lane commits the fix + tests + sign-off and records its cc-suite audit/verify chain in the COMMIT MESSAGE; it MUST NOT modify this lane's queue governance.
Forbidden files: services/case-box-persistence/src/sqlite/schema.ts (CURRENT_SCHEMA_VERSION stays 12; NO DDL/migration — the tenant_id columns already exist), docs/contracts/ (NO contract/vocabulary change), migrations/ (none), native/, package.json/package-lock.json of any package (NO dependency), apps/lawbar-desktop/ EXCEPT nothing (NO renderer/IPC/DTO/handler change — the IPC boundary already injects/strips tenant; if review shows a handler MUST change, STOP and report), dev-memo/run/intake/ (raw samples — never staged), docs/adr/ + docs/product/ + other dev-memo/plan/design docs, docs/release/ OTHER than the sign-off append to casebox-persistence-security-audit-00.md, scripts/ + .claude/ + .github/, Evidence-Genie-M0-Developer-Handover.md + xiaolai-dev-workflow-study.md; NO schema/migration; NO OcrQueueError/CaseBoxPersistenceError-code rename/removal/merge; NO thrown-shape change; NO contract change; NO dependency; NO native; NO OCR-flake fix; NO Forms T4/T5; NO product-UI buildout; NO signing/notarization; NO custody/JS-shim/A8; NO release GO/NO-GO decision. Push is NEVER automatic (autonomy hard-stop): local commit governance is the default; any branch push + PR open happens ONLY under explicit per-invocation user authorization; NO merge; NO branch delete
Gates: IMPL security-boundary lane (HIGH-RISK broker category per cc-suite — broker REQUIRED, not self-review). (1) THIS authoring lane: broker /cc-suite:review-plan on this queue WI, Path 1, retrievable, then govern. (2) The FUTURE exec lane: tests-first (new tenant-mismatch regressions RETURN failing before the fix), minimal implement, per-package npm test for case-box-persistence PASS (npm --prefix services/case-box-persistence test — conformance + hardening + impl-parity all green), broker /cc-suite:audit (security persona) over the diff with 11-field recording, broker /cc-suite:verify ALL CLOSED. (3) scripts/workflow/check-contract-integrity.sh exit 0; scripts/workflow/check-queue.sh PASS (both lanes). Forbidden-scope scan before commit: CURRENT_SCHEMA_VERSION EQUALS 12; no schema/contract/dependency/native/renderer change; no error-code rename; no raw-sample commit. STOP-points: a target path cannot be scoped without schema/contract change → STOP and report (do NOT smuggle); review/audit finds the predicate breaks parity or an error-shape → fix-forward inside scope or STOP; any forbidden file would change → STOP.
Acceptance criteria: check-queue and check-contract-integrity PASS and CURRENT_SCHEMA_VERSION EQUALS 12 and the diff CONTAINS only the allowed persistence/test/sign-off files and no schema/contract/dependency/native/renderer change appears and no error-code rename appears. M-1: each of the 8 child-row SELECTs (listEvidenceItemsSqlite, listDocketEntriesSqlite, listAuditEventsSqlite, getEffectiveClassificationSqlite, listConfidentialityClassificationsSqlite, getPrivilegeStatusSqlite, listPrivilegeMarkersSqlite, listOcrLinksSqlite) must CONTAIN a row-level tenant_id predicate binding query.tenant_id, and a new regression must RETURN a cross-tenant row EXCLUDED from each (real caller-tenant enforcement — the read receives query.tenant_id). M-2: each of the 7 child-entity mutation UPDATEs (updateFactRow, updateDeadlineRow, updateDocketEntryRow, updateDocketEntryEditRow, updateEvidenceItemRow, updateLinkMarkerRow, updatePrivilegeMarkerRow) must CONTAIN an id+tenant_id+matter_id predicate bound to the resolved row's own scope AND must assert RunResult.changes EQUALS 1 before the audit-event write (atomic-consistency, NOT caller-rejection — the API supplies no caller tenant; the by-id loaders stay unchanged, no API/signature change); a new regression must show a scope-mismatched UPDATE matches zero rows and RAISES rather than writing a spurious audit event. Both: check-queue and check-contract-integrity PASS, CURRENT_SCHEMA_VERSION EQUALS 12, the diff CONTAINS only allowed persistence/test/sign-off files, no schema/contract/dependency/native/renderer change appears, no error-code rename appears, the requireMatterTenant preflight and the stable tenant_mismatch/unknown_matter error surface are PRESERVED, the new regressions FAIL before the fix and PASS after, and the case-box-persistence conformance + hardening + impl-parity suites all PASS. review-plan (this lane) must RETURN READY. (Exec lane, recorded THERE not here: /cc-suite:audit + /cc-suite:verify must RETURN ALL CLOSED; the sign-off append must STATE gate 10 CLEARED pending user go-live approval and MUST NOT assert go-live.)
Risk flags: HIGH (security-boundary — tenant isolation; persistence code). Care points: (a) PREDICATE-ONLY — no schema/DDL (tenant_id columns exist); if a path needs schema, STOP; (b) STABLE SURFACE — tenant_mismatch/unknown_matter/unknown_document/matter_id_mismatch codes + thrown shapes UNCHANGED (a rename/collapse needs an ADR + user approval, per security-boundary.md §"No silent surface changes"); (c) PARITY — every SQLite predicate added must keep impl-parity-*.test.mjs green (mirror in the in-memory path if it has the same gap); (d) DEFENSE-IN-DEPTH, not a live-leak fix — v1 is single-tenant so this hardens forward-multi-tenant, and MUST NOT regress the existing preflight/write-invariant; (e) TESTS-FIRST — the regression must prove a cross-tenant/cross-matter row is excluded/rejected (red before, green after); (f) GO-LIVE INDEPENDENCE — clearing gate 10 does NOT imply go-live. If any cannot hold, STOP and report.
Depends on: none
Commit boundary: TWO separate lanes. (1) THIS authoring lane commits ONLY the queue governance (queue.md/linted/reviewed/governed) + reviews/queue-review-132.md — no persistence/test/report code, no implementation. (2) The FUTURE execution lane commits ONLY the persistence fix + regression tests + the sign-off append to docs/release/casebox-persistence-security-audit-00.md + its own review/audit artifact under dev-memo/run/reviews/, records its cc-suite audit/verify chain in the COMMIT MESSAGE, and does NOT re-stage this lane's queue governance / queue-review-132.md. NO schema/contract/migration/native/dependency/renderer file in either lane; NO staging of dev-memo/run/intake/ or untracked residue; push is NEVER automatic (autonomy hard-stop) — local commit governance is the default, and any branch push + PR open happens ONLY under explicit per-invocation user authorization; no merge; no branch delete. THIS authoring lane implements NOTHING — it only governs the fix WI.
