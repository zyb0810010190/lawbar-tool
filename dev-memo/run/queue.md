## WI-GATE3-R2-OVERDUE-DASHBOARD-BANNER-00: global (cross-matter) overdue-deadline dashboard banner at app-open home (Type: UI, release-readiness MEDIUM risk)

## Review packet (compact)
- Active plan summary: implement gate-3 residual **R2** — the missing **global overdue-deadline dashboard banner** (brief §10: a banner shown whenever ANY deadline across ANY matter is overdue or due within 7 days, visible when the case-box UI opens). This is DISTINCT from the already-existing per-matter urgency banner in `renderer/screens/viewMatterDeadlines.ts` (which stays unchanged and MUST NOT be rebuilt). The FUTURE execution lane implements the design artifact `dev-memo/design/2026-07-05-global-overdue-dashboard-banner.md`: a `role="status"` banner at the top of the matter-list home (`listMatters.ts`), summarising cross-matter overdue + due-soon counts, aggregated CLIENT-SIDE over the EXISTING channels (iterate `casebox:matter:list` matters, read each matter's deadlines via the EXISTING `casebox:deadline:list`, classify with the REUSED `classifyDeadlineUrgency` + `DEADLINE_DUE_SOON_WINDOW_MS` from `renderer/format.ts`) — NO new IPC channel, NO contract/DTO/persistence change, NO reimplementation of the urgency rule.
- Exact target files (future exec lane): `apps/lawbar-desktop/renderer/screens/listMatters.ts` (mount the banner at the home surface) and/or a small NEW banner module under `apps/lawbar-desktop/renderer/` (e.g. `renderer/overdueDashboardBanner.ts`) + minimal wiring in `renderer/index.ts`/`nav.ts` if a shell-level mount is chosen; `apps/lawbar-desktop/renderer/api.ts` (ONLY if a thin client-side aggregation helper over the existing listMatters + deadline:list calls is added — no new IPC); `apps/lawbar-desktop/renderer/i18n/` catalog (banner strings); `apps/lawbar-desktop/renderer/index.css` (banner styling via existing design tokens only — no hard-coded colour); a NEW renderer test (peer of `tests/renderer-deadline-urgency.test.mjs`). REUSE-ONLY (no edit): `renderer/format.ts` (`classifyDeadlineUrgency`, `DEADLINE_DUE_SOON_WINDOW_MS`, `deadlineUrgencyLabel`).
- Exact acceptance criteria: below.
- Exact out-of-scope: NO new IPC channel / handler / persistence / contract / DTO (client-side aggregation over existing `casebox:matter:list` + `casebox:deadline:list` only); NO change to the per-matter `viewMatterDeadlines.ts` banner or to `format.ts`'s urgency rule (reuse only); NO escalation/push/email/SMS (brief §10 excludes them); NO gate-4 signing/distribution decision; NO package/dependency change; NO schema/migration; NO OCR/Forms; NO release-doc finalization; NO gate-6 audit; NO final GO/NO-GO.
- Essential refs: `dev-memo/design/2026-07-05-global-overdue-dashboard-banner.md` (the design artifact — the noun this WI implements); brief §10 (global banner) + §18 (per-matter urgency already exists); `docs/release/gate3-client-release-readiness-00.md` §2/§3 (R2); `renderer/format.ts` (`classifyDeadlineUrgency`); `renderer/screens/viewMatterDeadlines.ts` (the existing per-matter banner — the pattern to mirror, NOT rebuild); `UI-GATES.md`.
- Review questions: (1) Does the WI correctly REUSE `classifyDeadlineUrgency` from `format.ts` rather than reimplementing urgency? (2) Is the data-source boundary truly UI-only (client-side aggregation over existing channels, no new IPC/persistence/contract)? (3) Does it clearly NOT rebuild/modify the existing per-matter `viewMatterDeadlines.ts` banner? (4) Are the states (shown/hidden/loading/degraded), a11y (`role="status"`), and i18n specified + testable? (5) Is any gate-4 decision, gate-6 audit, or release-doc finalization smuggled in, and does it keep gate 3 honest (PARTIAL/advance without collapsing gate-4)?

Type: UI
Design artifact: dev-memo/design/2026-07-05-global-overdue-dashboard-banner.md (committed design spec; the noun this Type:UI WI implements against, per UI-GATES.md)
Scope: Implement gate-3 residual R2 — the global (cross-matter) overdue-deadline dashboard banner at the app-open home, per the design artifact. The FUTURE execution lane: (a) adds a `role="status"` banner at the top of the matter-list home (`renderer/screens/listMatters.ts`) — or a shell-level mount if it does not duplicate the per-matter banner — summarising the cross-matter overdue + due-soon counts; (b) aggregates CLIENT-SIDE over the EXISTING channels (the matters from `casebox:matter:list`, each matter's deadlines from the EXISTING `casebox:deadline:list`), classifying each pending deadline with the REUSED `classifyDeadlineUrgency` + `DEADLINE_DUE_SOON_WINDOW_MS` from `renderer/format.ts` — NO new IPC/contract/DTO/persistence and NO reimplementation of the urgency rule; (c) handles the states (shown when overdue>0 or due-soon>0; hidden when both zero; no loading flash; a non-blocking degraded state on a failed read that never crashes the home), the a11y (`role="status"`, real text, tokenised colour — no hard-coded colour), and i18n (all strings via the existing catalog — no raw English); (d) adds a renderer test proving the aggregation + banner states + a11y + i18n; (e) leaves the existing per-matter `viewMatterDeadlines.ts` banner UNCHANGED. It records whether gate 3 remains PARTIAL or advances WITHOUT collapsing the gate-4 STOP-AND-ASK (signing/notarization/public distribution stay the user's) and NEVER implies go-live. FORBIDDEN in the exec lane: any new IPC channel / handler / persistence / contract / DTO; any change to `viewMatterDeadlines.ts`'s banner or to `format.ts`'s urgency rule; any escalation/push/email/SMS; any package/dependency change; any schema/migration; any OCR/Forms work; a gate-4/gate-6/release-doc-finalization/GO-NO-GO decision. If the exec lane finds the banner cannot be built without a new IPC/persistence aggregation (e.g. matter volume makes client-side aggregation unacceptable), STOP and report (a separate persistence/IPC WI is then required — out of this UI WI's scope).
Source of truth: dev-memo/design/2026-07-05-global-overdue-dashboard-banner.md (the design artifact); docs/product/project-requirements-brief.md §10 (global overdue dashboard banner — v1 must-have; "visible … only", no escalation) + §18 (per-matter urgency surfacing — already shipped); docs/release/gate3-client-release-readiness-00.md §2/§3 (R2 residual + the §10-vs-§18 distinction); docs/release/go-live-readiness-report.md §1 gate 3 (PARTIAL) + gate 4 (STOP-AND-ASK); apps/lawbar-desktop/renderer/format.ts (classifyDeadlineUrgency + DEADLINE_DUE_SOON_WINDOW_MS — reuse); apps/lawbar-desktop/renderer/screens/viewMatterDeadlines.ts + listMatters.ts (the existing per-matter banner to mirror-not-rebuild + the home host); apps/lawbar-desktop/src/caseBox/handlerShared.ts (the existing casebox:matter:list + casebox:deadline:list channels — the ONLY data source; no new channel); UI-GATES.md (Type:UI design-artifact + a11y/tokenised-colour conventions); .claude/rules/client-local-first.md + autonomy.md + staging-hygiene.md + loc-guardian.md
Allowed files: apps/lawbar-desktop/renderer/screens/listMatters.ts (home banner mount) + a small NEW apps/lawbar-desktop/renderer/ banner module (e.g. overdueDashboardBanner.ts) + minimal apps/lawbar-desktop/renderer/index.ts / nav.ts wiring IF a shell mount is chosen + apps/lawbar-desktop/renderer/api.ts (ONLY a thin client-side aggregation helper over the EXISTING listMatters + deadline:list calls — no new IPC) + apps/lawbar-desktop/renderer/i18n/ (banner strings) + apps/lawbar-desktop/renderer/index.css (banner styling via existing tokens only) + a NEW apps/lawbar-desktop/tests/ renderer test + dev-memo/run/reviews/ (the exec lane's review artifact). NOTE: THIS authoring lane commits ONLY the queue governance (queue.md/linted/reviewed/governed) + the design artifact dev-memo/design/2026-07-05-global-overdue-dashboard-banner.md + reviews/queue-review-138.md — NO renderer/source/test change. The future exec lane records its evidence in the COMMIT MESSAGE + review artifact and does NOT re-stage this lane's governance/design files.
Forbidden files: apps/lawbar-desktop/renderer/format.ts (REUSE-ONLY — no change to the urgency rule), apps/lawbar-desktop/renderer/screens/viewMatterDeadlines.ts (the existing per-matter banner — NOT rebuilt/modified), apps/lawbar-desktop/src/** + electron/** (NO IPC/handler/main/preload change — the data comes from the EXISTING channels), apps/lawbar-desktop/src/caseBox/handlerShared.ts (NO new channel), services/** (NO persistence/contract/OCR change), docs/contracts/, services/*/src/sqlite/schema.ts (CURRENT_SCHEMA_VERSION stays 12), migrations/, native/, package.json/package-lock.json of any package (NO dependency), apps/lawbar-desktop/package.json (NO metadata/build change), dev-memo/run/intake/ (raw samples — never staged), docs/adr/ + docs/product/ (READ-ONLY evidence — NO brief/ADR edit), docs/release/ (NO release-doc finalization — a future readiness-refresh reassesses gate 3), scripts/ + .claude/ + .github/; NO new IPC/persistence/contract/DTO; NO escalation/push/email/SMS; NO dependency; NO schema/migration; NO OCR; NO Forms T4/T5; NO gate-4 signing/distribution decision; NO gate-6 audit; NO release-doc finalization; NO final GO/NO-GO decision. Push is NEVER automatic (autonomy hard-stop): local commit governance is the default; any branch push + PR open happens ONLY under explicit per-invocation user authorization; NO merge; NO branch delete
Gates: UI lane (release-readiness MEDIUM risk — a renderer banner reusing existing urgency logic + existing data channels). (1) THIS authoring lane: broker /cc-suite:review-plan on this queue WI, Path 1, retrievable, then govern. UI design-artifact gate: the design artifact dev-memo/design/2026-07-05-global-overdue-dashboard-banner.md is committed + referenced (check-queue UI gate). (2) The FUTURE exec lane: implement per the design artifact; run npm --prefix apps/lawbar-desktop test (full desktop suite PASSES incl. the new banner test + the existing renderer-deadline-urgency / renderer-list-matters / i18n-guard / no-hardcoded-color guards); the PR body carries the concrete Design artifact: line (check-ui-design-artifact CI gate); broker /cc-suite:review-plan or /cc-suite:audit on the diff per convention. (3) scripts/workflow/check-contract-integrity.sh exit 0; scripts/workflow/check-queue.sh PASS (both lanes). Forbidden-scope scan before commit: CURRENT_SCHEMA_VERSION EQUALS 12; no src/electron/IPC/handler/persistence/contract change; no format.ts or viewMatterDeadlines.ts change; no dependency; no new channel; no raw sample. STOP-points: the banner needs a new IPC/persistence aggregation → STOP (separate WI); review asks to modify the per-matter banner or format.ts urgency rule → REJECT; any forbidden file would change → STOP.
Acceptance criteria: check-queue and check-contract-integrity PASS and CURRENT_SCHEMA_VERSION EQUALS 12 and the diff CONTAINS only renderer banner/i18n/css/test files + the exec review artifact and no src/electron/IPC/handler/persistence/contract/format.ts/viewMatterDeadlines.ts/dependency change appears. The banner must REUSE classifyDeadlineUrgency from format.ts — mechanically, the banner/aggregation module must `import { classifyDeadlineUrgency } from "../format.js"` (or the equivalent relative path) so the diff CONTAINS that import and no urgency rule is reimplemented — and aggregate over the EXISTING casebox:matter:list + casebox:deadline:list channels (no new IPC). The new renderer test must PASS and must: RENDER the banner with correct overdue + due-soon counts when either is > 0, keep the banner HIDDEN when both EQUAL 0, RETURN a non-blocking degraded state on a failed read (home still renders), assert the banner CONTAINS role="status", and assert i18n labels resolve (no raw English). The full npm --prefix apps/lawbar-desktop test suite must PASS. The existing viewMatterDeadlines.ts per-matter banner must remain UNCHANGED. review-plan (this lane) must RETURN READY.
Risk flags: MEDIUM (renderer UI + cross-matter aggregation; but reuses the existing urgency rule + existing data channels — no new IPC/persistence/contract). Care points: (a) REUSE not rebuild — classifyDeadlineUrgency from format.ts is reused; the per-matter viewMatterDeadlines.ts banner is untouched; (b) UI-ONLY DATA — client-side aggregation over existing channels; a new IPC/persistence aggregation is a STOP (separate WI); (c) DEGRADED-NOT-CRASH — a failed per-matter read must not crash the home; (d) A11y + i18n + tokenised colour — role="status", no raw English, no hard-coded colour (existing guards); (e) GATE-4 INTACT — no signing/distribution decision; gate 3 stays honest (PARTIAL/advance, never go-live); (f) DESIGN-ARTIFACT-GATED — implements the committed design spec, does not invent UI direction. If any cannot hold, STOP and report.
Depends on: none
Commit boundary: TWO separate lanes. (1) THIS authoring lane commits ONLY the queue governance (queue.md/linted/reviewed/governed) + the design artifact dev-memo/design/2026-07-05-global-overdue-dashboard-banner.md + reviews/queue-review-138.md — no renderer/source/test change, no implementation. (2) The FUTURE execution lane commits ONLY the renderer banner + i18n + css + the new renderer test + its own review artifact under dev-memo/run/reviews/, records its evidence + any cc-suite chain in the COMMIT MESSAGE, and does NOT re-stage this lane's governance/design files. NO src/electron/IPC/handler/persistence/contract/format.ts/viewMatterDeadlines.ts/schema/dependency file in either lane; NO staging of dev-memo/run/intake/ or untracked residue; push is NEVER automatic (autonomy hard-stop) — local commit governance is the default, any branch push + PR open happens ONLY under explicit per-invocation user authorization; no merge; no branch delete. THIS authoring lane implements NOTHING — it only governs the R2 dashboard-banner UI WI + commits its design artifact.
