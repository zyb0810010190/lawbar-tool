## WI-805: Split the ipc-handlers.unit.test.mjs monolith below the LOC threshold
Type: TEST
Scope: reduce apps/lawbar-desktop/tests/ipc-handlers.unit.test.mjs (currently 1534 raw LOC — over the loc-guardian hand-written-test fail threshold of 1200 AND past the 1500 "critical structural" line) below 1200 with real margin, by MOVING the cohesive case-box ENTITY IPC test group — the listDeadlines + listFacts read tests AND the WI-601 docket create/confirm + WI-602 fact create write tests (the trailing block from the "// ---------- listDeadlines ----------" section to EOF, ~lines 1037-1534, together with the makeDocketProvider / makeFactProvider / validCreateDto / ENTRY_ID / DEADLINE_ID helpers defined inside that block) — VERBATIM into a NEW sibling file apps/lawbar-desktop/tests/ipc-casebox-handlers.unit.test.mjs (mirroring the WI-704 renderer-fact-write split). The new file prepends a LOCAL copy of the shared harness helpers the moved tests use (FIXED_NOW, FIXED_ID, clock, idFactory, makeProvider) and the handler imports they need (listDeadlinesHandler / listFactsHandler from handlers.js; createDocketEntryHandler / confirmDocketEntryHandler from docketHandlers.js; createFactHandler from factHandlers.js; AND CHANNEL from handlerShared.js — the moved fact-create test asserts CHANNEL.factCreate). The new file MUST NOT import CaseBoxPersistenceError (the moved block does not use it; the HOST keeps CaseBoxPersistenceError + CHANNEL for its matter tests). The host (ipc-handlers.unit.test.mjs) keeps the matter + audit + document IPC tests and has ONLY its now-UNUSED imports pruned (listDeadlinesHandler, listFactsHandler, createDocketEntryHandler, confirmDocketEntryHandler, createFactHandler) — keep CHANNEL and CaseBoxPersistenceError in the host. Wire the new file into the package.json "test" AND "test:unit" AND "test:ipc-unit" scripts (all three currently name ipc-handlers.unit.test.mjs; the new file must join all three so the targeted scripts do not silently drop the moved deadline/fact/docket coverage). Preserve every assertion VERBATIM (move, not rewrite). TEST-ONLY: change NO product/renderer/electron/src/services/contract source; preserve coverage exactly (same total test count). Close the pre-existing critical LOC finding by recording + closing a row in dev-memo/deferred-audit-findings.md. If a clean move is impossible without touching production code, STOP and report.
Source of truth: apps/lawbar-desktop/tests/ipc-handlers.unit.test.mjs (the monolith + its inline harness); .claude/loc-guardian.local.md (hand-written-test fail = raw LOC >= 1200; critical >= 1500); apps/lawbar-desktop/tests/renderer-fact-write.test.mjs (the WI-704 sibling-split precedent)
Allowed files: apps/lawbar-desktop/tests/ipc-handlers.unit.test.mjs, apps/lawbar-desktop/tests/ipc-casebox-handlers.unit.test.mjs, apps/lawbar-desktop/package.json, dev-memo/deferred-audit-findings.md
Forbidden files: apps/lawbar-desktop/renderer/**, apps/lawbar-desktop/src/**, apps/lawbar-desktop/electron/**, services/case-box-persistence/**, docs/contracts/**, apps/lawbar-desktop/tests/_view-matter-dom.mjs, apps/lawbar-desktop/tests/renderer-view-matter.test.mjs
Gates: npm --prefix apps/lawbar-desktop test
Acceptance criteria: the case-box entity IPC tests (listDeadlines + listFacts + docket create/confirm + fact create) are MOVED (not duplicated) from ipc-handlers.unit.test.mjs into ipc-casebox-handlers.unit.test.mjs with assertions preserved; ipc-handlers.unit.test.mjs raw LOC drops BELOW 1200 with margin (target <= ~1050); ipc-casebox-handlers.unit.test.mjs is below 1200; the new file is added to the package.json "test", "test:unit", AND "test:ipc-unit" scripts (all three currently name ipc-handlers.unit.test.mjs) so the targeted scripts do not silently drop the moved coverage, and is exercised by the gate; the full apps/lawbar-desktop test suite passes with the SAME total test count as before the move AND the SAME set of moved test titles present in the destination file (verified by comparing the moved test-title set before vs after, not merely the total count); the host's now-unused handler imports are pruned (no unused-import lint/build error) while CHANNEL + CaseBoxPersistenceError remain in the host; the new file imports CHANNEL (for CHANNEL.factCreate) and does NOT import CaseBoxPersistenceError; the pre-existing 1534-LOC critical finding is recorded + closed in dev-memo/deferred-audit-findings.md citing this WI; no product/renderer/electron/src/services/contract file is modified
Risk flags: test-only maintenance; NO product behavior change; broker review-plan + audit + verify required; closes a pre-existing CRITICAL-tier LOC finding (1534-LOC test monolith); not a Type UI WI (no renderer/** path touched) so no Design-artifact gate applies; the move must prune the host's now-unused imports to keep the build green
Depends on: none
Commit boundary: one local commit for the test split + host import pruning + package.json wiring + LOC-finding ledger closure
