## WI-EVIDENCE-A10-DESIGN-00: court-fileable export / CanonicalExportModel architecture gate (Type: PLAN)
Type: PLAN
Scope: DESIGN/ADR ONLY — author ONE Evidence-Genie A10 architecture decision record, `docs/adr/ADR-evidence-a10-court-fileable-export.md`. NO implementation, NO CanonicalExportModel code, NO export/render stub, NO source/schema/native/contract/test change. A10 ("Court-fileable export reproducibility") is the last un-built hard-invariant architecture gate (A1/A3/A8/A10 per the TRACKED handover `docs/reference/evidence-genie-m0-developer-handover.md`); it gives A8.6 restore-and-reproduce a precise target (A10-T6 `CanonicalExportModel` byte-identical) without implementing it. Grounding (verified read-only on main): A10 = SPEC-ONLY (no `CanonicalExportModel` type anywhere; only the A3 precursor `ExportCitationResult` in `services/case-box-persistence/src/sqlite/exportCitationQueries.ts` + the BUILT A3 export-degradation contract `docs/adr/ADR-evidence-a3-export-degradation.md`); the native `golden-export`/`citation-stability-gate`/`a3-regression` harnesses are `not_implemented`; A0.7 is meaningfully green (messy fixtures merged). The ADR DECIDES the CORE A10 architecture from the handover ticket-depth spec — A10-T1 (citation-rendering contract: the SINGLE code path producing citation strings from `DocumentPage`, pinning `citationFormatVersion` + `exportTemplateVersion`, owning the `ExportCitationFlag` enum), A10-T2 (hyperlink degradation: every in-app link → textual 卷X页Y OR an explicit `ExportCitationFlag`, total + bijective, never dropped/silently-wrong — building on the A3-EXPORT-00 precedence), A10-T6 (`CanonicalExportModel` byte-identical golden via deterministic serialization), A10-T7 (`ExportPreview` with `canonicalModelSha256` written into the `SnapshotManifest`; verify-before-hearing; citation-map change invalidates the cached preview) — and TRANSCRIBES (does NOT invent) the handover's `CanonicalExportModel` / `ExportPreview` / `ExportCitationFlag` shapes + the canonicalModelSha256 determinism rule (stable key/row order, normalized Unicode/whitespace, no timestamps unless court-facing, no machine paths/renderer metadata) + the two-layer rule (canonical model byte-identical; rendered .docx/PDF deterministic only where the renderer is controlled — never raw bytes by default). It explicitly RECORDS the Chinese-court jurisdiction assumption (卷X页Y / 三性 / 证据目录 vocabulary, already committed by the handover + local-first single-lawyer posture) as a recorded assumption, NOT a silent choice; and it DEFERS A10-T3/T4/T5 (证据目录 / 举证质证表 / 质证记录 court work-product FORMS) — which the handover leaves at epic level, not ticket depth — behind an explicit downstream product/legal form-field-spec STOP-point, NOT designing their field layouts here (non-scope: do not invent product/legal export behavior). Preserve all A0.7/A1/A3/A8 invariants (esp. INV-A3-1..A3-10 citation/anchor-from-DocumentPage, the A3-EXPORT-00 total+bijective degradation, A8 manifest/seal anti-circularity, evidence-genie invariant 9 canonical-not-raw-bytes).
Source of truth: docs/reference/evidence-genie-m0-developer-handover.md (TRACKED; A10 invariant §3 + A10-T1..T7 ticket section + CanonicalExportModel/ExportPreview/ExportCitationFlag data-model + two-layer reproducibility rule + "do not hash raw bytes by default / do not let any module format citations independently"); docs/adr/ADR-evidence-a3-export-degradation.md (A3-EXPORT-00 — BUILT degradation precedence A10-T2 builds on); docs/adr/ADR-evidence-a3-anchor-link-contract.md (INV-A3-1..A3-10); docs/adr/ADR-evidence-a8-snapshot-integrity-confidentiality.md (A8.6 → A10-T6 dependency; manifest/ExportPreview tie-in); services/case-box-persistence/src/sqlite/exportCitationQueries.ts (`ExportCitationResult` A3 precursor + 卷X页Y text); native/evidence-core/lib/commands.mjs (golden-export not_implemented); .claude/rules/{evidence-genie,cc-suite,autonomy,execution-discipline,staging-hygiene,security-boundary,client-local-first}.md
Allowed files: docs/adr/ADR-evidence-a10-court-fileable-export.md, dev-memo/run/queue.md, dev-memo/run/queue.linted, dev-memo/run/queue.reviewed, dev-memo/run/queue.governed, dev-memo/run/reviews/queue-review-108.md
Forbidden files: services/** (NO source/schema/contract/export-code change), docs/contracts/** (NO contract/schema change), services/case-box-persistence/src/sqlite/{schema.ts,exportCitationQueries.ts} (NO schema/CURRENT_SCHEMA_VERSION/export-code change), native/** (NO native/Swift/PDFKit/harness code or stub), apps/** (NO renderer/electron/UI/preload/package/tarball/lockfile change), scripts/** + .claude/** + .github/** + migrations/**, AGENTS.md + CLAUDE.md + GEMINI.md, docs/adr/ADR-evidence-a07-** + docs/adr/ADR-evidence-a3-** + docs/adr/ADR-evidence-a8-** (READ-ONLY predecessors — do NOT edit), dev-memo/design/** (NO separate design artifact — single ADR), dev-memo/run/evidence/** (gitignored custody marker/ledger — never touched/staged), committed keys, dev-memo/run/reports/** + dev-memo/run/override-reason.md + dev-memo/run/autonomous-next.md (untracked run-state — never staged/edited), Evidence-Genie-M0-Developer-Handover.md + xiaolai-dev-workflow-study.md (root intake — READ-ONLY); NO CanonicalExportModel/export/render implementation or stub; NO A10-T3/T4/T5 form-field design; NO A8/A1-T6/A3-T10 implementation; NO A0.7 custody/marker/key; NO weakening of A0.7/A1/A3/A8 invariants; NO push/merge/branch-delete
Gates: broker /cc-suite:review-plan REQUIRED before authoring the ADR — confirm: A10 is correctly the last un-built hard-invariant gate; the CORE (T1/T2/T6/T7 + CanonicalExportModel transcription + reproducibility + A8.6 feed) is scopable NOW from the ticket-depth handover spec without inventing it; deferring A10-T3/T4/T5 court FORMS behind a product/legal form-spec stop-point is correct (NOT designing their fields here); recording the Chinese-court jurisdiction assumption (not silently choosing) is correct; no source/schema/native change in THIS lane. STOP-points: (1) if review-plan finds A10 is undefined/contradictory/not-ticket-depth in the handover, STOP; (2) if review-plan finds A10 CORE cannot be scoped without deciding product ownership / court-format jurisdiction / cloud / auth / China-filing / PIPL / local-first reconciliation (i.e. the jurisdiction assumption is NOT safely recordable), STOP; (3) if the design would weaken A0.7/A1/A3/A8 invariants, STOP; (4) if review-plan requires splitting into multiple design WIs, STOP; (5) if a separate UI design artifact is required, STOP; (6) if any implementation/source/schema/package/native file must change, STOP. If review-plan NEEDS-FIX on wording/scope, fix-forward; if a blocker, STOP. After authoring: `scripts/workflow/check-contract-integrity.sh` exits 0; broker /cc-suite:audit + /cc-suite:verify (no Critical/High/Medium open before commit). No A0.7 custody (docs/ADR-only).
Acceptance criteria: `docs/adr/ADR-evidence-a10-court-fileable-export.md` MUST exist and MUST contain sections that answer: what A10 is; why A10 is the next hard-invariant gate (and that A0.7 is now green, A8 decided, A1 partial, A3 built); what A10 must not do (no raw-byte hashing by default; no independent citation formatting; never drop/silently-wrong a citation); what `CanonicalExportModel` means in this repo (transcribed handover shape — exportType, citationFormatVersion, exportTemplateVersion, rows, citations, linkDegradations, flags[ExportCitationFlag], warnings, sourceObjectIds, generatedFromSnapshotId?; canonicalModelSha256 determinism rule); the difference between canonical model vs rendered PDF/.docx vs exhibit bundle vs court filing package; how citations are represented (single A10-T1 contract, from DocumentPage, 卷X页Y, citationFormatVersion + exportTemplateVersion pinned); how A3 anchors/links are represented; how unlinked/broken/needs_review/non-citable/ambiguous/replaced are represented (the ExportCitationFlag set + A3-EXPORT-00 total+bijective degradation); how deterministic ordering / stable IDs / timestamps / locale / formatting are handled (the canonicalModelSha256 rule); how export reproducibility is verified (A10-T6 golden-export; ExportPreview.canonicalModelSha256); how A10 feeds A8.6 restore-and-reproduce (A10-T6 = the canonical-model reproduce target; ExportPreview into SnapshotManifest); whether schema changes are needed LATER (yes, future ExportPreview/canonical-model persistence — none in THIS lane); whether renderer UI is needed LATER; whether native/Swift/PDFKit work is needed LATER; whether cloud/public-deployment/auth/China-filing/PIPL/local-first are blockers or explicitly out-of-scope (recorded: Chinese-court jurisdiction assumption; local-first/offline; no cloud/auth needed); the implementation WI sequence (A10-T1→T2→[T3/T4/T5 DEFERRED behind product/legal form-spec]→T6→T7, honoring the handover order); acceptance tests + verify harnesses per WI (citation-render-contract, hyperlink-degradation, export-evidence-index, export-juzheng-table, export-zhizheng-record, golden-export CI-gate, export-preview-hash); per-lane stop-points (incl. A10-T3/T4/T5 product/legal form-field decision; golden-export native harness; the canonical model must not weaken A1/A3/A8). The ADR MUST explicitly record A10=SPEC-ONLY, transcribe (NOT invent) the CanonicalExportModel, defer T3/T4/T5 forms, and record the jurisdiction assumption. NO schema/source/native/contract/test/UI file changed; `CURRENT_SCHEMA_VERSION` still equals 12; `scripts/workflow/check-contract-integrity.sh` passes (exit 0); broker review-plan returns READY + audit returns no Critical/High/Medium + verify returns ALL CLOSED; committed diff contained within Allowed files; no A0.7/A1/A3/A8 invariant weakened.
Risk flags: HIGH-RISK court-facing Evidence design lane (court-fileable export, reproducibility, citation/anchor fidelity) — but DOCS/ADR-ONLY (no code, no export impl, no schema). Load-bearing care points: (a) TRANSCRIBE the handover's CanonicalExportModel/ExportPreview/ExportCitationFlag shapes + determinism rule — do NOT invent or extend them; (b) DEFER A10-T3/T4/T5 court FORMS behind an explicit product/legal form-field-spec stop-point (handover leaves them epic-level) — do NOT design their fields (non-scope: no inventing legal export behavior); (c) RECORD the Chinese-court jurisdiction assumption explicitly (handover already commits 卷X页Y/三性/证据目录) — surface it, do not silently choose, and note multi-jurisdiction = separate future decision; (d) preserve INV-A3-1..A3-10 + A1 citation-from-DocumentPage + A3-EXPORT-00 total+bijective degradation + A8 anti-circularity + canonical-not-raw-bytes (invariant 9); (e) give A8.6 a precise A10-T6 target without implementing it; (f) A10 implementation gated behind the native golden-export harness + (for A8.6 closure) A1-T6/A3-T10. If the CORE cannot be scoped without a court-format/jurisdiction/product decision, STOP (stop-point 2). No push/merge/branch-delete.
Depends on: none
Commit boundary: governance (mark-reviewed + govern) run STANDALONE before the commit (never bundled with git commit). ONE design commit — docs/adr/ADR-evidence-a10-court-fileable-export.md + governance artifacts (queue.md/.linted/.reviewed/.governed + reviews/queue-review-108.md), staged by exact path; NO source/schema/native/contract/test/UI/package file; NO marker/evidence/key/report file; no push; no merge; no branch delete.
