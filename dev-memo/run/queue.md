## WI-RELEASE-G2-OCR-PIPELINE-VERIFY-00: define + govern the M0 gate-2 OCR pipeline verification (run the per-package OCR test sweep + integration confirmation as evidence) (Type: EVIDENCE, release-governance MEDIUM risk)

## Review packet (compact)
- Active plan summary: gate 2 of the M0 go-live readiness matrix (`docs/release/go-live-readiness-report.md` §1 row 2) is **PARTIAL (verify)** — evidence "`services/ocr-*` packages present + prior sweeps"; the outstanding item is a "Per-package test sweep + integration confirmation". This WI **governs** (does NOT execute) a FUTURE lane that runs that verification as a **test-only / read-only** exercise (runs the existing per-package OCR tests + inspects the pipeline surfaces) and records the transcript as evidence. THIS authoring lane produces the governed queue WI only; it IMPLEMENTS NO OCR change, changes NO source/test/config, clears NO gate, and decides NO user go-live hard-stop.
- Grounding note (verified 2026-07-07 — the exec lane MUST re-verify against the real tree before relying, per `AGENTS.md` §"Authoring discipline"): the OCR pipeline is FOUR production packages — `services/ocr-worker`, `services/ocr-persistence`, `services/ocr-ingestion`, `services/ocr-review` (the test commands are listed in `AGENTS.md` §"Test commands"). A fifth package `services/ocr-worker-bakeoff` is an **engine bakeoff harness for ADR-11A.1 that stays OUTSIDE the production dependency graph** (its README: "`services/{ocr-worker, ocr-persistence, ocr-ingestion, ocr-review}` and `docs/contracts` MUST NOT depend on this package") — so it is **NOT part of the v1 gate-2 pipeline verification** (the exec lane notes it as out-of-scope, an experiment/comparison harness). `ocr-persistence` runs a `pretest` `better-sqlite3` ABI smoke check (`scripts/abi-smoke.mjs`) so a stale native binding fails clearly. Some OCR-engine / transport-matrix tests are environment-gated (real-PaddleOCR model presence; SSRF/TLS transport matrix) and may legitimately be skipped — the exec lane MUST honestly explain each skip, never silently count a skip as a pass.
- Exact target files (FUTURE exec lane): a NEW `docs/release/gate2-ocr-pipeline-verify-00.md` (the verification procedure + transcript + result/evidence record) + the gate-2 evidence ROW of `docs/release/go-live-readiness-report.md` (update to reference the result; **gate 2 MUST remain `PARTIAL` in this WI** — add only a `[Δ] per-package OCR test sweep + integration PASS` (or FAIL) marker using the report's EXISTING vocabulary; a `PARTIAL → CLEARED` move belongs to a SEPARATE holistic readiness-refresh WI, NOT this one — and MUST NOT clear gates 5/6/9/12/14/15/18/19/20 or imply any cleared) + `dev-memo/run/reviews/` (the exec lane's review artifact). NO other file. The exec lane RUNS existing tests + inspects; it writes NO OCR product code and adds NO dependency (any dist rebuild by the test commands is gitignored — no tracked change).
- Exact acceptance criteria: below.
- Exact out-of-scope: NO OCR IMPLEMENTATION change; NO product source/test/package/config change; NO dependency change; NO schema/migration; NO contract change; NO real user documents (fixtures only); NO Forms work; NO gate-6 full-project audit; NO clearing of gates 5/6/9/12/14/15/18/19/20 (or implying any cleared); NO gate-7 D-G7 risk acceptance; NO release-doc implementation beyond the verification evidence doc + the gate-2 row; NO signing/notarization/公开 distribution decision (gate 4); NO 律师法 conclusion (gate 11); NO license/legal/business decision (gate 17); NO final GO/NO-GO (gate 21); NO readiness refresh; NO source/test/package/config/schema/contract change in EITHER lane (docs-only tracked output).
- Requirements the FUTURE verification doc must define + prove (what the exec lane's verification must specify + record) — at minimum:
  1. **Exact OCR packages / files to verify**: the four production packages `services/ocr-worker`, `services/ocr-persistence`, `services/ocr-ingestion`, `services/ocr-review` (state the package paths); explicitly note `services/ocr-worker-bakeoff` is OUT of scope (ADR-11A.1 bakeoff harness, outside the production dependency graph).
  2. **Worker / queue / persistence / ingestion / review surfaces to inspect**: the coordinator/lifecycle (`ocr-worker` coordinator owns lifecycle), the queue dedupe + `OcrQueueError` code stability (`dedupe_conflict`/`unknown_receipt`/`stale_receipt`/`lease_expired`/`invalid_claim` — do NOT collapse/rename), the persistence source-of-truth split, ingestion, and review surfaces. Confirm the `AGENTS.md` §"Critical invariants" hold (coordinator owns lifecycle; queue dedupe key = job_id + canonical submission JSON; no FK from queue rows to ocr_jobs; error-code stability) — via the tests, not by editing them. For EACH cited invariant, the exec lane MUST distinguish "directly asserted by test <file/case>" from "inspected in source/surface + supported by suite <name>" (do not overstate what a passing suite proves); an invariant only indirectly covered is recorded as such, not claimed as directly asserted.
  3. **Package test commands to run**: `npm --prefix services/ocr-persistence test`, `npm --prefix services/ocr-worker test`, `npm --prefix services/ocr-ingestion test`, `npm --prefix services/ocr-review test` (the `AGENTS.md` commands). Record the pass/fail/skip counts per package. `ocr-persistence` runs the `abi-smoke` pretest — record its result.
  4. **Integration / smoke checks (if already available)**: run any existing cross-package integration or smoke test the repo already ships (e.g. ingestion→worker→persistence→review round-trip, if present); if none exists as a single harness, record that the per-package suites + the shared contract build ARE the integration evidence, and note the absence of a dedicated end-to-end harness as a residual — do NOT fabricate one.
  5. **How to handle known engine skips / environment-dependent OCR behavior**: honestly enumerate each skipped test (e.g. real-PaddleOCR model-gated engine tests; SSRF/TLS transport-matrix tests gated on environment) with WHY it is skipped and whether the skip is expected/acceptable for v1 (deterministic, offline). A skip is NEVER counted as a pass; a skip whose absence would hide a real regression is flagged.
  6. **What counts as deterministic pass/fail**: PASS = all four production-package test suites exit 0 (green) with the ABI smoke passing, the `AGENTS.md` critical invariants exercised, and every skip honestly explained + acceptable for v1. FAIL = any suite red, the ABI smoke failing, a critical invariant broken, or a skip that masks a real regression. Record the exact runner-reported counts (tests/pass/fail/skip) where the runner emits them; otherwise record the command exit code PLUS the runner's reported summary verbatim. The `docs/contracts` build is NOT a separate required gate — it is already invoked inside the OCR packages' own `test` scripts (e.g. `ocr-worker`/`ocr-review` build the contract before testing); treat it as a test-command prerequisite, not an extra suite to score.
  7. **Expected evidence artifact**: the doc records the exact test commands, the per-package results (tests/pass/fail/skip counts), the abi-smoke result, the integration evidence, the honest skip list, and the environment (OS, node, better-sqlite3 version, date) — captured as text (NO real user documents; fixtures only).
  8. **Read-only / test-only / hybrid mode**: state the mode. Default recommendation (non-binding): **test-only** — run the existing per-package suites + inspect the surfaces; write NO OCR product code, add NO dependency, edit NO test. Any `dist/` rebuild by a test command is gitignored (no tracked change) and is NOT a source change.
  9. **How the result feeds gate 2 WITHOUT clearing unrelated gates**: a PASS supplies the "per-package test sweep + integration confirmation" evidence. **Gate 2 MUST remain `PARTIAL` in this WI** — the exec lane adds ONLY a `[Δ] per-package OCR test sweep + integration PASS` marker (report's existing vocabulary; roll-up bucket unchanged) and MUST NOT make a `PARTIAL → CLEARED` move (that belongs to a SEPARATE holistic readiness-refresh WI). It MUST NOT clear gates 5/6/9/12/14/15/18/19/20 or imply any cleared; go-live independence is kept.
  10. **Residual risks + follow-up WIs (if any)**: e.g. the absence of a single end-to-end OCR harness (if confirmed) is a residual; the real-PaddleOCR engine path is model-gated + not exercised offline (a runtime-with-models follow-up); the SSRF/TLS transport-matrix relationship to gate 9 (supporting context, not re-signed here).
- Dependencies mapped: **gate 5** (all-package-test sweep — the prior ocr-worker SIGINT-flake resolution is prior evidence; CITED as supporting context, NOT re-cleared here); **gate 9** (ocr-worker fetcher SSRF/TLS/DNS-pinning sign-off — supporting context for the transport surface, NOT re-signed/cleared here); **gate 6** (full-project audit — an OCR regression would surface there; LATER, NOT cleared); **gates 12/14/15/18/19/20** (adjacent evidence gates — NOT cleared). These are noted as related, NOT cleared by this WI.
- Essential refs: `AGENTS.md` §"Test commands" (the four OCR package test commands) + §"Critical invariants" (coordinator owns lifecycle; queue dedupe key; stable `OcrQueueError` codes; no FK queue→ocr_jobs) + §"Repo brief" (OCR services); `services/ocr-worker` + `services/ocr-persistence` + `services/ocr-ingestion` + `services/ocr-review` (the pipeline — READ-ONLY, the ground truth) + `services/ocr-worker-bakeoff/README.md` (out-of-scope bakeoff); `docs/release/go-live-readiness-report.md` §1 gate 2 (PARTIAL) + related gates 5/9/6/12/14/15/18/19/20; `.claude/rules/security-boundary.md` (OcrQueueError code stability; ocr-worker fetcher) + §"Go-live independence"; `AGENTS.md` §"Source hierarchy" + §"Authoring discipline".
- Review questions: (1) Does the WI correctly scope a TEST-ONLY / read-only OCR pipeline VERIFICATION (run existing tests + inspect + transcript evidence), NOT an OCR implementation change and NOT an execution in this authoring lane? (2) Does it enumerate the four production OCR packages + their test commands AND correctly exclude `ocr-worker-bakeoff` (ADR-11A.1, outside the production graph)? (3) Does it require honest handling of known engine/transport skips (a skip is never a pass) + the abi-smoke + the `AGENTS.md` critical invariants? (4) Does it keep gates 5/6/9/12/14/15/18/19/20 uncleared + not imply any cleared, and cite gates 5/9 as supporting context only? (5) Does it keep gate 2 `PARTIAL` (existing vocabulary), decide no STOP-AND-ASK (4/11/17/21) hard-stop, and make no GO/NO-GO? (6) Is any OCR implementation change, error-code rename, dependency change, product source/test change, Forms work, or readiness refresh smuggled in?

Type: EVIDENCE
Scope: Govern (do NOT execute) a FUTURE lane that runs the M0 gate-2 OCR pipeline verification. The FUTURE execution lane produces `docs/release/gate2-ocr-pipeline-verify-00.md` — defining requirements 1–10 above (exact OCR packages/files; worker/queue/persistence/ingestion/review surfaces; the four package test commands; integration/smoke checks; honest handling of known engine/transport skips; deterministic pass/fail; evidence artifact; test-only/read-only mode; how it feeds gate 2 without clearing others; residual risks) — then runs the existing per-package OCR test sweep + inspects the surfaces (test-only; NO OCR product code written, NO dependency added, NO test edited), records the transcript, and updates ONLY the gate-2 evidence row of `docs/release/go-live-readiness-report.md`, where **gate 2 MUST remain `PARTIAL`** (add only a `[Δ] per-package OCR test sweep + integration PASS` marker; a `PARTIAL → CLEARED` move belongs to a SEPARATE holistic readiness-refresh WI). It excludes `ocr-worker-bakeoff` (ADR-11A.1, outside the production graph), handles known skips honestly (never counting a skip as a pass), exercises the `AGENTS.md` critical invariants (coordinator owns lifecycle; queue dedupe key; stable `OcrQueueError` codes) via the tests, and cites gates 5/9 as supporting context WITHOUT re-clearing them. It PRESERVES every still-non-cleared gate + the STOP-AND-ASK gates (4/11/17/21); it makes NO final GO/NO-GO, resolves NO STOP-AND-ASK, runs NO gate-6 audit, IMPLEMENTS NO OCR change, renames NO error code, adds NO dependency, edits NO brief/ADR/test, runs NO readiness refresh, does NO Forms work, and touches NO real user documents. THIS authoring lane implements + runs + decides NOTHING — it only governs the gate-2 verification WI. If review asks to implement/alter OCR, rename an error code, change a dependency, clear a dependent gate, run gate 6, run a readiness refresh, or make a GO/NO-GO, REJECT and report.
Source of truth: AGENTS.md §"Test commands" (the four OCR package test commands) + §"Critical invariants" (coordinator owns lifecycle; queue dedupe key = job_id + canonical submission JSON; stable OcrQueueError codes dedupe_conflict/unknown_receipt/stale_receipt/lease_expired/invalid_claim — do NOT collapse/rename; no FK queue→ocr_jobs) + §"Repo brief"; services/ocr-worker + services/ocr-persistence + services/ocr-ingestion + services/ocr-review (the production pipeline — READ-ONLY ground truth) + services/ocr-worker-bakeoff/README.md (out-of-scope, ADR-11A.1 bakeoff outside the production graph); docs/release/go-live-readiness-report.md §1 gate 2 (PARTIAL) + gates 5/9/6/12/14/15/18/19/20; .claude/rules/security-boundary.md (OcrQueueError code stability + ocr-worker fetcher) + §"Go-live independence"; AGENTS.md §"Source hierarchy" + §"Authoring discipline"; .claude/rules/autonomy.md + staging-hygiene.md + loc-guardian.md
Allowed files: docs/release/gate2-ocr-pipeline-verify-00.md (the NEW verification evidence doc the FUTURE lane produces) + docs/release/go-live-readiness-report.md (the gate-2 evidence ROW ONLY — evidence-row TEXT ONLY, NO roll-up / status-bucket edit (gate 2 stays PARTIAL, its roll-up bucket unchanged)) + dev-memo/run/reviews/ (the exec lane's own review artifact). NOTE: THIS authoring lane commits ONLY the queue governance (queue.md/linted/reviewed/governed) + reviews/queue-review-156.md — NO verification doc, NO test run, NO report change. The FUTURE exec lane RUNS the existing tests (any dist rebuild is gitignored) and records its evidence in the COMMIT MESSAGE + review artifact; it does NOT re-stage this lane's queue governance and does NOT edit any OCR source/test.
Forbidden files: docs/release/ OTHER than the NEW gate2-ocr-pipeline-verify-00.md + the gate-2 row of go-live-readiness-report.md (the other audit/assessment/policy/drill/procedure/cert/verify reports are READ-ONLY evidence — NOT edited), docs/adr/ + docs/product/ (READ-ONLY — NO brief/ADR edit), docs/contracts/ (READ-ONLY — the OCR contracts + OcrQueueError codes are run, NOT edited/renamed), apps/ + services/ (NO product source/test/renderer/IPC/OCR change — the OCR packages are RUN + inspected, NOT edited; NO OCR implementation change; NO error-code rename), services/*/src/sqlite/schema.ts (CURRENT_SCHEMA_VERSION stays 12), migrations/, native/, package.json/package-lock.json of any package (NO dependency change), dev-memo/run/intake/ (raw samples — never staged), dev-memo/ OTHER than run/reviews/ (no other plan/design edit), .claude/ + .github/; NO OCR IMPLEMENTATION change; NO test edit; NO verification RUN in this authoring lane; NO Forms work; NO gate-6 full-project audit; NO readiness refresh; NO clearing of gates 5/6/9/12/14/15/18/19/20; NO signing/notarization/public-distribution decision (gate 4); NO 律师法 conclusion (gate 11); NO license/legal/business decision (gate 17); NO gate-7 D-G7 risk acceptance; NO final GO/NO-GO decision (gate 21); NO real user documents. Push is NEVER automatic (autonomy hard-stop): local commit governance is the default; any branch push + PR open happens ONLY under explicit per-invocation user authorization; NO merge; NO branch delete of shared branches
Gates: EVIDENCE/governance lane (MEDIUM release-governance risk — governs an OCR pipeline verification WI; no code, no test run, no OCR impl, no go-live decision). (1) THIS authoring lane: broker /cc-suite:review-plan on this queue WI, Path 1, retrievable, then govern. (2) The FUTURE exec lane: RUN the four production-package OCR test suites + inspect the surfaces, produce the verification doc + the gate-2 row update, then broker /cc-suite:review-plan on the produced artifacts. (3) scripts/workflow/check-contract-integrity.sh exit 0; scripts/workflow/check-queue.sh PASS (both lanes). Forbidden-scope scan before commit: CURRENT_SCHEMA_VERSION EQUALS 12; no product source/test/package/schema/contract change; no OCR impl; no error-code rename; no test edit; no dependency change; no gate-6 run; no readiness refresh; no gate 5/6/9/12/14/15/18/19/20 clear; no GO/NO-GO; no raw sample. STOP-points: review asks to implement/alter OCR, rename an error code, edit a test, change a dependency, clear a dependent gate, run gate 6, run a readiness refresh, or make a GO/NO-GO → REJECT/STOP and report; any forbidden file would change → STOP.
Acceptance criteria: check-queue and check-contract-integrity PASS and CURRENT_SCHEMA_VERSION EQUALS 12 and the diff CONTAINS only the queue governance (queue.md/linted/reviewed/governed) + reviews/queue-review-156.md and no docs/source/test/package/schema/contract change appears and no OCR impl / no error-code rename / no test edit / no dependency change / no readiness refresh appears. The governed WI must DEFINE the ten verification requirements (OCR packages/files; surfaces; the four test commands; integration/smoke; honest skip handling; deterministic pass/fail; evidence artifact; test-only mode; feeds-gate-2-without-clearing-others; residual risks), correctly EXCLUDE ocr-worker-bakeoff, keep gates 5/6/9/12/14/15/18/19/20 uncleared + not imply any cleared, keep gate 2 `PARTIAL`, and MUST NOT decide a STOP-AND-ASK hard-stop or assert go-live. review-plan (this lane) must be RUN and must PASS (READY, or READY with only Low-risk clarifications applied).
Risk flags: MEDIUM (release-governance — a verification WI that could over-claim readiness, drift into changing OCR, rename an error code, or clear a dependent gate). Care points: (a) NO TEST RUN in this authoring lane — this lane only governs the WI; (b) TEST-ONLY / READ-ONLY — the OCR packages are RUN + inspected, NEVER edited; NO OCR implementation change, NO test edit, NO error-code rename (a code rename is a security-boundary + contract hard-stop); (c) BAKEOFF EXCLUDED — ocr-worker-bakeoff is ADR-11A.1, outside the production graph, NOT part of the v1 gate-2 verification; (d) SKIPS ARE NOT PASSES — known engine/transport skips are honestly explained; a skip masking a regression is a FAIL; (e) NO DEPENDENCY CHANGE — a dependency change is an autonomy hard-stop; a gitignored dist rebuild by a test command is not a source change; (f) DEPENDENT GATES UNCLEARED — gates 5/6/9/12/14/15/18/19/20 stay uncleared; gates 5/9 are supporting context only; (g) GO-LIVE INDEPENDENCE — an OCR verification does NOT imply go-live; the final verdict + the STOP-AND-ASK hard-stops (4/11/17/21) remain the user's; (h) NO READINESS REFRESH / NO FORMS WORK / NO REAL USER DOCUMENTS. If any cannot hold, STOP and report.
Depends on: none
Commit boundary: TWO separate lanes. (1) THIS authoring lane commits ONLY the queue governance (queue.md/linted/reviewed/governed) + reviews/queue-review-156.md — no verification doc, no test run, no report change, no implementation, no decision. (2) The FUTURE execution lane commits ONLY the new docs/release/gate2-ocr-pipeline-verify-00.md + the gate-2 evidence-row update to go-live-readiness-report.md (— evidence-row TEXT ONLY, NO roll-up / status-bucket edit (gate 2 stays PARTIAL, its roll-up bucket unchanged)) + its own review artifact under dev-memo/run/reviews/, records its evidence (incl. the per-package test transcript + skip list) in the COMMIT MESSAGE, and does NOT re-stage this lane's queue governance. The exec lane RUNS existing tests (any dist rebuild is gitignored) and edits NO OCR source/test. NO product source/test/package/schema/contract file in either lane; NO OCR implementation change; NO error-code rename; NO dependency change; NO staging of dev-memo/run/intake/ or untracked residue; push is NEVER automatic (autonomy hard-stop) — local commit governance is the default, any branch push + PR open happens ONLY under explicit per-invocation user authorization; no merge; no branch delete of shared branches. THIS authoring lane implements NOTHING, runs NO tests, and decides NOTHING — it only governs the gate-2 OCR pipeline verification WI.

## Acceptance criteria (FUTURE execution lane — what the governed verification doc must satisfy)

These describe the deliverable the FUTURE exec lane must produce; they are NOT preconditions of THIS authoring lane. The authoring-lane acceptance is the structured `Acceptance criteria:` field above (queue governance + review READY only — the verification doc does NOT exist yet and MUST NOT be created in this lane).

1. A NEW `docs/release/gate2-ocr-pipeline-verify-00.md` exists defining requirements 1–10 above and recording the per-package test transcript + result.
2. The verification ran the four production-package OCR test commands (`ocr-persistence`/`ocr-worker`/`ocr-ingestion`/`ocr-review`), recorded the tests/pass/fail/skip counts + the abi-smoke result, exercised the `AGENTS.md` critical invariants, and EXCLUDED `ocr-worker-bakeoff` (ADR-11A.1, outside the production graph). No OCR source/test change; no error-code rename.
3. The evidence records the exact test commands, per-package results, the honest skip list (each skip explained; never counted as a pass), the integration evidence, the environment, and an explicit PASS/FAIL — fixtures only, no real user documents.
4. The gate-2 ROW references the verification; gate 2 **MUST remain `PARTIAL`** — add ONLY a `[Δ] per-package OCR test sweep + integration PASS` marker (roll-up bucket unchanged); any `PARTIAL → CLEARED` move belongs to a SEPARATE holistic readiness-refresh WI. MUST NOT clear gates 5/6/9/12/14/15/18/19/20 or imply any cleared. Roll-up-line semantics kept consistent.
5. The doc cites gates 5/9 as supporting context (not re-cleared), defers gate 6 (later), and keeps go-live independence (no sign-off; gates 4/11/17/21 remain user STOP-AND-ASK).
6. NO product source/test/package/config/schema/contract change in either lane; NO OCR implementation change; NO error-code rename; NO dependency change; `CURRENT_SCHEMA_VERSION` stays 12; docs-only tracked output (dist rebuilds by test commands are gitignored).
7. `scripts/workflow/check-queue.sh` PASS + `scripts/workflow/check-contract-integrity.sh` PASS on both lanes.
