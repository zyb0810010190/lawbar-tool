## WI-DPE2: Contract layer — docket proposal edit (DOCKET_ENTRY_REVISED + revised_at + edit invariant)
Type: SOURCE
Scope: Implement the contract layer authorized by docs/adr/docket-proposal-edit.md (WI-DPE1, accepted+merged). HIGH-RISK contract/audit-chain/security-boundary change (a new audit-event kind) — broker review-plan BEFORE implementation, broker audit + verify after; tests-first within the WI. NO persistence op, NO IPC, NO edit UI. Five additive contract changes + DPE1-L1 closeout: (1) add audit kind DOCKET_ENTRY_REVISED to CASE_BOX_AUDIT_EVENT_KINDS (src/audit-log.ts) as {action:"update", entity_type:"docket_entry", reasonRequired:false} AND to the case-box-audit-event.schema.json event_kind enum; it auto-participates in v2 canonicalization because canonicalAuditEventHashInput gates v2 kinds via hasOwnProperty(CASE_BOX_AUDIT_EVENT_KINDS, kind) (audit-log.ts:196) — NO canonicalization/verify-chain code change (hard stop if one appears needed). (2) add OPTIONAL nullable revised_at (type ["string","null"], format date-time) to case-box-docket-entry.schema.json properties — NOT in required (schema has 27 required props + additionalProperties:false; optional ⇒ existing entries stay valid); no lifted column, no migration. (3) add assertValidDocketEntryEdit(prior, revised) to src/docket-invariants.ts (+ a DocketEntryEditError class) and export both via src/index.ts: (a) if prior.confirmation_state!=="proposed" throw IllegalTransitionError(prior.confirmation_state, "proposed", "edit") — ADR §2 "illegal_transition", mirrors confirm/dismiss terminal-state rejection (review-plan H2 resolution: the error type is IllegalTransitionError, NOT DocketEntryEditError, for the non-proposed case); (b) define EDITABLE={proposed_kind, proposed_due_at, proposed_due_at_kind, proposed_due_at_timezone, proposed_owner_user_id, reminder_offsets, revised_at}, and for EVERY key in union(prior,revised) NOT in EDITABLE require node:util.isDeepStrictEqual(prior[key],revised[key]) (review-plan Low: structural deep-equal, NOT JSON-string compare, so nested reminder_offsets arrays compare type-sensitively) so all provenance/identity/lifecycle/confirmation+dismissal fields are immutable by construction (enumeration-free; robust to future schema fields); a key present on only one side outside EDITABLE is a DocketEntryEditError violation; (c) enforce schema-validity of the revised entry by calling validateDocketEntry(revised) and throwing DocketEntryEditError on !ok (review-plan H1 resolution: ADR §4 says the helper enforces "the revised entry is schema-valid" — the helper owns BOTH the edit-delta rules AND revised-schema-validity, not delta-only). date_only-confirm blocking stays in assertValidDocketEntryConfirmation; datetime-needs-timezone stays in schema D7 + assertValidIanaTimezone. NOTE (review-plan Medium): this helper is a content-integrity invariant, NOT an authority-boundary sanitizer — revised_at is in EDITABLE so the helper does not stop a caller-supplied timestamp; DPE3/DPE4 MUST derive revised_at server-side and ignore/strip any client-supplied revised_at (recorded here so DPE4 owns it). (4) add the single renderer label DOCKET_ENTRY_REVISED:"Docket proposal revised" to apps/lawbar-desktop/renderer/screens/auditEventLabels.ts EVENT_KIND_LABELS (ADR §8 — keeps the already-registered renderer-audit-labels.test.mjs sync test green atomically with the kind). (5) regenerate types via npm run gen:types (expect a VCS delta in ONLY src/generated/case-box-audit-event.ts and src/generated/case-box-docket-entry.ts; hard stop if any other generated file diffs). Plus close DPE1-L1: flip docs/adr/docket-proposal-edit.md Status line to review-plan-READY/downstream-only, and set the DPE1-L1 row in dev-memo/deferred-audit-findings.md status open→closed citing this WI. Renderer label-sync verification: the renderer imports case-box-contract from a gitignored packed tarball, so refresh it (build contract → pack:internal → npm install in apps), run npm --prefix apps/lawbar-desktop test to prove the sync test green, then git restore apps/lawbar-desktop/package-lock.json (the install refreshes the tracked lockfile's tarball integrity hash, which is out of DPE2 scope; the local gitignored tarball's hash is ephemeral) so the committed DPE2 diff equals exactly the declared allowed files. If the renderer test cannot go green without a tracked package-lock.json change, STOP and report. Tests added to EXISTING registered files only (no package.json change): assertValidDocketEntryEdit in invariants.test.mjs; new-kind canonicalization in audit-event-kind-v2.test.mjs; schema accept/reject in contract.test.mjs/validators.test.mjs; export check in exports.test.mjs; v1 golden + v2 canonical-hash pins MUST stay unchanged.
Source of truth: docs/adr/docket-proposal-edit.md (the accepted ADR — §3 editable fields, §4 immutable fields + assertValidDocketEntryEdit semantics, §5 DOCKET_ENTRY_REVISED audit kind, §6 revised_at, §7 no-migration, §8 renderer-label-in-DPE2); docs/contracts/case-box-contract/src/audit-log.ts CASE_BOX_AUDIT_EVENT_KINDS + canonicalAuditEventHashInput (the hasOwnProperty kind gate at :196); docs/contracts/case-box-contract/schemas/case-box-audit-event.schema.json event_kind enum (DOCKET kinds at :85) + dependentRequired pair; docs/contracts/case-box-contract/schemas/case-box-docket-entry.schema.json (27 required props, additionalProperties:false, datetime→timezone conditional D7); docs/contracts/case-box-contract/src/docket-invariants.ts (existing assertValidNewDocketEntry / assertValidDocketEntryConfirmation patterns + error classes); apps/lawbar-desktop/renderer/screens/auditEventLabels.ts + tests/renderer-audit-labels.test.mjs (sync test asserts EVENT_KIND_LABELS keys == CASE_BOX_AUDIT_EVENT_KINDS keys); dev-memo/plan-batch-casebox-docket-proposal-edit-00.md (proposal)
Allowed files: docs/adr/docket-proposal-edit.md, dev-memo/deferred-audit-findings.md, docs/contracts/case-box-contract/src/audit-log.ts, docs/contracts/case-box-contract/schemas/case-box-audit-event.schema.json, docs/contracts/case-box-contract/schemas/case-box-docket-entry.schema.json, docs/contracts/case-box-contract/src/generated/case-box-audit-event.ts, docs/contracts/case-box-contract/src/generated/case-box-docket-entry.ts, docs/contracts/case-box-contract/src/docket-invariants.ts, docs/contracts/case-box-contract/src/index.ts, docs/contracts/case-box-contract/tests/invariants.test.mjs, docs/contracts/case-box-contract/tests/audit-event-kind-v2.test.mjs, docs/contracts/case-box-contract/tests/contract.test.mjs, docs/contracts/case-box-contract/tests/validators.test.mjs, docs/contracts/case-box-contract/tests/exports.test.mjs, apps/lawbar-desktop/renderer/screens/auditEventLabels.ts
Forbidden files: services/**, docs/contracts/case-box-contract/src/audit-log canonicalization rewrite, apps/lawbar-desktop/package.json, apps/lawbar-desktop/package-lock.json (committed change), apps/lawbar-desktop/renderer/screens/viewMatterDocketProposals.ts, apps/lawbar-desktop/src/caseBox/dto/**, apps/lawbar-desktop/electron/**, dev-memo/run/**
Gates: broker review-plan via runner (HIGH-RISK contract/audit-chain change — required pre-impl, not self-review); npm run gen:types (only the 2 expected generated deltas); npm --prefix docs/contracts/case-box-contract test; npm --prefix apps/lawbar-desktop test (renderer label-sync, via tarball-refresh + package-lock restore); loc-guardian scan; broker audit + verify on the impl scope
Acceptance criteria: DOCKET_ENTRY_REVISED present in CASE_BOX_AUDIT_EVENT_KINDS (update/docket_entry/reasonRequired:false) AND the audit-event schema enum, and a v2 event with it canonicalizes + hashes deterministically with NO change to any existing kind's hash (v1 golden + v2 canonical pins unchanged); revised_at is additive/optional/nullable date-time, existing docket entries still validate; assertValidDocketEntryEdit + DocketEntryEditError exist, are exported, and are tested — accepts a content-only edit of each editable field (incl. date_only→datetime upgrade + revised_at set), rejects an edit to each of the 20 immutable provenance/identity/lifecycle/confirmation fields (enumeration-free, via isDeepStrictEqual), rejects a non-proposed prior with IllegalTransitionError, rejects an out-of-EDITABLE key add/remove, and rejects a schema-invalid revised entry via validateDocketEntry (DocketEntryEditError); gen:types produced a VCS delta in ONLY the 2 expected generated files; the renderer EVENT_KIND_LABELS gains exactly the one DOCKET_ENTRY_REVISED label and renderer-audit-labels.test.mjs is green; ADR Status line updated + DPE1-L1 closed in the backlog; the committed DPE2 diff touches ONLY the declared allowed files (package-lock.json restored); NO persistence/IPC/edit-UI code added
Risk flags: HIGH-RISK — new audit-event kind on the audit-chain/security boundary (cc-suite review-plan REQUIRED via broker per cc-suite §High-risk + security-boundary.md; full review-plan→tests-first→audit→verify loop). Cross-package coupling: the renderer label sync requires a tarball refresh whose npm install touches the TRACKED package-lock.json integrity hash — mitigated by restoring the lockfile post-verification (out-of-scope tracked file kept at committed state). gen:types regenerates ALL schemas — only the 2 changed generated files may diff. Provenance immutability (§4) is load-bearing: any provenance field becoming editable is a hard stop; the enumeration-free immutable check is the guard.
Depends on: none (WI-DPE1 ADR already merged at 50e9e10)
Commit boundary: one local commit for the WI-DPE2 contract change (+ ADR status + backlog close), staged by exact path
