## WI-A3-INTERNAL-DEPS-PACKAGING-ADR-00: decide stable desktop internal-dependency packaging (Type: PLAN)
Type: PLAN
Scope: DESIGN/DOC-ONLY. Author ONE ADR (docs/adr/ADR-evidence-desktop-internal-deps-packaging.md) that diagnoses + decides the fix for the desktop internal-dependency packaging defect the D1 round-trip surfaced, and RECORD the defect in dev-memo/deferred-audit-findings.md (open; ≥High; broker may rate Critical). Authorizes no implementation. The defect, precisely scoped + evidenced: (1) `apps/lawbar-desktop` depends on `case-box-contract` + `case-box-persistence` via `file:dist-tarballs/*.tgz`; (2) `dist-tarballs/` (tarballs + `manifest.json`) is GITIGNORED + regenerated by `npm run bootstrap` (`pack:internal` → `npm pack`); (3) the committed `apps/lawbar-desktop/package-lock.json` PINS the tarball sha512 integrity (per the tarball-PoC plan §7.3 "the lockfile is committed"); (4) `npm pack` is NOT byte-reproducible (mtimes), so every repack yields a DIFFERENT integrity; (5) the committed lock currently pins `sha512-XFMRa8bcJyFTrPECy5AF7Hk2i23yYC8WD…` (an OLD case-box-contract tarball WITHOUT the `LINK_CREATED`/`LINK_UNLINKED`/`LINK_RELINKED` audit kinds) while the current correct tarball (regenerated from the LINK_*-bearing source) hashes to `sha512-9O4iETKXoqfOhzMZpJH5LCig/eGCtrz2jkgxvUVYFiMIRDejPtRX8vQ4Phk…`. Consequence: `npm ci` from the committed lock fails `EINTEGRITY`; a plain `npm install` silently keeps stale cached (=0) content; so a clean/CI packaged build bundles a `case-box-contract` MISSING the LINK_* kinds → `createLink`/`unlinkLink`/`relinkLink` IPC fail at runtime ("unknown audit event kind LINK_CREATED") → the merged A3 link surface (incl. the shipped renderer UI) is broken in clean packaged builds, AND `WI-A3-LINK-D1-ROUNDTRIP-T1` cannot close D1. The SOURCE contract is correct (LINK_* present); the defect is the committed-lock-vs-regenerated-tarball integrity drift, structural to the gitignored-tarball + committed-integrity-lock design. The ADR MUST: compare the 5 options (1 commit internal tarballs [un-gitignore + pin tracked bytes]; 2 make pack byte-reproducible + refresh lock; 3 npm workspaces [no tarballs]; 4 drop integrity-pinning for the file: deps; 5 status quo [reject]) with pros/cons incl. the prior architecture decision (`dev-memo/plan-desktop-package-architecture-00.md` rev-3 READY chose Option-2 tarball-install — workspaces would reopen it); RECOMMEND ONE; define the EXACT next implementation WI (name, scope, allowed files, verification commands) incl. a clean-build acceptance (fresh `rm -rf node_modules && npm ci` from a clean checkout succeeds; the desktop-resolved `case-box-contract` contains all 3 LINK_* kinds; `npm run dist` bundles that contract; no stale local node_modules can mask the result) + a DRIFT GUARD (a gate that fails when the committed dep state no longer carries the current source's audit kinds); and define how it unblocks `WI-A3-LINK-D1-ROUNDTRIP-T1`. Include a `## Review packet (compact)` section (this ADR governs eventual packaging/dependency work; review-plan REQUIRED). NO implementation, NO package-lock/tarball/package.json edit, NO source change.
Source of truth: apps/lawbar-desktop/scripts/pack-internal-packages.mjs (the pack flow — `npm pack`, non-reproducible; writes `manifest.json` integrity); apps/lawbar-desktop/dist-tarballs/manifest.json (READ — current integrity `9O4iETK…`, gitignored); apps/lawbar-desktop/package-lock.json (READ — pins stale `XFMRa8b…`); apps/lawbar-desktop/.gitignore (`dist-tarballs/` ignored); apps/lawbar-desktop/README.md §"Fresh-clone bootstrap" + "What this DOES carry"; apps/lawbar-desktop/package.json (`bootstrap`/`pack:internal` scripts; `file:dist-tarballs/*.tgz` deps); dev-memo/plan-desktop-pkg-arch-tarball-poc-01.md (§5.2 gitignore, §7.3 committed-lock churn); dev-memo/plan-desktop-package-architecture-00.md (rev-3 READY — the Option-2 tarball-install decision); the D1 round-trip stop reports (this conversation — the EINTEGRITY evidence); docs/contracts/case-box-contract/dist/audit-log.js (source has the 3 LINK_* kinds); .claude/rules/{cc-suite,autonomy,execution-discipline,security-boundary,staging-hygiene,evidence-genie}.md; dev-memo/deferred-audit-findings.md (LINK-IPC-T1-D1)
Allowed files: docs/adr/ADR-evidence-desktop-internal-deps-packaging.md, dev-memo/deferred-audit-findings.md, dev-memo/run/queue.md, dev-memo/run/queue.linted, dev-memo/run/queue.reviewed, dev-memo/run/queue.governed, dev-memo/run/reviews/queue-review-101.md
Forbidden files: apps/lawbar-desktop/package-lock.json (NO edit — this lane DECIDES, a future WI fixes), apps/lawbar-desktop/dist-tarballs/** (NO edit/commit — gitignored), apps/lawbar-desktop/package.json (NO edit), apps/lawbar-desktop/.gitignore (NO edit — the future WI may un-ignore), apps/lawbar-desktop/scripts/** (NO edit), apps/lawbar-desktop/src/** + electron/** + renderer/** + tests/** (NO change; NO D1 hook/test), services/** + docs/contracts/** source + schemas (NO source/schema change — source is correct), services/case-box-persistence/src/sqlite/schema.ts (NO schema/`CURRENT_SCHEMA_VERSION` change), docs/adr/*.md EXCEPT the new ADR-evidence-desktop-internal-deps-packaging.md (NO other ADR edit), dev-memo/design/** (ADR-class; no design-artifact edit), native/**, .claude/**, .github/**, AGENTS.md, CLAUDE.md, GEMINI.md, scripts/workflow/**, migrations/**, committed dev-memo/run/evidence/** marker/ledger/key/evidence-run files, committed keys, dev-memo/run/reports/** (untracked run-state — never staged), dev-memo/run/override-reason.md (untracked residue — never staged), Evidence-Genie-M0-Developer-Handover.md + xiaolai-dev-workflow-study.md (root intake — READ-ONLY); NO packaging implementation, NO lock/tarball/package.json/gitignore edit, NO D1 hook/test, NO source/schema/contract change, NO marking D1 closed, NO push/merge/branch-delete
Gates: broker /cc-suite:review-plan REQUIRED before the design commit (the ADR governs eventual packaging/dependency work) — it MUST confirm: the defect diagnosis is accurate (committed lock pins stale tarball integrity; npm pack non-reproducible; clean build bundles stale contract; EINTEGRITY evidence); the 5 options are fairly compared incl. the prior Option-2 tarball-install decision; the recommended path is sound + defines a clean-build acceptance + a drift guard; the next-WI scope is concrete; D1 is NOT marked closed. STOP-points: if review-plan says this must SPLIT into a defect-recording lane vs an architecture-decision lane (stop-point 1); if it says a proof-only command is needed before choosing (stop-point 2); if any implementation file must be edited to make the ADR coherent (stop-point 3) — in each, STOP and report. If review-plan returns NEEDS-FIX, fix-forward (doc edits) and re-review. After authoring: broker /cc-suite:audit + /cc-suite:verify on the diff (no Critical/High/Medium open before commit); scripts/workflow/check-contract-integrity.sh exits 0. No A0.7 custody (DOC-only; no geometry/marker/product code).
Acceptance criteria: docs/adr/ADR-evidence-desktop-internal-deps-packaging.md EXISTS and (a) states the precise defect (committed lock `XFMRa8b…` vs current tarball `9O4iETK…`; npm-pack non-reproducibility; EINTEGRITY on clean install; clean packaged build bundles a contract missing LINK_* → A3 link IPC broken) with evidence; (b) compares Option 1/2/3/4/5 with pros/cons; (c) has an explicit single decision line naming the chosen path; (d) defines the next impl WI (name + scope + allowed files + verification commands) with a clean-build acceptance (fresh `npm ci` succeeds; resolved contract has all 3 LINK_* kinds; `npm run dist` bundles it; no stale node_modules masks it) and a drift GUARD; (e) states how it unblocks WI-A3-LINK-D1-ROUNDTRIP-T1; (f) has a `## Review packet (compact)` section; dev-memo/deferred-audit-findings.md records the packaging defect (open, ≥High) with the EINTEGRITY evidence + impact, and LINK-IPC-T1-D1 stays `open`; scripts/workflow/check-contract-integrity.sh exits 0; CURRENT_SCHEMA_VERSION remains 12; no apps/services/contract/schema/lock/tarball/package.json file changed; broker review-plan READY + audit no Critical/High/Medium + verify ALL CLOSED; committed diff confined to Allowed files.
Risk flags: DESIGN/DOC-ONLY ADR, HIGH-RISK-ADJACENT (it governs a future packaging/dependency change that commits binaries and/or rewrites the lockfile + reopens a prior architecture decision). Load-bearing care points: (a) the diagnosis MUST be evidence-grounded (the EINTEGRITY hashes, the gitignore, the §7.3 committed-lock design) — no hand-waving; (b) the recommendation MUST include a clean-build acceptance + a drift guard so the stale-contract bug cannot silently recur; (c) respect the prior Option-2 tarball-install decision (don't casually recommend a full workspace rewrite without weighing that it reopens settled architecture); (d) D1 stays OPEN — this ADR only charts the unblock; (e) NO implementation, NO lock/tarball/package.json/gitignore/source edit. If review-plan demands a defect/architecture split (stop-point 1) or a proof command (stop-point 2) or an implementation edit (stop-point 3), STOP. No push/merge/branch-delete.
Depends on: none
Commit boundary: governance (mark-reviewed + govern) run STANDALONE before the commit (never bundled with git commit). ONE design commit — the new ADR + the deferred-audit-findings.md packaging-defect record + governance artifacts (queue.md/.linted/.reviewed/.governed + reviews/queue-review-101.md), staged by exact path; NO lock/tarball/package.json/gitignore/source/schema/contract file; NO marker/evidence/key/report file; no push; no merge.
