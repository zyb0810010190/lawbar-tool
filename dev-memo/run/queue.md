## WI-802: Tenant-scoped fact transition IPC (review / accept / reject)
Type: IMPL
Scope: add a tenant-scoped write IPC channel CHANNEL.factTransition ("casebox:fact:transition") + transitionFactHandler in factHandlers.ts that moves a fact through the already-implemented lifecycle via persistence.transitionFact (the contract status enum candidate|reviewed|accepted|rejected and the persistence transitionFact + getFact already exist — DO NOT rewrite the contract or persistence). Renderer-supplied DTO is narrow: { matterId, factId, to, rejection_reason? } where to in reviewed|accepted|rejected and rejection_reason is required + non-empty when to === "rejected" AND MUST be absent otherwise (rejection_reason supplied when to !== "rejected" -> invalid_payload; not silently dropped). The handler captures activeTenantId, the active actor user id, and the at timestamp ONCE per invocation and reuses them for getMatter / getFact / transitionFact (no repeated getter/clock calls). The server INJECTS reviewer_actor_user_id (active actor) + at (server clock); the renderer MUST NOT supply reviewer_actor_user_id, at, status, reviewed_at, accepted_at, rejected_at, supersedes_fact_id, source_type, id, tenant_id, actor_user_id (forbidden-field + unknown-field guards reject them -> invalid_payload). CRITICAL fail-closed scoping: persistence.transitionFact(factId, opts) is UNSCOPED (takes only factId, no tenant/matter), so the handler MUST first verify the fact belongs to the requested matter AND active tenant via a scoped getFact({tenant_id, matter_id, fact_id}) preflight and reject a null (unknown / wrong-matter / wrong-tenant) with invalid_payload BEFORE calling transitionFact — exactly the WI-601 docket-confirm preflight pattern — otherwise a guessed foreign factId could be transitioned cross-matter/cross-tenant. Flow: shape-guard -> forbidden-field -> unknown-field -> field validation (matterId, factId, to enum, rejection_reason-required-when-rejected) -> getMatter existence + active-tenant (unknown_matter / tenant_mismatch) -> scoped getFact preflight (invalid_payload on miss) -> persistence.transitionFact(factId, { to, reviewer_actor_user_id, at, ...(to==="rejected" ? { rejection_reason } : {}) }) -> project the returned fact through a dedicated renderer-safe allowlist (same non-authority fields as CREATE_FACT_RESPONSE_FIELDS, stripping tenant_id/actor_user_id/reviewer_actor_user_id) -> { ok:true, value }. The domain transition rules (which from-states allow which to) stay in persistence; the handler MUST NOT re-implement them and surfaces illegal_transition unchanged via mapThrownError. Register the channel in caseBoxHandlers.ts and wire it like the other write channels. Do NOT touch any renderer path; do NOT add the tests to the 1534-LOC tests/ipc-handlers.unit.test.mjs (use a NEW dedicated file).
Source of truth: dev-memo/plan-batch-casebox-fact-review-00.md (proposal); services/case-box-persistence/src/types.ts (transitionFact(factId, FactTransitionOpts{to, reviewer_actor_user_id, at, rejection_reason?, supersedes_fact_id?}) + getFact(GetFactQuery{tenant_id, matter_id, fact_id})); docs/contracts/case-box-contract/schemas/case-box-fact.schema.json (status enum + lifecycle fields); apps/lawbar-desktop/src/caseBox/docketHandlers.ts (confirmDocketEntry scoped fail-closed preflight pattern); apps/lawbar-desktop/src/caseBox/factHandlers.ts (createFactHandler write/projection pattern)
Allowed files: apps/lawbar-desktop/src/caseBox/factHandlers.ts, apps/lawbar-desktop/src/caseBox/handlerShared.ts, apps/lawbar-desktop/src/caseBox/dto.ts, apps/lawbar-desktop/electron/ipc/caseBoxHandlers.ts, apps/lawbar-desktop/tests/ipc-fact-handlers.unit.test.mjs, apps/lawbar-desktop/tests/dto-contract.test.mjs, apps/lawbar-desktop/package.json, dev-memo/deferred-audit-findings.md
Forbidden files: apps/lawbar-desktop/renderer/**, services/case-box-persistence/**, docs/contracts/**, apps/lawbar-desktop/src/caseBox/errors.ts, apps/lawbar-desktop/electron/main.ts, apps/lawbar-desktop/tests/ipc-handlers.unit.test.mjs
Gates: npm --prefix apps/lawbar-desktop test
Acceptance criteria: a new fact-transition IPC handler performs the VALID domain transitions and returns ok with the projected fact (server-authority fields stripped, status reflecting the transition): candidate -> reviewed, candidate -> rejected, reviewed -> accepted, reviewed -> rejected; the ILLEGAL move candidate -> accepted is NOT special-cased by the handler and surfaces illegal_transition from persistence (the contract bans auto-accept: a candidate cannot go straight to accepted; persistence conformance enforces this); rejection requires a non-empty rejection_reason (missing/empty when to===rejected -> invalid_payload) and rejection_reason supplied when to!==rejected -> invalid_payload; an unknown matter returns unknown_matter; a wrong-tenant matter returns tenant_mismatch; a factId that does not belong to the matter+active-tenant returns invalid_payload via the scoped getFact preflight WITHOUT calling transitionFact (fail-closed, proven by a spy asserting transitionFact NOT called); an illegal domain transition surfaces illegal_transition (propagated from persistence, not re-implemented); forbidden renderer-supplied fields (reviewer_actor_user_id / at / status / reviewed_at / accepted_at / rejected_at / supersedes_fact_id / tenant_id / actor_user_id) -> invalid_payload; an unknown field -> invalid_payload; the projected response excludes tenant_id / actor_user_id / reviewer_actor_user_id; the new tests live in a NEW apps/lawbar-desktop/tests/ipc-fact-handlers.unit.test.mjs (NOT ipc-handlers.unit.test.mjs) and are wired into the package.json "test" script and exercised by the gate; no renderer / services/case-box-persistence/src / contract-schema / errors.ts file is modified; the full apps/lawbar-desktop test suite passes
Risk flags: security-boundary (tenant-scoped write over IPC; fail-closed scoped getFact preflight closing a cross-matter/cross-tenant transition bypass; allowlisted renderer-safe projection; server-injected reviewer/timestamp); domain transition rules preserved in persistence (illegal_transition surfaced, not re-implemented); broker review-plan + audit + verify required; no renderer/UI path touched (review UI is the separate design-gated WI-804); pre-existing critical LOC finding tests/ipc-handlers.unit.test.mjs = 1534 LOC is NOT touched by this WI (recorded in dev-memo/deferred-audit-findings.md as a separate follow-up if review/audit requires)
Depends on: none
Commit boundary: one local commit for the fact-transition IPC channel + handler + DTO + new dedicated IPC test file + package.json wiring

## WI-804: Fact review UI — bridge + matter-view controls + status rendering
Type: UI
Design artifact: dev-memo/design/2026-06-05-casebox-fact-review.md
Scope: wire the renderer IPC bridge for the already-merged casebox:fact:transition channel (WI-802) and add per-fact Review / Accept / Reject controls + review-state rendering to the Facts disclosure of the matter view (renderer/screens/viewMatterFacts.ts), per the committed design artifact. Bridge: preload invoke + transitionFact on CaseBoxClient/CaseBoxApi + createCaseBoxApi wiring with a RENDERER_TRANSITION_FACT_DTO_FIELDS strip-allowlist in renderer/types.ts (parity-checked by renderer-dto-sync). The renderer forwards ONLY { matterId, factId, to, rejection_reason? } and MUST NOT forward reviewer actor / timestamps / status / reviewed_at / accepted_at / rejected_at / supersedes_fact_id / tenant_id / actor_user_id (the server injects reviewer_actor_user_id + at and computes lifecycle fields). Per-row controls offer ONLY the legal edges for the current status: candidate -> Review (to reviewed) + Reject (to rejected); reviewed -> Accept (to accepted) + Reject (to rejected); accepted / rejected -> no transition controls (terminal). There is NO candidate -> accepted shortcut (the contract/ADR bans auto-accept; a server illegal_transition is surfaced inline). Reject reveals a required rejection_reason input -> the DTO carries to:"rejected" + the non-empty reason; an empty reason renders an inline role="alert" and does NOT call the channel; rejection_reason is NEVER sent for Review/Accept. Render per-fact status (candidate/reviewed/accepted/rejected) with a visible text label + timestamp (+ rejection_reason for rejected) and a data-status attribute — status is NOT color-only. On click: disable the row controls + show "...ing" in a try/catch/finally (finally re-enables; catch shows a generic inline alert); on ok refresh the facts list in place (reuse the WI-701 listContainer + loadGen/isCurrent stale-load guard); on !ok inline role="alert" with the server's safe message and no refresh. Safe DOM only (el()/setText, no innerHTML); layout-only view-facts-review-* CSS using design tokens. Do NOT change any backend handler / DTO contract / persistence; do NOT touch the deadline UI.
Source of truth: dev-memo/design/2026-06-05-casebox-fact-review.md; apps/lawbar-desktop/src/caseBox/dto.ts (TransitionFactDto + TRANSITION_FACT_DTO_FIELDS + TRANSITION_FACT_RESPONSE_FIELDS — already shipped WI-802); apps/lawbar-desktop/renderer/screens/viewMatterFacts.ts (Facts disclosure + WI-701 add control + listContainer/loadGen pattern to extend); apps/lawbar-desktop/renderer/types.ts (RENDERER_*_DTO_FIELDS pattern); docs/adr/case-box-step-2-fact-promotion-and-provenance.md (no auto-accept)
Allowed files: apps/lawbar-desktop/electron/preload.mts, apps/lawbar-desktop/renderer/api.ts, apps/lawbar-desktop/renderer/types.ts, apps/lawbar-desktop/renderer/screens/viewMatterFacts.ts, apps/lawbar-desktop/renderer/index.css, apps/lawbar-desktop/tests/renderer-fact-review.test.mjs, apps/lawbar-desktop/tests/renderer-api.test.mjs, apps/lawbar-desktop/tests/renderer-dto-sync.test.mjs, apps/lawbar-desktop/package.json
Forbidden files: apps/lawbar-desktop/renderer/screens/viewMatterDeadlines.ts, apps/lawbar-desktop/tests/renderer-view-matter.test.mjs, apps/lawbar-desktop/tests/renderer-fact-write.test.mjs, apps/lawbar-desktop/tests/_view-matter-dom.mjs, apps/lawbar-desktop/src/caseBox/**, apps/lawbar-desktop/electron/ipc/caseBoxHandlers.ts, services/case-box-persistence/**, docs/contracts/**
Gates: npm --prefix apps/lawbar-desktop test
Acceptance criteria: per-fact status renders a visible text label (candidate/reviewed/accepted/rejected) + a data-status attribute (not color-only); a candidate fact shows Review + Reject controls and NO Accept; a reviewed fact shows Accept + Reject; an accepted or rejected fact shows NO transition controls; clicking Review (candidate) or Accept (reviewed) calls transitionFact with { matterId, factId, to } (no rejection_reason) and on ok refreshes the facts list in place showing the new status; clicking Reject reveals a required rejection_reason input and Confirm reject forwards { matterId, factId, to:"rejected", rejection_reason }; an empty/whitespace rejection_reason renders an inline role="alert" and does NOT call transitionFact; rejection_reason is never forwarded for Review/Accept; a backend illegal_transition / invalid_payload / unknown_matter / tenant_mismatch envelope renders an inline role="alert" with the server message and no list refresh; transitionFact forwards only the allowlisted fields (RENDERER_TRANSITION_FACT_DTO_FIELDS parity passes in renderer-dto-sync); a stale superseded facts load cannot mutate the refreshed list; the UI tests live in a NEW apps/lawbar-desktop/tests/renderer-fact-review.test.mjs wired into the package.json "test" script; apps/lawbar-desktop/tests/renderer-view-matter.test.mjs is NOT modified; no backend / src/caseBox / persistence / contract file is modified; the full apps/lawbar-desktop test suite passes. REVIEW-LOW (review-plan-mq1n7pks-secu8y): refresh-in-place clears + reloads facts from the FIRST page, so a transitioned row on a later page may reset pagination — this is ACCEPTED for this slice; the tests MUST NOT assert pagination preservation across a transition refresh.
Risk flags: UI write affordance consuming a tenant-scoped fact-transition IPC with a server fail-closed scoped preflight (renderer forwards only the 4 allowlisted fields; server injects reviewer/timestamps + computes lifecycle); legal-edges-only controls (no candidate->accepted; illegal_transition surfaced inline); design-gated (Design artifact present); broker review-plan + audit + verify required; safe-DOM + token gates apply; renderer-view-matter.test.mjs (at the loc-guardian margin post-WI-704) is Forbidden — review tests go in a new dedicated file
Depends on: WI-802
Commit boundary: one local commit for the fact-review bridge wiring + matter-view controls + status rendering + renderer/api/dto-sync tests
