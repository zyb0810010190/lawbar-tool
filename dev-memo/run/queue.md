## WI-RELEASE-G18-EXPORT-BACKUP-FORMAT-CERT-00: define + govern the M0 gate-18 data-export / backup-format certification (certify the bounded v1 export+backup surface + its fidelity evidence) (Type: EVIDENCE, release-governance MEDIUM risk)

## Review packet (compact)
- Active plan summary: gate 18 of the M0 go-live readiness matrix (`docs/release/go-live-readiness-report.md` §1 row 18) requires **data-export / backup-format certification** ("Certify export format + fidelity (overlaps gate 14)", currently `OPEN`, evidence pointer "brief §14"). This WI **governs** (does NOT execute) a FUTURE lane that authors that certification as a release-evidence doc. The v1 export/backup surface is deliberately MINIMAL per the brief; the certification enumerates it, cites the fidelity evidence that already exists, and records the post-v1 exclusions — it creates NO export functionality. THIS authoring lane produces the governed queue WI only; it runs NO export, changes NO source/test/config, creates/alters NO export functionality, clears NO gate, edits NO brief, and decides NO user go-live hard-stop.
- Bounded v1 surface (what the FUTURE certification enumerates + certifies — every item MUST be verified against the real source/test/brief before it is relied upon, per `AGENTS.md` §"Authoring discipline"): 
  1. **Backup format = backup-as-directory** (Finder / Time-Machine copy of the whole data directory) — brief §165 ("Export (v1 day-one): lawyer-driven local file copy of original files and audit log out of the data directory. No structured export entity, no PDF report, no signed bundle") + §283 ("Backup/export tooling beyond Finder + Time Machine" is post-v1). The format is the on-disk directory itself; its manifest + procedure is certified by the gate-14 runbook (`docs/release/gate14-backup-recovery-00.md`) — CITED as supporting evidence WITHOUT clearing gate 14.
  2. **The single shipped v1 structured export = the T3 证据目录及说明 (evidence catalog) DOCX export** — the only structured export in v1 (`apps/lawbar-desktop/src/caseBox/t3ExportHandlers.ts`; channels `casebox:t3:previewCatalog` / `casebox:t3:exportDocx`; forms-T3 S3 COMPLETE).
  3. **Export fidelity evidence** — the A10 CanonicalExportModel deterministic-serialization certification (`apps/lawbar-desktop/tests/a10-canonical-export.unit.test.mjs` — byte-identical golden + sha256, rebuild + input-reorder → identical hash; the A10 reproducibility layer per `.claude/rules/evidence-genie.md` invariant 9: reproducibility is measured against the CanonicalExportModel, NOT raw .docx/PDF bytes by default) + the T3 DOCX export structural/ooxml determinism tests (`tests/t3-docx-export.unit.test.mjs`) + the T3 export IPC round-trip (`tests/ipc-casebox-t3-export.unit.test.mjs`) + the T3 catalog-model tests (`tests/t3-catalog-model.unit.test.mjs`). The exec lane MUST RUN these tests green and cite the result, not merely name the files.
  4. **Post-v1 export exclusions (recorded, NOT designed here)** — brief §166 ("Export (post-v1): signed evidence bundle (format TBD, tamper-evident); PDF chronology / proof matrix / privilege log; plain-text fact dump. Each is a separate STOP-AND-ASK ADR") + §64 (NO bulk export, NO cross-matter aggregation, NO privilege-log export) + §164 (NO redacted-derivative export). These are certified as OUT of the v1 surface and DEFERRED to their own STOP-AND-ASK ADRs.
- Exact target files (FUTURE exec lane): a NEW `docs/release/gate18-export-backup-format-cert-00.md` (the certification) + the gate-18 evidence ROW of `docs/release/go-live-readiness-report.md` (update to reference the certification; status may move `OPEN` → a bounded certification status IF the evidence supports it, but MUST NOT clear gates 12/14/15, MUST NOT imply gate 7 cleared, and a v1-surface certification is NOT a go-live sign-off) + `dev-memo/run/reviews/` (the exec lane's review artifact). NO other file.
- Exact acceptance criteria: below.
- Exact out-of-scope: NO creation or alteration of ANY export functionality (the T3 export + A10 model ship as-is — READ-ONLY here); NO Forms/source/test/package/config change; NO NEW export/backup software; NO clearing of gate 12 (audit-chain), gate 14 (backup+recovery — CITED not cleared), or gate 15 (rollback); NO implication that gate 7 is cleared; NO gate-6 full-project audit; NO EDIT of `docs/product/project-requirements-brief.md`; NO license/legal/business decision (gate 17); NO signing/notarization/公开 distribution decision (gate 4); NO 律师法 conclusion (gate 11); NO final GO/NO-GO (gate 21); NO designing a post-v1 export format (those are STOP-AND-ASK ADRs); NO source/test/package/config/schema/contract change in EITHER lane (docs-only).
- Dependencies mapped: **gate 14** (backup+recovery procedure — the backup-as-directory format is certified there; CITED as supporting evidence, NOT cleared); **gate 12** (audit-chain integrity — the audit log is part of what a backup preserves + what an export must not corrupt; related, NOT cleared); **gate 15** (rollback — related recovery surface, NOT cleared); **gate 7** (robustness — the crash-recovery drill exercised the backup format; informs, does NOT clear); **gate 6** (a certification gap would be a finding for that sweep). These are noted as related, NOT cleared by this WI.
- Essential refs: `docs/product/project-requirements-brief.md` §165/§166 (v1 Finder-copy export boundary + post-v1 STOP-AND-ASK structured exports) + §64/§164 (no bulk/privilege/redacted export) + §14/§18/§20 (export/backup/archive scope); `docs/release/gate14-backup-recovery-00.md` (the backup-as-directory format + manifest — supporting evidence); `apps/lawbar-desktop/tests/a10-canonical-export.unit.test.mjs` (CanonicalExportModel byte-determinism — the fidelity layer) + `tests/t3-docx-export.unit.test.mjs` + `tests/ipc-casebox-t3-export.unit.test.mjs` + `tests/t3-catalog-model.unit.test.mjs` (T3 export fidelity); `apps/lawbar-desktop/src/caseBox/t3ExportHandlers.ts` (the shipped T3 export surface); `.claude/rules/evidence-genie.md` invariant 9 (A10 reproducibility = CanonicalExportModel, not raw bytes) + invariant 7 (OptimizedDocumentRendition never a canonical source); `docs/release/go-live-readiness-report.md` §1 gate 18 (+ related gates 12/14/15/7/6); `.claude/rules/client-local-first.md`; `AGENTS.md` §"Source hierarchy" + §"Authoring discipline" (verify every named thing); `.claude/rules/security-boundary.md` §"Go-live independence".
- Execution-wording controls (applied from review-plan `mrab8cm2` Lows): (a) the gate-18 status label MUST be concretely bounded (name the v1 surface it certifies; compatible with the report's status vocabulary; plainly NOT a clearance of gates 12/14/15/7 and NOT a go-live sign-off — never an ambiguous label a reader could read as release clearance); (b) the gate-14 runbook is cited as SUPPORTING EVIDENCE for the backup format/procedure, NOT as a certification that gate 14 is complete.
- Review questions: (1) Does the WI correctly scope a CERTIFICATION of the EXISTING bounded v1 export/backup surface (evidence doc), NOT the creation or alteration of any export functionality? (2) Is the v1 surface correctly bounded by the brief — backup-as-directory (§165) + the T3 证据目录及说明 DOCX export as the only structured export, with post-v1 structured exports (§166) recorded as STOP-AND-ASK exclusions? (3) Does it require the exec lane to VERIFY every cited source/test (run the A10 + T3 export + IPC tests green; confirm the brief §165/§166 wording; confirm the gate-14 doc) before relying on it? (4) Does it correctly frame fidelity via the A10 CanonicalExportModel reproducibility (not raw .docx bytes), per evidence-genie invariant 9? (5) Does it keep gates 12/14/15 uncleared, avoid implying gate 7 is cleared, decide no STOP-AND-ASK (4/11/17/21), make no GO/NO-GO, and edit no brief? (6) Is any Forms/source/test/schema change, new export software, post-v1 export design, or dependency smuggled in?

Type: EVIDENCE
Scope: Govern (do NOT execute) a FUTURE lane that authors the M0 gate-18 data-export / backup-format certification. The FUTURE execution lane produces `docs/release/gate18-export-backup-format-cert-00.md` — enumerating the bounded v1 export/backup surface (backup-as-directory per brief §165 + the T3 证据目录及说明 DOCX export as the only shipped structured export), citing the fidelity evidence (the A10 CanonicalExportModel byte-determinism tests + the T3 DOCX/IPC/catalog-model tests, all RUN green by the exec lane), citing the gate-14 backup/recovery runbook as supporting evidence WITHOUT clearing gate 14, and recording the post-v1 structured-export exclusions (brief §166 — signed bundle / PDF chronology / proof matrix / privilege log / plain-text fact dump = separate STOP-AND-ASK ADRs) — then updates ONLY the gate-18 evidence row of `docs/release/go-live-readiness-report.md` to reference the certification, where the status may move `OPEN` → a bounded certification status IF the evidence supports it, but MUST NOT clear gates 12/14/15, MUST NOT imply gate 7 cleared, and is NOT a go-live sign-off. It verifies every cited source/test against the real artifact before relying on it (`AGENTS.md` §"Authoring discipline"). It PRESERVES every still-non-cleared gate + the STOP-AND-ASK gates (4/11/17/21); it makes NO final GO/NO-GO, resolves NO STOP-AND-ASK, runs NO gate-6 audit, creates/alters NO export functionality, writes NO new export/backup software, adds NO dependency, edits NO brief/ADR, designs NO post-v1 export format, and changes NO product source/test/schema/contract. THIS authoring lane implements + runs + decides NOTHING — it only governs the gate-18 certification WI. If review asks to build/alter an export, design a post-v1 export format, clear gate 12/14/15, imply gate 7 cleared, edit the brief, decide a user hard-stop, or add a dependency, REJECT and report.
Source of truth: docs/product/project-requirements-brief.md §165/§166 (v1 Finder-copy export + post-v1 STOP-AND-ASK structured exports — READ-ONLY here) + §64/§164/§14/§18/§20 (export/backup scope); docs/release/gate14-backup-recovery-00.md (backup-as-directory format — supporting evidence, NOT cleared); apps/lawbar-desktop/tests/a10-canonical-export.unit.test.mjs + tests/t3-docx-export.unit.test.mjs + tests/ipc-casebox-t3-export.unit.test.mjs + tests/t3-catalog-model.unit.test.mjs (fidelity evidence — RUN green + cited by the exec lane); apps/lawbar-desktop/src/caseBox/t3ExportHandlers.ts (the shipped T3 export surface — READ-ONLY); .claude/rules/evidence-genie.md invariant 9 (A10 reproducibility = CanonicalExportModel not raw bytes) + invariant 7; docs/release/go-live-readiness-report.md §1 gate 18 (OPEN) + gates 12/14/15/7/6; .claude/rules/client-local-first.md (v1 local-first/offline posture); AGENTS.md §"Source hierarchy" + §"Authoring discipline" (verify every named thing); .claude/rules/security-boundary.md §"Go-live independence"; .claude/rules/autonomy.md + staging-hygiene.md + loc-guardian.md
Allowed files: docs/release/gate18-export-backup-format-cert-00.md (the NEW certification doc the FUTURE lane produces) + docs/release/go-live-readiness-report.md (the gate-18 evidence ROW ONLY + any narrow roll-up-consistency edit strictly required by a gate-18 status move) + dev-memo/run/reviews/ (the exec lane's own review artifact). NOTE: THIS authoring lane commits ONLY the queue governance (queue.md/linted/reviewed/governed) + reviews/queue-review-148.md — NO certification doc, NO export run, NO report change. The future exec lane records its evidence in the COMMIT MESSAGE + review artifact and does NOT re-stage this lane's queue governance.
Forbidden files: docs/release/ OTHER than the NEW gate18-export-backup-format-cert-00.md + the gate-18 row of go-live-readiness-report.md (the other audit/assessment/policy/drill/procedure reports are READ-ONLY evidence — NOT edited; the gate-14 runbook is READ-ONLY, CITED not cleared), docs/adr/ + docs/product/ (READ-ONLY — NO brief/ADR edit), docs/contracts/, apps/ + services/ (NO product source/test/renderer/IPC/persistence/export/contract change — the T3 export + A10 model + their tests are READ-ONLY; NO new export/backup software), apps/lawbar-desktop/src/caseBox/t3ExportHandlers.ts + apps/lawbar-desktop/tests/*export*/*a10* (READ-ONLY — run the tests, do not edit them), services/*/src/sqlite/schema.ts (CURRENT_SCHEMA_VERSION stays 12), migrations/, native/, package.json/package-lock.json of any package (NO new dependency), dev-memo/run/intake/ (raw samples — never staged), dev-memo/ OTHER than run/reviews/ (no other plan/design edit), .claude/ + .github/; NO export RUN that mutates data; NO gate-6 full-project audit; NO designing a post-v1 export format; NO clearing gate 12/14/15; NO implying gate 7 cleared; NO license/legal/business decision (gate 17); NO signing/notarization/public-distribution decision (gate 4); NO 律师法 conclusion (gate 11); NO final GO/NO-GO decision (gate 21). Push is NEVER automatic (autonomy hard-stop): local commit governance is the default; any branch push + PR open happens ONLY under explicit per-invocation user authorization; NO merge; NO branch delete
Gates: EVIDENCE/governance lane (MEDIUM release-governance risk — governs an export/backup-format certification WI; no code, no export build, no go-live decision). (1) THIS authoring lane: broker /cc-suite:review-plan on this queue WI, Path 1, retrievable, then govern. (2) The FUTURE exec lane: RUN the cited A10 + T3 export + IPC + catalog-model tests green, produce the certification doc + the gate-18 row update, then broker /cc-suite:review-plan on the produced artifacts. (3) scripts/workflow/check-contract-integrity.sh exit 0; scripts/workflow/check-queue.sh PASS (both lanes). Forbidden-scope scan before commit: CURRENT_SCHEMA_VERSION EQUALS 12; no product source/test/package/schema/contract change; no export functionality created/altered; no gate-6 run; no new dependency; no brief edit; no post-v1 export design; no gate 12/14/15 clear; no gate-7 clear implication; no GO/NO-GO; no raw sample. STOP-points: review asks to build/alter an export, design a post-v1 export format, clear gate 12/14/15, imply gate 7 cleared, edit the brief, decide a user hard-stop (4/11/17/21), run gate 6, or add a dependency → REJECT/STOP and report; any forbidden file would change → STOP.
Acceptance criteria: check-queue and check-contract-integrity PASS and CURRENT_SCHEMA_VERSION EQUALS 12 and the diff CONTAINS only the queue governance (queue.md/linted/reviewed/governed) + reviews/queue-review-148.md and no docs/source/test/package/schema/contract change appears and no export functionality created/altered / no new export software / no new dependency appears. The governed WI must ENUMERATE the bounded v1 export/backup surface (backup-as-directory §165 + T3 DOCX export; post-v1 §166 exclusions), REQUIRE the exec lane to VERIFY every cited source/test (run A10 + T3 export + IPC + catalog-model tests green; confirm brief §165/§166; confirm the gate-14 doc), FRAME fidelity via the A10 CanonicalExportModel reproducibility (not raw bytes; evidence-genie invariant 9), keep gates 12/14/15 uncleared + not imply gate 7 cleared, and MUST NOT decide a STOP-AND-ASK hard-stop, edit the brief, design a post-v1 export, or assert go-live. review-plan (this lane) must RETURN READY.
Risk flags: MEDIUM (release-governance — a certification WI that could over-claim readiness, silently expand the v1 export surface, clear a dependent gate, imply go-live, or drift into building/altering an export). Care points: (a) NO EXPORT BUILD/ALTER — this lane governs a CERTIFICATION of the EXISTING surface; the T3 export + A10 model + their tests are READ-ONLY; (b) BOUNDED V1 SURFACE — backup-as-directory (§165) + the T3 DOCX export ONLY; post-v1 structured exports (§166 signed bundle / PDF / proof matrix / privilege log / fact dump) are STOP-AND-ASK exclusions, NOT designed here; (c) VERIFY BEFORE RELY — the exec lane RUNS the A10 + T3 + IPC + catalog-model tests green + confirms the brief/gate-14 citations before relying (`AGENTS.md` §"Authoring discipline"); (d) FIDELITY = CanonicalExportModel — reproducibility is the A10 model byte-determinism (evidence-genie invariant 9), NOT raw .docx/PDF bytes; an OptimizedDocumentRendition is never a canonical basis (invariant 7); (e) DEPENDENT GATES UNCLEARED — gate 14 is CITED not cleared; gates 12/15 related not cleared; gate 7 not implied cleared; (f) GO-LIVE INDEPENDENCE — a v1-surface certification does NOT imply go-live; the final verdict + the STOP-AND-ASK hard-stops (4/11/17/21) remain the user's; (g) NO BRIEF EDIT / NO NEW DEPENDENCY. If any cannot hold, STOP and report.
Depends on: none
Commit boundary: TWO separate lanes. (1) THIS authoring lane commits ONLY the queue governance (queue.md/linted/reviewed/governed) + reviews/queue-review-148.md — no certification doc, no export run, no report change, no implementation, no decision. (2) The FUTURE execution lane commits ONLY the new docs/release/gate18-export-backup-format-cert-00.md + the gate-18 evidence-row update to go-live-readiness-report.md (+ any narrow roll-up-consistency edit strictly required by a gate-18 status move) + its own review artifact under dev-memo/run/reviews/, records its evidence (incl. the RUN test results) in the COMMIT MESSAGE, and does NOT re-stage this lane's queue governance. NO product source/test/package/schema/contract/export-software file in either lane; NO new dependency; NO brief edit; NO staging of dev-memo/run/intake/ or untracked residue; push is NEVER automatic (autonomy hard-stop) — local commit governance is the default, any branch push + PR open happens ONLY under explicit per-invocation user authorization; no merge; no branch delete. THIS authoring lane implements NOTHING, runs NO export, and decides NOTHING — it only governs the gate-18 export/backup-format certification WI.

## Acceptance criteria (FUTURE execution lane — what the governed certification doc must satisfy)

These describe the deliverable the FUTURE exec lane must produce; they are NOT preconditions of THIS authoring lane. The authoring-lane acceptance is the structured `Acceptance criteria:` field above (queue governance + review READY only — the certification doc does NOT exist yet and MUST NOT be created in this lane).

1. A NEW `docs/release/gate18-export-backup-format-cert-00.md` exists enumerating the bounded v1 export/backup surface: backup-as-directory (brief §165, citing the gate-14 runbook as supporting evidence) + the T3 证据目录及说明 DOCX export as the only shipped structured export.
2. The certification cites the fidelity evidence and the exec lane RAN it green: the A10 CanonicalExportModel byte-determinism (`a10-canonical-export.unit.test.mjs`) + the T3 DOCX/IPC/catalog-model tests, with the run result recorded — not merely file names. Fidelity is framed via the CanonicalExportModel reproducibility (evidence-genie invariant 9), not raw .docx bytes.
3. Post-v1 structured exports (brief §166 — signed bundle / PDF chronology / proof matrix / privilege log / plain-text fact dump; §64 no bulk/privilege; §164 no redacted) are recorded as OUT of the v1 surface and DEFERRED to their own STOP-AND-ASK ADRs.
4. The gate-18 ROW of `docs/release/go-live-readiness-report.md` references the certification; status may move `OPEN` → a bounded certification status IF the evidence supports it, but MUST NOT clear gates 12/14/15 and MUST NOT imply gate 7 cleared. **The status label MUST be concretely bounded so no reader can mistake it for release clearance** — it names the v1 surface it certifies (e.g. a `PARTIAL — v1 export/backup formats certified` framing), is compatible with the readiness report's existing status vocabulary, and states plainly it is NOT a clearance of gates 12/14/15/7 and NOT a go-live sign-off. Roll-up-line semantics kept consistent (any status move that changes a bucket is made consistent, else the row is enriched without a bucket move).
   - **Gate-14 citation language:** the certification cites the gate-14 runbook as **supporting evidence for the backup format + procedure**, NOT as a certification that gate 14 itself is complete or cleared (gate 14 stays `OPEN — procedure documented + drill-verified`, non-cleared).
5. The doc keeps go-live independence (a v1-surface certification is not a sign-off; gates 4/11/17/21 remain user STOP-AND-ASK) and does not design any post-v1 export format.
6. NO product source/test/package/config/schema/contract change in either lane; the T3 export + A10 model + their tests are READ-ONLY (run, not edited); `CURRENT_SCHEMA_VERSION` stays 12; docs-only.
7. `scripts/workflow/check-queue.sh` PASS + `scripts/workflow/check-contract-integrity.sh` PASS on both lanes.
