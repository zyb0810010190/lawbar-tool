## WI-RELEASE-G15-ROLLBACK-DRY-RUN-00: define + govern the M0 gate-15 rollback dry-run (prove the committed-rollback mechanism on a disposable target without mutating main) (Type: EVIDENCE, release-governance MEDIUM risk)

## Review packet (compact)
- Active plan summary: gate 15 of the M0 go-live readiness matrix (`docs/release/go-live-readiness-report.md` §1 row 15) is **PARTIAL** — evidence `dev-memo/rollback-00.md` + the git-revert / closeout discipline exercised repeatedly; the outstanding next action is a **dry-run rollback of a non-critical v1 artifact**. This WI **governs** (does NOT execute) a FUTURE lane that runs that rollback dry-run on a DISPOSABLE target and records the transcript as evidence. The rollback mechanism under test is the committed-rollback policy `dev-memo/rollback-00.md` §3 — `git revert`, NEVER `git reset` — proven end-to-end WITHOUT mutating `main`. THIS authoring lane produces the governed queue WI only; it runs NO rollback, changes NO source/test/config, clears NO gate, and decides NO user go-live hard-stop.
- Rollback policy grounding (the mechanism the dry-run proves — verify against the real doc before relying): `dev-memo/rollback-00.md` §1 (fix-forward default), §2 (uncommitted rollback = targeted `git restore <path>`, never `git restore .`), **§3 (committed rollback = `git revert <hash>`, NEVER `git reset --hard` — the mechanism this dry-run exercises)**, §4 (overnight auto-revert forbidden), §6 (the 7-field cc-suite rollback recording for a high-risk revert). The dry-run proves §3 works cleanly (a revert produces the exact inverse diff + restores the pre-target tree) on a disposable target, so a real rollback of a real commit would be trustworthy.
- Exact target files (FUTURE exec lane): a NEW `docs/release/gate15-rollback-dry-run-00.md` (the dry-run procedure + transcript + result/evidence record) + the gate-15 evidence ROW of `docs/release/go-live-readiness-report.md` (update to reference the dry-run result; status MAY move `PARTIAL` → a bounded status IF the evidence supports it, but MUST NOT clear gates 12/14/18 and MUST NOT imply gate 7 cleared) + `dev-memo/run/reviews/` (the exec lane's review artifact). NO other file. The disposable dry-run happens on a TEMPORARY branch that is DELETED afterward (never pushed, never merged); it creates NO tracked file beyond the evidence doc.
- Exact acceptance criteria: below.
- Exact out-of-scope: NO rollback EXECUTION in this authoring lane; NO `git reset` of any ref (the policy forbids it — the dry-run uses `git revert` / `git revert --no-commit` / a disposable temp branch ONLY); NO push or merge of the temp/dry-run branch; NO mutation of `main`; NO product source/test/package/config change; NO schema/migration; NO contract/persistence change; NO gate-6 full-project audit; NO clearing of gate 12 (audit-chain), gate 14 (backup+recovery — CITED not cleared), or gate 18 (export/backup-format — CITED not cleared); NO implication that gate 7 is cleared; NO release-doc implementation beyond the dry-run evidence doc + the gate-15 row; NO signing/notarization/公开 distribution decision (gate 4); NO 律师法 conclusion (gate 11); NO license/legal/business decision (gate 17); NO final GO/NO-GO (gate 21); NO real user data; NO source/test/package/config/schema/contract change in EITHER lane (docs-only).
- Requirements the FUTURE dry-run doc must define + prove (what the exec lane's dry-run must specify + record) — at minimum:
  1. **Exact rollback target**: a NON-CRITICAL, disposable target — the preferred pattern is a **throwaway scratch commit created on a temporary branch** (a disposable docs/evidence-shaped file), so NO real tracked artifact and NO `main` history is touched; alternatively, a recent non-critical docs/evidence commit reverted IN ISOLATION on a temp branch. NEVER a persistence/schema/contract/security commit; NEVER real user data.
  2. **Branch-isolation model**: the dry-run runs on a TEMPORARY branch cut from `main` (e.g. `dry-run/g15-rollback-<disposable>`), fully isolated; `main` is never checked out for mutation during the dry-run; the temp branch is DELETED at the end (never pushed, never merged).
  3. **Commands allowed during the dry run**: `git checkout -b <temp>`, `git revert <hash>` / `git revert --no-commit <hash>`, `git status`, `git diff`, `git log`, read-only inspection, `git checkout main` / `git switch main` (to leave the temp branch BEFORE deleting it), and `git branch -D <temp>` to clean up. FORBIDDEN during the dry-run: `git reset` (any mode), `git push`, `git merge` to main, `git clean`, any real-data operation.
  4. **Mechanism choice**: state which of `git revert` (commit the revert on the temp branch), `git revert --no-commit` (stage the inverse then inspect without committing), a temporary branch, or a patch-based simulation is used, and WHY. Default recommendation (non-binding): a temp branch + `git revert` of a disposable scratch commit, so the full committed-rollback path (`rollback-00.md` §3) is exercised, then `git revert --no-commit` optionally shown to demonstrate inspect-before-commit.
  5. **Proof that `main` is NOT mutated**: record `git rev-parse main` (and `origin/main`) BEFORE and AFTER the dry-run — identical; confirm the temp branch was deleted and never pushed; confirm no tracked file changed on `main` as a result of the dry-run. If the preferred scratch-commit pattern is used, the evidence MUST show the scratch commit was created on the TEMP branch ONLY (e.g. `git log main..<temp>` shows it; `git branch --contains <scratch-hash>` lists only the temp branch), never on `main`.
  6. **Checks to run after the rollback simulation**: the revert applied with NO conflict; the reverted tree equals the pre-target state (e.g. the scratch file is gone / the target change is inverted); `git status` clean on the temp branch after; for a docs-only target no test run is required, but if a real (non-scratch) commit is chosen the exec lane states whether any gate/test applies.
  7. **Expected evidence artifact**: the dry-run doc records the target, the temp-branch name, the exact command transcript, the before/after `main` HEAD, the revert diff summary, the post-revert checks, and the environment (OS, git version, date) — captured as text (NO real case data; disposable/fixture only).
  8. **Explicit pass/fail criteria**: PASS = (the `git revert` applied cleanly with no conflict) AND (the reverted tree matches the pre-target state) AND (`main` HEAD unchanged before/after) AND (the temp branch deleted, never pushed) AND (no `git reset` used). FAIL = any conflict that cannot be cleanly resolved by the documented procedure, OR any mutation of `main`, OR any use of a forbidden command. A FAIL is a rollback-procedure finding (surface, do not paper over).
  9. **How the result feeds gate 15 WITHOUT clearing unrelated gates**: a PASS supplies the "dry-run rollback of a non-critical artifact" evidence gate 15's row asks for. The gate-15 status label MUST use the readiness report's EXISTING status vocabulary (`OPEN` / `PARTIAL` / `CLEARED` / `CLEARED pending user go-live approval` / `STOP-AND-ASK`) — the exec lane MUST NOT invent a new readiness state. The safe move (matching the gate-14/gate-18 precedent) is to KEEP gate 15 `PARTIAL` and enrich the row with a `[Δ] rollback dry-run PASS` evidence marker (roll-up bucket unchanged, stays consistent); a `PARTIAL → CLEARED` move is NOT made here (it belongs to a holistic readiness-refresh lane). It MUST NOT clear gates 12/14/18 and MUST NOT imply gate 7 cleared; the exec lane keeps go-live independence (a rollback dry-run is not a go-live sign-off).
  10. **Residual risks + follow-up WIs (if any)**: e.g. reverting a MERGE commit or a multi-commit range needs `-m` / range handling (note if out of scope); reverting a commit that a later commit depends on can conflict (the dry-run's disposable target avoids this); a real high-risk revert additionally needs the `rollback-00.md` §6 7-field recording (noted, not exercised here).
- Dependencies mapped: **gate 14** (backup+recovery — a related recovery surface; CITED as supporting context, NOT cleared); **gate 18** (export/backup-format certification — related recovery/format context; CITED, NOT cleared); **gate 12** (audit-chain operational recovery — related, NOT cleared, separately governed); **gate 7** (robustness — the crash-recovery drill is a related exercised recovery; informs, does NOT clear; gate 7 stays user-decision-dependent D-G7-1/D-G7-2); **gate 6** (full-project audit — should remain LATER, after rollback evidence is stable; a dry-run FAIL would be a finding there). These are noted as related, NOT cleared by this WI.
- Essential refs: `dev-memo/rollback-00.md` §1/§2/§3/§4/§6 (the rollback policy — §3 committed-rollback = git revert never git reset is the mechanism under test); `docs/release/go-live-readiness-report.md` §1 gate 15 (PARTIAL) (+ related gates 12/14/18/7/6); `.claude/rules/staging-hygiene.md` §"Committed rollback — git revert, never git reset" + §"Uncommitted rollback"; `.claude/rules/autonomy.md` §"Hard-stop list" (git reset --hard / git clean / push forbidden) + §"Committed rollback restrictions"; `docs/release/gate14-backup-recovery-00.md` + `docs/release/gate18-export-backup-format-cert-00.md` (related recovery/format context — CITED, NOT cleared); `.claude/rules/client-local-first.md`; `AGENTS.md` §"Source hierarchy" + §"Authoring discipline"; `.claude/rules/security-boundary.md` §"Go-live independence".
- Review questions: (1) Does the WI correctly scope a rollback DRY-RUN (procedure + transcript evidence) on a DISPOSABLE target, NOT a rollback of a real critical artifact and NOT a rollback execution in this authoring lane? (2) Does it enforce the `rollback-00.md` §3 mechanism (`git revert`, never `git reset`) + a temporary-branch isolation model + proof that `main` is not mutated + temp-branch deletion (never pushed/merged)? (3) Are the ten dry-run requirements complete + safe (disposable target, no real data, no destructive ops, no push)? (4) Does it keep gates 12/14/18 uncleared, not imply gate 7 cleared, defer gate 6 to later, and cite gates 14/18 as supporting context only? (5) Does it keep gate 15 PARTIAL after governance, decide no STOP-AND-ASK (4/11/17/21), and make no GO/NO-GO? (6) Is any rollback execution, `git reset`, push/merge of the temp branch, product source/test/schema change, or dependency smuggled in?

Type: EVIDENCE
Scope: Govern (do NOT execute) a FUTURE lane that runs the M0 gate-15 rollback dry-run. The FUTURE execution lane produces `docs/release/gate15-rollback-dry-run-00.md` — defining requirements 1–10 above (exact disposable rollback target; temporary-branch isolation model; allowed commands; mechanism choice = `git revert` / `git revert --no-commit` / temp branch / patch simulation; proof `main` is not mutated; post-simulation checks; expected evidence artifact; explicit pass/fail; how it feeds gate 15 without clearing unrelated gates; residual risks) — then runs the dry-run on a DISPOSABLE target on a TEMPORARY branch (deleted afterward, never pushed/merged), records the transcript, and updates ONLY the gate-15 evidence row of `docs/release/go-live-readiness-report.md` to reference the result, where the status MAY move `PARTIAL` → a bounded status IF the evidence supports it, but MUST NOT clear gates 12/14/18 and MUST NOT imply gate 7 cleared. It uses the `rollback-00.md` §3 mechanism (`git revert`, NEVER `git reset`), proves `main` is unmutated (before/after HEAD identical, temp branch deleted), cites gates 14/18 as supporting context WITHOUT clearing them, and DEFERS gate 12 (separately governed) + gate 6 (later). It PRESERVES every still-non-cleared gate + the STOP-AND-ASK gates (4/11/17/21); it makes NO final GO/NO-GO, resolves NO STOP-AND-ASK, runs NO gate-6 audit, mutates NO `main`, pushes/merges NO temp branch, uses NO `git reset`, touches NO real user data, adds NO dependency, edits NO brief/ADR, and changes NO product source/test/schema/contract. THIS authoring lane implements + runs + decides NOTHING — it only governs the gate-15 rollback-dry-run WI. If review asks to run the dry-run, use `git reset`, push/merge the temp branch, mutate `main`, clear gate 12/14/18, imply gate 7 cleared, run gate 6, or add a dependency, REJECT and report.
Source of truth: dev-memo/rollback-00.md §1/§2/§3/§4/§6 (rollback policy — §3 git revert never git reset = mechanism under test); docs/release/go-live-readiness-report.md §1 gate 15 (PARTIAL) + gates 12/14/18/7/6; .claude/rules/staging-hygiene.md §"Committed rollback — git revert, never git reset" + §"Uncommitted rollback"; .claude/rules/autonomy.md §"Hard-stop list" (git reset --hard / git clean / push forbidden) + §"Committed rollback restrictions"; docs/release/gate14-backup-recovery-00.md + docs/release/gate18-export-backup-format-cert-00.md (related recovery/format context — CITED, NOT cleared); .claude/rules/client-local-first.md; AGENTS.md §"Source hierarchy" + §"Authoring discipline"; .claude/rules/security-boundary.md §"Go-live independence"; .claude/rules/loc-guardian.md
Allowed files: docs/release/gate15-rollback-dry-run-00.md (the NEW dry-run evidence doc the FUTURE lane produces) + docs/release/go-live-readiness-report.md (the gate-15 evidence ROW ONLY + any narrow roll-up-consistency edit strictly required by a gate-15 status move) + dev-memo/run/reviews/ (the exec lane's own review artifact). NOTE: THIS authoring lane commits ONLY the queue governance (queue.md/linted/reviewed/governed) + reviews/queue-review-150.md — NO dry-run doc, NO rollback run, NO report change. The FUTURE exec lane's disposable dry-run happens on a TEMPORARY branch that is deleted (never pushed/merged) and produces NO tracked file beyond the evidence doc; it records its evidence in the COMMIT MESSAGE + review artifact and does NOT re-stage this lane's queue governance.
Forbidden files: docs/release/ OTHER than the NEW gate15-rollback-dry-run-00.md + the gate-15 row of go-live-readiness-report.md (the other audit/assessment/policy/drill/procedure/cert reports are READ-ONLY evidence — NOT edited; the gate-14 runbook + gate-18 cert are READ-ONLY, CITED not cleared), docs/adr/ + docs/product/ (READ-ONLY — NO brief/ADR edit), docs/contracts/, apps/ + services/ (NO product source/test/renderer/IPC/persistence/contract change), services/*/src/sqlite/schema.ts (CURRENT_SCHEMA_VERSION stays 12), migrations/, native/, package.json/package-lock.json of any package (NO new dependency), dev-memo/run/intake/ (raw samples — never staged), dev-memo/ OTHER than run/reviews/ (no other plan/design edit; rollback-00.md is READ-ONLY), .claude/ + .github/; NO rollback RUN in this authoring lane; NO `git reset` of any ref; NO push/merge of any temp/dry-run branch; NO mutation of `main`; NO gate-6 full-project audit; NO clearing of gate 12/14/18; NO implying gate 7 cleared; NO signing/notarization/public-distribution decision (gate 4); NO 律师法 conclusion (gate 11); NO license/legal/business decision (gate 17); NO final GO/NO-GO decision (gate 21). Push is NEVER automatic (autonomy hard-stop): local commit governance is the default; any branch push + PR open happens ONLY under explicit per-invocation user authorization; NO merge; NO branch delete of shared branches
Gates: EVIDENCE/governance lane (MEDIUM release-governance risk — governs a rollback dry-run WI; no code, no rollback run, no main mutation, no go-live decision). (1) THIS authoring lane: broker /cc-suite:review-plan on this queue WI, Path 1, retrievable, then govern. (2) The FUTURE exec lane: RUN the dry-run on a disposable temp branch (deleted after; never pushed/merged; main unmutated), produce the evidence doc + the gate-15 row update, then broker /cc-suite:review-plan on the produced artifacts. (3) scripts/workflow/check-contract-integrity.sh exit 0; scripts/workflow/check-queue.sh PASS (both lanes). Forbidden-scope scan before commit: CURRENT_SCHEMA_VERSION EQUALS 12; no product source/test/package/schema/contract change; no rollback run; no git reset; no main mutation; no temp-branch push/merge; no gate-6 run; no new dependency; no gate 12/14/18 clear; no gate-7 clear implication; no GO/NO-GO; no raw sample. STOP-points: review asks to run the dry-run, use `git reset`, push/merge the temp branch, mutate `main`, clear gate 12/14/18, imply gate 7 cleared, run gate 6, or add a dependency → REJECT/STOP and report; any forbidden file would change → STOP.
Acceptance criteria: check-queue and check-contract-integrity PASS and CURRENT_SCHEMA_VERSION EQUALS 12 and the diff CONTAINS only the queue governance (queue.md/linted/reviewed/governed) + reviews/queue-review-150.md and no docs/source/test/package/schema/contract change appears and no rollback run / no git reset / no main mutation / no new dependency appears. The governed WI must DEFINE the ten dry-run requirements (disposable target; temp-branch isolation; allowed commands; mechanism = git revert not git reset; proof main unmutated; post-checks; evidence artifact; explicit pass/fail; feeds-gate-15-without-clearing-others; residual risks), ENFORCE the `rollback-00.md` §3 mechanism + temp-branch isolation + main-unmutated proof + temp-branch deletion, keep gates 12/14/18 uncleared + not imply gate 7 cleared + defer gate 6, keep gate 15 PARTIAL after governance, and MUST NOT decide a STOP-AND-ASK hard-stop or assert go-live. review-plan (this lane) must RETURN READY.
Risk flags: MEDIUM (release-governance — a rollback-dry-run WI that could mutate main, use a destructive op, over-claim readiness, or clear a dependent gate). Care points: (a) NO ROLLBACK RUN in this authoring lane — this lane only governs the WI; (b) NEVER `git reset` — the mechanism is `git revert` per `rollback-00.md` §3 (git reset --hard is an autonomy hard-stop); (c) TEMP-BRANCH ISOLATION — the dry-run runs on a disposable temp branch, DELETED after, NEVER pushed/merged; `main` is proven unmutated (before/after HEAD identical); (d) DISPOSABLE TARGET / NO REAL DATA — a throwaway scratch commit or a non-critical docs commit, never a persistence/schema/security commit, never real user data; (e) DEPENDENT GATES UNCLEARED — gates 14/18 CITED as supporting context not cleared, gate 12 deferred (separately governed), gate 7 not implied cleared, gate 6 later; (f) GO-LIVE INDEPENDENCE — a rollback dry-run does NOT imply go-live; the final verdict + the STOP-AND-ASK hard-stops (4/11/17/21) remain the user's; (g) NO PUSH/MERGE of the temp branch, NO new dependency. If any cannot hold, STOP and report.
Depends on: none
Commit boundary: TWO separate lanes. (1) THIS authoring lane commits ONLY the queue governance (queue.md/linted/reviewed/governed) + reviews/queue-review-150.md — no dry-run doc, no rollback run, no report change, no implementation, no decision. (2) The FUTURE execution lane commits ONLY the new docs/release/gate15-rollback-dry-run-00.md + the gate-15 evidence-row update to go-live-readiness-report.md (+ any narrow roll-up-consistency edit strictly required by a gate-15 status move) + its own review artifact under dev-memo/run/reviews/, records its evidence (incl. the dry-run transcript + before/after main HEAD) in the COMMIT MESSAGE, and does NOT re-stage this lane's queue governance. The disposable dry-run runs on a TEMPORARY branch DELETED after the run (never pushed, never merged, main unmutated). NO product source/test/package/schema/contract file in either lane; NO new dependency; NO git reset; NO main mutation; NO staging of dev-memo/run/intake/ or untracked residue; push is NEVER automatic (autonomy hard-stop) — local commit governance is the default, any branch push + PR open happens ONLY under explicit per-invocation user authorization; no merge; no branch delete of shared branches. THIS authoring lane implements NOTHING, runs NO rollback, and decides NOTHING — it only governs the gate-15 rollback-dry-run WI.

## Acceptance criteria (FUTURE execution lane — what the governed dry-run doc must satisfy)

These describe the deliverable the FUTURE exec lane must produce; they are NOT preconditions of THIS authoring lane. The authoring-lane acceptance is the structured `Acceptance criteria:` field above (queue governance + review READY only — the dry-run doc does NOT exist yet and MUST NOT be created in this lane).

1. A NEW `docs/release/gate15-rollback-dry-run-00.md` exists defining requirements 1–10 above and recording the dry-run transcript + result.
2. The dry-run used the `rollback-00.md` §3 mechanism (`git revert`, NEVER `git reset`) on a DISPOSABLE target on a TEMPORARY branch, and recorded before/after `main` HEAD (identical) + the temp-branch deletion as proof `main` was not mutated.
3. The evidence records the exact rollback target, temp-branch name, command transcript, revert diff summary, post-revert checks, environment, and an explicit PASS/FAIL conclusion — fixture/disposable only, no real data.
4. The gate-15 ROW of `docs/release/go-live-readiness-report.md` references the dry-run; the status label uses the report's EXISTING vocabulary (no invented state) — the safe move is to KEEP `PARTIAL` and enrich with a `[Δ] rollback dry-run PASS` marker (roll-up bucket unchanged), NOT a `PARTIAL → CLEARED` move (that belongs to a readiness-refresh lane). It MUST NOT clear gates 12/14/18 and MUST NOT imply gate 7 cleared. Roll-up-line semantics kept consistent.
5. The doc cites gates 14/18 as supporting context (not cleared), defers gate 12 (separately governed) + gate 6 (later), and keeps go-live independence (no sign-off; gates 4/11/17/21 remain user STOP-AND-ASK).
6. NO product source/test/package/config/schema/contract change in either lane; NO `git reset`; NO `main` mutation; NO temp-branch push/merge; `CURRENT_SCHEMA_VERSION` stays 12; docs-only.
7. `scripts/workflow/check-queue.sh` PASS + `scripts/workflow/check-contract-integrity.sh` PASS on both lanes.
