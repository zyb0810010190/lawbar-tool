## WI-CL1: table-drive the deadline-transition forbidden-field IPC test (close DT-BATCH72-L1)
Type: TEST
Scope: Close deferred Low DT-BATCH72-L1 by replacing the two single-field forbidden-field tests for `transitionDeadline` (`transitionDeadline forbidden field actor_user_id` and `... status`) in `apps/lawbar-desktop/tests/ipc-casebox-handlers.unit.test.mjs` with ONE table-driven test that iterates EVERY `TRANSITION_DEADLINE_FORBIDDEN_FIELDS` member — `id`, `tenant_id`, `actor_user_id`, `matter_id`, `at`, `status`, `met_at`, `previous_status` — and for each asserts the handler returns `ok:false` with `error.code === "invalid_payload"` AND a populated `error.details.schemaPath` equal to the offending field (the handler's forbiddenFieldFailure sets schemaPath to the field name). TEST-ONLY: no change to `transitionDeadlineHandler`, the DTO, or any source/handler/electron file. Keep all other transition tests unchanged. After the test passes, flip the DT-BATCH72-L1 row in `dev-memo/deferred-audit-findings.md` to `status: closed` citing the resolution commit (rows stay visible).
Source of truth: apps/lawbar-desktop/tests/ipc-casebox-handlers.unit.test.mjs (existing transition tests + makeProvider/makeDeadlineProvider harness); apps/lawbar-desktop/src/caseBox/dto/deadline.ts (TRANSITION_DEADLINE_FORBIDDEN_FIELDS — the 8-field source of truth, READ-ONLY); dev-memo/deferred-audit-findings.md (DT-BATCH72-L1 row); dev-memo/study/2026-06-07-batch-audit-72.md (the finding)
Allowed files: apps/lawbar-desktop/tests/ipc-casebox-handlers.unit.test.mjs, dev-memo/deferred-audit-findings.md
Forbidden files: apps/lawbar-desktop/src/**, apps/lawbar-desktop/electron/**, apps/lawbar-desktop/renderer/**, services/**, docs/contracts/**, .claude/**, scripts/workflow/**, dev-memo/run/**, apps/lawbar-desktop/tests/renderer-deadline-urgency.test.mjs, apps/lawbar-desktop/tests/renderer-view-matter.test.mjs, apps/lawbar-desktop/tests/renderer-dto-sync.test.mjs, apps/lawbar-desktop/tests/_view-matter-dom.mjs
Gates: npm --prefix apps/lawbar-desktop test
Acceptance criteria: one table-driven test asserts that EACH of the 8 TRANSITION_DEADLINE_FORBIDDEN_FIELDS members, when present on the payload, yields ok:false + code "invalid_payload" + details.schemaPath equal to that field; the two prior single-field forbidden tests are removed (no duplicate coverage); all other transition tests unchanged; `npm --prefix apps/lawbar-desktop test` passes; NO source/handler/electron/renderer change; the test file stays under the 1200 LOC fail threshold; DT-BATCH72-L1 marked closed in dev-memo/deferred-audit-findings.md with the resolution commit
Risk flags: TEST-ONLY cleanup; no runtime/behavior change; no renderer path (no design gate); low-risk (cc-suite per workflow, self-review fallback acceptable); the handler guard is unchanged — this only widens existing coverage
Depends on: none
Commit boundary: one local commit for the table-driven test + the DT-BATCH72-L1 closure

## WI-CL2: refresh the stale viewMatterDeadlines header comment (close DT-BATCH74-L1)
Type: UI
Design artifact: dev-memo/design/2026-06-07-deadline-transition.md
Scope: Close deferred Low DT-BATCH74-L1 with a COMMENT-ONLY change to the header block of `apps/lawbar-desktop/renderer/screens/viewMatterDeadlines.ts`: replace the stale sentence "NO edit / deadline-status transition." with an accurate description that deadline status transitions are now supported since WI-DT3 (a lawyer can mark a deadline met / missed / withdrawn, and missed → met with a required reason), while PRESERVING the existing server-authority/security note ("the renderer never imports the service; ... main is the authoritative validator ... fail-closed scoped ... preflights"). NO runtime/behavior change — comments only; no code, no DOM, no control-flow edit. This WI touches a renderer path so the path-based UI design-artifact gate fires; it is satisfied honestly by the existing WI-DT2 artifact (cited above + in the PR body). After the change, flip the DT-BATCH74-L1 row in `dev-memo/deferred-audit-findings.md` to `status: closed` citing the resolution commit.
Source of truth: apps/lawbar-desktop/renderer/screens/viewMatterDeadlines.ts (header comment lines 1-12 — the stale "NO edit / deadline-status transition" line); dev-memo/design/2026-06-07-deadline-transition.md (WI-DT2 artifact, the design the comment now describes); dev-memo/deferred-audit-findings.md (DT-BATCH74-L1 row); dev-memo/study/2026-06-07-batch-audit-74.md (the finding)
Allowed files: apps/lawbar-desktop/renderer/screens/viewMatterDeadlines.ts, dev-memo/deferred-audit-findings.md
Forbidden files: apps/lawbar-desktop/src/**, apps/lawbar-desktop/electron/**, apps/lawbar-desktop/renderer/api.ts, apps/lawbar-desktop/renderer/types.ts, apps/lawbar-desktop/renderer/screens/viewMatterDocketProposals.ts, apps/lawbar-desktop/tests/**, services/**, docs/contracts/**, .claude/**, scripts/workflow/**, dev-memo/run/**
Gates: npm --prefix apps/lawbar-desktop test
Acceptance criteria: the header comment no longer claims "NO edit / deadline-status transition" and accurately states that deadline status transitions (met/missed/withdrawn + missed→met-with-reason) are supported since WI-DT3, preserving the server-authority note; the change is COMMENT-ONLY (no code/DOM/behavior diff — git diff shows only comment-line changes); `npm --prefix apps/lawbar-desktop test` passes unchanged (511 pass); loc-guardian unaffected (pure-LOC excludes comments); the PR body cites dev-memo/design/2026-06-07-deadline-transition.md so the path-based design gate passes; DT-BATCH74-L1 marked closed in dev-memo/deferred-audit-findings.md with the resolution commit
Risk flags: COMMENT-ONLY; Type UI only because the path-based design-artifact gate is path-scoped (renderer/**), NOT because of any behavior change; design gate satisfied by the existing DT2 artifact (no new artifact invented); zero pure-LOC impact; no services/contracts/persistence/handler/electron touch; low-risk (cc-suite per workflow)
Depends on: none
Commit boundary: one local commit for the comment refresh + the DT-BATCH74-L1 closure
