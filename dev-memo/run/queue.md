## WI-ENA3: PDF load/page-count probe with synthetic fixture (third authorized implementation hard-stop)
Type: SCAFFOLD
Scope: Extend the merged native/evidence-core-swift SwiftPM package with a minimal PDF load/parse probe that loads a tiny SYNTHETIC PDF fixture via PDFDocument(url:) and reports a deterministic structural result (loaded + pageCount), with a Swift test asserting the expected page count (2). Commit one tiny synthetic/public PDF fixture under the Swift package test fixtures path. This is a PDF load / page-count probe ONLY: NO geometry, NO coordinate extraction, NO page-box (media/crop/bleed) analysis, NO drawing/rendering, NO geometry assertions, NO page-identity/citation claim, NO renderer-conformance, NO A0.7 harness logic, NO A0.7 marker, NO marker provenance/HMAC, NO EVW5 hard hooks, NO Evidence UI/product behavior. Do NOT wire the probe to the JS shim. Do NOT change the existing native/evidence-core JS shim behavior. Keep it a local Swift test / macOS CI smoke only. The fixture is synthetic/public (no client/confidential material), tiny, generated deterministically from a minimal-PDF source; a README/test comment states why it is safe. If generating the fixture requires a new dependency, STOP and ask before adding it. README/status note states: PDF load/page-count proven; no geometry yet; no A0.7; no marker; no product behavior.
Source of truth: dev-memo/plan-batch-casebox-evidence-native-a07-feasibility-00.md (ENA0 feasibility plan, §1-§3 toolchain/boundary — PDF load/page-count is the next feasibility probe after PDFKit import); docs/adr/ADR-evidence-native-core-a07-feasibility.md (ENA-00); native/evidence-core-swift/** (the merged WI-ENA1+ENA2 package this WI extends); native/evidence-core/README.md (the JS shim contract, read-only reference); .github/workflows/evidence-core-swift-smoke.yml (the existing macOS smoke job); .github/workflows/ui-design-artifact.yml (the existing ubuntu check to preserve); .claude/rules/autonomy.md (this step is a user-authorized hard-stop); .claude/rules/evidence-genie.md (A0.7 is the first real Evidence architecture gate; load/page-count is NOT geometry/citation); local Swift toolchain (Apple Swift 5.9.2, arm64-apple-macosx, available).
Allowed files: native/evidence-core-swift/** (extend Sources/**, Tests/** incl. a Tests/**/Fixtures/ synthetic PDF, README/status note; Package.swift only if a target/test change requires it), .github/workflows/evidence-core-swift-smoke.yml (ONLY if a narrow test-command update is needed; otherwise byte-unchanged), dev-memo/** (queue/governance/audit/study artifacts)
Forbidden files: apps/**, native/evidence-core/** (the JS shim — NO behavior change), services/**, docs/contracts/**, docs/product/**, .claude/hooks/**, .claude/settings.json, .claude/rules/**, AGENTS.md, CLAUDE.md, GEMINI.md, dev-memo/run/evidence/**, Evidence-Genie-M0-Developer-Handover.md (root), xiaolai-dev-workflow-study.md, package.json; NO geometry/coordinate-extraction code; NO page-box (media/crop/bleed) analysis; NO drawing/rendering; NO geometry assertions; NO renderer-conformance; NO A0.7 harness logic; NO marker / HMAC / provenance code; NO confidential/client PDFs; NO new runtime/package dependency (STOP-and-ask if the fixture needs one)
Gates: broker /cc-suite:review-plan REQUIRED before impl (HIGH-RISK: first PDF load/parse on the Evidence native-core path + a committed binary fixture — no self-review); if review-plan returns READY but scopes broader than a PDF load/page-count probe, STOP and ask; broker /cc-suite:audit + /cc-suite:verify after impl (HIGH-RISK); swift build and swift test pass for native/evidence-core-swift (Swift available locally); the probe loads the synthetic fixture via PDFDocument(url:) and reports only loaded + pageCount (no geometry/coordinates/page-boxes); a test asserts pageCount == 2; the fixture is tiny + synthetic (no confidential content) with a safety comment; the macOS CI smoke job runs the test and stays macOS-only; the existing ubuntu ui-design-artifact.yml is byte-unchanged; PDF load appears ONLY in the probe/test under native/evidence-core-swift/** (grep); no geometry/coordinate/CGRect/CGPoint/page-box assertions (grep); no renderer-conformance impl (grep); no A0.7 marker and nothing under dev-memo/run/evidence/ (grep); no HMAC/provenance (grep); the native/evidence-core JS shim is byte-unchanged; README/status note states PDF-load/page-count-proven/no-geometry/no-A0.7/no-marker/no-product-behavior; scripts/workflow/check-contract-integrity.sh exits 0; npm --prefix apps/lawbar-desktop test passes (610/610); scripts/workflow/check-gates.sh returns GATES OK
Acceptance criteria: native/evidence-core-swift/ now contains a PDF load/page-count probe (EvidenceCorePdfLoadProbe.load(url:) -> {loaded, pageCount}) plus a Swift test that loads a committed tiny synthetic PDF fixture and asserts loaded == true and pageCount == 2 on macOS; swift test --package-path native/evidence-core-swift passes (skeleton + PDFKit probe + PDF-load probe); the fixture is a tiny synthetic/public PDF (no fonts/images/text/confidential material) with a README/test safety statement; grep shows PDF load (PDFDocument(url:)) only in the probe/test under native/evidence-core-swift/** and NO geometry/coordinate/CGRect/CGPoint/page-box/renderer-conformance/marker/HMAC/provenance code and nothing under dev-memo/run/evidence/; the macOS CI workflow is either byte-unchanged or changed only by a narrow test-command line and remains macOS-only; .github/workflows/ui-design-artifact.yml is byte-unchanged; the native/evidence-core JS shim is byte-unchanged; the README/status note states PDF-load/page-count-proven/no-geometry/no-A0.7/no-marker/no-product-behavior; npm desktop test shows pass 610 fail 0; check-gates GATES OK; broker audit returns no Critical/High/Medium and verify confirms ALL CLOSED; the committed diff is confined to native/evidence-core-swift/** plus (at most) the one macOS smoke workflow file
Risk flags: HIGH-RISK -- first PDF load/parse (PDFDocument load) on the Evidence native-core path AND a committed binary PDF fixture (autonomy hard-stop, EXPLICITLY user-authorized for this narrow PDF-load/page-count-probe scope ONLY, synthetic/public fixture only). broker review-plan REQUIRED + broker audit+verify after. Scope strictly limited to load + page count; NO geometry, NO coordinates, NO page boxes, NO rendering, NO page-identity/citation, NO renderer-conformance, NO A0.7 harness, NO marker/provenance/HMAC, NO hooks/UI/product behavior; the JS shim is untouched; no new dependency. Swift toolchain confirmed available locally (5.9.2).
Depends on: none (the ENA2 PDFKit import probe is already merged to main)
Commit boundary: one local commit -- native/evidence-core-swift/** (incl. the synthetic fixture) plus (at most) the one macOS smoke workflow file, staged by exact path; no push
