## WI-RELEASE-G3-R3-POLISH-ASSESS-00: define + govern the gate-3 R3 error/empty-state-polish ASSESSMENT lane (a READ-ONLY inspection of the v1 Mac-client renderer surfaces that classifies R3 as M0-blocking [needs an implementation WI before gate 6] OR non-M0/post-v1-deferrable [documented residual] — NO UI implementation, NO gate-3 clearance) (Type: EVIDENCE, release-governance MEDIUM risk)

## Review packet (compact)
- Active plan summary: gate 3 of the M0 go-live readiness matrix (`docs/release/go-live-readiness-report.md` §1 row 3) is **PARTIAL [Δ]** — the self-declaration blocker is closed; a substantial functional case-box UI is built (12 renderer screens + 26 case-box IPC channels + T3 preview/DOCX export). Three residuals remain per `docs/release/gate3-client-release-readiness-00.md` §2: **R1** signing/notarization/public-distribution → **gate 4 STOP-AND-ASK** (user-owned); **R2** the global overdue-deadline dashboard banner → **SHIPPED** (WI-GATE3-R2-OVERDUE-DASHBOARD-BANNER-00, per the readiness-report gate-3 row); **R3** "product-grade error / empty-state / resilience hardening" bounded polish — the gate-3 doc's own §2 R3 row already characterises it as "bounded polish items … not required to make gate 3's client *functional* claim accurate; no single blocker identified … none is an M0 correctness blocker." This WI **governs** (does NOT execute) a FUTURE lane that ASSESSES R3 by read-only evidence and CLASSIFIES it: (a) M0-blocking → govern a separate implementation WI before gate 6; (b) non-M0 / post-v1-deferrable → documented residual, proceed toward gate 6; (c) inconclusive → STOP and report. THIS authoring lane produces the governed queue WI only; it changes NO UI/source/test, clears NO gate, and decides NO user go-live hard-stop.
- Grounding note (verified 2026-07-08 — the exec lane MUST re-verify against the real files before relying, per `AGENTS.md` §"Authoring discipline"): the **12 renderer screens** are `apps/lawbar-desktop/renderer/screens/{listMatters,createMatter,archiveMatter,viewMatter,viewMatterAudit,viewMatterDeadlines,viewMatterDocketProposals,viewMatterDocuments,viewMatterFacts,viewMatterLinks,viewMatterT3Catalog}.ts` (+ `auditEventLabels.ts`, a label map, not a screen). A quick `grep` shows **11 of the 12 screens already reference `empty`/`error` handling patterns** — so error/empty-state handling broadly EXISTS; R3 is about *product-grade polish/consistency*, not a missing-functionality gap. `docs/release/gate3-client-release-readiness-00.md` (43 lines) §2 is the R-item source of truth; its R3 row already says "no single blocker identified … none is an M0 correctness blocker." **R2 reconciliation:** the readiness-report gate-3 row records R2 as **SHIPPED**, while the older `gate3-client-release-readiness-00.md` §2 R2 row is the pre-ship snapshot ("separate Type: UI WI"); the assessment treats R2 as **shipped** (per the newer readiness row) and does NOT re-litigate it — this discrepancy is a NOTE, not a finding.
- Exact target files (FUTURE exec lane): a NEW `docs/release/gate3-r3-polish-assessment-00.md` (the R3 assessment: the inspected surfaces, the error/empty-state criteria, the per-screen evidence, the M0-blocking-vs-post-v1 classification, the decision outcome) + the gate-3 evidence ROW of `docs/release/go-live-readiness-report.md` (evidence-row TEXT ONLY — the R3 disposition wording; **gate 3 MUST remain `PARTIAL` in this WI**; NO roll-up/status-bucket edit; gate 3 stays PARTIAL, still gate-4-blocked; MUST NOT clear gate 3 or imply gate 4 resolved) + `dev-memo/run/reviews/` (the exec lane's review artifact). NO other file. The exec lane's inspection is READ-ONLY: it reads the renderer screens + the gate-3 docs; it EDITS NO UI/source/test/config/package.
- Exact acceptance criteria: below.
- Exact out-of-scope: NO UI/source/test/package/config change (the assessment is read-only — it inspects renderer screens, it does NOT modify them); NO R3 polish IMPLEMENTATION (that is a SEPARATE future WI if the assessment finds R3 M0-blocking); NO Forms/OCR/custody/A8 work; NO dependency change; NO schema/migration (`CURRENT_SCHEMA_VERSION` stays 12); NO contract change; NO ADR/brief edit; NO gate-3 CLEARANCE; NO re-litigation of R2 (shipped) or R1 (gate-4 user-owned); NO gate-4 signing/notarization/公开 distribution decision (gate 4); NO gate-6 full-project audit; NO broad readiness refresh; NO gate-7 D-G7 risk acceptance; NO 律师法 conclusion (gate 11); NO license/legal/business decision (gate 17); NO final GO/NO-GO (gate 21); NO staging of `dev-memo/run/intake/` or untracked residue.
- Requirements the FUTURE assessment doc must define + record (what the exec lane's assessment must specify + record) — at minimum:
  1. **Exact gate-3 source documents to inspect**: `docs/release/gate3-client-release-readiness-00.md` (§2 R-item table — the R1/R2/R3 source of truth, incl. R3's "no single blocker / none M0" characterisation) + the `docs/release/go-live-readiness-report.md` §1 gate-3 row (the R1/R2/R3 disposition + the R2-shipped record). Cite exact sections/lines + the tool version/date read.
  2. **Exact UI/client surfaces to inspect for R3 (WITHOUT changing them)**: the 12 renderer screens under `apps/lawbar-desktop/renderer/screens/` (enumerate them) + their supporting `renderer/` shell/api modules as needed. READ-ONLY. Enumerate which screens have error-state handling, which have empty-state handling, and where there is a gap.
  3. **Error-state criteria**: what "product-grade error handling" means for a local-first case-box client — e.g. an IPC failure surfaces a visible, non-crashing error message/toast (not a silent failure or an unhandled rejection); consistency of the error surface across screens. Record the criteria used.
  4. **Empty-state criteria**: what "product-grade empty-state" means — e.g. an empty matter list / no-deadlines / no-documents / no-facts / no-links screen renders a clear empty-state message (not a blank/broken surface). Record the criteria used.
  5. **What counts as M0-blocking vs post-v1 polish**: an R3 item is **M0-blocking** ONLY if its absence makes a v1 day-one case-box workflow *incorrect or broken* (e.g. a screen crashes on empty data, an IPC failure silently loses lawyer input, a core workflow is unusable) — NOT if it is cosmetic consistency (toast wording/styling parity, nicer empty-state copy, resilience niceties). Cosmetic/consistency polish is **post-v1**. The gate-3 doc's own R3 characterisation ("none is an M0 correctness blocker") is the prior; the assessment CONFIRMS or REFUTES it by evidence, and if it REFUTES (finds a genuine M0-blocker), it STOPS and recommends a separate implementation WI.
  6. **Evidence required to classify R3**: per-screen inspection notes (error-state present? empty-state present? any crash-on-empty or silent-failure gap?), citing the exact screen file + the handling pattern (or its absence). The classification must be AUDITABLE (per-screen evidence), not a bare summary claim.
  7. **How R1 remains blocked by gate 4**: record that R1 (signing/notarization/public-distribution/release-posture) is a **gate-4 STOP-AND-ASK user decision** and is NOT touched by this assessment; gate 3's release posture stays at-least-partially blocked by gate 4 regardless of the R3 outcome.
  8. **How R2 is already shipped and not re-litigated**: record that R2 (the global overdue-deadline dashboard banner) is **SHIPPED** (WI-GATE3-R2-OVERDUE-DASHBOARD-BANNER-00, per the readiness-report gate-3 row) and is NOT re-assessed here; note the pre-ship snapshot in `gate3-client-release-readiness-00.md` §2 R2 as a documentation-lag reconciliation (not a finding).
  9. **Expected assessment artifact**: the doc records the inspected sources, the enumerated surfaces + per-screen error/empty-state evidence, the M0-vs-post-v1 criteria + classification, the R1/R2 dispositions, and the environment (date) — captured as text. **Allowed readiness-report update**: the gate-3 ROW ONLY, and STRICTLY the R3 disposition wording — it MUST NOT change the gate-3 status, the roll-up/status bucket, the readiness summary, or any language implying gate 6 may run automatically; gate 3 stays PARTIAL; no gate-3 clearance; no gate-4 implication. Even a non-M0 R3 outcome does NOT auto-authorize gate 6 — gate 6 remains a SEPARATE user-directed lane.
  10. **Pass/fail / decision outcomes (one of three)**: (a) **`R3 requires implementation WI before Gate 6`** — the assessment found a genuine M0-blocking error/empty-state defect → STOP after the assessment and govern a separate implementation WI (do NOT proceed to gate 6); (b) **`R3 non-M0 / deferred with evidence`** — no M0-blocking defect; R3 is post-v1 polish with a documented residual → gate 6 may proceed (in a later user-directed lane); (c) **`assessment inconclusive — stop and report`** — the evidence does not support a confident classification → STOP and report to the user. In ALL outcomes gate 3 stays PARTIAL and at-least-partially gate-4-blocked.
- Dependencies mapped: **gate 4** (signing/distribution — R1 is gate-4 STOP-AND-ASK; gate 3 stays gate-4-blocked regardless; NOT touched); **gate 6** (full-project audit — the R3 outcome informs whether gate 6 can proceed or an R3 impl WI comes first; gate 6 NOT run here); **R2** (shipped; not re-litigated). The Forms T3/T4/T5 boundary is unrelated. These are noted as related, NOT cleared by this WI.
- Essential refs: `docs/release/gate3-client-release-readiness-00.md` §2 (R1/R2/R3 source of truth) + §3 (the R2 overdue-surfacing gap); `docs/release/go-live-readiness-report.md` §1 gate 3 (PARTIAL) + gate 4 (STOP-AND-ASK); `apps/lawbar-desktop/renderer/screens/*.ts` (the 12 screens — READ-ONLY inspection surface); `.claude/rules/client-local-first.md` (v1 posture); `.claude/rules/security-boundary.md` §"Go-live independence"; `AGENTS.md` §"Source hierarchy" + §"Authoring discipline"; `.claude/rules/autonomy.md` + `staging-hygiene.md` + `loc-guardian.md`; `UI-GATES.md` (a `Type: UI` implementation WI — if R3 is found M0-blocking — needs a design artifact; this ASSESSMENT lane is read-only EVIDENCE, not UI).
- Review questions: (1) Does the WI correctly scope a READ-ONLY R3 assessment (inspect + classify), NOT a UI implementation and NOT an execution in this authoring lane? (2) Does it define M0-blocking-vs-post-v1 correctly (a crash-on-empty / silent-IPC-failure is M0; cosmetic toast/empty-copy consistency is post-v1) and require per-screen auditable evidence? (3) Does it keep gate 3 `PARTIAL` (no clearance) + gate 4 STOP-AND-ASK (R1) + R2-shipped-not-re-litigated? (4) Does it define the three decision outcomes (M0-blocking → separate impl WI before gate 6; non-M0 → deferred; inconclusive → stop)? (5) Does it forbid any UI/source/test change, gate clearance, gate-4 decision, gate-6 run, and any go-live decision? (6) Is any UI implementation, gate clearance, or readiness refresh smuggled in?

Type: EVIDENCE
Scope: Govern (do NOT execute) a FUTURE lane that ASSESSES the gate-3 R3 error/empty-state polish. The FUTURE execution lane produces `docs/release/gate3-r3-polish-assessment-00.md` — defining requirements 1–10 above (gate-3 source docs; the 12 renderer surfaces to inspect read-only; error-state + empty-state criteria; M0-blocking-vs-post-v1 definition; per-screen evidence; R1-gate-4-blocked; R2-shipped-not-re-litigated; assessment artifact; allowed readiness-row update; the three decision outcomes) — then runs the READ-ONLY inspection (read the 12 renderer screens + the gate-3 docs), classifies R3, records the result, and updates ONLY the gate-3 evidence row of `docs/release/go-live-readiness-report.md`, where **gate 3 MUST remain `PARTIAL`** (R3 disposition wording only; NO roll-up/status-bucket edit; NO gate-3 clearance; NO gate-4 implication). It IMPLEMENTS NO UI, changes NO source/test/config/package, re-litigates NEITHER R1 (gate-4 user-owned) NOR R2 (shipped), and preserves every still-non-cleared gate + the STOP-AND-ASK gates (4/11/17/21). It makes NO final GO/NO-GO, resolves NO STOP-AND-ASK, runs NO gate-6 audit, does NO Forms/OCR work, adds NO dependency, and runs NO readiness refresh. If the assessment finds a genuine M0-blocking R3 defect it STOPS and recommends a separate implementation WI (it does NOT implement the fix). THIS authoring lane implements + runs + decides NOTHING — it only governs the R3 assessment WI. If review asks to implement UI polish, change source/test, clear gate 3, make a gate-4 decision, run gate 6, or run a readiness refresh, REJECT and report.
Source of truth: docs/release/gate3-client-release-readiness-00.md §2 (R1/R2/R3) + §3; docs/release/go-live-readiness-report.md §1 gate 3 (PARTIAL) + gate 4 (STOP-AND-ASK); apps/lawbar-desktop/renderer/screens/*.ts (the 12 screens — READ-ONLY); .claude/rules/client-local-first.md; .claude/rules/security-boundary.md §"Go-live independence"; AGENTS.md §"Source hierarchy" + §"Authoring discipline"; UI-GATES.md; .claude/rules/autonomy.md + staging-hygiene.md + loc-guardian.md
Allowed files: docs/release/gate3-r3-polish-assessment-00.md (the NEW assessment evidence doc the FUTURE lane produces) + docs/release/go-live-readiness-report.md (the gate-3 evidence ROW ONLY — evidence-row TEXT ONLY, R3 disposition wording; NO roll-up/status-bucket edit; gate 3 stays PARTIAL) + dev-memo/run/reviews/ (the exec lane's own review artifact). NOTE: THIS authoring lane commits ONLY the queue governance (queue.md/linted/reviewed/governed) + reviews/queue-review-169.md — NO assessment doc, NO report change. The FUTURE exec lane records its evidence (incl. the per-screen inspection notes) in the COMMIT MESSAGE + the assessment doc + review artifact, does NOT re-stage this lane's queue governance, and EDITS NO UI/source/test.
Forbidden files: apps/lawbar-desktop/renderer/ + src/ + electron/ (READ-ONLY — NO UI/source/test change; the assessment inspects, never edits), apps/ + services/ test/config/package (NO change), services/*/src/sqlite/schema.ts (CURRENT_SCHEMA_VERSION stays 12), migrations/, native/, package.json/package-lock.json of any package (NO dependency change), docs/product/project-requirements-brief.md (READ-ONLY — NO brief edit), docs/adr/ (READ-ONLY — NO ADR edit), docs/release/ OTHER than the NEW gate3-r3-polish-assessment-00.md + the gate-3 row of go-live-readiness-report.md (the other gate evidence docs are READ-ONLY — referenced, NOT edited; `gate3-client-release-readiness-00.md` is READ-ONLY source), docs/contracts/, dev-memo/run/intake/ (raw samples — never staged), dev-memo/ OTHER than run/reviews/ (no other plan/design edit), .claude/ + .github/; NO UI implementation; NO R3 polish fix; NO source/test/package/config change; NO ASSESSMENT RUN in this authoring lane; NO gate-3 clearance; NO R1/R2 re-litigation; NO gate-4 signing/distribution decision; NO gate-6 full-project audit; NO broad readiness refresh; NO 律师法 conclusion (gate 11); NO license/legal/business decision (gate 17); NO gate-7 D-G7 risk acceptance; NO final GO/NO-GO decision (gate 21). Push is NEVER automatic (autonomy hard-stop): local commit governance is the default; any branch push + PR open happens ONLY under explicit per-invocation user authorization; NO merge; NO branch delete of shared branches
Gates: EVIDENCE/governance lane (MEDIUM release-governance risk — governs a read-only UI-surface assessment WI; no code, no UI change, no gate clearance, no go-live decision). (1) THIS authoring lane: broker /cc-suite:review-plan on this queue WI, Path 1, retrievable, then govern. (2) The FUTURE exec lane: RUN the read-only inspection (read the 12 renderer screens + the gate-3 docs), produce the assessment doc + the gate-3 row update, then broker a POST-ARTIFACT /cc-suite:review-plan on the produced artifacts. (3) scripts/workflow/check-contract-integrity.sh exit 0; scripts/workflow/check-queue.sh PASS (both lanes). Forbidden-scope scan before commit: CURRENT_SCHEMA_VERSION EQUALS 12; no UI/source/test/package/config change; no ADR/brief edit; no gate-3 clearance; no gate-4 decision; no gate-6 run; no readiness refresh; no R1/R2 re-litigation; no GO/NO-GO; no raw sample. STOP-points: review asks to implement UI polish, change source/test, clear gate 3, make a gate-4 decision, run gate 6, or run a readiness refresh → REJECT/STOP and report; the assessment finds a genuine M0-blocking R3 defect → outcome (a): STOP after the assessment and recommend a separate implementation WI (do NOT implement); the assessment is inconclusive → outcome (c): STOP and report; any forbidden file would change → STOP.
Acceptance criteria: check-queue and check-contract-integrity PASS and CURRENT_SCHEMA_VERSION EQUALS 12 and the diff CONTAINS only the queue governance (queue.md/linted/reviewed/governed) + reviews/queue-review-169.md and no assessment-doc/UI/source/test/package/config change appears and no ADR/brief edit / no gate-3 clearance / no gate-4 decision / no readiness refresh appears. The governed WI must DEFINE the ten assessment requirements (gate-3 source docs; the 12 read-only renderer surfaces; error-state + empty-state criteria; M0-blocking-vs-post-v1 definition; per-screen evidence; R1-gate-4-blocked; R2-shipped-not-re-litigated; assessment artifact; allowed gate-3-row update; the three decision outcomes), keep gate 3 `PARTIAL`, keep gate 4 STOP-AND-ASK, forbid a UI implementation + a gate-3 clearance, and MUST NOT decide a STOP-AND-ASK hard-stop or assert go-live. review-plan (this lane) must be RUN and must PASS (READY, or READY with only Low-risk clarifications applied).
Risk flags: MEDIUM (release-governance — an assessment WI that could over-claim readiness, drift into implementing UI polish, clear gate 3, or imply gate 4 resolved). Care points: (a) NO ASSESSMENT RUN in this authoring lane — this lane only governs the WI; (b) READ-ONLY — the assessment inspects the renderer screens, it NEVER edits them; an R3 fix is a SEPARATE implementation WI (design-artifact-gated per UI-GATES.md if Type: UI); (c) M0-DISCIPLINE — an R3 item is M0-blocking ONLY if it breaks/corrupts a v1 day-one workflow (crash-on-empty, silent IPC-failure losing lawyer input); cosmetic consistency is post-v1; the gate-3 doc's "none is an M0 correctness blocker" is the prior, CONFIRMED-or-REFUTED by per-screen evidence; (d) GATE INDEPENDENCE — gate 3 stays PARTIAL + gate-4-blocked (R1); R2 shipped, not re-litigated; gate 6 not run; (e) GO-LIVE INDEPENDENCE — an R3 assessment does NOT imply go-live; the final verdict + the STOP-AND-ASK hard-stops (4/11/17/21) remain the user's; (f) THREE OUTCOMES — M0-blocking → separate impl WI before gate 6; non-M0 → deferred residual; inconclusive → stop. If any cannot hold, STOP and report.
Depends on: none
Commit boundary: TWO separate lanes. (1) THIS authoring lane commits ONLY the queue governance (queue.md/linted/reviewed/governed) + reviews/queue-review-169.md — no assessment doc, no report change, no implementation, no decision. (2) The FUTURE execution lane commits ONLY the new docs/release/gate3-r3-polish-assessment-00.md + the gate-3 evidence-row update to go-live-readiness-report.md + its own review artifact under dev-memo/run/reviews/, records its evidence (incl. the per-screen inspection notes + the classification) in the COMMIT MESSAGE, does NOT re-stage this lane's queue governance, and EDITS NO UI/source/test. NO UI/source/test/package/config file in either lane; NO R3 polish implementation; NO gate-3 clearance; NO gate-4 decision; NO dependency change; NO staging of dev-memo/run/intake/ or untracked residue; push is NEVER automatic (autonomy hard-stop) — any branch push + PR open happens ONLY under explicit per-invocation user authorization; no merge; no branch delete of shared branches. THIS authoring lane implements NOTHING, runs NO assessment, and decides NOTHING — it only governs the gate-3 R3 assessment WI.

## Acceptance criteria (FUTURE execution lane — what the governed assessment doc must satisfy)

These describe the deliverable the FUTURE exec lane must produce; they are NOT preconditions of THIS authoring lane. The authoring-lane acceptance is the structured `Acceptance criteria:` field above (queue governance + review READY only — the assessment doc does NOT exist yet and MUST NOT be created in this lane).

1. A NEW `docs/release/gate3-r3-polish-assessment-00.md` exists defining requirements 1–10 above and recording the inspection findings + the R3 classification.
2. The doc enumerates the 12 renderer screens inspected (read-only), records per-screen error-state + empty-state evidence (present / absent / gap), and cites the exact screen files — no UI edited.
3. The doc states the error-state + empty-state criteria + the M0-blocking-vs-post-v1 definition, and classifies R3 into one of the three outcomes with auditable per-screen evidence (confirming or refuting the gate-3 doc's "none is an M0 correctness blocker" prior).
4. R1 is recorded as gate-4 STOP-AND-ASK (user-owned, gate 3 stays gate-4-blocked); R2 is recorded as SHIPPED (not re-litigated; the `gate3-client-release-readiness-00.md` pre-ship snapshot noted as a documentation-lag reconciliation).
5. The exec lane CITES the exact gate-3 row (by its `| 3 | …` line) before editing, and edits ONLY that row's R3 disposition TEXT — NO adjacent roll-up/status-bucket line touched. Gate 3 **MUST remain `PARTIAL`**; MUST NOT clear gate 3, imply gate 4 resolved, or imply gate 6 may run automatically (even a non-M0 R3 outcome does not auto-authorize gate 6 — it stays a separate user-directed lane).
6. NO UI/source/test/package/config/schema/contract change; NO R3 polish implementation; NO ADR/brief edit; `CURRENT_SCHEMA_VERSION` stays 12; docs-only tracked output.
7. `scripts/workflow/check-queue.sh` PASS + `scripts/workflow/check-contract-integrity.sh` PASS on both lanes. If the outcome is (a) M0-blocking, the lane STOPS and recommends a separate implementation WI (design-artifact-gated if Type: UI) BEFORE gate 6; if (b) non-M0, the residual is documented and gate 6 may proceed in a later lane; if (c) inconclusive, STOP and report.
