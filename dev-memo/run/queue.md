## WI-RELEASE-G20-TELEMETRY-CRASH-REPORTING-VERIFY-00: define + govern the M0 gate-20 telemetry / crash-reporting verification (prove the v1 Mac client ships NO telemetry and crash reporting OFF by default) (Type: EVIDENCE, release-governance MEDIUM risk)

## Review packet (compact)
- Active plan summary: gate 20 of the M0 go-live readiness matrix (`docs/release/go-live-readiness-report.md` §1 row 20) is **PARTIAL** — evidence "brief §4 (no telemetry; crash reporting OFF default)"; the outstanding item is "Verify the Mac-client code ships no telemetry + crash-reporting disabled." This WI **governs** (does NOT execute) a FUTURE lane that runs that verification as a **read-only inspection** of the shipped client and records the transcript as evidence. The bar is the brief §4 posture (fully offline; no telemetry; crash reporting OFF by default) + `.claude/rules/evidence-genie.md` invariant 1 (macOS App Sandbox with no network entitlements — offline is OS-enforced). THIS authoring lane produces the governed queue WI only; it IMPLEMENTS NO telemetry or crash reporting, changes NO source/test/package/config, clears NO gate, and decides NO user go-live hard-stop.
- Grounding note (verified 2026-07-07 — the exec lane MUST re-verify against the real tree before relying, per `AGENTS.md` §"Authoring discipline"): a scoped scan of `apps/lawbar-desktop` found **NO** `crashReporter` / `Sentry` / `Bugsnag` / `Crashpad` / analytics / `telemetry` / metrics / `mixpanel` / `amplitude` / `posthog` / `sendBeacon` / `autoUpdater` usage in source (the only "tracking" hits are CSS letter-spacing custom properties `--tracking-mono`/`--tracking-label`/`--tracking-tight`/`--tracking-snug` in `src/theme/tokens.ts` — **NOT telemetry**; the exec lane MUST exclude these false positives). The desktop `package.json` runtime deps are `better-sqlite3`, `docx`, and the two internal contract/persistence tarballs; devDeps are `electron`, `electron-builder`, `playwright`, `@playwright/test`, `@types/node`, `typescript` — **none is a telemetry / crash-reporting / analytics library**. The exec lane confirms this reality and records it.
- Exact target files (FUTURE exec lane): a NEW `docs/release/gate20-telemetry-crash-reporting-verify-00.md` (the inspection procedure + transcript + result/evidence record) + the gate-20 evidence ROW of `docs/release/go-live-readiness-report.md` (update to reference the result; the status uses the report's EXISTING vocabulary — the safe move is to KEEP gate 20 `PARTIAL` and enrich with a `[Δ] no-telemetry + crash-reporting-off verified` marker; a `PARTIAL → CLEARED` move belongs to a SEPARATE holistic readiness-refresh WI, NOT this one — and it MUST NOT clear gates 4/6/7/11/12/14/15/18/19/21 and MUST NOT imply any of them cleared) + `dev-memo/run/reviews/` (the exec lane's review artifact). NO other file. The exec lane's inspection is READ-ONLY (grep / dependency listing); it writes NO code and adds NO dependency.
- Exact acceptance criteria: below.
- Exact out-of-scope: NO telemetry IMPLEMENTATION; NO crash-reporting IMPLEMENTATION; NO Electron `crashReporter.start()` added; NO product source/test/package/config change; NO dependency change (adding OR removing); NO schema/migration; NO contract/persistence change; NO gate-6 full-project audit; NO clearing of gates 4/6/7/11/12/14/15/18/19/21 (or implying any cleared); NO release-doc implementation beyond the verification evidence doc + the gate-20 row; NO signing/notarization/公开 distribution decision (gate 4); NO 律师法 conclusion (gate 11); NO license/legal/business decision (gate 17); NO gate-7 D-G7 risk acceptance; NO final GO/NO-GO (gate 21); NO readiness refresh; NO source/test/package/config/schema/contract change in EITHER lane (docs-only tracked output).
- Requirements the FUTURE verification doc must define + prove (what the exec lane's inspection must specify + record) — at minimum:
  1. **Exact files / packages to inspect**: `apps/lawbar-desktop/electron/{main.ts,preload.mts}` + `apps/lawbar-desktop/src/**` + `apps/lawbar-desktop/renderer/**` (the shipped client surface) + `apps/lawbar-desktop/package.json` + its lockfile **`apps/lawbar-desktop/package-lock.json`** (the tracked lockfile) + the bundled internal deps (`case-box-persistence`, `case-box-contract`) as they appear in the client. State the exact paths + the tool version / date the tree was read.
  2. **Telemetry surfaces to search for**: analytics / tracking / metrics (`analytics`, `mixpanel`, `amplitude`, `posthog`, `google-analytics`/`gtag`), remote logging, crash uploaders + crash-reporting SDKs (`Sentry`, `Bugsnag`, `Crashpad`, `@sentry/*`), Electron `crashReporter` (esp. `crashReporter.start`), auto-update telemetry (`autoUpdater`, `electron-updater`), network beacons (`navigator.sendBeacon`, `fetch`/`XMLHttpRequest`/`WebSocket` to any EXTERNAL host — where "external host" = a NON-loopback (not `127.0.0.1` / `localhost` / `::1`), NON-`file://`, NON-app-local host; a **local loopback / in-process gateway call is NOT telemetry** and is NOT a finding). **Explicitly EXCLUDE the CSS `--tracking-*` letter-spacing custom properties** (`src/theme/tokens.ts`) as non-telemetry false positives.
  3. **Package / dependency inspection criteria**: enumerate `package.json` dependencies + devDependencies (and the lockfile) and confirm NONE is a telemetry / crash-reporting / analytics library. Record the dependency list verbatim.
  4. **Renderer / main / preload inspection criteria**: `electron/main.ts` — no `crashReporter.start()`, no analytics/telemetry init; `electron/preload.mts` — no telemetry/tracking bridge exposed to the renderer; `renderer/**` — no external beacon / analytics call. Record the grep results (matches + explicit "no matches" for each surface).
  5. **Network / API surface inspection criteria**: confirm the client's network surface is local-only (in-process / local-loopback per `.claude/rules/client-local-first.md` GW-00 framing) with NO external telemetry endpoint; cross-reference the offline posture (`.claude/rules/evidence-genie.md` invariant 1 — App Sandbox no-network entitlement is the OS-enforced backstop). Note whether the App Sandbox entitlements are in scope here or deferred to gate 4 signing/packaging (they are gate-4-adjacent; this lane verifies the CODE, the entitlement is packaging).
  6. **Expected evidence artifact**: the doc records the exact inspection commands (grep patterns + dependency listing), the results (matches / "no matches" per surface), the crashReporter-absent confirmation, and the environment (OS, tool versions, date) — captured as text (NO real case data; code-inspection only).
  7. **Explicit pass/fail criteria**: PASS = (no telemetry / analytics / metrics library or init) AND (no crash-reporting SDK) AND (no `crashReporter.start()` — Electron crash reporting is opt-in, so absent = OFF by default) AND (no external beacon / analytics network call) AND (dependency list carries no telemetry/crash lib). FAIL = any telemetry surface present AND on by default, OR a crash reporter started by default. A FAIL is a privacy/offline-posture finding (surface, do not paper over).
  8. **How the result feeds gate 20 without clearing unrelated gates**: a PASS supplies the "ships no telemetry + crash-reporting disabled" verification. The gate-20 status uses ONLY the readiness report's EXISTING vocabulary (`OPEN` / `PARTIAL` / `CLEARED` / `CLEARED pending user go-live approval` / `STOP-AND-ASK`) — the exec lane MUST quote the report's current gate-20 status before editing and MUST NOT invent a new state. The safe move (matching the gate-12/gate-15 precedent) is to KEEP gate 20 `PARTIAL` and enrich the row with a `[Δ] no-telemetry + crash-reporting-off verified` marker (roll-up bucket unchanged); a `PARTIAL → CLEARED` move is NOT made here (it belongs to a separate holistic readiness-refresh WI). It MUST NOT clear gates 4/6/7/11/12/14/15/18/19/21 or imply any cleared; go-live independence is kept.
  9. **Residual risks + follow-up WIs (if any)**: e.g. the App Sandbox no-network entitlement is verified at packaging/signing (gate 4, STOP-AND-ASK) — this lane verifies the CODE ships no telemetry, the OS-enforced offline backstop is gate-4 packaging; a future dependency-provenance sweep overlaps gate 19 (supply-chain); a runtime network-egress observation (vs static inspection) could be a deeper follow-up.
- Dependencies mapped: **gate 4** (signing/distribution/packaging — the App Sandbox no-network entitlement is verified there, STOP-AND-ASK; this lane verifies the CODE, NOT the entitlement — related, NOT cleared); **gate 19** (supply-chain — the dependency inspection overlaps; related, NOT cleared); **gate 6** (full-project audit — a telemetry finding would surface there; LATER, NOT cleared); the brief §4 offline posture + evidence-genie invariant 1 (offline) inform this but are not gates. These are noted as related, NOT cleared by this WI.
- Essential refs: `docs/product/project-requirements-brief.md` §4 (fully offline; no telemetry; crash reporting OFF by default); `.claude/rules/evidence-genie.md` invariant 1 (macOS App Sandbox, no network entitlements — offline OS-enforced); `.claude/rules/client-local-first.md` (v1 local-first/offline; the gateway is loopback, not a public endpoint); `apps/lawbar-desktop/electron/main.ts` + `electron/preload.mts` + `src/**` + `renderer/**` + `package.json` (the shipped client surface — READ-ONLY, the ground truth); `docs/release/go-live-readiness-report.md` §1 gate 20 (PARTIAL) + related gates 4/19/6; `AGENTS.md` §"Source hierarchy" + §"Authoring discipline"; `.claude/rules/security-boundary.md` §"Go-live independence".
- Review questions: (1) Does the WI correctly scope a READ-ONLY telemetry/crash-reporting VERIFICATION (inspection + transcript evidence), NOT the implementation of telemetry/crash-reporting and NOT an execution in this authoring lane? (2) Does it enumerate the telemetry surfaces to search (analytics/tracking/metrics, crash SDKs, Electron `crashReporter`, auto-update telemetry, network beacons) AND explicitly exclude the CSS `--tracking-*` false positives? (3) Does it require dependency + main/preload/renderer + network-surface inspection with recorded results? (4) Does it keep gates 4/6/7/11/12/14/15/18/19/21 uncleared + not imply any cleared, and correctly note the App Sandbox entitlement is gate-4 packaging (not this lane)? (5) Does it keep the status within the report's existing vocabulary, decide no STOP-AND-ASK (4/11/17/21) hard-stop, and make no GO/NO-GO? (6) Is any telemetry/crash-reporting implementation, `crashReporter.start()`, dependency change, product source/test/schema change, or readiness refresh smuggled in?

Type: EVIDENCE
Scope: Govern (do NOT execute) a FUTURE lane that runs the M0 gate-20 telemetry / crash-reporting verification. The FUTURE execution lane produces `docs/release/gate20-telemetry-crash-reporting-verify-00.md` — defining requirements 1–9 above (exact files/packages; telemetry surfaces to search incl. excluding the CSS `--tracking-*` false positives; dependency inspection; main/preload/renderer inspection; network-surface inspection; evidence artifact; explicit pass/fail; how it feeds gate 20 without clearing others; residual risks) — then runs the READ-ONLY inspection (grep + dependency listing; NO code written, NO dependency added), records the transcript, and updates ONLY the gate-20 evidence row of `docs/release/go-live-readiness-report.md` to reference the result, where the status uses the report's EXISTING vocabulary — the safe move is to KEEP gate 20 `PARTIAL` and enrich with a `[Δ] no-telemetry + crash-reporting-off verified` marker; a `PARTIAL → CLEARED` move belongs to a SEPARATE holistic readiness-refresh WI, NOT this one — and it MUST NOT clear gates 4/6/7/11/12/14/15/18/19/21 or imply any cleared. It verifies the CODE ships no telemetry + crash reporting OFF by default (no `crashReporter.start()`), notes the App Sandbox no-network entitlement is gate-4 packaging (NOT cleared here), and cross-references gate 19 (dependency/supply-chain overlap, NOT cleared). It PRESERVES every still-non-cleared gate + the STOP-AND-ASK gates (4/11/17/21); it makes NO final GO/NO-GO, resolves NO STOP-AND-ASK, runs NO gate-6 audit, IMPLEMENTS NO telemetry/crash-reporting, adds/removes NO dependency, edits NO brief/ADR, runs NO readiness refresh, and changes NO product source/test/schema/contract. THIS authoring lane implements + runs + decides NOTHING — it only governs the gate-20 verification WI. If review asks to implement telemetry/crash-reporting, add `crashReporter.start()`, change a dependency, clear a dependent gate, run gate 6, run a readiness refresh, or make a GO/NO-GO, REJECT and report.
Source of truth: docs/product/project-requirements-brief.md §4 (fully offline; no telemetry; crash reporting OFF by default); .claude/rules/evidence-genie.md invariant 1 (App Sandbox no network entitlements — offline OS-enforced); .claude/rules/client-local-first.md (v1 local-first/offline; loopback gateway not public endpoint); apps/lawbar-desktop/electron/main.ts + electron/preload.mts + src/** + renderer/** + package.json + lockfile (the shipped client surface — READ-ONLY ground truth); docs/release/go-live-readiness-report.md §1 gate 20 (PARTIAL) + gates 4/19/6; AGENTS.md §"Source hierarchy" + §"Authoring discipline" (verify every named thing); .claude/rules/security-boundary.md §"Go-live independence"; .claude/rules/autonomy.md + staging-hygiene.md + loc-guardian.md
Allowed files: docs/release/gate20-telemetry-crash-reporting-verify-00.md (the NEW verification evidence doc the FUTURE lane produces) + docs/release/go-live-readiness-report.md (the gate-20 evidence ROW ONLY + any narrow roll-up-consistency edit strictly required by a gate-20 status move) + dev-memo/run/reviews/ (the exec lane's own review artifact). NOTE: THIS authoring lane commits ONLY the queue governance (queue.md/linted/reviewed/governed) + reviews/queue-review-154.md — NO verification doc, NO inspection run, NO report change. The FUTURE exec lane's inspection is READ-ONLY (grep + dependency listing, no committed script needed); it records its evidence in the COMMIT MESSAGE + review artifact and does NOT re-stage this lane's queue governance.
Forbidden files: docs/release/ OTHER than the NEW gate20-telemetry-crash-reporting-verify-00.md + the gate-20 row of go-live-readiness-report.md (the other audit/assessment/policy/drill/procedure/cert/dry-run reports are READ-ONLY evidence — NOT edited), docs/adr/ + docs/product/ (READ-ONLY — NO brief/ADR edit), docs/contracts/, apps/ + services/ (NO product source/test/renderer/IPC/electron change — the client surface is READ-ONLY, inspected not edited; NO telemetry/crash-reporting code; NO crashReporter.start()), apps/*/package.json + package-lock.json + services/*/package.json (NO dependency add/remove), services/*/src/sqlite/schema.ts (CURRENT_SCHEMA_VERSION stays 12), migrations/, native/, dev-memo/run/intake/ (raw samples — never staged), dev-memo/ OTHER than run/reviews/ (no other plan/design edit), .claude/ + .github/; NO telemetry/crash-reporting IMPLEMENTATION; NO inspection RUN in this authoring lane; NO gate-6 full-project audit; NO readiness refresh; NO clearing of gates 4/6/7/11/12/14/15/18/19/21; NO signing/notarization/public-distribution decision (gate 4); NO 律师法 conclusion (gate 11); NO license/legal/business decision (gate 17); NO gate-7 D-G7 risk acceptance; NO final GO/NO-GO decision (gate 21). Push is NEVER automatic (autonomy hard-stop): local commit governance is the default; any branch push + PR open happens ONLY under explicit per-invocation user authorization; NO merge; NO branch delete of shared branches
Gates: EVIDENCE/governance lane (MEDIUM release-governance risk — governs a telemetry/crash-reporting verification WI; no code, no inspection run, no telemetry impl, no go-live decision). (1) THIS authoring lane: broker /cc-suite:review-plan on this queue WI, Path 1, retrievable, then govern. (2) The FUTURE exec lane: RUN the read-only inspection (grep + dependency listing), produce the verification doc + the gate-20 row update, then broker /cc-suite:review-plan on the produced artifacts. (3) scripts/workflow/check-contract-integrity.sh exit 0; scripts/workflow/check-queue.sh PASS (both lanes). Forbidden-scope scan before commit: CURRENT_SCHEMA_VERSION EQUALS 12; no product source/test/package/schema/contract change; no telemetry/crash-reporting impl; no crashReporter.start(); no dependency change; no gate-6 run; no readiness refresh; no gate 4/6/7/11/12/14/15/18/19/21 clear; no GO/NO-GO; no raw sample. STOP-points: review asks to implement telemetry/crash-reporting, add crashReporter.start(), change a dependency, clear a dependent gate, run gate 6, run a readiness refresh, or make a GO/NO-GO → REJECT/STOP and report; any forbidden file would change → STOP.
Acceptance criteria: check-queue and check-contract-integrity PASS and CURRENT_SCHEMA_VERSION EQUALS 12 and the diff CONTAINS only the queue governance (queue.md/linted/reviewed/governed) + reviews/queue-review-154.md and no docs/source/test/package/schema/contract change appears and no telemetry/crash-reporting impl / no crashReporter.start() / no dependency change / no readiness refresh appears. The governed WI must DEFINE the nine verification requirements (files/packages; telemetry surfaces to search incl. excluding the CSS --tracking-* false positives; dependency inspection; main/preload/renderer inspection; network-surface inspection; evidence artifact; explicit pass/fail; feeds-gate-20-without-clearing-others; residual risks), keep gates 4/6/7/11/12/14/15/18/19/21 uncleared + not imply any cleared, correctly note the App Sandbox entitlement is gate-4 packaging, keep the status within the report's existing vocabulary, and MUST NOT decide a STOP-AND-ASK hard-stop or assert go-live. review-plan (this lane) must be RUN and must PASS (READY, or READY with only Low-risk clarifications applied).
Risk flags: MEDIUM (release-governance — a verification WI that could over-claim readiness, drift into implementing telemetry/crash-reporting, change a dependency, or clear a dependent gate). Care points: (a) NO INSPECTION RUN in this authoring lane — this lane only governs the WI; (b) READ-ONLY VERIFICATION — the client surface + package.json are grepped/listed, NEVER edited; NO telemetry/crash-reporting code, NO crashReporter.start(); (c) FALSE-POSITIVE EXCLUSION — the CSS --tracking-* letter-spacing tokens are NOT telemetry and MUST be excluded from the finding; (d) NO DEPENDENCY CHANGE — no telemetry/crash lib added or removed (a dependency change is an autonomy hard-stop); (e) DEPENDENT GATES UNCLEARED — gates 4/6/7/11/12/14/15/18/19/21 stay uncleared; the App Sandbox no-network entitlement is gate-4 packaging (verified there, not here); the dependency overlap is gate-19; (f) GO-LIVE INDEPENDENCE — a telemetry verification does NOT imply go-live; the final verdict + the STOP-AND-ASK hard-stops (4/11/17/21) remain the user's; (g) NO READINESS REFRESH — the holistic roll-up reconciliation is a separate lane. If any cannot hold, STOP and report.
Depends on: none
Commit boundary: TWO separate lanes. (1) THIS authoring lane commits ONLY the queue governance (queue.md/linted/reviewed/governed) + reviews/queue-review-154.md — no verification doc, no inspection run, no report change, no implementation, no decision. (2) The FUTURE execution lane commits ONLY the new docs/release/gate20-telemetry-crash-reporting-verify-00.md + the gate-20 evidence-row update to go-live-readiness-report.md (+ any narrow roll-up-consistency edit strictly required by a gate-20 status move) + its own review artifact under dev-memo/run/reviews/, records its evidence (incl. the grep/dependency transcript) in the COMMIT MESSAGE, and does NOT re-stage this lane's queue governance. The inspection is READ-ONLY (no committed script). NO product source/test/package/schema/contract file in either lane; NO telemetry/crash-reporting impl; NO crashReporter.start(); NO dependency change; NO staging of dev-memo/run/intake/ or untracked residue; push is NEVER automatic (autonomy hard-stop) — local commit governance is the default, any branch push + PR open happens ONLY under explicit per-invocation user authorization; no merge; no branch delete of shared branches. THIS authoring lane implements NOTHING, runs NO inspection, and decides NOTHING — it only governs the gate-20 telemetry/crash-reporting verification WI.

## Acceptance criteria (FUTURE execution lane — what the governed verification doc must satisfy)

These describe the deliverable the FUTURE exec lane must produce; they are NOT preconditions of THIS authoring lane. The authoring-lane acceptance is the structured `Acceptance criteria:` field above (queue governance + review READY only — the verification doc does NOT exist yet and MUST NOT be created in this lane).

1. A NEW `docs/release/gate20-telemetry-crash-reporting-verify-00.md` exists defining requirements 1–9 above and recording the read-only inspection transcript + result.
2. The inspection covered the client surface (`electron/main.ts`, `electron/preload.mts`, `src/**`, `renderer/**`) + `package.json`/lockfile + the telemetry surfaces enumerated in requirement 2, EXCLUDING the CSS `--tracking-*` false positives, with recorded results (matches / "no matches" per surface) and the dependency list verbatim.
3. The evidence records the exact inspection commands, the results, the `crashReporter.start()`-absent confirmation (crash reporting OFF by default), the environment, and an explicit PASS/FAIL — code-inspection only, no real data, no committed script.
4. The gate-20 ROW references the verification; the status label uses ONLY the report's existing vocabulary (`OPEN`/`PARTIAL`/`CLEARED`/`CLEARED pending user go-live approval`/`STOP-AND-ASK`) — the safe move is to KEEP `PARTIAL` and enrich with a `[Δ] no-telemetry + crash-reporting-off verified` marker (roll-up bucket unchanged); a `PARTIAL → CLEARED` move belongs to a separate holistic readiness-refresh WI. MUST NOT clear gates 4/6/7/11/12/14/15/18/19/21 or imply any cleared. Roll-up-line semantics kept consistent.
5. The doc notes the App Sandbox no-network entitlement is verified at gate-4 packaging (not this lane), cross-references gate 19 (dependency overlap), and keeps go-live independence (no sign-off; gates 4/11/17/21 remain user STOP-AND-ASK).
6. NO product source/test/package/config/schema/contract change in either lane; NO telemetry/crash-reporting implementation; NO `crashReporter.start()`; NO dependency change; `CURRENT_SCHEMA_VERSION` stays 12; docs-only tracked output.
7. `scripts/workflow/check-queue.sh` PASS + `scripts/workflow/check-contract-integrity.sh` PASS on both lanes.
