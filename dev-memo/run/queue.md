## WI-A3-LINK-IPC-DESIGN-00: design the IPC/API surface for audited evidence links (design-only; NOT A0.7-gated)
Type: PLAN
A0.7-classification: WI-A3-LINK-IPC-DESIGN-00 is a DESIGN-ONLY ADR (it authorizes no code, executes no A0.7-dependent code, reads no A0.7 marker), so it is NOT A0.7-gated and carries NO `Requires-A07:` line. It governs eventual A0.7-gated work (the IPC impl over court-facing link state), so broker /cc-suite:review-plan + /cc-suite:audit + /cc-suite:verify are REQUIRED on the design. If review-plan says the design itself must be A0.7-gated, STOP and report.
Scope: Produce a DESIGN-ONLY ADR (`docs/adr/ADR-evidence-a3-link-ipc-surface.md`) for the minimal IPC/API surface that exposes the audited link lifecycle (createLink/unlinkLink/relinkLink/resolve/export — all live on main) to the Electron app layer. No product code. The ADR records, grounded in the EXISTING app IPC conventions: (a) channel/method names (casebox:link:*); (b) request/response DTOs; (c) error taxonomy + stable codes; (d) the A0.7 gating model for mutating IPC calls; (e) how actor/user context is supplied without trusting renderer input; (f) tenant/matter/evidence/anchor boundary enforcement across the IPC boundary; (g) confidentiality rules (no real evidence content in fixtures/logs/reports; no raw evidence body/PDF crosses the boundary; link DTOs expose identifiers/status/flags, not content); (h) idempotence/rejection semantics matching persistence; (i) how UNLINKED/broken/needs_review/valid statuses are represented to clients; (j) the ADR reconciliation note (createLink evidence-existence extension); (k) the explicit separation of IPC-contract-design vs IPC-impl vs renderer/UI vs export-rendering vs schema/contract changes; (l) a test plan + sequencing. DESIGN-ONLY: no apps/** code, no electron/**/IPC handlers, no renderer/UI, no services/** change, no schema or CURRENT_SCHEMA_VERSION change, no audit-contract change, no package/dependency change. If the design proves any of those are REQUIRED before the IPC design can complete, STOP and report.
Required decisions to record: (1) CHANNELS — casebox:link:create/unlink/relink/list/export, matching the casebox:<entity>:<op> convention; preload extends window.lawbar.caseBox. (2) DTOs — request DTOs carry only renderer-authorable fields; the main process injects tenant_id + actor_user_id (renderer-supplied = forbidden field); response DTOs projected through a LINK_RESPONSE_FIELDS allowlist excluding authority tenant_id + internal payload_json; export returns the existing buildExportCitations shape. (3) ERRORS — reuse IpcEnvelope + the existing CaseBoxPersistenceErrorCode set + SAFE_MESSAGES (no new error code). (4) A0.7 MODEL — A0.7 is a DEV/COMMIT-time gate, NOT a runtime gate; the IPC IMPL lane is A0.7-gated (custody 9b) at commit time; at runtime the mutating ops rely on the OS sandbox offline guarantee + local-first posture + main-injected actor/tenant; the impl MUST NOT invent a runtime A0.7 marker check (a new mechanism would need its own ADR). (5) ACTOR/TENANT — main-injected (getActiveActorUserId/getActiveTenantId); renderer-supplied forbidden. (6) BOUNDARIES — matter_id renderer-specified + validated by persistence; tenant injected; evidence/anchor existence enforced by persistence (the IPC layer delegates). (7) CONFIDENTIALITY — identifiers/status/flags only; no evidence content/PDF/OCR text crosses the bridge; synthetic fixtures. (8) IDEMPOTENCE/REJECTION — matches persistence (illegal_transition/invalid_argument/duplicate_id), relayed via the error envelope. (9) STATUS REPRESENTATION — resolver status valid|needs_review|broken via list; export flag UNLINKED|BROKEN|NEEDS_REVIEW|NON_CITABLE|AMBIGUOUS|null via export; no new vocabulary. (10) RECONCILIATION + SEQUENCING — record the createLink evidence-existence extension (IPC preserves it; optional later docs ADR cleanup); the next lane is the IPC/API impl (A0.7-gated, IPC-only), UI deferred; note the impl lane must register a new desktop test file in apps/lawbar-desktop/package.json (editable for that additive edit, unlike the persistence package.json).
Source of truth: apps/lawbar-desktop/src/caseBox/handlerShared.ts (CHANNEL + projection) + electron/ipc/caseBoxHandlers.ts (registration) + electron/preload.mts (contextBridge) + src/caseBox/dto/shared.ts (IpcEnvelope) + src/caseBox/matterHandlers.ts (handler pattern) + src/caseBox/errorMap.ts (error mapping) + src/security/{activeActor,activeTenant}.ts (injection) + src/caseBox/caseBoxRuntime.ts (wiring) (READ-ONLY — the IPC conventions the design mirrors); services/case-box-persistence/src/sqlite/{linkRepoQueries,linkStatusResolverQueries,exportCitationQueries}.ts + src/errors.ts (READ-ONLY — the persistence ops + error codes the IPC exposes); docs/adr/ADR-evidence-a3-link-create-operation.md + ADR-evidence-a3-unlink-break-link-workflow.md (the persistence design this IPC exposes); .claude/rules/{client-local-first,autonomy,cc-suite,evidence-genie}.md (local-first posture; user-authorized design lane).
Allowed files: docs/adr/ADR-evidence-a3-link-ipc-surface.md (the DESIGN ADR — the only deliverable), dev-memo/run/queue.md, dev-memo/run/queue.linted, dev-memo/run/queue.reviewed, dev-memo/run/queue.governed, dev-memo/run/reviews/queue-review-093.md, dev-memo/study/** (a batch-audit study packet if a window closes), dev-memo/cc-suite-reliability-log.md (only if a TIMEOUT retry occurs), dev-memo/deferred-audit-findings.md (only if a finding is deferred)
Forbidden files: apps/** (NO app/IPC/renderer code — the IPC impl is the future WI-A3-LINK-IPC-T1), electron/** (NO handlers/preload), services/** (NO persistence change), services/case-box-persistence/src/sqlite/schema.ts (NO schema/version change), docs/contracts/** (NO contract change), apps/lawbar-desktop/package.json + any package manifest (NO change — the impl lane registers its test file), native/**, .claude/**, .github/**, AGENTS.md, CLAUDE.md, GEMINI.md, scripts/workflow/**, migrations/**, package-lock.json / any dependency manifest, committed dev-memo/run/evidence/** marker/ledger/key/evidence-run files, committed keys, dev-memo/run/reports/** (untracked local run-state — never staged), dev-memo/run/override-reason.md (untracked residue — never staged), Evidence-Genie-M0-Developer-Handover.md (root intake — READ-ONLY, never moved/deleted), xiaolai-dev-workflow-study.md (root intake — READ-ONLY, never moved/deleted); NO product/IPC/renderer code, NO schema/version change, NO contract change, NO persistence change, NO package/dependency change, NO implementation, NO runtime A0.7 mechanism invented
Gates: broker /cc-suite:review-plan REQUIRED before/at authoring (confirm DESIGN-ONLY + NOT-A0.7-gated classification; confirm the channels/DTO/error model match the existing app IPC conventions; confirm the A0.7 model — A0.7 is dev/commit-time, the IPC impl lane is A0.7-gated, NO runtime marker invented — or STOP if review-plan disagrees; confirm actor/tenant main-injection + renderer-forbidden; confirm the confidentiality posture (identifiers/status/flags only); confirm idempotence/rejection matches persistence; confirm the status representation; confirm the sequencing IPC-impl-next/UI-deferred + the impl-lane test-file registration note; confirm CONTRACT/IMPL/UI/EXPORT/SCHEMA separation); if review-plan returns NEEDS-FIX, STOP and report; if review-plan says the design must be A0.7-gated, STOP and report; broker /cc-suite:audit + /cc-suite:verify on the ADR diff after authoring (no Critical/High/Medium open before commit); scripts/workflow/check-contract-integrity.sh exits 0 (sanity — always-loaded contract docs untouched); no apps/electron/services/schema/contract/package/dependency change; no committed marker/ledger/key/evidence-run; root intake untouched
Acceptance criteria: docs/adr/ADR-evidence-a3-link-ipc-surface.md exists and records D1-D11 — channels (casebox:link:create/unlink/relink/list/export), request/response DTOs with main-injected actor/tenant + a LINK_RESPONSE_FIELDS allowlist excluding tenant_id/payload_json, the IpcEnvelope error taxonomy reusing the existing CaseBoxPersistenceErrorCode set (no new code), the A0.7 dev/commit-time-gate model (NO runtime marker; the IPC impl lane A0.7-gated), actor/tenant injection + boundary enforcement, confidentiality (identifiers/status/flags only), idempotence/rejection matching persistence, status representation (valid/needs_review/broken + UNLINKED/etc), the createLink evidence-existence reconciliation note, a test plan, and the sequencing (IPC impl next, A0.7-gated, IPC-only; UI deferred; the impl-lane desktop test-file registration note); DESIGN-ONLY (no product code, no schema/contract/persistence/package change); broker review-plan READY (design + classification + A0.7-model confirmed) + audit reports no Critical/High/Medium + verify ALL CLOSED; scripts/workflow/check-contract-integrity.sh exits 0; the committed diff is confined to the ADR + the governance artifacts (dev-memo/run/**)
Risk flags: PLAN — a DESIGN-ONLY ADR (no product code). NOT A0.7-gated (no geometry-derived behavior; no marker/key). Doc-only, but it GOVERNS eventual A0.7-gated IPC work over court-facing link state, so broker review-plan + audit + verify are REQUIRED on the design. The ADR MUST NOT implement IPC/renderer code, MUST NOT change schema/CURRENT_SCHEMA_VERSION, MUST NOT change the contract or persistence, MUST NOT change package manifests/dependencies, MUST NOT invent a runtime A0.7 mechanism, and MUST NOT weaken any Evidence invariant (offline/local-first; identifiers-only confidentiality; main-injected actor/tenant). If the design proves a schema/contract/persistence/package change is required, STOP and report. No push, no merge.
Depends on: none
Commit boundary: governance (mark-reviewed + govern) run STANDALONE before the commit (never bundled with git commit). ONE commit -- docs/adr/ADR-evidence-a3-link-ipc-surface.md + dev-memo/run/queue.md + dev-memo/run/queue.linted + dev-memo/run/queue.reviewed + dev-memo/run/queue.governed + dev-memo/run/reviews/queue-review-093.md, staged by exact path; no product code; no push; no merge
