## WI-AP1: project the audit-list IPC response to a renderer-safe DTO (close AUDIT-AUD-1 leak)
Type: IMPL
Scope: Close a live, security-adjacent authority-field leak on the `casebox:audit:listEvents` channel. `listAuditEventsHandler` (`apps/lawbar-desktop/src/caseBox/auditHandlers.ts`) currently returns the RAW persistence page (`return { ok: true, value: page }`) with NO response projection, so `CaseBoxAuditEvent`'s server-authority fields (`tenant_id`, `actor_user_id`, `matter_id`, the event `id`, and any open `[k: string]: unknown` extras) cross the IPC boundary. This is the SAME class as the fixed FACTS-AUD-3 / GET-AUD-1; the audit LIST channel was missed. Fix MIRRORS the FACTS-AUD-3 pattern exactly. In `apps/lawbar-desktop/src/caseBox/dto/audit.ts`: import `CaseBoxAuditEvent` from `case-box-contract`; add `LIST_AUDIT_EVENTS_RESPONSE_FIELDS` = `action, entity_type, entity_id, timestamp, reason, before_state_hash, after_state_hash, prev_event_hash` (display + audit-chain-verification fields only); add `RendererAuditEventRow = Pick<CaseBoxAuditEvent, (typeof LIST_AUDIT_EVENTS_RESPONSE_FIELDS)[number]>`; add a renderer-safe page type `RendererAuditEventsPage { readonly rows: ReadonlyArray<RendererAuditEventRow>; readonly next_cursor: string | null }`; retype `ListAuditEventsResult = IpcEnvelope<RendererAuditEventsPage>` (drop the now-unused persistence `ListAuditEventsPage` import if it becomes unused; keep `AuditChainHead`). In `auditHandlers.ts`: import `projectPage` from `./handlerShared.js` + the new response-fields const + `RendererAuditEventRow` type, and replace the raw return with `return { ok: true, value: projectPage<RendererAuditEventRow>(page, LIST_AUDIT_EVENTS_RESPONSE_FIELDS) }`. EXCLUDE from the allowlist (must NOT cross the boundary): `tenant_id`, `actor_user_id`, `id`, `matter_id` (projectRow's allowlist also drops the open-index extras). Do NOT change `chainHeadHandler` (already returns a typed `AuditChainHead` with no authority fields) unless impl proves it leaks. The consumed renderer fields are exactly `timestamp/action/entity_type/entity_id/reason` (verified `viewMatterAudit.ts:237-264`), all inside the allowlist; if a consumed field is found outside the set, STOP and surface it (do not silently broaden). NO renderer UI change, NO Electron IPC wiring change, NO persistence/contract change.
Source of truth: apps/lawbar-desktop/src/caseBox/dto/deadline.ts (LIST_DEADLINES_RESPONSE_FIELDS + RendererDeadlineRow + renderer-safe page precedent); apps/lawbar-desktop/src/caseBox/deadlineHandlers.ts (projectPage usage precedent); apps/lawbar-desktop/src/caseBox/handlerShared.ts (projectPage helper, READ-ONLY); docs/contracts/case-box-contract/src/generated/case-box-audit-event.ts (CaseBoxAuditEvent shape — READ-ONLY); apps/lawbar-desktop/renderer/screens/viewMatterAudit.ts (renderAuditEventRow — the consumed fields, READ-ONLY); apps/lawbar-desktop/tests/ipc-list-projection.unit.test.mjs (the FACTS-AUD-3 projection-test pattern to extend); dev-memo/plan-batch-casebox-audit-projection-00.md (proposal)
Allowed files: apps/lawbar-desktop/src/caseBox/dto/audit.ts, apps/lawbar-desktop/src/caseBox/auditHandlers.ts, apps/lawbar-desktop/tests/ipc-list-projection.unit.test.mjs, apps/lawbar-desktop/tests/renderer-dto-sync.test.mjs, dev-memo/deferred-audit-findings.md
Forbidden files: apps/lawbar-desktop/renderer/**, apps/lawbar-desktop/electron/**, services/**, docs/contracts/**, .claude/**, scripts/workflow/**, dev-memo/run/**, apps/lawbar-desktop/src/caseBox/handlerShared.ts, apps/lawbar-desktop/src/caseBox/handlers.ts, all other tests
Gates: npm --prefix apps/lawbar-desktop test
Acceptance criteria: `listAuditEventsHandler` projects every response row through `LIST_AUDIT_EVENTS_RESPONSE_FIELDS`; the IPC response OMITS `tenant_id`, `actor_user_id`, `id`, `matter_id`, and any open-index extra field, while INCLUDING the renderer-consumed fields (`timestamp/action/entity_type/entity_id/reason`) and the chain hashes; `next_cursor` passes through unchanged; `ListAuditEventsResult` is retyped to the renderer-safe page; `chainHeadHandler` unchanged; the extended `tests/ipc-list-projection.unit.test.mjs` proves raw-row-includes-authority vs response-omits across ≥2 rows incl. an open-index extra and preserved pagination; `renderer-dto-sync.test.mjs` asserts `LIST_AUDIT_EVENTS_RESPONSE_FIELDS` excludes `tenant_id`/`actor_user_id`; the 6 existing audit panel tests stay green; `npm --prefix apps/lawbar-desktop test` passes; NO renderer/electron/services/contracts change; AUDIT-AUD-1 recorded + closed in dev-memo/deferred-audit-findings.md with the resolution commit
Risk flags: SECURITY BOUNDARY (IPC authority-field leak) → cc-suite review-plan + audit + verify REQUIRED, self-review NOT acceptable; mirrors the shipped FACTS-AUD-3 fix; persistence/contract READ-ONLY (the leak is purely a missing main-process projection); allowlist must EXCLUDE id/matter_id/tenant_id/actor_user_id; projectRow's allowlist semantics also drop the open-index extras; no renderer path (no design gate); wire-safety only (renderer already expects projected rows)
Depends on: none
Commit boundary: one local commit for the audit-list projection + tests + the AUDIT-AUD-1 closure
