## WI-A3-00: anchor/link engine contract design (design-only; NOT A0.7-gated)
Type: PLAN
A0.7-classification: A3-00 is design-only and intentionally NOT A0.7-gated. This docs-only contract/design WI does not execute anchor math, geometry resolution, persistence, link status, native code, or product behavior, and needs no live provenance-valid A0.7 marker to author the ADR/spec. Therefore this WI carries NO `Requires-A07:` line (explicit user reclassification on 2026-06-23, overriding the prior "must include Requires-A07: yes" instruction). The A0.7 hard gate (check-a07-gate.sh) is UNCHANGED and remains correct: it still fails closed for genuinely A0.7-dependent actions. ALL FUTURE A3 implementation WIs that touch anchor math, geometry resolution, persistence, link status, native code, or product behavior MUST declare `Requires-A07: yes` and pass the A0.7 hard gate with a provenance-valid local marker. review-plan must confirm this classification; if review-plan says this docs-only WI must be A0.7-gated anyway, STOP and report.
Scope: Author the A3 anchor/link engine contract/design packet BEFORE any implementation. Convert the handover's A3 anchor/link model (handover §10 entities + §5 A0.7 class-1/2 + the A3 ticket set A3-T1..A3-T10) into repo-local ADR/spec boundaries: (1) the A3 anchor/link invariants; (2) the expected HEADLESS API/contract shapes (no implementation) for normalize page-space rect -> page-ratio rect, denormalize page-ratio rect -> page-space rect, create anchor, resolve link, classify link status, mark replacement/quarantine effects; (3) the first A3 implementation WI sequence + which single WI may safely touch persistence/native; (4) the test fixtures the first implementation lane will need; (5) the A3 failure classes and stop conditions. Design/contract only: no code beyond documentation, no schema migration, no dependency, no marker/key handling. Outputs: docs/adr/ADR-evidence-a3-anchor-link-contract.md (A3-CONTRACT-00) + dev-memo/plan-batch-casebox-evidence-a3-anchor-contract-00.md; an optional narrow contract checklist under docs/product/ only if review-plan recommends it.
Required design decisions to record: (1) A3-00 design-only is NOT A0.7-gated; (2) future A3 implementation WIs ARE A0.7-gated (Requires-A07: yes + provenance-valid local marker); (3) anchor identity binds to documentId + physicalPageIndex; (4) anchor geometry binds to a captured DocumentPageGeometry version (geometryCapturedAt); (5) persisted rects use page_ratio, never viewport/screen coordinates; (6) ratios are derived only from persisted resolved page box/bounds/rotation (box origin subtracted, rotation-aware); (7) geometry mismatch or document replacement yields needs_review, never a stale valid coordinate; (8) broken links must be explicit, never silent; (9) anchor-delete cascade policy (A3-T6) remains UNRESOLVED — the handover §11 leaves the rule undecided; do not invent product policy; (10) first implementation lane recommendation: start with pure headless page-ratio normalization math (A3-T2 / coordinate-roundtrip) before schema/persistence/native/product UI, unless review-plan recommends otherwise; record that the schema/persistence WI (A3-T1) is the one that may touch persistence and is gated.
Source of truth: Evidence-Genie-M0-Developer-Handover.md (root intake; READ-ONLY, never moved/edited) §10 data model (Anchor/Link/LinkStatus/rect/DocumentPage/DocumentPageGeometry), §5 A0.7 class-1/class-2 classification, the A3 ticket set (A3-T1..A3-T10), §11 open decisions (anchor-delete cascade undecided); .claude/rules/evidence-genie.md (inv.5 citation identity from DocumentPage, inv.6 anchors page-ratio vs captured geometry, inv.7 OptimizedDocumentRendition never citation/anchor basis, inv.3/inv.4 A0.7-first / no-UI-before-A0.7); docs/adr/ADR-evidence-a07-renderer-conformance-gate.md (A07-GATE-00) + docs/adr/ADR-evidence-a07-marker-provenance.md (A07-MARK-00) for the A0.7-dependence declaration mechanism; dev-memo/plan-batch-casebox-evidence-workflow-port-00.md (A3 sits after A0.7 on the build order); .claude/rules/autonomy.md (this design lane is user-authorized).
Allowed files: docs/adr/ADR-evidence-a3-anchor-link-contract.md, dev-memo/plan-batch-casebox-evidence-a3-anchor-contract-00.md, dev-memo/** (queue/governance/review/study/plan artifacts), docs/product/** ONLY a narrow contract checklist if review-plan explicitly recommends it, scripts/workflow/** ONLY a narrow contract-check stub if review-plan explicitly approves one (prefer none)
Forbidden files: apps/**, native/** (native/evidence-core-swift + native/evidence-core JS shim — NO change), .claude/**, .github/**, services/**, docs/contracts/**, AGENTS.md, CLAUDE.md, GEMINI.md, committed dev-memo/run/evidence/** marker/ledger/key/evidence-run files, committed keys, Evidence-Genie-M0-Developer-Handover.md (root intake), xiaolai-dev-workflow-study.md (root intake), dev-memo/run/override-reason.md (untracked residue — never staged); NO anchor math implementation, NO schema/persistence/migration, NO GRDB/SQLCipher or any new dependency, NO Evidence UI/product behavior, NO OCR/AI/cloud/auth/network, NO marker/key handling, NO A0.7 harness-semantics or gate-behavior change, NO Requires-A07 on this WI
Gates: broker /cc-suite:review-plan REQUIRED before authoring (confirm the design-only / NOT-A0.7-gated classification); if review-plan returns NEEDS-FIX, STOP and report; if review-plan says this docs-only WI must be A0.7-gated anyway, STOP and report; if review-plan returns READY but asks for implementation, UI, DB-dependency addition, native code, or key/marker changes, STOP and ask; broker /cc-suite:audit + /cc-suite:verify on the design packet after authoring; scripts/workflow/check-contract-integrity.sh exits 0; scripts/workflow/check-gates.sh returns GATES OK (the A0.7 gate passes: the queue carries NO Requires-A07 and no markers exist -> not A0.7-dependent -> pass); npm --prefix apps/lawbar-desktop test passes 610/610; no apps/native/.claude/.github/services/docs-contracts change; no committed marker/ledger/key/evidence-run file; dev-memo/run/evidence/** remains absent; root intake files remain untracked + untouched
Acceptance criteria: docs/adr/ADR-evidence-a3-anchor-link-contract.md EXISTS and records the A3 invariants (anchor identity binds documentId+physicalPageIndex; geometry binds a captured DocumentPageGeometry version; persisted rects use page_ratio; ratios derived only from persisted resolved box/bounds/rotation with box origin subtracted + rotation-aware; geometry mismatch or document replacement RETURNS needs_review never a stale valid coordinate; broken links explicit not silent; no viewport/screen coordinate enters persistence; export degradation maps links to textual citation/卷X页Y or an ExportCitationFlag, never dropped/silently wrong); the ADR defines the expected headless API contracts (normalize page-space rect->page-ratio rect; denormalize page-ratio rect->page-space rect; create anchor; resolve link; classify link status; mark replacement/quarantine effects) as signatures/contracts only, NOT implementations; the ADR records all 10 required design decisions including the explicit "A3-00 design-only NOT A0.7-gated; all A3 implementation WIs MUST declare Requires-A07: yes + pass the A0.7 hard gate with a provenance-valid local marker"; dev-memo/plan-batch-casebox-evidence-a3-anchor-contract-00.md defines the first A3 implementation WI sequence (recommending pure headless A3-T2 normalization math first, before schema/persistence/native/UI), names the single WI that may touch persistence (A3-T1) as A0.7-gated, lists the test fixtures the first implementation lane needs, and records the A3 failure classes + stop conditions; the anchor-delete cascade rule (A3-T6) is PRESERVED as explicitly unresolved (not invented); scripts/workflow/check-contract-integrity.sh exits 0; scripts/workflow/check-gates.sh returns GATES OK; broker review-plan READY (classification confirmed) + audit reports no Critical/High/Medium + verify ALL CLOSED; the committed design diff is confined to docs/adr/** + dev-memo/** (plus an optional docs/product checklist only if review-plan approved)
Risk flags: DESIGN-ONLY (Type PLAN). Explicitly NOT A0.7-gated by user reclassification (override of the prior Requires-A07: yes instruction, recorded above) — a contract ADR does not execute anchor math/persistence/geometry/product and needs no live marker; the A0.7 hard gate is left unchanged and remains correct. ALL future A3 implementation WIs MUST carry Requires-A07: yes. No code, no schema/migration, no new dependency (no GRDB/SQLCipher), no UI/product, no marker/key handling. broker review-plan + audit + verify REQUIRED (the design governs eventual HIGH-RISK anchor/persistence/native work). The design must not weaken any Evidence invariant (citation-from-DocumentPage, anchors-page-ratio-vs-captured-geometry, optimized-never-canonical, A0.7-first). No push, no merge.
Depends on: none
Commit boundary: governance commit separate (dev-memo/run/queue.md + queue.linted/reviewed/governed + dev-memo/run/reviews/queue-review-067.md); one design commit -- docs/adr/ADR-evidence-a3-anchor-link-contract.md + dev-memo/plan-batch-casebox-evidence-a3-anchor-contract-00.md (+ an optional docs/product checklist only if review-plan approved), staged by exact path; no push; no merge
