## WI-EVW5: A0.7 hard hooks — reality-gate enforcement (no UI/product)
Type: IMPL
Scope: Implement the deferred EVW5 hard hooks now that the real A0.7 marker machinery (harness + fail-closed guard + marker write/validation + the ENA11-FIX1 isMarker bind/check) is merged. Add scripts/workflow/check-a07-gate.sh: a workflow-level hard gate that enforces "a protected, A0.7-DEPENDENT lane/action may not proceed unless a genuine, provenance-valid LOCAL A0.7 marker exists." Behavior (fail-closed): (1) ALWAYS delegate to check-marker-guard.sh --scan (rejects committed/tracked evidence + fabricated/touched/copied/schema-only local markers; validates only genuine local markers with the env key; clean tree passes); (2) determine A0.7-dependence ("required") via A07_REQUIRED=1|true|yes env OR a queue.md line `Requires-A07: yes|true`; (3) if required, at least one provenance-valid local marker must exist (env key + a07_marker.py validate) else FAIL CLOSED; (4) if not required, pass (clean tree fine). Add scripts/workflow/check-a07-gate.test.sh (temp-dir + ephemeral-key matrix). Wire check-a07-gate.sh + its self-test into scripts/workflow/check-gates.sh (smallest enforcement surface; no .claude hook needed). The gate NEVER writes a marker, NEVER creates the namespace, NEVER relaxes the guard, NEVER treats harness isMarker=false as marker-valid. NO Evidence UI/product/apps change; NO A0.7 harness-semantics change; NO marker schema/key-custody change; NO committed marker/ledger/key/evidence-run file. Do NOT delete/move the root intake files.
Required behavior: fail closed for fabricated/copied/touched/committed markers; allow clean repo (no local marker) where a marker is NOT required; require a valid local A0.7 marker only for actions that depend on A0.7 green; env key absent + marker required -> fail closed; env key absent + marker NOT required -> do NOT fail merely because no local marker exists; staged/committed dev-memo/run/evidence/** rejected unconditionally; no hook generates a marker as a side effect; no hook relaxes the guard; no hook treats isMarker=false as marker-valid.
Source of truth: docs/adr/ADR-evidence-a07-renderer-conformance-gate.md (A07-GATE-00 §1/§4, no-build-on-non-green); .claude/rules/evidence-genie.md (inv.3 A0.7-first, inv.4 no-UI-before-A0.7, inv.10 not_implemented=fail); docs/adr/ADR-evidence-a07-marker-provenance.md (A07-MARK-00, validation authority); scripts/workflow/check-marker-guard.sh + a07_marker.py + a07-marker-write.sh (the marker machinery the gate reuses, unchanged); scripts/workflow/check-gates.sh (the gate battery to integrate into); .claude/rules/autonomy.md (this impl lane is user-authorized).
Allowed files: scripts/workflow/** (check-a07-gate.sh + check-a07-gate.test.sh + the narrow check-gates.sh integration), .claude/** hook config ONLY if review-plan confirms a PreToolUse hard hook is required and this is the existing hard-hook location (prefer NOT to; smallest surface is the scripts/workflow gate), dev-memo/** (queue/governance/audit/study artifacts), narrow README/doc clarification only if needed
Forbidden files: committed dev-memo/run/evidence/** marker/ledger/key/evidence-run files, committed keys, native/evidence-core-swift/** (NO change unless review-plan identifies a required test-only clarification — prefer none), native/evidence-core/** (JS shim — NO change), apps/**, services/**, docs/contracts/**, docs/product/**, AGENTS.md, CLAUDE.md, GEMINI.md, .claude/settings.json + .claude/hooks/** (UNLESS review-plan explicitly confirms a PreToolUse hard hook is required here), .claude/rules/**, Evidence-Genie-M0-Developer-Handover.md (root), xiaolai-dev-workflow-study.md, package.json; NO marker write side-effect from hooks; NO key-custody change; NO A0.7 harness-semantics change; NO marker-schema change; NO Evidence UI/product behavior; NO OCR/AI/cloud/auth/network
Gates: broker /cc-suite:review-plan REQUIRED before impl (hard enforcement of the apex gate — no self-review); if review-plan returns READY but scopes into UI/product/native-semantics/key-custody/marker-write/marker-schema changes, STOP and ask; if review-plan says a .claude PreToolUse hook is REQUIRED (vs the scripts/workflow gate), STOP and ask (it would touch .claude/settings.json); broker /cc-suite:audit + /cc-suite:verify after impl; clean tree with no marker passes for non-A0.7-dependent actions; a protected (A07_REQUIRED or queue Requires-A07) action without a valid marker fails closed; missing env key + required fails closed; genuine local marker + env key passes; fabricated/touched/copied/schema-only marker fails; staged/committed evidence namespace fails unconditionally; the gate does not create the namespace or write a marker; isMarker=false is not marker-valid; existing marker guard tests (17) + marker write tests (16) still pass; the native/evidence-core JS shim is byte-unchanged; scripts/workflow/check-contract-integrity.sh exits 0; swift test passes (19); npm --prefix apps/lawbar-desktop test passes (610/610); scripts/workflow/check-gates.sh returns GATES OK (now incl. the A0.7 gate + its self-test)
Acceptance criteria: scripts/workflow/check-a07-gate.sh enforces the required behavior (delegate to the guard; require a provenance-valid local marker only when A0.7-dependent via A07_REQUIRED or queue Requires-A07; fail closed on missing marker/key when required; pass clean non-dependent tree); scripts/workflow/check-a07-gate.test.sh proves: not-required clean pass, required-no-marker fail, required-no-key fail, required+genuine+key pass, fabricated fail (required + not-required), genuine+key not-required pass, queue-Requires-A07 pass/fail, committed/tracked marker fail unconditionally, gate-does-not-create-namespace, isMarker-tampered fail; check-gates.sh runs the gate + its self-test and returns GATES OK on the clean repo (real queue has no Requires-A07, no markers -> not required -> pass); no committed marker/ledger/key/evidence-run; no .claude change unless review-plan confirmed; the native package + JS shim are byte-unchanged; check-contract-integrity PASS; swift test pass; npm desktop 610/610; broker audit no Critical/High/Medium and verify ALL CLOSED; the committed diff is confined to scripts/workflow/** plus dev-memo/** governance artifacts
Risk flags: HIGH-RISK -- hard enforcement wiring the apex A0.7 reality gate into the gate battery. broker review-plan + audit + verify required. Scope strictly limited to a fail-closed workflow gate + self-test + a narrow check-gates integration reusing the merged marker machinery; NO marker write/side-effect, NO guard relaxation, NO key-custody/schema/harness change, NO UI/product, NO .claude/hooks/settings change unless review-plan explicitly confirms a PreToolUse hook is the required hard-hook location (then STOP and ask). Markers stay local-only/gitignored; committed/staged evidence rejected unconditionally; isMarker=false never marker-valid. No new dependency.
Depends on: none
Commit boundary: one local commit -- scripts/workflow/check-a07-gate.sh + scripts/workflow/check-a07-gate.test.sh + the narrow scripts/workflow/check-gates.sh integration, staged by exact path; governance commit separate; no push
