## WI-GATE3-CLIENT-RELEASE-HARDENING-00: bounded client release-readiness assessment + the one auditable non-STOP-AND-ASK honesty fix (Type: IMPL, release/client-hardening MEDIUM risk)

## Review packet (compact)
- Active plan summary: advance M0 go-live **gate 3** (`docs/release/go-live-readiness-report.md` §1 gate 3 = PARTIAL — "Substantial functional case-box UI is built" but the app self-declares "Token-compliance fixture only; NOT product UI; NOT signed; NOT for distribution" at `apps/lawbar-desktop/package.json:6`; remaining = product-grade hardening + the overdue-deadline dashboard-banner (brief §10) + release posture; "Depends on gate 4 (STOP-AND-ASK)"). Gate 3 CANNOT be fully cleared in this lane — its release posture depends on gate 4 (Apple Developer ID / code-sign / notarization / public distribution — a USER STOP-AND-ASK) and one remaining product-UI feature (the overdue banner). So this is the SMALLEST auditable hardening slice: it (a) fixes the ONE bounded, auditable, non-STOP-AND-ASK, non-product-UI item — the stale `package.json` self-declaration — reframing the app accurately as the v1 local-first Mac desktop case-box client (per [[client-local-first]]) WHILE PRESERVING the honest gate-4 deferral (still a dev build; signing/notarization/public-distribution remain gate-4 user decisions); and (b) produces a gate-3 client release-readiness ASSESSMENT doc that enumerates every concrete gate-3 gap with its disposition (fixed here / separate bounded UI WI / gate-4 STOP-AND-ASK / bounded follow-up), and (c) records gate 3 as remaining **PARTIAL with a documented residual** — NOT cleared, NOT a go-live decision.
- Exact target files (future exec lane): `apps/lawbar-desktop/package.json` — the `description` FIELD ONLY (reframe fixture→product client + keep the gate-4 signing/distribution deferral honest; NO change to dependencies, version, scripts, `engines`, or the electron-builder `build` block) + a NEW `docs/release/gate3-client-release-readiness-00.md` (the assessment doc) + `docs/release/go-live-readiness-report.md` (the gate-3 evidence ROW ONLY — reflect the self-declaration fixed + point to the assessment; keep gate 3 PARTIAL-pending-gate-4) + `dev-memo/run/reviews/` (the exec lane's review artifact).
- Exact acceptance criteria: below.
- Exact out-of-scope (each mapped): the **overdue-deadline dashboard-banner** (brief §10 — "dashboard banner when any deadline is overdue or due within 7 days") is a MISSING product-UI feature → this WI PROPOSES it as a SEPARATE bounded `Type: UI` WI (needs a design artifact per [[UI-GATES]]); it is NOT built here. **Signing / notarization / public distribution / release posture** → gate 4 STOP-AND-ASK (USER); NOT touched. Product-grade error/empty-state hardening beyond the self-declaration → enumerated in the assessment as bounded follow-ups, NOT implemented here. NO gate-6 full-project audit; NO release-doc finalization; NO final GO/NO-GO.
- Essential refs: `docs/release/go-live-readiness-report.md` §1 gate 3 + gate 4 + §3 item 1; `docs/product/project-requirements-brief.md` §10 (deadlines/overdue) + §4/§16 (distribution/signing = STOP-AND-ASK); `apps/lawbar-desktop/package.json:6` (the stale self-declaration); `.claude/rules/client-local-first.md` (v1 = local-first Mac desktop app — the accurate framing).
- Exec-lane note (folded from review-plan-mr7nakgj-yvprnu): brief §10's deadline UX has TWO parts — a visible **overdue list** AND a **dashboard banner** (overdue or due within 7 days). The assessment MUST distinguish them: check whether the overdue LIST is already evidenced in the client (e.g. `viewMatterDeadlines.ts` / `renderer-deadline-urgency.test.mjs`) versus only the dashboard BANNER remaining, and disposition each accordingly (the banner is the item proposed as a separate Type:UI WI).
- Review questions: (1) Is the self-declaration reframe correct + honest — does it accurately describe the product client WITHOUT claiming signed/distributable (which is gate-4)? (2) Is scoping the package.json edit to the `description` field only (no deps/version/scripts/build-block) sufficient to avoid a dependency/packaging-config change? (3) Is the overdue-banner correctly deferred to a separate UI WI rather than built here? (4) Does the WI correctly leave gate 3 PARTIAL (documented residual, gate-4-dependent) rather than overclaiming a clear? (5) Is any gate-4 STOP-AND-ASK decision, gate-6 audit, or product-UI buildout smuggled in?

Type: IMPL
Scope: Advance M0 go-live **gate 3** (client release-hardening) by the smallest auditable, non-STOP-AND-ASK slice, WITHOUT clearing gate 3 (which depends on gate 4). The FUTURE execution lane: (a) corrects the stale `apps/lawbar-desktop/package.json` `description` field — the app self-declares "Token-compliance fixture only; NOT product UI; NOT signed; NOT for distribution", which is factually stale (the app is a substantial functional local-first case-box client: 12 renderer screens, 26 case-box IPC channels, electron main/preload, forms-T3 preview+DOCX). Reframe it accurately as the v1 local-first Mac desktop case-box client per `.claude/rules/client-local-first.md`, WHILE PRESERVING the honest release deferral (a dev build; code-signing / notarization / public distribution remain gate-4 USER decisions — do NOT claim signed or distributable). (b) Produces a client release-readiness ASSESSMENT at `docs/release/gate3-client-release-readiness-00.md` that inventories every concrete gate-3 gap and assigns each a disposition: the self-declaration (FIXED here); the overdue-deadline dashboard-banner brief §10 (PROPOSED as a separate bounded `Type: UI` WI with a design artifact — NOT built here); product-grade error/empty-state hardening (enumerated as bounded follow-ups); signing/notarization/public-distribution/release-posture (gate-4 STOP-AND-ASK, USER). (c) Updates ONLY the gate-3 evidence ROW of `docs/release/go-live-readiness-report.md` to reflect the self-declaration fixed + point to the assessment, KEEPING gate 3 **PARTIAL** (documented residual, pending gate 4 + the proposed UI WI). It changes NO renderer/UI source, NO electron-builder `build` block / signing / notarization / packaging config, NO dependency, NO schema/contract/persistence, NO OCR, NO Forms; it makes NO gate-4 decision, runs NO gate-6 audit, finalizes NO release docs, and makes NO go-live GO/NO-GO decision. If the exec lane finds the self-declaration cannot be corrected without touching deps/version/scripts/the build block, STOP and report. FORBIDDEN in the exec lane: any renderer/UI feature build (incl. the overdue banner — that is a separate UI WI); any electron-builder/signing/notarization/packaging-config edit; any dependency/lockfile change; any schema/migration/contract/persistence change; any OCR/Forms work; a gate-4/gate-6/release-doc-finalization/GO-NO-GO decision. If review asks to build the overdue banner or decide signing/distribution here, REJECT and report.
Source of truth: docs/release/go-live-readiness-report.md §1 gate 3 (PARTIAL — the self-declaration + product-hardening + overdue-banner + release-posture blockers) + gate 4 (STOP-AND-ASK signing/distribution) + §3 item 1; docs/product/project-requirements-brief.md §10 (overdue-deadline dashboard banner = v1 must-have) + §4/§16 (distribution/signing = STOP-AND-ASK); apps/lawbar-desktop/package.json:6 (the stale self-declaration — the one auditable fix target); .claude/rules/client-local-first.md (v1 = local-first Mac desktop app; the accurate framing; signing/cloud/distribution = deliberate deferrals); .claude/rules/autonomy.md (hard-stop list — signing/distribution/GO-NO-GO are user decisions) + staging-hygiene.md + loc-guardian.md + UI-GATES.md (a Type:UI WI needs a concrete design artifact)
Allowed files: apps/lawbar-desktop/package.json (the `description` FIELD ONLY — reframe fixture→product client, preserve gate-4 signing/distribution deferral; NO deps/version/scripts/engines/build-block change) + docs/release/gate3-client-release-readiness-00.md (NEW assessment doc the exec lane produces) + docs/release/go-live-readiness-report.md (the gate-3 evidence ROW ONLY) + dev-memo/run/reviews/ (the exec lane's own review artifact). NOTE: THIS authoring lane commits ONLY the queue governance (queue.md/linted/reviewed/governed) + reviews/queue-review-136.md — NO app/doc change. The future exec lane records its before/after evidence in the COMMIT MESSAGE + its review artifact; it does NOT re-stage this lane's queue governance.
Forbidden files: apps/lawbar-desktop/renderer/** (NO product-UI change — the overdue banner is a SEPARATE Type:UI WI), apps/lawbar-desktop/src/** + electron/** (NO app/IPC/handler source change), apps/lawbar-desktop/package.json dependencies/devDependencies/version/scripts/engines/build (electron-builder/signing/packaging config — NO change; description field is the ONLY allowed edit), apps/lawbar-desktop/entitlements* + any signing/notarization/electron-builder config, package-lock.json / any package lockfile, services/** (NO OCR/persistence/contract change), docs/contracts/, services/*/src/sqlite/schema.ts (CURRENT_SCHEMA_VERSION stays 12), migrations/, native/, dev-memo/run/intake/ (raw samples — never staged), docs/adr/ + docs/product/ (READ-ONLY evidence — NO brief/ADR edit), docs/release/ OTHER than the NEW gate3-client-release-readiness-00.md + the gate-3 row of go-live-readiness-report.md, scripts/ + .claude/ + .github/; NO renderer/UI feature build; NO signing/notarization/distribution decision; NO dependency; NO schema/migration/contract/persistence change; NO OCR; NO Forms T4/T5; NO gate-6 audit; NO release-doc finalization; NO final GO/NO-GO decision. Push is NEVER automatic (autonomy hard-stop): local commit governance is the default; any branch push + PR open happens ONLY under explicit per-invocation user authorization; NO merge; NO branch delete
Gates: IMPL release-hardening lane (MEDIUM risk — a metadata honesty fix + an assessment doc; NOT security/persistence, NOT a signing/distribution decision). (1) THIS authoring lane: broker /cc-suite:review-plan on this queue WI, Path 1, retrievable, then govern. (2) The FUTURE exec lane: apply the description reframe + write the assessment; run npm --prefix apps/lawbar-desktop test (the desktop suite must stay green — a description-only package.json edit must not break the build/tests; if it somehow does, STOP); broker /cc-suite:review-plan or /cc-suite:audit on the diff per convention (docs + a one-field metadata change = low-code; review-plan on the produced assessment is the convention gate, mirroring the readiness-refresh + gate-10 EVIDENCE precedent). (3) scripts/workflow/check-contract-integrity.sh exit 0; scripts/workflow/check-queue.sh PASS (both lanes). Forbidden-scope scan before commit: CURRENT_SCHEMA_VERSION EQUALS 12; no renderer/src/electron/build-block/dependency/signing change; the package.json diff touches ONLY the description string; no gate-4/gate-6/GO-NO-GO decision; no raw sample. STOP-points: the self-declaration cannot be fixed without a deps/build-block change → STOP; review asks to build the overdue banner or decide signing/distribution → REJECT; any forbidden file would change → STOP.
Acceptance criteria: check-queue and check-contract-integrity PASS and CURRENT_SCHEMA_VERSION EQUALS 12 and the diff CONTAINS only the package.json description field + the new assessment doc + the gate-3 evidence row + the review artifact and no renderer/src/electron/dependency/signing/schema/contract change appears. The `apps/lawbar-desktop/package.json` diff must touch ONLY the `description` string (git diff shows one changed field) and the new description must NOT claim the app is signed or distributable (the gate-4 deferral is preserved). The assessment doc must RENDER: each concrete gate-3 gap with its disposition (self-declaration FIXED; overdue-banner → separate Type:UI WI; product-hardening follow-ups; signing/distribution → gate-4 STOP-AND-ASK), and must STATE gate 3 remains PARTIAL (documented residual, gate-4-dependent) and MUST NOT assert go-live. The gate-3 evidence row must be updated to reflect the fix + point to the assessment while keeping PARTIAL. The desktop test suite (npm --prefix apps/lawbar-desktop test) must PASS. review-plan (this lane) must RETURN READY.
Risk flags: MEDIUM (release/client posture; but a metadata honesty fix + an assessment doc — no product-UI build, no security/persistence, no signing decision). Care points: (a) NO OVERCLAIM — the description reframe must NOT claim signed/distributable (gate 4 owns that); gate 3 stays PARTIAL, NOT cleared; (b) NO PRODUCT-UI BUILD — the overdue banner is a SEPARATE proposed Type:UI WI, not built here; (c) DESCRIPTION-FIELD-ONLY — the package.json edit is one field; deps/version/scripts/build-block untouched (else it becomes a dependency/packaging change → out of scope); (d) STOP-AND-ASK INTACT — no signing/notarization/distribution decision; (e) GO-LIVE INDEPENDENCE — advancing gate 3's auditable slice does NOT imply go-live; the final verdict + the three STOP-AND-ASK hard-stops remain the user's; (f) BRIEF/ADR READ-ONLY — the brief + ADRs are evidence, not edit targets. If any cannot hold, STOP and report.
Depends on: none
Commit boundary: TWO separate lanes. (1) THIS authoring lane commits ONLY the queue governance (queue.md/linted/reviewed/governed) + reviews/queue-review-136.md — no app/doc change, no implementation. (2) The FUTURE execution lane commits ONLY the package.json description reframe + the new docs/release/gate3-client-release-readiness-00.md + the gate-3 evidence-row update to go-live-readiness-report.md + its own review artifact under dev-memo/run/reviews/, records its before/after evidence in the COMMIT MESSAGE, and does NOT re-stage this lane's queue governance. NO renderer/src/electron/build-block/dependency/signing/schema/contract/OCR/Forms file in either lane; NO staging of dev-memo/run/intake/ or untracked residue; push is NEVER automatic (autonomy hard-stop) — local commit governance is the default, any branch push + PR open happens ONLY under explicit per-invocation user authorization; no merge; no branch delete. THIS authoring lane implements NOTHING — it only governs the gate-3 release-hardening WI.
