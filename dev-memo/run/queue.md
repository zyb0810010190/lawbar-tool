## WI-EVW4: generic Lawbar workflow commands (/feature-workflow + /fix-issue)
Type: WORKFLOW
Scope: Create two generic Lawbar workflow-orchestration command files that chain the existing least-privilege agents (planner -> test-designer -> implementer -> reviewer -> release-steward). /feature-workflow is generic feature orchestration; /fix-issue is issue-remediation orchestration (NOT a bypass around planning/review). Slash-command markdown only; no product code, no Evidence-specific implementation, no hooks/native/harness/apps changes, no Evidence behavior. The commands REFERENCE the agents and existing governance; they wire nothing new beyond the two .md files.
Source of truth: docs/adr/ADR-evidence-m0-xiaolai-workflow-composition.md (EVW-00); dev-memo/plan-batch-casebox-evidence-workflow-port-00.md section 4.2 (Lawbar .claude plan command list); .claude/agents/*.md (the six least-privilege agents committed 0309e48); AGENTS.md section "Evidence-Genie M0 workflow composition"; .claude/rules/cc-suite.md, .claude/rules/autonomy.md, .claude/rules/staging-hygiene.md, .claude/rules/execution-discipline.md (the governance these commands preserve); existing .claude/commands style (frontmatter description plus optional allowed-tools).
Allowed files: .claude/commands/feature-workflow.md, .claude/commands/fix-issue.md
Forbidden files: AGENTS.md, CLAUDE.md, GEMINI.md, .claude/rules/**, .claude/hooks/**, .claude/agents/**, .claude/settings.json, .claude/commands/evidence-workflow.md, .claude/commands/evidence-geometry-gate.md, services/**, docs/contracts/**, apps/**, native/**, package.json
Gates: both command files have valid frontmatter (description, and allowed-tools if used); each command documents that test design precedes implementation, no autonomous git commit, no push, exact staging only when explicitly instructed, stop on guard denial, the least-privilege sequence (planner/test-designer/implementer/reviewer/release-steward only on explicit human request), queue-governance preservation (no self-authorizing queue edits; governed changes only via review/lint/govern), hard-versus-soft gate classification on reporting, and the workflow-gets-gates-green versus human-decides-commit distinction; /fix-issue explicitly does NOT authorize bypassing planning/review; scripts/workflow/check-contract-integrity.sh exits 0; npm --prefix apps/lawbar-desktop test passes (610/610); scripts/workflow/check-gates.sh returns GATES OK
Acceptance criteria: .claude/commands/feature-workflow.md and .claude/commands/fix-issue.md exist with valid frontmatter; grep shows each mentions test design before implementation, no autonomous commit and no push, exact staging, queue governance, and hard/soft gate classification, and lists the planner/test-designer/implementer/reviewer/release-steward sequence; grep shows /fix-issue states it does not bypass planning or review; check-contract-integrity.sh exits 0; npm desktop test output shows pass 610 fail 0; check-gates returns GATES OK; the committed diff touches only the two command files
Risk flags: generic orchestration documentation only (not Evidence-specific); NOT an autonomy hard-stop; NOT high-risk per cc-suite (no persistence/security-code/cloud/public-API/runtime/migration/LLM); the commands preserve, never bypass, the commit and queue-governance controls
Depends on: WI-EVW3 (six .claude/agents committed 0309e48)
Commit boundary: one local commit -- the two .claude/commands files only, staged by exact path; no push
