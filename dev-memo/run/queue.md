## WI-FORMS-T3-S0-SCHEMA-ADR-00: design the T3 lawyer-entered catalog fields schema ADR (Type: PLAN)
Type: PLAN
Scope: Documentation / design / governance ONLY — no product code, no native source, no tests, no package manifests, NO schema/contract/validator/generated-type edit, no migration file, NO CURRENT_SCHEMA_VERSION change, no fixtures, no IPC, no custody/marker, no JS shim, no A8, no renderer, no DOCX/PDF generation, NO forms implementation, NO T4/T5 work. CREATE ONE new ADR `dev-memo/adr-forms-t3-s0-schema.md` (the only PRODUCT/DESIGN file changed; the `dev-memo/run/queue.*` + `reviews/queue-review-121.md` governance artifacts are also modified per this WI's Commit boundary) that DESIGNS (does not implement) the S0 schema change the T3 plan (`dev-memo/plan-forms-t3-evidence-catalog-00.md` §3/§4) marked blocking: the missing lawyer-entered persisted truth 证据名称 + 证明内容 (evidence item) and 提交人诉讼地位 (matter), plus the two plan-review Low decisions (lawyer-controlled display order; submitter-selection rule). The ADR MUST record: (1) problem — current schema cannot honestly derive these from notes/filename/Party.role (manual-truth posture); (2) required persisted data incl. the verdict that existing `exhibit_page_range` IS sufficient for physical 页码 (verbatim passthrough, null renders explicit blank); (3) scope boundary — internal trial-review only, no court filing, no seal/custody/A8, no renderer decision, no T4/T5 schema; (4) candidate schema shape — Option A (RECOMMENDED): payload-only additive optional contract properties (`evidence_title`, `proof_statement`, `display_order` on evidence; `litigation_position` enum plaintiff|defendant on matter), NO new SQLite columns/DDL because payload_json is canonical and columns are lifted only for indexes; Option B (deferred): lifted display_order column + index via a V13 additive ALTER per the V12 precedent; (5) migration implications — Option A requires NO CURRENT_SCHEMA_VERSION increment, NO migration, NO backfill; existing rows stay valid; repository-layer validation rules per the no-cross-column-CHECK convention; (6) contract/API implications — DTOs gain optional fields; T3CatalogModel is a SIBLING of the A10-T6 canonical model MIRRORING its deterministic-serialization pattern (stableStringify is module-private; S1 duplicates the pattern or extracts a shared helper in its own governed WI, not modifying a10CanonicalExportModel.ts by default); 卷X页Y remains supporting-only and never replaces 页码; (7) later-implementation test obligations (contract/migration-noop/validation/row-construction/missing-field/ordering/no-T4-T5-leakage/no-raw-sample-fixtures); (8) explicit non-decisions (no implementation of any kind this lane). The raw sample files under `dev-memo/run/intake/**` remain INPUT ONLY — untracked and unstaged. If review asks for schema implementation, REJECT as out of scope and report; ADR-insufficiency findings are fixed forward in the ADR/governance files only.
Source of truth: dev-memo/plan-forms-t3-evidence-catalog-00.md (parent plan §3 blocked-pending-S0 verdict + §4 S0 decision list, BINDING unless disproven by repo evidence); dev-memo/forms-spec-a10-t3-t5-sample-adr-00.md §H DR-00 (binding product decisions) + §E (non-decisions preserved); services/case-box-persistence/src/sqlite/schema.ts (CURRENT_SCHEMA_VERSION=12, V7 evidence DDL, V12 additive precedent, canonical-source rule — READ-ONLY); docs/contracts/case-box-contract/schemas/case-box-evidence-item.schema.json + case-box-matter.schema.json (current contract state — READ-ONLY); apps/lawbar-desktop/src/caseBox/export/a10CanonicalExportModel.ts (READ-ONLY); .claude/rules/{evidence-genie,cc-suite,autonomy,execution-discipline,staging-hygiene}.md
Allowed files: dev-memo/adr-forms-t3-s0-schema.md (NEW ADR), dev-memo/run/queue.md, dev-memo/run/queue.linted, dev-memo/run/queue.reviewed, dev-memo/run/queue.governed, dev-memo/run/reviews/queue-review-121.md
Forbidden files: dev-memo/run/intake/** (raw sample PDF/DOCX — INPUT ONLY; MUST NOT be staged/committed/edited/moved/deleted), apps/** (NO application/renderer/electron/test/package change), native/** (NO native source change), services/** (NO persistence/schema change; schema.ts READ-ONLY; NO CURRENT_SCHEMA_VERSION change; NO migration/DDL), docs/contracts/** (NO contract/validator/generated-type/schema-JSON change — READ-ONLY inputs), docs/adr/** + docs/reference/** + docs/product/** (READ-ONLY), dev-memo/plan-forms-t3-evidence-catalog-00.md + dev-memo/forms-spec-a10-t3-t5-sample-adr-00.md (READ-ONLY this lane — the ADR cites them, does not amend them), dev-memo/evidence-a10-closeout-00.md + dev-memo/deferred-audit-findings.md (READ-ONLY), scripts/** + .claude/** + .github/** + migrations/**, dev-memo/run/evidence/** (gitignored custody marker/ledger — never touched/staged), committed keys, dev-memo/run/reports/** + dev-memo/run/override-reason.md + dev-memo/run/autonomous-next.md (untracked run-state — never staged/edited), Evidence-Genie-M0-Developer-Handover.md + xiaolai-dev-workflow-study.md (root intake — READ-ONLY); NO A8; NO T3/T4/T5 forms implementation; NO schema/contract mutation; NO DOCX/PDF renderer; NO custody/marker/seal; NO JS shim; push limited to this WI's scoped branch + PR open; NO merge; NO branch delete
Gates: broker /cc-suite:review-plan REQUIRED (FULL review — this ADR designs a persistence/schema change, the high-risk category, even though the lane itself is docs-only) — confirm: the ADR's payload-only Option A is sound under the canonical-source rule (or a lifted column/DDL is genuinely forced now); nullability/backfill/validation choices keep existing rows valid; the ADR authorizes NO implementation and makes NO renderer decision; T4/T5 stay unexpanded; §E non-decisions intact; 卷X页Y supporting-only preserved; exhibit_page_range-sufficiency verdict correct; raw samples not staged. STOP-points: (1) if the ADR misstates the schema/contract state -> fix-forward in the ADR; (2) if review requests schema/code implementation -> REJECT as out of scope and report; (3) if any non-docs file (or the raw samples) would be staged -> STOP (scope drift). A separate /cc-suite:audit+verify chain is NOT required for this docs-only ADR lane unless review-plan findings require it (per .claude/rules/cc-suite.md the audit scope is the diff, which is documentation/governance only); if review-plan requires an audit, run it before commit. After: changed files are documentation/governance only; `scripts/workflow/check-contract-integrity.sh` exits 0; `scripts/workflow/check-queue.sh` PASSES; broker review-plan READY. NO full desktop suite (docs-only). Batch is 1/3 (marker 545adec); NOT due — no closeout this lane.
Acceptance criteria: `dev-memo/adr-forms-t3-s0-schema.md` EXISTS and CONTAINS: the problem statement (missing lawyer-entered truth; no dishonest derivation from notes/filename/role); the required-data table incl. exhibit_page_range-is-sufficient for 页码; the scope boundary (internal review; no court filing/seal/custody/A8; no renderer decision; no T4/T5 schema); candidate shape Option A payload-only additive optional properties RECOMMENDED with NO DDL + NO CURRENT_SCHEMA_VERSION increment and Option B lifted-column deferred; migration implications (no migration/backfill; existing rows valid; repository-layer validation rules); contract/API implications (optional DTO fields; T3CatalogModel sibling of A10-T6; 卷X页Y supporting-only); the later-implementation test obligations list; the explicit non-decisions list. The committed diff CONTAINS ONLY the new ADR + governance artifacts and does NOT stage `dev-memo/run/intake/**` or any code/native/schema/contract/test/fixture/IPC/manifest/migration file; CURRENT_SCHEMA_VERSION remains 12 and untouched; `check-contract-integrity` PASSES (exit 0); `check-queue` PASSES; broker review-plan returns READY; review REJECTS any implementation request as out of scope.
Risk flags: LOW-RISK documentation lane DESIGNING a HIGH-RISK future change — ONE new ADR + governance artifacts only; NO code/native/schema/contract/test/fixture/IPC/manifest/migration change, NO A8/forms/custody/JS-shim, NO staging of the raw sample files. Load-bearing care points: (a) ACCURACY — the payload-canonical / columns-only-for-indexes claim and the V12 additive precedent must match schema.ts exactly; the no-version-bump conclusion depends on it; (b) DESIGN-ONLY — the ADR proposes, the later governed high-risk WI implements; (c) GATING PRESERVED — T4/T5 gated, §E non-decisions intact, renderer decision separate; (d) INPUT-ONLY SAMPLES — never stage `dev-memo/run/intake/**`. If any cannot hold, STOP and report. Push limited to this WI's scoped branch + PR open; no merge; no branch delete.
Depends on: none
Commit boundary: governance (mark-reviewed + govern) run STANDALONE before the commit (never bundled with git commit). ONE documentation commit — dev-memo/adr-forms-t3-s0-schema.md + governance artifacts (queue.md/.linted/.reviewed/.governed + reviews/queue-review-121.md), staged by exact path; NO apps/native/services/contracts/schema/test/fixture/manifest/IPC/migration file; NO staging of dev-memo/run/intake/** (raw samples stay untracked); NO marker/evidence/key/report file; push of the scoped WI branch + PR open authorized by the user's Stage 1 directive; no merge; no branch delete.
