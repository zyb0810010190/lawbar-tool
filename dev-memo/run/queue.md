## WI-A3-PAGE-T1: DocumentPage schema V9 (case-box-persistence; A0.7-gated)
Type: IMPL
Requires-A07: yes
A07 custody mode: 9b. Human runs marker mint + A0.7-gated check-gates directly; agent does not receive the HMAC key. The implementation commit is BLOCKED until human-run gated verification reports PASS (A07_REQUIRED=1 check-a07-gate + A07_REQUIRED=1 check-gates both PASS with LAWBAR_A07_MARKER_HMAC_KEY in the human's shell, and no marker/ledger/key/evidence-run staged or committed). The agent never exports/echoes/commits/mints the key, never weakens or drops Requires-A07, never bypasses the gate.
Scope: Add SCHEMA-ONLY version V9 to services/case-box-persistence for the DocumentPage page-identity owner, per A3-PAGE-00 + A3-SCHEMA-00 §6 (reconciled, no-FK). The FIRST foundation implementation lane: page identity BEFORE geometry (A3-PAGE-T2) and anchors (A3-T1-IMPL). NO DocumentPageGeometry, NO anchor/link tables, NO resolver/status, NO export, NO UI/product behavior, NO production evidence ingestion, NO encryption/key management, NO new dependency, NO package.json/package-lock change, NO SQLite FK constraints, NO marker/key/gate change. Changes confined to services/case-box-persistence/src/sqlite/schema.ts (add DDL_STATEMENTS_V9 + register [9, DDL_STATEMENTS_V9] in DDL_BY_VERSION + bump CURRENT_SCHEMA_VERSION 8->9, via the existing idempotent applySchema convention; one BEGIN/COMMIT per the existing migration loop; forward-only, no down-migration, no destructive reset) and services/case-box-persistence/tests/hardening-schema.test.mjs (extend the EXISTING in-script schema test with V9 cases — no package.json test-list change).
Schema requirements (0-based page index, confirmed by the user 2026-06-24): create table case_box_document_pages with columns: id TEXT PRIMARY KEY; tenant_id TEXT NOT NULL; matter_id TEXT NOT NULL; document_id TEXT NOT NULL (app-layer invariant -> case_box_documents.id; NO SQLite FK); physical_page_index INTEGER NOT NULL CHECK (physical_page_index >= 0) (0-based — first physical page = 0; the human-facing citation/page LABEL is a separate concern kept in payload_json, never conflated with the machine index); created_at TEXT NOT NULL COLLATE BINARY; payload_json TEXT NOT NULL (the canonical DocumentPage record — citationVolume?/citationPageLabel/citationPageSortKey?/isCitable/note? live here per the case-box canonical-source rule; only identity/scoping fields are lifted to columns); UNIQUE (document_id, physical_page_index) (the canonical page identity). Add one matter/document-scoped index (e.g. idx_case_box_document_pages_by_document on (tenant_id, matter_id, document_id, physical_page_index)) following the existing index convention. NO geometry columns (no resolved box/bounds/rotation/page_ratio), NO viewport/screen columns, NO anchor/link tables, NO FK constraints. Record the 0-based decision in a code comment + the test names.
Required behavior: applySchema applies V9 idempotently (no-op when already at 9); upgrades from V8 without dropping existing data; schema_version row becomes 9; a future-version DB still refuses; the case_box_document_pages table exists with the columns above; physical_page_index = 0 is ACCEPTED; a negative physical_page_index is REJECTED (CHECK); uniqueness of (document_id, physical_page_index) is enforced; no geometry/viewport columns exist; no anchor/link tables are created; no SQLite FK constraints are introduced.
Source of truth: docs/adr/ADR-evidence-a3-page-geometry-foundation.md (A3-PAGE-00 — DocumentPage owner fields decision 5; no-FK app-layer invariant; no viewport; no production evidence); docs/adr/ADR-evidence-a3-schema-persistence-contract.md (A3-SCHEMA-00 + §6 reconciliation); docs/adr/ADR-evidence-a3-persistence-substrate.md (A3-DB-00 — reuse case-box-persistence; encryption hard stop stands); Evidence-Genie-M0-Developer-Handover.md (root intake; READ-ONLY) §10 DocumentPage shape, §33 citation identity from DocumentPage; services/case-box-persistence/src/sqlite/schema.ts (the existing applySchema/DDL_BY_VERSION/schema_version convention + CHECK-constraint + column style + no-FK design at :14/:134) + tests/hardening-schema.test.mjs (the schema-test pattern to extend); docs/adr/ADR-evidence-a07-key-custody-operating-model.md (A07-KEY-00 — custody mode 9b). User decision 2026-06-24: physical_page_index is 0-based (CHECK >= 0).
Allowed files: services/case-box-persistence/src/sqlite/schema.ts (DDL_STATEMENTS_V9 + DDL_BY_VERSION registration + CURRENT_SCHEMA_VERSION bump + a version-9 comment), services/case-box-persistence/tests/hardening-schema.test.mjs (extend with V9 cases), dev-memo/** (queue/governance/review/study/report artifacts)
Forbidden files: services/case-box-persistence/package.json + package-lock.json (NO dependency/test-list change), any other services/** file (NO change), docs/contracts/** (contract package — NO change), apps/**, native/**, scripts/workflow/** (NO gate/marker change), .claude/**, .github/**, AGENTS.md, CLAUDE.md, GEMINI.md, committed dev-memo/run/evidence/** marker/ledger/key/evidence-run files, committed keys, Evidence-Genie-M0-Developer-Handover.md (root intake), xiaolai-dev-workflow-study.md (root intake), dev-memo/run/override-reason.md (untracked residue — never staged); NO DocumentPageGeometry, NO anchor/link tables, NO geometry/viewport columns, NO SQLite FK constraints, NO new dependency, NO package.json change, NO resolver/status/export/UI/product, NO production evidence ingestion, NO encryption/key management, NO marker/key/gate change, NO migration beyond schema V9
Gates: broker /cc-suite:review-plan REQUIRED before impl (confirm the V9 DocumentPage schema is correct + minimal; confirm 0-based index; confirm no-FK app-layer-invariant approach; if review-plan disagrees on the index base OR asks for a broader page-label/citation model, STOP and report; if review-plan asks for a dependency, encryption/key-mgmt, DocumentPageGeometry, anchors/links, UI/product, FK constraints, or marker/key/gate changes, STOP and ask); broker /cc-suite:audit + /cc-suite:verify after impl AND after the human-run gated PASS; A0.7-gated under custody 9b — the implementation commit is BLOCKED until the human reports A07_REQUIRED=1 check-a07-gate PASS + A07_REQUIRED=1 check-gates PASS (human shell holds the key) with no marker/ledger/key/evidence-run staged or committed; agent-side (non-secret): npm --prefix services/case-box-persistence test passes (incl. the new V9 hardening-schema cases + all existing tests green); scripts/workflow/check-contract-integrity.sh exits 0; npm --prefix apps/lawbar-desktop test 610/610; npm --prefix native/evidence-core test 38/38; the A0.7 gate is intact (A07_REQUIRED=1 with no marker/key fails closed; not-required passes); the agent's own check-gates correctly FAILS CLOSED at the A0.7 gate because the governed queue declares Requires-A07: yes and the agent has no key (expected under 9b — the human's gated run is authoritative); no package.json/dependency change; no DocumentPageGeometry/anchor/link/geometry/viewport/FK; no apps/native/scripts/.claude/.github change; no committed marker/ledger/key/evidence-run
Acceptance criteria: services/case-box-persistence/src/sqlite/schema.ts adds DDL_STATEMENTS_V9 (case_box_document_pages + index) registered as [9, DDL_STATEMENTS_V9] with CURRENT_SCHEMA_VERSION = 9; services/case-box-persistence/tests/hardening-schema.test.mjs PASSES new V9 cases proving: V9 applies from empty; V9 upgrades from V8 without dropping existing data; schema_version becomes 9; case_box_document_pages exists with the expected columns; physical_page_index = 0 ACCEPTED; negative physical_page_index REJECTED; UNIQUE(document_id, physical_page_index) enforced; NO geometry columns; NO viewport/screen columns; NO anchor/link tables; NO SQLite FK constraint introduced; npm --prefix services/case-box-persistence test exits 0 (all existing + new tests green); scripts/workflow/check-contract-integrity.sh exits 0; npm --prefix apps/lawbar-desktop test 610/610; npm --prefix native/evidence-core test 38/38; no package.json/package-lock change; the human-run A0.7-gated verification (custody 9b) reports PASS before the implementation commit; broker review-plan READY + audit no Critical/High/Medium + verify ALL CLOSED; the committed diff is confined to services/case-box-persistence/src/sqlite/schema.ts + services/case-box-persistence/tests/hardening-schema.test.mjs + dev-memo/** governance artifacts
Risk flags: PERSISTENCE / MIGRATION IMPL — A0.7-DEPENDENT (Requires-A07: yes), custody mode 9b — the implementation commit is BLOCKED until the human runs the A0.7-gated check-gates with the HMAC key and reports PASS; the agent never receives/echoes/commits/mints the key, never weakens/drops Requires-A07, never bypasses the gate. Schema-only V9 DocumentPage: NO DocumentPageGeometry/anchor/link, NO geometry/viewport columns, NO FK (app-layer invariant per the case-box no-FK convention), NO new dependency, NO package.json change, NO UI/product, NO production evidence ingestion, NO encryption/key management. Migration is forward-only (no down-migration; rollback via git revert + a new forward version, never a destructive reset). broker review-plan + audit + verify REQUIRED. Must not weaken any Evidence invariant nor the A3-DB-00 encryption hard stop. No push, no merge.
Depends on: none
Commit boundary: governance commit separate (dev-memo/run/queue.md + queue.linted/reviewed/governed + dev-memo/run/reviews/queue-review-074.md); one implementation commit -- services/case-box-persistence/src/sqlite/schema.ts + services/case-box-persistence/tests/hardening-schema.test.mjs, staged by exact path, ONLY after human-run gated PASS + broker audit/verify; no push; no merge
