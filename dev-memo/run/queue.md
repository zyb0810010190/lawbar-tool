## WI-A3-LINK-D1-DESIGN-REV-00: revise D1 closure ADR for a feasible Electron test seed (Type: PLAN)
Type: PLAN
Source design: docs/adr/ADR-evidence-a3-link-d1-roundtrip-closure.md (D1-ROUNDTRIP-00, the ADR being revised); dev-memo/plan-casebox-ipc-impl-01.md (the ESM/app.evaluate limitation record); apps/lawbar-desktop/electron/main.ts:137 (the tarball-PoC env-gated globalThis hook precedent)
Scope: DESIGN/DOC-ONLY. Revise the merged D1 ADR (docs/adr/ADR-evidence-a3-link-d1-roundtrip-closure.md) to CORRECT a false feasibility assumption discovered at WI-A3-LINK-D1-ROUNDTRIP-T1 review-plan (`review-plan-mqxfalb5-h0mkf3`, 2026-06-28). The ADR's Option-2 seed mechanism — seed `case_box_anchors`/pages/geometries by having Playwright `electronApp.evaluate()` directly `require`/dynamic-`import()` `better-sqlite3` inside the Electron main process — is INFEASIBLE: the desktop main is ESM (`apps/lawbar-desktop/package.json` `"type":"module"`, so no `require`), and dynamic `import()` inside the `app.evaluate` VM is empirically falsified ("A dynamic import callback was not specified") per `dev-memo/plan-casebox-ipc-impl-01.md` §"ESM app.evaluate limitation". The revision MUST: (1) add a "Feasibility correction" section stating exactly why the direct-`app.evaluate`-import seed is infeasible (ESM main; no `require`; dynamic-import-in-VM blocked; cite the plan-01 record); (2) REPLACE the selected closure mechanism with a DEFAULT-OFF, env-gated `globalThis` TEST HOOK installed in `electron/main.ts` whose `await import(...)` runs in the main process's NATIVE ESM loader (NOT the `app.evaluate` VM), invoked only by the Electron integration test driver via `app.evaluate(() => globalThis.__<hook>(...))` — modeled on the proven tarball-PoC `LAWBAR_TARBALL_POC_TEST_HOOK` pattern (`electron/main.ts:137`); (3) resolve the SOURCE CONFLICT by explaining why this env-gated test hook is NOT the previously-rejected "Option 4 dev-conditional product seed IPC backdoor": it is not an IPC channel, not preload/contextBridge-exposed, not renderer-reachable, has no user-triggerable path, is test-harness-only, default-off (fail-closed), and inert/absent in production launches — materially different from a product IPC channel; (4) keep Option 1 (product `casebox:anchor:*` IPC) REJECTED for D1 (A0.7-geometry work, evidence-genie #3/#4) and keep Option 3 (defer) as the not-preferred fallback; (5) define containment requirements for the future hook (named test-only env var; fail-closed default; no production enablement; no `contextBridge`/preload/IPC/renderer-global/UI exposure; expose only the minimal seed op for D1; synthetic/no-real-data fixtures; must not bypass audited production user flows except for fixture setup); (6) update the future impl WI definition (WI-A3-LINK-D1-ROUNDTRIP-T1, Type TEST) so its allowed scope MAY include a bounded `electron/main.ts` env-gated hook + one Electron integration test under `apps/lawbar-desktop/tests/**` + additive `package.json` test registration + the D1 closure flip (after pass); note that adding the hook makes that future lane a small PRODUCT change, so it is likely A0.7-commit-gated; (7) keep/realign the D1-closure acceptance criteria (seed matter/document/evidence/pages/geometries/anchors via the test-only hook → drive real `casebox:link:*` round-trip → create ok; list/resolve sees the link; status `valid`; export grouping proves the link is seen (`byFlag.CLEAN===1`, `exportFlag===null`); synthesized `卷X页Y` text if applicable; negative scope cases fail safely; no evidence content leaks). Also update dev-memo/deferred-audit-findings.md to CLARIFY the REVISED planned closure path on the LINK-IPC-T1-D1 row — D1 MUST NOT be marked closed. NO implementation code, NO electron/main.ts hook implementation, NO test implementation in this lane.
Source of truth: docs/adr/ADR-evidence-a3-link-d1-roundtrip-closure.md (the ADR revised); dev-memo/plan-casebox-ipc-impl-01.md §"ESM app.evaluate limitation" + "globalThis test hook pattern" (the empirical limitation + the sanctioned env-gated hook pattern); apps/lawbar-desktop/electron/main.ts:137-149 (READ-ONLY — the tarball-PoC `LAWBAR_TARBALL_POC_TEST_HOOK` globalThis hook precedent); apps/lawbar-desktop/package.json (`"type":"module"`; asarUnpack better-sqlite3); apps/lawbar-desktop/tests/casebox-ipc.electron.test.mjs (the launchPackaged + window/app.evaluate harness); services/case-box-persistence/src/sqlite/{linkStatusResolverQueries,exportCitationQueries,linkRepoQueries,schema}.ts (READ-ONLY — the resolver valid ladder + citation identity + seed-table DDL); .claude/rules/{cc-suite,autonomy,execution-discipline,security-boundary,evidence-genie,client-local-first,staging-hygiene}.md; dev-memo/deferred-audit-findings.md (LINK-IPC-T1-D1)
Allowed files: docs/adr/ADR-evidence-a3-link-d1-roundtrip-closure.md, dev-memo/deferred-audit-findings.md, dev-memo/run/queue.md, dev-memo/run/queue.linted, dev-memo/run/queue.reviewed, dev-memo/run/queue.governed, dev-memo/run/reviews/queue-review-099.md
Forbidden files: apps/lawbar-desktop/** (NO electron/main.ts hook impl, NO test impl, NO renderer/preload/src change — this lane DESIGNS the hook, a future WI implements it), services/** (NO persistence/schema change), services/case-box-persistence/src/sqlite/schema.ts (NO schema/`CURRENT_SCHEMA_VERSION` change), docs/contracts/** (NO contract change), docs/adr/*.md EXCEPT docs/adr/ADR-evidence-a3-link-d1-roundtrip-closure.md (NO other ADR edit), dev-memo/design/** (ADR-class decision; no design-artifact edit), package.json + package-lock.json + dist-tarballs/** + native/** (NO manifest/dependency/native/lockfile change), .claude/**, .github/**, AGENTS.md, CLAUDE.md, GEMINI.md, scripts/workflow/**, migrations/**, committed dev-memo/run/evidence/** marker/ledger/key/evidence-run files, committed keys, dev-memo/run/reports/** (untracked run-state — never staged), dev-memo/run/override-reason.md (untracked residue — never staged), Evidence-Genie-M0-Developer-Handover.md + xiaolai-dev-workflow-study.md (root intake — READ-ONLY); NO anchor IPC, NO product/dev-conditional seed IPC, NO electron/main.ts hook implementation, NO test implementation, NO marking D1 closed, NO push/merge/branch-delete
Gates: broker /cc-suite:review-plan REQUIRED before the design-revision commit (the ADR governs eventual test-harness/security work) — it MUST confirm: the feasibility correction accurately states why the direct-app.evaluate-import seed is infeasible; the env-gated `globalThis` test-hook replacement is sound + adequately contained (default-off; native-loader; no IPC/preload/contextBridge/renderer-global/UI exposure; not renderer-reachable; inert in production); the source-conflict resolution (test hook != rejected product-IPC backdoor) is convincing; the future-WI scope + containment + D1-closure criteria are concrete + testable; D1 is NOT marked closed. STOP-points: if review-plan says the ADR revision must SPLIT into a separate security ADR for test hooks (stop-point 1); if review-plan REJECTS the env-gated globalThis hook as unsafe even with containment (stop-point 2); if any implementation change is required to make the design coherent (stop-point 3) — in each, STOP and report. If review-plan returns NEEDS-FIX, fix-forward (doc edits) and re-review; if it stays NEEDS-FIX on a design-split/safety basis, STOP. After authoring: broker /cc-suite:audit + /cc-suite:verify on the diff (no Critical/High/Medium open before commit); scripts/workflow/check-contract-integrity.sh exits 0. No A0.7 custody (DOC-only; no geometry/marker/product code).
Acceptance criteria: docs/adr/ADR-evidence-a3-link-d1-roundtrip-closure.md contains (a) a "Feasibility correction" section stating the direct-`app.evaluate` dynamic-import/`require` seed is infeasible (ESM main; no require; dynamic-import-in-VM blocked) citing dev-memo/plan-casebox-ipc-impl-01.md; (b) a revised decision selecting the DEFAULT-OFF env-gated `globalThis` test hook in electron/main.ts (native-loader import) as the seed mechanism; (c) an explicit source-conflict resolution explaining why that hook is NOT the rejected product-IPC backdoor (not IPC/preload/contextBridge/renderer-reachable; default-off; test-only; inert in production); (d) hook containment requirements; (e) an updated WI-A3-LINK-D1-ROUNDTRIP-T1 definition whose allowed scope includes a bounded electron/main.ts env-gated hook + the Electron test + additive package.json registration + the post-pass D1 flip, noting likely A0.7-commit-gating; (f) the realigned D1-closure acceptance criteria; dev-memo/deferred-audit-findings.md LINK-IPC-T1-D1 shows the REVISED planned-closure path and remains `Status | open` (NOT closed); scripts/workflow/check-contract-integrity.sh exits 0; CURRENT_SCHEMA_VERSION remains 12; no apps/services/schema/contract/dependency/manifest file changed; broker review-plan READY + audit no Critical/High/Medium + verify ALL CLOSED; committed diff confined to Allowed files.
Risk flags: DESIGN/DOC-ONLY revision of a merged ADR, HIGH-RISK-ADJACENT (it governs a future test hook that runs in the product main process). Load-bearing care points: (a) the revised mechanism (env-gated globalThis test hook) MUST be specified with hard containment — default-off/fail-closed, native-loader import, NO IPC channel, NO preload/contextBridge, NO renderer global, NO UI path, inert in production — so it is genuinely NOT a product backdoor; (b) the future impl lane adding the hook is a real (small) PRODUCT change to electron/main.ts and is likely A0.7-commit-gated — the ADR must say so; (c) Option 1 (product anchor IPC) stays rejected for D1 (A0.7-geometry, evidence-genie #3/#4); (d) D1 stays OPEN; (e) NO implementation/test/electron code in THIS lane. If review-plan demands a separate security ADR for test hooks (stop-point 1) or rejects the hook as unsafe (stop-point 2) or requires impl to be coherent (stop-point 3), STOP. No push/merge/branch-delete.
Depends on: none
Commit boundary: governance (mark-reviewed + govern) run STANDALONE before the commit (never bundled with git commit). ONE design-revision commit — the revised ADR + the deferred-audit-findings.md revised-planned-closure clarification + governance artifacts (queue.md/.linted/.reviewed/.governed + reviews/queue-review-099.md), staged by exact path; NO apps/services/schema/contract/dependency/manifest/native/lockfile file; NO electron/test/renderer/src code; NO marker/evidence/key/report file; no push; no merge.
