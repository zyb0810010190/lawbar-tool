## WI-ENA10: A0.7 tamper/fabrication guard implementation (fail-closed; no marker write)
Type: IMPL
Scope: Implement a fail-closed guard for the future A0.7 marker namespace, integrated into the existing workflow gate scripts. The guard (scripts/workflow/check-marker-guard.sh) rejects, fail-closed, ANY file under dev-memo/run/evidence/** because no authorized marker writer/validator exists yet (accepted-marker set is EMPTY): a fabricated/touched/copied/schema-only marker cannot land unnoticed; a harness result with isMarker=false is not a marker and cannot satisfy the guard. The guard does NOT write a marker, generate provenance, compute HMAC/signatures, manage key custody, or CREATE the marker namespace (read-only classify; never mkdir dev-memo/run/evidence). Add a self-test (scripts/workflow/check-marker-guard.test.sh) exercising rejection via synthetic --paths strings and a temp-dir --scan case (no real marker file committed to the repo). Integrate both into scripts/workflow/check-gates.sh (the gate battery runs the guard scan + its self-test) so the protection is enforced by gates. This lane implements the GUARD that must land before marker write; it does NOT implement marker write. NO marker creation, NO marker write implementation, NO HMAC/signing/key custody, NO marker provenance generation, NO EVW5 hard hooks, NO harness result-semantics change, NO Evidence UI/product behavior, NO JS-shim change. Do NOT delete/move the root intake files.
Source of truth: docs/adr/ADR-evidence-a07-marker-provenance.md (A07-MARK-00 §4 validation, §5 guard requirements incl. guard-before-write + ledger-bound non-replay, §6 marker path dev-memo/run/evidence/**); docs/adr/ADR-evidence-a07-renderer-conformance-gate.md (A07-GATE-00 §5 marker policy, §8 sequencing guard-before-write); scripts/workflow/check-gates.sh (the existing gate battery to integrate into); scripts/workflow/check-queue.test.sh + govern-queue.test.sh (the *.test.sh convention to follow); .claude/rules/evidence-genie.md (invariants 3, 8, 10); .claude/rules/autonomy.md (this impl lane is user-authorized).
Allowed files: scripts/workflow/** (the new check-marker-guard.sh + check-marker-guard.test.sh; a narrow integration edit to check-gates.sh to run the guard scan + self-test), dev-memo/** (queue/governance/audit/study artifacts), docs/adr/** (ONLY for a short cross-reference if needed; prefer not to)
Forbidden files: dev-memo/run/evidence/** (NO committed marker files; the guard must NOT create this path), native/evidence-core-swift/** (NO change unless review-plan explicitly requires a README-only clarification — prefer none), native/evidence-core/** (the JS shim — NO change), .github/**, .claude/hooks/**, .claude/settings.json, .claude/rules/**, apps/**, services/**, docs/contracts/**, docs/product/**, AGENTS.md, CLAUDE.md, GEMINI.md, Evidence-Genie-M0-Developer-Handover.md (root), xiaolai-dev-workflow-study.md, package.json; NO marker write implementation; NO HMAC/signature/key-custody implementation; NO provenance generation; NO EVW5 hooks
Gates: broker /cc-suite:review-plan REQUIRED before impl (the guard protects future marker authority — no self-review); if review-plan returns READY but scopes into marker-write/HMAC/key-custody/provenance/hooks/UI/product behavior, STOP and ask; broker /cc-suite:audit + /cc-suite:verify after impl (guard protects future marker authority); the guard rejects (exit non-zero) ANY path under dev-memo/run/evidence/** with the accepted set EMPTY (fail-closed); a clean tree (namespace absent) passes; a fabricated/copied/schema-only marker path fails; an isMarker=false harness result cannot satisfy the guard; the guard does NOT create dev-memo/run/evidence/** during a scan; the self-test passes without committing a real marker file; check-gates.sh runs the guard scan + self-test; NO committed marker files; dev-memo/run/evidence/** absent after tests; NO HMAC/signature/key-custody/marker-write/provenance code; NO EVW5 hooks; the native/evidence-core JS shim is byte-unchanged; scripts/workflow/check-contract-integrity.sh exits 0; npm --prefix apps/lawbar-desktop test passes (610/610); scripts/workflow/check-gates.sh returns GATES OK
Acceptance criteria: scripts/workflow/check-marker-guard.sh exists and exits 2 for any path under dev-memo/run/evidence/** (via --paths synthetic strings and via --scan of a temp dir containing a fabricated marker) and exits 0 for a clean tree / non-marker paths, never creating the namespace; scripts/workflow/check-marker-guard.test.sh passes (all cases) without committing a real marker file; scripts/workflow/check-gates.sh invokes the guard scan + the self-test and still returns GATES OK on the clean repo; no file is committed under dev-memo/run/evidence/** and the namespace is absent after tests; grep shows NO marker-write / HMAC / signature / key-custody / provenance-generation / EVW5-hook code; the native/evidence-core JS shim is byte-unchanged; check-contract-integrity PASS; npm desktop test pass 610 fail 0; check-gates GATES OK (now including the guard); broker audit returns no Critical/High/Medium and verify confirms ALL CLOSED; the committed diff is confined to scripts/workflow/** plus dev-memo/** governance artifacts
Risk flags: HIGH-RISK -- the guard protects the FUTURE A0.7 marker authority (the durable court-facing A0.7 claim) and is the fail-closed precondition that must land BEFORE any marker-write WI. broker review-plan REQUIRED + broker audit+verify after. Scope strictly limited to a fail-closed namespace guard + self-test + a narrow check-gates integration; NO marker write, NO HMAC/signature/key custody, NO provenance generation, NO EVW5 hooks, NO UI/product behavior, NO JS-shim change; the guard never creates dev-memo/run/evidence/**. The accepted-marker set is intentionally EMPTY until a future authorized marker-writer/validator WI replaces it with a provenance-valid + ledger-bound non-replay acceptance check (A07-MARK-00). No new dependency.
Depends on: none (A07-GATE-00 and A07-MARK-00 are already merged to main; marker write remains a separate later WI gated behind this guard)
Commit boundary: one local commit -- scripts/workflow/check-marker-guard.sh + scripts/workflow/check-marker-guard.test.sh + the narrow scripts/workflow/check-gates.sh integration, staged by exact path; governance commit separate; no push
