## WI-ENA4: page-box inspection probe (fourth authorized implementation hard-stop)
Type: SCAFFOLD
Scope: Extend the merged native/evidence-core-swift package with a minimal page-box inspection probe that loads the EXISTING synthetic fixture (Tests/EvidenceCoreSmokeTests/Fixtures/synthetic-twopage.pdf) via the PDF load path, reads each page's mediaBox (and/or cropBox) box extent via page.bounds(for:), and reports deterministic values expected from the fixture (width 612, height 792), with a Swift test asserting those values. This is a page-box READ probe ONLY: reading a page object and its bounds(for:)/mediaBox/cropBox is allowed; NO coordinate transforms, NO conversion between PDF/user/view spaces, NO normalized-coordinate math, NO anchor/citation/page-identity logic, NO geometry pass/fail classification, NO drawing/rendering, NO thumbnail generation, NO renderer-conformance, NO A0.7 harness logic, NO A0.7 marker, NO marker provenance/HMAC, NO EVW5 hard hooks, NO Evidence UI/product behavior, NO A0.7 green claim. Reuse the EXISTING synthetic fixture (NO new fixture unless review-plan explicitly requires one). Do NOT wire the probe to the JS shim. Do NOT change the existing native/evidence-core JS shim behavior. Keep it a local Swift test / macOS CI smoke only. README/status note states: page-box read proven; no coordinate transforms; no A0.7; no marker; no product behavior.
Source of truth: dev-memo/plan-batch-casebox-evidence-native-a07-feasibility-00.md (ENA0 feasibility plan, §1-§3 toolchain/boundary — page-box read is the next feasibility probe after PDF load/page-count); docs/adr/ADR-evidence-native-core-a07-feasibility.md (ENA-00); native/evidence-core-swift/** (the merged WI-ENA1+ENA2+ENA3 package this WI extends, incl. the existing synthetic fixture + PdfLoadProbe); native/evidence-core/README.md (the JS shim contract, read-only reference); .github/workflows/evidence-core-swift-smoke.yml (the existing macOS smoke job); .github/workflows/ui-design-artifact.yml (the existing ubuntu check to preserve); .claude/rules/autonomy.md (this step is a user-authorized hard-stop); .claude/rules/evidence-genie.md (A0.7 is the first real Evidence architecture gate; anchors are page-ratio geometry against captured page geometry — a page-box read is NOT anchor/coordinate math); local Swift toolchain (Apple Swift 5.9.2, arm64-apple-macosx, available).
Allowed files: native/evidence-core-swift/** (extend Sources/**, Tests/**, README/status note; Package.swift only if a target/test change requires it; reuse the EXISTING Fixtures/synthetic-twopage.pdf), .github/workflows/evidence-core-swift-smoke.yml (ONLY if a narrow test-command update is needed; otherwise byte-unchanged), dev-memo/** (queue/governance/audit/study artifacts)
Forbidden files: apps/**, native/evidence-core/** (the JS shim — NO behavior change), services/**, docs/contracts/**, docs/product/**, .claude/hooks/**, .claude/settings.json, .claude/rules/**, AGENTS.md, CLAUDE.md, GEMINI.md, dev-memo/run/evidence/**, Evidence-Genie-M0-Developer-Handover.md (root), xiaolai-dev-workflow-study.md, package.json; NO new PDF fixtures (reuse the existing one unless review-plan explicitly requires a new one); NO coordinate-transform logic; NO normalized-coordinate math; NO anchor/citation/page-identity logic; NO drawing/rendering; NO thumbnail generation; NO renderer-conformance; NO A0.7 harness logic; NO marker / HMAC / provenance code
Gates: broker /cc-suite:review-plan REQUIRED before impl (HIGH-RISK: first page-geometry-adjacent read (mediaBox/cropBox via bounds(for:)) on the Evidence native-core path — no self-review); if review-plan returns READY but scopes broader than page-box inspection, STOP and ask; broker /cc-suite:audit + /cc-suite:verify after impl (HIGH-RISK); swift build and swift test pass for native/evidence-core-swift (Swift available locally); the probe reads only page mediaBox/cropBox extents (width/height) and reports them with no coordinate transform/normalization/anchor logic; a test asserts width 612 and height 792 for both pages of the existing synthetic fixture; the macOS CI smoke job runs the test and stays macOS-only; the existing ubuntu ui-design-artifact.yml is byte-unchanged; page-box access (bounds(for:)/mediaBox/cropBox/page(at:)) appears ONLY in the page-box probe/test under native/evidence-core-swift/** (grep); no coordinate-transform/normalized-coordinate/anchor/citation/page-identity logic (grep); no renderer-conformance impl (grep); no A0.7 marker and nothing under dev-memo/run/evidence/ (grep); no HMAC/provenance (grep); no new fixture; the native/evidence-core JS shim is byte-unchanged; README/status note states page-box-read-proven/no-coordinate-transforms/no-A0.7/no-marker/no-product-behavior; scripts/workflow/check-contract-integrity.sh exits 0; npm --prefix apps/lawbar-desktop test passes (610/610); scripts/workflow/check-gates.sh returns GATES OK
Acceptance criteria: native/evidence-core-swift/ now contains a page-box inspection probe (EvidenceCorePageBoxProbe.inspectMediaBoxes(url:) -> {loaded, mediaBoxes:[{width,height}]}) plus a Swift test that loads the EXISTING synthetic fixture and asserts each of the 2 pages has mediaBox extent width 612, height 792; swift test --package-path native/evidence-core-swift passes (skeleton + PDFKit probe + PDF-load probe + page-box probe); grep shows page-box access (bounds(for:)/mediaBox/page(at:)) only in the page-box probe/test under native/evidence-core-swift/** and NO coordinate-transform/normalized-coordinate/anchor/citation/page-identity/renderer-conformance/marker/HMAC/provenance code and nothing under dev-memo/run/evidence/; NO new PDF fixture was added; the macOS CI workflow is either byte-unchanged or changed only by a narrow test-command line and remains macOS-only; .github/workflows/ui-design-artifact.yml is byte-unchanged; the native/evidence-core JS shim is byte-unchanged; the README/status note states page-box-read-proven/no-coordinate-transforms/no-A0.7/no-marker/no-product-behavior; npm desktop test shows pass 610 fail 0; check-gates GATES OK; broker audit returns no Critical/High/Medium and verify confirms ALL CLOSED; the committed diff is confined to native/evidence-core-swift/** plus (at most) the one macOS smoke workflow file
Risk flags: HIGH-RISK -- first page-geometry-adjacent read (page mediaBox/cropBox extents via bounds(for:)) on the Evidence native-core path (autonomy hard-stop, EXPLICITLY user-authorized for this narrow page-box-inspection-probe scope ONLY). broker review-plan REQUIRED + broker audit+verify after. Scope strictly limited to reading box extents (width/height) of the existing synthetic fixture; NO coordinate transforms, NO normalized coordinates, NO anchor/citation/page-identity, NO rendering, NO geometry classification, NO renderer-conformance, NO A0.7 harness, NO marker/provenance/HMAC, NO hooks/UI/product behavior; the JS shim is untouched; no new dependency; no new fixture. Swift toolchain confirmed available locally (5.9.2).
Depends on: none (the ENA3 PDF load/page-count probe + synthetic fixture are already merged to main)
Commit boundary: one local commit -- native/evidence-core-swift/** plus (at most) the one macOS smoke workflow file, staged by exact path; no push
