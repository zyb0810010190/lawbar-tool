## WI-EVIDENCE-A0.7-FIXTURE-COVERAGE-00: strengthen real Swift/PDFKit A0.7 fixture coverage (Type: IMPL)
Type: IMPL
Scope: Make the EXISTING (already-green-on-trivial) real Swift/PDFKit A0.7 renderer-conformance harness MEANINGFUL against messy synthetic fixtures, per `docs/reference/evidence-genie-m0-developer-handover.md` §5/§13 + `docs/adr/ADR-evidence-a07-renderer-conformance-gate.md` (A07-GATE-00). Native-only; NO JS shim, NO custody/marker, NO schema/contract/UI. Discovery (verified on main): `native/evidence-core-swift/` Swift package builds + `swift test` is GREEN (19/19); `a07-harness-cli synthetic-twopage.pdf oracle.json` → `pass	ok	2`; the harness exercises the REAL PDFKit path (`PDFDocument`, `page.bounds(.mediaBox)`, normalized-sample roundtrip) with full class_1/class_2/inconclusive vocabulary — BUT only against ONE trivial fixture (two identical US-Letter mediaBox pages; the oracle README says "Not production anchor geometry"). The messy A0.7 cases the handover demands are UNTESTED. This WI adds deterministic synthetic/public fixtures + extends the harness/oracle to cover, at minimum: (a) rotated pages, (b) cropBox/mediaBox mismatch, (c) non-zero mediaBox origin, (d) mixed page sizes; plus a negative/malformed-oracle case proving `inconclusive`/mismatch is NOT treated as pass. Approach: (1) author hand-written minimal deterministic PDFs (same byte-stable, no-timestamp style as the existing 432-byte `synthetic-twopage.pdf`, correct xref offsets) under `native/evidence-core-swift/Tests/EvidenceCoreSmokeTests/Fixtures/`, one per messy case; (2) author a per-fixture committed `oracle.json` (expected values derived ONLY from the fixture's known construction, INDEPENDENT of harness output, per A07-GATE-00 §3) under sibling `Fixtures/a07-*/` dirs; (3) extend the `A07Oracle` Decodable + `A07Observed` + `EvidenceCoreA07Harness.run`/`evaluate` to observe + classify per-page cropBox (`page.bounds(.cropBox)`), rotation (`page.rotation`), and mediaBox ORIGIN (currently width/height only) — with the new oracle fields OPTIONAL/additive so the existing `synthetic-twopage` oracle still decodes + passes unchanged; (4) classification semantics per the handover: a renderer-structure disagreement (page count / box extent / box origin / rotation that the renderer reports differently than the recorded geometry) is class_2 (geometry-source instability, STOP, never downgraded); a correct box with a wrong DERIVED value (e.g. ratio uses mediaBox while geometry says cropBox; origin not subtracted in normalization) is class_1 (normalization/math bug); (5) extend `A07ConformanceHarnessTests` to assert each new fixture's expected verdict + a malformed-oracle negative test (status != pass). Keep `inconclusive` ≠ pass and `not_implemented` = FAIL.
Source of truth: docs/adr/ADR-evidence-a07-renderer-conformance-gate.md (A07-GATE-00 §3 oracle-independence, §4 class_1/class_2 + inconclusive-not-pass, §5 no-marker-here, §6 JS-shim-unchanged); docs/reference/evidence-genie-m0-developer-handover.md §5 (A0.7 checks: stable physicalPageIndex, resolved-box bounds incl mediaBox fallback + non-zero origin, rotation, ratio→page-space→ratio roundtrip) + §13 (messy fixtures: rotated, cropBox/mediaBox mismatch, non-zero origin, mixed sizes); native/evidence-core-swift/Sources/EvidenceCoreSmoke/A07ConformanceHarness.swift (the harness to extend); native/evidence-core-swift/Tests/EvidenceCoreSmokeTests/Fixtures/{synthetic-twopage.pdf,a07-renderer-conformance/oracle.json} (the precedent fixture + oracle to mirror); native/evidence-core-swift/Sources/A07HarnessCLI/main.swift (CLI verdict contract); .claude/rules/{evidence-genie,cc-suite,autonomy,execution-discipline,staging-hygiene,security-boundary,client-local-first}.md
Allowed files: native/evidence-core-swift/Sources/EvidenceCoreSmoke/A07ConformanceHarness.swift, native/evidence-core-swift/Tests/EvidenceCoreSmokeTests/A07ConformanceHarnessTests.swift, native/evidence-core-swift/Tests/EvidenceCoreSmokeTests/Fixtures/** (new synthetic PDFs + per-fixture oracle/manifest/README), native/evidence-core-swift/README.md (only if fixture-set documentation needs it), dev-memo/run/queue.md, dev-memo/run/queue.linted, dev-memo/run/queue.reviewed, dev-memo/run/queue.governed, dev-memo/run/reviews/queue-review-107.md
Forbidden files: native/evidence-core/** (NO JS shim wiring; commands.mjs/cli.mjs UNCHANGED — A07-GATE-00 §6), native/evidence-core-swift/Package.swift (NO target/product change unless review-plan explicitly requires it — if so, STOP and report first), native/evidence-core-swift/Sources/EvidenceCoreSmoke/{PageBoxProbe,PdfKitProbe,PdfLoadProbe,CoordinateRoundtripProbe,EvidenceCoreSmoke}.swift (unrelated probes — no change), apps/** + services/** + docs/contracts/** (NO product/schema/contract/persistence/UI/electron change), services/case-box-persistence/src/sqlite/schema.ts (NO schema/CURRENT_SCHEMA_VERSION change), scripts/workflow/** + .claude/** + .github/** (NO gate/hook/custody-script change), docs/adr/ADR-evidence-a07-** (READ-ONLY — NO ADR amendment in this lane; if JS-shim/ADR change is required, STOP), docs/adr/** other, dev-memo/run/evidence/** (gitignored custody marker/ledger — NEVER touched/staged; it exists locally from a prior run and MUST remain untouched), committed keys, dev-memo/run/reports/** + dev-memo/run/override-reason.md + dev-memo/run/autonomous-next.md (untracked run-state — never staged/edited), Evidence-Genie-M0-Developer-Handover.md + xiaolai-dev-workflow-study.md (root intake — READ-ONLY), native/evidence-core-swift/.build/** (gitignored build artifacts — never staged); NO custody/marker write; NO HMAC/key request/receive/print/persist/stage; NO confidential/client/court/non-public fixture; NO push/merge/branch-delete
Gates: broker /cc-suite:review-plan REQUIRED before implementation — confirm: the approach (hand-written deterministic minimal PDFs + per-fixture independent oracles + additive-optional oracle fields + harness extension to cropBox/rotation/origin + class_1/class_2 split) is sound and does NOT weaken the A0.7 ADR contract; the classification semantics (cropBox-vs-mediaBox = class_1 normalization; renderer-structure disagreement = class_2) match the handover; existing synthetic-twopage oracle still passes (backward compatible); NO JS shim, NO marker, NO Package.swift change, NO schema/contract. STOP-points: (1) if synthetic fixtures cannot be generated deterministically → STOP; (2) if a fixture would need confidential/client/court/non-public material → STOP; (3) if the harness cannot exercise the real Swift/PDFKit path → STOP; (4) if the only way to pass is to weaken/skip/relabel assertions → STOP; (5) if JS-shim wiring is required → STOP; (6) if custody-marker writing or key handling is required → STOP; (7) if schema/package/tarball/lockfile change becomes necessary without review-plan approval → STOP; (8) if review-plan says split into design/discovery first → STOP. If review-plan NEEDS-FIX on approach, fix-forward; if a blocker, STOP. After implementation: `swift test` (in native/evidence-core-swift) passes; `a07-harness-cli` against the existing + each new fixture/oracle pair reports the expected verdict (old still pass; new messy cases produce the expected class_1/class_2, NOT inconclusive); malformed-oracle case proves inconclusive/mismatch != pass; `scripts/workflow/check-contract-integrity.sh` exits 0; broker /cc-suite:audit + /cc-suite:verify (no Critical/High/Medium open before commit). NO A0.7 custody/marker in this lane.
Acceptance criteria: `native/evidence-core-swift` `swift test` MUST pass (exit 0, 0 failures); `a07-harness-cli Tests/EvidenceCoreSmokeTests/Fixtures/synthetic-twopage.pdf Tests/EvidenceCoreSmokeTests/Fixtures/a07-renderer-conformance/oracle.json` MUST still output `pass	ok	2` (regression); at least four NEW deterministic synthetic fixtures exist — rotated-page, cropBox/mediaBox-mismatch, non-zero-origin, mixed-page-size — each with a committed independent oracle, and the CLI against each MUST produce the expected `pass` (when the harness math is correct) or the expected `class_1`/`class_2` verdict for an instability-probe oracle, and MUST NOT produce `inconclusive` where a checkable assertion exists; at least one negative/malformed-oracle test MUST prove `inconclusive` or a mismatch is NOT treated as pass (status != pass); the harness now observes per-page cropBox + rotation + mediaBox origin (not mediaBox-extent-only); the existing oracle still decodes (new oracle fields are optional/additive); NO confidential/client fixture added; NO change to native/evidence-core/** (JS shim), Package.swift (unless review-plan approved), schema, contracts, services, apps, scripts, .claude, A0.7 ADR; `CURRENT_SCHEMA_VERSION` remains 12; `dev-memo/run/evidence/**` untouched; no A0.7 HMAC/custody key requested/received/printed/persisted/staged; `check-contract-integrity` exits 0; broker review-plan READY + audit no Critical/High/Medium + verify ALL CLOSED; committed diff confined to Allowed files (native/evidence-core-swift Sources/Tests/Fixtures + governance).
Risk flags: HIGH-RISK Evidence-invariant lane (A0.7 is the first reality gate; nothing downstream builds until it is meaningfully green) — but native-harness/test/fixture coverage only; NO product/schema/contract/UI/marker change. Load-bearing care points: (a) fixtures MUST be deterministic (hand-written minimal PDFs, byte-stable, no timestamps — mirror the existing 432-byte fixture; correct xref offsets) and synthetic/public ONLY (A07-GATE-00 §2; never confidential — AGENTS hard-stop); (b) oracle values MUST be derived from the fixture's known construction, INDEPENDENT of harness output (§3 — never back-fill harness output into the oracle); (c) classification fidelity — renderer-structure disagreement (page count/box extent/origin/rotation) = class_2 STOP, never downgraded; correct box + wrong derivation (cropBox-vs-mediaBox, origin-not-subtracted) = class_1; (d) `inconclusive`/`not_implemented` NEVER pass; (e) new oracle fields OPTIONAL so the existing oracle still passes (backward compat); (f) NO JS shim wiring (§6), NO marker/custody, NO `dev-memo/run/evidence/**` touch, NO key handling; (g) NO Package.swift change without review-plan approval. If any of these cannot hold, STOP and report. No push/merge/branch-delete.
Depends on: none
Commit boundary: governance (mark-reviewed + govern) run STANDALONE before the commit (never bundled with git commit). ONE implementation commit — native/evidence-core-swift Sources (A07ConformanceHarness.swift) + Tests (A07ConformanceHarnessTests.swift) + Fixtures/** (new synthetic PDFs + oracles + optional README) [+ native/evidence-core-swift/README.md only if used] + governance artifacts (queue.md/.linted/.reviewed/.governed + reviews/queue-review-107.md), staged by exact path; NO native/evidence-core/** (JS shim); NO Package.swift unless review-plan approved; NO schema/contract/services/apps/scripts/.claude file; NO marker/evidence/key/report file; NO .build artifact; no push; no merge; no branch delete.
