## WI-RELEASE-G7-CRASH-RECOVERY-DRILL-00: define + govern the M0 gate-7 crash-recovery drill (the closure-evidence deliverable the robustness policy requires) (Type: EVIDENCE, release-governance MEDIUM risk)

## Review packet (compact)
- Active plan summary: the gate-7 robustness policy (`docs/release/gate7-robustness-policy-00.md`, shipped by WI-RELEASE-G7-ROBUSTNESS-POLICY-00, PR #193 merge `b1eacfb`) recommends — and its user-decision D-G7-2 may REQUIRE — a **documented crash-recovery drill** as the load-bearing closure evidence for gate-7 criteria 1/2/4 (crash/data-loss tolerance; local-first persistence/recovery; audit/event integrity under failure). This WI **governs** (does NOT execute) a FUTURE lane that authors + runs that drill and records its result as evidence. The robustness bar is v1 **local-first data survival + recovery + audit integrity**, NOT distributed fault-tolerance (`.claude/rules/client-local-first.md`). THIS authoring lane produces the governed queue WI only; it runs NO drill, changes NO source/test/config, clears NO gate, and decides NEITHER user risk-decision (D-G7-1 accept-without-mutation-testing; D-G7-2 require-the-drill).
- Exact target files (future exec lane): a NEW `docs/release/gate7-crash-recovery-drill-00.md` (the drill procedure + result/evidence record) + the gate-7 evidence ROW of `docs/release/go-live-readiness-report.md` (update to reference the drill result, keeping gate 7 NOT CLEARED — a passing drill supplies evidence but does NOT by itself clear gate 7, which still depends on the USER decisions D-G7-1/D-G7-2) + `dev-memo/run/reviews/` (the exec lane's review artifact). The drill script, if the exec lane makes it scripted/hybrid, lands under `scripts/` (or a drill-specific dir) — that is the EXEC lane's decision + a separate scope note, NOT authorized by this authoring lane.
- Exact acceptance criteria: below.
- Exact out-of-scope: NO drill EXECUTION in this authoring lane; NO robustness/mutation/fuzz/crash-injection HARNESS implementation; NO gate-6 full-project audit; NO release-doc finalization beyond the drill doc + the gate-7 row (exec lane); NO signing/notarization/公开 distribution decision; NO 律师法 conclusion; NO final GO/NO-GO; NO unilateral gate-7 CLEARED; NO deciding D-G7-1/D-G7-2 for the user; NO source/test/package/config/schema/contract change in THIS lane.
- Drill requirements the FUTURE drill doc must define (what the exec lane's drill must specify + prove) — at minimum:
  1. **Fixture / data-directory expectations**: an exact, disposable test data directory mirroring the v1 layout (`case-box.sqlite` + `audit-log.sqlite` + content-hash-addressed `blobs/` under an isolated path, NOT the real `~/Library/Application Support/lawbar/`), seeded with a known fixture set (N matters + documents + audit events) so the pre/post state is deterministic and comparable.
  2. **Backup / restore procedure**: the backup-as-directory model per brief §14 — capture the data-directory manifest (the two SQLite files `case-box.sqlite` + `audit-log.sqlite` PLUS the content-hash-addressed `blobs/` directory) via copy-out or a Time-Machine-equivalent snapshot, then the restore step ("drop directory back") — with the exact commands / manual steps enumerated. If the drill is scripted/hybrid (requirement 9), the exec lane MUST state the "drill artifact, not product harness" boundary in its own scope + review packet before editing `scripts/`.
  3. **Simulated crash / interruption boundary**: the exact fault-injection point (e.g. `kill -9` / simulated power loss) taken MID-WRITE (during an in-flight transaction / audit-append), specifying HOW the mid-write moment is reached deterministically enough to be a meaningful test (not a quiescent-state kill).
  4. **SQLite integrity checks**: post-restart `PRAGMA integrity_check` (and/or the app's startup schema-integrity check per brief §14) on BOTH `case-box.sqlite` and `audit-log.sqlite`, plus confirmation the WAL/rollback left no partial-write / torn-page corruption; the exact expected output ("ok").
  5. **Audit / event integrity checks**: the hash-chained audit log verifies + the `event_count == COUNT(*) == MAX(sequence)` invariant holds across the crash/restart (overlaps gate 12); tamper-evidence (chain continuity) confirmed. State whether the invariant is applicable to the fixture and the exact query/assertion.
  6. **Expected recovery result**: after restart + (if needed) restore, all seeded matters/documents/audit events are present + uncorrupted; an in-flight transaction that was interrupted is EITHER fully committed OR fully rolled back (never torn); no data loss beyond the atomic-rollback of the single in-flight transaction.
  7. **Required evidence artifacts**: the drill doc must record the concrete artifacts — command transcripts, `integrity_check` output, the invariant query result, before/after counts, and the environment (OS, node, better-sqlite3 version, date) — captured as text in the doc (NO raw case data / NO real lawyer documents; fixture-only).
  8. **Explicit pass/fail criteria**: a crisp PASS = (integrity_check ok on both DBs) AND (audit invariant holds) AND (no data loss beyond atomic in-flight rollback) AND (startup schema-integrity + audit-chain verification pass); FAIL = any corruption / torn write / invariant break / unrecoverable state. A FAIL is a class-2 robustness STOP (surface, do not paper over).
  9. **Manual-only / scripted / hybrid**: the drill doc DECIDES + states its mode. Default recommendation for v1 (non-binding on the exec lane): a **hybrid** — a scripted seed + fault-injection + integrity-check harness where feasible, with a documented manual fallback for the parts that need a real process kill; if scripted, the harness is a drill artifact (NOT a product mutation/fuzz harness and NOT a new runtime dependency).
  10. **How the result feeds gate 7 WITHOUT auto-clearing it**: a PASS supplies the closure evidence for criteria 1/2/4 and is recorded in the gate-7 row + the drill doc, but gate 7 still requires the USER decisions D-G7-1 (accept v1 without mutation testing?) + D-G7-2 (require-the-drill? — a pass satisfies the drill requirement IF the user answered YES) before it can clear. The exec lane MUST NOT flip gate 7 to CLEARED; at most it moves the row to "PARTIAL / drill PASS — pending user D-G7-1/D-G7-2" or keeps it "OPEN — policy authored + drill PASS".
- Dependencies mapped: **gate 7** (this drill is the policy's required closure evidence — informs but does NOT clear it); **gate 12** (audit-chain tamper-evidence / `event_count==COUNT==MAX(sequence)` — overlaps requirement 5); **gate 14** (backup + recovery procedure — the restore in requirement 2 IS the gate-14 procedure exercised); **gate 15** (rollback procedure tested — the crash-recovery drill overlaps the dry-run rollback); **gate 6** (full-project audit — a drill FAIL would be a robustness finding for that sweep). These are noted as related, NOT cleared by this WI.
- Essential refs: `docs/release/gate7-robustness-policy-00.md` (§2 closure criteria 1/2/4, §3 recommendation, §4 D-G7-2); `docs/release/go-live-readiness-report.md` §1 gate 7 (+ gates 12/14/15/6); `docs/product/project-requirements-brief.md` §14 (backup manifest = matter SQLite + audit-log SQLite + blob store; restoration = drop-directory-back + startup schema integrity + audit-chain verification); `.claude/rules/client-local-first.md` (v1 local-first/offline posture); `AGENTS.md` §"Critical invariants" (audit-chain `event_count==COUNT==MAX(sequence)`); `.claude/rules/security-boundary.md` §"Go-live independence".
- Review questions: (1) Does the WI correctly scope a crash-recovery DRILL (procedure + evidence record), NOT a drill execution or a mutation/fuzz/crash-injection PRODUCT harness? (2) Are the ten drill requirements complete + v1-local-first (fixture-only, no real case data, `PRAGMA integrity_check` + the audit invariant)? (3) Does the WI keep gate 7 NOT cleared and NOT decide D-G7-1/D-G7-2 for the user? (4) Are the gate-7/12/14/15/6 dependencies mapped without clearing them? (5) Is any drill execution, product-harness implementation, gate-6 audit, new dependency, or go-live decision smuggled in?

Type: EVIDENCE
Scope: Govern (do NOT execute) a FUTURE lane that authors + runs the gate-7 crash-recovery drill. The FUTURE execution lane produces `docs/release/gate7-crash-recovery-drill-00.md` — defining the ten drill requirements above (fixture/data-dir expectations; backup/restore procedure; simulated crash/interruption boundary; SQLite `PRAGMA integrity_check`; audit/event integrity incl. `event_count==COUNT==MAX(sequence)`; expected recovery result; required evidence artifacts; explicit pass/fail criteria; manual/scripted/hybrid mode; how the result feeds gate 7 WITHOUT auto-clearing it) — then runs the drill on a disposable FIXTURE data directory (never real case data) and records the result, then updates ONLY the gate-7 evidence row of `docs/release/go-live-readiness-report.md` to reference the drill result while keeping gate 7 NON-CLEARED (a PASS is evidence; the clear still needs the USER decisions D-G7-1/D-G7-2). It maps the gate-7/12/14/15/6 dependencies without clearing them; it PRESERVES every still-non-cleared gate + the STOP-AND-ASK gates (4/11/21); it makes NO final GO/NO-GO, resolves NO STOP-AND-ASK, runs NO gate-6 audit, decides NEITHER D-G7-1 nor D-G7-2 for the user, adds NO new runtime dependency, and changes NO product source/test/schema/contract. If the exec lane makes the drill scripted, the harness is a drill artifact (NOT a product mutation/fuzz/crash-injection harness). THIS authoring lane implements + runs + decides NOTHING — it only governs the drill WI. If review asks to run the drill, run gate 6, unilaterally clear gate 7, decide the user risk-acceptance, or add a product crash-injection harness/dependency, REJECT and report.
Source of truth: docs/release/gate7-robustness-policy-00.md (the robustness policy — §2 closure criteria 1/2/4, §3 recommendation of a documented crash-recovery drill, §4 user decisions D-G7-1/D-G7-2); docs/release/go-live-readiness-report.md §1 gate 7 (OPEN — policy authored) + gates 12/14/15/6 (dependencies); docs/product/project-requirements-brief.md §14 (backup manifest + restoration = drop-directory-back + startup schema-integrity + audit-chain verification) + §4 (fully offline; crash reporting OFF); .claude/rules/client-local-first.md (v1 local-first/offline; robustness bar = local-data survival, not distributed FT); AGENTS.md §"Critical invariants" (audit-chain event_count==COUNT==MAX(sequence); stable OcrQueueError codes; payload_json canonical; no read-layer re-sort); .claude/rules/security-boundary.md §"Go-live independence"; .claude/rules/autonomy.md + staging-hygiene.md + loc-guardian.md
Allowed files: docs/release/gate7-crash-recovery-drill-00.md (the NEW drill doc the FUTURE lane produces) + docs/release/go-live-readiness-report.md (the gate-7 evidence ROW ONLY) + dev-memo/run/reviews/ (the exec lane's own review artifact); if the exec lane makes the drill scripted/hybrid, a drill harness/fixture under scripts/ or a drill-specific dir is the EXEC lane's separately-scoped artifact (NOT a product source/test change, NOT a new dependency). NOTE: THIS authoring lane commits ONLY the queue governance (queue.md/linted/reviewed/governed) + reviews/queue-review-144.md — NO drill doc, NO drill run, NO report change. The future exec lane records its evidence in the COMMIT MESSAGE + review artifact and does NOT re-stage this lane's queue governance.
Forbidden files: docs/release/ OTHER than the NEW gate7-crash-recovery-drill-00.md + the gate-7 row of go-live-readiness-report.md (the other audit/assessment/policy reports are READ-ONLY evidence — NOT edited; the robustness-policy doc gate7-robustness-policy-00.md is READ-ONLY here), docs/adr/ + docs/product/ (READ-ONLY — NO brief/ADR edit), docs/contracts/, apps/ + services/ (NO product source/test/renderer/IPC/persistence/contract change — NO mutation-test/fuzz/crash-injection PRODUCT harness), services/*/src/sqlite/schema.ts (CURRENT_SCHEMA_VERSION stays 12), migrations/, native/, package.json/package-lock.json of any package (NO new dependency — no mutation-testing/fuzzing/crash-injection tool added), dev-memo/run/intake/ (raw samples — never staged), dev-memo/ OTHER than run/reviews/ (no other plan/design edit), .claude/ + .github/; NO drill EXECUTION in this authoring lane; NO gate-6 full-project audit; NO release-doc finalization beyond the drill doc + the gate-7 row; NO signing/notarization/public-distribution decision; NO 律师法 conclusion; NO final GO/NO-GO decision; NO unilateral gate-7 CLEARED; NO deciding D-G7-1/D-G7-2 for the user. Push is NEVER automatic (autonomy hard-stop): local commit governance is the default; any branch push + PR open happens ONLY under explicit per-invocation user authorization; NO merge; NO branch delete
Gates: EVIDENCE/governance lane (MEDIUM release-governance risk — governs a crash-recovery drill WI; no code, no drill run, no go-live decision, no risk-acceptance made for the user). (1) THIS authoring lane: broker /cc-suite:review-plan on this queue WI, Path 1, retrievable, then govern. (2) The FUTURE exec lane: produce the drill doc + run the drill on a fixture data dir + the gate-7 row update, then broker /cc-suite:review-plan (and, if a scripted harness lands, an /cc-suite:audit on that diff) on the produced artifacts. (3) scripts/workflow/check-contract-integrity.sh exit 0; scripts/workflow/check-queue.sh PASS (both lanes). Forbidden-scope scan before commit: CURRENT_SCHEMA_VERSION EQUALS 12; no product source/test/package/schema/contract change; no drill run; no gate-6 run; no new dependency; no GO/NO-GO; no unilateral gate-7 clear; no D-G7-1/D-G7-2 decision; no raw sample. STOP-points: review asks to run the drill, run gate 6, unilaterally clear gate 7, decide the user risk-acceptance, or add a product crash-injection harness/dependency → REJECT/STOP and report; any forbidden file would change → STOP.
Acceptance criteria: check-queue and check-contract-integrity PASS and CURRENT_SCHEMA_VERSION EQUALS 12 and the diff CONTAINS only the queue governance (queue.md/linted/reviewed/governed) + reviews/queue-review-144.md and no docs/source/test/package/schema/contract change appears and no drill run / no product crash-injection harness / no new dependency appears. The governed WI must DEFINE the ten drill requirements (fixture/data-dir; backup/restore; crash/interruption boundary; SQLite integrity_check; audit/event integrity incl. event_count==COUNT==MAX(sequence); expected recovery result; evidence artifacts; explicit pass/fail; manual/scripted/hybrid; feeds-gate-7-without-auto-clearing), must MAP the gate-7/12/14/15/6 dependencies, must keep gate 7 NON-CLEARED, and MUST NOT decide D-G7-1/D-G7-2 for the user or assert go-live. review-plan (this lane) must RETURN READY.
Risk flags: MEDIUM (release-governance — a drill WI that could over-claim readiness, smuggle a drill run, add a crash-injection product harness/dependency, or make a user risk-acceptance). Care points: (a) NO DRILL RUN in this authoring lane — this lane only governs the WI; (b) NO UNILATERAL CLEAR — a drill PASS is evidence; gate 7's clear still depends on USER decisions D-G7-1/D-G7-2; (c) FIXTURE-ONLY — the drill runs on a disposable fixture data dir, NEVER real case data / real lawyer documents (offline, no raw sample staged); (d) NO PRODUCT HARNESS / NO DEPENDENCY — a scripted drill harness is a drill artifact, not a product mutation/fuzz/crash-injection harness, and adds no new runtime dependency (that would be a hard-stop); (e) DEPENDENCIES NOT CLEARED — gate 7/12/14/15/6 are mapped, not cleared; (f) GO-LIVE INDEPENDENCE — a drill result does NOT imply go-live; the final verdict + the three STOP-AND-ASK hard-stops remain the user's; (g) POLICY/BRIEF READ-ONLY — the robustness policy, brief, and other release reports are evidence, not edit targets. If any cannot hold, STOP and report.
Depends on: none
Commit boundary: TWO separate lanes. (1) THIS authoring lane commits ONLY the queue governance (queue.md/linted/reviewed/governed) + reviews/queue-review-144.md — no drill doc, no drill run, no report change, no implementation, no decision. (2) The FUTURE execution lane commits ONLY the new docs/release/gate7-crash-recovery-drill-00.md + the gate-7 evidence-row update to go-live-readiness-report.md + (if scripted) a separately-scoped drill harness/fixture + its own review artifact under dev-memo/run/reviews/, records its evidence in the COMMIT MESSAGE, and does NOT re-stage this lane's queue governance. NO product source/test/package/schema/contract/crash-injection-harness file in the authoring lane; NO new dependency in either lane; NO staging of dev-memo/run/intake/ or untracked residue; push is NEVER automatic (autonomy hard-stop) — local commit governance is the default, any branch push + PR open happens ONLY under explicit per-invocation user authorization; no merge; no branch delete. THIS authoring lane implements NOTHING, runs NO drill, and decides NOTHING — it only governs the gate-7 crash-recovery-drill WI.
