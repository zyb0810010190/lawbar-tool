## WI-A3-INTERNAL-DEPS-COMMIT-TARBALLS-T2: commit internal tarballs + lock + drift guard + complete A3 link audit labels (Type: UI)
Type: UI
Design artifact: dev-memo/design/2026-06-28-a3-link-audit-event-labels.md
Requires-A07: no
A0.7-classification: DOWNGRADED from yes to no by review-plan `review-plan-mqxwmfeq-4lns17` (2026-06-28): this WI introduces NO geometry, marker rendering, citation identity, anchor placement, export behavior, or new Evidence UI workflow — only (a) packaging determinism (committed tarballs + lock + drift guard) and (b) STATIC display labels for the already-defined/persisted `LINK_*` audit-event kinds. It is not an A0.7-dependent action, so custody-9b is not required; a normal exact-path commit applies.
Source design: docs/adr/ADR-evidence-desktop-internal-deps-packaging.md (DESKTOP-DEPS-00, Option 1) + dev-memo/design/2026-06-28-a3-link-audit-event-labels.md (the audit-label completion)
Scope: COMBINED packaging + renderer-label fix — the two halves are compile-coupled (refreshing the bundled `case-box-contract` exposes `LINK_CREATED`/`LINK_UNLINKED`/`LINK_RELINKED` in `CaseBoxAuditEventKind`; the renderer's EXHAUSTIVE audit-kind label map then fails `tsc` until those 3 labels exist; and a labels-only change fails against the stale installed contract — so they MUST land together). HALF A (packaging, DESKTOP-DEPS-00 Option 1; already done + proven in the working tree): (1) the internal tarballs `apps/lawbar-desktop/dist-tarballs/{case-box-contract,case-box-persistence}-0.1.0.tgz` + `manifest.json` regenerated from current source via `pack:internal` are now TRACKED — `apps/lawbar-desktop/.gitignore` no longer ignores `dist-tarballs/`; (2) `apps/lawbar-desktop/package-lock.json` refreshed NARROWLY — only the two `file:dist-tarballs` `integrity` entries updated to the committed tarball bytes' `sha512` (no `rm lock && npm install`; no external-dep churn), so `rm -rf node_modules && npm ci` succeeds (no EINTEGRITY); (3) a SEMANTIC-PAYLOAD drift guard `scripts/workflow/check-internal-tarballs.mjs` (mirrors `pack:internal` staging; per package: validate the committed `manifest.json` SELF-CONSISTENTLY — committed `sizeBytes`+`integrity` MATCH the committed tarball bytes, static identity fields = expected constants, ignore `packedAt`; re-stage from source + `npm pack` into temp + extract BOTH temp + committed tarballs + compare the extracted `package/**` file LIST as a SET and each file's sha256 CONTENT; exit non-zero with the diff on drift) registered as a gate (`apps/lawbar-desktop/package.json` `check:internal-tarballs` script + a `scripts/workflow/check-gates.sh` invocation). HALF B (renderer audit-event labels, per the design note): add EXACTLY the 3 missing LINK_* audit-event labels mirroring the existing pattern — `renderer/i18n/labels.ts` `EVENT_KIND_ID` (`LINK_CREATED`/`LINK_UNLINKED`/`LINK_RELINKED` → `eventKind.LINK_*`), the `eventKind.LINK_CREATED`/`eventKind.LINK_UNLINKED`/`eventKind.LINK_RELINKED` strings in `renderer/i18n/catalog.ts`, and `renderer/screens/auditEventLabels.ts` ONLY IF it independently enumerates kinds (else leave it). NO layout/screen/workflow/filter/permission/data-exposure/behavior change; NO evidence content; NO audit-event semantics change; NO unrelated i18n/catalog key change. If editing `renderer/screens/auditEventLabels.ts` shifts the i18n drift-guard line numbers, regenerate `renderer/i18n/ui-strings-allowlist.json` (catalog.ts + labels.ts are NOT scanned by the guard). Flip `DESKTOP-DEPS-STALE-LOCK-01` to `closed` in `dev-memo/deferred-audit-findings.md` ONLY AFTER all verification passes. KEEP D1 OPEN. NO source-contract/persistence/schema/contract-semantic change.
Source of truth: docs/adr/ADR-evidence-desktop-internal-deps-packaging.md (Option 1 + §6 guard/acceptance); dev-memo/design/2026-06-28-a3-link-audit-event-labels.md (the 3 LINK_* labels + the not-in-scope list); apps/lawbar-desktop/scripts/pack-internal-packages.mjs (the staging the guard mirrors); apps/lawbar-desktop/renderer/i18n/labels.ts (the exhaustive `EVENT_KIND_ID` map needing the 3 LINK_* entries) + catalog.ts (the `eventKind.*` strings) + renderer/screens/auditEventLabels.ts (consumer); apps/lawbar-desktop/renderer/i18n/ui-strings-allowlist.json + tests/renderer-i18n-guard.test.mjs (the i18n drift guard — scans renderer/screens, NOT renderer/i18n); apps/lawbar-desktop/package-lock.json (lockfileVersion 3; the two file: integrity entries); docs/contracts/case-box-contract/dist/audit-log.js (source has the 3 LINK_* kinds); dev-memo/deferred-audit-findings.md (DESKTOP-DEPS-STALE-LOCK-01; LINK-IPC-T1-D1); .claude/rules/{cc-suite,autonomy,execution-discipline,security-boundary,staging-hygiene,loc-guardian,evidence-genie,client-local-first}.md
Allowed files: apps/lawbar-desktop/.gitignore, apps/lawbar-desktop/dist-tarballs/case-box-contract-0.1.0.tgz, apps/lawbar-desktop/dist-tarballs/case-box-persistence-0.1.0.tgz, apps/lawbar-desktop/dist-tarballs/manifest.json, apps/lawbar-desktop/package-lock.json, apps/lawbar-desktop/package.json, scripts/workflow/check-internal-tarballs.mjs, scripts/workflow/check-gates.sh, apps/lawbar-desktop/renderer/i18n/labels.ts, apps/lawbar-desktop/renderer/i18n/catalog.ts, apps/lawbar-desktop/renderer/screens/auditEventLabels.ts, apps/lawbar-desktop/renderer/i18n/ui-strings-allowlist.json, apps/lawbar-desktop/tests/renderer-i18n.test.mjs, dev-memo/design/2026-06-28-a3-link-audit-event-labels.md, dev-memo/deferred-audit-findings.md, dev-memo/run/queue.md, dev-memo/run/queue.linted, dev-memo/run/queue.reviewed, dev-memo/run/queue.governed, dev-memo/run/reviews/queue-review-102.md
Allowed-files carve-out (user-authorized 2026-06-28): apps/lawbar-desktop/tests/renderer-i18n.test.mjs is permitted for a COUNT-ACCURACY BUMP ONLY — the hard-coded `eventKind.*` catalog-key count `50` → `53` (the 3 new LINK_* keys) + the matching `50`→`53` in the test name/comment. NO other edit in that file; NO weakening/skipping/loosening/removing of any i18n assertion. The broader `apps/lawbar-desktop/tests/**` forbid otherwise stands (NO D1 test; NO other test edit).
Forbidden files: apps/lawbar-desktop/electron/** (NO change; NO D1 hook), apps/lawbar-desktop/src/** (NO source change), apps/lawbar-desktop/renderer/** EXCEPT the four named renderer files (labels.ts, catalog.ts, screens/auditEventLabels.ts, i18n/ui-strings-allowlist.json) — NO other renderer/UI change, NO new flow/screen/component, NO layout/behavior change, apps/lawbar-desktop/tests/** (NO D1 test; NO test edit — the i18n allowlist regen is data not a test), services/** source + docs/contracts/** source + schemas (NO source/schema/contract-semantic change — the tarballs carry the ALREADY-CORRECT source), services/case-box-persistence/src/sqlite/schema.ts (NO schema/`CURRENT_SCHEMA_VERSION` change), docs/adr/** (NO ADR edit), native/**, .claude/**, .github/**, AGENTS.md, CLAUDE.md, GEMINI.md, migrations/**, committed dev-memo/run/evidence/** marker/ledger/key/evidence-run files, committed keys, dev-memo/run/reports/** (untracked run-state — never staged), dev-memo/run/override-reason.md (untracked residue — never staged), Evidence-Genie-M0-Developer-Handover.md + xiaolai-dev-workflow-study.md (root intake — READ-ONLY); NO D1 resume/hook/test, NO anchor/seed IPC, NO IPC/preload/persistence/contract-source/schema change, NO unrelated dependency change, NO marking D1 closed, NO push/merge/branch-delete
Gates: broker /cc-suite:review-plan REQUIRED before continuing implementation — confirm: the combined lane is coherent (packaging + the 3 LINK_* labels are compile-coupled, must land together); the renderer change is LIMITED to the 3 audit-event labels (no layout/behavior/semantics/data-exposure); the narrow lock refresh (only the two file: integrity entries; never `rm lock && npm install`) yields a reproducible `npm ci`; the drift guard is a genuine semantic-payload comparison of both packages (not a LINK grep) that fails on drift; `package.json` changes only by the guard script (restore cosmetic churn); the Design artifact suffices for the UI gate (if review-plan says the UI-label design must be a SEPARATE artifact/lane, STOP, stop-point 1); D1 stays open; A0.7 — court-facing audit-label UI ⇒ `Requires-A07: yes` (custody 9b) unless review-plan records a concrete downgrade. If review-plan NEEDS-FIX, fix-forward; if a blocker per the stop-points, STOP. After implementation: `npm --prefix apps/lawbar-desktop run build` (tsc) GREEN against the refreshed contract; `node scripts/workflow/check-internal-tarballs.mjs` PASSES (+ demonstrably FAILS on injected drift, restored); `rm -rf apps/lawbar-desktop/node_modules && npm --prefix apps/lawbar-desktop ci` succeeds with resolved contract = 3 LINK_* kinds + persistence staged manifest (dep `case-box-contract: "0.1.0"`, no file:/link:); `npm --prefix apps/lawbar-desktop test` GREEN (incl. i18n guard); `npm --prefix apps/lawbar-desktop run dist` succeeds + the packaged contract has the 3 kinds; `scripts/workflow/check-contract-integrity.sh` exits 0; broker /cc-suite:audit + /cc-suite:verify on the diff (no Critical/High/Medium open before commit); A0.7 custody-9b gated PASS reported by the human BEFORE the impl commit (agent never receives/persists the HMAC key; stages no marker/evidence/key/report file) UNLESS review-plan downgrades Requires-A07.
Acceptance criteria: `apps/lawbar-desktop/.gitignore` no longer ignores the 2 `.tgz` + `manifest.json`; those 3 + `package-lock.json` are TRACKED + committed; the lock pins the committed `case-box-contract` integrity (NOT `XFMRa8b…`) so `npm ci` succeeds (no EINTEGRITY); `scripts/workflow/check-internal-tarballs.mjs` exists, semantic-payload-compares both packages, passes on committed tarballs, fails on injected drift; `renderer/i18n/labels.ts` `EVENT_KIND_ID` includes `LINK_CREATED`/`LINK_UNLINKED`/`LINK_RELINKED`; `renderer/i18n/catalog.ts` has the 3 `eventKind.LINK_*` strings; `tsc` build GREEN against the refreshed contract; a fresh `rm -rf node_modules && npm --prefix apps/lawbar-desktop ci` resolves the contract with all 3 LINK_* kinds; `npm --prefix apps/lawbar-desktop test` GREEN; `npm --prefix apps/lawbar-desktop run dist` exits 0 + packaged contract has the 3 kinds; `scripts/workflow/check-contract-integrity.sh` exits 0; `CURRENT_SCHEMA_VERSION` remains 12; `package.json` changed ONLY by the additive guard script; NO electron/src/IPC/preload/services/contract-source/schema change; NO renderer change beyond the 4 named files (audit labels + allowlist); `dev-memo/deferred-audit-findings.md` flips DESKTOP-DEPS-STALE-LOCK-01 to `Status | closed` (citing this WI + the post-fix proof) ONLY AFTER verification, LINK-IPC-T1-D1 stays `open`; broker review-plan READY + audit no Critical/High/Medium + verify ALL CLOSED; A0.7 custody-9b gated PASS recorded (or downgraded by review-plan); committed diff confined to Allowed files.
Risk flags: COMBINED packaging + court-facing audit-label UI fix — A0.7 COMMIT-GATED (custody 9b) per the UI surface, though the renderer change is only static audit-event labels (no geometry/marker/behavior). Commits BINARY tarballs (~320 KB) + refreshes the lock + adds a gate + 3 renderer labels. Load-bearing care points: (a) the committed lock MUST make `npm ci` reproducible (no EINTEGRITY); (b) the drift guard MUST be a real semantic-payload comparison (both packages, content hashes, metadata-ignored) that FAILS on injected drift; (c) the renderer change MUST be ONLY the 3 LINK_* labels (no layout/behavior/semantics/data-exposure; no evidence content); (d) restore any cosmetic `package.json` reformat; (e) D1 stays OPEN; (f) NO D1 hook/test/source code; (g) NO source-contract/schema/persistence change. Heavy-op risk: `npm ci` + `npm run dist` required + slow. If the UI-label design must be separate (stop-point 1), the labels need broader UI change (stop-point 2), the lock/package metadata churns beyond scope (stop-point 3), the guard needs restructuring (stop-point 4), an electron/src/IPC/persistence/schema/contract-semantic change is required (stop-point 5), or a reliable clean install/dist can't be achieved (stop-point 6) — STOP and report. No push/merge/branch-delete.
Acceptance criteria addendum: the i18n drift guard (`npm --prefix apps/lawbar-desktop test` i18n) stays GREEN — if `auditEventLabels.ts` is edited and shifts scanned line numbers, `renderer/i18n/ui-strings-allowlist.json` is regenerated to the exact scan; NO fixed user-facing prose literal is added outside the catalog.
Depends on: none
Commit boundary: governance (mark-reviewed + govern) run STANDALONE before the commit (never bundled with git commit); A0.7 custody-9b gated PASS reported by the human BEFORE the impl commit (unless review-plan downgrades Requires-A07). ONE combined commit — `.gitignore` + the 2 `.tgz` + `manifest.json` + `package-lock.json` + `package.json` (guard script) + `scripts/workflow/check-internal-tarballs.mjs` + `scripts/workflow/check-gates.sh` + `renderer/i18n/labels.ts` + `renderer/i18n/catalog.ts` (+ `renderer/screens/auditEventLabels.ts` + `renderer/i18n/ui-strings-allowlist.json` if needed) + `dev-memo/design/2026-06-28-a3-link-audit-event-labels.md` + `dev-memo/deferred-audit-findings.md` (DESKTOP-DEPS close, after verification) + governance artifacts (queue.md/.linted/.reviewed/.governed + reviews/queue-review-102.md), staged by exact path; NO electron/src/IPC/preload/services/contract-source/schema file; NO marker/evidence/key/report file; no push; no merge.
