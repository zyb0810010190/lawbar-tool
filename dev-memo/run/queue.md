## WI-FORMS-T3-IMPLEMENTATION-PLAN-00: plan the later implementation of T3 证据目录及说明 (Type: PLAN)
Type: PLAN
Scope: Documentation / design / governance ONLY — no product code, no native source, no tests, no package manifests, no schema, no fixtures, no IPC, no custody/marker, no JS shim, no A8, no renderer, no DOCX/PDF generation, NO forms implementation, NO T4/T5 work. CREATE ONE new plan document `dev-memo/plan-forms-t3-evidence-catalog-00.md` (the only PRODUCT/DESIGN file changed; the `dev-memo/run/queue.*` + `reviews/queue-review-120.md` governance artifacts are also modified per this WI's Commit boundary, as every governed WI does) that plans the LATER implementation of the T3 证据目录及说明 form under the binding DR-00 decisions (internal lawyer trial-review audience, NOT court filing; DOCX-first direction with NO renderer implementation authorized; label 证明内容; 页码 = physical bundle page range first, A10 卷X页Y supporting metadata only, never replacing 页码; no custody/seal/A8 implied). The plan MUST record: (1) product target (internal trial review, no court-template compliance claim); (2) output target (DOCX-first; the repo has NO DOCX generation mechanism today, so the plan RECORDS the required later renderer choice instead of implementing one; no PDF this phase); (3) data-model verdict — the four required columns 序号/证据名称/证明内容/页码 checked against the CURRENT contract + sqlite schema: 序号 synthesizable, 页码 supported by exhibit_page_range, but 证据名称 + 证明内容 (and the header 提交人诉讼地位 field) have NO persisted source, therefore SCHEMA MUTATION IS REQUIRED and the future implementation WI is marked BLOCKED PENDING A SCHEMA ADR (no direct code planned around the gap); (4) narrowest staged slice proposal (S0 schema ADR blocking-first -> S1 logical export model/adapter mirroring the A10-T6 deterministic pattern -> S2 optional UI preview gated on a Design artifact per UI-GATES -> S3 DOCX generation double-gated on the renderer decision + S1), authorizing NONE of them; (5) expected later tests (row ordering, missing/unknown page ranges, 证明内容 preserved verbatim, 卷X页Y supporting-only, no-T4/T5 regression, raw samples never fixtures unless separately redacted + authorized); (6) explicit non-decisions (no forms impl, no schema mutation, no renderer/DOCX/PDF, no custody/seal/A8, no T4/T5, no raw sample commit, no new runtime dependency). The raw sample files under `dev-memo/run/intake/**` are INPUT ONLY — read as reference, MUST remain untracked and unstaged. CURRENT_SCHEMA_VERSION unchanged. If review asks for implementation, REJECT as out of scope and report; if review says schema is required, that CONFIRMS the plan's blocked-pending-schema-ADR verdict (already recorded).
Source of truth: dev-memo/forms-spec-a10-t3-t5-sample-adr-00.md §A (sample structure) + §E (non-decisions, preserved) + §H DR-00 (binding Q1-Q4 resolutions, 2026-07-02); dev-memo/run/intake/forms-samples/孙乐驰-证据目录及说明-一审.pdf (READ-ONLY reference input, never staged); docs/adr/ADR-evidence-a10-court-fileable-export.md (A10-DESIGN-00 §10 forms gated behind form-spec); docs/product/evidence-m0-content-inventory.md (surface 4 证据目录); docs/contracts/case-box-contract/schemas/case-box-evidence-item.schema.json + services/case-box-persistence/src/sqlite/schema.ts (current data-model state the verdict is derived from — READ-ONLY); apps/lawbar-desktop/src/caseBox/export/a10CanonicalExportModel.ts (deterministic pattern S1 mirrors — READ-ONLY); .claude/rules/{evidence-genie,cc-suite,autonomy,execution-discipline,staging-hygiene}.md
Allowed files: dev-memo/plan-forms-t3-evidence-catalog-00.md (NEW plan doc), dev-memo/run/queue.md, dev-memo/run/queue.linted, dev-memo/run/queue.reviewed, dev-memo/run/queue.governed, dev-memo/run/reviews/queue-review-120.md
Forbidden files: dev-memo/run/intake/** (raw sample PDF/DOCX — INPUT ONLY; MUST NOT be staged/committed/edited/moved/deleted), apps/** (NO application/renderer/electron/test/package change), native/** (NO native source change), services/** + docs/contracts/** (NO services/schema/contract change; read-only inputs), services/case-box-persistence/src/sqlite/schema.ts (NO schema/CURRENT_SCHEMA_VERSION change), apps/lawbar-desktop/src/caseBox/export/** (A10 modules — READ-ONLY), docs/adr/** + docs/reference/** + docs/product/** (READ-ONLY), dev-memo/forms-spec-a10-t3-t5-sample-adr-00.md (READ-ONLY this lane — the plan cites it, does not amend it), dev-memo/evidence-a10-closeout-00.md + dev-memo/deferred-audit-findings.md (READ-ONLY), scripts/** + .claude/** + .github/** + migrations/**, dev-memo/run/evidence/** (gitignored custody marker/ledger — never touched/staged), committed keys, dev-memo/run/reports/** + dev-memo/run/override-reason.md + dev-memo/run/autonomous-next.md (untracked run-state — never staged/edited), Evidence-Genie-M0-Developer-Handover.md + xiaolai-dev-workflow-study.md (root intake — READ-ONLY); NO A8; NO T3/T4/T5 forms implementation; NO schema mutation; NO DOCX/PDF renderer; NO custody/marker/seal; NO JS shim; NO push-without-authorization beyond this WI's scoped branch push + PR; NO merge; NO branch delete
Gates: broker /cc-suite:review-plan REQUIRED for this docs/design PLAN lane — confirm: the plan stays implementation-free (authorizes NO slice); the schema-blocked verdict is correctly derived from the current contract/schema state (no title / proof-statement / litigation-position fields exist); the staged S0-S3 slices keep T4/T5 gated and the forms-spec §E non-decisions intact; the DOCX renderer decision is RECORDED as a separate future decision, not made; DR-00 bindings are respected (证明内容 label, 页码 physical-range-first, 卷X页Y supporting-only); raw samples not staged. STOP-points: (1) if the plan misstates DR-00 or the schema state -> fix-forward in the plan; (2) if review requests forms/code/schema/renderer implementation -> REJECT as out of scope and report; (3) if any non-docs file (or the raw samples) would be staged -> STOP (scope drift). After: changed files are documentation/governance only; `scripts/workflow/check-contract-integrity.sh` exits 0; `scripts/workflow/check-queue.sh` PASSES; broker review-plan READY (docs-only; a separate /cc-suite:audit is not required for a docs-only plan note). NO full desktop suite (docs-only). Batch is 1/3 (marker e871d4d); NOT due — no closeout this lane.
Acceptance criteria: `dev-memo/plan-forms-t3-evidence-catalog-00.md` EXISTS and CONTAINS: the internal-trial-review product target with no court-template compliance claim; the DOCX-first output target recording that the repo lacks any DOCX mechanism and the renderer choice is a required LATER decision (candidates recorded, none chosen); the data-model table concluding 序号 synthesizable + 页码 supported + 证据名称/证明内容/提交人诉讼地位 missing -> SCHEMA MUTATION REQUIRED -> future implementation WI BLOCKED PENDING SCHEMA ADR; the staged S0->S3 slice proposal authorizing none; the expected-tests section (ordering, missing/unknown ranges, 证明内容 verbatim, 卷X页Y supporting-only, no-T4/T5 regression, no raw-sample fixtures); the explicit non-decisions list; T4/T5 explicitly NOT expanded. The committed diff CONTAINS ONLY the new plan doc + governance artifacts and does NOT stage `dev-memo/run/intake/**` or any code/native/schema/test/fixture/IPC/manifest file; `check-contract-integrity` PASSES (exit 0); `check-queue` PASSES; broker review-plan returns READY; review REJECTS any implementation request as out of scope.
Risk flags: LOW-RISK documentation / design PLAN lane — ONE new plan doc + governance artifacts only; NO code/native/schema/test/fixture/IPC/manifest/renderer change, NO A8/forms/custody/JS-shim, NO staging of the raw sample files. Load-bearing care points: (a) ACCURACY — the schema-blocked verdict must match the real contract/schema state and DR-00 exactly; do NOT invent fields or make the renderer decision; (b) IMPLEMENTATION-FREE — the plan proposes slices but authorizes none; (c) GATING PRESERVED — T4 under-specified, T5 design-gated, forms-spec §E non-decisions intact; (d) INPUT-ONLY SAMPLES — never stage `dev-memo/run/intake/**`. If any cannot hold, STOP and report. Push limited to this WI's scoped branch + PR open; no merge; no branch delete.
Depends on: none
Commit boundary: governance (mark-reviewed + govern) run STANDALONE before the commit (never bundled with git commit). ONE documentation commit — dev-memo/plan-forms-t3-evidence-catalog-00.md + governance artifacts (queue.md/.linted/.reviewed/.governed + reviews/queue-review-120.md), staged by exact path; NO apps/native/services/schema/test/fixture/manifest/IPC file; NO staging of dev-memo/run/intake/** (raw samples stay untracked); NO marker/evidence/key/report file; push of the scoped WI branch + PR open authorized by the user's Stage 1 directive; no merge; no branch delete.
