## WI-GQ1: Defensive hardening of govern-queue.sh (atomic write + post-write self-verify + verbose success)
Type: WORKFLOW
Scope: Add defense-in-depth to `scripts/workflow/govern-queue.sh` so any future silent-stale `queue.governed` becomes a LOUD failure of the script itself, reducing operator friction — WITHOUT changing the content-bound governance model, the queue semantics, or any guard. The script is NOT buggy (a scratch reproduction refreshed `queue.governed` correctly across standalone/piped/compound/sh shapes; the live incidents were unreproducible operational artifacts and the WI-G1 content-binding guard already failed closed). Changes to `write_governed`: (1) ATOMIC WRITE — write the `governed=<kind> <ts>` + `queue_sha256=<64-hex>` content to a temp file IN THE SAME DIRECTORY as queue.governed (`$RUN`), then `mv` it into place; clean up the temp on any failure; the temp name must not collide with a protected run-control filename. (2) POST-WRITE SELF-VERIFY — after the mv, re-read the written queue.governed, recompute `sha256(queue.md)`, and `exit 1` (fail loud, after `rm -f` the bad file) if the recorded `queue_sha256` does NOT equal the recomputed hash OR the canonical `queue_sha256=<64-lowercase-hex>` line is absent. (3) VERBOSE SUCCESS — the success message includes the recorded hash and the ABSOLUTE queue.governed path so automation can assert immediately. PRESERVE: the `governed=...` + `queue_sha256=` format byte-compatible with `batch-commit-guard.sh`'s reader (one canonical `^queue_sha256=[0-9a-f]{64}$` line); BOTH the `--human-approved` and lint+review paths still write the hash via the same write_governed; every existing fail-closed branch (`rm -f $GOV; exit 1` on missing/unreadable/unhashable/bad-length/write-fail) stays. NO change to batch-commit-guard.sh, block-run-control-bash-write.sh, .claude/settings.json, product source, or queue semantics. Add `scripts/workflow/govern-queue.test.sh` covering the four invocation shapes + the fail-loud self-verify. If implementation requires touching batch-commit-guard.sh / block-run-control-bash-write.sh / changing queue semantics / weakening content-bound enforcement, STOP and report.
Source of truth: scripts/workflow/govern-queue.sh (the script to harden); scripts/workflow/check-queue.test.sh (the workflow-script test precedent + style); .claude/hooks/batch-commit-guard.sh (the reader contract: one canonical `^queue_sha256=[0-9a-f]{64}$` line — the output format MUST stay compatible, READ-ONLY reference, do not edit); dev-memo/plan-batch-govern-queue-hardening-00.md (proposal); dev-memo/study/2026-06-05-batch-audit-70.md (the CCSUITE discovery reproduction)
Allowed files: scripts/workflow/govern-queue.sh, scripts/workflow/govern-queue.test.sh, dev-memo/deferred-audit-findings.md, dev-memo/run/queue.md, dev-memo/run/queue.linted, dev-memo/run/queue.reviewed, dev-memo/run/queue.governed, dev-memo/run/reviews/queue-review-018.md
Forbidden files: .claude/hooks/batch-commit-guard.sh, .claude/hooks/block-run-control-bash-write.sh, .claude/hooks/**, .claude/settings.json, apps/**, services/**, docs/contracts/**, scripts/workflow/check-queue.sh, scripts/workflow/batch-closeout.mjs, scripts/workflow/mark-queue-reviewed.sh
Gates: bash scripts/workflow/govern-queue.test.sh && bash scripts/workflow/check-queue.test.sh && node scripts/workflow/batch-closeout.test.mjs
Acceptance criteria: `govern-queue.sh` writes `queue.governed` ATOMICALLY (temp-in-$RUN + mv; temp cleaned up on failure) and then SELF-VERIFIES (re-reads the file, recomputes sha256(queue.md), exits 1 + removes the file if the recorded hash is absent/mismatched); the success output includes the recorded 64-hex hash AND the absolute queue.governed path; the written format is still exactly one `governed=<kind> <ISO8601>` line + one canonical `queue_sha256=<64-lowercase-hex>` line (byte-compatible with batch-commit-guard.sh's `^queue_sha256=[0-9a-f]{64}$` reader); both the `--human-approved` and lint+review paths still produce a content-bound governed file; all prior fail-closed branches still `rm` the file + exit nonzero; a new `scripts/workflow/govern-queue.test.sh` exists and PASSES (`bash scripts/workflow/govern-queue.test.sh` exits 0), asserting: (a) standalone, (b) `| tail -1` piped, (c) compound `... ; govern | tail ; ...`, and (d) `sh govern-queue.sh` each refresh queue.governed to sha256(queue.md) against a scratch CLAUDE_PROJECT_DIR; (e) a forced hash-mismatch / unhashable-queue case makes the script exit nonzero AND leave NO stale queue.governed; the existing `scripts/workflow/check-queue.test.sh` still passes (no regression); no batch-commit-guard.sh / block-run-control-bash-write.sh / settings.json / product/source / queue-semantics change
Risk flags: WORKFLOW hardening of the single governance writer; output format MUST stay byte-compatible with batch-commit-guard.sh's canonical-hash reader (regression here would break ALL future governed commits — covered by the format-compat acceptance + check-queue/batch-closeout no-regression); atomic temp must live in $RUN (same fs) and not collide with a protected run-control filename; self-verify must fail CLOSED (rm + exit 1), never leave a half-written file; broker review-plan + audit + verify required; not Type UI (no renderer path) so no Design-artifact gate
Depends on: none
Commit boundary: one local commit for the govern-queue.sh hardening + the new govern-queue.test.sh (+ optional deferred-findings note)
