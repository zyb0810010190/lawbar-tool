## WI-ENA8: A0.7 renderer-conformance harness command implementation (no marker)
Type: IMPL
Scope: Implement the minimal A0.7 renderer-conformance harness in the Swift native package (native/evidence-core-swift/**). The harness LOADS the existing synthetic fixture (Tests/EvidenceCoreSmokeTests/Fixtures/synthetic-twopage.pdf), READS the committed oracle/manifest (Tests/EvidenceCoreSmokeTests/Fixtures/a07-renderer-conformance/oracle.json — expected values decoded from disk, NOT hardcoded, so the harness cannot self-fulfill a pass), computes observed page count + page-box (mediaBox) extents + normalized sample-point values, compares observed vs the oracle within the oracle's own tolerance, and emits a DETERMINISTIC classified verdict: status pass/fail/inconclusive + classification. The harness MUST NOT write any A0.7 marker, MUST NOT touch dev-memo/run/evidence/**, MUST NOT create provenance/HMAC, MUST NOT add a tamper/fabrication guard, MUST NOT add EVW5 hooks, anchors, citation/page-identity persistence, PDFView/UI conversion, export, OCR/AI/cloud/auth/network, or Evidence UI/product behavior. The smallest Swift testable harness API + tests proving the exact result shape; do NOT alter the native/evidence-core JS shim (its not_implemented failure semantics stay intact). No command claims marker-validity. Keep implementation under native/evidence-core-swift/**. Do NOT delete/move the root intake files.
Result semantics: pass ONLY if every required fixture/oracle assertion is observed within tolerance; fail if a required assertion mismatches, the fixture is corrupt, the oracle is corrupt/undecodable, not_implemented, or required data is absent; inconclusive ONLY for an explicitly documented condition that is neither pass nor implementation-missing (the oracle defines no checkable assertions) and inconclusive is NOT pass. Failure classifications: class_1_normalization_math_bug (box correct but a normalized value wrong); class_2_geometry_source_instability (renderer page count / page box disagrees with recorded geometry) — a Class-2 result forces STOP/reassess and is NEVER downgraded to pass; fixture_or_oracle_invalid; not_implemented (PDFKit unavailable -> harness cannot execute -> FAIL); inconclusive_no_checkable_assertions. not_implemented is a FAIL.
Source of truth: docs/adr/ADR-evidence-a07-renderer-conformance-gate.md (A07-GATE-00 §3 oracle, §4 classification Class-1/Class-2 + not_implemented=fail + no-Class-2-downgrade, §5 marker policy no-marker-this-lane, §6 command/contract boundary preserve not_implemented-fails + JS shim unchanged); native/evidence-core-swift/Tests/EvidenceCoreSmokeTests/Fixtures/a07-renderer-conformance/{manifest.json,oracle.json,README.md} (the committed oracle authority materialized in WI-ENA7); native/evidence-core-swift/Tests/EvidenceCoreSmokeTests/Fixtures/synthetic-twopage.pdf (the fixture); the ENA3/ENA4/ENA5 probes (page count, mediaBox read, coordinate roundtrip) whose logic the harness composes; .claude/rules/evidence-genie.md (invariants 3 A0.7-first/Class-1-vs-Class-2, 10 not_implemented-fails); .claude/rules/autonomy.md (this impl lane is user-authorized).
Allowed files: native/evidence-core-swift/** (Sources/** harness API, Tests/** harness tests, README/status note), dev-memo/** (queue/governance/audit/study artifacts), .github/workflows/evidence-core-swift-smoke.yml (ONLY if a narrow test-command update is required; otherwise byte-unchanged)
Forbidden files: apps/**, native/evidence-core/** (the JS shim — NO change), services/**, docs/contracts/**, docs/product/**, .claude/hooks/**, .claude/settings.json, .claude/rules/**, AGENTS.md, CLAUDE.md, GEMINI.md, dev-memo/run/evidence/**, Evidence-Genie-M0-Developer-Handover.md (root), xiaolai-dev-workflow-study.md, package.json; NO A0.7 marker files; NO marker/HMAC/provenance implementation; NO tamper/fabrication guard implementation; NO EVW5 hooks; NO anchors; NO citation/page-identity persistence; NO PDFView/UI conversion; NO export/OCR/AI/cloud/auth/network
Gates: broker /cc-suite:review-plan REQUIRED before impl (first A0.7 harness implementation — no self-review); if review-plan returns READY but scopes into marker/provenance/tamper/hooks/UI/product behavior, STOP and ask; broker /cc-suite:audit + /cc-suite:verify after impl (first A0.7 harness); swift build and swift test pass for native/evidence-core-swift (Swift available locally); the harness reads the committed manifest/oracle from disk (observed values compared to the pre-existing oracle, NOT hardcoded as a self-fulfilling pass); status can be pass ONLY from matched oracle values within tolerance; inconclusive is not pass; not_implemented is fail; a Class-2 result cannot downgrade to pass; a test proves the exact result shape; NO A0.7 marker and nothing under dev-memo/run/evidence/ (grep); NO marker/HMAC/provenance/tamper-guard code (grep); NO hooks/UI/product/apps change; the native/evidence-core JS shim is byte-unchanged; scripts/workflow/check-contract-integrity.sh exits 0; npm --prefix apps/lawbar-desktop test passes (610/610); scripts/workflow/check-gates.sh returns GATES OK
Acceptance criteria: native/evidence-core-swift/ contains an A0.7 harness (EvidenceCoreA07Harness.run(fixtureURL:oracleURL:) -> A07ConformanceResult{status,classification,observedPageCount,detail} with a deterministic orderedFields() result shape carrying isMarker=false) plus a pure evaluate(observed:oracle:) classifier; the harness decodes oracle.json from disk and on the committed fixture returns status=pass classification=ok observedPageCount=2; tests prove the exact result shape AND each classification: class_1 (normalized mismatch), class_2 (page-count or box mismatch), fixture_or_oracle_invalid (missing oracle / non-PDF fixture), inconclusive_no_checkable_assertions (empty-assertion oracle, not pass); swift test --package-path native/evidence-core-swift passes; grep shows NO marker / no dev-memo/run/evidence / no HMAC/provenance / no tamper-guard / no anchors-citation-persistence / no PDFView code; the macOS CI workflow is byte-unchanged or changed only by a narrow test-command line; the native/evidence-core JS shim is byte-unchanged; README states the harness classifies but writes no marker; npm desktop test pass 610 fail 0; check-gates GATES OK; broker audit returns no Critical/High/Medium and verify confirms ALL CLOSED; the committed diff is confined to native/evidence-core-swift/** plus (at most) the one macOS smoke workflow file
Risk flags: HIGH-RISK -- first A0.7 renderer-conformance HARNESS implementation (the apex Evidence gate's executable surface). broker review-plan REQUIRED + broker audit+verify after. The harness must read the INDEPENDENT committed oracle (not self-supply a pass), must classify Class-1 vs Class-2 without downgrading Class-2, must treat not_implemented + inconclusive as non-pass, and must write NO marker (no provenance/HMAC, no tamper guard, no dev-memo/run/evidence/**). The JS shim is untouched; no new dependency. A passing harness result is NOT an A0.7 marker (the durable marker is a separate authorized WI). Swift toolchain confirmed available locally (5.9.2).
Depends on: none (A07-GATE-00 gate design and the WI-ENA7 fixture/oracle artifacts are already merged to main)
Commit boundary: one local commit -- native/evidence-core-swift/** plus (at most) the one macOS smoke workflow file, staged by exact path; no push
