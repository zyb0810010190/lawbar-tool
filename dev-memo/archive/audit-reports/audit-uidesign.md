Findings:

| id | severity | file | why | fix |
|---|---:|---|---|---|
| A3-LINK-UI-DESIGN-01 | Medium | [dev-memo/design/2026-06-26-audited-evidence-links-ui.md](/Users/zhongyibao/ClaudeProjects/lawbar-tool/dev-memo/design/2026-06-26-audited-evidence-links-ui.md:31) | The artifact says the lazy-loaded row list is populated via `api.listLinks({ matterId })`, then says each row carries `flags` and tests require rows to render `flag`. But `RendererLink` / `LINK_RESPONSE_FIELDS` only contains `id, matter_id, source_type, source_id, anchor_id, status, created_at, unlinked_at, unlink_reason`; `exportFlag` exists only in `ExportCitationResult.citations`. This leaves the UI impl with an unsafe ambiguity: either invent `exportFlag` on `RendererLink`, change IPC, or silently add an export call not specified in the placement flow. | Clarify the row data contract: either row flags appear only in the export-citations panel, or the disclosure load must call `exportLinkCitations` and join `citations[]` to `RendererLink[]` by link id. Explicitly forbid adding `exportFlag` to `LINK_RESPONSE_FIELDS`. |
| A3-LINK-UI-DESIGN-02 | Low | [dev-memo/design/2026-06-26-audited-evidence-links-ui.md](/Users/zhongyibao/ClaudeProjects/lawbar-tool/dev-memo/design/2026-06-26-audited-evidence-links-ui.md:86) | Error handling is internally mixed: it says renderer shows the server safe message verbatim and adds no i18n at the error boundary, then says envelope codes should be mapped to catalog copy. Both can be valid designs, but not simultaneously as written. | Pick one: server-safe message verbatim, or renderer maps known codes to catalog copy and falls back to server safe message. |

Other checks passed: A0.7 is correctly dev/commit-time only; no runtime marker UI is designed; unlink/relink DTOs correctly include `matterId`; export shape is `{ citations, byFlag }`; confidentiality excludes raw evidence and chain internals; the lane remains design-only and defers implementation/code changes.

AUDIT-VERDICT: FAIL C0 H0 M1 L1
