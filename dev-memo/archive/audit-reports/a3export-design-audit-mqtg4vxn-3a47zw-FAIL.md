**Findings**

Medium — [ADR-evidence-a3-export-degradation.md](/Users/zhongyibao/ClaudeProjects/lawbar-tool/docs/adr/ADR-evidence-a3-export-degradation.md:62) and [plan-batch-casebox-evidence-a3-export-degradation-00.md](/Users/zhongyibao/ClaudeProjects/lawbar-tool/dev-memo/plan-batch-casebox-evidence-a3-export-degradation-00.md:33): `REPLACED` is internally inconsistent and not derivable from the stated inputs. The ADR says every `needs_review` emits `NEEDS_REVIEW`, but later says `REPLACED` is keyed on current replacement signals. The plan tests `valid-but-replaced`, but resolver-first ordering means replacement should refresh to `needs_review`, not remain `valid`. Concrete fix: define export-flag precedence and inputs explicitly, e.g. `broken -> BROKEN`; `needs_review caused by document replacement/supersession -> REPLACED`; other `needs_review -> NEEDS_REVIEW`; `valid` can only become `AMBIGUOUS`/`NON_CITABLE` from A1/A10 citation checks. Add either a resolver status reason or authorize reading document lifecycle/replacement signals for flag derivation.

Medium — [ADR-evidence-a3-export-degradation.md](/Users/zhongyibao/ClaudeProjects/lawbar-tool/docs/adr/ADR-evidence-a3-export-degradation.md:71): the ADR permits omission “when explicitly chosen and justified,” which weakens A10’s “every in-app link -> textual 卷X页Y or matching `ExportCitationFlag`; never dropped” invariant. This is not just a silence issue; A10 forbids dropping the link/citation mapping. Concrete fix: remove the omission escape hatch. Require a deterministic `BROKEN` export object/flag for every in-app link whose target is missing, preserving link/source identity even when citation is empty.

Low — [plan-batch-casebox-evidence-a3-export-degradation-00.md](/Users/zhongyibao/ClaudeProjects/lawbar-tool/dev-memo/plan-batch-casebox-evidence-a3-export-degradation-00.md:55): the test strategy says stale stored status is “refreshed, never trusted as-is,” which is correct, but it should also require a regression for replacement/status-reason flagging once the `REPLACED` decision above is fixed. Concrete fix: add a test row for replacement/supersession producing `linkStatus = needs_review` plus `exportFlag = REPLACED` if that is the chosen precedence.

The docs-only and A0.7 classification are otherwise correct: the design WI itself is not A0.7-gated, while future export implementation WIs are gated. The A1 DocumentPage-only and no optimized-rendition rules are mostly preserved, but the two Medium issues block PASS because they leave an A10 weakening and a replacement flag ambiguity.

AUDIT-VERDICT: FAIL C0 H0 M2 L1
