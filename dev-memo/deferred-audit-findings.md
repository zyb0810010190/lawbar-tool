# Deferred Audit Findings — Backlog

**Status**: append-only-ish backlog of cc-suite audit findings that were not fixed in their originating WI. Started 2026-05-21.

**Purpose**: provide a durable, scannable location for every Low (and any escalated Medium-or-higher) the assistant chose not to fix inside its originating WI. The commit-message recording per `.claude/rules/cc-suite.md` §"Audit remediation policy" is per-WI; this file is the project-wide rollup so future WIs can find prior deferrals without scraping `git log`.

**Update rules**:

- **Append on every WI close** when the audit deferred any finding. Group entries by WI / commit.
- **Update `status`** (open / closed / superseded) when a later WI addresses an open entry. Do NOT delete rows — flip the status and add a "resolved-in" reference. The historical evidence stays visible.
- **Group several findings into one row** only when they share severity + deferral category + target backlog label. Otherwise keep one row per finding for greppability.
- **Security-adjacent Lows** (SSRF / TLS / DNS / auth / sandbox per `.claude/rules/security-boundary.md`) MUST NOT appear here as `status: open` — `.claude/skills/security-wi-loop/SKILL.md` §6 requires escalation, not deferral, for those.

**Column legend**:

- `WI` — work-item label and resolution commit short hash.
- `Audit job` — `/cc-suite:status`-retrievable Path-1 job id from the originating WI.
- `Verify job` — `/cc-suite:status`-retrievable verify-pass job id that recorded the deferral.
- `Finding ID` — the auditor's identifier (e.g. `F2.1`, `Dim 3 #1`).
- `Severity` — Critical / High / Medium / Low. Anything above Low SHOULD appear as `status: escalated`, NOT `status: open` per `.claude/rules/cc-suite.md` §"Audit remediation policy" item 1.
- `Reason for deferral` — one sentence citing which clause of §"Resolution rules" item 2 applies.
- `Target` — future WI label, backlog tag, or `cleanup-accepted (no follow-up planned)`.
- `Safe-to-proceed?` — `YES` / `NO`. `NO` means this row should already be an escalation, not a deferral.
- `Status` — `open` / `closed` / `superseded`. `closed` means a later WI addressed it (cite the resolution commit in `Notes`). `superseded` means the underlying audit pattern stopped applying (e.g. the affected code was deleted).
- `Notes` — free-form context + resolution-commit reference once closed.

---

## Staging-guard detection hardening (WI-SCAFFOLD-006, commit `<pending>`)

Closes the gap surfaced when WI-SCAFFOLD-005 removed the `if` filters so all git guards run on
every Bash call: `batch-commit-guard.sh` had statement-aware command-word detection (WI-SCAFFOLD-004)
but the two staging guards still used substring/boundary matching, so path-prefixed forms slipped
them even when invoked. Ported the command-word detection (path-prefix, wrappers, env assignments,
git global options) into both staging guards.

| Finding ID | Severity | Reason for deferral / disposition | Target | Safe? | Status | Notes |
|---|---|---|---|---|---|---|
| BGAA-1 | High | FIXED in WI-SCAFFOLD-006 — `block-git-add-all.sh` now detects broad `git add` across `/usr/bin/git`, `./git`, `\git`, wrappers (`env`/`command`/`exec`/`time`/…), env assignments, and global options (`-c`/`-C`/`--no-pager`); also catches quoted broad pathspecs (`"*"`, `"."`). | — | YES | closed | resolved-in `<pending>`; tests `block-git-add-all.test.sh` (30 cases) |
| BCSA-1 | High | FIXED in WI-SCAFFOLD-006 — `block-commit-stage-all.sh` now detects `git commit -a/--all/-am…` across the same git forms; quoted message content stripped first so `-a` in a message is ignored. | — | YES | closed | resolved-in `<pending>`; tests `block-commit-stage-all.test.sh` (26 cases) |
| BGAA/BCSA-2 | Low | Deferred (bounded, mirrors batch-commit-guard) — both staging guards inherit the same out-of-scope limits: command-substitution / variable-indirected forms, arg-taking wrapper flags (`env -u NAME`). | WI: shell-aware parsing (shared with BCG-8/9/10, BRCBW path-indirection) | YES | open | consistent with the rest of the guard suite |

---

## Independent enforcement-hook audits — post-canary (WI-SCAFFOLD-004, commit `059e7f6`)

Three independent cc-suite full audits of the workflow enforcement hooks, run while Codex exec was responsive. Severities below are my adjudicated values; the hooks' own headers state the threat model is "cooperative agent + direct-write block, NOT cryptographic", so most bypasses are defense-in-depth gaps, not active Criticals. User (2026-05-31) authorized fixing ONLY the batch-commit-guard git-detection set + rev-list fail-open in WI-SCAFFOLD-004; path-indirection and token-nonce hardening are explicitly deferred to later WIs.

| Audit job | Scope |
|---|---|
| `audit-mpuesqmt-4zzpxr` | `.claude/hooks/batch-commit-guard.sh` |
| `audit-mpuesqil-hgp1vs` | `.claude/hooks/block-run-control-bash-write.sh` |
| `audit-mpuesqrc-zmw5b5` | `.claude/hooks/protect-run-control.sh` |

| Finding ID | Severity | Reason for deferral / disposition | Target | Safe? | Status | Notes |
|---|---|---|---|---|---|---|
| BCG-1 | High | FIXED in WI-SCAFFOLD-004 — git-detection now statement-aware: `/usr/bin/git commit`, `git -c k=v commit`, `git --no-pager commit`, `FOO=bar git commit`, plus command-prefix wrappers `\git` / `command` / `exec` / `time` / `env [-i] FOO=bar git` (scope-extension, audit-mpufm338-2gtelo #1) | — | YES | closed | resolved-in `059e7f6`; tests `batch-commit-guard-detect.test.sh` (28 cases) |
| BCG-2 | High | FIXED in WI-SCAFFOLD-004 — multiple `git commit` in one Bash call now denied (one decision != many commits) | — | YES | closed | resolved-in `059e7f6` |
| BCG-3 | High | FIXED in WI-SCAFFOLD-004 — `rev-list` non-zero/non-numeric now denies, in BOTH the final count AND the divergent-base helpers (was fail-open COUNT=0; audit-mpufm338-2gtelo #4) | — | YES | closed | resolved-in `059e7f6` |
| BCG-4 | High | CLOSED — gated-mode `human.ack` is now consume-on-success: the commit is authorized (`exit 0`) ONLY if `rm -f "$RUN/human.ack" && [ ! -e ]` confirms the single-use token was actually removed; if removal fails the token persists, so the hook now DENIES (fail-closed) instead of allowing a reusable token. | WI-302 (BATCH-BCG-HARDENING-00; `wi-302-bcg45-token-atomicity`; plan `dev-memo/plan-wi-302-bcg45-token-atomicity.md`) | YES | closed | resolved-in `f2d793f`'s successor (this WI's commit); cc-suite review-plan `review-plan-mq0hxn25-e81iy9` (READY, after 2 NEEDS-FIX rounds); tests `batch-commit-guard-base.test.sh` BCG-4 happy/rm-fail/no-ack (perm-forced rm-fail, root-skip-guarded). |
| BCG-5 | High | CLOSED — batch-mode `human.override` is now atomically consumed: `OVERRIDE=1` (which bypasses risk/governed/count) is granted ONLY when, in order, (1) the override reason (first line of `override-reason.md`) is readable + non-empty — checked BEFORE removal so a blank/unreadable reason denies WITHOUT spending the token; (2) the token is removed and confirmed gone; (3) the "consumed" audit line is appended. Any failure → deny (fail-closed). rm-then-log ordering means `log.md` never records a "consumed" override that is still present or had no reason (prior log-then-rm could). | WI-302 (BATCH-BCG-HARDENING-00; `wi-302-bcg45-token-atomicity`) | YES | closed | resolved-in this WI's commit; review-plan `review-plan-mq0hxn25-e81iy9` (READY); tests BCG-5 happy/blank-reason/unreadable-reason/append-fail (root-portable via directory fixtures, always run) + rm-fail (chmod-unwritable, non-root only with explicit SKIP under root) → audit `audit-mq0i47h7-xni5ci` (C0 H0 M0, 1 Low root-coverage → fixed) → verify `verify-mq0i9tz2-m5ztwy` (ALL CLOSED). Suite 40→48. |
| BCG-6 | High | CLOSED by WI-G1 (was: open, user-authorized deferral) — the governed-mode gate in `.claude/hooks/batch-commit-guard.sh` trusted only the PRESENCE of `queue.governed`, and `scripts/workflow/govern-queue.sh` wrote it as a bare timestamp with NO binding to `queue.md` content, so a post-governance `queue.md` edit (new WI, widened Allowed-files) was undetected and still authorized commits (GOVERNANCE-CHAIN-001; `dev-memo/hook-audit-canary-01.md`). | WI-G1 (WORKFLOW-GOVERNANCE-INTEGRITY-00; `wi-g1-govern-content-bind`) | YES | closed | CLOSED by WI-G1 — `govern-queue.sh` now records `queue_sha256=sha256(queue.md)` into `queue.governed` (both the lint+review and `--human-approved` paths; fails closed if queue.md is missing/unhashable), and the guard recomputes `sha256(queue.md)` at the governed-mode gate and DENIES (strictly additive, after the presence check) when the recorded hash is absent, malformed (not 64 lowercase hex), unreadable, or MISMATCHED. A portable `shasum → sha256sum → openssl` helper is shared text-identically by writer + guard. Tests: `batch-commit-guard-base.test.sh` gains 7 BCG-6 cases (unchanged-passes / post-governance-edit-denies / missing-hash-denies / malformed-hash-denies×3 / missing-queue.md-denies / re-govern-allows / govern-queue-fails-closed-on-missing-queue.md) = 57 pass; the collateral harnesses `batch-commit-guard-detect.test.sh` (28 pass) and `scripts/workflow/batch-closeout.test.mjs` (28 pass) were updated to content-bound governed fixtures (setup-only, no deny expectation weakened — amended into WI-G1 Allowed-files, re-review `review-plan-mq1zfopj-e9lvdy` PASS). resolved-in this WI's commit; cc-suite review-plan `review-plan-mq1yykph-48tr82` (queue PASS) + the per-WI WI-G1 audit/verify chain. |
| BCG-7 | Medium | CLOSED — `batch-commit-guard.sh` now validates batch config FAIL-CLOSED before any integer compare: `AUTO_ADVANCE_MAX` must be one of the documented modes {1,3,10} (string `case`), and `BATCH_AUDIT_EVERY`, when present, must be a bare integer in [1,100] with its digit-length bounded to ≤3 BEFORE any `$(( ))`/`[ -ge ]` so a huge-digit value can no longer overflow/crash the compare and silently skip the breaker (the fail-open). An unset/empty `BATCH_AUDIT_EVERY` preserves the documented `EVERY=MAX` fallback (MAX already validated). Only a WHITESPACE-SEPARATED trailing `# comment` is stripped before validation (audit Low fix): `VAR=3#junk` is a literal → fails closed, `VAR=3 # note` → `3`. | WI-301 (BATCH-BCG-HARDENING-00; `wi-301-bcg7-config-validation`; plan `dev-memo/plan-wi-301-bcg7-config-validation.md`) | YES | closed | resolved-in `f2d793f` (PR #46); cc-suite review-plan `review-plan-mq0gsk2l-zjy8fn` (READY-WITH-LOW) → audit `audit-mq0gye8z-hbqghu` (C0 H0 M0, 1 Low comment-strip → fixed) → verify `verify-mq0h3ea7-p8tucq` (ALL CLOSED); tests `batch-commit-guard-base.test.sh` +14 (5 genuine red-before: MAX=5/MAX=huge/EVERY=abc/EVERY=huge/EVERY=101; + MAX=abc/EVERY=0 regression; 3 valid ALLOWs; 4 comment-strip cases). Suite 26→40. |
| BCG-8 | Low | Deferred per user (no alias resolution) — git aliases that invoke commit (`git -c alias.x=commit x`) evade subcommand detection (audit-mpufm338-2gtelo #2). Esoteric; generic alias resolution in-hook is heavy. | WI: (only if a real alias-commit workflow appears) | YES | open | my calibration Low vs Codex Critical — needs a deliberately commit-aliased subcommand |
| BCG-9 | Medium | CLOSED by WI-G2 (was: open, user-authorized deferral) — `count_git_commits()` in `.claude/hooks/batch-commit-guard.sh` split statements with a GLOBAL quote-blind `awk` gsub, so a single commit whose `-m` message contained `; git commit …` (or `\|`/`&` + the text `git commit`) was split and wrongly counted as 2 → DENIED (audit-mpufm338-2gtelo #3). Failed CLOSED (over-denied a legit commit), not a security bypass. | WI-G2 (WORKFLOW-GOVERNANCE-INTEGRITY-00; `wi-g2-quote-aware-count`) | YES | closed | CLOSED by WI-G2 — a quote-aware neutralize pass now precedes the split: a 3-state machine (unquoted / single / double, honoring backslash escapes outside single quotes per POSIX) replaces `;`/`\|`/`&` that occur INSIDE a quoted span with `_`, so the split only breaks on OUT-OF-QUOTE separators; UNBALANCED quotes fall back to the original line (over-deny, never under-count). Genuine chained `git commit && git commit` (separators outside quotes) still counts 2 → DENY. `batch-commit-guard-detect.test.sh` gains 10 BCG-9 cases (single commit with `;`/`\|`/`&`+`git commit` in a double- or single-quoted message = 1; separators-only message = 1; real chains after/between quoted messages = 2; unbalanced-quote fallback = 2) = 38 pass; base 60 + closeout 28 unchanged. Bounded quote handling, NOT a full shell parser (a separator in a message spanning multiple physical lines is out of scope → over-deny). resolved-in this WI's commit; cc-suite review-plan `review-plan-mq1yykph-48tr82` (queue PASS) + the per-WI WI-G2 audit/verify chain. |
| BCG-10 | Low | Deferred (bounded unwrap only) — command-prefix wrappers with arg-taking flags (`env -u NAME`, `exec -a NAME`) can consume the next token and miss `git` (fail-open for that exotic form). Only no-arg wrapper flags + assignments are unwrapped in WI-SCAFFOLD-004. | WI: shell-aware parsing (with BRCBW path-indirection) | YES | open | `env -i FOO=bar git commit` IS handled; `env -u X git commit` is not |
| BRCBW-1 | High | CLOSED — Mechanism B (bounded indirection resolver, `scan_indirection` in `block-run-control-bash-write.sh`): tracks a literal `cd dev-memo/run` directory + standalone `VAR=<literal>` assignments within one command line and resolves a write-verb operand / redirect target through them before the authority check. Closes `cd dev-memo/run && echo x > config` / `rm config` / `tee config`, subshell `(cd dev-memo/run; rm config)`, `cd … && (cd ..; rm config)` (deny-safe over-approx), and literal-var `p=dev-memo/run/config; echo x > "$p"`. Bounded: literal RHS only, last-wins, prior-assignments-only; the bare-reserved-basename fail-closed fallback fires ONLY under a tracked run-control cd (no global bare-basename deny — `echo x > config` / `grep config dev-memo/run/queue.md` stay ALLOW). | WI: BRCBW-IND (`security-brcbw-1-10-path-indirection`; plan `dev-memo/plan-brcbw-1-10-path-indirection.md`; spec `plan-brcbw-parser-depth-00.md` §3 Mechanism B) | YES | closed | resolved-in `<pending>`; review-plan `review-plan-mpzkrvt2-jl9yi1` (READY-WITH-LOW); tests `block-run-control-bash-write.test.sh` (+36 fixtures; 143→179). Residual (Mechanism-C non-goal): computed paths (`p=$(printf …)`, `${p:-…}`, `$IFS`, brace, `eval`, dynamic cd) — documented, deferred. needs-shell-awareness met via the bounded lexical resolver. |
| BRCBW-2 | High | CLOSED (literal-var subset) — the SAME `scan_indirection` literal-var resolver (BRCBW-IND, shared code path) now catches `echo x > "$p"` / `rm "$p"` / `echo x > "${p}"` when `p=<literal authority path>`. The COMPUTED subset (`p=$(printf …)`, `${p:-…}`, file/network-sourced) remains a documented Mechanism-C non-goal. | WI: BRCBW-IND (closed as a side effect of Mechanism B — same machinery as BRCBW-1) | YES | closed | resolved-in `<pending>`; tests `block-run-control-bash-write.test.sh` (var-redirect/operand DENY + computed-residual ALLOW). Dynamic/computed targets stay residual. |
| BRCBW-3 | High | Closed — the BRCBW-5 statement-aware rewrite (split on `;`/`&&`/`\|\|`/`\|`/`&`, `-a` detected per statement) already parses each `tee` invocation separately; per-invocation regression proof added (`tee -a /tmp/ok; tee dev-memo/run/log.md` denies, `tee dev-memo/run/log.md; tee -a /tmp/ok` denies, `tee -a /tmp/ok` alone allows, prose mentions of `tee`+protected paths do not false-deny) | WI: per-invocation tee/sed parsing | YES | closed (regression proof, no guard change; tests in `.claude/hooks/tests/block-run-control-bash-write.test.sh`) | parse each `tee` separately |
| BRCBW-4 | High | Closed — in-place detection in the `sed\|perl` branch now matches any single-dash cluster containing `i` (`-*i*`: `-i`, `-i.bak`, and the clustered perl forms `-pi`/`-ni`/`-wpi`/`-pi.bak`) plus `--in-place[=…]`, while explicitly ignoring other long opts; a strict SUPERSET of the prior `-i*` glob so no existing protection weakened. `sed -i.bak` was already covered by `-i*`; this WI closes the perl-cluster gap. | WI: per-invocation tee/sed parsing | YES | closed (commit on `workflow-brcbw4-perl-inplace`; tests in `.claude/hooks/tests/block-run-control-bash-write.test.sh`) | match `-iEXT`, `-i ''`, perl `-pi`/`-i.bak` |
| BRCBW-5 | Medium | FIXED — `block-run-control-bash-write.sh` pass 2/3 is now statement-aware + command-word gated: a write verb only counts as a statement's command word, and only an authority path in a WRITE-TARGET position denies (cp/install/rsync destination, `dd of=`, any operand for rm/mv/ln/touch/truncate/tee/sed-i). Read-source (`cp dev-memo/run/config /tmp/x`, `dd if=`) and prose mentions now pass; no real write weakened. | — | YES | closed | resolved-in `<pending>`; tests `block-run-control-bash-write.test.sh` (63 cases, +18). BRCBW-6 NOT bundled. |
| BRCBW-6 | Medium | Closed — when jq is unavailable the parse fallback cannot decode JSON escapes, so an escaped protected path (`dev-memo\/run\/config`, `\uXXXX`, `\\`-spliced, `\"`-spliced) slipped the post-parse `grep dev-memo/run/`. Fix: the jq-absent branch now FAILS CLOSED on ANY backslash in the raw payload (in well-formed JSON a backslash occurs only inside a string escape, so its presence = an escape we cannot safely resolve without jq). Closes the whole jq-absent obfuscation class, not a narrow allow-list. jq-PRESENT behavior byte-for-byte unchanged (fix is wholly in the else branch). cc-suite chain: audit `audit-mpxi9cp3-oz4ae8` (High: `\\`/`\"` defeated the initial `\/`+`\uXXXX` allow-list) → broadened to any-backslash → Path-1 verify TIMEOUT (`spawnSync codex ETIMEDOUT`) → Path-2 direct MCP verify (session `019e8ba6-835a-7553-a363-e8e3197b5116`, gpt-5.5) = "JQ-ABSENT CLASS CLOSED". Tests 126→143, 0 failed (controlled missing-jq harness: temp PATH without jq); main's 126-case suite passes against the new hook (additive). | WI: require-jq-or-deny (commit on `workflow-brcbw6-jq-failclosed`) | YES | closed | jq-absent class closed; bash-metacharacter path obfuscation split to BRCBW-10 |
| BRCBW-10 | Low | CLOSED (lexical shell-word obfuscation subset) — `block-run-control-bash-write.sh` now computes a de-obfuscated copy `DEOBF` of the command (strip an unquoted `\`-before-non-special; remove balanced `'`/`"` quote pairs of literal concatenation, preserving contents; ODD count ⇒ unchanged, fail-safe), widens the fast-exit gate to `dev-memo/run` (no trailing slash) over `CMD` OR `DEOBF`, and ADDITIVELY rescans `DEOBF` when it differs from `CMD`. Closes `echo X > dev-memo/r\un/config`, `dev-memo/"run"/config`, `dev-memo/ru''n/config`, `d\ev-memo/run/config`. The COMPUTED subset (`$IFS`, `$(...)`-spelled, brace expansion, variable-computed segments) stays a documented Mechanism-C non-goal — relabelled "lexical shell-word obfuscation closed," NOT "all computed path spelling." | WI: BRCBW-IND (`security-brcbw-1-10-path-indirection`; plan `dev-memo/plan-brcbw-1-10-path-indirection.md`) | YES | closed | resolved-in `<pending>`; review-plan `review-plan-mpzkrvt2-jl9yi1` (READY-WITH-LOW); tests `block-run-control-bash-write.test.sh` (BRCBW-10 backslash/quote DENY fixtures). Residual: computed/`$IFS`/`$()`-spelled = Mechanism-C non-goal. |
| BRCBW-7 | Low | Closed for the proven behaviors only: (a) the `==`/`!=`/`=`/`test` co-occurrence forms already allow via the BRCBW-5 command-word gating (`[[`/`[`/`test` are not write verbs), proven by regression; (b) the literal `[[ x > config ]]` case (where pass-1 read `>` as redirection) is fixed by blanking `[[ … ]]` spans before pass-1 redirection detection — bash never redirects from inside `[[ ]]`, and a redirect OUTSIDE/after `]]` stays outside the span and is still denied; (c) **non-nested** `$(...)` / backtick command substitutions containing a real write (redirection, `tee`, or a write verb) deny — closed by an ADDITIVE pass-4 that scans each non-nested substitution body with the same redirection + write-verb detection (no `[[ ]]` blank) — it can only ADD denials, so no write protection is weakened; substitutions that only READ a protected path still allow. NOT covered (see BRCBW-8): NESTED substitutions. Known fail-closed friction: an escaped/single-quoted literal `$(...)` (e.g. `echo "\$(rm config)"`, `echo '$(rm config)'`) that bash would NOT execute is still scanned textually and over-denies — harmless (denies a safe command), not a bypass. | WI: shell-token parsing | YES | closed (commit on `workflow-brcbw7-test-expr`; tests in `.claude/hooks/tests/block-run-control-bash-write.test.sh`) | ignore `[[ … ]]` contexts |
| BRCBW-8 | Low | Closed (WI-A) — nested/unparseable command-substitution inner writes now deny. pass-4 replaced with INNERMOST-substitution peeling (any depth) + a fail-closed pass-4b backstop: if an UNRESOLVED `$(`/backtick remains after peeling (nesting beyond MAXDEPTH, or a body with a literal paren/backtick the innermost regex cannot classify) AND it references a protected authority file, deny (pass-0 posture). cc-suite chain: audit `audit-mpxebdwd-4vuouz` (2 High under-denials: depth cap + literal-paren body) → fixes → verify `verify-mpxehbp8` (Finding 2 partial: space-only verb regex) → word-boundary fix → verify `verify-mpxelds9` (sed/perl in-place variants) → authority-ref fail-closed backstop → verify `verify-mpxerfok`. Tests `block-run-control-bash-write.test.sh` 103→126, 0 failed; main's 103-case suite passes against the new hook (additive). | WI-A (commit on `workflow-brcbw8-flatten-impl`; spec `dev-memo/plan-brcbw-parser-depth-00.md` §3 Mechanism A) | YES | closed | additive (deny-only); BRCBW-1/2 (var/cd indirection) still WI-B; interpreter-internal writes split to BRCBW-9 |
| BRCBW-9 | Low | Open — interpreter-internal writes to a protected path are not detected, e.g. `awk 'BEGIN{print "x" > "dev-memo/run/config"}'`, `ex -sc 'wq' dev-memo/run/config`, `ruby -e 'File.write "dev-memo/run/config","x"'`, `python -c "open('dev-memo/run/config','w')…"`. The write happens inside the interpreter's OWN language, not via a shell verb/redirection, so the lexical bash guard cannot see it. Surfaced by `verify-mpxerfok` during WI-A. NOT BRCBW-8 and NOT introduced by WI-A: verified ALLOW on `main` AND the new hook, WITH and WITHOUT command substitution (the `$(...)` wrapper is irrelevant) — pre-existing and substitution-independent. Explicitly OUTSIDE the guard's stated threat model ("cooperative agent + direct-write block, NOT cryptographic") and the spec's Mechanism A non-goals ("runtime/semantic, not lexical"). Closing it is an unbounded interpreter allow-list (awk/ex/ed/ruby/python/lua/…) and/or a threat-model change → requires explicit user authorization (security policy). | WI: interpreter-write policy decision (threat-model change; needs user authorization) | YES | open | ESCALATED to user during WI-A; over-deny-all-interpreters-touching-authority is the only lexical option and is high-friction; recommend accept-as-known-gap unless the threat model changes |
| PRC-1 | High | CLOSED — append-only enforcement for `dev-memo/run/log.md` (the real need; NOT the rejected blanket Write/Edit block). `protect-run-control.sh` routes any `*log.md*` Write/Edit/MultiEdit to a side-effect-free Node verifier (`.claude/hooks/logmd-append-guard.mjs`): a `Write` may only CREATE an absent trail; `Edit`/`MultiEdit` are SIMULATED and allowed iff the result keeps the current content as a strict prefix (append-only at EOF); fail-closed on node-absent / parse error / malformed-or-unknown tool shape / non-ENOENT read error. log.md identity uses LEXICAL `path.resolve` canonicalization (catches `./`, `//`, `../`, absolute spellings); non-`log.md` `*log.md*` files (e.g. cc-suite-reliability-log.md) fall through. The Bash side was already append-only (`block-run-control-bash-write.sh`, unchanged). | WI: PRC1-APPEND-ONLY (`security-prc1-logmd-append-only`; plan `dev-memo/plan-prc1-logmd-append-only.md`) | YES | closed | resolved-in `<pending>`; cc-suite review-plan `review-plan-mpz4u8vw-cc8mvl` (READY); tests `.claude/hooks/tests/logmd-append-guard.test.mjs` (22) + `protect-run-control.test.sh` (+7 e2e). RESIDUAL (was deferred to PRC-2): the symlink / non-`"log.md"`-named indirection is now **CLOSED by PRC-2** — `runcontrol-canon.mjs` realpaths every path and routes a symlink-reached log.md to the verifier, with helper-11 + verifier-3 ⇒ DENY (fail-closed). |
| PRC-2 | High | CLOSED — path canonicalization for `protect-run-control.sh`. A side-effect-free Node helper (`.claude/hooks/runcontrol-canon.mjs`) runs on EVERY parsed Write/Edit/MultiEdit path (no `*dev-memo/run*` pre-filter), resolves it (`path.resolve` for `./`/`//`/`../`/absolute, then best-effort `realpathSync` for symlinks, realpath BOTH the candidate and `dev-memo/run` so a symlinked ancestor like macOS `/tmp`→`/private/tmp` matches consistently), and classifies by EXACT normalized dir+basename: 10 blanket-protected→deny, 11 log.md→route to PRC-1 verifier, 0 not-protected→allow, 2 parse-error→fall through to the bash-glob fallback. Closes the lexical spelling bypasses AND the PRC-1-deferred symlink residual (a path that realpaths to a protected file is caught regardless of its own name). Critical compose fix: helper-11 + verifier-3 (a symlink-reached log.md the append-verifier can't simulate) ⇒ DENY. Node-absent degrades to the pre-PRC-2 bash suffix-globs (never worse, never fail-open). Behavior note: protection is now SCOPED to this workspace's `dev-memo/run/` — a foreign absolute path under a different root is correctly out of scope (the old suffix glob over-matched). | WI: PRC2-PATH-CANON (`security-prc2-path-canon`; plan `dev-memo/plan-prc2-path-canon.md`) | YES | closed | resolved-in `<pending>`; cc-suite review-plan `review-plan-mpzid6cq-3780u4` (READY-WITH-LOW); tests `runcontrol-canon.test.mjs` (11) + `protect-run-control.test.sh` (+18 e2e incl. every protected basename enumerated through the hook). Low residual: node-absent degrades to bash globs (Node 22+ is a project requirement). |
| PRC-3 | High | FIXED — `protect-run-control.sh` now fails CLOSED: an unparseable/missing target path that still references a protected `dev-memo/run/` file is denied (was fail-open `[ -z "$P" ] && exit 0`). A parse failure with no run-control reference still passes (not over-blocking). | — | YES | closed | resolved-in `<pending>`; tests `protect-run-control.test.sh` (14 cases). Note: BRCBW-6 (jq-absent fallback escapes) is a SEPARATE finding, not bundled here. |
| PRC-4 | Medium | CLOSED — `protect-run-control.sh` `deny()` now emits VALID JSON for C0 control bytes (0x00–0x1F: newline/tab/CR) + quotes + backslashes. jq-present path encodes the whole reason with `jq -Rs .` (fully-escaped JSON string literal incl quotes + `\uXXXX` + correct UTF-8), interpolated as a bare `%s`. jq-absent OR jq-errors path strips C0 controls to spaces (`tr '\000-\037' ' '`) then escapes `\`/`"` (still valid JSON; non-UTF-8 bytes in that doubly-anomalous path are a documented residual, outside the C0 scope). Both branches exit 0 (unchanged control flow). Reachability: NOT known reachable today (every reason is a fixed string or `$(basename "$P")`, and a control-char basename isn't classified protected) — closed as defense-in-depth so `deny()` cannot emit invalid JSON for any future reason. FOLLOW-UP (NOT fixed here, separate findings): `block-run-control-bash-write.sh` `emit_deny()` and `batch-commit-guard.sh` `deny()` share the same sed-only pattern — recorded for a later WI; PRC-4 scope was `protect-run-control.sh` `deny()` only. | WI: PRC4-DENY-JSON (`security-prc4-deny-json-escaping`; plan `dev-memo/plan-prc4-deny-json-escaping.md`) | YES | closed | resolved-in `<pending>`; cc-suite review-plan `review-plan-mpznrewh-0cwh19` (READY-WITH-LOW); tests `protect-run-control.test.sh` (+12: newline/tab/CR/quote+backslash/mixed/normal × jq-present + jq-fallback-via-stub). |
| PRC-4-FU | Medium | CLOSED — ported the PRC-4 `deny()` JSON hardening to both sibling emitters: `block-run-control-bash-write.sh` `emit_deny()` and `batch-commit-guard.sh` `deny()`. Each now emits VALID JSON for C0 control bytes (0x00–0x1F) + quotes + backslashes: jq-present encodes the whole reason via `jq -Rs .` (bare `%s`); jq-absent/errors/empty strips C0 to spaces then escapes `\`/`"` (non-UTF-8 in that path a documented residual). Names + call sites unchanged; control flow unchanged (both still `exit 0`); JSON shape unchanged. Same honest reachability posture as PRC-4 (not known reachable today; defense-in-depth). `protect-run-control.sh` was already fixed in PRC-4. | WI: PRC4FU-DENY-JSON (`security-prc4fu-sibling-deny-json`; plan `dev-memo/plan-prc4fu-sibling-deny-json.md`) | YES | closed | resolved-in `<pending>`; cc-suite review-plan `review-plan-mpzoupz6-3jbkia` (READY-WITH-LOW); tests: BRCBW suite 179→191 (+12 emit_deny), batch-commit-guard-base 14→26 (+12 deny) — newline/tab/CR/quote+backslash/mixed/normal × jq-present + jq-fallback-via-stub. |
| PRC-4-FU2 | Medium | CLOSED — ported the PRC-4 `deny()` JSON hardening to the last two PreToolUse deny emitters: `block-git-add-all.sh` `emit_deny()` and `block-commit-stage-all.sh` `emit_deny()` (the staging-hygiene commit hooks). Each now emits VALID JSON for C0 control bytes (0x00–0x1F) + quotes + backslashes: jq-present encodes via `jq -Rs .` (bare `%s`); jq-absent/errors/empty strips C0 to spaces then escapes `\`/`"` (non-UTF-8 in that path a documented residual). Names + call sites + control flow (`exit 0`) + JSON shape unchanged. Honest reachability posture as PRC-4 (not known reachable; defense-in-depth). **All FIVE PreToolUse deny emitters now share the hardened pattern** (review-plan confirmed no sixth). | WI: PRC4FU2-DENY-JSON (`security-prc4fu2-staging-deny-json`; plan `dev-memo/plan-prc4fu2-staging-deny-json.md`) | YES | closed | resolved-in `<pending>`; cc-suite review-plan `review-plan-mq05hi3k-3a026d` (READY); tests: block-git-add-all +13 (incl red-before), block-commit-stage-all +13 — newline/tab/CR/quote+backslash/mixed/normal × jq-present + jq-fallback-via-stub. |
| PRC-5 | Low | CLOSED — incidentally fixed by PRC-2's exact normalized-path matching: `runcontrol-canon.mjs` classifies by `dirname(resolved) === <repo>/dev-memo/run` AND exact basename, so `xdev-memo/run/config` (parent `<repo>/xdev-memo/run`) is a different file and is NOT denied. | WI: PRC2-PATH-CANON (with PRC-2) | YES | closed | resolved-in `<pending>`; tests `runcontrol-canon.test.mjs` (`xdev-memo/run/config => 0`) + `protect-run-control.test.sh` e2e (`xdev-memo/run/config ALLOW`). |

---

## Tier 1 FileVault enforcement impl (commit `<pending Tier 1 impl commit hash>`)

| Audit job | Verify job |
|---|---|
| `audit-mpkmebjv-65rgwo` | (not run — VERDICT was 0 C/H/M; verify pass only required when fixes apply) |

| Finding ID | Severity | Reason for deferral | Target | Safe? | Status | Notes |
|---|---|---|---|---|---|---|
| AT1-L1 | Low | CLOSED (prefix-parse fail-open only) — `parseFdesetupStatus` is now END-ANCHORED (`/^FileVault is On\.?$/i` / `…Off\.?$/i`, no `m` flag), so an unexpected trailing suffix or extra line (e.g. `FileVault is On: weird`, `FileVault is On.\n…`) classifies as `unknown` (→ `decideAction` block-prod / warn-dev, fail-closed) instead of being prefix-matched to `on`/`off` (the fail-OPEN). `raw.trim()` unchanged; every documented accepted form (`FileVault is On`/`On.`, leading/trailing whitespace, case-insensitive) still returns on/off — verified by the preserved tests. Preemptive fail-closed hardening (only tightens). Scope: closes the prefix-parse fail-open ONLY, NOT broader FileVault enforcement coverage (AT1-L2 remains open). | WI-401 (BATCH-PRODUCT-PROBE-00; `wi-401-fdesetup-anchor`; plan `dev-memo/plan-wi-401-fdesetup-anchor.md`) | YES | closed | resolved-in this WI's commit; cc-suite review-plan `review-plan-mq0k9xl5-br7ipy` (READY-WITH-LOW); tests `apps/lawbar-desktop/tests/main.test.mjs` +4 anchor assertions (red-before verified: suffix → `on` pre-fix; full suite 369 pass). |
| CBW-601-BARREL | Low | Open (follow-up) — WI-601 added the docket-entry create+confirm IPC write path (`apps/lawbar-desktop/src/caseBox/docketHandlers.ts`), but the `handlers.js` barrel (`src/caseBox/handlers.ts`) was OUTSIDE WI-601's governed Allowed-files, so the new handlers are imported DIRECTLY (in `caseBoxHandlers.ts` + the IPC test) rather than re-exported through the barrel like every other entity. Functional + tested; purely a convention inconsistency (the barrel does not re-export the docket handlers). **WI-602 EXTENDS this**: the fact-create write handler (`createFactHandler` in `src/caseBox/factHandlers.ts`) is ALSO imported directly (in `caseBoxHandlers.ts` + the IPC test) because `handlers.ts` was again outside WI-602's governed Allowed-files — same deliberate convention deferral, same fail-closed behavior, fully tested. | future small WI: add `export { createDocketEntryHandler, confirmDocketEntryHandler } from "./docketHandlers.js"` AND `export { createFactHandler } from "./factHandlers.js"` to `handlers.ts` (a single barrel-reconciliation WI once `handlers.ts` is in scope) | YES | open | deliberate Allowed-files-scoped choice in WI-601 and WI-602; no behavior impact; reconcile when `handlers.ts` is next in scope. |
| CBW-602-PURPOSE-ENUM | Low | Deferred (cleanup-only) — WI-602 audit `audit-mq0rxpx5-78c551` noted `FACT_PURPOSES` in `factHandlers.ts` manually re-lists the contract's fact `purpose` enum; current values match the schema, but it is a drift point if the contract enum changes. Mitigated: `persistence.appendFact` applies the schema's own enum validation on the way to disk, so a handler/schema divergence fails closed at the write (defense in depth), and a smuggled out-of-enum purpose is already rejected by the handler's own check. No behavioral/security impact. | future small WI: import the enum as an exported runtime constant from `case-box-contract`, OR add a schema-parity test asserting `FACT_PURPOSES` === the schema enum (folds with the barrel-reconciliation WI or a contract-constants WI) | YES | open | out-of-scope for WI-602 (no contract-package edit allowed); reconcile when contract constants are exported or a parity-test WI is opened. |
| CBW-602-FORBIDDEN-TESTS | Low | Deferred (test-completeness, cleanup-only) — WI-602 audit `audit-mq0rxpx5-78c551` noted the forbidden-field IPC test samples only `actor_user_id`. The boundary is already enforced by construction (the `CREATE_FACT_FORBIDDEN_FIELDS` loop + the unknown-field DTO-allowlist guard reject every non-DTO key, covered by the `unknown field -> invalid_payload` test); the single sampled field plus the unknown-field test already exercise both guard branches. A table-driven test over `status`/`source_type`/`matter_id`/`supersedes_fact_id`/etc. would pin each named field explicitly but adds no new code path. No behavioral/security impact. | WI-FF1 (BATCH-CASEBOX-FORBIDDEN-FIELD-TESTS-00) | YES | closed | CLOSED by WI-FF1 — the `factCreate` forbidden-field test (`ipc-casebox-handlers.unit.test.mjs`) is now table-driven over EVERY `CREATE_FACT_FORBIDDEN_FIELDS` member (each → ok:false + invalid_payload + details.schemaPath == the field + persistence-not-reached), replacing the single `actor_user_id` sample — exactly the recorded target. (Same WI also table-drove createDocket/confirmDocket forbidden tests + transitionFact, and added the 4 missing dto-sync response-allowlist authority-exclusion checks.) TEST-ONLY; no handler/source change. resolved-in this WI's commit. |
| AT1-L2 | Low | Cleanup-only / coverage gap — auditor flagged that `tests/main.test.mjs:218` covers the pure `decideAction` matrix but does NOT include an Electron-level smoke that asserts `BrowserWindow` does NOT open after a `block` verdict (e.g. an `electron.launch({env: {LAWBAR_MODE: "production"}})` against a FileVault-off Mac that times out without a window). Code ordering in `electron/main.ts:111` is correct; this is a pin-with-test gap, not a behavior gap. | future Tier 1 hardening WI OR Tier 2 SQLCipher WI (which will need an analogous block-on-missing-key smoke) | YES | open | Cannot be exercised in dev environments with FileVault OFF without an environment flag to FORCE block; would require either fdesetup mocking inside main.ts (additional surface) or a CI-gated test. Reasonable to defer. |
| AT1-L3 | Low | CLOSED — corrected the stale §6.1 test row in `dev-memo/plan-encryption-at-rest-00.md`: the `fdesetup` execution-error row no longer claims the app should ignore the error, and now asserts the SHIPPED impl behavior — subprocess error → state `unknown` → `decideAction` maps to **block-prod / warn-dev** (per §4.1; `unknown` → `off`/fail-closed semantics, confirmed in `apps/lawbar-desktop/src/security/fileVaultProbe.ts` lines 47/99/110). Doc-only; no impl change (impl was always correct; the §6.1 text was stale). | WI-303 (BATCH-BCG-HARDENING-00; docs-only) | YES | closed | resolved-in this WI's commit; grounded against current impl (`fileVaultProbe.ts`: execFile error/catch → `state:"unknown"`; `decideAction(unknown, production)`=`block`, dev=`warn`). Governed gate `grep -q "block-prod" && ! grep "proceed with audit log"` PASSES on the plan file. Low/docs-only → self-review recorded below. |
| LOC-RVM-1200 | Low | CLOSED by WI-704 (was: open, follow-up = a bounded TEST split WI) — `apps/lawbar-desktop/tests/renderer-view-matter.test.mjs` reached EXACTLY 1200 raw LOC during WI-701/WI-703 (loc-guardian hand-written-test fail = raw LOC >= 1200), a pre-existing violation by 0 lines over the boundary. WI-702 review-plan `review-plan-mq11is6s-atrgyr` flagged it (Medium) when WI-702 was about to add deadline tests there. **User-approved deferral (2026-06-05, Option B)**: WI-702's new deadline UI + DST-helper tests go into a NEW dedicated file `apps/lawbar-desktop/tests/renderer-deadline-write.test.mjs`; `renderer-view-matter.test.mjs` is moved to WI-702 Forbidden-files and left UNCHANGED at 1200, so WI-702 adds NO new violation. The pre-existing 1200-LOC condition is a KNOWN, UNCHANGED violation accepted for this batch step only. | WI-704 (TEST-only; `wi-704-loc-split`) | YES | closed | CLOSED by WI-704 — the cohesive WI-701/WI-703 "Add fact" write-test block (stubWithFact + mountFactsWithAdd + the 11 add-fact tests) was MOVED verbatim out of `renderer-view-matter.test.mjs` into a new sibling `apps/lawbar-desktop/tests/renderer-fact-write.test.mjs` (wired into the package.json "test" gate), dropping the host from 1200 → 973 raw LOC (well under the 1200 cap, ~227 margin) with the moved test-title set IDENTICAL before/after (no coverage lost). Test-only; no product/renderer/electron/src change. resolved-in this WI's commit; cc-suite review-plan `review-plan-mq12cs1g-m7dz9b` (queue READY-WITH-LOW) + the per-WI WI-704 review/audit/verify chain. |
| IPC-HANDLERS-1534 | Low (critical-tier LOC) | CLOSED by WI-805 (was: a PRE-EXISTING violation surfaced during the BATCH-CASEBOX-FACT-REVIEW-00 discovery) — `apps/lawbar-desktop/tests/ipc-handlers.unit.test.mjs` had grown to 1534 raw LOC, over the loc-guardian hand-written-test fail threshold (1200) AND past the 1500 "critical structural" line, through the accumulated matter/audit/document/deadline/fact/docket IPC tests. Flagged in the WI-802 proposal (`dev-memo/plan-batch-casebox-fact-review-00.md`); WI-802 used a NEW dedicated file for its tests rather than touching the monolith, deferring the split to here. | WI-805 (TEST-only; `wi-805-ipc-test-split`) | YES | closed | CLOSED by WI-805 — the cohesive case-box ENTITY IPC test group (listDeadlines + listFacts read + WI-601 docket create/confirm + WI-602 fact create, with makeDocketProvider/makeFactProvider/validCreateDto/ENTRY_ID/DEADLINE_ID) was MOVED verbatim out of `ipc-handlers.unit.test.mjs` into a new sibling `apps/lawbar-desktop/tests/ipc-casebox-handlers.unit.test.mjs` (a local harness copy + CHANNEL import; wired into the package.json test + test:unit + test:ipc-unit scripts), dropping the host from 1534 → 1028 raw LOC (under the 1200 cap, ~172 margin) with the moved test-title set IDENTICAL before/after (38 titles, no coverage lost) and the host's now-unused handler imports pruned. Test-only; no product/renderer/electron/src change. resolved-in this WI's commit; cc-suite review-plan `review-plan-mq1onptw-806y2i` (queue READY) + the per-WI WI-805 audit `audit-mq1owtmk-gy0had` (C0 H0 M0 L2; both Lows fixed in-WI) + verify chain. |
| CBW-UI-804-HEADER | Low | CLOSED by WI-806 (was: open, recorded in study packets #60/#61 — `dev-memo/study/2026-06-05-batch-audit-60.md` + `-61.md` — not yet a formal ledger row) — the WI-804 Layer-B closeout audit `audit-mq1o8o9x-8pzc6s` (BATCH-PASS C0 H0 M0 L1) found pure comment drift: the file-top doc comment in `apps/lawbar-desktop/renderer/screens/viewMatterFacts.ts` still read "NO edit / delete / **review** / evidence linking", which became stale when WI-804 (`fef623a`) added per-fact Review/Accept/Reject controls (consuming `casebox:fact:transition`) in that same file. **Bounded impact**: internal doc comment only — no behavioral, security, contract, selector, or user-visible effect; the functional review code was correct + tested in WI-804. Fix requires touching a renderer file → a follow-up WI. | WI-806 (Type UI, comment/header-only; `wi-806-fix-facts-header`): correct the stale header comment to document the WI-804 review controls; remove "review" from the "NO ..." exclusion. | YES | closed | CLOSED by WI-806 — `viewMatterFacts.ts` file-top comment now documents the ADD (`casebox:fact:create`, WI-701) **and** REVIEW/ACCEPT/REJECT (`casebox:fact:transition`, WI-804) controls and drops "review" from the "NO edit / delete / evidence linking" list. Comment/header-only: the WI diff touches only `//`-prefixed lines (no executable/import/selector/visible-text/style/test/package change); full suite 453 pass / 0 fail (unchanged). The renderer-path design-artifact gate was satisfied by the EXISTING WI-803 artifact `dev-memo/design/2026-06-05-casebox-fact-review.md` (no new artifact, gate unmodified). Resolution lands in this WI's single implementation commit; cc-suite review-plan `review-plan-mq1xycju-xvzos1` (queue PASS) + the per-WI WI-806 audit `audit-mq1y2s3f-x13vma` (C0 H0 M0 L2; both Lows fixed in-WI before commit) + verify chain. |
| DTO-LOC-797 | Low | CLOSED by WI-DTO1 (was: open, maintainability) — WI-D2 audit `audit-mq32ptpx-2pen80` (C0 H0 M0 L3) noted `apps/lawbar-desktop/src/caseBox/dto.ts` reached **797 raw LOC**, 3 under the loc-guardian hand-written-source fail cap (800), one DTO addition from breaching it. | WI-DTO1 (BATCH-CASEBOX-DTO-SPLIT-00; `wi-dto1-split`) | YES | closed | CLOSED by WI-DTO1 — `dto.ts` split (behavior-preserving) into per-entity modules under `apps/lawbar-desktop/src/caseBox/dto/` (`shared` 22, `matter` 124, `audit` 43, `document` 178, `deadline` 49, `docket` 237, `fact` 253 LOC — all well under 800) behind a thin re-export barrel; `dto.ts` dropped from **797 → 12 LOC**. All 97 exports preserved VERBATIM (moved, not rewritten); every importer keeps `import ... from "./dto.js"` unchanged (no churn); no entity module imports the barrel (no cycle). The one coupling change: `renderer-dto-sync.test.mjs`'s canonical reader now reads+merges the `dto/*.ts` modules (asserts ≥1 module found so an empty extraction cannot false-pass; renderer↔canonical set-equality + `*_RESPONSE_FIELDS` authority-exclusion checks preserved). Suite unchanged total, all green; `dto-contract.test.mjs` untouched + passing (barrel re-exports the same symbol set). resolved-in this WI's commit; cc-suite review-plan `review-plan-mq3v327m-ps3zc6` (queue PASS, compact retry) + the per-WI WI-DTO1 audit/verify chain. |
| CBW-UI-701-DEFAULT-PURPOSE | Low | CLOSED by WI-703 (was: open, follow-up = WI-703) — WI-701 Layer-B closeout audit `audit-mq0zsm14-ic09t5` (BATCH-PASS C0 H0 M0 L1) found a real default-value correctness bug the per-WI MOCK tests missed: `FACT_PURPOSES` in `apps/lawbar-desktop/renderer/screens/viewMatterFacts.ts` lists `"claim"` first and no `<option>` is marked `selected`, so in a REAL browser an unchanged Add-Fact `<select>` defaults to its first option and would forward `purpose: "claim"` instead of the design's intended `"other"`. The mock test missed it because `MockEl.value` starts `undefined` (the handler's `\|\| "other"` fallback fires in the mock but not in a real browser, where `value` resolves to the first option). **Bounded impact**: the server still creates a VALID candidate fact (`"claim"` is a legal purpose) — no security / tenant-isolation / data-integrity effect; only the default purpose label is wrong when the user does not pick one. Fix requires a new WI because WI-701 is merged + closed. | WI-703 (BATCH-CASEBOX-UI-00; `wi-703-fix-default-purpose`): mark the `"other"` `<option selected>` (browser default = other) + a browser-faithful regression test asserting the untouched purpose select defaults to `other`, not the first option. | YES | closed | CLOSED by WI-703 — `viewMatterFacts.ts` now renders the `"other"` `<option selected>` so a real browser's untouched select resolves to `"other"`; the handler's `\|\| "other"` guard remains as defense-in-depth. Regression test "purpose select browser default resolves to 'other', not the first option (WI-703)" encodes the browser rule (selected option, else first) and asserts `=== "other"` (FAILS pre-fix where the first option `"claim"` would win). resolved-in this WI's commit; cc-suite review-plan `review-plan-mq109o73-r7vh3s` (queue READY-WITH-LOW) + the per-WI WI-703 review/audit/verify chain. |

---

## Phase B11 — SQLite replay-safe Once variants + Phase B FINAL sweep (commit `<pending B11 impl commit hash>`)

**Phase B SQLite implementation declared COMPLETE.** Go-live readiness remains separately gated per `.claude/rules/autonomy.md` §"Hard-stop list".

| Audit job | Verify job |
|---|---|
| `audit-mpi0qhw5-y5xo98` (mini; Path 1 native --background) | not invoked (0 C/H/M; 3 Lows — 1 fixed in-WI, 2 deferred) |

| Finding ID | Severity | Reason for deferral | Target | Safe? | Status | Notes |
|---|---|---|---|---|---|---|
| D2#1 (B11) | Low | Cleanup-only — `SqliteCaseBoxPersistenceTestWrapper` class duplicated between `sqlite.conformance.test.mjs` and `sqlite-final.conformance.test.mjs` (~15 LOC). Reviewer accepted: "optional cleanup; not blocking". | Future shared test-wrapper extraction WI | YES | open | Reviewer: extract only if more SQLite conformance entrypoints appear. |
| D4#1 (B11) | Low | Cleanup-only — `SqliteCaseBoxPersistence.ts` at 610 LOC (was 609 post-B10; +1 net LOC after thin call-through + dead-helper deletion). Carried debt from B7-B10. | Re-evaluate at Phase C if it appears | YES | open | Reviewer: "carried debt rather than B11 regression". |

Lows fixed in-WI:
- L D3#1 (dead `notImplemented()` + `methodSubWiHint()` helpers + stale B1-era comment in `SqliteCaseBoxPersistence.ts`): FIXED — both helpers deleted; top-of-file comment refreshed to reflect "Phase B SQLite implementation complete".

---

## Phase B10 — SQLite read-side aggregations (commit `bb40855`)

| Audit job | Verify job |
|---|---|
| `audit-mphygc7h-vsklaa` (mini; Path 1 native --background) | not invoked (0 C/H/M; 4 Lows — 1 fixed in-WI, 3 deferred) |

| Finding ID | Severity | Reason for deferral | Target | Safe? | Status | Notes |
|---|---|---|---|---|---|---|
| D2#1 (B10) | Low | Cleanup-only — local `loadMatterChecked` helper continues the matter+tenant duplication pattern across sibling RepoQueries files (B7 L D2#1 / B9 D2#1 progression). | Future shared SQLite matter-guard extraction WI | YES | open | Reviewer accepted: "do not refactor this lane to reduce duplication". |
| D2#2 (B10) | Low | Cleanup-only — `getMatterSummarySqlite` uses 8 inline bucket queries; repetitive but clear. UNION ALL deferred until profiling shows latency impact. | Future perf WI if measured | YES | open | v1 lawyer-scale acceptable. |
| D4#1 (B10) | Low | Cleanup-only — `SqliteCaseBoxPersistence.ts` at 609 LOC (was 602 post-B9; +7 LOC for 5 thin call-throughs). Same posture as B7-B9 deferred Lows. | Re-evaluate at B11 if class grows materially | YES | open | `#runImmediateWrite` extraction pattern not applicable here (B10 is read-only). |

---

## Phase B9 — SQLite OCR links (read-only mirror by value) (commit `6289d89`)

| Audit job | Verify job |
|---|---|
| `audit-mphx2ods-qf6qsv` (mini; Path 1 native --background) | not invoked (0 C/H/M; 5 Lows — 2 fixed in-WI, 3 deferred) |

| Finding ID | Severity | Reason for deferral | Target | Safe? | Status | Notes |
|---|---|---|---|---|---|---|
| D2#1 (B9) | Low | Cleanup-only — `loadMatter` / `loadDocumentEntry` helpers duplicate per-file patterns across `ocrLinkRepoQueries.ts` + sibling RepoQueries files. Reviewer accepted: "consistent with sibling pattern; defer until naturally touched". | Future shared SQLite read-helper extraction WI | YES | open | Reviewer recommendation: do not block B9. |
| D4#1 (B9) | Low | Cleanup-only — `SqliteCaseBoxPersistence.ts` at 602 LOC (was 596 post-B8; +6 LOC for 3 thin call-throughs). Same posture as B7 D4#2 / B8 D4#1. | Re-evaluate at B10/B11 if class grows materially | YES | open | `#runImmediateWrite` pattern keeps growth flat per added method. |
| D4#2 (B9) | Low | Cleanup-only — `schema.ts` at 547 LOC (was 500 post-B8; over warn by ~47 LOC after DDL_V8). | Next schema-bearing WI (B10/B11) should extract DDL by version. | YES | open | Reviewer recommendation: defer DDL extraction; not B9 blocker. |

---

## Phase B8 — SQLite evidence items (commit `bfda127`)

| Audit job | Verify job |
|---|---|
| `audit-mphtd7pt-ajcyux` (mini; Path 1 native --background) | not invoked (0 C/H, 1 M + 1 L; M fixed in-WI, 1 L deferred) |

| Finding ID | Severity | Reason for deferral | Target | Safe? | Status | Notes |
|---|---|---|---|---|---|---|
| D4#1 (B8) | Low | Cleanup-only — `SqliteCaseBoxPersistence.ts` at 596 LOC (was 588 post-B7; +8 LOC for 4 thin call-throughs). Same posture as B7 D4#2. | Re-evaluate at B9/B10 if class grows materially | YES | open | `#runImmediateWrite` pattern keeps growth flat per added method. |
| D4#2 (B8) | Low | Cleanup-only — `schema.ts` at 500 LOC (exactly at warn boundary; +42 from B7 v7 DDL). Future B9-B11 schema additions will push it over. | Re-evaluate at next schema-bearing WI (B9 or B11) | YES | open | Acceptable at boundary; flag for next schema-bearing WI to consider DDL split (e.g., one constant per phase). |
| D2#1 (B8) | Low | Cleanup-only — `package.json` `--test-name-pattern` regex duplicates `sqlite.conformance.test.mjs` B8_PATTERN. Drifted before during B6→B7 renaming. | Future shared-pattern-source refactor (B11 cleanup or dedicated WI) | YES | open | Preflight inventory enforcement reduces but does not eliminate the drift risk. |

---

## Phase B7 — SQLite docket entries + deadline materialization (commit `69db974`)

| Audit job | Verify job |
|---|---|
| `audit-mphm1ece-aki58h` (mini; Path 1 native --background) | not invoked (0 C/H, 1 M + 4 L; M + 3 Lows fixed in-WI, 1 Low deferred at the time; D4#1 now CLOSED by B8) |

| Finding ID | Severity | Reason for deferral | Target | Safe? | Status | Notes |
|---|---|---|---|---|---|---|
| D4#1 (B7) | Low | `sqlite.hardening.test.mjs` at 882 LOC over 700 warn. Per B7 plan §6 risk #6: B8 split if crossed. | WI-B8-pre-impl split into per-entity hardening test files | YES | closed | Resolved in B8 (commit `<pending B8 impl commit hash>`): 882-LOC monolith split into 1 shared module + 8 per-phase files (hardening-{pragma, schema, audit, classification, privilege, facts, docket, evidence}.test.mjs). 40 pre-split test names preserved verbatim post-split (verified via grep inventory diff). |
| D4#2 (B7) | Low | `SqliteCaseBoxPersistence.ts` at 588 LOC over warn (was 572 post-B6). Same posture as B5/B6 D4#1; reviewer accepted under 800 fail. | Re-evaluate at B8/B9 if class grows materially | YES | open | B8 adds +8 LOC (596 LOC); see B8 D4#1 entry above. Same band. |

---

## Phase B6 — SQLite facts (commit `667bb9c`)

| Audit job | Verify job |
|---|---|
| `audit-mph4cun6-aav6q2` (mini; Path 1 native --background) | not invoked (no C/H/M to verify; 0 C/H/M, 4 Lows; 2 Lows fixed in-WI, 2 Lows deferred at the time; D4#1 now CLOSED by B7) |

| Finding ID | Severity | Reason for deferral | Target | Safe? | Status | Notes |
|---|---|---|---|---|---|---|
| D4#1 (B6) | Low | `impl-parity.test.mjs` at 773 LOC over 700 warn. Per B6 plan §"Review packet" Q7 + Risk #6: B7 MANDATORY split when crossed. | WI-B7-pre-impl split into `impl-parity-{matter,document,audit,classification,privilege,facts,docket,stub-frontier}.test.mjs` | YES | closed | Resolved in B7 (commit `<pending B7 impl commit hash>`): 773-LOC monolith split into 7 per-entity test files + 1 shared common module; each well under warn threshold. |
| D4#2 (B6) | Low | `SqliteCaseBoxPersistence.ts` over warn (572 LOC; B6 added +9 LOC for 4 thin call-throughs). Same posture as B5 D4#1. | Re-evaluate at B7/B8 if class grows materially | YES | open | B7 adds +16 LOC (588 LOC); see B7 D4#2 entry above. Same band. |

---

## Phase B5 — SQLite privilege markers (commit `<pending B5 impl commit hash>`)

| Audit job | Verify job |
|---|---|
| `audit-mpgzx0ka-r4jne5` | not invoked (no C/H/M to verify; 0 C/H/M, 5 Lows; 1 Low fixed in-WI, 4 Lows deferred) |

| Finding ID | Severity | Reason for deferral | Target | Safe? | Status | Notes |
|---|---|---|---|---|---|---|
| D1#2 | Low | Cleanup-only — `getPrivilegeStatusSqlite` loads ALL matter markers then lets `effectivePrivilegeStatus` filter; could narrow at SQL level via `WHERE matter_id, target_type, target_id`. Acceptable at v1 lawyer-scale. | Future perf WI when marker volume grows | YES | open | Reviewer accepted as Phase-B debt. |
| D2#1 | Low | Cleanup-only — `buildShadowAppendState` and `buildShadowTransitionState` in `privilegeRepoQueries.ts` duplicate the global id+markerIndex SELECT loop. Two callers today; extract if a third privilege helper appears. | Future privilege-side extraction OR B-future-WI | YES | open | Reviewer accepted: "extract `loadPrivilegeIdIndex(db, state)` if a third privilege helper appears." |
| D4#1 | Low | Cleanup-only — `SqliteCaseBoxPersistence.ts` at 563 LOC over the 500-LOC LOC-01 extraction trigger (under 800 fail). B4 D4#1 was naturally fixed structurally via `#runImmediateWrite` + `#writeAudit` + `validateDocumentTarget` extractions; residual LOC is mostly imports + 33 `not_implemented` stubs. Reviewer: "Do not introduce a stub mixin yet; it would add indirection without reducing real complexity." | Re-evaluate at B6/B7 if growth continues | YES | open | Treated as accepted-as-known-divergence under 800 fail floor. |
| D4#2 | Low | Cleanup-only — global privilege id scans (and the analogous global classification id scans from B4) will not scale as a long-term pattern. v1 acceptable. | Future targeted-existence-lookup WI once multiple SQLite entity helpers repeat this pattern | YES | open | Reviewer: "Backlog a targeted existence/indexed lookup refactor once multiple SQLite entity helpers repeat this pattern." |

---

## Phase B4 — SQLite confidentiality classification (commit `bb285ca`)

| Audit job | Verify job |
|---|---|
| `audit-mpgyzp9t-iftt4p` | not invoked (no C/H/M to verify; 0 C/H/M, 4 Lows; 2 Lows fixed in-WI, 2 Lows deferred at the time; both now CLOSED by B5) |

| Finding ID | Severity | Reason for deferral | Target | Safe? | Status | Notes |
|---|---|---|---|---|---|---|
| D4#1 | Low | LOC trigger 524 > 500 in SqliteCaseBoxPersistence.ts; transaction-wrapper extraction. | WI-B5-impl OR dedicated extraction WI before B5 | YES | closed | Resolved in B5 (commit `<pending B5 impl commit hash>`) — extracted `#runImmediateWrite` + `#writeAudit` private helpers per NIGHT-RUN-SQLITE-B5-IMPL lane "fix cleanly when naturally touched" instruction. Class still 563 LOC (mostly stubs + imports); structural duplication eliminated. |
| D2#1 | Low | `getEffectiveClassificationSqlite` re-implements in-memory `getEffectiveClassificationHelper` validation (matter / tenant / target_type / document resolution). | WI-B5-impl OR future docs-only extraction WI | YES | closed | Resolved in B5 (commit `<pending B5 impl commit hash>`) — extracted shared `validateDocumentTarget(db, query)` helper in `documentRepoQueries.ts`; both B4 `getEffectiveClassificationSqlite` and B5 `getPrivilegeStatusSqlite` now call it. |

---

## Phase A6 — evidence items (commit `<pending A6 commit hash>`)

| Audit job | Verify job |
|---|---|
| `audit-mpfm9yx3-u37dx3` | `verify-mpfmi1u1-m2rzhh` |

| Finding ID | Severity | Reason for deferral | Target | Safe? | Status | Notes |
|---|---|---|---|---|---|---|
| F5.1 | Low | Cleanup-only — defensive cross-tenant supersession branch is unreachable through the public API (matter-tenant binding enforces; same-matter cross-tenant evidence would require direct state tampering). Plan's §6.A6.16b test was never added; helper's defensive code retained for forward compatibility. Audit accepted the deferral. | cleanup-accepted (no follow-up planned; revisit only if a future WI introduces a path that allows same-matter cross-tenant rows) | YES | open | Defense-in-depth witness. No test fixture available without a tamper seam. |

---

## Phase A5 — docket entries + deadline materialization (commit `<pending A5 commit hash>`)

| Audit job | Verify job |
|---|---|
| `audit-mpfktk5e-pac8by` | `verify-mpfl8uz3-ajvcvj` |

| Finding ID | Severity | Reason for deferral | Target | Safe? | Status | Notes |
|---|---|---|---|---|---|---|
| _no deferred findings_ | — | All findings (1 Low stale-imports) fixed inline rather than deferred. | n/a | YES | n/a | Verify verdict ALL CLOSED. |

Backlog relabel (NOT a closure): A2 F4.3's `Target` field updated from "future A5-fact-targets WI" to "future fact-target broadening WI" — A5 is dockets/deadlines, not fact-targets; the original label was a misnomer.

---

## Phase A4 — facts (commit `<pending A4 commit hash>`)

| Audit job | Verify job |
|---|---|
| `audit-mpfi97ns-kf42ma` | `verify-mpfijdi4-6cdzoi` |

| Finding ID | Severity | Reason for deferral | Target | Safe? | Status | Notes |
|---|---|---|---|---|---|---|
| _no deferred findings_ | — | All Medium/High closed; no Lows deferred (one Low — `PrepareTransitionResult.prior` dead surface — was fixed inline rather than deferred). | n/a | YES | n/a | Verify verdict ALL CLOSED. The audit re-verified the round-1 reconciliation; no residual issues. |

---

## Phase A3 — privilege markers (commit `b2ef9f1`)

| Audit job | Verify job |
|---|---|
| `audit-mpfgref5-1drdsi` | `verify-mpfgxs2h-xxgvzi` |

| Finding ID | Severity | Reason for deferral | Target | Safe? | Status | Notes |
|---|---|---|---|---|---|---|
| F2.1 | Low | Cleanup-only — local interfaces in `inMemoryPrivilege` duplicate the public types in `types.ts`. No behavior or invariant impact. Audit accepted deferral. | Phase A4 (broader sibling-module refactor when fact entities land) | YES | closed | Closed in Phase A4 (commit `<pending A4>`). `ListPrivilegeMarkersQuery` and `ListPrivilegeMarkersPage` were removed from `inMemoryPrivilege.ts` and imported from `types.ts` instead; same refactor applied to `inMemoryClassification.ts` for the parallel `ListClassifications*` types. |
| F2.2 | Low | Out-of-scope — tenant/matter/document consistency path overlaps with `inMemoryClassification`. Resurfaces in A4 when facts add a third sibling. Audit accepted deferral. | Phase A4 (extract `resolveDocumentTarget` helper) | YES | closed | Closed in Phase A4 (commit `<pending A4>`). New shared helper `src/resolveTarget.ts` consumed by `inMemoryClassification`, `inMemoryPrivilege`, and `inMemoryFact`. Preserves the exact error-code semantics (unknown_document / tenant_mismatch / matter_id_mismatch). |
| F4.2 | Low | Cleanup-only — defensive markerIndex behavior is acceptable (no delete API exists; an index miss throws before mutation). Audit verdict: "no code change required". | cleanup-accepted (no follow-up planned) | YES | open | Defense-in-depth defensive path. Resurfaces only if A3+ introduces a marker-delete API; reopen this row at that time. |
| F5.2 | Low | Audit verdict was CLEAN — no tenant/matter leak found in `getPrivilegeStatus`. Recorded as "deferred" only in the sense that it stayed an inspected-and-accepted item; no fix required. | cleanup-accepted (no follow-up planned) | YES | open | Persistent-cleanliness witness. Keep an eye on it during A4 when `target_type === "fact"` opens up. |
| F5.3 | Low | Audit verdict was CLEAN — no disclosure-safety inference from `hasProtectiveAssertion` in any persistence path. Recorded as "deferred" only in the same sense as F5.2. | cleanup-accepted (no follow-up planned) | YES | open | Anti-disclosure-clearance invariant witness. Defended by invariants test §6.2.A3.2; that test must not be deleted or weakened without an ADR. |

---

## Phase A2 — confidentiality classification (commit `f7f4bf5`)

| Audit job | Verify job |
|---|---|
| `audit-mpfcyd9a-l60m5r` | `verify-mpfd4e2y-6hzr6w` |

| Finding ID | Severity | Reason for deferral | Target | Safe? | Status | Notes |
|---|---|---|---|---|---|---|
| F2.2 | Low | Out-of-scope — tenant/matter/document consistency check pattern repeated across append and get-effective paths. Small now; would grow with facts. Audit accepted deferral. | Phase A4 (extract `resolveDocumentTarget` helper) | YES | closed | Closed in Phase A4 (commit `<pending A4>`) alongside A3 F2.2 — shared `resolveDocumentTarget` helper now consumed by `inMemoryClassification`. |
| F3.1 | Low | Cleanup-only — `unknown_document` code retained for forward compatibility per the documented 10-code set; not emitted by A2 methods. | cleanup-accepted (no follow-up planned) | YES | open | A3 introduced paths that DO emit `unknown_document` (appendPrivilegeMarker, getPrivilegeStatus). Mark for **review on A4 close**: if at least one A3 conformance case exercises every documented code, this row can flip to `closed`. |
| F3.2 | Low | Cleanup-only — invariants test §6.2.6 SAMPLES error codes rather than exhaustively triggering each. Acceptable per audit. | cleanup-accepted (no follow-up planned) | YES | open | Same paired observation as F3.1 — if A3+ tests reach full code coverage, the §6.2.6 sampled comment can be rephrased to "comprehensive" without code changes. |
| F4.2 | Low | Cleanup-only — whitespace-only reasons accepted by `appendConfidentialityClassification` for codes other than `"other"`. Audit accepted. | cleanup-accepted (no follow-up planned) | YES | open | Persistence DOES reject whitespace for `change_reason_code === "other"` (added during A2 audit-fix). Other reason codes are enum-bound so the whitespace concern is bounded. |
| F4.3 | Low | CLOSED (stale — pinning already present) — the row claimed the intended distinction (`listConfidentialityClassifications` empty for a never-classified document while `getEffectiveClassification` returns `unclassified`) was unpinned, but the conformance suite already pins BOTH halves against a never-classified document seeded by the existing `seedMatterDoc` (registers a matter + document, no classification): **`6.A2.13`** asserts `getEffectiveClassification` → `effectiveLevel === "unclassified"` and `history.length === 0`; **`6.A2.16`** asserts `listConfidentialityClassifications` → `rows === []` and `next_cursor === null`. Per the AGENTS.md source hierarchy (actual files beat the doc), the ledger was stale, not the coverage. No new test added (a duplicate atomic case was deliberately NOT created); no production/harness change. | WI-501 (BATCH-PRODUCT-PROBE-00; docs-only stale-ledger correction) | YES | closed | resolved-in this WI's commit; verified `6.A2.13` + `6.A2.16` pass across all three impls (InMemory, Sqlite-B11, Sqlite-Final) via the conformance harness. NOTE: the originally-proposed WI-501 (add an atomic combined distinction case) was withdrawn after discovery — the distinction is already covered by these two existing cases. |

---

## Phase A1 — case-box persistence in-memory base (commit `5de5530`)

| Audit job | Verify job |
|---|---|
| `audit-mpf8prhx-qenxkd` | `verify-mpf974sx-sn3hum` |

| Finding ID | Severity | Reason for deferral | Target | Safe? | Status | Notes |
|---|---|---|---|---|---|---|
| F2.1 | Low | Cleanup-only — audit-event construction sequencing duplicated across matter create / document register / matter transition paths. Small; refactor would not change behavior. | future bounded refactor WI when a 4th audit-emitting path lands | YES | open | A2 + A3 added more audit-emitting paths (classification, privilege). The duplication factor is now ~5; consider opening a refactor WI once A4 (facts) lands. |
| F3.1 | Low | Cleanup-only — `unknown_document` retained for forward compatibility (see A2 F3.1). | (paired with A2 F3.1) | YES | superseded by A3 | A3's `appendPrivilegeMarker` and `getPrivilegeStatus` exercise this code. Closing under that justification — see `b2ef9f1` conformance §6.A3.10, §6.A3.24. |
| F3.2 | Low | Cleanup-only — test exhaustiveness for §6.2.6 (see A2 F3.2). | (paired with A2 F3.2) | YES | open | Cross-WI test coverage may have grown enough; revisit comment after A4. |
| F4.2 | Low | Cleanup-only — whitespace-only `reason` accepted by `archiveMatter` / `unarchiveMatter`. Audit accepted. | cleanup-accepted (no follow-up planned) | YES | open | Same pattern as A2 F4.2; matter-transition reasons are free-text by design. |

---

## Legend for future entries

When closing an entry, leave the row in place and update `Status` + append to `Notes` like:

```
| ... | closed | Resolved in WI-XX (commit abc1234) — extracted `resolveDocumentTarget`; conformance case 6.A4.N covers. |
```

When a finding becomes irrelevant (e.g. the affected code was removed by another WI), mark `superseded` and cite the commit that removed the underlying surface.

When several rows close together (e.g. A2 F2.2 + A3 F2.1 + A3 F2.2 all close under one A4 refactor), keep them as separate rows so the grep for any one of them still surfaces the close.

## Case-box list channels — persistence tenant-filter gap (facts WI, audit `audit-mpxoq4ma-dn3m0h`)

Surfaced during the read-only facts WI (UI+IPC). The findings are NOT in that WI's scope
(`services/**` SQL) and are pre-existing; the WI's IPC/UI layer audited clean. Recorded here
for a dedicated persistence-hardening WI.

| Finding ID | Severity | Reason for deferral / disposition | Target | Safe? | Status | Notes |
|---|---|---|---|---|---|---|
| FACTS-AUD-1 | Medium | ESCALATED (out of UI-WI scope; pre-existing; v1-single-tenant-moot) — `listFactsSqlite` (`services/case-box-persistence/src/sqlite/factsRepoQueries.ts:124`) and `listDeadlines` (`deadlineRepoQueries.ts:86`) filter `WHERE matter_id = ?` only, NOT `tenant_id`. The IPC handlers verify the MATTER's tenant + inject `tenant_id` into the query, but the SQL ignores it, so a fact/deadline row whose own `tenant_id` differs from its matter's (a data-integrity violation, impossible in single-tenant v1) would be returned. `listDocuments` already tenant-filters (`documentRepoQueries.ts:117`) — the fix is to make facts + deadlines match. Touching `services/**` SQL is a security-boundary/persistence change requiring the full review-plan→tests→audit→verify loop. | WI: persistence list tenant-filter hardening (facts + deadlines list SQL; align with documents) | YES (v1 single-tenant) | closed | NOT introduced by the facts WI; affects the already-merged deadline list channel (PR #32) too. FIXED in `SECURITY(persistence): tenant-filter facts and deadlines list queries` — `listFactsSqlite` + `listDeadlinesSqlite` now lead `WHERE tenant_id = ? AND matter_id = ?`; cursor seek tuple + pagination unchanged. Resolved-in `b42954d` (PR #34, merged to main); audit `audit-mpxp6lsc-3111sr` → verify `verify-mpxpbjg9-ayd50v` CLOSED; regression test `services/case-box-persistence/tests/tenant-filter-lists.test.mjs` (cross-tenant exclusion + pagination, both facts and deadlines). |
| FACTS-AUD-2 | Low | Accepted-as-intentional (consistent + prior-CLEAN) — `listFactsHandler` returns `unknown_matter` for a missing matter vs `tenant_mismatch` for a wrong-tenant matter, a cross-tenant matter-existence oracle. This is the SAME pattern used by every handler (matter/audit/document/deadline), audited CLEAN in each prior WI, and the distinct boundary error codes are a deliberate stable contract per `.claude/rules/security-boundary.md` (do not collapse codes). Single-tenant-moot in v1. | cleanup-accepted (no follow-up planned; revisit if multi-tenant ships) | YES | open | revisiting requires an ADR if the error-code contract is to change |
| FACTS-AUD-3 | Low | Accepted-as-intentional (consistent + defense-in-depth present) — `listFactsHandler` (`apps/lawbar-desktop/src/caseBox/factHandlers.ts:75`) returns the raw `ListFactsPage`, so `CaseBoxFact`'s server-authority fields (`tenant_id`, `actor_user_id`, `reviewer_actor_user_id`) cross the IPC boundary even though the renderer displays a subset. This is the SAME `return { ok: true, value: page }` pattern as `listDocumentsHandler` + `listDeadlinesHandler` (both audited CLEAN), and `renderer/api.ts` already strips to `RENDERER_LIST_FACTS_DTO_FIELDS` (§3 server-authority defense-in-depth). In single-user/single-tenant v1 the fields are the user's own — no second party to leak to. Surfaced by audit `audit-mpxpsc81-vsayi3`. | WI: handler-side DTO projection for all list channels (cross-cutting; project page→renderer-safe DTO in main process; do facts/documents/deadlines together, not facts alone) | YES (v1 single-tenant/single-user) | closed | FIXED in `PRODUCT(ipc): project list responses to renderer-safe DTOs in the main process`. Correction to the original reason: `renderer/api.ts` strips only the outgoing REQUEST dto, NOT the response — so the authority fields genuinely crossed the IPC boundary unmitigated (the "renderer-side strip in place" premise was wrong). Fix: `projectRow`/`projectPage` in `handlerShared.ts` + per-entity `LIST_*_RESPONSE_FIELDS` allowlists in `dto.ts`; all three list handlers (facts/documents/deadlines) project every row before returning, stripping `tenant_id`/`actor_user_id` (+`reviewer_actor_user_id` facts, +`custody_chain` documents). Renderer row types are `Pick<Entity, (typeof LIST_*_RESPONSE_FIELDS)[number]>` (closed types tied to the runtime allowlist). Tests: `tests/ipc-list-projection.unit.test.mjs` (raw-includes-authority vs IPC-response-omits, ≥2 rows/channel) + extended `renderer-dto-sync.test.mjs`. Resolved-in `<pending>` (this branch); audit `audit-mpxqrjkn-mjjzg3` → verify `verify-mpxqz4ak-dm456q`. |
| GET-AUD-1 | Low | Accepted-deferred (out of the list-channel WI scope) — `getDocumentHandler` (`apps/lawbar-desktop/src/caseBox/documentHandlers.ts:179`) still returns the full `CaseBoxDocument` entity over IPC, so the same server-authority fields (`tenant_id`/`actor_user_id`/`custody_chain`) cross the boundary on the GET channel. The FACTS-AUD-3 WI projected only the three LIST channels (its declared scope). Surfaced by audit `audit-mpxqrjkn-mjjzg3` as an explicit "future separate item, not this WI defect". | WI: extend response projection to the get-* channels (`getDocument`, and any future `getFact`/`getDeadline`) reusing `projectRow` + a GET allowlist | YES (v1 single-tenant/single-user; same low practical risk as FACTS-AUD-3 was) | closed | FIXED in `PRODUCT(ipc): project get-document response to a renderer-safe DTO`. Added dedicated `GET_DOCUMENT_RESPONSE_FIELDS` allowlist + `RendererDocumentDetail = Pick<CaseBoxDocument, (typeof GET_DOCUMENT_RESPONSE_FIELDS)[number]>`; `getDocumentHandler` now projects the single returned document via the reused `projectRow` (`tenant_id`/`actor_user_id`/`custody_chain` stripped); null / cross-matter / cross-tenant paths unchanged (no projection on reject paths). `GetDocumentResult` retyped to `IpcEnvelope<RendererDocumentDetail | null>`. Tests `tests/ipc-get-document-projection.unit.test.mjs` (raw-includes-authority vs response-omits + null/other-matter/other-tenant/absent paths) + `renderer-dto-sync.test.mjs` extended. Resolved-in `<pending>` (this branch); audit `audit-mpxrhqzq-jn9zzl` CLEAN (no findings, no verify needed). Only `getDocument` exists today; `getFact`/`getDeadline` get-channels are not implemented, so nothing further to project. |

## Deadline-transition batch — Layer-B audit (BATCH-CASEBOX-DEADLINE-TRANSITION-00, post WI-DT1 `bcfb8bd`)

| Finding ID | Severity | Reason for deferral / disposition | Target | Safe? | Status | Notes |
|---|---|---|---|---|---|---|
| DT-BATCH72-L1 | Low | Cleanup/coverage-only (no behavioral/correctness/security impact) — the `transitionDeadline` forbidden-field IPC test exercises `actor_user_id` + `status` but not a table over EVERY `TRANSITION_DEADLINE_FORBIDDEN_FIELDS` member (e.g. `at`, `id`, `tenant_id`, `matter_id`, `met_at`, `previous_status`). The handler iterates the full `TRANSITION_DEADLINE_FORBIDDEN_FIELDS` array with a uniform guard, so coverage of 2 representative members + the loop is sufficient for correctness; the gap is test completeness only. WI-DT1 is already merged, so the fix is a follow-up (the governed queue holds only DT1/DT2/DT3 — inventing a DT1b is out of bounds). | WI-CL1 (BATCH-CASEBOX-DEADLINE-CLEANUP-00) | YES | closed | CLOSED by WI-CL1 — the two single-field tests (`actor_user_id`, `status`) were replaced with one table-driven test ("transitionDeadline rejects every forbidden server-authority field (table-driven)") iterating all 8 `TRANSITION_DEADLINE_FORBIDDEN_FIELDS` (id, tenant_id, actor_user_id, matter_id, at, status, met_at, previous_status), each asserting `ok:false` + `code:invalid_payload` + `details.schemaPath` == the field. TEST-ONLY; no handler/source change. Originating audit `audit-mq3z23e7-gcb4qv`; study packet `dev-memo/study/2026-06-07-batch-audit-72.md`. resolved-in this WI's commit (PR for BATCH-CASEBOX-DEADLINE-CLEANUP-00). |

## Deadline-transition batch — Layer-B audit (BATCH-CASEBOX-DEADLINE-TRANSITION-00, post WI-DT3 `eed5166`)

| Finding ID | Severity | Reason for deferral / disposition | Target | Safe? | Status | Notes |
|---|---|---|---|---|---|---|
| DT-BATCH74-L1 | Low | Cleanup/comment-only (no behavioral/correctness/security impact) — the `apps/lawbar-desktop/renderer/screens/viewMatterDeadlines.ts` header comment still reads "NO edit / deadline-status transition", which became stale when WI-DT3 added the transition UI. The governed DT1/DT2/DT3 queue is complete, so a comment fix is a new unplanned change (inventing a DT3b is out of bounds). | WI-CL2 (BATCH-CASEBOX-DEADLINE-CLEANUP-00) | YES | closed | CLOSED by WI-CL2 — the header comment of `viewMatterDeadlines.ts` now accurately states that since WI-DT3 each non-terminal deadline carries status-transition controls (pending → met/missed/withdrawn; missed → met with a required reason; met/withdrawn terminal) and that only deadline-FIELD edits remain unsupported; the server-authority note now lists the confirm/dismiss/transition preflights. COMMENT-ONLY (git diff: only `//` lines), no behavior change. Originating audit `audit-mq3zy4rg-bfrhrx`; study packet `dev-memo/study/2026-06-07-batch-audit-74.md`. resolved-in this WI's commit (PR for BATCH-CASEBOX-DEADLINE-CLEANUP-00). |

## Audit-list response projection (BATCH-CASEBOX-AUDIT-PROJECTION-00, WI-AP1 `<pending>`)

| Finding ID | Severity | Reason / disposition | Target | Safe? | Status | Notes |
|---|---|---|---|---|---|---|
| AUDIT-AUD-1 | Low | DISCOVERED + CLOSED in WI-AP1 (discovery for the audit-projection batch surfaced it) — `listAuditEventsHandler` returned the raw persistence page (`return { ok: true, value: page }`) with NO response projection, so `CaseBoxAuditEvent`'s server-authority/internal fields (`tenant_id`, `actor_user_id`, `matter_id`, the event `id`) + any open `[k: string]: unknown` extras crossed the IPC boundary on the LIVE `casebox:audit:listEvents` channel (consumed by the shipped audit panel `viewMatterAudit.ts:306`). Same class as FACTS-AUD-3 / GET-AUD-1; the audit LIST channel was the one the FACTS-AUD-3 remediation ("facts/documents/deadlines") missed. Single-tenant/single-user-moot in v1 (the fields are the user's own), hence Low — but it is the most sensitive field set (actor identity per audited action), so fixed immediately rather than deferred. | WI-AP1 (BATCH-CASEBOX-AUDIT-PROJECTION-00; `wi-ap1-audit-list-projection`) | YES (v1 single-tenant/single-user) | closed | CLOSED by WI-AP1 — added `LIST_AUDIT_EVENTS_RESPONSE_FIELDS` (action, entity_type, entity_id, timestamp, reason, before_state_hash, after_state_hash, prev_event_hash) + `RendererAuditEventRow = Pick<CaseBoxAuditEvent, …>` + `RendererAuditEventsPage` in `dto/audit.ts`; `ListAuditEventsResult` retyped; `listAuditEventsHandler` now `projectPage<RendererAuditEventRow>(page, LIST_AUDIT_EVENTS_RESPONSE_FIELDS)` (strips tenant_id/actor_user_id/id/matter_id + open-index extras; next_cursor preserved). `chainHeadHandler` unchanged (already typed `AuditChainHead`, no authority fields). Tests: `tests/ipc-list-projection.unit.test.mjs` audit block (raw-includes-authority+open-index vs response-omits across 2 rows + preserved cursor) + `renderer-dto-sync.test.mjs` `LIST_AUDIT_EVENTS_RESPONSE_FIELDS` authority-exclusion entry; the 6 existing audit panel tests stay green (513 pass). resolved-in this WI's commit; cc-suite review-plan `review-plan-mq4kc7jy-bswp51` (queue PASS) + the per-WI WI-AP1 audit/verify chain. |

## CaseBox IPC boundary sweep (BATCH-CASEBOX-IPC-BOUNDARY-SWEEP-00, WI-BS1 `<pending>`)

| Finding ID | Severity | Reason / disposition | Target | Safe? | Status | Notes |
|---|---|---|---|---|---|---|
| MATTER-AUD-1 | Low | DISCOVERED (IPC boundary scan) + CLOSED in WI-BS1 — matter:create/get/list/archive returned the raw `CaseBoxMatter` (carries tenant_id + actor_user_id + open `[k:string]:unknown` index; `case-box-matter.ts:23,27`) with NO response projection; `dto/matter.ts` had no `*_RESPONSE_FIELDS`. Same class as the closed FACTS-AUD-3/GET-AUD-1/AUDIT-AUD-1; the matter CRUD channels were never swept. v1 single-tenant/single-user-moot (Low) but the most-used read/write surface; wire-safe (renderer reads only display fields). | WI-BS1 (BATCH-CASEBOX-IPC-BOUNDARY-SWEEP-00; `wi-bs1-ipc-boundary-projection`) | YES (v1 single-tenant/single-user) | closed | CLOSED by WI-BS1 — added ONE shared `MATTER_RESPONSE_FIELDS` (14 renderer-consumed display fields incl. id; excludes tenant_id/actor_user_id/open-index + non-consumed opt-in/successor) + `RendererMatter`/`RendererMattersPage`; `matterHandlers` projectRow on create/get(null-safe)/archive + projectPage on list (next_cursor preserved). Result types retyped. Tests: ipc-list-projection matter blocks (create/get/get-null/list≥2rows+cursor/archive, raw-vs-projected + open-index) + renderer-dto-sync MATTER_RESPONSE_FIELDS authority-exclusion; collateral assertion ipc-handlers.unit.test.mjs:209 flipped (tenant_id present->absent) per AMENDMENT-1. resolved-in this WI's commit; review-plan review-plan-mq4m0z81-dqtpth + per-WI audit/verify. |
| REGDOC-AUD-1 | Low | DISCOVERED (IPC boundary scan) + CLOSED in WI-BS1 — document:register returned the raw `CaseBoxDocument` (tenant_id/actor_user_id/custody_chain) with no projection, while document:list + document:get already project (FACTS-AUD-3/GET-AUD-1). v1-moot (Low); the renderer only null-checks the register result. | WI-BS1 (same) | YES (v1 single-tenant/single-user) | closed | CLOSED by WI-BS1 — added `REGISTER_DOCUMENT_RESPONSE_FIELDS` (mirrors GET_DOCUMENT_RESPONSE_FIELDS; excludes tenant_id/actor_user_id/custody_chain + open-index) + `RendererRegisteredDocument`; `registerDocumentHandler` projectRow on the registered document (null/cancel path unchanged). Tests: ipc-list-projection register block + renderer-dto-sync REGISTER_DOCUMENT_RESPONSE_FIELDS authority-exclusion. resolved-in this WI's commit. |
