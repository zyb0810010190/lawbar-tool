<!-- batch-audit-attestation v1
range_base: 30a9f67aa6d4a07547cdab42f74806b42d782706
target_sha: 08fc9bec0ea922211c04878bbe3b218468e2f2cf
verdict: BATCH-PASS
findings: C0 H0 M0 L0
broker_job_id: audit-mq4nohzk-moth67
broker_output_sha256: bad47ad37514e8d2fd309fbf792d9378e0478424ef77e3091a67f81a67229880
-->

# Batch audit 80 — BATCH-CASEBOX-IPC-BOUNDARY-SWEEP-00 (post WI-BS1)

**Window**: `30a9f67..08fc9be` (4 commits). **Verdict**: BATCH-PASS (C0 H0 M0 L0).
**Broker**: Path 1 runner v0.2.18 native --background, gpt-5.5/high/read-only, job `audit-mq4nohzk-moth67`.
**Date**: 2026-06-07.

## Tasks completed

| Commit | What |
|--------|------|
| `a570b37` | Layer-B closeout of the BS-governance housekeeping window. |
| `1feb967` | AMENDMENT-2 governance (add casebox-ipc.electron.test.mjs to BS1 Allowed). |
| `568878f` | WI-BS1 — project matter:create/get/list/archive + document:register (close MATTER-AUD-1/REGDOC-AUD-1). Security-adjacent. npm test 521 pass; test:ipc-packaged 1 pass. |
| `08fc9be` | merge PR #75. |

## Decisions
- Completed the response-projection sweep across the last raw channels (matter CRUD + document register), mirroring FACTS-AUD-3. One shared MATTER_RESPONSE_FIELDS (14 consumed display fields incl. id; authority excluded); REGISTER_DOCUMENT_RESPONSE_FIELDS mirrors GET_DOCUMENT. getMatter null-safe; listMatters per-row + cursor. No persistence/contract/renderer/electron change.
- Two governed amendments (AMENDMENT-1 ipc-handlers.unit.test.mjs; AMENDMENT-2 casebox-ipc.electron.test.mjs) flipped pre-existing collateral assertions that were checking the PRE-FIX leak. The packaged bundle was rebuilt (electron-builder, signing skipped) so test:ipc-packaged validated the projected shape.

## Audit findings + disposition
- Per-WI: review-plan PASS + 2 amendment re-reviews PASS; audit `audit-mq4n02kr-7eem2t` (0C/0H/0M + 1 Low -> fixed via AMENDMENT-2); verify `verify-mq4nl9lr-17orrn` ALL CLOSED.
- Layer-B `audit-mq4nohzk-moth67`: **no findings**; product change confined to the projection files + tests; zero forbidden-area touch; security posture holds.

## Mistakes / near-misses
- TWO collateral test breakages from the projection (a unit test + a packaged-electron test) each asserting the pre-fix leak — both surfaced (audit + my own gate run), stopped-and-asked, governed amendments applied. Reinforces: a response-shape change ripples to every test asserting that shape, incl. packaged tests outside the per-WI gate.
- The packaged test first failed against a 5-day-stale bundle, then a transient 30s launch flake, then passed after `npm run dist` rebuild. Packaged tests need a fresh bundle to validate source changes.

## Review-later
- WI-BS2 (test-depth hardening: extend dto-sync to the 4 remaining allowlists; projection regression tests for createFact/transitionFact/createDocket/dismissDocket; table-drive forbidden-field tests) is next. After it, the IPC boundary sweep is complete.
- Cadence overhead (governance/amendment/closeout commits vs BATCH_AUDIT_EVERY=3) is now acute for this batch — noted across 74-80; a future WORKFLOW WI could reconsider.
