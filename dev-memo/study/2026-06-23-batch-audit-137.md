# Batch-audit study packet — WI-ENA5 impl + PR #107 merge window

**Date**: 2026-06-23. **Range**: `41801fc..79c12cd` (`main` / `evidence-a07-gate-design`).
**Type**: GOVERNANCE + SCAFFOLD + MERGE (the WI-ENA5-window closeout, the WI-ENA5 governance commit, the
WI-ENA5 coordinate transform roundtrip probe implementation, and the PR #107 merge into main).

## What shipped in this range
- `8ca95ab` — `chore(workflow): batch-audit closeout 61c0ca0..41801fc` (study packet 136 + closeout-log line;
  advanced marker to `41801fc`).
- `d17b709` — `chore(workflow): govern queue — WI-ENA5 coordinate transform roundtrip probe` (queue.* +
  queue-review-058 + a cc-suite reliability-log entry for the ENA5 review-plan TIMEOUT→retry).
- `a0f1e2f` — `scaffold(evidence): coordinate transform roundtrip probe (WI-ENA5)` — adds the internal-only
  `CoordinateRoundtripProbe.swift` (`normalize`/`denormalize`/`roundtripOnFixture`) + `CoordinateRoundtripProbeTests.swift`
  (asserts `maxAbsError < 1e-9` on the existing synthetic fixture), and a README update. Reuses the existing
  fixture; no new fixture.
- `79c12cd` — `Merge pull request #107 …` — content-empty merge of WI-ENA5 into main.

No product source/schema/contract/test/dependency/UI delta beyond the internal coordinate roundtrip probe. The
existing `native/evidence-core` JS shim is byte-unchanged.

## Audit trail
- `audit-mqq74kz9-at686f` (Path 1 runner v0.2.18, native `--background`, gpt-5.5 / high / read-only) over
  `41801fc..79c12cd`: **BATCH-PASS C0 H0 M0 L0**. Range + verdict declared on their own lines. Codex confirmed:
  internal Swift roundtrip probe + tests + README/governance only; no new deps/fixture, no JS-shim delta, no
  public API; no drift into rendering/PDFView/anchors/citation/page-identity/persistence/A0.7
  harness/marker/HMAC/provenance/Evidence-UI. rawOutput sha256 `3333ebb3…`.
  - Required recording: kind=audit; scope=`41801fc..79c12cd` (4 commits: closeout + governance + ENA5 impl +
    merge); runner=`~/.claude/plugins/cache/xiaolai/cc-suite/0.2.18/scripts/codex-runner.mjs`;
    model/effort/sandbox=gpt-5.5/high/read-only; jobId=`audit-mqq74kz9-at686f`; status=retrievable=YES;
    first-attempt success.

## Low disposition
- None. C0 H0 M0 L0.

## State
- Clears the audit-DUE window for `41801fc..79c12cd` and advances the marker to `79c12cd`. PR #107 (coordinate
  transform roundtrip probe) is merged to main. This closeout runs on the new branch `evidence-a07-gate-design`,
  opening WI-ENA6 (A0.7 renderer-conformance gate DESIGN — a docs/ADR lane, not implementation).

**Verdict: BATCH-PASS C0 H0 M0 L0** (safe to proceed).

<!-- batch-audit-attestation v1
range_base: 41801fc7a895f8c62b3af0fe83751899d426e239
target_sha: 79c12cd9b91ffdc89c45dfd70fc6edd30d9f2692
verdict: BATCH-PASS
findings: C0 H0 M0 L0
broker_job_id: audit-mqq74kz9-at686f
broker_output_sha256: 3333ebb33523a06d730464a848b5fb627936d1f14665b3afdbf9732323edad3d
-->
