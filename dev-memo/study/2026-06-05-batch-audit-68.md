# Batch-audit study packet — WI-D3/docket design + WI-D4 boundary amendment (#65), Layer B

**Date**: 2026-06-07.
**Batch range**: `08f0a7f..e2ca889` (4 commits since the prior `last-batch-audit` marker `08f0a7f`).
**Trigger**: `BATCH_AUDIT_EVERY=3`; the #65 merge brought commits-since-marker to 4 → breaker DUE.
**Audit broker job**: `audit-mq3rotni-86w273` (Path 1 runner v0.2.18, `gpt-5.5`, effort high, sandbox read-only, foreground).
**Verdict**: **BATCH-PASS (C0 / H0 / M0 / L1)** — the L1 is the carried, tracked `DTO-LOC-797` deferral.
**Closeout**: thirtieth production use of `scripts/workflow/batch-closeout.mjs`. DOCS/GOVERNANCE ONLY range (WI-D3 design + WI-D4 boundary amendment + prior WI-D2 closeout doc).
**Note**: range includes the prior WI-D2 closeout doc `f9d8cbf` (wrote `08f0a7f` into the marker), the WI-D4 boundary amendment `ad53ffe`, the WI-D3 design artifact `00dae80`, and the merge `e2ca889`.

<!-- batch-audit-attestation v1
range_base: 08f0a7f46b7b3a5937c362ce77337281af0cdfb4
target_sha: e2ca88993ad22079638b0750a3fbe18d970bf6d3
verdict: BATCH-PASS
findings: C0 H0 M0 L1
broker_job_id: audit-mq3rotni-86w273
broker_kind: audit
broker_output_sha256: 7d2244ea76de3f5ce9e734f9013c267c336c4a27c92f21091e16e2b9cb48d1ac
-->

## Tasks completed in the batch range

| Commit | PR | Behavior |
|---|---|---|
| `f9d8cbf` | (workflow) | DOC-ONLY: prior WI-D2 Layer-B closeout study packet (`2026-06-05-batch-audit-67.md`, range `316417f..08f0a7f`) + log line. Advanced marker to `08f0a7f`. |
| `ad53ffe` | (workflow) | GOVERNANCE AMENDMENT (user-approved): corrected WI-D4's defective file boundary — Allowed `renderer/preload.mts` (nonexistent) → `apps/lawbar-desktop/electron/preload.mts` (the real bridge); Forbidden `electron/**` → `electron/ipc/**` + `electron/main.ts`; + a pagination requirement (first page + Show-more on non-null `next_cursor`). Boundary correction only, re-reviewed PASS (`review-plan-mq3rjkvu-m4pz0t`). |
| `00dae80` | #65 | WI-D3: the pending-proposals read + dismiss UI design artifact (`dev-memo/design/2026-06-06-casebox-docket-pending-dismiss.md`). Doc-only ASSET; satisfies WI-D4's renderer-path design gate. |
| `e2ca889` | #65 | GitHub PR merge of WI-D3 to main. |

## Decisions (alternatives, why chosen, what would falsify)

1. **Stop-and-report the WI-D4 boundary defect, then amend with explicit approval** — the WI-D3 design review found WI-D4's governed Allowed listed a nonexistent `renderer/preload.mts` and Forbidden `electron/**` forbade the real `electron/preload.mts`. Implementing WI-D4 as-governed would require a forbidden path (a hard stop) and fixing it broadens a WI's file permissions (AGENTS.md needs explicit approval). The agent halted and the user approved the boundary correction. Falsifier: a product-scope expansion in the amendment (none — list-proposed + dismiss-proposed only).
2. **Narrow `electron/**` to `electron/ipc/** + electron/main.ts`** rather than allow all of `electron/**` — keeps main-process handler wiring + the entrypoint forbidden; only the preload bridge becomes touchable. Minimal correction.
3. **Pagination = first page + Show-more on non-null `next_cursor`** (not unbounded eager-fetch) — the design review's Medium; mirrors the existing facts `next_cursor` pattern and avoids silently hiding proposals beyond page one.

## Audit findings + disposition

- **C0 / H0 / M0 / L1.** Range is purely `dev-memo/**` (8 files); no `apps/**`/`services/**`/`docs/contracts/**`/`.claude/**` change. Governance content-bound (`queue_sha256` matches `queue.md` at `e2ca889`); `queue-review-015.md` single PASS documenting a boundary-correction-only amendment. WI-D3 design doc internally consistent, names `electron/preload.mts` + the pagination rule. Prior closeout `f9d8cbf` docs-only.
- **L1 = `DTO-LOC-797`** (carried; `dto.ts` 797/800) — unchanged, open, no DTO split performed.

## Per-WI provenance

- WI-D3 (#65): design review `review-plan-mq33imd0-bdmap1` (NEEDS-FIX) → fixes → amendment re-review `review-plan-mq3rjkvu-m4pz0t` (PASS). PR-time `check-ui-design-artifact` = SUCCESS (no renderer path). WI-D4 governance amendment `ad53ffe` (review artifact `queue-review-015.md`).
- `f9d8cbf` is the doc-only product of the WI-D2 closeout (`audit-mq33ck2r-9bn7o1` BATCH-PASS for `316417f..08f0a7f`).

## Batch state after this closeout

Advances `dev-memo/run/last-batch-audit` from `08f0a7f` to `e2ca889`, clearing the breaker. **WI-D1 (list) + WI-D2 (dismiss) merged; WI-D3 (design) merged; WI-D4 (renderer UI) governed (boundary-corrected) + pending implementation.** Open deferred: `DTO-LOC-797` (Low). Next: implement WI-D4.
