# Batch-audit study packet — WI-ENA3 impl + PR #105 merge window

**Date**: 2026-06-22. **Range**: `a77c780..61c0ca0` (`main` / `evidence-native-pagebox-probe`).
**Type**: GOVERNANCE + SCAFFOLD + MERGE (the WI-ENA3-window closeout, the WI-ENA3 governance commit, the
WI-ENA3 PDF load/page-count probe implementation, and the PR #105 merge into main).

## What shipped in this range
- `000953f` — `chore(workflow): batch-audit closeout 4b2d62b..a77c780` (study packet 134 + closeout-log line;
  advanced marker to `a77c780`).
- `ccd61d7` — `chore(workflow): govern queue — WI-ENA3 PDF load/page-count probe` (queue.* + queue-review-056).
- `2716ffb` — `scaffold(evidence): PDF load/page-count probe + synthetic fixture (WI-ENA3)` — adds
  `PdfLoadProbe.swift` (`load(url:) -> {loaded, pageCount}` via `PDFDocument(url:)`), `PdfLoadProbeTests.swift`
  (asserts `pageCount == 2`, guarded by `#if canImport(PDFKit)`), the 432-byte synthetic 2-page fixture
  `Fixtures/synthetic-twopage.pdf` (sha256 `63d91a6c…`), and a README update with fixture provenance + checksum.
- `61c0ca0` — `Merge pull request #105 …` — content-empty merge of WI-ENA3 into main.

No product source/schema/contract/test/dependency/UI delta beyond the PDF load/page-count probe + its synthetic
fixture. The existing `native/evidence-core` JS shim is byte-unchanged.

## Audit trail
- `audit-mqpywq6e-losl6s` (Path 1 runner v0.2.18, native `--background`, gpt-5.5 / high / read-only) over
  `a77c780..61c0ca0`: **BATCH-PASS C0 H0 M0 L0**. Range + verdict declared on their own lines. Codex confirmed:
  Swift PDF load + page-count only via `PDFDocument(url:)`; tests assert the synthetic 2-page fixture reports 2;
  no new deps, no JS-shim change, no UI/product wiring, no A0.7 harness/marker/HMAC/provenance, no
  renderer-conformance, no geometry/page-box extraction or assertions; fixture tiny + synthetic. rawOutput sha256
  `4698742e…`.
  - Required recording: kind=audit; scope=`a77c780..61c0ca0` (4 commits: closeout + governance + ENA3 impl +
    merge); runner=`~/.claude/plugins/cache/xiaolai/cc-suite/0.2.18/scripts/codex-runner.mjs`;
    model/effort/sandbox=gpt-5.5/high/read-only; jobId=`audit-mqpywq6e-losl6s`; status=retrievable=YES;
    first-attempt success.

## Low disposition
- None. C0 H0 M0 L0.

## State
- Clears the audit-DUE window for `a77c780..61c0ca0` and advances the marker to `61c0ca0`. PR #105 (PDF
  load/page-count probe) is merged to main. This closeout runs on the new branch
  `evidence-native-pagebox-probe`, opening WI-ENA4 (page-box inspection probe — the next authorized
  implementation hard-stop). WI-ENA4 reuses the existing synthetic fixture (no new fixture).

**Verdict: BATCH-PASS C0 H0 M0 L0** (safe to proceed).

<!-- batch-audit-attestation v1
range_base: a77c780b131b0168378b9097c8ca0db155e98e83
target_sha: 61c0ca08f99ad4996cb57996199f9fe31c127379
verdict: BATCH-PASS
findings: C0 H0 M0 L0
broker_job_id: audit-mqpywq6e-losl6s
broker_output_sha256: 4698742e76d38cde9fd905227dc58a933f855eeed1f112ca39ea721ecf83d50f
-->
