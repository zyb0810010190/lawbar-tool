# Batch-audit study packet — WI-704/LOC-split (#56) + governance + prior closeout, Layer B

**Date**: 2026-06-05.
**Batch range**: `e15d833..7b01a78` (4 commits since the prior `last-batch-audit` marker `e15d833`).
**Trigger**: `BATCH_AUDIT_EVERY=3`; the #56 merge brought commits-since-marker to 4 (≥ 3) → `batch-commit-guard.sh` breaker DUE on post-merge main.
**Audit broker job**: `audit-mq12te5b-8sdz5u` (Path 1 runner v0.2.18, `gpt-5.5`, effort high, sandbox read-only, native `--background`).
**Verdict**: **BATCH-PASS (C0 / H0 / M0 / L0)**.
**Closeout**: nineteenth production use of `scripts/workflow/batch-closeout.mjs`. Closes the last open BATCH-CASEBOX-UI-00 follow-up (LOC-RVM-1200).

<!-- batch-audit-attestation v1
range_base: e15d83353e9f607d817efe121fa67ec436432a1c
target_sha: 7b01a78d792b64e9b40e5936bf268fd6b41023bc
verdict: BATCH-PASS
findings: C0 H0 M0 L0
broker_job_id: audit-mq12te5b-8sdz5u
broker_kind: audit
broker_output_sha256: 597a1bdbbde3954e7214d089be0cca67fa29fdaf2b38b87f3b68f1ab024887b1
-->

## Tasks completed in the batch range

| Commit | PR | Behavior |
|---|---|---|
| `27be0a3` | (workflow) | DOC-ONLY: the prior WI-702 closeout's study packet (`dev-memo/study/2026-06-05-batch-audit-56.md`, range `7ddc9b8..e15d833`) + a `dev-memo/batch-closeout-log.md` line. |
| `8670e45` | (workflow) | GOVERNANCE: added Type:TEST WI-704 (split fact-write tests to close LOC-RVM-1200) to the governed queue; queue.linted/reviewed/governed + `reviews/queue-review-008.md`. |
| `f4796ac` | #56 | WI-704: MOVED the 11 "add fact" tests (+ stubWithFact/mountFactsWithAdd helpers) VERBATIM out of `renderer-view-matter.test.mjs` into a new sibling `renderer-fact-write.test.mjs` (local factRow; shared `_view-matter-dom.mjs` untouched), wired into the `package.json` "test" gate, closed LOC-RVM-1200. Host 1200→973 LOC; new file 259. Suite stays 422 pass / 0 fail. |
| `7b01a78` | #56 | GitHub PR merge of WI-704 to main. |

## Decisions (alternatives, why chosen, what would falsify)

1. **Resolve LOC-RVM-1200 by a sibling-file MOVE, not a rewrite** — the cohesive WI-701/WI-703 "Add fact" block moved verbatim into `renderer-fact-write.test.mjs` (mirroring `renderer-deadline-write.test.mjs`); a local `factRow` avoids editing the shared harness. Falsifier: a dropped test/assertion, a touched product file, or the host still ≥ 1200 LOC (none — audit A1/A2/A3; the moved test-title set was verified identical before/after). Per-WI audited `audit-mq12kjd1-uspmt7` (PASS C0 H0 M0 L0) + verified `verify-mq12nicm-dt0xrl` (ALL CLOSED).
2. **TEST-only, no product behavior** — Type TEST, no `renderer/**` path, so no Design-artifact gate; `package.json` only appends the new test path (no dependency). Falsifier: any behavior or dependency change in the range (none — audit A2).

## Audit findings + disposition

- **C0 / H0 / M0 / L0.** No findings. The range is test-maintenance + governance/closeout docs only; no `services/case-box-persistence/src`, `docs/contracts`, `src/caseBox`, `renderer/**`, `electron/**`, shared harness, error-code surface, dependency, hook, marker/breaker source tamper, or test deletion/weakening (a move is not a deletion). Host below the 1200 cap with margin (973); new file in the gate; LOC-RVM-1200 closed; governance internally consistent. No deferred-findings disposition required (L0).

## Per-WI provenance (already independently verified)

- #56 WI-704: queue review-plan `review-plan-mq12cs1g-m7dz9b` (READY-WITH-LOW) → audit `audit-mq12kjd1-uspmt7` (PASS C0 H0 M0 L0) → verify `verify-mq12nicm-dt0xrl` (ALL CLOSED). Gate `npm --prefix apps/lawbar-desktop test` = 422 pass / 0 fail (same total as before the move).
- `8670e45` governance: queue-lint PASSED; review-plan `review-plan-mq12cs1g-m7dz9b` READY-WITH-LOW; `reviews/queue-review-008.md`.
- `27be0a3` is the doc-only product of the prior WI-702 closeout (`audit-mq1256bf-zflal4` BATCH-PASS for `7ddc9b8..e15d833`).
- This Layer-B pass checks the ACCUMULATION; clean.

## Batch state after this closeout — BATCH-CASEBOX-UI-00 COMPLETE, all follow-ups closed

This closeout advances `dev-memo/run/last-batch-audit` from `e15d833` to `7b01a78`, clearing the breaker. **All BATCH-CASEBOX-UI-00 WIs are merged**: WI-701 (Add Fact UI, #53), WI-703 (default-purpose fix, #54), WI-702 (Add/Confirm Deadline UI, #55), WI-704 (TEST split, #56). **All deferred follow-ups from this batch are now CLOSED**: `CBW-UI-701-DEFAULT-PURPOSE` (closed by WI-703) and `LOC-RVM-1200` (closed by WI-704). No open BATCH-CASEBOX-UI-00 follow-ups remain. The governed queue holds no further WIs.
