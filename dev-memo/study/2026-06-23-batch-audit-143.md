# Batch-audit study packet — WI-ENA11 window + WI-ENA11-FIX1 remediation (BATCH-FAIL → BATCH-PASS)

**Date**: 2026-06-23. **Range**: `5d75f57..786c984` (`evidence-a07-marker-write-fix1`).
**Type**: REMEDIATION CLOSEOUT (a prior BATCH-FAIL window, now passing after the fix).

## What this closeout covers
The WI-ENA11 marker-write window initially **BATCH-FAILED**; this closeout covers that window **plus** the
WI-ENA11-FIX1 remediation, which the re-run audit confirms BATCH-PASS.

Commits in range:
- `153a578` — `chore(workflow): batch-audit closeout ac25c6e..5d75f57` (prior closeout; marker → `5d75f57`).
- `f33832d` — `chore(workflow): govern queue — WI-ENA11 …` (ENA11 governance).
- `cde45dc` — `feat(evidence): A0.7 marker write + provenance validation, local-only HMAC (WI-ENA11)`.
- `67c6132` — `Merge pull request #113 …` (ENA11 into main).
- `f1b3a74` — `chore(workflow): govern queue — WI-ENA11-FIX1 …` (FIX1 governance; via single-use human.override).
- `786c984` — `fix(evidence): bind + check isMarker in A0.7 marker provenance (WI-ENA11-FIX1)` (FIX1 impl; via
  single-use human.override).

## The BATCH-FAIL and its remediation
- **First audit** `audit-mqqlaozw-jns64v` over `5d75f57..67c6132`: **BATCH-FAIL C0 H0 M1 L0**. M1: in
  `scripts/workflow/a07_marker.py`, `isMarker` was written (true) but NOT in the HMAC-bound canonical
  `PAYLOAD_FIELDS` and NOT checked by `validate`, so editing a genuine marker's `isMarker: true -> false`
  survived validation (violating "any touched marker fails" / "isMarker=false cannot satisfy validity").
- **Remediation WI-ENA11-FIX1** (option B, user-chosen): added `isMarker` to `PAYLOAD_FIELDS` (now covered by
  `provenancePayloadHash` + HMAC; writer + validator share `_canonical()`) and made `validate` require
  `isMarker is True` (strict boolean); removed the redundant post-canonical assignment; added a regression test
  flipping only `isMarker` true→false. Governance: queue-review-065 (review-plan READY, NARROW-FIX-ONLY,
  `review-plan-mqqm2c4q-r7mf70`); fix audit `audit-mqqmdvqn-lksbgg` (no findings); verify `verify-mqqmgen9-ct59fr`
  (ALL CLOSED).
- **Override authorization**: the BATCH-FAIL window blocked the count gate (no closeout possible without a
  BATCH-PASS), so the two direct FIX1 commits landed via single-use, user-authorized `human.override`
  (governance + impl), both consumed + logged in `dev-memo/run/log.md` (`2026-06-23T12:25:34Z`,
  `2026-06-23T12:40:03Z`).

## Re-run audit trail (this closeout)
- `audit-mqqmuf79-eg6fxq` (Path 1 runner v0.2.18, native `--background`, gpt-5.5 / high / read-only) over
  `5d75f57..786c984`: **BATCH-PASS C0 H0 M0 L0**. Codex confirmed M1 resolved (isMarker HMAC-bound + strictly
  checked; regression present), markers local-only/gitignored, no committed marker/ledger/key, env-only
  fail-closed key custody, provenance-valid (not schema-valid), ledger non-replay, only pass+ok eligible,
  committed/staged evidence rejected; no new C/H/M; no UI/product/JS-shim/harness change. rawOutput sha256
  `b52ed190…`.
  - Required recording: kind=audit; scope=`5d75f57..786c984` (remediation window);
    runner=`~/.claude/plugins/cache/xiaolai/cc-suite/0.2.18/scripts/codex-runner.mjs`;
    model/effort/sandbox=gpt-5.5/high/read-only; jobId=`audit-mqqmuf79-eg6fxq`; status=retrievable=YES;
    first-attempt success.

## Low disposition
- None. C0 H0 M0 L0. (The prior M1 is closed by WI-ENA11-FIX1.)

## State
- Clears the (previously failed) window and advances the marker to `786c984`. The ENA11 marker-write defect is
  remediated on the `evidence-a07-marker-write-fix1` branch. EVW5 (A0.7 hard hooks) remains deferred and is NOT
  resumed by this lane.

**Verdict: BATCH-PASS C0 H0 M0 L0** (remediation complete; safe to proceed).

<!-- batch-audit-attestation v1
range_base: 5d75f57b454be97ba3febeaa46fad380d49bdd89
target_sha: 786c98462b250f9bcdc60043cc3cb88704e28c4b
verdict: BATCH-PASS
findings: C0 H0 M0 L0
broker_job_id: audit-mqqmuf79-eg6fxq
broker_output_sha256: b52ed19006932318c14789055f5110afcfe9e476887da90810aa8a947b8c07be
-->
