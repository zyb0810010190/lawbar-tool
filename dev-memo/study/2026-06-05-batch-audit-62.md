# Batch-audit study packet — WI-806/fix-facts-header (#60), Layer B

**Date**: 2026-06-06.
**Batch range**: `ee594ce..132aa30` (4 commits since the prior `last-batch-audit` marker `ee594ce`).
**Trigger**: `BATCH_AUDIT_EVERY=3`; the #60 merge brought commits-since-marker to 4 (≥ 3) → `batch-commit-guard.sh` breaker DUE on post-merge main.
**Audit broker job**: `audit-mq1ybywz-4wdxnk` (Path 1 runner v0.2.18, `gpt-5.5`, effort high, sandbox read-only, foreground).
**Verdict**: **BATCH-PASS (C0 / H0 / M0 / L0)**.
**Closeout**: twenty-fourth production use of `scripts/workflow/batch-closeout.mjs`. WI-806 was a standalone Type:UI comment/header-only maintenance WI (no umbrella batch).
**Note**: the range includes the prior closeout doc commit `9dd5d9d` (which wrote `ee594ce` into the marker file and is therefore the first commit *after* the marker content), the WI-806 governance commit `f8be1e0`, the WI-806 implementation `01a61b9`, and the merge `132aa30`.

<!-- batch-audit-attestation v1
range_base: ee594cedfed6decb1730fbbd78dde349742d485a
target_sha: 132aa3050f9975267026c0d34b92bd6ed0873548
verdict: BATCH-PASS
findings: C0 H0 M0 L0
broker_job_id: audit-mq1ybywz-4wdxnk
broker_kind: audit
broker_output_sha256: c45fa2e16a8d1bbddacce0ca82912257f53f7ea670e2738e79858d5624c2048d
-->

## Tasks completed in the batch range

| Commit | PR | Behavior |
|---|---|---|
| `9dd5d9d` | (workflow) | DOC-ONLY: the prior Layer-B closeout's study packet (`dev-memo/study/2026-06-05-batch-audit-61.md`, range `6c7f767..ee594ce`) + a `dev-memo/batch-closeout-log.md` line. Advanced the marker to `ee594ce` (closing the WI-805 LOC-split batch). Docs/governance only. |
| `f8be1e0` | (workflow) | GOVERNANCE: WI-806 queue (`queue.md` + `queue.linted`/`queue.reviewed`/`queue.governed` = lint+review) + the review artifact `dev-memo/run/reviews/queue-review-012.md` (single `QUEUE_REVIEW_VERDICT=PASS`, review-plan `review-plan-mq1xycju-xvzos1`). Governance only; no product change. |
| `01a61b9` | #60 | WI-806: comment/header-only fix of `apps/lawbar-desktop/renderer/screens/viewMatterFacts.ts` — the stale file-top doc comment `NO edit / delete / review / evidence linking` (stale since WI-804 added per-fact Review/Accept/Reject controls) now documents the ADD control (`casebox:fact:create`, WI-701) AND the REVIEW/ACCEPT/REJECT controls (`casebox:fact:transition`, WI-804), with `review` dropped from the `NO ...` list. Every changed line is `//`-prefixed; no executable/import/selector/visible-text/style/test/package change. Plus the `CBW-UI-804-HEADER` ledger closure in `dev-memo/deferred-audit-findings.md`. |
| `132aa30` | #60 | GitHub PR merge of WI-806 to main. |

## Decisions (alternatives, why chosen, what would falsify)

1. **Option A — cite the existing WI-803 design artifact** — the PR-time `check-ui-design-artifact` gate keys on file PATH (`apps/*/renderer/*`), not diff content, so even a comment-only renderer change demands a `Design artifact:` line. Rather than invent a new artifact (rejected) or modify the gate (rejected — a separate SCAFFOLD WI was explicitly declined), the PR body cited the EXISTING WI-803 artifact `dev-memo/design/2026-06-05-casebox-fact-review.md`, which governed the very WI-804 review controls this comment documents. Falsifier: a newly-created design file in the range, or any edit to `check-ui-design-artifact.sh` / `ui-design-artifact.yml`. Audit confirmed neither — gate logic unchanged, no new artifact.
2. **Stop-and-report before governance was the prior turn's correct call** — the path-based gate over-triggering on a comment-only change is a known limitation; surfacing it for a user decision (Option A vs gate-fix vs won't-fix) rather than silently picking one preserved the "don't invent a design artifact" boundary. The user chose Option A.
3. **Comment wording verified against real code** — the new comment claims per-fact Review/Accept/Reject via `casebox:fact:transition` with server-injected reviewer/timestamps and renderer-forwards-narrow-DTOs. Audit A2 confirmed `renderReviewControls` + `api.transitionFact` + the transition handler match the description (no overclaim).

## Audit findings + disposition

- **C0 / H0 / M0 / L0.** No security or behavioral drift: the entire range is comment-only + docs/governance; `viewMatterFacts.ts` changes only `//`-prefixed lines (A1); the new comment is accurate to the code (A2); governance is clean — `queue.governed`=lint+review, single PASS verdict, no marker/breaker/hook/secrets/**design-gate-logic** change (A3); ledger `CBW-UI-804-HEADER` accurately closed citing WI-806, honestly attributing the gate to the existing WI-803 artifact (A4); zero behavior/test/package/contract impact (A5).
- The per-WI audit `audit-mq1y2s3f-x13vma` had surfaced 2 Lows (ledger wording overclaim; untracked-scratch hygiene); the wording was corrected and the scratch dispositioned via exact-path staging before commit `01a61b9`, confirmed by verify `verify-mq1y66ml-0nwplb` (ALL CLOSED). This Layer-B pass independently found nothing further.

## Per-WI provenance (already independently verified)

- #60 WI-806: review-plan `review-plan-mq1xycju-xvzos1` (queue PASS) → audit `audit-mq1y2s3f-x13vma` (C0 H0 M0 L2; both Lows fixed in-WI) → verify `verify-mq1y66ml-0nwplb` (ALL CLOSED). Gate `npm --prefix apps/lawbar-desktop test` = 453 pass / 0 fail. PR-time `check-ui-design-artifact` = SUCCESS via the existing WI-803 artifact.
- `9dd5d9d` is the doc-only product of the prior Layer-B closeout (`audit-mq1xibfy-fryn1a` BATCH-PASS for `6c7f767..ee594ce`).
- This Layer-B pass checks the ACCUMULATION; static results clean.

## Batch state after this closeout

This closeout advances `dev-memo/run/last-batch-audit` from `ee594ce` to `132aa30`, clearing the breaker (commits-since-marker back to 0). **`CBW-UI-804-HEADER` is now CLOSED** — the last open follow-up from the recent fact-review / LOC-split / header-fix UI line (CBW-UI-701-DEFAULT-PURPOSE, IPC-HANDLERS-1534, and now CBW-UI-804-HEADER are all closed).

**Other deferred findings remain intentionally open** and are NOT in scope here: `dev-memo/deferred-audit-findings.md` still carries 38 open rows (39 closed), almost all pre-existing cleanup-only / accepted-deferral Lows from unrelated lanes — the case-box-persistence B-series LOC/duplication debt (`D4#1`/`D2#1` across B8-B11), the staging/commit-guard hardening backlog (`BCG-6` High and `BCG-8/9/10`, `BGAA/BCSA-2`, `BRCBW-9` — all user-authorized deferrals), `AT1-L2`, and the WI-601/602 barrel/enum/test-hardening items (`CBW-601-BARREL`, `CBW-602-PURPOSE-ENUM`, `CBW-602-FORBIDDEN-TESTS`). These are tracked, dispositioned, and deferred to their own future WIs; none is a follow-up of WI-806.
