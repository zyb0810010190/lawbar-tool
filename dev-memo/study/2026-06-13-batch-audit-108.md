# Batch-audit study packet — WI-i18n-2 (shell + matter-list migration) + header remediation

**Date**: 2026-06-13. **Range**: `0019e8f..6b3e955` (branch `product-i18n-shell-list`).
**Type**: UI (renderer: shell chrome + matter-list migrated to i18n) + 2 comment-only remediations.

## What shipped in this range
- **WI-i18n-2** (commit `f1ca3ca`): wired the **app shell** chrome (index.html `data-i18n`/`data-i18n-aria`
  → index.ts `applyShellI18n()` → `t()`) and the **matter-list** route (`listMatters.ts` → `i18n/labels`
  facade + `t()`) to the zh-CN catalog; removed the list-local `*Zh` label maps (single source); burned
  down the migrated shell+list allowlist entries; updated tests.
  - **INTENTIONAL visible change (shell only):** shell chrome English → zh-CN (drift-closure per
    plan-i18n-00 §6.5; user-authorized for WI-i18n-2). Matter-list copy unchanged zh-CN (same catalog values).
- **Remediation 1** (`0424c64`, comment-only): fixed the stale `catalog.ts` header (it had claimed no
  screen consumes the catalog / seeds only enum keys).
- **Remediation 2** (`6b3e955`, comment-only): swept the remaining stale `labels.ts` + `t.ts`
  "infra-only/UNWIRED/no screen imports" headers.
- Plus carried already-audited governance docs (a prior closeout + PR #95 merge).

## Audit trail (BATCH-FAILs were all comment-drift Lows)
- `audit-mqco4u8l-fgwfmk` **BATCH-FAIL C0 H0 M0 L1** (stale catalog.ts header) → fixed `0424c64`.
- `audit-mqcodsqc-gr0ayv` **BATCH-FAIL C0 H0 M0 L1** (sibling stale labels.ts header) → swept labels.ts+t.ts `6b3e955`.
- `audit-mqd2lkvw-hmf5f4` (Path 1 runner v0.2.18 native `--background`, gpt-5.5 / medium / read-only) over
  `0019e8f..6b3e955`: **BATCH-PASS C0 H0 M0 L0** — headers accurate, remediations comment-only, scope clean
  (no detail/create/archive/sub-section/src/electron/IPC/contract change), `*LabelZh` gone, list copy
  unchanged zh-CN, shell catalog-driven, guard burn-down correct, no dep, t() throws, eventKindLabel
  type-only import.

Lesson (for the next migration WI): when a screen-migration WI wires modules that carried WI-i18n-1
"infra-only/unwired" header comments, sweep ALL of those module headers in the same commit — the first
remediation fixed only catalog.ts and the sibling labels.ts/t.ts staleness recurred.

## Gates
- `npm --prefix apps/lawbar-desktop test` → **609/609**.

**Verdict: BATCH-PASS C0 H0 M0 L0.**

<!-- batch-audit-attestation v1
range_base: 0019e8f4911f79751aa3d4944a4c1ae1f7863904
target_sha: 6b3e955fb5c7640438da71e90e482f1696b47621
verdict: BATCH-PASS
findings: C0 H0 M0 L0
broker_job_id: audit-mqd2lkvw-hmf5f4
broker_output_sha256: 5ea34ad808d7831b986cd13ebc37f5996cd199ec01d2c2a38a882907c2dae72e
-->
