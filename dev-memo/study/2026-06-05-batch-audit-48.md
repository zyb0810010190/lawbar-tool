# Batch-audit study packet — WI-303/AT1-L3 (#48) + prior closeout, Layer B (BATCH-BCG-HARDENING-00 final)

**Date**: 2026-06-05.
**Batch range**: `b737122..cb7486d` (3 commits since the prior `last-batch-audit` marker `b737122`).
**Trigger**: `BATCH_AUDIT_EVERY=3`; 3 commits ≥ 3 → `batch-commit-guard.sh` breaker fired DUE on post-merge main.
**Audit broker job**: `audit-mq0iwk5v-8xggv0` (Path 1 runner v0.2.18, `gpt-5.5`, effort high, sandbox read-only, native `--background`).
**Verdict**: **BATCH-PASS (C0 / H0 / M0 / L0)**.
**Closeout**: tenth production use of `scripts/workflow/batch-closeout.mjs`; runs on post-merge `main`. **Completes governed batch BATCH-BCG-HARDENING-00.**

<!-- batch-audit-attestation v1
range_base: b73712283b0f1846d30dcdab0df0c8b43db75590
target_sha: cb7486d4cff309e54f27a0f801ea84401ef6ca7a
verdict: BATCH-PASS
findings: C0 H0 M0 L0
broker_job_id: audit-mq0iwk5v-8xggv0
broker_kind: audit
broker_output_sha256: 64babf8b27caa9492f2137ecc86421143d4a532ea12552c4efe101b9e36c1cf5
-->

## Tasks completed in the batch

| Commit | PR | Behavior |
|---|---|---|
| `172240a` | (workflow) | DOC-ONLY: the prior batch-closeout's study packet (`9cf96a0..b737122`, #47) + a `dev-memo/batch-closeout-log.md` line. |
| `132cb0a` | #48 | WI-303 / AT1-L3: corrected the stale §6.1 `fdesetup`-error test row in `dev-memo/plan-encryption-at-rest-00.md` to match the shipped impl (error → `unknown` → block-prod / warn-dev), and closed the AT1-L3 ledger row. Doc-only; no impl change. |
| `cb7486d` | #48 | GitHub PR merge of WI-303 to main. |

## Decisions (alternatives, why chosen, what would falsify)

1. **Correct the doc to match code, not the reverse** — the §6.1 test row claimed "proceed with audit log" but the shipped `fileVaultProbe.ts` treats an `fdesetup` error as `unknown` → block-prod / warn-dev. Per AGENTS.md source hierarchy (generated/impl beats doc), the doc was stale. Falsifier: the impl actually ignoring the error (it does not — execFile error/catch → `state:"unknown"`, lines 99/110; `decideAction(unknown, production)`=`block`, line 47).
2. **Self-review (Low / docs-only)** — no per-WI broker chain; grounded against the authoritative impl; this Layer-B batch audit independently re-checked the correction (no new doc inaccuracy found).
3. **Governed gate enforced** — `grep -q "block-prod" && ! grep "proceed with audit log"` on the plan file passes.

## Audit findings + disposition

- **C0 / H0 / M0 / L0.** No accumulation drift. Net diff confined to four `dev-memo` files (the §6.1 wording change, the AT1-L3 ledger close, and the prior closeout's study packet + log). NO product/runtime code (the `fileVaultProbe.ts` impl is UNCHANGED — WI-303 corrected the doc to MATCH it), no UI, no manifest/dependency, no schema/fixture, no hook changes, no marker/breaker source tampering, no test deletion/weakening, no security-boundary surface change. The broker independently confirmed the §6.1 correction matches `fileVaultProbe.ts` (error → `unknown`; `decideAction("unknown","production")` blocks, dev warns) with no new inaccuracy.

## Batch-level closure — BATCH-BCG-HARDENING-00 (all 3 WIs)

| WI | Finding | Severity | Resolution commit | Closeout |
|---|---|---|---|---|
| WI-301 | BCG-7 (config validation, fail-closed) | Medium | `f2d793f` (#46) | `bf12bed`→`8a05e9e` chain |
| WI-302 | BCG-4 + BCG-5 (token atomicity) | High ×2 | `77c337b` (#47) | `172240a` |
| WI-303 | AT1-L3 (doc correction) | Low | `132cb0a` (#48) | this packet |

All deferred-finding rows BCG-7 / BCG-4 / BCG-5 / AT1-L3 are CLOSED on main.

## Per-WI provenance (already independently verified)

- #48 WI-303/AT1-L3: self-review (doc-only, grounded in `fileVaultProbe.ts`); governed WI-303 gate PASS.
- `172240a` is the doc-only product of the prior closeout (`audit-mq0igtnv-3vrkdo` BATCH-PASS for `9cf96a0..b737122`).
- This Layer-B pass checks the ACCUMULATION, not the per-WI line findings.
