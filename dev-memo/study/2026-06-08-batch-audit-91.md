<!-- batch-audit-attestation v1
range_base: d34b99ec7a63164417285d7952b5b3829b8770fd
target_sha: 53bc08cadccda50f6e8b17ed7e38d5fb33fc9303
verdict: BATCH-PASS
findings: C0 H0 M0 L0
broker_job_id: audit-mq5b63yo-z5g8jm
broker_output_sha256: a7fa3cd5174913273dede1a393a76d1e5e3629d6a592d4189f333a319e546b6e
-->

# Batch audit 91 — BATCH-CASEBOX-AUDIT-EVENT-KIND-V2-UI-00 (batch-end, post merge #82)

**Window**: `d34b99e..53bc08c` (4 commits). **Verdict**: BATCH-PASS (C0 H0 M0 L0).
**Broker**: Path 1 runner v0.2.18 native --background, gpt-5.5/high/read-only, job `audit-mq5b63yo-z5g8jm`.
**Date**: 2026-06-08. **Closes BATCH-CASEBOX-AUDIT-EVENT-KIND-V2-UI-00 (Batch 2 of 2) — and the whole
audit-event-kind feature.**

## Tasks in window
| Commit | What |
|--------|------|
| `bc59cc8` | Mid-batch closeout (post WI-U1). |
| `c1a5ec8` | WI-U2 — design artifact (the 49-key label table). |
| `f2c4c9e` | WI-U3 — renderer humanized audit labels + new test + one package.json test-line. |
| `53bc08c` | Merge PR #82 to main. |

## Batch summary (3 WIs) — surface event_kind in the audit UI
- **WI-U1 (IMPL)** `d34b99e`: `event_kind` added to `LIST_AUDIT_EVENTS_RESPONSE_FIELDS`;
  `audit_schema_version` stays internal; `ipc-list-projection` proves it crosses while authority +
  internal fields are stripped; no handler change.
- **WI-U2 (ASSET)** `c1a5ec8`: design artifact `dev-memo/design/2026-06-08-audit-event-kind-labels.md`
  — all 49 `CASE_BOX_AUDIT_EVENT_KINDS` labels + null/unknown → `action·entity_type` fallback + a11y +
  honest wording.
- **WI-U3 (UI)** `f2c4c9e`: new `auditEventLabels.ts` (`EVENT_KIND_LABELS` 49-entry map + `auditEventLabel`);
  `viewMatterAudit.ts` renders the humanized label in the accessible `view-audit-action` span for a
  known kind, raw `action` for null/legacy/unknown (no transition inference); new
  `renderer-audit-labels.test.mjs` (map↔contract sync + label + fallback). 538 desktop tests pass.

The audit panel now shows e.g. "Deadline marked met" vs "Deadline marked missed" — the distinction that
was lost before Batch 1, now persisted, hashed/tamper-evident, projected, and humanized end-to-end.

## Audit / review trail (this batch)
- review-plan `review-plan-mq5ajxhu` architecture CONFIRMED (FAIL line = pre-govern stale-hash, resolved
  by governing).
- WI-U1 audit `audit-mq5arjgn` PASS. Mid-batch `audit-mq5atyp6` BATCH-PASS. WI-U3 audit `audit-mq5b0xyr`
  PASS. Batch-end `audit-mq5b63yo` BATCH-PASS C0 H0 M0 L0.

## Feature complete (both batches)
- Batch 1 (`BATCH-CASEBOX-AUDIT-EVENT-KIND-V2-CONTRACT-00`): persist hashed `event_kind` +
  `audit_schema_version` via versioned canonicalization; v1 byte-identical; mixed chains verify;
  tamper-evident; persistence conformance — no migration.
- Batch 2 (this): project `event_kind` (audit_schema_version internal) + humanized renderer labels with
  honest fallback.

## Review-later
- DT-V1-L1 (Batch-1 schema-description "TS validator" → "chain verifier") remains OPEN/deferred — a
  contract-doc wording Low for a future contract-touching WI. BRCBW-SUBSTR open. Cadence overhead
  persists (batches 74-91).
