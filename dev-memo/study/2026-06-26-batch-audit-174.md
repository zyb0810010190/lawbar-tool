# Batch-audit study packet — WI-A3-LINK-IPC-T1 implementation window

**Date**: 2026-06-26. **Range**: `93976ca..9b70e08` (`evidence-a3-link-ipc-operation`, branched from `main`).
**Type**: CLOSEOUT + GOVERN + IMPL (the audited link IPC/API surface).

## What shipped in this range
- `660c8a6` — `chore(workflow): batch-audit closeout 6fa2870..93976ca` (study 173; marker → `93976ca`).
- `0e9fe77` — `chore(workflow): govern queue — WI-A3-LINK-IPC-T1 …` (queue.* + queue-review-094; A0.7-gated,
  custody 9b; 3 review-plan rounds incl. the 1a/2i amendment).
- `9b70e08` — `feat(apps/lawbar-desktop): audited link IPC/API surface (WI-A3-LINK-IPC-T1)` — 5 `casebox:link:*`
  channels + main-process handlers (createLink/unlink/relink/list/export) + camelCase DTOs + `LINK_RESPONSE_FIELDS`
  projection + preload `CaseBoxApi` methods + a concrete `LinkPersistenceProvider` (SqliteCaseBoxPersistence + db;
  InMemory fallback → safe boundary error) + the 1-line `errorMap.ts` `anchor_referenced` exhaustiveness sync +
  the 19-test desktop unit suite + the `package.json` test-list registration. Actor/tenant main-injected; scoped
  unlink/relink preflight (fail-closed); IPC + preload only.

Confined to `apps/lawbar-desktop/**` (the link IPC files + the 1-line errorMap + the test-list package.json line)
+ `dev-memo/**` governance/closeout/plan/deferred-findings. NO renderer UI (renderer/api.ts + renderer/types.ts
untouched), NO `services/**`, NO `schema.ts` / `CURRENT_SCHEMA_VERSION` change, NO `docs/contracts/**`, NO native,
NO dependency (package-lock.json + dist-tarballs NOT committed), NO runtime A0.7 marker. Source plain UTF-8 text.
No secrets / no marker/evidence-run committed; fixtures synthetic-only.

## Audit trail
- `audit-mqv2c2xq-so3ey7` (Path 1 runner v0.2.18, native `--background`, gpt-5.5 / high / read-only) over
  `93976ca..9b70e08`: **BATCH-PASS C0 H0 M0 L1**. rawOutput sha256
  `f37dfae9122926ddda1516232e9f2980655f4763d36d9673d3be807437840242`.
  - Required recording: kind=audit; scope=`93976ca..9b70e08` (3 commits: closeout + governance + impl);
    runner=`~/.claude/plugins/cache/xiaolai/cc-suite/0.2.18/scripts/codex-runner.mjs`;
    model/effort/sandbox=gpt-5.5/high/read-only; jobId=`audit-mqv2c2xq-so3ey7`; status retrievable=YES;
    first-attempt success.
- (Lane-internal records, already audited: review-plan READY (3 rounds, the 1a/2i amendment); human 9b gated PASS;
  impl audit `audit-mqv217b3-b5hfht` PASS C0 H0 M0 L1 (L1 sourceType enum fixed); verify `verify-mqv25tg6-s4emw2`
  ALL CLOSED.)

## Low disposition
- **L1 (deferred — doc-consistency, no runtime/security impact)**: `LINK-IPC-T1-D2`. The committed governance prose
  (`dev-memo/run/queue.md` Risk-flags + `dev-memo/run/reviews/queue-review-094.md`) still carries a leftover
  "no errorMap change" / "anchor_referenced NOT surfaced" phrase from BEFORE the user-authorized 1a amendment that
  ADDED the one `anchor_referenced` exhaustiveness line. The actual code change is exactly the authorized one-line
  sync; the stale prose is an internal governance-record inconsistency (the Allowed-files + Commit-boundary blocks
  WERE correctly amended). Cleanup-only; safe-to-proceed. Target: a follow-up docs-only touch (or fold into the
  next governance doc pass); recorded in `dev-memo/deferred-audit-findings.md`.

## State
- Clears the audit-DUE window for `93976ca..9b70e08` and advances the marker to `9b70e08`. The audited link IPC/API
  surface is committed (unmerged): the A3 link program is now end-to-end (persistence lifecycle → IPC design ADR →
  IPC/API implementation). UI is the next (separate, not-started) lane.

**Verdict: BATCH-PASS C0 H0 M0 L1** (safe; the L1 is a deferred governance-text-consistency cleanup, not a
runtime/security defect).

<!-- batch-audit-attestation v1
range_base: 93976caba85a0c3e78bb88e9798b713204554509
target_sha: 9b70e08dd6c54577cb99ef4b7afa64a14eb78ca0
verdict: BATCH-PASS
findings: C0 H0 M0 L1
broker_job_id: audit-mqv2c2xq-so3ey7
broker_output_sha256: f37dfae9122926ddda1516232e9f2980655f4763d36d9673d3be807437840242
-->
