# Batch-audit study packet — WI-DPE3 (post-merge cumulative window)

**Date**: 2026-06-11. **Range**: `e86747f..f680d2e` (`main`). **Type**: persistence (docket proposal edit).

## What shipped in this range
- **WI-DPE3** (SOURCE, persistence, PR #87 merge `f680d2e`, impl `d15b778`): `editDocketEntry(opts)` on the
  CaseBoxPersistence interface (in-memory + SQLite) + scoped `EditDocketEntryOpts`. Tenant-scoped entry
  resolution BEFORE the matter check (foreign-tenant `entry_id` → `invalid_argument`, no cross-tenant
  `matter_id` disclosure) then `matter_id_mismatch`; strict six-field allow-list apply; server-derived
  `revised_at` (one clock sample reused for `revised_at` + the audit timestamp; caller value ignored);
  `assertValidDocketEntryEdit` (proposed-only → `illegal_transition`; provenance/schema → `invalid_payload`);
  one `DOCKET_ENTRY_REVISED` event (before/after-state hashes, chained); dedicated `updateDocketEntryEditRow`
  (lifted `proposed_kind` + `payload_json`; `confirmation_state` stays `proposed`); NO schema migration.
  Method-allowlist invariant 43→44; `EditDocketEntryOpts` single-sourced in `types.ts`.
- Plus 2 benign prior workflow commits: `3451de0` (the DPE2+FIX1 Layer-B closeout log + study packet) and
  `046fa6b` (the consumed-override audit-log line).

## Review + remediation trail
- review-plan `review-plan-mq9joana-myktjt` NEEDS-FIX → folded → `review-plan-mq9k05oz-xl4ug3`
  READY-with-clarifications. Governed queue sha `0c76327046742a658609a04332fa769f3a0ecaaab6e9fac3f0a56d247e7dfa80`;
  review artifact `queue-review-034.md` (+ mechanical method-allowlist scope addendum).
- per-WI audit `audit-mq9l2hok-oycuoh`: C0 H0 M1 L1 — Medium (cross-tenant `matter_id` disclosure) + Low
  (duplicated opts type) both FIXED (commit `d15b778`); verify `verify-mq9layiw-9ae8ui`: ALL CLOSED.
- Gates: `npm --prefix services/case-box-persistence test` 457/457 (+ sqlite runs 273/288); loc-guardian
  PASS; no schema/`gen:types`.

## Layer-B batch audit (cumulative)
broker audit `audit-mqa55gvw-khgdo3` (Path 1 runner v0.2.18 native --background, gpt-5.5/high/read-only)
over `e86747f..f680d2e`. NOTE: two prior Layer-B attempts (`audit-mq9mgo36-vy6rg4`, `audit-mqa3u9sg-nm4ugs`)
hit `spawnSync codex ETIMEDOUT` (runner ~30-min limit) — infrastructure timeouts, NOT content findings;
the third attempt used a tightened, file-scoped final-attestation prompt and completed. Boundary confirmed
(only `services/case-box-persistence/**` + `dev-memo/**`; no schema.ts/contracts/apps/errors.ts). DPE3
end-state verified; the prior Medium (cross-tenant disclosure) confirmed CLOSED. Governance content-bound
(`queue.governed` == sha256(queue.md) `0c763270`). No secret/gate weakening; no artifact deletion.

**Verdict: BATCH-PASS C0 H0 M0 L0.**

<!-- batch-audit-attestation v1
range_base: e86747fa1de552a52d082dc821aa103bf55dd7fe
target_sha: f680d2e40e97029b2f554d4cc7c6eb8e2a55b2fb
verdict: BATCH-PASS
findings: C0 H0 M0 L0
broker_job_id: audit-mqa55gvw-khgdo3
broker_output_sha256: 48bcd705e9a7609e37cebe11e0b5a11131b9aea492fa6812dbc6a0572989f9f1
-->
