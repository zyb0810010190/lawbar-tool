<!-- batch-audit-attestation v1
range_base: 08fc9bec0ea922211c04878bbe3b218468e2f2cf
target_sha: 5b84395800a68186f5e41f9f201e8db2b017447f
verdict: BATCH-PASS
findings: C0 H0 M0 L0
broker_job_id: audit-mq4q5u18-wudt8u
broker_output_sha256: 96e7c74e0f7e9788d1fe3118380ec0f7820fd70dcbce03acd247298407712e13
-->

# Batch audit 81 — BATCH-CASEBOX-FORBIDDEN-FIELD-TESTS-00 (post WI-FF1, final)

**Window**: `08fc9be..5b84395` (4 commits). **Verdict**: BATCH-PASS (C0 H0 M0 L0).
**Broker**: Path 1 runner v0.2.18 native --background, gpt-5.5/high/read-only, job `audit-mq4q5u18-wudt8u`.
**Date**: 2026-06-08. **Closes the batch — CBW-602 closed.**

## Tasks completed

| Commit | What |
|--------|------|
| `888ecf5` | Layer-B closeout of the BS1 window. |
| `5de7561` | Governed BATCH-CASEBOX-FORBIDDEN-FIELD-TESTS-00 (fresh queue). |
| `fdf14f5` | WI-FF1 — dto-sync coverage for all 12 response allowlists + table-drive createDocket/createFact/confirmDocket/transitionFact forbidden tests; close CBW-602. TEST-ONLY; 525 pass. |
| `5b84395` | merge PR #76. |

## Decisions
- Completed the corrected/narrowed BS2 test-completeness work. The discovery for this batch overturned the earlier BS-sweep's claims: dismissDocket forbidden was already table-driven and every write handler already asserted response-no-leak, so the real gap was just the 4 dto-sync allowlist entries + 4 forbidden table-drive conversions. All 12 canonical `*_RESPONSE_FIELDS` now participate in the authority-exclusion check.

## Audit findings + disposition
- Per-WI: review-plan `review-plan-mq4pnzix-592m1j` PASS; test-only self-review.
- Layer-B `audit-mq4q5u18-wudt8u`: **no findings**; zero src/renderer/electron/services/contracts/package change; loops cover canonical counts (22/6/20/13); all 12 allowlists covered; no assertion weakened (single-sample → stricter table-driven).

## Mistakes / near-misses
- The `npm test` gate first showed 3 Electron-smoke FAILURES (30s launch timeouts) — diagnosed as a stale `better_sqlite3.node` (x86_64) left by the PRIOR batch's `npm run dist` (electron-builder rebuilt x64 last) on an arm64 host. Restored with `electron-builder install-app-deps` (node_modules only, no tracked change). Unrelated to the test-only WI. Lesson: running `npm run dist` for a packaged test mutates the local native-module ABI; re-run `install-app-deps` afterward before the dev-Electron smoke tests.
- The combined govern command (mark-reviewed + govern in one Bash call referencing dev-memo/run/ paths) didn't take effect; re-running them STANDALONE worked (consistent with the long-standing run-control standalone lesson).

## Review-later
- The CaseBox IPC boundary is now fully projected AND fully test-covered (all 12 response allowlists authority-checked; all write handlers' forbidden fields table-driven). CBW-602 closed. Remaining open Lows (CBW-601-BARREL, FACTS-AUD-2, B-series) are unrelated.
- Cadence overhead (governance/closeout commits vs BATCH_AUDIT_EVERY=3) remains a standing watch item across batches 74-81.
