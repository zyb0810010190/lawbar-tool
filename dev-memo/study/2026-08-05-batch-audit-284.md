# Batch audit 284 — matter-details edit Phase B (persistence) + Phase C (IPC) window

**Range:** `a1b55f0..99c5517`. **Branch:** `feature/pta-claimtrack-vertical-slice` (local; no push).

## Window contents (3 commits)
- `c0a3bac` — prior batch-audit closeout (study 283; governance-only — a study packet + closeout-log line, no
  product code).
- `8b591cf` — **matter-details-edit Phase B**: HIGH-RISK persistence write path `updateMatterDetails` (in-memory +
  SQLite), appending exactly one audited `MATTER_DETAILS_UPDATED` event per edit; strict PATCH (6 editable
  free-text fields), no-op reject, sorted `changed_fields`, D4a fail-closed state-hash continuity, archived reject.
  Per-WI audit `audit-msfvqio3-w2m1ww` (no Critical; 1 High + 1 Low) → High FIXED (new-path
  `assertMatterAuditHeadConsistent` head-consistency check), Low deferred as the cross-cutting
  `MDU-B-HEADCHECK-XCUT-1` (archive/ensurePartyIds share the same pre-existing pattern). Verify
  `verify-msfw46wi-xx13rw`: both CLOSED. Persistence suite 288/0; contract 564/0.
- `99c5517` — **matter-details-edit Phase C**: Tier-1 IPC `casebox:matter:updateDetails` + internal-tarball refresh
  (DESKTOP-DEPS-00) + exhaustive-consumer catch-up. Server authority injected (`getActiveActorUserId()`), tenant
  preflight, forbidden/patch restriction (null-prototype own-keys view, 6 editable fields only), response projected
  via `MATTER_RESPONSE_FIELDS`, `mapThrownError`→`SAFE_MESSAGES` (no raw message on the wire). Per-WI audit
  `audit-msfx6cy6-imnb22`: no Critical/High/Medium; 2 test-coverage Lows FIXED in-WI (leak-safety table +=
  `tenant_mismatch`; `renderer-error-message` `CODES` derived from `KNOWN_ERROR_CODES`). Desktop 888/887/1.

## Decision / disposition
Continued the edit-case vertical per Frank (pivot to #2 "edit case info"): Phase A (contract, batch-283) →
Phase B (persistence) → Phase C (IPC), each implemented → per-WI cc-suite audit → verify → committed
fully-green → batch-closed. Phase D (renderer edit screen) remains. The persistence layer is the audited source of
truth for the 6 free-text matter fields; the IPC layer is the server-authority boundary. No contract/persistence
SOURCE changed in Phase C — the tarball refresh only re-packs the already-landed Phase A+B (drift-gate-verified).

## Audit findings + resolution
- **Batch-284 Layer-B** (`audit-msfxq8mz-1nf786`): **BATCH-PASS C0 H0 M0 L1.** Batch-level checks all clean —
  Phase B/C agree on the 6 editable fields + `matter_archived`/`no_editable_change`/`MATTER_DETAILS_UPDATED`; IPC
  injects actor server-side and tenant-gates before persistence (renderer authority rejected/stripped); the SQLite
  path is one `BEGIN IMMEDIATE` transaction (read → continuity → row update → audit append → head update) with
  rollback on any thrown guard; `event_count == COUNT(*) == MAX(sequence)` guarded for the new write path; no
  deleted tests; `git diff --check` clean.
- **L1 (deferred → next WI)** `services/case-box-persistence/src/inMemoryMatter.ts` reason guard: persistence
  accepts a whitespace-only `reason` for `updateMatterDetails`, while the IPC handler already rejects it
  (`dto.reason.trim().length === 0`, `matterHandlers.ts:300`). **Product path is safe today** — the IPC handler is
  the only renderer-facing caller, so no user path can append a blank-reason court-facing event. The gap is
  defense-in-depth for a future direct persistence caller. **Resolution:** the immediately-following WI
  (`WI-matter-details-reason-hardening`) tightens the persistence guard to reject whitespace-only `reason`
  (`typeof … !== "string" || reason.trim().length === 0` → `invalid_payload`) with in-memory ↔ SQLite parity tests
  (`"   "` → `invalid_payload`). Deferral clause: cleanup/defense-in-depth-only, no current behavioral impact
  (§"Resolution rules" item 2); Safe-to-proceed: YES. A `deferred-audit-findings.md` row is opened+closed by that
  WI (cannot be committed pre-closeout without moving HEAD and invalidating this attestation).

## Mistakes / near-misses
- **Self-inflicted-friction control held this window.** After batch-281's stale-i18n-allowlist lesson
  ([[feedback_rerun_gate_after_any_edit]]), every source edit this window re-ran the FULL desktop/persistence gate
  before commit. Phase C's 2 audit Lows were fixed + re-gated (888/887/1) BEFORE commit; no override needed.
- **No new override consumed this window.** The uncommitted `dev-memo/run/log.md` override-consumption lines are
  pre-existing run-control trail from batch-281, untouched by these WIs and left unstaged (phase commits never carry
  run-control).

## Review-later / carried
- `MDU-B-HEADCHECK-XCUT-1` (from Phase B) — cross-cutting: `archiveMatter` + `ensureMatterPartyIds` share the same
  pre-existing "next-sequence from head event_count without a consistency assert" pattern that Phase B hardened only
  on its new path. Its own hardening WI. Still `open`.
- **L1 whitespace-reason** — resolved by the immediately-following reason-hardening WI (see above).
- **Phase D** — renderer edit screen (`#/matters/:id/edit`, seeded form, reason field, archived read-only) needs a
  design artifact (macOS style, original palette preserved).

## Verdict
- **AUDIT-RANGE: a1b55f0a219ea2ef992d7169c4809021dfe34821..99c5517c31796468fb100bd44b8ccc57faf61323**
- **AUDIT-VERDICT: BATCH-PASS C0 H0 M0 L1** — Phase B adds the audited persistence write path fail-closed; Phase C
  adds the server-authority IPC boundary with no wire leak; cross-WI shapes agree; audit-chain append-only +
  atomicity + head-consistency intact; the single Low is deferred defense-in-depth with a safe product path. Job
  `audit-msfxq8mz-1nf786`, rawOutput sha256 `5d671a3462bfec3ff2da705b63ef79857d966eda6ec0845693ff6387e2b3e20d`.

## Gate evidence
- `npm --prefix services/case-box-persistence test` — 288/0 (hardening-matter-details + impl-parity-matter-details +
  invariants incl. the new write path).
- `npm --prefix docs/contracts/case-box-contract test` — 564/0 (unchanged; Phase C touched no contract source).
- `npm --prefix apps/lawbar-desktop test` — 888 tests, 887 pass, 1 fail = only the pre-existing environmental
  `smoke.electron.test.mjs:21` Electron launch timeout (accepted per AGENTS.md).
- DESKTOP-DEPS-00: `check:internal-tarballs` PASS + `check:internal-lock` current.

## Outcome
Phase B (persistence) + Phase C (IPC) of the edit-case vertical are committed + batch-closed at `99c5517`. Next: the
one-finding reason-hardening WI (closes L1), then Phase D (renderer edit screen). Everything local; nothing pushed.
Two unpushed branches (`feature/pta-claimtrack-vertical-slice`, `chore/workflow-pretest-gate`) still await the
user's push/merge decision.

<!-- batch-audit-attestation v1
range_base: a1b55f0a219ea2ef992d7169c4809021dfe34821
target_sha: 99c5517c31796468fb100bd44b8ccc57faf61323
verdict: BATCH-PASS
findings: C0 H0 M0 L1
broker_job_id: audit-msfxq8mz-1nf786
broker_output_sha256: 5d671a3462bfec3ff2da705b63ef79857d966eda6ec0845693ff6387e2b3e20d
-->
