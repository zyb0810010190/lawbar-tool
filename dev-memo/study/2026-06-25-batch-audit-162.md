# Batch-audit study packet — WI-A3-UNLINK-00 merge window

**Date**: 2026-06-25. **Range**: `98abd11..b03e8cd` (`evidence-a3-unlink-schema-design`, branched from `main`).
**Type**: CLOSEOUT + GOVERNANCE + DESIGN + MERGE (the A3-UNLINK-00 unlink/break-link workflow DESIGN lane).

## What shipped in this range
- `b775335` — `chore(workflow): batch-audit closeout c81ce5a..98abd11` (study 161; marker → `98abd11`).
- `779ac00` — `chore(workflow): govern queue — WI-A3-UNLINK-00 …` (queue.* + queue-review-083; design-only,
  not A0.7-gated).
- `33bc448` — `docs(evidence): A3 unlink/break-link workflow — A3-UNLINK-00` — the unlink-workflow ADR +
  the `plan-batch-casebox-evidence-a3-unlink-workflow-00.md` sequencing plan.
- `b03e8cd` — `Merge pull request #132 …` — merge into main.

Confined to `docs/adr/**` + `dev-memo/**`. No product source, no `schema.ts`, no migration, no
`apps/**`/`native/**`/`scripts/workflow/**`/`.claude/**`/`.github/**`, no dependency/package-lock, no
marker/ledger/key/evidence-run files, no `dev-memo/run/evidence/**`. The ADR is DESIGN-ONLY: it defines unlink
as a separate explicit operation (never an implicit cascade) and records the load-bearing finding that a
durable, link-preserving unlink is NOT M0-implementable without a schema decision (V11 `case_box_links.anchor_id`
is NOT NULL, status has no `unlinked`, the resolver recomputes status to `ELSE 'valid'`, and export runs the
resolver first). Anchor + link preserved; export degrades non-clean; the durable-unlink schema mechanism + the
audit-event shape are deferred. No unlink/delete/schema code.

## Audit trail
- `audit-mqu4tg0t-bfm36o` (Path 1 runner v0.2.18, native `--background`, gpt-5.5 / high / read-only) over
  `98abd11..b03e8cd`: **BATCH-PASS C0 H0 M0 L0**. rawOutput sha256
  `c0d99cd6c8e919cbdcd612db7fa09efb2dd40d03c0435210e759bae170eb6c74`.
  - Required recording: kind=audit; scope=`98abd11..b03e8cd` (4 commits: closeout + governance + design +
    merge); runner=`~/.claude/plugins/cache/xiaolai/cc-suite/0.2.18/scripts/codex-runner.mjs`;
    model/effort/sandbox=gpt-5.5/high/read-only; jobId=`audit-mqu4tg0t-bfm36o`; status retrievable=YES;
    first-attempt success (no TIMEOUT).
- (Lane-internal records, already audited: review-plan `review-plan-mqto7gr6-l4mtez` READY
  CONFIRMED-DESIGN-ONLY-NOT-GATED DURABLE-UNLINK-FINDING CONFIRMED-NEEDS-SCHEMA-WI; design audit
  `audit-mqtock0d-hyppdv` PASS C0 H0 M0 L0.)

## Low disposition
- None. C0 H0 M0 L0.

## State
- Clears the audit-DUE window for `98abd11..b03e8cd` and advances the marker to `b03e8cd`. PR #132 (the
  A3-UNLINK-00 unlink/break-link workflow DESIGN) is merged to main. This closeout runs on the new branch
  `evidence-a3-unlink-schema-design`, OPENING WI-A3-UNLINK-SCHEMA-00 — the durable-unlink SCHEMA-MECHANISM DESIGN
  lane (NOT A0.7-gated) that decides HOW a durable, link-preserving explicit unlink is represented (a reason/flag
  column vs an `unlinked` status vs a nullable anchor_id vs a separate lifecycle table) so the resolver/export
  can distinguish explicit unlink from structural `broken`, before any schema implementation.

**Verdict: BATCH-PASS C0 H0 M0 L0** (safe to proceed to the WI-A3-UNLINK-SCHEMA-00 governance).

<!-- batch-audit-attestation v1
range_base: 98abd11695e610583d1b60b836c3daa4881c163a
target_sha: b03e8cd6d03c2e5cc008c6c16da2188f2e1de1df
verdict: BATCH-PASS
findings: C0 H0 M0 L0
broker_job_id: audit-mqu4tg0t-bfm36o
broker_output_sha256: c0d99cd6c8e919cbdcd612db7fa09efb2dd40d03c0435210e759bae170eb6c74
-->
