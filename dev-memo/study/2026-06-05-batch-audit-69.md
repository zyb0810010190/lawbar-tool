# Batch-audit study packet — WI-D4/docket-proposals UI (#66), Layer B — BATCH-CASEBOX-DOCKET-LIFECYCLE-00 COMPLETE

**Date**: 2026-06-07.
**Batch range**: `e2ca889..9b98726` (4 commits since the prior `last-batch-audit` marker `e2ca889`).
**Trigger**: `BATCH_AUDIT_EVERY=3`; the #66 merge brought commits-since-marker to 4 → breaker DUE.
**Audit broker job**: `audit-mq3sya2l-mn45i6` (Path 1 runner v0.2.18, `gpt-5.5`, effort high, sandbox read-only, foreground).
**Verdict**: **BATCH-PASS (C0 / H0 / M0 / L1)** — the L1 is the carried, tracked `DTO-LOC-797` deferral.
**Closeout**: thirty-first production use of `scripts/workflow/batch-closeout.mjs`. FINAL WI of BATCH-CASEBOX-DOCKET-LIFECYCLE-00 → batch fully implemented.
**Note**: range includes the prior WI-D3 closeout doc `5a05eba` (wrote `e2ca889` into the marker), the WI-D4 AMENDMENT-2 governance `d2797f1`, the WI-D4 implementation `eb98a5e`, and the merge `9b98726`.

<!-- batch-audit-attestation v1
range_base: e2ca88993ad22079638b0750a3fbe18d970bf6d3
target_sha: 9b98726c952fe0d88cbb321be6d5decd6b5068dd
verdict: BATCH-PASS
findings: C0 H0 M0 L1
broker_job_id: audit-mq3sya2l-mn45i6
broker_kind: audit
broker_output_sha256: 10d0de77eb50607e4ac1580cb0048d8a593dfe0d85490f1fb75391de9d0ffb2b
-->

## Tasks completed in the batch range

| Commit | PR | Behavior |
|---|---|---|
| `5a05eba` | (workflow) | DOC-ONLY: prior WI-D3 Layer-B closeout study packet (`2026-06-05-batch-audit-68.md`, range `08f0a7f..e2ca889`) + log line. Advanced marker to `e2ca889`. |
| `d2797f1` | (workflow) | GOVERNANCE AMENDMENT-2 (user-approved): added `tests/_view-matter-dom.mjs` to WI-D4 Allowed-files (collateral-harness correction — the shared `makeStubApi` needed `listDocketEntries`/`dismissDocketEntry` once the disclosure loads proposals). Re-reviewed PASS (`review-plan-mq3sgpjq-a5bsqk`). |
| `eb98a5e` | #66 | WI-D4: pending docket-proposals UI + dismiss. New `viewMatterDocketProposals.ts` (proposals group, two-step reason-capture dismiss, Show-more pagination with re-entrancy guard), wired into `viewMatterDeadlines.ts`; preload+api+types bridge; +2 dto-sync PAIRS; +11 UI tests; shared-stub +2 methods. Suite 475→487. Renderer + preload only. |
| `9b98726` | #66 | GitHub PR merge of WI-D4 to main. |

## Decisions (alternatives, why chosen, what would falsify)

1. **Extract `viewMatterDocketProposals.ts`** rather than grow `viewMatterDeadlines.ts` — keeps the deadlines screen at 539 LOC (under the 800 cap). Falsifier: `viewMatterDeadlines.ts` ≥ 800 (it is not).
2. **Two boundary-correction amendments, both stop-and-report + user-approved** — AMENDMENT-1 fixed the wrong preload path (`renderer/preload.mts` → `electron/preload.mts`; narrowed `electron/**` to `electron/ipc/**`+`main.ts`); AMENDMENT-2 added the shared test harness `_view-matter-dom.mjs` to Allowed. Both surfaced via the WI-D3 design review + the WI-D4 test run; neither broadened product scope. The agent halted for approval rather than broaden file permissions autonomously (AGENTS.md self-improvement bound).
3. **Show-more re-entrancy guard** (audit Medium) — a `pageLoading` flag dropped a concurrent click so a double-click cannot append the same page twice; regression test added. Falsifier: two page-2 requests from a double-click (test proves one).

## Audit findings + disposition

- **C0 / H0 / M0 / L1.** Scope clean (renderer + `electron/preload.mts` + tests + governance docs only; no `src/**`/`electron/ipc/**`/`main.ts`/`services/**`/`docs/contracts/**`/`dto.ts`). Authority clean (dismiss forwards exactly `{matterId,entryId,dismissal_reason}`; allowlist stripping; projected rows carry no authority fields; dto-sync PAIRS hold). Pagination clean (first page + Show-more + re-entrancy guard + repeated-cursor guard; no silent truncation/unbounded fetch). Harness fix setup-only; governance content-bound; prior closeout docs-only.
- **L1 = `DTO-LOC-797`** (carried; `dto.ts` 797/800, unchanged in range) — still open; split before the next DTO-adding WI.
- Per-WI: audit `audit-mq3smfcg-1obisy` (C0 H0 M1 L0; Medium re-entrancy fixed in-WI) → verify `verify-mq3ssjil-p426bq` (ALL CLOSED, incl. `tsc --noEmit`).

## Batch state after this closeout — BATCH-CASEBOX-DOCKET-LIFECYCLE-00 COMPLETE

Advances `dev-memo/run/last-batch-audit` from `e2ca889` to `9b98726`, clearing the breaker. **All four WIs merged:** WI-D1 (`casebox:docket:list`, #63), WI-D2 (`casebox:docket:dismiss`, #64), WI-D3 (design artifact, #65), WI-D4 (pending-proposals UI + dismiss, #66). The orphaned-proposal gap is closed end-to-end: a lawyer can now see durable pending docket proposals after reload and dismiss stale ones, over tenant-scoped fail-closed IPC with server-injected authority and renderer-safe projection. **Out of scope (unchanged):** deadline status transitions, edit-in-place (needs ADR), reminders/scheduling. **Open deferred:** `DTO-LOC-797` (Low — split `src/caseBox/dto.ts` per-entity before the next DTO-adding WI). Ledger otherwise 0 High / 0 Medium.
