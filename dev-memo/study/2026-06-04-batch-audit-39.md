# Batch-audit study packet — deadline-urgency (#39) + prior closeout, Layer B

**Date**: 2026-06-04.
**Batch range**: `866fd7b..1ab1888` (3 commits since the prior `last-batch-audit` marker `866fd7b`).
**Trigger**: `BATCH_AUDIT_EVERY=3`; 3 commits ≥ 3 → `batch-commit-guard.sh` breaker fired DUE.
**Audit broker job**: `audit-mpzgii4s-0krj7q` (Path 1 runner, `gpt-5.5`, effort high, sandbox read-only, native `--background`).
**Verdict**: **BATCH-PASS (C0 / H0 / M0 / L0)**.
**Closeout**: second production use of `scripts/workflow/batch-closeout.mjs`.

<!-- batch-audit-attestation v1
range_base: 866fd7b1f71e4cdb25782de52d8a1169311e1aeb
target_sha: 1ab188822101096fb73eaae99e800169ca624d92
verdict: BATCH-PASS
findings: C0 H0 M0 L0
broker_job_id: audit-mpzgii4s-0krj7q
broker_kind: audit
broker_output_sha256: c971825b2d5810b45b1b61def253d907c6dd51b9688adbc45e12e6c72dfbbe27
-->

## Tasks completed in the batch

| Commit | PR | Behavior |
|---|---|---|
| `46f4d45` | (workflow) | DOC-ONLY: the first batch-closeout's study-packet attestation (`dev-memo/study/2026-06-04-batch-audit-37-38.md`) + a `dev-memo/batch-closeout-log.md` line. Produced by `scripts/workflow/batch-closeout.mjs`. No code. |
| `109c390` | #39 | Deadline-urgency UI WI: `classifyDeadlineUrgency` + per-row pills + summary banner (`renderer/format.ts`, `renderer/screens/viewMatterDeadlines.ts`, `renderer/index.css`); shared DOM test harness extracted to `tests/_view-matter-dom.mjs`; eager-load with a `seenCursors` guard; `nowMs` resolved at load time. |
| `1ab1888` | #39 | GitHub PR merge to main. |

## Decisions (alternatives, why chosen, what would falsify)

1. **Test-harness extracted to a single shared source** (`tests/_view-matter-dom.mjs`) rather than duplicated, to satisfy loc-guardian (renderer-view-matter.test.mjs had crossed 1200) without weakening assertions. Falsifier: a duplicate harness definition reappearing inline, or a lost assertion (suite count would drop below 368).
2. **Eager-load is bounded by a `seenCursors` guard** (audit-mpz6d70x H1 fix): a non-advancing cursor aborts with an inline alert instead of spinning. Falsifier: a repeated cursor causing unbounded IPC.
3. **`nowMs` resolved at load (click) time**, not render, so a lazily-opened disclosure classifies against fresh time (audit M fix).

## Audit findings + disposition

- **C0 / H0 / M0 / L0.** No accumulation drift. Renderer/test/design-doc scope only — no services, contracts, IPC, persistence, or new runtime deps. Harness extraction is single-source (no duplication). Token compliance + a11y roles (`role="status"` banner, `role="alert"` errors) clean at the accumulation level.

## Per-WI provenance (already independently verified)

- #39 deadline-urgency: audit `audit-mpz6d70x-cjx1tm` → remediated → verify `verify-mpz6oejw-bxzgzc` (ALL CLOSED).
- `46f4d45` is the doc-only product of the prior closeout (`audit-mpz7356b-m2gxa7` BATCH-PASS).
- This Layer-B pass checks the ACCUMULATION, not the per-WI line findings.
