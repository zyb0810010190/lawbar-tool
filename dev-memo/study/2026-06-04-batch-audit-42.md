# Batch-audit study packet — BRCBW-IND (#42) + prior closeout, Layer B

**Date**: 2026-06-04.
**Batch range**: `d36bf00..b60a061` (3 commits since the prior `last-batch-audit` marker `d36bf00`).
**Trigger**: `BATCH_AUDIT_EVERY=3`; 3 commits ≥ 3 → `batch-commit-guard.sh` breaker fired DUE.
**Audit broker job**: `audit-mpzmw82j-ji5rrr` (Path 1 runner, `gpt-5.5`, effort high, sandbox read-only, native `--background`).
**Verdict**: **BATCH-PASS (C0 / H0 / M0 / L0)**.
**Closeout**: fourth production use of `scripts/workflow/batch-closeout.mjs`; runs on the PR #42 branch (rides #42).

<!-- batch-audit-attestation v1
range_base: d36bf00a1940bea11b511978c0f0576f8acfc3a9
target_sha: b60a06120ee620f74fcb2a1d1ec9d5a237e6a134
verdict: BATCH-PASS
findings: C0 H0 M0 L0
broker_job_id: audit-mpzmw82j-ji5rrr
broker_kind: audit
broker_output_sha256: 8a6c5a0802625077442c6d227219c26e98118ab4f34ea58abafcf5d8c937e5cf
-->

## Tasks completed in the batch

| Commit | PR | Behavior |
|---|---|---|
| `520dac9` | (workflow) | DOC-ONLY: the prior batch-closeout's study-packet attestation + a `dev-memo/batch-closeout-log.md` line. No code. |
| `ca6ead0` | #41 | GitHub PR merge (the prior closeout doc) to main. |
| `b60a061` | #42 | BRCBW-1/2/10: bounded cd/literal-var indirection resolver + lexical de-obfuscation in `block-run-control-bash-write.sh` (closes the lexical bypass subsets; computed = Mechanism-C residual). Tests 143→179. Sibling hooks unmodified. |

## Decisions (alternatives, why chosen, what would falsify)

1. **Bounded lexical resolver (Mechanism B) + de-obfuscation (Mechanism 0)**, additive over the existing scans — NOT the rejected Mechanism-C blanket resolution. Falsifier: a lexical cd/var/obfuscation bypass that still allows an authority write within scope.
2. **BRCBW-2 closed only for the literal-var subset; BRCBW-10 only for lexical shell-word obfuscation** — the computed/`$IFS`/`$()`-spelled subsets stay documented Mechanism-C non-goals.
3. **Narrow context-gated bare-basename fallback** (only under a tracked run-control cd) preserves BRCBW-5 read-source/prose ALLOWs.

## Audit findings + disposition

- **C0 / H0 / M0 / L0.** No accumulation drift. `deobf`/`scan_indirection` compose additively with `scan_redir`/`scan_verbs`/`scan_interp`/pass-4; no sibling-hook changes (`protect-run-control.sh`, `batch-commit-guard.sh`, `logmd-append-guard.mjs`, `runcontrol-canon.mjs` untouched in this range); no product/UI/manifest/dependency drift.

## Per-WI provenance (already independently verified)

- #42 BRCBW-IND: review-plan `review-plan-mpzkrvt2-jl9yi1` (READY-WITH-LOW) → audit `audit-mpzlb7b3-c1ckcq` → remediated → verify `verify-mpzlp3om-kudqoa` (ALL CLOSED).
- `520dac9` is the doc-only product of the prior closeout (`audit-mpzgii4s-0krj7q` BATCH-PASS).
- This Layer-B pass checks the ACCUMULATION, not the per-WI line findings.
