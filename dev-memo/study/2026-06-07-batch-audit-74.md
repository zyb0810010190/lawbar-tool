<!-- batch-audit-attestation v1
range_base: 067455bf8cb1eacd550d9bd00edda9512fc77037
target_sha: 1fb286dd272d9a5206b88e32f1a52cae1cffd499
verdict: BATCH-PASS
findings: C0 H0 M0 L1
broker_job_id: audit-mq3zy4rg-bfrhrx
broker_output_sha256: c29f7c8124def6acbb04b62c571968028b5d97a84f4f580ffa05fe19e7d7ebd4
-->

# Batch audit 74 — BATCH-CASEBOX-DEADLINE-TRANSITION-00 (post WI-DT3, final)

**Window**: `067455b..1fb286d` (3 commits). **Verdict**: BATCH-PASS (C0 H0 M0 **L1**).
**Broker**: Path 1 runner v0.2.18, gpt-5.5/high/read-only, job `audit-mq3zy4rg-bfrhrx`.
**Date**: 2026-06-07. **This closes the batch.**

## Tasks completed

| WI | Commit | Files | Behavior |
|----|--------|-------|----------|
| (closeout) | `4d97450` | study-73 + log | Layer-B closeout of the DT2 window. |
| WI-DT3 | `eed5166` | 6 renderer/test files | Renderer bridge + deadline-transition UI per the WI-DT2 design. 8 new renderer tests; 511 pass. |
| merge | `1fb286d` | — | PR #71 merged (design gate passed via the cited artifact). |

## Decisions
- The renderer bridge (types/api) + dto-sync PAIR + `TRANSITION_DEADLINE_RESPONSE_FIELDS` authority-exclusion entry landed in DT3, closing the DT1 temporary dto-sync handoff (DT1 shipped the canonical src DTO without a renderer counterpart; the PAIRS-driven sync test ignored it until now).
- UI matches the design exactly: pending single-click edges; missed→met two-step required reason; terminal rows have no controls; errors keep the row + no refresh; success refreshes once; busy guard; cancel discards the typed reason; post-success refresh failure is non-fatal (not a transition error).

## Audit findings + disposition
- Per-WI audit `audit-mq3zqdrn-5vtx8j`: 0C/0H + 1 Medium + 1 Low — **both fixed in-WI** (clear reason on cancel/collapse; separate post-success refresh failure from transition error) + regression tests. Verify `verify-mq3zv0y7-9alrfc`: ALL CLOSED.
- Layer-B batch audit `audit-mq3zy4rg-bfrhrx`: BATCH-PASS, **1 Low** — `viewMatterDeadlines.ts` header comment still reads "NO edit / deadline-status transition", stale after WI-DT3. Doc/comment-only, non-behavioral. **Deferred** (DT-BATCH74-L1) to `dev-memo/deferred-audit-findings.md`; the governed DT1/DT2/DT3 queue is complete (no DT3b to invent), so the comment refresh folds into the next `viewMatterDeadlines.ts`-touching WI. Safe to proceed.

## Mistakes / near-misses
- DT3 audit caught a stale-reason-on-cancel Medium (collapse didn't clear the input) — fixed in-WI before merge.
- Recurring breaker cadence: governance + each WI merge + closeout commits all count toward BATCH_AUDIT_EVERY=3, so this 3-WI batch required four Layer-B closeouts (71–74). Working as designed; noted for cost awareness.

## Review-later
- DT-BATCH72-L1 (forbidden-field test table) + DT-BATCH74-L1 (stale header comment) — both open Lows targeting the next IPC / viewMatterDeadlines WI.
- Deadline-transition feature is now end-to-end (persistence → IPC → bridge → UI). `met`/`withdrawn` are terminal in v1 (no reopen); reminders/scheduling/docket-edit remain deferred (separate ADR-gated batches).
