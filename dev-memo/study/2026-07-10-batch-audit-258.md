# Batch audit — window 8e4977c..4a50dff (PR #258 OCR fetcher SSRF hardening)

**Verdict**: BATCH-PASS C0 H0 M0 L1. **Broker job**: `audit-mreurria-qdciyz` (Path 1 runner `cc-suite/0.2.18`, gpt-5.5/high/read-only; inlined-diff audit).

## Window

`8e4977c0392f922b882f391abe4b8f3b5604d19e..4a50dff785965a353fcd2e857249796601246a31`:
- `57e24ef` chore(workflow): batch-audit closeout 2c839ee..8e4977c.
- `aabe559` fix(ocr): harden fetcher SSRF blocklist + fail-closed isPrivateIp (WI-23).
- `4a50dff` merge PR #258.

## Checks (Layer B)

- **Net delta**: `privateIp.ts` (fail-closed on empty/non-string + 6 IANA non-global IPv6 ranges added) +
  `fetcher.privateIp.test.mjs` (direct regression) + `package.json` test-list + docs + run-control records. No
  schema / UI / data-model change.
- **Security**: SSRF core hardened (fail-closed uniformity + IPv6 blocklist completion); no public-address
  over-block that breaks fetches (boundary cases tested); existing SSRF/TLS/redirect gates unweakened.
- **Live proof**: the `ocr-chain` job (incl. the new SSRF test) passed on ubuntu in 1m14s.

## Findings

One **Low** (described): `dev-memo/plan-ocr-fetcher-security-23.md` states the `fetcher.privateIp.test.mjs` count as
"3/3", but the file grew to **4** `node:test` cases when the non-string regression was added (audit L2 of the WI-23
per-WI audit). Immaterial (a stale doc count). **Disposition:** corrected in the immediately-following
`WI-OCR-FAILURE-MODE-DETERMINISM-24` (which edits the same OCR-docs family) — recorded here rather than in the
deferred backlog since the fix lands in the next commit.

<!-- batch-audit-attestation v1
range_base: 8e4977c0392f922b882f391abe4b8f3b5604d19e
target_sha: 4a50dff785965a353fcd2e857249796601246a31
verdict: BATCH-PASS
findings: C0 H0 M0 L1
broker_job_id: audit-mreurria-qdciyz
broker_output_sha256: 300259908ab50f7f7f96962342b10f259f4c3ba87686896dce825bf4c5398696
-->
