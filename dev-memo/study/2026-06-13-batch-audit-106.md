# Batch-audit study packet — WI-i18n-1 i18n infrastructure (+ M1/L1 remediation)

**Date**: 2026-06-13. **Range**: `3126b24..7988dfc` (branch `product-i18n-infra`).
**Type**: UI infra (renderer i18n, UNWIRED) + a BATCH-FAIL remediation.

## What shipped in this range
- **WI-i18n-1** (commit `e0fdbea`): renderer i18n **infrastructure only**, UNWIRED — `renderer/i18n/`
  catalog.ts (zh-CN), t.ts (named interpolation; always throws on missing key/param), labels.ts (typed
  exhaustive enum-label facade; eventKindLabel type-only contract import), an exact-occurrence anti-drift
  guard (ui-strings-allowlist.json seeded to current literals; scanner tests/_i18n-ui-scan.mjs) + tests;
  package.json registration. No screen consumes it; no visible UI copy change; format.ts/listMatters.ts
  unchanged.
- **M1/L1 remediation** (commit `7988dfc`): the first Layer-B audit of `3126b24..e0fdbea`
  (`audit-mqcmq2lo-w0aniq`) returned **BATCH-FAIL C0 H0 M1 L1**:
  - *M1* — the scanner missed `node.textContent = "literal"` visible copy (e.g. renderer/index.ts
    not-found branch). Fixed: scanner now flags a "textContent" kind; allowlist regenerated to seed the
    existing index.ts cases.
  - *L1* — tests/_i18n-ui-scan.mjs + tests/renderer-i18n-guard.test.mjs carried literal CJK range chars
    **and NUL bytes** (the dedupe-key delimiters), so git treated them as binary. Fixed: CJK ranges use
    `一-鿿` escapes, NUL removed, dedupe keys use `JSON.stringify` — both files are now ASCII text.
  Remediation landed via a single-use `human.override` (batch was audit-DUE; logged in run/log.md
  2026-06-13T17:40:00Z).
- Plus carried doc/governance: the PR #93 planning-merge + its closeout (already audited).

## Layer-B batch audit (post-remediation)
Broker audit `audit-mqcn6bzw-oto35d` (Path 1 runner v0.2.18 native `--background`, gpt-5.5 / medium /
read-only) over `3126b24..7988dfc`. Verdict: **BATCH-PASS C0 H0 M0 L0**. Auditor confirmed: M1 fixed
(textContent now flagged), L1 fixed (.mjs are ASCII text, no NULs, reviewable), i18n unwired, no visible
copy change, format.ts/listMatters unchanged, eventKindLabel type-only import, t() throws on missing
key/param, exact-occurrence allowlist, no new dependency / contract change. Full suite 608/608.

Reliability note: a first remediation re-audit attempt (`audit-mqcn5n70-en8utk`) used a prompt whose
backticks were shell-interpreted (unquoted heredoc) and was discarded; re-run clean as
`audit-mqcn6bzw-oto35d`.

## Decisions
- WI-i18n-1 keeps the facade UNWIRED and old helpers untouched so NO screen renders differently; screens
  migrate to the facade (accepting zh-CN copy) in later per-area WIs that burn the guard allowlist down.

**Verdict: BATCH-PASS C0 H0 M0 L0.**

<!-- batch-audit-attestation v1
range_base: 3126b24799ff4ca591e9993d19ed0c490bc73f89
target_sha: 7988dfc381d270224aa429afd9441da2878ace1d
verdict: BATCH-PASS
findings: C0 H0 M0 L0
broker_job_id: audit-mqcn6bzw-oto35d
broker_output_sha256: 7542f7b338b30e58032ad9aa21999b06a3bc152f240eed6fe3930f63b3da45ce
-->
