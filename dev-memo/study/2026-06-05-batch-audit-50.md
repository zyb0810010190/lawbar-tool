# Batch-audit study packet — WI-501/F4.3 (#50) + prior closeout, Layer B (BATCH-PRODUCT-PROBE-00 final)

**Date**: 2026-06-05.
**Batch range**: `f444325..bf8df09` (3 commits since the prior `last-batch-audit` marker `f444325`).
**Trigger**: `BATCH_AUDIT_EVERY=3`; 3 commits ≥ 3 → `batch-commit-guard.sh` breaker fired DUE on post-merge main.
**Audit broker job**: `audit-mq0l2wsh-9ht7f0` (Path 1 runner v0.2.18, `gpt-5.5`, effort high, sandbox read-only, native `--background`).
**Verdict**: **BATCH-PASS (C0 / H0 / M0 / L0)**.
**Closeout**: twelfth production use of `scripts/workflow/batch-closeout.mjs`; runs on post-merge `main`. **Completes governed batch BATCH-PRODUCT-PROBE-00.**

<!-- batch-audit-attestation v1
range_base: f4443251a86be2a36e948cfb96dfc376d8e62498
target_sha: bf8df092abf847349d6f736ed90119117aa364f4
verdict: BATCH-PASS
findings: C0 H0 M0 L0
broker_job_id: audit-mq0l2wsh-9ht7f0
broker_kind: audit
broker_output_sha256: 673967ca0a6d2d0469760714145a8f5b9a698f0635a4a193dcfa96c959239941
-->

## Tasks completed in the batch

| Commit | PR | Behavior |
|---|---|---|
| `7fd22d8` | (workflow) | DOC-ONLY: the prior batch-closeout's study packet (`cb7486d..f444325`, #49) + a `dev-memo/batch-closeout-log.md` line. |
| `0c40705` | #50 | WI-501 / F4.3: DOC-ONLY single ledger-row edit closing F4.3 as stale — the never-classified classification distinction is already pinned by existing conformance cases `6.A2.13` + `6.A2.16`. No conformance harness change, no SQLite filtered-harness change, no product/runtime code, no new test. |
| `bf8df09` | #50 | GitHub PR merge of WI-501 to main. |

## Decisions (alternatives, why chosen, what would falsify)

1. **Option B — close F4.3 as stale, do NOT add a duplicate atomic case** (user-directed after discovery). The F4.3 row claimed the distinction was unpinned, but `6.A2.13` (effective → unclassified, history 0) and `6.A2.16` (list → [], next_cursor null) already pin both halves against a `seedMatterDoc` never-classified document. Per the AGENTS.md source hierarchy (actual files beat the doc), the ledger was stale. Falsifier: either cited case failing or not covering the never-classified state — verified passing across all three impls (InMemory, Sqlite-B11, Sqlite-Final).
2. **No SQLite-filter surgery** — deliberately did NOT add `6.A2.25` / touch `B11_PATTERN` / `B11_EXPECTED_CASE_IDS`; a near-duplicate test plus error-prone filter edits were not worth it for an already-covered behavior.
3. **Self-review (docs-only)** — single stale-ledger-row correction, no behavior/contract change, grounded in the actual passing tests; the broker chain is for high-risk WIs.

## Audit findings + disposition

- **C0 / H0 / M0 / L0.** No accumulation drift. Net diff in range confined to three `dev-memo` files (the F4.3 row flip, and the prior closeout's study packet + log). NO conformance harness / SQLite filtered harness / product / runtime / test / UI / manifest / hook / security-boundary changes. The F4.3 stale-closure is supported by the existing `6.A2.13` + `6.A2.16` (both `seedMatterDoc`, no classification added). No marker/breaker source tampering; no test deletion/weakening.

## Batch-level closure — BATCH-PRODUCT-PROBE-00 (both WIs)

| WI | Finding | Severity | Resolution commit | PR | Closeout |
|---|---|---|---|---|---|
| WI-401 | AT1-L1 (fdesetup probe fail-closed anchor) | Low (product correctness) | `a863e01` | #49 | `7fd22d8` |
| WI-501 | F4.3 (classification distinction — stale ledger, already pinned) | Low (product/test) | `0c40705` | #50 | this packet |

Both ledger rows AT1-L1 and F4.3 are CLOSED on main. AT1-L2 remains open (deferred; Electron-infra-heavy).

## Per-WI provenance (already independently verified)

- #50 WI-501/F4.3: self-review (docs-only, grounded in `6.A2.13`/`6.A2.16` passing across 3 impls). Discovery: F4.3 was stale, not an unpinned gap.
- `7fd22d8` is the doc-only product of the prior closeout (`audit-mq0klouf-br8zcj` BATCH-PASS for `cb7486d..f444325`).
- This Layer-B pass checks the ACCUMULATION, not the per-WI line findings.
