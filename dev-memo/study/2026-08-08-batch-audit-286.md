# Batch audit 286 — UI button-family adoption + contract-clause repair window

**Range:** `307abab..c743e26`. **Branch:** `feature/pta-claimtrack-vertical-slice` (local; no push).

## Window contents (3 commits)

- `07e2e3a` — prior batch-audit closeout (study 285; governance-only — study packet + closeout-log line).
- `c9e3187` — **WI-UI-1: matter sub-section buttons adopt the shared `.button` family**. 27 bespoke
  buttons across the 7 matter sub-section screens (documents / facts / claim-tracks / deadlines /
  docket-proposals / links / T3-catalog) prepend `button button--<weight>` while keeping the bespoke
  `view-*` class as a suffix. Weights: danger = unlink/dismiss/reject + their confirms; primary =
  add/create/save/confirm-a-benign-step; secondary = export / inline-edit / relink / cancel / status
  toggles / pagination. Every `data-test-id`, label, and handler byte-identical; diff is +27/-27 on
  `class:` lines only, so no line shifts and no i18n-allowlist regeneration was required.
  Design artifact: `dev-memo/design/2026-08-05-ui-batch-redesign-spec.md` (APPROVED).
  **Scope correction recorded in that artifact §0:** the draft's premise ("all screens abandoned the
  button system") was verified FALSE — the `.button` family was already complete and the core-flow
  screens already used it. Only the 7 sub-section screens were unstyled. UI-2..4 must be re-verified
  against real code the same way. Desktop gate 906/907 (sole failure the accepted environmental
  `smoke.electron.test.mjs:21` launch timeout).
- `c743e26` — **WI-CONTRACT-FIX: three unenforceable contract clauses repaired + cc-suite bridge
  re-pointed**. Found by a `/nlpm:score` pass over 39 NL artifacts (overall 89/100, 1 file below the
  70 threshold). Each finding re-verified against the real files before editing.
  - `.claude/skills/cc-suite` re-pointed 0.2.10 → 1.5.2; Path 1 proven live end-to-end
    (`audit-mskg0nmy-sj6ir5`, `status:"completed"`). The historic `--approval-policy`/`--quiet` flag
    mismatch is fixed upstream at cc-suite 1.5.2 + codex-cli 0.137.0 — the runner must NOT be patched.
  - **Defect 1** — `rules/echo-sleuth.md` mandated `/echo-sleuth:*` commands from an uninstalled
    plugin, making every WI a silent contract violation. Added an "Enablement gate": triggers are
    DORMANT while the plugin is unavailable, obligation redirected to `git log` / `dev-memo/` / rule
    files, never dropped. Mirrored in `AGENTS.md` and `evidence-genie.md` invariant 12.
  - **Defect 2** — `rules/loc-guardian.md` built its whole gate on `/loc-guardian:scan` from an
    uninstalled plugin, while declaring an erroring scan "failed-closed"; read literally, autopilot
    could never legally start. Added "Scan mechanism": plugin preferred, deterministic `find|wc -l`
    fallback at the SAME thresholds. Raw ≥ pure LOC so the fallback errs safe; plugin absence is
    explicitly NOT a passing scan.
  - **Defect 3** — bare `/review-plan` `/audit` `/audit-fix` `/verify` in `security-boundary.md` and
    `AGENTS.md` resolve to nothing (only `/cc-suite:*` exists). Namespaced. A 4th instance in
    `AGENTS.md` §"Queue governance" was caught by an independent sweep, not by the scorers.

## Provenance and supersession (echo-sleuth recall, retroactive)

- Defect 3 **supersedes** the bare-name list reintroduced by `27ffa0a` (2026-05-31) and **restores**
  the project-wide namespacing decision of `107db33` (2026-05-20); `security-boundary.md`'s bare
  names came from `b803c60` (2026-05-20), predating `107db33`, so this completes a missed migration.
- `evidence-genie.md` invariant 12 was authored by `89df1b9` (WI-EVW8) to close `EVW-PORT-L1` by
  moving the §C pre-flight SHOULD → REQUIRED with an anti-downgrade clause. This window conditions
  the **mechanism** on plugin availability and leaves the modal verb REQUIRED unchanged — a
  deliberate narrowing, recorded here so it is not misread as the regression that clause forbids.
- Prior art for the dormancy shape already existed in tracked policy: `BATCH-AUDIT.md` §"Study
  packet" carves out `/echo-sleuth:extract` as un-runnable in an unattended batch and defers rather
  than drops the obligation. Cited in the rule.
- Defect 2's fallback **retro-documents existing de-facto practice** — the WI-VS-3 gate reports
  (2026-08-04) already used `wc -l` with these thresholds and the same "raw is an upper bound on
  pure" reasoning. No prior decision anywhere in the corpus rejected a non-plugin fallback.
- **Disclosure:** the echo-sleuth §C pre-flight was bypassed twice in this session — first because
  the plugin was absent (the very defect being fixed), then because it was installed mid-session
  after edits were already underway. The recall was run AFTER the edits as a retroactive discharge,
  and its findings are incorporated above.
- **Durable lesson:** an earlier revision embedded "the plugin is not installed at present" into
  three always-loaded contract docs; installing the plugins ~80 minutes later made those lines false
  in documents that load every turn. Replaced with the availability CHECK rather than the answer.
  Never embed volatile environment state in an always-loaded contract doc.

## Broker audit result

`audit-mskgn1w4-dfvp3p` (cc-suite Path 1, foreground, gpt-5.5/high/read-only; 52 KB unified diff
inlined per the standing "renderer audits time out on the tree-walk" lesson).

Verdict **BATCH-PASS C0 H0 M0 L1**. Codex judged the load-bearing question directly: the
plugin-conditional changes "do not quietly weaken enforcement — echo-sleuth is correctly treated as
an optional continuity mechanism with substitute repo evidence, while loc-guardian keeps the gate
mandatory via deterministic fallback." No audit-chain weakening, no contract-doc corruption pattern,
no hardcoded colors, all `data-test-id` values preserved, no new dependencies, no public-API change.

**L1 — `BATCH286-LOCFALLBACK-VERDICT-1`** (deferred): the deterministic LOC fallback lists the top-20
raw counts but does not itself emit the `VERDICT: N over limit, M warnings` line the gate semantics
reference, nor classify source-vs-test thresholds mechanically. Not a gate weakening (the surrounding
text requires pure-LOC confirmation and applies the semantics unchanged), but a future agent could
record an informal "clean" result without a deterministic verdict format. Dispositioned in
`dev-memo/deferred-audit-findings.md`.

<!-- batch-audit-attestation v1
range_base: 307abab3abb74f6ca83394a09c8acfa47c71f86f
target_sha: c743e26d3ee26b75b7a1e73ca551c4b5994c8e51
verdict: BATCH-PASS
findings: C0 H0 M0 L1
broker_job_id: audit-mskgn1w4-dfvp3p
broker_output_sha256: 5b8ebd39c9e64b0fd8cc4173f7c5d25a7e2b255d81a2fa924373b3e9f97286b8
-->
