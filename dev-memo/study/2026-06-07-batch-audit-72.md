<!-- batch-audit-attestation v1
range_base: ed8e58847819b9b1798a1df2e9113dfbbc8303de
target_sha: 8d4fb671438aa3a03de251547793bc6d4374d57d
verdict: BATCH-PASS
findings: C0 H0 M0 L1
broker_job_id: audit-mq3z23e7-gcb4qv
broker_output_sha256: d8686a4f18294057fccf6d8531039b2a55b997b3737453ffb44984ea4465c453
-->

# Batch audit 72 — BATCH-CASEBOX-DEADLINE-TRANSITION-00 (post WI-DT1)

**Window**: `ed8e588..8d4fb67` (4 commits). **Verdict**: BATCH-PASS (C0 H0 M0 **L1**).
**Broker**: Path 1 runner v0.2.18, gpt-5.5/high/read-only, job `audit-mq3z23e7-gcb4qv`.
**Date**: 2026-06-07.

## Tasks completed

| WI | Commit | Files | Behavior |
|----|--------|-------|----------|
| (govern) | `9a33d13` | `dev-memo/run/*` + `reviews/queue-review-019.md` | Governed the 3-WI deadline-transition queue (lint+review, content-bound). review-plan attempt-1 NEEDS-FIX (pre-governance state + stale memo) → attempt-2 `review-plan-mq3yen76-dat44k` PASS. |
| WI-DT1 | `bcfb8bd` | 7 desktop files | `casebox:deadline:transition` IPC handler + preload. Mirrors WI-802 fact transition. 14 new IPC tests; 501 pass. |
| merge | `8d4fb67` | — | PR #69 merged. |

(`f7b8acb`, the prior batch's own closeout, also falls in this window — expected housekeeping.)

## Decisions
- **No contract/persistence/ADR/dependency.** The deadline-transition request DTO lives at the IPC layer (mirroring fact-transition WI-802), which shipped with no contract op. Persistence `transitionDeadline` + audit kinds already existed. *Falsifier:* if a reviewer required a contract transition schema for facts, the same would apply to deadlines — neither has one.
- **DT1 = backend only (no renderer path).** Renderer bridge moved to DT3 so DT1 has no design-gate trigger; safe because preload imports the DTO from `../src/caseBox/dto.js` and `renderer-dto-sync` is PAIRS-driven.
- **Edge-bound reason rule.** `transition_reason` required+nonempty IFF existing status `missed` AND `to` `met`, forbidden otherwise — derived from the scoped preflight, with persistence still enforcing illegal edges.

## Audit findings + disposition
- Per-WI audit `audit-mq3ytx78-wnxs1j`: 0C/0H/0M + 3 Low — **all fixed in-WI** (real `CaseBoxPersistenceError` in the illegal_transition test; pending→met forbidden-reason coverage; preload-scope comment). Verify `verify-mq3yyjye-xnqrr2`: ALL CLOSED.
- Layer-B batch audit `audit-mq3z23e7-gcb4qv`: BATCH-PASS, **1 Low** — the `transitionDeadline` forbidden-field test exercises `actor_user_id` + `status` but not a table over *every* `TRANSITION_DEADLINE_FORBIDDEN_FIELDS` member (e.g. `at`). The handler iterates the full array (uniform guard), so this is a **test-coverage completeness gap, not a runtime bug**. **Deferred** to `dev-memo/deferred-audit-findings.md` (DT-BATCH72-L1); safe to proceed.

## Mistakes / near-misses
- DT1 test initially redeclared `const DEADLINE_ID` (already declared for the docket/fact tests) → `SyntaxError`; fixed by reusing the existing const.
- DT1 illegal_transition test first threw a plain `Error` with `.code`; `mapThrownError` maps non-`CaseBoxPersistenceError` to `not_implemented`, so it didn't prove code preservation. Fixed to throw the real `CaseBoxPersistenceError` (audit L1, in-WI).

## Review-later
- The forbidden-field coverage gap (DT-BATCH72-L1) — fold a table-driven assertion over all `TRANSITION_DEADLINE_FORBIDDEN_FIELDS` into the next IPC-handler-touching WI or a dedicated TEST WI.
