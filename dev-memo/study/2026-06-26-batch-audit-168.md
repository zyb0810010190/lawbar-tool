# Batch-audit study packet — WI-A3-UNLINK-T1 implementation window

**Date**: 2026-06-26. **Range**: `c72e8bf..29393bb` (`evidence-a3-unlink-operation`, branched from `main`).
**Type**: CLOSEOUT + GOVERN + IMPL (the audited durable unlink/relink OPERATION over case_box_links).

## What shipped in this range
- `6f0c409` — `chore(workflow): batch-audit closeout ebd6bb7..c72e8bf` (study 167; marker → `c72e8bf`).
- `9a74b20` — `chore(workflow): govern queue — WI-A3-UNLINK-T1 …` (queue.* + queue-review-089; A0.7-gated,
  custody 9b; the commit also folded a user-authorized Option-B test-wiring amendment + a Medium
  single-timestamp fix — three review-plan rounds, all recorded).
- `29393bb` — `feat(case-box-persistence): audited durable unlink/relink operation (WI-A3-UNLINK-T1)` —
  `linkRepoQueries.ts` (NEW: `unlinkLink`/`relinkLink` via `#runImmediateWrite`; build event → marker-only
  UPDATE → one audit append, all in one BEGIN IMMEDIATE; `LINK_UNLINKED`/`LINK_RELINKED`, entity_type=link;
  before/after_state_hash excludes the resolver-derived `status`; single `nowIso()` stamp for marker + event;
  reject re-unlink/re-relink + relink-reason; no row delete) + `SqliteCaseBoxPersistence.ts` (2 methods) +
  `index.ts` (3 type exports) + the extended `hardening-link-status-resolver.test.mjs` (19 op tests) + the
  plan dev-memo.

Confined to `services/case-box-persistence/src/sqlite/linkRepoQueries.ts` (new) +
`SqliteCaseBoxPersistence.ts` + `src/index.ts` + `tests/hardening-link-status-resolver.test.mjs` + `dev-memo/**`
governance/closeout/plan. NO `schema.ts` change (CURRENT_SCHEMA_VERSION stays 12), NO CHECK/FK/index, NO
`docs/contracts/**` change (the LINK kinds shipped in PR #136), NO `inMemoryRepo.ts`/shared-interface change, NO
resolver/export/UI/canonicalization/verifier behavior change (resolver/export integration-tested only), NO
`apps/**`/`native/**`/`scripts/workflow/**`/`.claude/**`/`.github/**`, NO dependency/package-lock. SQLite-only;
concrete-class methods. Marker-only UPDATE; the link row is preserved (no DELETE). Source plain UTF-8 text.

## Audit trail
- `audit-mqul0mdt-ovca7n` (Path 1 runner v0.2.18, native `--background`, gpt-5.5 / high / read-only) over
  `c72e8bf..29393bb`: **BATCH-PASS C0 H0 M0 L0**. rawOutput sha256
  `78d4b6274655f55a9dafebbe06eaaf14c5d0a4ad658ebc489750daaa07ef364f`.
  - Required recording: kind=audit; scope=`c72e8bf..29393bb` (3 commits: closeout + governance + impl);
    runner=`~/.claude/plugins/cache/xiaolai/cc-suite/0.2.18/scripts/codex-runner.mjs`;
    model/effort/sandbox=gpt-5.5/high/read-only; jobId=`audit-mqul0mdt-ovca7n`; status retrievable=YES;
    first-attempt success.
- (Lane-internal records, already audited: review-plan `review-plan-mqujz9pi-brq8ob` READY (rounds
  `-mquixkb5-h3hhyh` / `-mquju7dc-50f0i2` / `-mqujz9pi-brq8ob`); human 9b gated PASS; impl audit
  `audit-mqukpxes-fz17ut` PASS C0 H0 M0 L2 (L1 relink-reason-reject + L2 status-exclusion test — BOTH fixed);
  verify `verify-mqukunsj-ut6106` ALL CLOSED.)

## Low disposition
- None. C0 H0 M0 L0. (The lane-internal audit's two Lows were fixed in `29393bb`, not deferred.)

## State
- Clears the audit-DUE window for `c72e8bf..29393bb` and advances the marker to `29393bb`. The WI-A3-UNLINK-T1
  durable unlink/relink OPERATION is committed (unmerged): `unlinkLink`/`relinkLink` set/clear the V12 markers
  and emit `LINK_UNLINKED`/`LINK_RELINKED` — the emitter the V12 schema + the resolver/export marker-awareness +
  the PR #136 audit kinds were built to support. Headless persistence only; no Evidence UI/IPC.

**Verdict: BATCH-PASS C0 H0 M0 L0** (safe; the operation is the emitter; no schema/contract/UI/dependency drift).

<!-- batch-audit-attestation v1
range_base: c72e8bf74d97b4fecd3c71bf3c2c774d6e58c64a
target_sha: 29393bb8bb591e0621577e0b1553118ac1224aa6
verdict: BATCH-PASS
findings: C0 H0 M0 L0
broker_job_id: audit-mqul0mdt-ovca7n
broker_output_sha256: 78d4b6274655f55a9dafebbe06eaaf14c5d0a4ad658ebc489750daaa07ef364f
-->
