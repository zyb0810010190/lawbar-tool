# Batch-audit study packet — WI-DPE5 (post-merge cumulative window)

**Date**: 2026-06-12. **Range**: `f60367c..8be6086` (`main`). **Type**: UI (renderer — docket proposal edit).

## What shipped in this range
- **WI-DPE5** (UI, renderer, PR #89 merge `8be6086`, impl `f44aba6`, design artifact `56e19b4`): the terminal
  UI layer of the docket-proposal-edit feature. Renderer-only; reuses the frozen DPE4 `casebox:docket:edit`
  channel + DTO (NO new channel, NO persistence/IPC/contract/error-map change). Adds the renderer
  `editDocketEntry` bridge (`renderer/api.ts`, `stripDtoFields → RENDERER_EDIT_DOCKET_DTO_FIELDS`) + the
  `EditDocketEntryDto` mirror + the 8-field allowlist (`renderer/types.ts`). Adds a per-row two-step **Edit**
  reveal in `viewMatterDocketProposals.ts` mirroring the dismiss control: Edit/Dismiss mutually exclusive;
  **Edit and Confirm stay distinct** (no edit-then-confirm); five editable scalar fields + `reminder_offsets`
  read-only **passthrough** (sent unchanged as data — null + array; no rich editor); an **"(edited)" badge**
  on `revised_at` (display-only, never an input/sent); Save keeps the entry **proposed** + refreshes in place;
  error keeps the form open with a no-leak inline alert + re-enable; Cancel makes no api call; a `saving`
  re-entrancy guard makes Escape/Cancel/double-Save no-ops mid-flight. No sibling split (screen ~550 pure < 800).
- Plus 1 prior benign workflow commit `c754aed` (the DPE4 `f680d2e..f60367c` Layer-B closeout log + study
  packet 97) and the PR-#89 merge commit `8be6086`.

## Review + remediation trail
- review-plan `review-plan-mqaiq0th-mw0jj0` READY (Low-risk clarifications; 2 Lows folded — reminder_offsets
  passed AS DATA tested for null+array; sibling split presentational-only). Governed queue sha
  `d2f8476889813c2e1e62086519eedb162af6c2372229bd3bbdaafd5f4b7ac7b2`; review artifact `queue-review-036.md`.
  Design artifact `dev-memo/design/2026-06-12-docket-proposal-edit-ui.md` committed `56e19b4`, cited in the
  governed queue block (`Design artifact:` line) + the PR.
- per-WI audit `audit-mqaj95l9-7062cm`: C0 H0 **M1 L1** — Medium (Escape-during-save re-entrancy: the form
  could collapse mid-save) + Low (isolation test didn't observe the dismiss path) — **BOTH FIXED** in `f44aba6`
  (a `saving` guard; a wired dismiss counter + a re-entrancy test); verify `verify-mqajdmxs-7wylbh`: ALL CLOSED.
- Gates: `npm --prefix apps/lawbar-desktop test` 576/576 (DPE5 edit subset 18/18); loc-guardian PASS
  (`viewMatterDocketProposals.ts` ~550 pure < 800; new test 292 raw < 1200); `renderer-dto-sync` EDIT pair
  green; UI design-artifact gate passes with the PR `Design artifact:` line.

## Layer-B batch audit (cumulative)
broker audit `audit-mqajtaux-184eei` (Path 1 runner v0.2.18 native `--background`, gpt-5.5/high/read-only)
over `f60367c..8be6086`, file-scoped final-attestation prompt. Verdict: **No C/H/M/L regression findings.**
Boundary confirmed: the range touches ONLY `apps/lawbar-desktop/{renderer,tests}/**` + `package.json` +
`dev-memo/**`; no `services/case-box-persistence`, `docs/contracts`, `apps/lawbar-desktop/src/caseBox`,
`apps/lawbar-desktop/electron`, or error-map change; no new IPC channel. WI-DPE5 end-state verified (bridge,
DTO allowlist, edit/dismiss mutual exclusion, no edit-then-confirm, passthrough reminders, display-only
`revised_at` badge, save/error/cancel/re-entrancy behavior). Design artifact present + cited. Governance
content-bound (`queue.governed` == sha256(queue.md) `d2f84768`). No secret/gate weakening; no artifact deletion.

**Verdict: BATCH-PASS C0 H0 M0 L0.**

<!-- batch-audit-attestation v1
range_base: f60367cd48cf3a0d850d20f94470fd5932cf2365
target_sha: 8be6086e0c736b9671fba6f56d26493083a06a91
verdict: BATCH-PASS
findings: C0 H0 M0 L0
broker_job_id: audit-mqajtaux-184eei
broker_output_sha256: 8d61ffc7a0596607fa966c94712d6a44372f6fc1a95cd8d5191dbcc738825ed1
-->
