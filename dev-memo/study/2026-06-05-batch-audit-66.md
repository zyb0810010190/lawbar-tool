# Batch-audit study packet — WI-D1/docket:list (#63), Layer B

**Date**: 2026-06-06.
**Batch range**: `82f922f..316417f` (4 commits since the prior `last-batch-audit` marker `82f922f`).
**Trigger**: `BATCH_AUDIT_EVERY=3`; the #63 merge brought commits-since-marker to 4 (≥ 3) → `batch-commit-guard.sh` breaker DUE on post-merge main.
**Audit broker job**: `audit-mq32f0ue-d3zh3d` (Path 1 runner v0.2.18, `gpt-5.5`, effort high, sandbox read-only, foreground).
**Verdict**: **BATCH-PASS (C0 / H0 / M0 / L0)**.
**Closeout**: twenty-eighth production use of `scripts/workflow/batch-closeout.mjs`. First implementation WI of BATCH-CASEBOX-DOCKET-LIFECYCLE-00 (WI-D2/D3/D4 still queued, unimplemented).
**Note**: the range includes the prior WI-G2 closeout doc `a8daaa3` (which wrote `82f922f` into the marker and is the first commit after the marker content), the docket-batch governance `86919d9`, the WI-D1 implementation `2b4a648`, and the merge `316417f`.

<!-- batch-audit-attestation v1
range_base: 82f922f5c3b871f8615d262f35ef5311ce4d9cd1
target_sha: 316417fb2b59f5c46e9abd74293422f84e6bb5a2
verdict: BATCH-PASS
findings: C0 H0 M0 L0
broker_job_id: audit-mq32f0ue-d3zh3d
broker_kind: audit
broker_output_sha256: 4b8b9117e1c677f4a35665b6da08dcc30cb517437c30ec3e7c65ea9cad543c57
-->

## Tasks completed in the batch range

| Commit | PR | Behavior |
|---|---|---|
| `a8daaa3` | (workflow) | DOC-ONLY: the prior WI-G2 Layer-B closeout's study packet (`dev-memo/study/2026-06-05-batch-audit-65.md`, range `7685646..82f922f`) + a `dev-memo/batch-closeout-log.md` line. Advanced the marker to `82f922f`. |
| `86919d9` | (workflow) | GOVERNANCE: BATCH-CASEBOX-DOCKET-LIFECYCLE-00 queue (WI-D1..WI-D4) + `queue.linted/reviewed/governed` (content-bound) + `reviews/queue-review-014.md` (review-plan `review-plan-mq21baq8-wnu3o7` PASS after a NEEDS-FIX→fix on WI-D2 proposed-only + mandatory projection). |
| `2b4a648` | #63 | WI-D1: `casebox:docket:list` IPC read handler over `listDocketEntries`, projected through `DOCKET_ENTRY_RESPONSE_FIELDS`; fail-closed getMatter+tenant preflight; server-injected tenant_id; enum/limit/cursor validation. +11 tests; suite 453→464. No persistence/contract/renderer change. |
| `316417f` | #63 | GitHub PR merge of WI-D1 to main. |

## Decisions (alternatives, why chosen, what would falsify)

1. **Read handler mirrors the existing list pattern** — `listDocketEntriesHandler` reuses the `listDeadlines`/`listFacts` shape (validation order, scoped preflight, `projectPage`) rather than inventing a new flow. Falsifier: a success path bypassing `projectPage(…, DOCKET_ENTRY_RESPONSE_FIELDS)` (audit confirmed the only success return is projected).
2. **Filters validated against enum allowlists before persistence** — `confirmation_state`/`source_type` are checked against `DOCKET_CONFIRMATION_STATES`/`DOCKET_SOURCE_TYPES` so a malformed filter fails closed rather than reaching the query. Falsifier: an unvalidated filter passed through (audit A3 confirmed validation precedes the read).
3. **No persistence/contract change** — `listDocketEntries` + `DOCKET_ENTRY_RESPONSE_FIELDS` already existed; WI-D1 is pure IPC wiring. Falsifier: any `services/**`/`docs/contracts/**` diff (none).

## Audit findings + disposition

- **C0 / H0 / M0 / L0.** Product delta is WI-D1 only + docs/governance; no `renderer/**`, `services/**`, `docs/contracts/**`, `settings.json`, or persistence/contract change. The IPC addition is read-only (no create/confirm/dismiss/transition). Security boundary holds: validation precedes reads, getMatter+active-tenant preflight before the query, server-injected tenant_id, mandatory `projectPage` projection (allowlist excludes the four authority identities). Governance hygiene: `queue.governed` is `lint+review` AND content-bound (`queue_sha256` recomputed at `316417f` matches `queue.md` — the WI-G1 invariant still holds); `queue-review-014.md` single PASS. Prior closeout `a8daaa3` docs-only. WI-D2/D3/D4 remain queued, unimplemented.
- The per-WI audit `audit-mq2eufi1-2aijge` was already CLEAN (C0 H0 M0 L0); verify `verify-mq2ezxzm-ho9rdf` (ALL CLOSED). No deferred findings introduced.

## Per-WI provenance (already independently verified)

- #63 WI-D1: review-plan `review-plan-mq21baq8-wnu3o7` (queue PASS) → audit `audit-mq2eufi1-2aijge` (CLEAN C0 H0 M0 L0) → verify `verify-mq2ezxzm-ho9rdf` (ALL CLOSED). Suite 464 pass / 0 fail; loc-guardian GATES OK. PR-time `check-ui-design-artifact` = SUCCESS (no renderer path).
- `a8daaa3` is the doc-only product of the WI-G2 closeout (`audit-mq20t0l8-dlg0rg` BATCH-PASS for `7685646..82f922f`).

## Batch state after this closeout

This closeout advances `dev-memo/run/last-batch-audit` from `82f922f` to `316417f`, clearing the breaker (commits-since-marker → 0). **WI-D1 (`casebox:docket:list`) is merged** — the IPC layer can now read durable proposed docket entries; the reload-visibility gap is closeable from the renderer once WI-D4 lands. **Remaining in BATCH-CASEBOX-DOCKET-LIFECYCLE-00 (queued, unimplemented):** WI-D2 (`casebox:docket:dismiss`, proposed-only, fail-closed), WI-D3 (design artifact), WI-D4 (renderer bridge + pending-proposals UI + dismiss UI, design-gated). Ledger: 0 open High, 0 open Medium.
