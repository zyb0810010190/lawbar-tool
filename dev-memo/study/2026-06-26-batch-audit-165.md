# Batch-audit study packet — WI-A3-UNLINK-SCHEMA-01 V12 implementation window

**Date**: 2026-06-26. **Range**: `e553060..27dbe51` (`evidence-a3-unlink-resolve`, branched from `main`).
**Type**: CLOSEOUT + IMPL + MERGE (the durable-unlink V12 additive-migration implementation lane).

## What shipped in this range
- `0aef4cb` — `chore(workflow): batch-audit closeout cf35ffd..e553060` (study 164 — the mid-lane governance
  closeout; marker → `e553060`).
- `0c23586` — `feat(case-box-persistence): durable-unlink V12 additive migration` — `schema.ts`
  (`DDL_STATEMENTS_V12` = two additive `ALTER TABLE case_box_links ADD COLUMN` — `unlinked_at TEXT COLLATE
  BINARY` + `unlink_reason TEXT`; `CURRENT_SCHEMA_VERSION` 11->12; `DDL_BY_VERSION` `[12,…]`; V1-V11 unchanged)
  + `hardening-schema.test.mjs` (6 V12 cases + version-pin updates) + `hardening-anchor-delete-guard.test.mjs`
  (narrow stale-version-pin fix only).
- `27dbe51` — `Merge pull request #134 …` — merge into main.

Confined to `services/case-box-persistence/src/sqlite/schema.ts` (the V12 additive migration) + two test files +
`dev-memo/**` governance/closeout. NO resolver/export/guard-SOURCE change, NO errors/index/package.json change,
NO `apps/**`/`native/**`/`scripts/workflow/**`/`.claude/**`/`.github/**`, NO dependency/package-lock. The V12
migration is additive + nullable (no NOT NULL, no default, no FK/cascade/index/CHECK on the marker); V1-V11 DDL
unchanged; `applySchema` version-gated; `CURRENT_SCHEMA_VERSION` is 12; `anchor_id` stays NOT NULL; `LinkStatus`
unchanged (no `unlinked`). Schema-only — nothing reads/writes the marker yet. Source is plain UTF-8 text.

## Audit trail
- `audit-mquew58e-6bub7u` (Path 1 runner v0.2.18, native `--background`, gpt-5.5 / high / read-only) over
  `e553060..27dbe51`: **BATCH-PASS C0 H0 M0 L0**. rawOutput sha256
  `d60c65ba1bfc0c7a662978a02e969ba09d4d06aeffd2c709b9dcca5bfd30c021`.
  - Required recording: kind=audit; scope=`e553060..27dbe51` (3 commits: closeout + impl + merge);
    runner=`~/.claude/plugins/cache/xiaolai/cc-suite/0.2.18/scripts/codex-runner.mjs`;
    model/effort/sandbox=gpt-5.5/high/read-only; jobId=`audit-mquew58e-6bub7u`; status retrievable=YES;
    first-attempt success (no TIMEOUT).
- (Lane-internal records, already audited: review-plan `review-plan-mqud08x6-r1lqqy` + re-review
  `review-plan-mqudbjfx-92shcs`; human 9b gated PASS; impl audit `audit-mque68vl-5m64c3` PASS C0 H0 M0 L0;
  verify `verify-mque8x58-ryswx8` ALL CLOSED; governance-window audit `audit-mquebuew-vguwue` BATCH-PASS.)

## Low disposition
- None. C0 H0 M0 L0.

## State
- Clears the audit-DUE window for `e553060..27dbe51` and advances the marker to `27dbe51`. PR #134 (the durable-
  unlink V12 migration) is merged to main: `case_box_links` now has the nullable `unlinked_at` + `unlink_reason`
  marker columns. This closeout runs on the new branch `evidence-a3-unlink-resolve`, OPENING WI-A3-UNLINK-RESOLVE
  — the A0.7-gated (custody 9b) resolver/export MARKER-AWARENESS WI: make `resolveLinkStatuses` treat
  `unlinked_at IS NOT NULL` as a highest-precedence non-clean status (never recompute to valid) and
  `buildExportCitations` flag an unlinked link distinctly (non-clean, distinguishable from structural broken;
  A10 no-drop). No schema change in that WI.

**Verdict: BATCH-PASS C0 H0 M0 L0** (safe to proceed to the WI-A3-UNLINK-RESOLVE governance).

<!-- batch-audit-attestation v1
range_base: e553060cf2e0c70bc43b992e335ec45435aa2d62
target_sha: 27dbe517404c02ff497cb6fd165f2481e9137731
verdict: BATCH-PASS
findings: C0 H0 M0 L0
broker_job_id: audit-mquew58e-6bub7u
broker_output_sha256: d60c65ba1bfc0c7a662978a02e969ba09d4d06aeffd2c709b9dcca5bfd30c021
-->
