# Batch audit 288 — workflow-rule window (UI-plan closure + plan-execution rule)

**Range:** `52a8207..d20d8e2`. **Branch:** `feature/pta-claimtrack-vertical-slice` (local; no push).
**All three commits are docs/workflow — no product source, test, schema, or config in this window.**

## Window contents (3 commits)

- `66f9d4f` — prior batch-audit closeout (study 287; governance only).
- `d19b8c8` — **UI redesign plan CLOSED at UI-3; UI-4 verified as a no-op.** Both UI-4 items were
  re-verified against real code: (L) "header = 编辑 primary, 危险操作 = 归档 danger" was ALREADY
  SATISFIED (`viewMatter.ts:317-318` and `:488-501`), and the item contradicted its own document —
  §"Visual-weight rules" already called that separation "intentional, not drift"; (R) Settings'
  interactive half was already deferred (needs IPC) and the static screen has no gap. Also recorded
  `BATCH286-LOCFALLBACK-VERDICT-1` in the deferred backlog.
- `d20d8e2` — **NEW rule `.claude/rules/plan-execution.md`** (112 lines): §1 pre-implementation
  re-verification with a four-way item classification, §2 post-implementation gap audit (4 gap
  definitions, list printed first, 3-pass bound, leftovers OUTSTANDING), §3 status stamping with the
  HEAD-binding timing constraint and the `queue.md` exclusion, §4 cc-suite runner-only invocation,
  §5 explicit bounds. Adapted from an external `/execute-plan` spec; three of that spec's instructions
  were REJECTED (`Skill(cc-suite:*)`, autonomous multi-plan execution, blanket rewrite/clean licence).

## Programme outcome recorded this window

The 4-batch UI redesign is complete. Its durable lesson: **the design draft's item list never survived
contact with the code in any of the four batches** — UI-1's core premise was false, UI-2's action-order
item was one line not a sweep, UI-3 had one already-satisfied item plus one wrong premise, UI-4 was
empty. Roughly half the planned work did not exist. `plan-execution.md` §1 exists to make that
re-verification systematic rather than dependent on the operator's diligence.

## Broker audit result

`audit-msl8tlwr-eym4wt` (cc-suite Path 1, foreground, gpt-5.5/high/read-only; 22 KB unified diff
inlined). Verdict **BATCH-PASS C0 H0 M0 L2**.

Confirmed by the auditor: the new rule does **not** weaken any hard stop (§5 preserves WI file bounds,
one-WI-at-a-time, governed-queue authority, no unrelated cleanup, real verification before DONE); its
queue/stamp/HEAD-binding claims are internally consistent; the `Skill(cc-suite:*)` warning is concrete
and points at the working invocation path rather than mandating an unavailable command; no contract-doc
markdown corruption; no secrets, client identifiers, product drift, or audit-chain weakening.

**L1 — ACCEPTED as written; no follow-up.** §2's "print the gap list BEFORE fixing anything" is
behavioural discipline rather than a durable post-hoc check — not auditable from the final commit
without transcripts. Explicitly NOT a gate weakening: the same section still requires every gap to be
closed and re-verified, or recorded as OUTSTANDING in `dev-memo/deferred-audit-findings.md`, and those
parts ARE durably checkable. Rewording it into a commit-visible artifact would add ceremony without
adding safety. No backlog row.

**L2 — CLOSED BY VERIFICATION; no follow-up.** The auditor noted d19b8c8's UI-4 no-op evidence is
repo-textual and could not be independently revalidated because the prompt forbade filesystem reads
(deliberate — the inlined-diff pattern exists because renderer-scope audits time out on the tree walk).
The cited lines were verified directly against the working tree before UI-4 was closed:
`viewMatter.ts:317-318` renders 编辑 as `button button--primary view-edit-btn`, and `:488-501` renders
归档 as `button button--danger view-archive-btn` inside the `detail.dangerZone` pull card. The auditor's
own reading agrees it "does not read as skipped work". No backlog row.

<!-- batch-audit-attestation v1
range_base: 52a8207f95bfe2db30a057eb824e6858410bda18
target_sha: d20d8e2f85fc2cb142ea8698975c08a9a8f405ad
verdict: BATCH-PASS
findings: C0 H0 M0 L2
broker_job_id: audit-msl8tlwr-eym4wt
broker_output_sha256: 1ce54a4415af2c28e989ac95838a190096a0f69d1bf87e422a274f9efbc6a32f
-->
