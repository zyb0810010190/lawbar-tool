# Batch-audit study packet — WI-703/fix-default-purpose (#54) + governance + prior closeout, Layer B

**Date**: 2026-06-05.
**Batch range**: `f416a4e..7ddc9b8` (4 commits since the prior `last-batch-audit` marker `f416a4e`).
**Trigger**: `BATCH_AUDIT_EVERY=3`; the #54 merge brought commits-since-marker to 4 (≥ 3) → `batch-commit-guard.sh` breaker DUE on post-merge main, before any WI-702 implementation commit.
**Audit broker job**: `audit-mq10q8em-3bbahz` (Path 1 runner v0.2.18, `gpt-5.5`, effort high, sandbox read-only, native `--background`).
**Verdict**: **BATCH-PASS (C0 / H0 / M0 / L0)**.
**Closeout**: seventeenth production use of `scripts/workflow/batch-closeout.mjs`. Closes the WI-701 Layer-B follow-up (CBW-UI-701-DEFAULT-PURPOSE) inside BATCH-CASEBOX-UI-00; WI-702 remains.

<!-- batch-audit-attestation v1
range_base: f416a4e679920825d3ebb718e4ea06b9c63e9c86
target_sha: 7ddc9b8dbeab8e743ae4a5d78151a8addb1ba4fd
verdict: BATCH-PASS
findings: C0 H0 M0 L0
broker_job_id: audit-mq10q8em-3bbahz
broker_kind: audit
broker_output_sha256: 0925006866bd9580121eb0e3bc413b948980521e7040d7a07674548d5252530d
-->

## Tasks completed in the batch range

| Commit | PR | Behavior |
|---|---|---|
| `9210c8b` | (workflow) | DOC-ONLY: the prior WI-701 closeout's study packet (`dev-memo/study/2026-06-05-batch-audit-54.md`, range `3d70e9f..f416a4e`) + a `dev-memo/batch-closeout-log.md` line. |
| `cb542b3` | (workflow) | GOVERNANCE: added WI-703 (Fix Add Fact default purpose) AHEAD of WI-702 to the governed queue; recorded CBW-UI-701-DEFAULT-PURPOSE in the deferred ledger; `queue.linted`/`reviewed`/`governed` + `reviews/queue-review-006.md`. |
| `d3f20d3` | #54 | WI-703: marks the Add-Fact purpose `"other"` `<option selected>` so a real browser's untouched select defaults to `"other"` (not the first option `"claim"`); + a browser-faithful regression test; ledger row flipped to closed. +1 test; suite 404→405. |
| `7ddc9b8` | #54 | GitHub PR merge of WI-703 to main. |

## Decisions (alternatives, why chosen, what would falsify)

1. **Fix the WI-701 Layer-B Low via a bounded WI-703, not a hotfix** — the bug (browser `<select>` default = first option `"claim"`, not the design's `"other"`) lived in merged code, so it required a governed WI. Falsifier: any change beyond the purpose-select default or outside the 3 Allowed-files (none — audit A1/A2). Per-WI audited `audit-mq10env5-gyva4z` (PASS-WITH-LOW, the one Low fixed in-WI) + verified `verify-mq10gav4-3xlm1g` (ALL CLOSED).
2. **`"other"` `<option selected>` as the browser-faithful fix** — option order unchanged; the handler's `|| "other"` guard stays as defense-in-depth, not the primary mechanism. The regression test encodes the browser rule (selected option, else first) and asserts `=== "other"` — fails pre-fix, passes post-fix — so it cannot recreate the original mock blind spot.
3. **WI-703 ahead of WI-702, both depend on WI-701** — lint-valid (no dependency on a higher-numbered WI); they share `renderer-view-matter.test.mjs` but execute sequentially.

## Audit findings + disposition

- **C0 / H0 / M0 / L0.** No accumulation drift (A1 — WI-703 touches only the purpose-select default; DTO/refresh/read-path/bridge/WI-701 behavior intact). No forbidden-area change across the range (A2 — `viewMatterFacts.ts` + its test + governance/ledger/closeout docs only; no `services/case-box-persistence/src`, `docs/contracts`, `src/caseBox`, `electron/**`, `renderer/api.ts`/`types.ts`, `viewMatterDeadlines.ts`, error-code surface, manifest/dependency, hook, marker/breaker source, or test deletion/weakening). Governance internally consistent and the CBW-UI-701-DEFAULT-PURPOSE ledger row is consistently closed (A3). No deferred-findings disposition required (L0).

## Per-WI provenance (already independently verified)

- #54 WI-703: queue review-plan `review-plan-mq109o73-r7vh3s` (READY-WITH-LOW) → audit `audit-mq10env5-gyva4z` (PASS-WITH-LOW, Low fixed in-WI) → verify `verify-mq10gav4-3xlm1g` (ALL CLOSED). Gate `npm --prefix apps/lawbar-desktop test` = 405 pass / 0 fail.
- `cb542b3` governance: queue-lint PASSED; review-plan `review-plan-mq109o73-r7vh3s` READY-WITH-LOW; `reviews/queue-review-006.md`.
- `9210c8b` is the doc-only product of the prior WI-701 closeout (`audit-mq0zsm14-ic09t5` BATCH-PASS L1 for `3d70e9f..f416a4e`).
- This Layer-B pass checks the ACCUMULATION; clean.

## Batch state after this closeout

This closeout advances `dev-memo/run/last-batch-audit` from `f416a4e` to `7ddc9b8`, clearing the breaker. **WI-702 (Add/Confirm Deadline UI + bridge) is the sole remaining governed WI**; its `Depends on: WI-701` is satisfied (WI-701 merged) and the CBW-UI-701-DEFAULT-PURPOSE follow-up is now closed — nothing else blocks WI-702.
