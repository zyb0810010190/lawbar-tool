# Batch-audit study packet — WI-A3-UNLINK-SCHEMA-00 merge window

**Date**: 2026-06-26. **Range**: `b03e8cd..cf35ffd` (`evidence-a3-unlink-schema-v12`, branched from `main`).
**Type**: CLOSEOUT + GOVERNANCE + DESIGN + MERGE (the A3-UNLINK-SCHEMA-00 durable-unlink schema-mechanism DESIGN
lane).

## What shipped in this range
- `a4eba8f` — `chore(workflow): batch-audit closeout 98abd11..b03e8cd` (study 162; marker → `b03e8cd`).
- `cb27e5d` — `chore(workflow): govern queue — WI-A3-UNLINK-SCHEMA-00 …` (queue.* + queue-review-084;
  design-only, not A0.7-gated).
- `d6ca277` — `docs(evidence): A3 durable-unlink schema mechanism — A3-UNLINK-SCHEMA-00` — the
  schema-mechanism ADR + the `plan-batch-casebox-evidence-a3-unlink-schema-00.md` sequencing plan.
- `cf35ffd` — `Merge pull request #133 …` — merge into main.

Confined to `docs/adr/**` + `dev-memo/**`. No product source, no `schema.ts`, no migration, no
`apps/**`/`native/**`/`scripts/workflow/**`/`.claude/**`/`.github/**`, no dependency/package-lock, no
marker/ledger/key/evidence-run files, no `dev-memo/run/evidence/**`. The ADR is DESIGN-ONLY: it chooses option 1
(durable `unlinked_at` + `unlink_reason` marker columns on `case_box_links`, preserving `anchor_id NOT NULL` +
`LinkStatus` unchanged), with the resolver respecting the marker and export reading the marker as a hard
clean-export override; V11->V12 forward-only additive; `CURRENT_SCHEMA_VERSION` stays 11 in the design lane; the
exact V12 DDL / export flag / audit shape are deferred. No schema/DDL/migration/resolver/export code.

## Audit trail
- `audit-mqucvacp-fpna0w` (Path 1 runner v0.2.18, native `--background`, gpt-5.5 / high / read-only) over
  `b03e8cd..cf35ffd`: **BATCH-PASS C0 H0 M0 L0**. rawOutput sha256
  `9514f914aa6936cc1bb405a30342bf7ac9139e6909c84f1c5614cf823518d22a`.
  - Required recording: kind=audit; scope=`b03e8cd..cf35ffd` (4 commits: closeout + governance + design +
    merge); runner=`~/.claude/plugins/cache/xiaolai/cc-suite/0.2.18/scripts/codex-runner.mjs`;
    model/effort/sandbox=gpt-5.5/high/read-only; jobId=`audit-mqucvacp-fpna0w`; status retrievable=YES;
    first-attempt success (no TIMEOUT).
- (Lane-internal records, already audited: review-plan `review-plan-mqu4xxym-rdv1pz` READY
  CONFIRMED-DESIGN-ONLY-NOT-GATED MECHANISM OPTION-1-MARKER-COLUMNS-SOUND; design audit `audit-mqu53v32-wd0ght`
  PASS C0 H0 M0 L1 — §7 actor/who wording fixed.)

## Low disposition
- None. C0 H0 M0 L0.

## State
- Clears the audit-DUE window for `b03e8cd..cf35ffd` and advances the marker to `cf35ffd`. PR #133 (the
  A3-UNLINK-SCHEMA-00 durable-unlink schema-mechanism DESIGN) is merged to main. This closeout runs on the new
  branch `evidence-a3-unlink-schema-v12`, OPENING WI-A3-UNLINK-SCHEMA-01 — the V12 additive-migration
  IMPLEMENTATION lane (A0.7-gated, custody 9b) that adds the `unlinked_at` + `unlink_reason` marker columns to
  `case_box_links` per A3-UNLINK-SCHEMA-00 §2 (CURRENT_SCHEMA_VERSION 11 -> 12; forward-only additive; resolver/
  export marker-awareness + the unlink operation remain separate future WIs).

**Verdict: BATCH-PASS C0 H0 M0 L0** (safe to proceed to the WI-A3-UNLINK-SCHEMA-01 governance).

<!-- batch-audit-attestation v1
range_base: b03e8cd6d03c2e5cc008c6c16da2188f2e1de1df
target_sha: cf35ffdfaaf93ddd2670e75385ea5ca5dc2068b3
verdict: BATCH-PASS
findings: C0 H0 M0 L0
broker_job_id: audit-mqucvacp-fpna0w
broker_output_sha256: 9514f914aa6936cc1bb405a30342bf7ac9139e6909c84f1c5614cf823518d22a
-->
