# Batch-audit study packet — WI-A3-LINK-UI-T1 renderer UI impl + design-merge window

**Date**: 2026-06-27. **Range**: `e6c7526..ad75a58` (`evidence-a3-link-ui-impl`, branched from `main`).
**Type**: CLOSEOUT + MERGE + UI-IMPL (the renderer UI for the audited evidence-link lifecycle).

## What shipped in this range
- `9a52117` — `chore(workflow): batch-audit closeout 9b70e08..e6c7526` (study 175; marker → `e6c7526`; dev-memo only).
- `7240bb1` — `Merge pull request #143` — the renderer UI DESIGN artifact for audited links (`dev-memo/design/2026-06-26-audited-evidence-links-ui.md`); dev-memo-only; already audited in its own lane (batch-175).
- `ad75a58` — `UI(casebox): renderer UI for audited evidence links (WI-A3-LINK-UI-T1)` — the renderer UI IMPLEMENTATION (apps/lawbar-desktop renderer only + tests + package.json test-list + governance).

Confined to `apps/lawbar-desktop/renderer/**` (+ renderer tests + the `package.json` test list) and `dev-memo/**` (governance + design + closeout). NO `apps/lawbar-desktop/src/caseBox/**`, `electron/**`, `services/**`, `docs/contracts/**`, `schema.ts`/`CURRENT_SCHEMA_VERSION` (stays 12), `package-lock.json`, `dist-tarballs/**`, native, or dependency change. No secrets / no marker/evidence-run committed.

## Audit trail
- `audit-mqw17g8o-wlw2sa` (Path 1 runner v0.2.18, native `--background`, gpt-5.5 / high / read-only) over
  `e6c752685c380d8bebe266a8ef541a747c65c2e8..ad75a584df86817093821351e2e7e5359efeaade`:
  **BATCH-PASS C0 H0 M0 L1**. rawOutput sha256
  `30fd63badff7046e08ed0c1a4d97b3a8d0c11ffe10363388c6f88e16c2dc3da3`.
  - Required recording: kind=audit; scope=`e6c7526..ad75a58` (3 commits: closeout + PR #143 merge + UI impl);
    runner=`~/.claude/plugins/cache/xiaolai/cc-suite/0.2.18/scripts/codex-runner.mjs`;
    model/effort/sandbox=gpt-5.5/high/read-only; jobId=`audit-mqw17g8o-wlw2sa`; status retrievable=YES;
    first-attempt success. (A first run `audit-mqw1415f-pteuep` returned the same BATCH-PASS but emitted the
    AUDIT-RANGE with abbreviated SHAs, which the closeout validator rejects; re-run with full SHAs is authoritative.)
- Scope integrity, security/trust-boundary, data-contract integrity, confidentiality, Evidence invariants
  (no runtime A0.7; local-first; D1 deferred), and code quality all PASS with ZERO findings.
- (Lane-internal records, already audited within ad75a58's commit message: review-plan `review-plan-mqvm0y3l-z08xih`
  READY (round 5/5); per-WI audit `audit-mqvm5pmo-yi0kj7` C0 H1(working-tree-scope, resolved by exact-path staging)
  M0 L3(all fixed); verify `verify-mqvmcdu1-g1ow61` ALL CLOSED. Gates: desktop 665/665; i18n-guard 5/5;
  contract-integrity PASS 14.)

## Low disposition
- **L1 (deferred — test-coverage hardening, NO active leak)**: `LINK-UI-T1-D5`. `tests/renderer-dto-sync.test.mjs`
  does not add `LINK_RESPONSE_FIELDS` to its central `RESPONSE_ALLOWLISTS` authority-field regression set. The
  auditor confirms this is NOT an active leak: link rows are projected through the canonical `LINK_RESPONSE_FIELDS`
  (which already excludes `tenant_id`/`payload_json`) at the IPC layer, and the IPC handler tests
  (`ipc-link-handlers.unit.test.mjs`) cover that projection. The finding is to ALSO centralize the link response
  forbidden-field invariant in the renderer sync guard. Cleanup/test-hardening only; safe-to-proceed. Target: fold
  into the same docs/test-cleanup follow-up lane as D2/D3/D4. Recorded in `dev-memo/deferred-audit-findings.md`.
  (The per-WI audit's 3 Lows were FIXED in `ad75a58`, not deferred.)

## Deferred (carried, unchanged — not closed in this window)
- `LINK-IPC-T1-D1` — real-db Electron `casebox:link:*` round-trip; needs a dedicated future WI (anchor IPC OR a
  sanctioned Electron integration-test anchor-seed capability). Recorded in `dev-memo/deferred-audit-findings.md`.
- `D2`/`D3`/`D4` — docs-only cleanup (errorMap prose / IPC ADR §2 `matterId` table / queue row-flag prose).

## State
- Clears the audit-DUE window for `e6c7526..ad75a58` and advances the marker to `ad75a58`. The A3 link program:
  persistence lifecycle + IPC surface + UI design + **renderer UI implementation** now on the branch (PR pending).

**Verdict: BATCH-PASS C0 H0 M0 L1** (clean; no runtime/security/contract/scope defect; the L1 is a deferred
test-hardening item with no active leak; D1/D2/D3/D4/D5 remain deferred).

AUDIT-RANGE: e6c752685c380d8bebe266a8ef541a747c65c2e8..ad75a584df86817093821351e2e7e5359efeaade
AUDIT-VERDICT: BATCH-PASS C0 H0 M0 L1

<!-- batch-audit-attestation v1
range_base: e6c752685c380d8bebe266a8ef541a747c65c2e8
target_sha: ad75a584df86817093821351e2e7e5359efeaade
verdict: BATCH-PASS
findings: C0 H0 M0 L1
broker_job_id: audit-mqw17g8o-wlw2sa
broker_output_sha256: 30fd63badff7046e08ed0c1a4d97b3a8d0c11ffe10363388c6f88e16c2dc3da3
-->
