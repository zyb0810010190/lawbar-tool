# Batch-audit study packet — WI-805/ipc-test-split (#59), Layer B

**Date**: 2026-06-06.
**Batch range**: `6c7f767..ee594ce` (4 commits since the prior `last-batch-audit` marker `6c7f767`).
**Trigger**: `BATCH_AUDIT_EVERY=3`; the #59 merge brought commits-since-marker to 4 (≥ 3) → `batch-commit-guard.sh` breaker DUE on post-merge main.
**Audit broker job**: `audit-mq1xibfy-fryn1a` (Path 1 runner v0.2.18, `gpt-5.5`, effort high, sandbox read-only, foreground). The first attempt `audit-mq1xdg2v-ndt3if` returned an identical clean verdict but echoed the range with SHORT hashes; this attempt re-emitted the machine-parsed lines with FULL 40-char SHAs as `batch-closeout.mjs` requires.
**Verdict**: **BATCH-PASS (C0 / H0 / M0 / L0)**.
**Closeout**: twenty-third production use of `scripts/workflow/batch-closeout.mjs`. WI-805 was a standalone TEST-only LOC-debt WI (no umbrella batch).
**Note**: the range includes the prior closeout doc commit `ce4c224` (which wrote `6c7f767` into the marker file and is therefore the first commit *after* the marker content), the WI-805 governance commit `15b27b6`, the WI-805 implementation `8a2b198`, and the merge `ee594ce`.

<!-- batch-audit-attestation v1
range_base: 6c7f767ce0574fb9c04f25da9369127c3aa61390
target_sha: ee594cedfed6decb1730fbbd78dde349742d485a
verdict: BATCH-PASS
findings: C0 H0 M0 L0
broker_job_id: audit-mq1xibfy-fryn1a
broker_kind: audit
broker_output_sha256: 6542e5ab1a2c8afc741be7d66674642b337eb866e310ad8eae3a39bc3e584201
-->

## Tasks completed in the batch range

| Commit | PR | Behavior |
|---|---|---|
| `ce4c224` | (workflow) | DOC-ONLY: the prior Layer-B closeout's study packet (`dev-memo/study/2026-06-05-batch-audit-60.md`, range `867985b..6c7f767`) + a `dev-memo/batch-closeout-log.md` line. Advanced the marker to `6c7f767`, closing BATCH-CASEBOX-FACT-REVIEW-00. Docs/governance only. |
| `15b27b6` | (workflow) | GOVERNANCE: WI-805 queue (`queue.md` + `queue.linted`/`queue.reviewed`/`queue.governed` = lint+review) + the review artifact `dev-memo/run/reviews/queue-review-011.md` (single `QUEUE_REVIEW_VERDICT=PASS`, review-plan `review-plan-mq1onptw-806y2i`). Governance only; no product change. |
| `8a2b198` | #59 | WI-805: TEST-only split of `apps/lawbar-desktop/tests/ipc-handlers.unit.test.mjs` (1534 → 1028 raw LOC) — the cohesive case-box ENTITY IPC test group (listDeadlines + listFacts read + WI-601 docket create/confirm + WI-602 fact create, with inline `makeDocketProvider`/`makeFactProvider`/`validCreateDto`/`ENTRY_ID`/`DEADLINE_ID`) MOVED verbatim into a new sibling `tests/ipc-casebox-handlers.unit.test.mjs` (559 LOC; local harness copy + `CHANNEL` import; no `CaseBoxPersistenceError`). Host's 5 now-unused handler imports pruned. New file wired into the `test`/`test:unit`/`test:ipc-unit` package scripts. Ledger row `IPC-HANDLERS-1534` recorded + closed. Suite stays 453 pass / 0 fail. |
| `ee594ce` | #59 | GitHub PR merge of WI-805 to main. |

## Decisions (alternatives, why chosen, what would falsify)

1. **Move, not rewrite** — the whole case-box entity block (`// ---------- listDeadlines ----------` → EOF) was relocated byte-for-byte (498 lines), with a LOCAL copy of the shared harness prepended to the new file rather than a new shared import, mirroring the WI-704 `renderer-fact-write` split precedent. Falsifier: any moved test with a changed assertion/title, or a test present in both files (double-count) or neither (lost). Audit A2 confirmed 68 + 38 = 106 = original, no drift.
2. **Keep `CaseBoxPersistenceError` only in the host** — the moved block never used it (its `illegal_transition` test stayed with the matter/audit group in the host); the new file imports only `CHANNEL` (for `CHANNEL.factCreate`). Falsifier: an unused import in either file, or a missing import that would fail at runtime. Audit A3 confirmed clean.
3. **Wire the new file into all three named scripts** (`test`, `test:unit`, `test:ipc-unit`) — the WI-805 review-plan M2 requirement, so the targeted unit scripts do not silently drop the moved coverage. Audit A4 confirmed all three include the new path.

## Audit findings + disposition

- **C0 / H0 / M0 / L0.** No security or behavioral drift: the entire range is TEST-only + docs/governance; no product/renderer/electron/src/services/contract source changed (A1). Move-faithful (A2); LOC + imports clean (A3); package wiring complete with no missing/dropped file (A4); governance hygiene intact — `queue.governed`=lint+review, single PASS review verdict, no hook/marker/breaker/test tamper, no secrets (A5); ledger `IPC-HANDLERS-1534` accurately closed, `CBW-UI-804-HEADER` left OPEN and untouched (no renderer file in range) (A6).
- The per-WI audit `audit-mq1owtmk-gy0had` had surfaced 2 Lows (ledger LOC accuracy; host EOF whitespace); both were fixed in-WI before commit `8a2b198` and confirmed by verify `verify-mq1p2cct-0c5oes` (ALL CLOSED). This Layer-B pass independently found nothing further.

## Per-WI provenance (already independently verified)

- #59 WI-805: review-plan `review-plan-mq1onptw-806y2i` (queue READY) → audit `audit-mq1owtmk-gy0had` (C0 H0 M0 L2; both Lows fixed in-WI) → verify `verify-mq1p2cct-0c5oes` (ALL CLOSED). Gate `npm --prefix apps/lawbar-desktop test` = 453 pass / 0 fail.
- `ce4c224` is the doc-only product of the prior Layer-B closeout (`audit-mq1o8o9x-8pzc6s` BATCH-PASS for `867985b..6c7f767`).
- This Layer-B pass checks the ACCUMULATION; static results clean (the read-only sandbox could not rerun `tsc`/the suite, so the executing-agent gate result is the runtime evidence).

## Batch state after this closeout

This closeout advances `dev-memo/run/last-batch-audit` from `6c7f767` to `ee594ce`, clearing the breaker (commits-since-marker back to 0). The 1534-LOC `ipc-handlers.unit.test.mjs` monolith is resolved; the case-box entity IPC tests live in their own sibling file, all under the 1200 hand-written-test cap. **Remaining follow-up (non-blocking, NOT started)**: the **CBW-UI-804-HEADER** Low (one-line `viewMatterFacts.ts` header correction — a stale "NO … review" comment after WI-804 added review controls) remains deferred and OPEN.
