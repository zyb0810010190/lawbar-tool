# Batch-audit study packet — PRC-2 (#40) + prior closeout, Layer B

**Date**: 2026-06-04.
**Batch range**: `1ab1888..d36bf00` (3 commits since the prior `last-batch-audit` marker `1ab1888`).
**Trigger**: `BATCH_AUDIT_EVERY=3`; 3 commits ≥ 3 → `batch-commit-guard.sh` breaker fired DUE.
**Audit broker job**: `audit-mpzjwug1-z3ppcs` (Path 1 runner, `gpt-5.5`, effort high, sandbox read-only, native `--background`).
**Verdict**: **BATCH-PASS (C0 / H0 / M0 / L0)**.
**Closeout**: third production use of `scripts/workflow/batch-closeout.mjs`.

<!-- batch-audit-attestation v1
range_base: 1ab188822101096fb73eaae99e800169ca624d92
target_sha: d36bf00a1940bea11b511978c0f0576f8acfc3a9
verdict: BATCH-PASS
findings: C0 H0 M0 L0
broker_job_id: audit-mpzjwug1-z3ppcs
broker_kind: audit
broker_output_sha256: ba8a86926b125cd42b2432a3e6c56ca90042135e2627ade6a7597bc631195fe4
-->

## Tasks completed in the batch

| Commit | PR | Behavior |
|---|---|---|
| `e819ca3` | (workflow) | DOC-ONLY: the prior batch-closeout's study-packet attestation + a `dev-memo/batch-closeout-log.md` line. No code. |
| `aafd450` | #40 | PRC-2: path canonicalization for `protect-run-control.sh`. New `runcontrol-canon.mjs` (canonicalize + exact-classify; realpath; dangling-symlink resolution; fail-closed wrapper). Reused `logmd-append-guard.mjs` unmodified. Closes PRC-2 + PRC-5 + the PRC-1 symlink residual. |
| `d36bf00` | #40 | GitHub PR merge to main. |

## Decisions (alternatives, why chosen, what would falsify)

1. **Canonicalize-then-EXACT-classify in a Node helper** (not bash suffix globs, not GNU `readlink -f`). Falsifier: a lexical/symlink spelling that still bypasses, or a protected basename dropped from the helper's set.
2. **Workspace-scoped protection** — a foreign absolute path under a different root is out of scope (the old glob over-matched). The audit affirmed this is a correct narrowing, not a weakening.
3. **Fail-closed wrapper** — only helper exit `2` falls through to the bash fallback; any other unrecognized exit denies. Falsifier: an unexpected helper exit reopening the lexical bypass.

## Audit findings + disposition

- **C0 / H0 / M0 / L0.** No accumulation drift. `runcontrol-canon.mjs` composes cleanly with `protect-run-control.sh` + the reused PRC-1 verifier; `batch-commit-guard.sh`, `block-run-control-bash-write.sh`, `logmd-append-guard.mjs` unmodified; no product/UI/manifest changes; no new deps.

## Per-WI provenance (already independently verified)

- #40 PRC-2: review-plan `review-plan-mpzid6cq-3780u4` (READY-WITH-LOW) → audit `audit-mpzisjeu-n81up6` → remediated → verify `verify-mpzj2f9t-19kixv` (ALL CLOSED).
- `e819ca3` is the doc-only product of the prior closeout (`audit-mpzgii4s-0krj7q` BATCH-PASS).
- This Layer-B pass checks the ACCUMULATION, not the per-WI line findings.
