# Batch audit 277 — WI-PTA-VS1 (ClaimTrack persistence) window

**Range:** `d87fb96..a0f8e2d`. **Branch:** `feature/pta-claimtrack-vertical-slice`.

## Window contents (3 commits)
- `c366ba9` — prior batch-audit closeout (study 276 + closeout-log; governance-only).
- `5750a54` — WI-PTA-VS1 docket (docs-only plan; review-plan READY `review-plan-msdbti05`).
- `a0f8e2d` — **WI-PTA-VS1**: ClaimTrack persistence — v13 `case_box_claim_tracks` table + create/get/list
  on the shared `CaseBoxPersistence` interface + atomic `CLAIM_TRACK_CREATED` audit; additive
  `unknown_party` error code. Individually audited (`audit-msdcrh8g`, C0 H0 M1 L2 — all test-hardening,
  implementation sound) → 3 FIXED (SQLite rollback-atomicity test via `UNIQUE(matter_id, sequence)`
  injection; strengthened red-before/green-after event-content assertions; respondent-party
  `unknown_party` parity) → verified ALL CLOSED (`verify-msddo426`).

## Verdict (Layer-B backstop over the cumulative range)
- **AUDIT-RANGE: d87fb96a9c4cecb786ef4cabccf8972b2fd91704..a0f8e2d3a275b15321bd00554594ebb7fe0d5a13**
- **AUDIT-VERDICT: BATCH-PASS C0 H0 M0 L0** — no unaddressed cross-cutting defects. Job
  `audit-msddu3tf-p04kdg`, rawOutput sha256
  `3dba9509a3afab29a8788c57907f4a1b78bacef5a0aecc5e37fa1cfadf19b077`.
- Confirmed holistically: additive-only (`CURRENT_SCHEMA_VERSION` 12→13; `unknown_party` additive; no
  rename/remove/merge of audit kinds or error codes; no existing shape changed); the only new table is
  `case_box_claim_tracks`; **no FK**; no dependency/lockfile change; in-memory ↔ SQLite parity via the
  shared `prepareCreateClaimTrack` path; append-only audit chain preserved (one `CLAIM_TRACK_CREATED`,
  `event_count == COUNT == MAX(sequence)`, atomic rollback proven by the injected UNIQUE-conflict test);
  tenant scoping (preflight + row-level predicate on get/list); the forced 12→13 sibling version-pin
  bumps in `hardening-evidence`/`hardening-link-status-resolver` are mechanical (no behavior change).

## Gate evidence (run interactively this lane)
- `npm --prefix docs/contracts/case-box-contract test` — 542/0.
- `npm --prefix services/case-box-persistence test` — 630/273/288 = 1191/0 (abi-smoke OK).

## Outcome
The ClaimTrack **persistence** layer of the vertical slice is complete (VS-0 party-identity foundation
+ VS-1 ClaimTrack table/create/get/list/audit). VS-2 (IPC read + minimal create channel) and VS-3
(renderer list + add form) remain to make it a user-visible feature. All local commits; no push.

<!-- batch-audit-attestation v1
range_base: d87fb96a9c4cecb786ef4cabccf8972b2fd91704
target_sha: a0f8e2d3a275b15321bd00554594ebb7fe0d5a13
verdict: BATCH-PASS
findings: C0 H0 M0 L0
broker_job_id: audit-msddu3tf-p04kdg
broker_output_sha256: 3dba9509a3afab29a8788c57907f4a1b78bacef5a0aecc5e37fa1cfadf19b077
-->
