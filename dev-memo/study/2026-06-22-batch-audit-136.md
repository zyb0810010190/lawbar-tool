# Batch-audit study packet — WI-ENA4 impl + PR #106 merge window

**Date**: 2026-06-22. **Range**: `61c0ca0..41801fc` (`main` / `evidence-native-coordinate-probe`).
**Type**: GOVERNANCE + SCAFFOLD + MERGE (the WI-ENA4-window closeout, the WI-ENA4 governance commit, the
WI-ENA4 page-box inspection probe implementation, and the PR #106 merge into main).

## What shipped in this range
- `02ad77e` — `chore(workflow): batch-audit closeout a77c780..61c0ca0` (study packet 135 + closeout-log line;
  advanced marker to `61c0ca0`).
- `c8e4915` — `chore(workflow): govern queue — WI-ENA4 page-box inspection probe` (queue.* + queue-review-057).
- `53a0240` — `scaffold(evidence): page-box inspection probe (WI-ENA4)` — adds `PageBoxProbe.swift`
  (`inspectMediaBoxes(url:)` reading page `mediaBox` width/height via `page.bounds(for: .mediaBox)`),
  `PageBoxProbeTests.swift` (asserts 612 x 792 for both pages of the existing synthetic fixture), and a README
  update. Reuses the existing fixture; no new fixture.
- `41801fc` — `Merge pull request #106 …` — content-empty merge of WI-ENA4 into main.

No product source/schema/contract/test/dependency/UI delta beyond the page-box read probe. The existing
`native/evidence-core` JS shim is byte-unchanged.

## Audit trail
- `audit-mqq2s8jc-tkzn76` (Path 1 runner v0.2.18, native `--background`, gpt-5.5 / high / read-only) over
  `61c0ca0..41801fc`: **BATCH-PASS C0 H0 M0 L0**. Range + verdict declared on their own lines. Codex confirmed:
  page-box read probe reusing the existing synthetic fixture, asserts 612 x 792; no new deps/fixture, no JS-shim
  change, no product/UI wiring; no coordinate transforms/normalized coordinates/anchors/citation/page-identity/
  geometry-classification/rendering/renderer-conformance/A0.7/marker/HMAC/provenance. rawOutput sha256
  `563e7eb6…`.
  - Required recording: kind=audit; scope=`61c0ca0..41801fc` (4 commits: closeout + governance + ENA4 impl +
    merge); runner=`~/.claude/plugins/cache/xiaolai/cc-suite/0.2.18/scripts/codex-runner.mjs`;
    model/effort/sandbox=gpt-5.5/high/read-only; jobId=`audit-mqq2s8jc-tkzn76`; status=retrievable=YES;
    first-attempt success.

## Low disposition
- None. C0 H0 M0 L0.

## State
- Clears the audit-DUE window for `61c0ca0..41801fc` and advances the marker to `41801fc`. PR #106 (page-box
  inspection probe) is merged to main. This closeout runs on the new branch `evidence-native-coordinate-probe`,
  opening WI-ENA5 (coordinate transform roundtrip probe — the first geometry-adjacent transform step). WI-ENA5
  reuses the existing synthetic fixture (no new fixture).

**Verdict: BATCH-PASS C0 H0 M0 L0** (safe to proceed).

<!-- batch-audit-attestation v1
range_base: 61c0ca08f99ad4996cb57996199f9fe31c127379
target_sha: 41801fc7a895f8c62b3af0fe83751899d426e239
verdict: BATCH-PASS
findings: C0 H0 M0 L0
broker_job_id: audit-mqq2s8jc-tkzn76
broker_output_sha256: 563e7eb688ffea76022f4811f9e2e26cd88fd31a556e253554fe5881099bee94
-->
