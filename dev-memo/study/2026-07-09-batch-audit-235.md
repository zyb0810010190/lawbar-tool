# Batch audit — window 70c6600..ec270ef (PR #235 close)

**Verdict**: BATCH-PASS C0 H0 M0 L1. **Broker job**: `audit-mrctjfia-qel222` (Path 1 runner, gpt-5.5/high/read-only).

## Window

`70c660050a6e6bd11dbfc63c1b55eee3fec958ed..ec270ef594a7d75c81784208277de8e8e8504e26` — three commits:
- `147b262` chore(workflow): batch-audit closeout cf545e4..70c6600 (marker/doc housekeeping).
- `50c440a` fix(lawbar-desktop): party role/kind enum selects — stop create-matter "persistence schema violation" (PR #235; CI-green).
- `ec270ef` merge PR #235.

## Checks (Layer B)

- **Goal drift**: none. Range aligns with the authorized create-matter party-enum fix + workflow closeout; no silent scope expansion, no unqueued work.
- **Garbage-code risk**: none material. No new dependencies, no public-API change, no oversized-file/dead-code/circular-dep signal. Test changes are targeted and materially improve regression coverage (DTO→`validateMatter` end-to-end guard).
- **Architecture drift**: none. No unrequested abstractions, no cross-layer imports.
- **UI drift**: the fix replaces free-text party inputs with enum `<select>`s — a correctness fix, not scope expansion.

## Findings

- **L1 (cleanup)**: `renderer/screens/createMatter.ts` introduced local hardcoded party enum labels/values, drifting from the i18n/catalog + labels-facade pattern. Not a defect; dispositioned in `dev-memo/deferred-audit-findings.md` (BATCH-70c6600-L1, **closed**) — superseded by the immediately-following `WI-DESKTOP-ZH-CN-SETTINGS-ENTRY-00`, which sources party display labels from the catalog via `partyRoleLabel`/`partyKindLabel`.

## Study notes

- Decision: fix the schema-violation at the UI layer (enum selects) rather than loosen the contract — the contract is the vocabulary owner; the form must emit its enums. Falsifiable later if a legitimate party role/kind outside the current enum is required (then the contract enum changes first, via ADR).
- Review-later: none outstanding; the L1 burn-down is already implemented in the next WI.

<!-- batch-audit-attestation v1
range_base: 70c660050a6e6bd11dbfc63c1b55eee3fec958ed
target_sha: ec270ef594a7d75c81784208277de8e8e8504e26
verdict: BATCH-PASS
findings: C0 H0 M0 L1
broker_job_id: audit-mrctjfia-qel222
broker_output_sha256: 967250eecdfdfdb2deb573270ba106b6075fb1d344fa7ee3c0434a388d5e0d16
-->
