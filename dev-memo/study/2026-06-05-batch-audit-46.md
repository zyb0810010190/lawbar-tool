# Batch-audit study packet — WI-301/BCG-7 (#46) + governance + prior closeout, Layer B

**Date**: 2026-06-05.
**Batch range**: `4f806a6..9cf96a0` (4 commits since the prior `last-batch-audit` marker `4f806a6`).
**Trigger**: `BATCH_AUDIT_EVERY=3`; 4 commits ≥ 3 → `batch-commit-guard.sh` breaker fired DUE on post-merge main.
**Audit broker job**: `audit-mq0hg6uz-z31u00` (Path 1 runner v0.2.18, `gpt-5.5`, effort high, sandbox read-only, native `--background`).
**Verdict**: **BATCH-PASS (C0 / H0 / M0 / L1)**.
**Closeout**: eighth production use of `scripts/workflow/batch-closeout.mjs`; runs on post-merge `main`.

<!-- batch-audit-attestation v1
range_base: 4f806a61d265197573cddb9ebb352e8cf56408e0
target_sha: 9cf96a0d1d8235e9f95f6671c0f46d956c5c774a
verdict: BATCH-PASS
findings: C0 H0 M0 L1
broker_job_id: audit-mq0hg6uz-z31u00
broker_kind: audit
broker_output_sha256: 1cc04220d4a90c91385c4b04d3f9ba051eb7ac9acbcb2ab45786bef06bba4d9a
-->

## Tasks completed in the batch

| Commit | PR | Behavior |
|---|---|---|
| `5e89906` | (workflow) | DOC-ONLY: the prior batch-closeout's study packet (`f446807..4f806a6`, #45) + a `dev-memo/batch-closeout-log.md` line. |
| `9b7488a` | (workflow) | GOVERNANCE: replaced the consumed canary `queue.md` with batch BATCH-BCG-HARDENING-00 (WI-301/302/303), refreshed `queue.linted`/`queue.reviewed`/`queue.governed`, added `reviews/queue-review-002.md`. No product code. |
| `f2d793f` | #46 | WI-301 / BCG-7: fail-closed validation of `AUTO_ADVANCE_MAX` {1,3,10} + `BATCH_AUDIT_EVERY` [1,100] in `batch-commit-guard.sh` before the integer compares (closes a fail-open). Tests 26→40. |
| `9cf96a0` | #46 | GitHub PR merge of WI-301 to main. |

## Decisions (alternatives, why chosen, what would falsify)

1. **Fail-closed config validation BEFORE the integer compare** (string `case` for MAX; digit-length bound ≤3 BEFORE `$(( ))` for EVERY) — NOT a post-hoc clamp. Falsifier: a malformed/huge config value that still reaches a crashing compare and skips the breaker. Audited Q1–Q7 in `audit-mq0gye8z-hbqghu`; verify `verify-mq0h3ea7-p8tucq` ALL CLOSED.
2. **Validate only PRESENT `BATCH_AUDIT_EVERY`**, preserving the documented unset→`MAX` fallback (MAX now bounded {1,3,10}) — avoids an unintended behavior change.
3. **Governance kept generation/execution separate** (9b7488a): proposal `plan-batch-bcg-hardening-00.md` left untracked; only `queue.md` + markers + review artifact committed; review = `review-plan-mq0g75im-14r993` READY-WITH-LOW (Lows resolved).

## Audit findings + disposition

- **C0 / H0 / M0 / L1.** No accumulation drift. The WI-301 change is confined to the config-validation block of `batch-commit-guard.sh` (count/governed/risk/BASE logic, gated-mode routing, and the PRC-4-hardened `deny()` unchanged); the live config (`AUTO_ADVANCE_MAX=3`, `BATCH_AUDIT_EVERY=3`) stays valid. Governance commit internally consistent (queue.md ↔ queue.governed; one `QUEUE_REVIEW_VERDICT=PASS`). No product/UI/contract/manifest/schema/fixture drift; no test deletion/weakening; no marker/breaker source tampering; no `OcrQueueError`/security-boundary surface change.
- **L1 (provenance drift, cleanup-only) — DISPOSITIONED/DEFERRED**: the BCG-7 row in `dev-memo/deferred-audit-findings.md` still shows `resolved-in <pending>` and `verify <pending>` although WI-301 verification is complete. This is the repo's standing ledger placeholder convention (PRC-1/PRC-2/PRC-4 closed rows use the same `<pending>`); it does not affect guard behavior or batch authorization. **Authoritative resolution provenance**: resolution commit `f2d793f`, verify `verify-mq0h3ea7-p8tucq` (ALL CLOSED) — recorded here and in the `f2d793f` commit message. **Backfill target**: WI-302 (its Allowed-files include `dev-memo/deferred-audit-findings.md`) will fill the placeholders when it next edits the ledger; no separate commit is spent now (breaker is DUE; only closeout artifacts are committed this turn). Safe-to-proceed: YES (does not weaken WI-301's acceptance).

## Per-WI provenance (already independently verified)

- #46 WI-301/BCG-7: review-plan `review-plan-mq0gsk2l-zjy8fn` (READY-WITH-LOW) → audit `audit-mq0gye8z-hbqghu` (C0 H0 M0; 1 Low comment-strip → fixed) → verify `verify-mq0h3ea7-p8tucq` (ALL CLOSED).
- `9b7488a` governance: review-plan `review-plan-mq0g75im-14r993` (READY-WITH-LOW); queue-lint PASSED; `reviews/queue-review-002.md`.
- `5e89906` is the doc-only product of the prior closeout (`audit-mq0fp3v2-klvz5u` BATCH-PASS for `f446807..4f806a6`).
- This Layer-B pass checks the ACCUMULATION, not the per-WI line findings.
