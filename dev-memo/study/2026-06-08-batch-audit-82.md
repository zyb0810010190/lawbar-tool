<!-- batch-audit-attestation v1
range_base: 5b84395800a68186f5e41f9f201e8db2b017447f
target_sha: 8e1739049035f5240628a7ad1f5e012452d148b4
verdict: BATCH-PASS
findings: C0 H0 M0 L0
broker_job_id: audit-mq4rb0tf-6tc63x
broker_output_sha256: c8e056dffdf1d124259575f5d02df68178e68890f647ab621db198e6c0cda535
-->

# Batch audit 82 — BATCH-WORKFLOW-TEST-ENV-HYGIENE-00 (post WI-WH1, final)

**Window**: `5b84395..8e17390` (4 commits). **Verdict**: BATCH-PASS (C0 H0 M0 L0).
**Broker**: Path 1 runner v0.2.18 native --background, gpt-5.5/high/read-only, job `audit-mq4rb0tf-6tc63x`.
**Date**: 2026-06-08. **Closes the batch.**

## Tasks completed

| Commit | What |
|--------|------|
| `2ea65a5` | Layer-B closeout of the FF1 window. |
| `c726112` | Governed BATCH-WORKFLOW-TEST-ENV-HYGIENE-00 (fresh queue). |
| `4ad7d06` | WI-WH1 — `postdist` native-arch restore (package.json) + AGENTS.md governance-sequencing note + BRCBW-SUBSTR deferred Low. WORKFLOW; 525 pass; heavy acceptance ran. |
| `8e17390` | merge PR #77. |

## Decisions
- Fixed the recurring native-arch drift: `npm run dist` rebuilds better-sqlite3 for both mac arches (x64 last), breaking the dev-Electron smoke tests; a `postdist: electron-builder install-app-deps` lifecycle hook now auto-restores the host arch. Validated end-to-end (dist → x64 rebuild → postdist → arm64 restored → smoke tests green).
- Documented the govern/commit sequencing rule in AGENTS.md (the always-loaded contract): never bundle `mark-reviewed`/`govern-queue` with the dependent `git commit` in one Bash call — `batch-commit-guard.sh` (PreToolUse) denies the whole call before govern refreshes `queue.governed`.
- Deferred the run-control hook substring-anchoring issue (BRCBW-SUBSTR) — a 538-line security guard change needing the security-WI loop; documented, not fixed.

## Audit findings + disposition
- Per-WI: review-plan `review-plan-mq4r33hf-wbutzm` PASS; self-review (low-risk WORKFLOW).
- Layer-B `audit-mq4rb0tf-6tc63x`: **no findings**; only `package.json` (one `scripts.postdist` key) + `AGENTS.md` (additive note) + the ledger row changed; zero forbidden-area touch; the AGENTS edit weakens no gate/hard-stop/commit-policy; the security hook documented-not-edited.

## Mistakes / near-misses
- This batch's own governance initially tripped the very rule it documents: a bundled mark-reviewed+govern+commit Bash call was denied by batch-commit-guard before govern ran. Re-ran standalone → governed → committed separately. The AGENTS.md note now codifies the fix.

## Review-later
- BRCBW-SUBSTR remains an open deferred Low (run-control hook unanchored substring) — a future security-WI if the false-positive friction grows.
- Cadence overhead (governance/closeout commits vs BATCH_AUDIT_EVERY=3) remains the standing watch item across batches 74-82.
