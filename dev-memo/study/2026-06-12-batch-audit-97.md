# Batch-audit study packet — WI-DPE4 (post-merge cumulative window)

**Date**: 2026-06-12. **Range**: `f680d2e..f60367c` (`main`). **Type**: IMPL (desktop IPC/DTO — docket proposal edit).

## What shipped in this range
- **WI-DPE4** (IMPL, desktop IPC/DTO, PR #88 merge `f60367c`, impl `8f803fa`): the `casebox:docket:edit`
  IPC channel exposing the merged DPE3 `editDocketEntry` persistence op to the renderer under the existing
  confirm/dismiss security discipline. Adds `EditDocketEntryDto` (8 fields: scope `matterId`/`entryId`
  camelCase + the six editable content fields snake_case), `EDIT_DOCKET_DTO_FIELDS`,
  `EDIT_DOCKET_FORBIDDEN_FIELDS` (the disjoint complement — incl. the snake_case scope aliases
  `tenant_id`/`matter_id`/`entry_id`, `editor_actor_user_id`, `revised_at`, and every
  provenance/lifecycle/identity/audit field), `EditDocketEntryResult`; adds `revised_at` to
  `DOCKET_ENTRY_RESPONSE_FIELDS` (the one projection delta — input-forbidden, output-exposed).
  `editDocketEntryHandler(payload, provide, now)` mirrors confirm/dismiss: `isPlainJsonObject` shape guard
  → forbidden-field REJECT (not strip) → unknown-field REJECT → field validation → scoped preflight
  fail-closed (`getMatter` null → `unknown_matter`; tenant mismatch → `tenant_mismatch`; scoped
  `getDocketEntry` null → `invalid_payload`, NO write, no id disclosure) → proposed-only fail-closed BEFORE
  write → server-inject `{tenant_id, matter_id, entry_id, editor_actor_user_id}` + the six content fields
  (`revised_at` NOT passed — persistence server-derives it) → `projectRow(entry, DOCKET_ENTRY_RESPONSE_FIELDS)`
  → `mapThrownError` (static no-leak messages). Channel `CHANNEL.docketEdit` + `ipcMain.handle` +
  `preload` exposure only. NO renderer screen/call site (DPE5), NO persistence/contract change. DPE3
  persistence remains the final authority; the handler preflight is read-only defense-in-depth.
- Plus 1 benign prior workflow commit: `9d55d5c` (the DPE2+FIX1→DPE3 `e86747f..f680d2e` Layer-B closeout
  log + study packet 96) and the PR-#88 merge commit `f60367c`.

## Review + remediation trail
- review-plan `review-plan-mqa9a907-cqu5g3` READY (HIGH-RISK IPC security boundary — required pre-impl;
  no self-review). Governed queue sha
  `08b8b9da4f6d1834586a4485ff6f0c021bfcbe009d3560bfdf4289d4760cbb31`; review artifact `queue-review-035.md`
  (+ a mechanical loc-split scope addendum: the edit handler-unit tests went in a NEW sibling
  `tests/ipc-casebox-docket-edit.unit.test.mjs` registered in `package.json`, because
  `ipc-casebox-handlers.unit.test.mjs` sat at the 1200 raw test-fail line — user-authorized 2026-06-11).
- per-WI audit `audit-mqabn2z3-ishxe2`: **C0 H0 M0 L0** (clean); verify `verify-mqacv1jn-3fh0vs`: **ALL CLOSED**.
- Gates: `npm --prefix apps/lawbar-desktop test` 556/556 (DPE4 subset 121/121); loc-guardian PASS
  (`docketHandlers.ts`/`dto/docket.ts` < 800 pure; new sibling test file well under 1200 raw); no
  schema/`gen:types`; `check-ui-design-artifact` N/A (Type IMPL, no `renderer/**` path).

## Layer-B batch audit (cumulative)
broker audit `audit-mqahys5t-dgaoua` (Path 1 runner v0.2.18 native `--background`, gpt-5.5/high/read-only)
over `f680d2e..f60367c`, file-scoped final-attestation prompt (the tightened pattern that succeeded for
DPE3). Verdict: **No findings.** Boundary confirmed: the range touches ONLY
`apps/lawbar-desktop/{src/caseBox,electron,tests}/**` + `package.json` + `dev-memo/**`; no
`services/case-box-persistence`, `docs/contracts`, renderer screen path, or error-map change.
`casebox:docket:edit` is the only new channel, wired through preload + main IPC; the handler rejects
forbidden/unknown fields, fail-closes on scoped-preflight/proposed-only before write, forwards only
server-derived authority/scope + the six content fields to `persistence.editDocketEntry`, and exposes
`revised_at` only in projected output. Governance content-bound (`queue.governed` == sha256(queue.md)
`08b8b9da`). No secret/gate weakening; no artifact deletion.

**Verdict: BATCH-PASS C0 H0 M0 L0.**

<!-- batch-audit-attestation v1
range_base: f680d2e40e97029b2f554d4cc7c6eb8e2a55b2fb
target_sha: f60367cd48cf3a0d850d20f94470fd5932cf2365
verdict: BATCH-PASS
findings: C0 H0 M0 L0
broker_job_id: audit-mqahys5t-dgaoua
broker_output_sha256: 12347d026455c45513219280854f21dc23e27ca69a51be1d8faeda4ce3286616
-->
