# Batch-audit study packet — PR3 (dynamic sidebar state + detail layout)

**Date**: 2026-06-13. **Range**: `96d0a99..b64c28f` (branch `product-ui-detail-aria`).
**Type**: UI (renderer: sidebar a11y + #/matters/:id detail layout) + carried governance commits.

## What shipped in this range
- **PR3 — dynamic sidebar state + detail layout** (commit `b64c28f`, product/test only, 9 files):
  1. **UISHELL-L1 fix** — new `renderer/nav.ts` (`activeNavKey` + `applySidebarCurrent`) drives the shell
     sidebar `aria-current` from the active hash route (exact-link policy: list→Matters, new→New matter;
     view/archive/not-found→none); `index.ts` applies it per route; `index.html` dropped the hardcoded
     attribute and added `data-nav` hooks.
  2. **Detail route rework** — `viewMatter.ts` `renderDetail` onto `.view-desktop` / `.view-card` /
     `.view-meta-strip` / `.view-aside` / `.colophon` / `.archive-pull`, preserving all `data-test-id`s,
     focus order, English labels (reused `format.ts`, no new/duplicated label code), and the lazy
     `<details>` documents/deadlines/facts/audit sections (modules unchanged — only relocated).
- Carried governance commits in the window: `68303a5` (batch-audit closeout 3011afa..96d0a99) and
  `6b08165` (override-consumed log append). These are separate, properly-scoped commits; `b64c28f`
  itself touched no `dev-memo/` files.

## Layer-B batch audit (with a false-positive re-audit)
- First audit `audit-mqciwyrd-q61hs0` returned **BATCH-FAIL C0 H0 M2 L0** on two findings that were
  both **false positives**:
  - *M1* claimed `renderer-shell.test.mjs`'s no-hardcoded-`aria-current` assertion would fail on the
    in-file comment — but that test reads `index.html` with HTML comments **stripped** first; the test
    and full suite pass (595/595). `index.html` has no real `aria-current` attribute.
  - *M2* read the window's `dev-memo/` files as PR3 scope drift — but they belong to separate prior
    governance commits (`68303a5`, `6b08165`); the PR3 product commit `b64c28f` is product-only (9 files).
- Corrected re-audit `audit-mqcj15g8-res6kz` (Path 1 runner v0.2.18 native `--background`, gpt-5.5 /
  medium / read-only), given the two disputed facts to **independently verify** (no verdict steering),
  confirmed both as false positives and returned **BATCH-PASS C0 H0 M0 L0**. It also re-checked the
  standard axes: no goal drift, `format.ts`/`listMatters.ts` unchanged, no raw renderer-TS colors, no
  v08/v11/v13 leakage, detail layout preserves data-test-ids + lazy `<details>`.

Reliability note: this window's audits otherwise behaved (medium effort + leaner prompt avoids the
high-effort ETIMEDOUT and foreground ENOBUFS classes seen on prior windows).

## Decisions
- **Detail route kept English** (reused `format.ts`), not converted to Chinese: PR3 didn't request CN,
  the lazy sub-sections + create/archive are still English, and half-converting one page would mix
  languages + duplicate label logic. The list(CN)↔detail(EN) gap is flagged for a holistic i18n WI.
- **UISHELL-L1 closed** by this PR (recorded in `dev-memo/deferred-audit-findings.md`, citing `b64c28f`).

## Gates
- `npm --prefix apps/lawbar-desktop test` → **595/595** at `b64c28f` (build + renderer-import lint +
  no-real-data + ui-color + smoke.electron incl. the new aria-current integration test + palette-sync +
  font-wiring + shell + nav + view-matter layout).

## Review-later
- Holistic i18n WI (list CN vs detail/create/archive EN).
- Cross-screen CN label duplication if/when detail is localized (currently only listMatters has CN labels).

**Verdict: BATCH-PASS C0 H0 M0 L0.**

<!-- batch-audit-attestation v1
range_base: 96d0a9959f75f273dcc292dee546e5a7e32a1ff3
target_sha: b64c28f3bdbaf6071ccdd852f6d332c11a198a61
verdict: BATCH-PASS
findings: C0 H0 M0 L0
broker_job_id: audit-mqcj15g8-res6kz
broker_output_sha256: 1a7925790f8979deddd5fcae37a44efb492cfafbdde082ffd7656397dbb25cba
-->
