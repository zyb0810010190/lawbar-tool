# Batch audit 283 — matter-details edit Phase A (audit-chain contract) window

**Range:** `6078d35..a1b55f0`. **Branch:** `feature/pta-claimtrack-vertical-slice`.

## Window contents (3 commits)
- `17e2bf8` — prior batch-audit closeout (study 282; governance-only).
- `bde002c` — matter-details-edit umbrella PLAN (dev-memo; review-plan READY after timeout→NEEDS-FIX→rev-1→READY).
- `a1b55f0` — **matter-details-edit Phase A**: contract change — add `MATTER_DETAILS_UPDATED` audit-event kind +
  structured `changed_fields` + extend the canonical event hasher (conditional spread) + a canonicalizer guard.
  Per-WI audit `audit-msfu7fye` (no C/H; 1 M + 2 L all FIXED) + verify `verify-msfuhuc1` (ALL CLOSED). Contract
  suite 564/0.

## Decision / disposition
Pivoted from the restyle to #2 (edit case info) per Frank. Codex sequencing consult (`019fd104`) → open Phase A
now, atomic, with fully-green-before-commit discipline. Phase A is the audit-chain foundation only (no
persistence/IPC/UI). The #1 invariant — every existing event hashes byte-identically — is designed via a
conditional-spread (`changed_fields` enters the canonical object only when present, alphabetical slot) so the whole
existing chain verifies unchanged.

## Audit findings + resolution
- **Phase A per-WI audit** (`audit-msfu7fye`): no C/H; **M** — canonicalizer must reject `changed_fields` on any
  non-`MATTER_DETAILS_UPDATED` event (defense-in-depth at the security boundary) → FIXED (guard throws); **L1** —
  AJV `strictRequired` on `else:{not:{required}}` → FIXED (`else:{properties:{changed_fields:false}}`); **L2** —
  `changed_fields` sorted-order unenforced while the hash is order-sensitive → FIXED (schema/ADR require canonical
  sorted order; the Phase-B emitter enforces it; order-sensitivity test added). Verify `verify-msfuhuc1`: ALL CLOSED.
- **Batch-283 Layer-B** (`audit-msfumaax-gnixin`): **BATCH-PASS C0 H0 M0 L0.** Scope clean (contract + ADR +
  dev-memo only); byte-preservation confirmed (existing v2 canonical hashes to the pinned value); generated types
  deterministic; verifyAuditChain + append-only invariants intact.

## Mistakes / near-misses
- **None new this window** — the "fully green before commit" control held: audit findings fixed + verified,
  contract suite re-run to 564/0 (incl. the pinned goldens) BEFORE the commit, exact-path staging, desktop
  correctly untouched (tarball defers the new-kind label to Phase C). No override needed.

## Review-later / carried
- The desktop `eventKind.MATTER_DETAILS_UPDATED` i18n label + the contract-tarball republish are Phase-C bootstrap
  work (VS-0 precedent), tracked implicitly by Phase C.
- Phase B (persistence `updateMatterDetails`) MUST: emit `changed_fields` in canonical SORTED order (schema/ADR
  requirement — enforced + tested there); fail-closed state-hash continuity against the LATEST PRIOR
  `entity_type="matter"` event (D4a); strict PATCH semantics (D5); no-op + archived rejected.

## Verdict
- **AUDIT-RANGE: 6078d3515d0065e56961b40696b4117d13919a72..a1b55f0a219ea2ef992d7169c4809021dfe34821**
- **AUDIT-VERDICT: BATCH-PASS C0 H0 M0 L0** — Phase A adds the audit vocabulary + hasher extension cleanly; the
  existing audit chain is byte-preserved; no security-boundary weakening. Job `audit-msfumaax-gnixin`, rawOutput
  sha256 `4601a640b05fcaef9fcd711049545342fdd8e1fc47601676873d5a3e70698a26`.

## Gate evidence
- `npm --prefix docs/contracts/case-box-contract test` — 564/0 (byte-preservation goldens + guard + order +
  schema accept/reject; 0 AJV strictRequired warnings).
- Desktop unchanged (contract-only WI; consumes the prior tarball).

## Outcome
The edit-case audit foundation is committed + batch-closed. Phase B (persistence) opens next. Everything local; no
push. Two unpushed branches (`feature/pta-claimtrack-vertical-slice`, `chore/workflow-pretest-gate`) still await the
user's push/merge decision.

<!-- batch-audit-attestation v1
range_base: 6078d3515d0065e56961b40696b4117d13919a72
target_sha: a1b55f0a219ea2ef992d7169c4809021dfe34821
verdict: BATCH-PASS
findings: C0 H0 M0 L0
broker_job_id: audit-msfumaax-gnixin
broker_output_sha256: 4601a640b05fcaef9fcd711049545342fdd8e1fc47601676873d5a3e70698a26
-->
