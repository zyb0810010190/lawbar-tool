# Batch-audit study packet — WI-D2/docket:dismiss (#64), Layer B

**Date**: 2026-06-06.
**Batch range**: `316417f..08f0a7f` (3 commits since the prior `last-batch-audit` marker `316417f`).
**Trigger**: `BATCH_AUDIT_EVERY=3`; the #64 merge brought commits-since-marker to 3 → `batch-commit-guard.sh` breaker DUE.
**Audit broker job**: `audit-mq33ck2r-9bn7o1` (Path 1 runner v0.2.18, `gpt-5.5`, effort high, sandbox read-only, foreground).
**Verdict**: **BATCH-PASS (C0 / H0 / M0 / L1)** — the L1 is the carried, tracked `DTO-LOC-797` deferral (no new finding).
**Closeout**: twenty-ninth production use of `scripts/workflow/batch-closeout.mjs`. Second implementation WI of BATCH-CASEBOX-DOCKET-LIFECYCLE-00 (WI-D3/D4 still queued).
**Note**: range includes the prior WI-D1 closeout doc `8f9578e` (wrote `316417f` into the marker), the WI-D2 implementation `066e1ab`, and the merge `08f0a7f`.

<!-- batch-audit-attestation v1
range_base: 316417fb2b59f5c46e9abd74293422f84e6bb5a2
target_sha: 08f0a7f46b7b3a5937c362ce77337281af0cdfb4
verdict: BATCH-PASS
findings: C0 H0 M0 L1
broker_job_id: audit-mq33ck2r-9bn7o1
broker_kind: audit
broker_output_sha256: 101af8cd0325dcd13e90006f54a125d7275f600072a36c33b4cb9d1f5a6990b4
-->

## Tasks completed in the batch range

| Commit | PR | Behavior |
|---|---|---|
| `8f9578e` | (workflow) | DOC-ONLY: the prior WI-D1 Layer-B closeout's study packet (`dev-memo/study/2026-06-05-batch-audit-66.md`, range `82f922f..316417f`) + a `batch-closeout-log.md` line. Advanced the marker to `316417f`. |
| `066e1ab` | #64 | WI-D2: `casebox:docket:dismiss` write IPC handler — PROPOSED-only, fail-closed scoped `getDocketEntry` preflight, server-injected `dismissal_actor_user_id` + `dismissed_at`, 8 forbidden authority/timestamp/state fields rejected, required `dismissal_reason`, projectRow over `DOCKET_ENTRY_RESPONSE_FIELDS`. +11 tests (suite 464→475). No persistence/contract/renderer change. |
| `08f0a7f` | #64 | GitHub PR merge of WI-D2 to main. |

## Decisions (alternatives, why chosen, what would falsify)

1. **Proposed-only at the IPC layer** (persistence allows proposed|confirmed→dismissed) — dismissing a confirmed entry would orphan its materialized deadline, out of slice; the handler asserts `confirmation_state==="proposed"` after the scoped preflight, before the write. Falsifier: a path letting a confirmed entry reach `dismissDocketEntry` (audit confirmed none).
2. **Defer the `dto.ts` 797/800 LOC finding** rather than split now — extraction adds new files outside WI-D2's Allowed-files; recorded as `DTO-LOC-797` (must precede the next DTO-adding WI). Falsifier: a WI-D2 diff that split dto.ts (it did not).

## Audit findings + disposition

- **C0 / H0 / M0 / L1.** Scope holds (5 src/test files + dev-memo; no renderer/services/contracts/settings/hooks/marker/breaker change; read+single-write only, no deadline transition). Proposed-only + fail-closed scoping + authority injection + mandatory projection all hold (no deadline-orphan path; no authority leak). Governance content-bound (`queue_sha256` recomputed at `08f0a7f` matches `queue.md`). Prior closeout `8f9578e` docs-only.
- **L1 = `DTO-LOC-797`** (carried, tracked open in `dev-memo/deferred-audit-findings.md`): `dto.ts` 797/800 LOC; split into per-entity DTO modules behind a barrel before the next DTO-adding WI. Cleanup/refactor-only; no behavioral/security impact; safe-to-proceed YES.
- Per-WI: audit `audit-mq32ptpx-2pen80` (C0 H0 M0 L3) → verify `verify-mq32ydh3-nrx2x7` (ALL CLOSED — 2 test Lows fixed in-WI, L3 deferred).

## Batch state after this closeout

Advances `dev-memo/run/last-batch-audit` from `316417f` to `08f0a7f`, clearing the breaker. **WI-D1 (list) + WI-D2 (dismiss) IPC handlers are merged** — the renderer can now (once WI-D4 lands) read durable proposed docket entries and dismiss them. **Remaining in BATCH-CASEBOX-DOCKET-LIFECYCLE-00 (queued):** WI-D3 (design artifact), WI-D4 (renderer bridge + pending-proposals UI + dismiss UI, design-gated). Open deferred findings: `DTO-LOC-797` (Low); ledger otherwise 0 High / 0 Medium.
