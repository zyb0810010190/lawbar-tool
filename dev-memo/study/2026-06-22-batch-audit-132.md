# Batch-audit study packet — WI-ENA1 govern + impl + PR #103 merge window

**Date**: 2026-06-22. **Range**: `5d825e0..34e6c43` (`main` / `evidence-native-pdfkit-probe`).
**Type**: GOVERNANCE + SCAFFOLD + MERGE (closeout + WI-ENA1 queue governance + the SwiftPM skeleton /
macOS CI smoke + the PR #103 merge into main).

## What shipped in this range
- `88df8ef` — `chore(workflow): batch-audit closeout 02ce88d..5d825e0` (study packet 131 + closeout-log line;
  advanced marker to `5d825e0`).
- `ccaf0ba` — `chore(workflow): govern queue — WI-ENA1 SwiftPM skeleton + macOS CI smoke` (queue.md +
  queue.linted/reviewed/governed + queue-review-054; also fixed the ENA0-REVIEW-L1 stale queue-sha typo in
  queue-review-053). queue.governed content-bound to queue.md sha `9506c53f…`.
- `bb4ef85` — `scaffold(evidence): SwiftPM skeleton + macOS CI smoke (WI-ENA1)` — adds
  `native/evidence-core-swift/**` (Package.swift, EvidenceCoreSmoke source + 2 XCTest cases, README, .gitignore)
  and `.github/workflows/evidence-core-swift-smoke.yml` (macos-latest; `swift build` + `swift test` only).
- `34e6c43` — `Merge pull request #103 …` — content-empty merge of WI-ENA1 (SwiftPM skeleton + macOS CI smoke)
  into main.

No product source/schema/contract/test/dependency/UI delta beyond the skeleton Swift package. No PDFKit, no
geometry, no A0.7 harness, no marker, no provenance/HMAC, no Evidence UI. The existing `native/evidence-core`
JS deterministic-JSON shim is byte-unchanged (absent from the diff).

## Audit trail
- `audit-mqpwtbw8-c47zs2` (Path 1 runner v0.2.18, native `--background`, gpt-5.5 / high / read-only) over
  `5d825e0..34e6c43`: **BATCH-PASS C0 H0 M0 L0**. Range + verdict declared on their own lines. Codex confirmed:
  range aligned with WI-ENA1 (skeleton + macOS smoke only); package exposes only `smokeVersion` / `smoke()`,
  zero package dependencies; workflow runs only `swift build` / `swift test`; no PDFKit, geometry, A0.7 harness,
  marker, file writes, HMAC/provenance, or Evidence UI; JS shim byte-unchanged. rawOutput sha256 `63159662…`.
  - Required recording: kind=audit; scope=`5d825e0..34e6c43` (4 commits: closeout + governance + skeleton + merge);
    runner=`~/.claude/plugins/cache/xiaolai/cc-suite/0.2.18/scripts/codex-runner.mjs`;
    model/effort/sandbox=gpt-5.5/high/read-only; jobId=`audit-mqpwtbw8-c47zs2`; status=retrievable=YES.
  - Failure classification: first attempt `audit-mqpvpnl9-7ogsx6` = **TIMEOUT** (`spawnSync codex ETIMEDOUT`);
    retried per cc-suite §"Timeout / failure classification" with a tighter diff-bounded prompt (attempt 2),
    which succeeded on Path 1 native `--background`. Logged to `dev-memo/cc-suite-reliability-log.md`.

## Low disposition
- None. C0 H0 M0 L0.

## State
- Clears the audit-DUE window for `5d825e0..34e6c43` and advances the marker to `34e6c43`. PR #103 (SwiftPM
  skeleton + macOS CI smoke) is merged to main. This closeout runs on the new branch
  `evidence-native-pdfkit-probe`, opening WI-ENA2 (PDFKit compile/import probe — the next authorized
  implementation hard-stop).

**Verdict: BATCH-PASS C0 H0 M0 L0** (safe to proceed).

<!-- batch-audit-attestation v1
range_base: 5d825e072e8b5fb9aa0ef836dfb716bd363286d9
target_sha: 34e6c43f9b0654f72370714f4a6ff199a5cb6e0a
verdict: BATCH-PASS
findings: C0 H0 M0 L0
broker_job_id: audit-mqpwtbw8-c47zs2
broker_output_sha256: 631596621b50ece2ca0f38fedc64aa89db2aa37a886502fc629deb3018dc7f7b
-->
