# Batch-audit study packet — BATCH-CLOSEOUT-AUTO-00 (#37) + PRC-1 (#38), Layer B

**Date**: 2026-06-04.
**Batch range**: `9ae690c..866fd7b` (4 commits since the prior `last-batch-audit` marker `9ae690c`).
**Trigger**: `BATCH_AUDIT_EVERY=3`; 4 commits ≥ 3 → `batch-commit-guard.sh` breaker fired DUE.
**Audit broker job**: `audit-mpz7356b-m2gxa7` (Path 1 runner, `gpt-5.5`, effort high, sandbox read-only, native `--background`).
**Verdict**: **BATCH-PASS (C0 / H0 / M0 / L0)**.
**Closeout**: first production use of `scripts/workflow/batch-closeout.mjs` (BATCH-CLOSEOUT-AUTO-00).

<!-- batch-audit-attestation v1
range_base: 9ae690cf4a3dbee02220e5962d5b88600cf89907
target_sha: 866fd7b1f71e4cdb25782de52d8a1169311e1aeb
verdict: BATCH-PASS
findings: C0 H0 M0 L0
broker_job_id: audit-mpz7356b-m2gxa7
broker_kind: audit
broker_output_sha256: 2499e5fa5ffaad240e0a33b5aeb7651f23aacfe113366f41f6df440620226b51
-->

## Tasks completed in the batch

| Commit | PR | WI | Behavior |
|---|---|---|---|
| `96a2314` | #37 | BATCH-CLOSEOUT-AUTO-00 | Automate the Layer-B batch-audit marker advance: `scripts/workflow/batch-closeout.mjs` (verified, range-bound broker binding, transactional behind a `.closeout-pending` sentinel) + an ADDITIVE `batch-commit-guard.sh` sentinel deny + interpreter-inline mutation tightening + `.closeout-pending` protection in the two run-control hooks. |
| `060e643` | #38 | PRC-1 | Append-only enforcement for `dev-memo/run/log.md`: `protect-run-control.sh` routes `*log.md*` Write/Edit/MultiEdit to a new side-effect-free Node verifier `logmd-append-guard.mjs` (create-only Write; Edit/MultiEdit simulated; `result.startsWith(current)`); lexical canon; fail-closed. |
| `1c4e80e`, `866fd7b` | #37, #38 | — | GitHub PR merges to main. |

## Decisions (alternatives, why chosen, what would falsify)

1. **Marker advance automated, breaker untouched in spirit.** #37 added only an *additive* deny to `batch-commit-guard.sh` (sentinel) — a tightening, never a weakening — under explicit user authorization. Falsifier: if a commit that the count/governed/risk logic would have blocked is ever permitted, the additive claim is void.
2. **PRC-1 solved as append-only, not the rejected blanket block.** The DISPUTED blanket Write/Edit deny would have broken the lifecycle's own append step; the verifier enforces append-only instead. Falsifier: a legitimate lifecycle append wrongly denied.
3. **Symlink/path-indirection deferred to PRC-2** (PRC-1 uses lexical canon only) — recorded in `deferred-audit-findings.md`.

## Audit findings + disposition

- **C0 / H0 / M0 / L0.** The two run-control hook changes compose coherently (`.closeout-pending` protected as authority state; `log.md` routed to the append-only verifier before the blanket deny). No cross-WI duplication, dead code, unrequested abstraction, ordering conflict, or new runtime dependency. Both WIs trace to authorized plans. No product/UI paths touched.

## Per-WI provenance (already independently verified)

- #37: audit `audit-mpytolwn-mfi5rq` → verify `verify-mpyu6pti-ujo5qg` (ALL CLOSED).
- #38: audit `audit-mpz56jzu-6xiqep` → verify `verify-mpz5fasz-5quylr` (ALL CLOSED).
- This Layer-B pass checks the ACCUMULATION, not the per-WI line findings.
