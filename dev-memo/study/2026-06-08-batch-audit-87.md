<!-- batch-audit-attestation v1
range_base: 6e388686f16bfa0e345cf198b9b3905e4d75e3f1
target_sha: f0259d4e78e483d8364d4c0869ccb541f825d732
verdict: BATCH-PASS
findings: C0 H0 M0 L0
broker_job_id: audit-mq53bl6f-vkr64c
broker_output_sha256: 88f61fa99c28f9f49b647c461005d762e3c3cf26ced4327f08d1a4feeaf03509
-->

# Batch audit 87 — BATCH-WORKFLOW-CONTRACT-INTEGRITY-GUARD-00 (batch-end, post merge #80)

**Window**: `6e38868..f0259d4` (3 commits). **Verdict**: BATCH-PASS (C0 H0 M0 L0).
**Broker**: Path 1 runner v0.2.18 native --background, gpt-5.5/high/read-only, job `audit-mq53bl6f-vkr64c`.
**Date**: 2026-06-08. **Closes BATCH-WORKFLOW-CONTRACT-INTEGRITY-GUARD-00.**

## Tasks in window
| Commit | What |
|--------|------|
| `ef8f6c8` | Mid-batch closeout (post WI-CI1). |
| `0d670d5` | WI-CI2 — AGENTS.md contract-doc no-autofix note (prose-only). |
| `f0259d4` | Merge PR #80 to main. |

## Batch summary (both WIs)
Closes the AGENTS.md-corruption anomaly (an external editor markdown formatter corrupting an
always-loaded contract doc) by defending the commit boundary:
- **WI-CI1 (SCAFFOLD)**: `.claude/hooks/block-contract-corruption.sh` (PreToolUse Bash guard) +
  `scripts/workflow/check-contract-integrity.sh` (standalone checker) + `.claude/settings.json`
  hooks-key wiring. Denies a porcelain `git add`/`git commit` staging/committing a contract doc with
  a v1 corruption marker. Porcelain-only defense-in-depth; additive; over-deny-safe; fail-closed on
  inspection error; never mutates files.
- **WI-CI2 (WORKFLOW)**: AGENTS.md "Authoring discipline" subsection — never markdown-autofix the
  contract docs; the guard + checker enforce it; disable editor format-on-save.

End-to-end validation: the now-live WI-CI1 guard **allowed** the clean WI-CI2 AGENTS.md commit (and
would deny a corrupted one) — the guard protects its own contract-doc edit.

## cc-suite invocation log (this batch)
| Kind | Job | Outcome |
|------|-----|---------|
| review-plan x7 | …`mq4wrzzj`…`mq4x9wof` (6 FAIL hardening git-surface) | → `mq51266b` **GOVERNABLE** |
| audit (WI-CI1) | `audit-mq51mliu-40bz6f` | FAIL (High `git -C` bypass; Medium control structures; 2 Lows) → fixed |
| verify | `verify-mq52ywxx-81erjv` | ALL CLOSED + acknowledged Lows |
| audit (mid) | `audit-mq532ljl-0ddlp4` | BATCH-PASS C0 H0 M0 L0 |
| audit (end) | `audit-mq53bl6f-vkr64c` | BATCH-PASS C0 H0 M0 L0 |

Mid-run reliability: Path-1 timeouts on the final review-plan confirm + Path-2 model-block (ChatGPT
account rejects the offered codex models) → escalated to user → bounded Path-1 retry authorized → PASS.

## Mistakes / near-misses
- The newly-wired guard over-denied a non-git Bash command whose heredoc PROSE mentioned git add/commit
  (broke an audit-launch + a test harness). Root cause: tokenizing the whole command incl. heredoc
  bodies. Fixed: strip heredoc bodies before tokenizing; take NO action on tokenize-failure (exotic
  quoting is outside the clean porcelain surface). This is the canonical example of a guard needing to
  distinguish a real `git add/commit` invocation from prose mentioning one.
- The audit found a real `git -C <dir>` cwd bypass + a control-structure (`if git commit`) gap; both
  fixed (cwd-anchored resolution via repo-top + `git -C eff ls-files --full-name`; control-keyword
  skipping) and verified.

## Review-later
- Marker set is v1 (numeric HTML entity, quad-asterisk, escaped underscore). If a future legitimate
  contract doc needs one of these literally, the guard scope (contract docs only) + an explicit
  allow path would be designed in a follow-up.
- The existing `block-commit-stage-all.sh` shares the heredoc-prose false-positive class (it denies a
  `git commit` stage-all flag mentioned in heredoc prose) — pre-existing, out of this batch's scope.
- BRCBW-SUBSTR remains open. Cadence overhead persists (batches 74-87).
