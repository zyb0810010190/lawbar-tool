# Batch-audit study packet — WI-802/fact-transition-ipc (#57) + fresh-batch governance + prior closeout, Layer B

**Date**: 2026-06-05.
**Batch range**: `7b01a78..b6c0b8d` (4 commits since the prior `last-batch-audit` marker `7b01a78`).
**Trigger**: `BATCH_AUDIT_EVERY=3`; the #57 merge brought commits-since-marker to 4 (≥ 3) → `batch-commit-guard.sh` breaker DUE on post-merge main.
**Audit broker job**: `audit-mq1mh1aq-9wwz14` (Path 1 runner v0.2.18, `gpt-5.5`, effort high, sandbox read-only, native `--background`).
**Verdict**: **BATCH-PASS (C0 / H0 / M0 / L0)**.
**Closeout**: twentieth production use of `scripts/workflow/batch-closeout.mjs`. FIRST WI of the new batch BATCH-CASEBOX-FACT-REVIEW-00 (the prior BATCH-CASEBOX-UI-00 is closed and was NOT appended to).

<!-- batch-audit-attestation v1
range_base: 7b01a78d792b64e9b40e5936bf268fd6b41023bc
target_sha: b6c0b8df0749229227cd95955aa12e847292555c
verdict: BATCH-PASS
findings: C0 H0 M0 L0
broker_job_id: audit-mq1mh1aq-9wwz14
broker_kind: audit
broker_output_sha256: ee701371357f051b123934b00c7d55d77d7b1e245a4dab214af095b6ebd1cb82
-->

## Tasks completed in the batch range

| Commit | PR | Behavior |
|---|---|---|
| `afc40fb` | (workflow) | DOC-ONLY: the prior WI-704 closeout's study packet (`dev-memo/study/2026-06-05-batch-audit-57.md`, range `e15d833..7b01a78`) + a `dev-memo/batch-closeout-log.md` line. |
| `b9dd51c` | (workflow) | GOVERNANCE: replaced the CLOSED BATCH-CASEBOX-UI-00 queue with the FRESH batch BATCH-CASEBOX-FACT-REVIEW-00 (WI-802 only); queue.linted/reviewed/governed + `reviews/queue-review-009.md`. |
| `378efbb` | #57 | WI-802: tenant-scoped fact transition IPC — `CHANNEL.factTransition` + `transitionFactHandler` consuming the EXISTING persistence `transitionFact` + `getFact`; renderer DTO `{matterId, factId, to, rejection_reason?}`; server injects `reviewer_actor_user_id` + `at`; fail-closed scoped `getFact` preflight before the unscoped `transitionFact`; dedicated allowlist projection; state machine stays in persistence (`illegal_transition` surfaced). New `tests/ipc-fact-handlers.unit.test.mjs` (16 tests) + 4 dto-contract; suite 422→442. |
| `b6c0b8d` | #57 | GitHub PR merge of WI-802 to main. |

## Decisions (alternatives, why chosen, what would falsify)

1. **Reuse the existing contract + persistence; add only IPC** — `transitionFact` + `getFact` + the status enum already exist, so WI-802 is IPC-only (Type IMPL, no contract/persistence change). Falsifier: any `services/case-box-persistence/src` or `docs/contracts` change in the range (none — audit A3).
2. **State machine stays in persistence** — the handler validates DTO shape + the `to` enum + the rejection-reason rule, then forwards; it does NOT block `candidate→accepted`, which reaches persistence and surfaces `illegal_transition` (no-auto-accept ADR respected). A review-plan Critical (`review-plan-mq1loihw-mgoinb`) caught an earlier acceptance that wrongly treated `candidate→accepted` as valid; corrected and confirmed vs the ADR + `state-machine.test.mjs`. Falsifier: the handler hard-coding allowed edges. Per-WI audited `audit-mq1m2url-8cy905` (ACCEPT C0 H0 M0 L0) + verified `verify-mq1m56zf-42fmv8` (ALL CLOSED).
3. **Fail-closed scoped getFact preflight** — `transitionFact(factId, …)` is unscoped, so a `getFact({tenant_id, matter_id, fact_id})` preflight rejects a foreign/unknown/wrong-tenant `factId` before transitioning (mirrors WI-601 docket-confirm). Server injects `reviewer_actor_user_id` + `at`; renderer forwards only `{matterId, factId, to, rejection_reason?}`; the response strips authority identities.
4. **Tests in a new dedicated file** — the 1534-LOC `ipc-handlers.unit.test.mjs` monolith is NOT touched (Forbidden); transition tests live in `ipc-fact-handlers.unit.test.mjs`, wired into the gate.

## Audit findings + disposition

- **C0 / H0 / M0 / L0.** Clean accumulation. Existing create/list paths unchanged; the transition DTO blocks server-authority + lifecycle fields and rejects unknowns; response projection strips `tenant_id`/`actor_user_id`/`reviewer_actor_user_id` (A1). `candidate→accepted` is not special-cased and surfaces `illegal_transition` from persistence (A2). No forbidden-area change — no persistence src / contracts / renderer / `errors.ts` / `electron/main.ts` / hook / marker-breaker source / dependency / test deletion; `ipc-handlers.unit.test.mjs` UNCHANGED; `ipc-fact-handlers.unit.test.mjs` in the gate (A3). Governance consistent + a genuinely FRESH batch (queue.md = WI-802 only) (A4). No deferred-findings disposition required (L0).

## Per-WI provenance (already independently verified)

- #57 WI-802: review-plan `review-plan-mq1loihw-mgoinb` (NEEDS-FIX, Critical candidate→accepted + 2 Medium) → `review-plan-mq1lrx4k-pe42tb` (READY TO GOVERN) → audit `audit-mq1m2url-8cy905` (ACCEPT C0 H0 M0 L0) → verify `verify-mq1m56zf-42fmv8` (ALL CLOSED). Gate `npm --prefix apps/lawbar-desktop test` = 442 pass / 0 fail.
- `b9dd51c` governance: queue-lint PASSED; review-plan `review-plan-mq1lrx4k-pe42tb` READY TO GOVERN; `reviews/queue-review-009.md`.
- `afc40fb` is the doc-only product of the prior WI-704 closeout (`audit-mq12te5b-8sdz5u` BATCH-PASS for `e15d833..7b01a78`).
- This Layer-B pass checks the ACCUMULATION; clean.

## Batch state after this closeout

This closeout advances `dev-memo/run/last-batch-audit` from `7b01a78` to `b6c0b8d`, clearing the breaker. BATCH-CASEBOX-FACT-REVIEW-00 WI-802 (transition IPC) is merged + closed — the backend review path is complete. Remaining (NOT started): WI-803 (fact-review design artifact via Claude Design) then WI-804 (review bridge + matter-view controls + status rendering, Type UI). The optional WI-805 (split the 1534-LOC `ipc-handlers.unit.test.mjs` monolith) remains a recommended independent cleanup.
