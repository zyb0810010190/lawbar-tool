# Batch audit 276 — WI-PTA-VS0 (party-identity foundation) window

**Range:** `44a21d1..d87fb96`. **Branch:** `feature/pta-claimtrack-vertical-slice`.

## Window contents (3 commits)
- `38f090e` — prior batch-audit closeout (study 275 + closeout-log; governance-only).
- `9638278` — **WI-PTA-VS0 Phase A**: additive audit-event kind `MATTER_PARTY_IDS_ASSIGNED`
  (contract schema enum + `CASE_BOX_AUDIT_EVENT_KINDS` map + regenerated type + 3 tests + ADR).
  Individually audited CLEAN (`audit-mscwa84f`, C0 H0 M0 L0).
- `d87fb96` — **WI-PTA-VS0 Phase C**: matter party-identity persistence (create-time party-ULID
  assignment + audited `ensureMatterPartyIds` backfill with fail-closed state-hash continuity;
  additive error code `audit_chain_desync`; bounded collision retry). Individually audited
  (`audit-mscx6unm`, C1 H1 M3) → 3 FIXED + verified ALL CLOSED (`verify-mscxmy96`); 2 dispositioned
  (VS0-TENANT-1 → VS-2 handler per `archiveMatter` precedent; VS0-PARTYID-SHAPE-1 → covered by the
  contract ULID `$ref`), plus cross-cutting VS0-STATEHASH-XCUT-1 recorded, all in
  `dev-memo/deferred-audit-findings.md`.

## Verdict (Layer-B backstop over the cumulative range)
- **AUDIT-RANGE: 44a21d1583b59f3bd91ca7244a706919315bc6e0..d87fb96a9c4cecb786ef4cabccf8972b2fd91704**
- **AUDIT-VERDICT: BATCH-PASS C0 H0 M0 L0** — no unaddressed cross-cutting defects. Job
  `audit-mscxsn5c-98tug4`, rawOutput sha256
  `b44ceb23188bcc5812860b81515025758125bf421386eda38a122c323c442837`.
- Confirmed holistically: additive-only (schema enum ↔ TS map both at 72 keys, no drift;
  `audit_chain_desync` additive in the error union; no rename/remove/merge; no existing event/error
  shape changed); `CURRENT_SCHEMA_VERSION` stays 12 (no DDL); no FK; no dependency/lockfile change;
  in-memory ↔ SQLite share one prepare path; the backfill appends one event only when needed, fails
  closed on payload↔last-event state-hash desync, and writes matter row + audit row + chain head in
  one immediate transaction; the new hardening + parity tests are wired into the package test command.

## Gate evidence (run interactively this lane, outside the read-only audit sandbox)
- `npm --prefix docs/contracts/case-box-contract test` — 542/0.
- `npm --prefix services/case-box-persistence test` — 614/273/288 = 1175/0 (abi-smoke OK).

## Outcome
VS-0 (party-identity foundation) is complete on the branch. VS-1 (ClaimTrack persistence) is
unblocked: `createClaimTrack` will reference party ids that now always pre-exist. All local commits;
no push.

<!-- batch-audit-attestation v1
range_base: 44a21d1583b59f3bd91ca7244a706919315bc6e0
target_sha: d87fb96a9c4cecb786ef4cabccf8972b2fd91704
verdict: BATCH-PASS
findings: C0 H0 M0 L0
broker_job_id: audit-mscxsn5c-98tug4
broker_output_sha256: b44ceb23188bcc5812860b81515025758125bf421386eda38a122c323c442837
-->
