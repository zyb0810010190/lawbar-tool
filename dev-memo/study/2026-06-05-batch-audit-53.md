# Batch-audit study packet — docs window (closeout + design artifacts), Layer B

**Date**: 2026-06-05.
**Batch range**: `3ed9ccb..3d70e9f` (3 commits since the prior `last-batch-audit` marker `3ed9ccb`).
**Trigger**: `BATCH_AUDIT_EVERY=3`; 3 commits ≥ 3 → `batch-commit-guard.sh` breaker fired DUE on main.
**Audit broker job**: `audit-mq0xofzl-f1bvbj` (Path 1 runner v0.2.18, `gpt-5.5`, effort high, sandbox read-only, native `--background`).
**Verdict**: **BATCH-PASS (C0 / H0 / M0 / L0)**.
**Closeout**: fifteenth production use of `scripts/workflow/batch-closeout.mjs`. This window is **documentation-only** — the prior batch closeout's own artifacts plus the case-box write-flow design intake (design-source request + two approved design artifacts). No product, renderer, persistence, or contract code.

<!-- batch-audit-attestation v1
range_base: 3ed9ccb3273cc5db06e68ec0d2db07454d6b6e4b
target_sha: 3d70e9f27315dda0f0ca81d7002c9e340a3ec0cd
verdict: BATCH-PASS
findings: C0 H0 M0 L0
broker_job_id: audit-mq0xofzl-f1bvbj
broker_kind: audit
broker_output_sha256: f46defdb44daab940080e990077192fd2b83bf4ba539d0c7768fc221e64a0c87
-->

## Tasks completed in the batch range

| Commit | Behavior |
|---|---|
| `90325fc` | DOC-ONLY: the prior batch-closeout's study packet (`dev-memo/study/2026-06-05-batch-audit-52.md`, range `7c30664..3ed9ccb`) + a `dev-memo/batch-closeout-log.md` line. |
| `d334032` | DOC-ONLY: design-source handoff `dev-memo/design-source/2026-06-05-casebox-write-flows-design-request.md` — a design REQUEST, explicitly non-authoritative; does NOT satisfy the `Design artifact:` gate. |
| `3d70e9f` | DOC-ONLY: two approved UI design artifacts `dev-memo/design/2026-06-05-casebox-fact-create.md` + `dev-memo/design/2026-06-05-casebox-deadline-create-confirm.md` (each: problem / smallest-slice / IPC contract / UI / error+success / a11y / tests / out-of-scope), satisfying the `Design artifact:` gate for the future Add-Fact and Add+Confirm-Deadline UI WIs. |

## Decisions (alternatives, why chosen, what would falsify)

1. **Design intake stays docs-only; renderer stays blocked** — the write-flow UI cannot be implemented until a human-authored design exists (`UI-GATES.md`). The design-source request and the two approved artifacts are the human "nouns"; no `renderer/**` was touched. Falsifier: any renderer/persistence/contract change riding in this window (none — see audit A1).
2. **design-source vs design separation** — the request lives under `dev-memo/design-source/` (excluded by `check-ui-design-artifact.sh`, non-authoritative), the approved artifacts under `dev-memo/design/` (gateable). Falsifier: the source file being treated as a valid `Design artifact:` reference (it is not).
3. **Design docs describe EXISTING IPC, propose no backend change** — the artifacts consume `casebox:fact:create` / `casebox:docket:create` / `casebox:docket:confirm` as merged (WI-601/602), keep server-authority fields server-injected, and faithfully reflect the two-step propose→confirm flow + the `timeline_event` conditional `as_of_date`. Falsifier: a design asserting a contract/schema/error-surface change (none — see audit A2/A3).

## Audit findings + disposition

- **C0 / H0 / M0 / L0.** No findings. The range touches only five `dev-memo/**/*.md` files; no renderer/UI, no `services/case-box-persistence/src`, no contract schema, no `errors.ts`/error-code surface, no manifest/dependency, no hook, no marker/breaker source tampering, no test deletion/weakening. The design artifacts match the existing IPC surface (narrow DTOs; authority/status/provenance forbidden from renderer input and injected server-side; allowlisted responses). No deferred-findings disposition required (L0).

## Provenance note

The broker audit job ran via Path 1 runner and returned a complete local audit with the range-bound `AUDIT-RANGE` + `AUDIT-VERDICT` lines. The auditor noted its own optional sub-delegated `cc-suite:audit` lane returned no usable output (`user cancelled MCP tool call`); the verdict is the runner job's local audit, which is the load-bearing artifact the closeout binds to (rawOutput sha256 `f46defdb…`). No timeout/fallback at the runner level; first-attempt success.

## Batch closure

This closeout advances `dev-memo/run/last-batch-audit` from `3ed9ccb` to `3d70e9f`, clearing the breaker. The two approved design artifacts remain committed and gateable; the Add-Fact and Add+Confirm-Deadline UI WIs are now eligible to carry a concrete `Design artifact:` line but are NOT yet queued, lint-checked, review-planned, or governed.
