# Batch-audit study packet — WI-A3-EXPORT-T1 lane window

**Date**: 2026-06-25. **Range**: `839a5c2..34f2d0f` (`evidence-a3-cascade-policy-design`, branched from `main`).
**Type**: CLOSEOUT + GOVERNANCE + IMPL + MERGE (the A3 export-citation builder implementation lane).

## What shipped in this range
- `a59983c` — `chore(workflow): batch-audit closeout 2ea7f97..839a5c2` (study 158; marker → `839a5c2`).
- `990494e` — `chore(workflow): govern queue — WI-A3-EXPORT-T1 …` (queue.* + queue-review-080; A0.7-gated,
  custody 9b).
- `5fee002` — `feat(case-box-persistence): add A3 headless export-citation builder` — new
  `src/sqlite/exportCitationQueries.ts` (buildExportCitations) + additive `src/index.ts` export +
  `tests/hardening-export-citation.test.mjs` (15 A3-EXPORT-T1 tests) + `package.json` test-script wiring.
- `34f2d0f` — `Merge pull request #129 …` — merge into main.

Confined to `services/case-box-persistence` (export builder + index export + test + package.json test wiring) +
`dev-memo/**` governance/closeout. NO `schema.ts` change, NO resolver (`linkStatusResolverQueries.ts`) change,
NO migration, NO `apps/**`/`native/**`/`scripts/workflow/**`/`.claude/**`/`.github/**`, NO dependency/package-lock.
`CURRENT_SCHEMA_VERSION` stays 11. The builder runs `resolveLinkStatuses` first (the only write, via the
resolver), then derives read-only one export-citation object per link (linkStatus + exportFlag + a DocumentPage
卷X页Y citation when safe); citation identity from V9 payload_json; flags BROKEN/NEEDS_REVIEW/NON_CITABLE/
AMBIGUOUS; valid never REPLACED; A10 no-drop; no audit events; no export-file rendering; no cascade/delete. The
impl-audit L1 (raw NUL bytes used as map-key separators) was fixed by an explicit `KEY_SEP` escape constant —
the merged source is plain UTF-8 text with no NUL bytes.

## Audit trail
- `audit-mqtl4ue9-m9sj1g` (Path 1 runner v0.2.18, native `--background`, gpt-5.5 / high / read-only) over
  `839a5c2..34f2d0f`: **BATCH-PASS C0 H0 M0 L0**. rawOutput sha256
  `9e33de9b74967b43380c1b9dd570048cb94b695b45401e40d68f2af7ac357d71`.
  - Required recording: kind=audit; scope=`839a5c2..34f2d0f` (4 commits: closeout + governance + impl +
    merge); runner=`~/.claude/plugins/cache/xiaolai/cc-suite/0.2.18/scripts/codex-runner.mjs`;
    model/effort/sandbox=gpt-5.5/high/read-only; jobId=`audit-mqtl4ue9-m9sj1g`; status retrievable=YES;
    first-attempt success (no TIMEOUT).
- (Lane-internal records, already audited: review-plan `review-plan-mqtjvkay-uiom96` READY CONFIRMED-A07-GATED
  CITATION-IDENTITY ACCEPTED-PAYLOAD-JSON; impl audit `audit-mqtkfmmm-yklchf` PASS C0 H0 M0 L1 — L1 NUL-bytes
  fixed + locally verified; human 9b gated PASS.)

## Low disposition
- None at the window level. C0 H0 M0 L0. (The lane-internal impl-audit L1 — NUL bytes — was fixed before the
  impl commit; the merged source is NUL-free, confirmed by this window audit.)

## State
- Clears the audit-DUE window for `839a5c2..34f2d0f` and advances the marker to `34f2d0f`. PR #129 (the A3
  export-citation builder) is merged to main. This closeout runs on the new branch
  `evidence-a3-cascade-policy-design`, OPENING WI-A3-CASCADE-00 — the anchor-delete cascade policy DESIGN lane
  (NOT A0.7-gated) that resolves the long-deferred anchor-delete/cascade policy (reject referenced-anchor
  deletion; no ON DELETE CASCADE; no silent link deletion) before any delete behavior is implemented.

**Verdict: BATCH-PASS C0 H0 M0 L0** (safe to proceed to the WI-A3-CASCADE-00 governance).

<!-- batch-audit-attestation v1
range_base: 839a5c2d05e708e75ae23634b8a09d01b233dc80
target_sha: 34f2d0f8874b473c86ddd18b28eb6be03ab9014a
verdict: BATCH-PASS
findings: C0 H0 M0 L0
broker_job_id: audit-mqtl4ue9-m9sj1g
broker_output_sha256: 9e33de9b74967b43380c1b9dd570048cb94b695b45401e40d68f2af7ac357d71
-->
