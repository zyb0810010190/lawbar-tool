# Batch audit 293 — CI trigger fix + client-data rule

**Range:** `d8ce725..eb7a7f5`. **Branch:** `feature/pta-claimtrack-vertical-slice` (local; no push).

## Window contents (3 commits)

- `8d6b6b6` — prior batch-audit closeout (study 292; governance only).
- `a130213` — **CI trigger fix.** `desktop-release-gates.yml` fired only on
  `paths: apps/lawbar-desktop/**`, while the desktop `pretest` chain runs `check:no-real-data:all` whose
  party-name allowlist guards `docs/contracts/**/fixtures/**`. A PR touching ONLY a contract fixture
  therefore never fired the gate protecting it — present in CI but structurally blind to its own use
  case. Added `docs/contracts/**` with an inline comment recording why.
  **Re-verification note:** the backlog item read "add a CI invocation of the full scan"; checking real
  code showed that was WRONG (pretest already chains it, CI already runs the desktop suite), so a new job
  would have been redundant churn. Classified OVERSTATED, implemented as the verified subset — the
  fifth time this session that `.claude/rules/plan-execution.md` §1 changed or eliminated a planned item.
- `eb7a7f5` — **NEW RULE `.claude/rules/client-data.md`.** Five obligations for real client material
  supplied from outside the repo: read-only in place; never into git; never to an external service
  (explicitly including an inlined `git diff` — a scrub commit's `-` lines carry every identifier it
  removes); **never hand such a path to a sub-agent** (a clause with ZERO prior coverage anywhere in the
  repo); and no case identifiers in any committed artifact, with anonymous `DOC-nn` ids whose mapping
  stays in-session. Two narrow carve-outs, neither self-service: `dev-memo/run/intake/**` as the one
  sanctioned (sealed, gitignored) in-repo location, and redacted real material as fixtures ONLY by
  explicit per-instance user authorization per `go-live-plan.md` WI-11a.

## Why the rule was needed

The pre-flight recall found the gap was structural: `AGENTS.md` says NOTHING about client data (zero hits
for confidential/PII/intake); the only mechanical enforcement anywhere is one `.gitignore` line scoped to
one path (`bbf8419`); no hook covers client data; and the user's own WI-13 confidentiality instruction
(2026-07-09) was never promoted to a rule — it survived only as a repeated "out of scope" line in ~10
plan files. A posture living in ten plan footers lapses the moment someone works from a different plan.

## Broker audit result

`audit-mslvu62f-gp3zva` (cc-suite Path 1, foreground, gpt-5.5/high/read-only; 14 KB diff inlined, no
redaction needed).

Verdict **BATCH-PASS C0 H0 M0 L0 — no findings.** Independently confirmed: the CI edit changes only the
`pull_request.paths` filter — no trigger type, job, step, permission, or secret usage — and cannot newly
trigger a signing/release path; the new rule is enforceable (concrete, checkable obligations; it does NOT
mandate an unavailable plugin or a guard-disallowed edit path — the defect class this repo shipped twice
before); the two carve-outs are coherent and do not swallow the default prohibition; no weakening; no
contract-doc markdown corruption; and the rule text itself contains no client identifier.

## Carried forward to the next window (uncommitted at this attestation)

**WI-BATCH-REMEDIATION-LANE-00 is implemented but NOT committed** — 9 modified files + 1 new test file,
594 shell + 36 node assertions, 0 failures, desktop 953/952 unchanged. It should land as TWO commits:

1. **BRCBW-11 — a pre-existing HIGH under-denial in `block-run-control-bash-write.sh`,** found while
   trying to PROVE the plan's acceptance criterion "the agent cannot create the artifact". `scan_redir`
   split redirection targets on whitespace only, so a target terminated by an unquoted metacharacter
   escaped the authority check entirely: `echo X > dev-memo/run/config; echo done` was ALLOWED. This
   affected EVERY protected run-control file — including `human.override` and `last-batch-audit` — via
   the most obvious spelling an agent would type, i.e. an agent could have written its own override
   token. Verified pre-existing on committed `HEAD`. Fixed by truncating the target at the first
   `;`/`|`/`&`/`)`; 14 red-before regression cases. **This deserves its own commit** — burying a
   security fix inside a feature commit hides it from anyone scanning history for security changes.
2. **The remediation lane itself** — the narrow escape letting the commit that RESOLVES a FAILED batch
   audit actually land, per `dev-memo/plan-batch-remediation-lane-00.md`, with the user's §8 answer
   applied: **FAILED audits only, never merely-DUE.**

Disclosure: the plan's §7 review path specifies `/cc-suite:review-plan` on the plan before
implementation. That step was skipped for context reasons; the post-implementation batch audit of the
diff is the substitute. The audit reviews the implementation, not the design — recorded so the deviation
is visible rather than silent.

<!-- batch-audit-attestation v1
range_base: d8ce725f59c6ed0fd469a1b5849d48e769e9e7e5
target_sha: eb7a7f5e6e766eb00fa54d6305d9be7857505145
verdict: BATCH-PASS
findings: C0 H0 M0 L0
broker_job_id: audit-mslvu62f-gp3zva
broker_output_sha256: 634c76d3c7cb9c486b9f68f0da34edad8d91a1be9f1b8b8305469df4b07a48be
-->
