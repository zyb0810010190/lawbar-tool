# Batch-audit study packet — PRC-4-FU (#44) + prior closeout, Layer B

**Date**: 2026-06-04.
**Batch range**: `350a7ab..f446807` (3 commits since the prior `last-batch-audit` marker `350a7ab`).
**Trigger**: `BATCH_AUDIT_EVERY=3`; 3 commits ≥ 3 → `batch-commit-guard.sh` breaker fired DUE on post-merge main.
**Audit broker job**: `audit-mq05884f-juk8gv` (Path 1 runner v0.2.18, `gpt-5.5`, effort high, sandbox read-only, native `--background`).
**Verdict**: **BATCH-PASS (C0 / H0 / M0 / L0)**.
**Closeout**: sixth production use of `scripts/workflow/batch-closeout.mjs`; runs on post-merge `main`.

<!-- batch-audit-attestation v1
range_base: 350a7ab84f0df4ef656d6b94f4932269b07d3382
target_sha: f446807691f844532fb6ae3bf34811e6c6b8de30
verdict: BATCH-PASS
findings: C0 H0 M0 L0
broker_job_id: audit-mq05884f-juk8gv
broker_kind: audit
broker_output_sha256: 66cae977414ab1e66a279d99e49c9b1c6bb7986cd43ab1353634c217c4947c30
-->

## Tasks completed in the batch

| Commit | PR | Behavior |
|---|---|---|
| `bf12bed` | (workflow) | DOC-ONLY: the prior batch-closeout's study packet (`b60a061..350a7ab`, #43) + a `dev-memo/batch-closeout-log.md` line. Advanced the local gitignored marker; no source/code change. |
| `85a5ffd` | #44 | PRC-4-FU: ported the PRC-4 control-character-safe `deny()` JSON hardening (jq `-Rs` primary + tr-strip/sed fallback) to the two sibling emitters — `block-run-control-bash-write.sh` `emit_deny()` and `batch-commit-guard.sh` `deny()`. Tests +12 each (BRCBW 179→191, BCG-base 14→26). Only the emitter bodies changed. |
| `f446807` | #44 | GitHub PR merge of PRC-4-FU to main. |

## Decisions (alternatives, why chosen, what would falsify)

1. **Port the PRC-4 emitter body verbatim into the two siblings** (names + call sites + control flow unchanged) — NOT a broader emitter refactor. Falsifier: a reason byte/condition that still emits invalid JSON or empty output in either sibling. Audited Q1–Q7 in `audit-mpzp0vaz-bgvs2s`: none found.
2. **Scope = the two run-control-recorded siblings only.** The audit's Q7 surfaced two MORE emitters with the same pattern (`block-git-add-all.sh`, `block-commit-stage-all.sh`, staging-hygiene hooks) — NOT changed, recorded as **PRC-4-FU2** (open). Honest closure: PRC-4-FU closes the two recorded siblings, not a global "all emitters fixed" claim.
3. **Layer-B audits the ACCUMULATION, not per-WI lines** — #44 was already independently review-planned, audited, and verified.

## Audit findings + disposition

- **C0 / H0 / M0 / L0.** No accumulation drift. The PRC-4-FU emitter changes compose cleanly with the already-merged PRC-4 `protect-run-control.sh` `deny()` (three run-control emitters now share the hardened pattern); detection/gate/resolver/breaker logic in both hooks unchanged (only emitter bodies). The `bf12bed` closeout commit did not commit the local marker into source, did not tamper with the breaker, and only added a study packet + log line. No product/UI/contract/manifest/schema/fixture drift; no test deletion/weakening; no `OcrQueueError`-code or security-boundary surface change.
- Broker note: the read-only sandbox could not run the full hook suites (temp dirs / git locks blocked); the broker ran `git diff --check`, `bash -n`, surface inspection, and no-write JSON emitter checks (jq-present + jq-fallback) for both emitters. The full suites (191 BRCBW + 26 BCG-base + 54 protect-run-control + all siblings) passed locally on merged `main`.

## Per-WI provenance (already independently verified)

- #44 PRC-4-FU: review-plan `review-plan-mpzoupz6-3jbkia` (READY-WITH-LOW) → audit `audit-mpzp0vaz-bgvs2s` (PASS C0 H0 M0 L0; Q7 → PRC-4-FU2) → verify `verify-mpzp5hvd-3i02i6` (ALL CLOSED).
- `bf12bed` is the doc-only product of the prior closeout (`audit-mpzolzv6-43ld14` BATCH-PASS for `b60a061..350a7ab`).
- This Layer-B pass checks the ACCUMULATION, not the per-WI line findings.
