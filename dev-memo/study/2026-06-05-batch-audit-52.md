# Batch-audit study packet — WI-602/fact-write (#52) + prior closeout, Layer B

**Date**: 2026-06-05.
**Batch range**: `7c30664..3ed9ccb` (3 commits since the prior `last-batch-audit` marker `7c30664`).
**Trigger**: `BATCH_AUDIT_EVERY=3`; 3 commits ≥ 3 → `batch-commit-guard.sh` breaker fired DUE on post-merge main.
**Audit broker job**: `audit-mq0vp39e-tm6pqq` (Path 1 runner v0.2.18, `gpt-5.5`, effort high, sandbox read-only, native `--background`).
**Verdict**: **BATCH-PASS (C0 / H0 / M0 / L3)**.
**Closeout**: fourteenth production use of `scripts/workflow/batch-closeout.mjs`; runs on post-merge `main`. FINAL WI of the case-box write batch BATCH-CASEBOX-WRITE-00 → batch operationally closed.

<!-- batch-audit-attestation v1
range_base: 7c306648ac1e9b198211555d5e5dcd615e004471
target_sha: 3ed9ccb3273cc5db06e68ec0d2db07454d6b6e4b
verdict: BATCH-PASS
findings: C0 H0 M0 L3
broker_job_id: audit-mq0vp39e-tm6pqq
broker_kind: audit
broker_output_sha256: de289a8d320809bf67cf58e3833ecec8ba2633b3b13b24ca0e4ae96410904190
-->

## Tasks completed in the batch range

| Commit | PR | Behavior |
|---|---|---|
| `6fea65b` | (workflow) | DOC-ONLY: the prior batch-closeout's study packet (`bf8df09..7c30664`, #51) + a `dev-memo/batch-closeout-log.md` line. |
| `e398012` | #52 | WI-602: tenant-scoped fact create IPC (claims/timeline write path) — `CHANNEL.factCreate` + `createFactHandler` (manual lawyer-authored candidate fact), dedicated `CREATE_FACT_RESPONSE_FIELDS` allowlist stripping authority identities, matter-existence + active-tenant before `appendFact`, two `as_of_date` rules (date-only format always + required when `timeline_event`). +12 IPC tests; suite 379→391. |
| `3ed9ccb` | #52 | GitHub PR merge of WI-602 to main. |

## Decisions (alternatives, why chosen, what would falsify)

1. **Fact create as a manual candidate fact** (status `candidate`, source_type `lawyer_authored`, all provenance/review fields null) — the only manual lawyer-authored entry shape `appendFact` accepts (it rejects non-candidate status; the candidate-status conditional requires reviewer/review/supersedes null). Falsifier: a renderer-supplied status/source_type/reviewer field reaching persistence, or a created fact whose authority identities cross the IPC boundary. Audited Q1–Q5 in `audit-mq0rxpx5-78c551` (PASS C0 H0 M0 L2); verify `verify-mq0s1mwz-bw7dro` ALL CLOSED.
2. **Two `as_of_date` rules** (date-only format whenever supplied + required when `timeline_event`) — resolved the review-plan Medium (`review-plan-mq0rkzy9-6lxii7` NEEDS-FIX → `review-plan-mq0rn6df-3mp3jx` READY). Format regex is intentionally not calendar-validity; `appendFact` applies schema semantic validation (defense in depth). Falsifier: a malformed/timestamped `as_of_date` reaching persistence, or a `timeline_event` fact persisted without one.
3. **Barrel-bypass kept in-scope** — `handlers.ts` was outside WI-602 Allowed-files (as in WI-601), so `createFactHandler` is imported directly; recorded as the extended CBW-601-BARREL (an L below).

## Audit findings + disposition

- **C0 / H0 / M0 / L3.** No accumulation drift. WI-602 is a strict ADDITION (one tenant-scoped fact-write channel + handler + DTO constants + tests) — it does not weaken WI-601's docket write path, the read handlers, or the existing matter/document write paths; the fact write enforces matter-existence + active-tenant before `appendFact`; the response uses a dedicated allowlisted projection stripping `tenant_id`/`actor_user_id`/`reviewer_actor_user_id`. No renderer/UI, no `services/case-box-persistence/src`, no contract schema, no `errors.ts`/error-code surface, no manifest/dependency, no hook, no marker/breaker source tampering, no test deletion/weakening.
- **L3 — all DISPOSITIONED (deferred, safe-to-proceed YES; recorded in `dev-memo/deferred-audit-findings.md`)**:
  - **CBW-601-BARREL** (cleanup-only, extended): `factHandlers.ts`'s `createFactHandler` is direct-imported (not via the `handlers.js` barrel) because `handlers.ts` was outside WI-602's governed Allowed-files — same as WI-601's docket handlers. Functional + tested; reconcile in a single barrel-reconciliation WI.
  - **CBW-602-PURPOSE-ENUM** (cleanup-only): `FACT_PURPOSES` re-lists the schema enum; mitigated because `persistence.appendFact` applies the schema enum validation at write (fail-closed). Target: export the contract enum / add a parity test.
  - **CBW-602-FORBIDDEN-TESTS** (test-completeness): the forbidden-field IPC test samples only `actor_user_id`; the boundary is enforced by construction (`CREATE_FACT_FORBIDDEN_FIELDS` loop + unknown-field DTO-allowlist guard) and exercised by the existing forbidden + unknown-field tests. Target: table-driven forbidden-field test.

## Per-WI provenance (already independently verified)

- #52 WI-602: review-plan `review-plan-mq0rkzy9-6lxii7` (NEEDS-FIX, one Medium) → revised → `review-plan-mq0rn6df-3mp3jx` (READY) → audit `audit-mq0rxpx5-78c551` (PASS C0 H0 M0 L2) → verify `verify-mq0s1mwz-bw7dro` (ALL CLOSED + DEFERRED-PER-WI Lows). Gate `npm --prefix apps/lawbar-desktop test` = 391 pass / 0 fail.
- `6fea65b` is the doc-only product of the prior closeout (`audit-mq0q7zj2-tq0fwy` BATCH-PASS for `bf8df09..7c30664`, #51).
- This Layer-B pass checks the ACCUMULATION across `7c30664..3ed9ccb`, not the per-WI line findings.

## Batch closure

BATCH-CASEBOX-WRITE-00 had exactly two governed WIs: WI-601 (docket-entry create+confirm IPC, #51, merged) and WI-602 (fact create IPC, #52, merged). Both are on main. The governed queue holds no further WIs. After this closeout advances `dev-memo/run/last-batch-audit` to `3ed9ccb`, the batch is operationally closed.
