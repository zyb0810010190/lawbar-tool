<!-- batch-audit-attestation v1
range_base: f0259d4e78e483d8364d4c0869ccb541f825d732
target_sha: d0387577a09663da549ab479407777e8cc7bc5d8
verdict: BATCH-PASS
findings: C0 H0 M0 L1
broker_job_id: audit-mq57iffm-hqi18k
broker_output_sha256: c50c9d1e5011780c0c48f61eebe4320825cde2881deb6a778d80ab11d92850a5
-->

# Batch audit 88 — BATCH-CASEBOX-AUDIT-EVENT-KIND-V2-CONTRACT-00 (mid-batch, post WI-V1)

**Window**: `f0259d4..d038757` (3 commits). **Verdict**: BATCH-PASS (C0 H0 M0 L1).
**Broker**: Path 1 runner v0.2.18 native --background, gpt-5.5/high/read-only, job `audit-mq57iffm-hqi18k`.
**Date**: 2026-06-08. **Mid-batch breaker reset** (advances `last-batch-audit` to `d038757` so WI-V2
can commit). Batch NOT complete.

## Tasks in window
| Commit | What |
|--------|------|
| `7776031` | Prior batch (contract-integrity guard) final closeout doc. |
| `12a04f8` | Governed BATCH-CASEBOX-AUDIT-EVENT-KIND-V2-CONTRACT-00 (5-round review-plan → GOVERNABLE). |
| `d038757` | WI-V1 — audit-event-kind v2 contract: schema + regenerated type + audit-log.ts v1/v2 canonicalization branch + verifyAuditChain consistency check + buildCaseBoxAuditEvent + new v2 test file + package.json test-line. |

## WI-V1 summary
New audit events carry hashed `event_kind` + `audit_schema_version=2` via versioned canonicalization:
v1 byte-identical (golden untouched); v2 = 14 fields; verifyAuditChain enforces the ADR §4
`event_kind ↔ {action,entity_type,reasonRequired}` consistency at the verification boundary
(errorReason `event_kind_inconsistent`), making met/missed/withdrawn tamper-evident; mixed v1/v2
chains verify. 393 contract tests pass. Per-WI audit `audit-mq57c37l-cs6nv6` PASS-WITH-FINDINGS
(C0 H0 M0, Low downgrade/version tests added) → verify `verify-mq57fzl9-2mrhsu` Low 1 CLOSED.

## Audit findings + disposition
- **C0 H0 M0** — changed files limited to the 5 allowed contract files + governance/run; validators.test.mjs
  unchanged; v1 byte-identical; no services/apps/SQL/dependency change; package.json only the test-line.
- **L1 (DT-V1-L1) — DEFERRED (cleanup-only doc wording).** The schema description (and the regenerated
  type comment) say "The TS validator additionally enforces that the kind's declared {action,
  entity_type, reasonRequired} matches the event." The enforcement actually lives in `verifyAuditChain`
  (the chain verifier), not `validateAuditEvent` (which is schema-only). Behavior is correct (the WI
  deliberately enforces at the verification boundary); only the description string is imprecise. **Reason
  for deferral**: cleanup-only, no behavioral/correctness/security impact; fixing it would re-open the
  already-committed WI-V1 schema + regenerate the type + re-audit, disproportionate for a one-phrase doc
  nit. **Target**: the next contract-touching WI (e.g. a future audit-kind follow-up); recorded in
  `dev-memo/deferred-audit-findings.md` (row DT-V1-L1) and committed with WI-V2 (which allows that file).
  **Safe-to-proceed**: YES.

## Review-later
- DT-V1-L1: correct the schema/generated description "TS validator" → "chain verifier (verifyAuditChain)".
- BRCBW-SUBSTR remains open. Cadence overhead persists (batches 74-88).
