# Batch-audit study packet — WI-A3-LINK-CREATE-AUDIT-KIND contract window

**Date**: 2026-06-26. **Range**: `9b0067d..ba0cf3e` (`evidence-a3-link-create-audit-kind`, branched from `main`).
**Type**: CLOSEOUT + MERGE + SOURCE (the `LINK_CREATED` audit-event-kind contract addition).

## What shipped in this range
- `c1a93e5` — `chore(workflow): batch-audit closeout 29393bb..9b0067d` (study 169; marker → `9b0067d`).
- `a46ceea` — `Merge pull request #138 …` (the WI-A3-LINK-CREATE-DESIGN-00 design ADR into `main`; design-only doc).
- `ba0cf3e` — `feat(case-box-contract): add LINK_CREATED audit-event kind (WI-A3-LINK-CREATE-AUDIT-KIND)` —
  additive, append-only: `event_kind` gains `LINK_CREATED { action: create, entity_type: link, reasonRequired:
  false }` (schema enum + `CASE_BOX_AUDIT_EVENT_KINDS`); regenerated `src/generated/case-box-audit-event.ts`
  (AUTO-GENERATED banner intact); 2 LINK_CREATED cases + the tuple assertion in `audit-event-kind-v2.test.mjs`;
  the record dev-memo. The `link` entity_type already existed (PR #136) — no entity_type change.

Confined to `docs/contracts/case-box-contract/**` (schema + `src/audit-log.ts` + generated type + v2 test) +
`dev-memo/**` governance/closeout/record + the merged design ADR (`docs/adr/**`, design-only). NO `services/**`
change, NO SQLite schema / `CURRENT_SCHEMA_VERSION` change, NO emitter/operation, NO resolver/export/UI/IPC change,
NO canonicalization/verifier-logic change, NO `apps/**`/`native/**`/`scripts/workflow/**`/`.claude/**`/`.github/**`,
NO dependency/package-lock, NO contract-package package.json change. Append-only; schema/TS/generated all at 53
event kinds with no drift (auditor mechanical check). Source plain UTF-8 text. No secrets / no marker/evidence-run.

## Audit trail
- `audit-mqux5j8u-h957hw` (Path 1 runner v0.2.18, native `--background`, gpt-5.5 / high / read-only) over
  `9b0067d..ba0cf3e`: **BATCH-PASS C0 H0 M0 L0**. rawOutput sha256
  `22c05a7597adeeee2814533553b8fe8c743555ceac2e04d04c4d04fbdb6cc0d2`.
  - Required recording: kind=audit; scope=`9b0067d..ba0cf3e` (3 commits: closeout + PR #138 merge + contract);
    runner=`~/.claude/plugins/cache/xiaolai/cc-suite/0.2.18/scripts/codex-runner.mjs`;
    model/effort/sandbox=gpt-5.5/high/read-only; jobId=`audit-mqux5j8u-h957hw`; status retrievable=YES;
    first-attempt success.
- (Lane-internal records, already audited: review-plan `review-plan-mquong84-3png0m` READY (1 Low done); impl
  audit `audit-mquosu3z-1qjpoo` PASS C0 H0 M0 L0; verify `verify-mquovm6q-mkgkz1` ALL CLOSED.)

## Low disposition
- None. C0 H0 M0 L0.

## State
- Clears the audit-DUE window for `9b0067d..ba0cf3e` and advances the marker to `ba0cf3e`. PR #138 (the
  link-create design ADR) is merged to `main`; the `LINK_CREATED` audit-kind contract support is committed
  (unmerged on this branch). With `LINK_CREATED` in the contract, the NEXT lane is the persistence impl
  `WI-A3-LINK-CREATE-T1` (the A0.7-gated `createLink` operation that emits `LINK_CREATED`).

**Verdict: BATCH-PASS C0 H0 M0 L0** (safe; contract-only; the emitter remains the future WI-A3-LINK-CREATE-T1).

<!-- batch-audit-attestation v1
range_base: 9b0067dd5bbd3d6acf3a8ceaa8082e051c2afc18
target_sha: ba0cf3ec58bf4f443edd2a6a9d22bd1088006728
verdict: BATCH-PASS
findings: C0 H0 M0 L0
broker_job_id: audit-mqux5j8u-h957hw
broker_output_sha256: 22c05a7597adeeee2814533553b8fe8c743555ceac2e04d04c4d04fbdb6cc0d2
-->
