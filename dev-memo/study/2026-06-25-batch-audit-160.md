# Batch-audit study packet — WI-A3-CASCADE-00 merge window

**Date**: 2026-06-25. **Range**: `34f2d0f..c81ce5a` (`evidence-a3-anchor-delete-refusal`, branched from `main`).
**Type**: CLOSEOUT + GOVERNANCE + DESIGN + MERGE (the A3-CASCADE-00 anchor-delete-policy DESIGN lane).

## What shipped in this range
- `11c6d9b` — `chore(workflow): batch-audit closeout 839a5c2..34f2d0f` (study 159; marker → `34f2d0f`).
- `6bd50ca` — `chore(workflow): govern queue — WI-A3-CASCADE-00 …` (queue.* + queue-review-081; design-only,
  not A0.7-gated).
- `8a0cea1` — `docs(evidence): A3 anchor-delete cascade policy — A3-CASCADE-00` — the anchor-delete-policy ADR +
  the `plan-batch-casebox-evidence-a3-cascade-policy-00.md` sequencing plan.
- `c81ce5a` — `Merge pull request #130 …` — merge into main.

Confined to `docs/adr/**` + `dev-memo/**`. No product source, no `schema.ts`, no migration, no
`apps/**`/`native/**`/`scripts/workflow/**`/`.claude/**`/`.github/**`, no dependency/package-lock, no
marker/ledger/key/evidence-run files, no `dev-memo/run/evidence/**`. The ADR is DESIGN-ONLY: it resolves the
anchor-delete cascade (reject referenced-anchor delete; no ON DELETE CASCADE; no implicit cascade; no silent link
deletion), keeps the resolver missing-anchor->broken as a corruption SAFETY NET (not a deletion path), preserves
the export A10 no-drop, requires the future delete to be atomic (one write txn; no-FK race), and DEFERS
unlink/break-link (separate future WI), the unreferenced-anchor delete (product decision), the audit-event shape,
and soft-delete/tombstone schema. No delete/unlink/cascade code; the future delete IMPL WI (WI-A3-DELETE-T1)
remains A0.7-gated (custody 9b).

## Audit trail
- `audit-mqtm5ajf-h7hdpg` (Path 1 runner v0.2.18, native `--background`, gpt-5.5 / high / read-only) over
  `34f2d0f..c81ce5a`: **BATCH-PASS C0 H0 M0 L0**. rawOutput sha256
  `570c1850f63aa599daaae8f5b09255284c468cecf51c2bb29f4b7796b1f5ada9`.
  - Required recording: kind=audit; scope=`34f2d0f..c81ce5a` (4 commits: closeout + governance + design +
    merge); runner=`~/.claude/plugins/cache/xiaolai/cc-suite/0.2.18/scripts/codex-runner.mjs`;
    model/effort/sandbox=gpt-5.5/high/read-only; jobId=`audit-mqtm5ajf-h7hdpg`; status retrievable=YES;
    first-attempt success (no TIMEOUT).
- (Lane-internal records, already audited: review-plan `review-plan-mqtla99t-tjae9k` READY
  CONFIRMED-DESIGN-ONLY-NOT-GATED CASCADE-POLICY REJECT-REFERENCED-DELETE-SOUND; design audit
  `audit-mqtlfwla-ba28oq` PASS C0 H0 M0 L1 — T1 sequencing ambiguity fixed; re-audit `audit-mqtlj3ll-3cpubo`
  PASS C0 H0 M0 L0.)

## Low disposition
- None. C0 H0 M0 L0.

## State
- Clears the audit-DUE window for `34f2d0f..c81ce5a` and advances the marker to `c81ce5a`. PR #130 (the
  A3-CASCADE-00 anchor-delete policy DESIGN) is merged to main. This closeout runs on the new branch
  `evidence-a3-anchor-delete-refusal`, OPENING WI-A3-DELETE-T1 — the A0.7-gated (custody 9b) referenced-anchor-
  delete REFUSAL guard (negative-path / refusal contract only; no physical unreferenced delete) that proves a
  referenced anchor cannot be deleted through the persistence API.

**Verdict: BATCH-PASS C0 H0 M0 L0** (safe to proceed to the WI-A3-DELETE-T1 governance).

<!-- batch-audit-attestation v1
range_base: 34f2d0f8874b473c86ddd18b28eb6be03ab9014a
target_sha: c81ce5a497f792a9b1696a87ccc18f90fe913e68
verdict: BATCH-PASS
findings: C0 H0 M0 L0
broker_job_id: audit-mqtm5ajf-h7hdpg
broker_output_sha256: 570c1850f63aa599daaae8f5b09255284c468cecf51c2bb29f4b7796b1f5ada9
-->
