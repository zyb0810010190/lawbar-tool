# Batch-audit study packet — WI-A3-LINK-UI-DESIGN-00 design + merge window

**Date**: 2026-06-26. **Range**: `9b70e08..e6c7526` (`evidence-a3-link-ui-design`, branched from `main`).
**Type**: CLOSEOUT + MERGE + DESIGN (the renderer UI design artifact for audited links).

## What shipped in this range
- `3b5d1d2` — `chore(workflow): batch-audit closeout 93976ca..9b70e08` (study 174; marker → `9b70e08`).
- `b8d199e` — `Merge pull request #142 …` (the link IPC/API implementation into `main`; already audited).
- `e6c7526` — `docs(design): renderer UI design artifact — audited evidence links (WI-A3-LINK-UI-DESIGN-00)` — the
  DESIGN-ONLY artifact (`dev-memo/design/2026-06-26-audited-evidence-links-ui.md`, sections 1-12) + governance
  (queue.* + queue-review-095). Authorizes no code.

Confined to `dev-memo/**` (the design artifact + governance/closeout) — the design commit is dev-memo-only. The
#142 merge brings the already-audited link IPC impl into the base. NO NEW product runtime in this window's design
commit: no `services/**`, no `apps/**` renderer/UI source, no `schema.ts` / `CURRENT_SCHEMA_VERSION` change, no
`docs/contracts/**`, no `package.json`, no dependency, no native. Source plain UTF-8 text. No secrets / no
marker/evidence-run committed.

## Audit trail
- `audit-mqv3fozv-djfa9o` (Path 1 runner v0.2.18, native `--background`, gpt-5.5 / high / read-only) over
  `9b70e08..e6c7526`: **BATCH-PASS C0 H0 M0 L1**. rawOutput sha256
  `1efb2a2b9dc6b1ea6754e20df21e417b5cb942da9bd494a49ecc6d5e3512849f`.
  - Required recording: kind=audit; scope=`9b70e08..e6c7526` (3 commits: closeout + PR #142 merge + design);
    runner=`~/.claude/plugins/cache/xiaolai/cc-suite/0.2.18/scripts/codex-runner.mjs`;
    model/effort/sandbox=gpt-5.5/high/read-only; jobId=`audit-mqv3fozv-djfa9o`; status retrievable=YES;
    first-attempt success.
- (Lane-internal records, already audited: review-plan `review-plan-mqv2zdee-z5aoms` READY (2 Lows folded); design
  audit `audit-mqv34npj-glwp5d` FAIL→fixed (M1 list-row-no-flags + L1 single-error-model); verify
  `verify-mqv3awo3-9p3uz8` ALL CLOSED.)

## Low disposition
- **L1 (deferred — governance-text drift, no product/runtime impact)**: `LINK-UI-DESIGN-D4`. The committed
  `dev-memo/run/queue.md` Scope/decision-2/acceptance prose still describes export flags as part of the link-ROW
  state, which lags the FINAL design artifact's corrected contract (rows are `RendererLink` only — status +
  lifecycle; export flags live only in the export-citations panel). The authoritative artifact is correct; only the
  queue prose drifted (the queue was authored before the design audit's M1 fix). Cleanup-only; safe-to-proceed.
  Target: fold the queue-prose reconciliation into the same docs-only cleanup lane as D2 (errorMap prose) + D3 (IPC
  ADR §2 table); recorded in `dev-memo/deferred-audit-findings.md`.

## State
- Clears the audit-DUE window for `9b70e08..e6c7526` and advances the marker to `e6c7526`. The renderer UI design
  artifact is committed (unmerged). The A3 link program: persistence lifecycle + IPC surface live on `main`; the
  UI design is the predecessor for the renderer UI impl (WI-A3-LINK-UI-T1, the next lane, citing this artifact).

**Verdict: BATCH-PASS C0 H0 M0 L1** (safe; the L1 is a deferred governance-prose cleanup, not a runtime/security
defect).

<!-- batch-audit-attestation v1
range_base: 9b70e08dd6c54577cb99ef4b7afa64a14eb78ca0
target_sha: e6c752685c380d8bebe266a8ef541a747c65c2e8
verdict: BATCH-PASS
findings: C0 H0 M0 L1
broker_job_id: audit-mqv3fozv-djfa9o
broker_output_sha256: 1efb2a2b9dc6b1ea6754e20df21e417b5cb942da9bd494a49ecc6d5e3512849f
-->
