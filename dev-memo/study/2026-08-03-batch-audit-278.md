# Batch audit 278 — VS-2 prerequisite (phase lane + desktop catch-up) window

**Range:** `a0f8e2d..8809a9e`. **Branch:** `feature/pta-claimtrack-vertical-slice`.

## Window contents (3 commits)
- `6cfdddb` — prior batch-audit closeout (study 277; governance-only).
- `9fbe17e` — ClaimTrack-completion PHASE-LANE authorization (docs-only; review-plan READY
  `review-plan-msep339v`). The governed envelope for VS-2 + VS-3 with risk tiers + a discovery envelope.
- `8809a9e` — desktop CATCH-UP (mechanical, additive): refreshed the internal case-box tarballs to the
  already-audited VS-0/VS-1 state (persistence 264K→290K = VS-0/VS-1; contract +175B = the additive
  `MATTER_PARTY_IDS_ASSIGNED` kind) + the exhaustive-consumer entries the refresh forced (`errorMap`
  SAFE_MESSAGES + renderer `KNOWN_ERROR_CODES` += `audit_chain_desync`/`unknown_party`; `EVENT_KIND_ID`
  += `MATTER_PARTY_IDS_ASSIGNED`; catalog zh-CN += the 3 keys). Unblocks VS-2 (the desktop can now
  compile against the ClaimTrack persistence surface).

## Discovery-envelope note (the phase lane's first WI)
The desktop catch-up was NOT in the lane's VS-2 file scope — the VS-2 planner surfaced the stale-tarball
blocker (R1) as a lane-scope gap. It was resolved as a bounded, mechanical PREREQUISITE (build-plumbing,
additive, no product decision — the VS-0 docket had explicitly deferred the desktop i18n label to "a
separate step per the PTA-*b precedent"), NOT folded into VS-2's diff. This is the discovery envelope
working as designed: a real scope gap surfaced + handled at the right layer, not silently absorbed.

## Verdict
- **AUDIT-RANGE: a0f8e2d3a275b15321bd00554594ebb7fe0d5a13..8809a9eee1020fb9fa0174d9eb32337849d243f6**
- **AUDIT-VERDICT: BATCH-PASS C0 H0 M0 L0** — additive-only text diff; no rename/remove/semantic change of
  any error code, audit label, or catalog entry; the i18n additions are consistent + zh-CN is safe (no
  identifier leakage). Job `audit-msepy9mf-ocwfuc`, rawOutput sha256
  `be7b1ed2b320edb503911a209cecd41e81a574ba1d35864c6df7a522d5b46d8d`. The binary tarballs + lockfile were
  verified out-of-band by the DESKTOP-DEPS-00 drift gate (`check-internal-tarballs` PASS) + `check-internal-lock`
  (current), not by the text audit.

## Gate evidence
- `npm --prefix apps/lawbar-desktop test` — 837/0 (incl. the drift/allowlist tests that assert the renderer
  error-code + audit-kind maps match the persistence union).
- `check-internal-tarballs` PASS; `check-internal-lock` current.

## Outcome
VS-2 (ClaimTrack IPC) is unblocked and ready to open under the phase lane. Next: VS-2 docket → review-plan →
implement → audit → verify, then VS-3 (which stops for Frank's UI design input). All local; no push.

<!-- batch-audit-attestation v1
range_base: a0f8e2d3a275b15321bd00554594ebb7fe0d5a13
target_sha: 8809a9eee1020fb9fa0174d9eb32337849d243f6
verdict: BATCH-PASS
findings: C0 H0 M0 L0
broker_job_id: audit-msepy9mf-ocwfuc
broker_output_sha256: be7b1ed2b320edb503911a209cecd41e81a574ba1d35864c6df7a522d5b46d8d
-->
