# Batch-audit study packet — PRC-4 (#43) + #42 merge + prior closeout, Layer B

**Date**: 2026-06-04.
**Batch range**: `b60a061..350a7ab` (4 commits since the prior `last-batch-audit` marker `b60a061`).
**Trigger**: `BATCH_AUDIT_EVERY=3`; 4 commits ≥ 3 → `batch-commit-guard.sh` breaker fired DUE on post-merge main.
**Audit broker job**: `audit-mpzolzv6-43ld14` (Path 1 runner v0.2.18, `gpt-5.5`, effort high, sandbox read-only, foreground).
**Verdict**: **BATCH-PASS (C0 / H0 / M0 / L0)**.
**Closeout**: fifth production use of `scripts/workflow/batch-closeout.mjs`; runs on post-merge `main`.

<!-- batch-audit-attestation v1
range_base: b60a06120ee620f74fcb2a1d1ec9d5a237e6a134
target_sha: 350a7ab84f0df4ef656d6b94f4932269b07d3382
verdict: BATCH-PASS
findings: C0 H0 M0 L0
broker_job_id: audit-mpzolzv6-43ld14
broker_kind: audit
broker_output_sha256: a7445e0b46146b1b697971b4cefbf34eaba0cee8e3a3a1efa66f66048d4ce410
-->

## Tasks completed in the batch

| Commit | PR | Behavior |
|---|---|---|
| `c08d01b` | (workflow) | DOC-ONLY: the prior batch-closeout's study packet (`d36bf00..b60a061`) + a `dev-memo/batch-closeout-log.md` line. No code. |
| `ceb13e8` | #42 | GitHub PR merge of BRCBW-1/2/10 (bounded cd/literal-var indirection resolver + lexical de-obfuscation in `block-run-control-bash-write.sh`) to main. |
| `240fb81` | #43 | PRC-4: control-character-safe `deny()` JSON in `protect-run-control.sh` (jq `-Rs` primary + tr-strip-then-sed fallback). Tests 42→54. Sibling hooks unmodified. |
| `350a7ab` | #43 | GitHub PR merge of PRC-4 to main. |

## Decisions (alternatives, why chosen, what would falsify)

1. **PRC-4 deny() hardening via `jq -Rs .` (primary) + C0-strip fallback** — NOT a hand-rolled control-byte escaper. Falsifier: a reason byte/condition that still emits invalid JSON or empty output (no deny registered). Audited Q1–Q6 in `audit-mpzo7b65-tjwv93`: none found.
2. **PRC-4 scope = `protect-run-control.sh` `deny()` only** — the two sibling hooks sharing the sed-only pattern (`emit_deny` / batch-commit-guard `deny`) are left unchanged and tracked as **PRC-4-FU**. Falsifier: an accumulation finding that PRC-4 cannot honestly close without also fixing the siblings — audit Q6 confirmed it can.
3. **Layer-B audits the ACCUMULATION, not per-WI lines** — each WI (#42, #43) was already independently review-planned, audited, and verified. This pass checks cross-WI drift only.

## Audit findings + disposition

- **C0 / H0 / M0 / L0.** No accumulation drift. The PRC-4 `deny()` change composes additively with the already-merged BRCBW-1/2/10 hook changes (separate functions; no shared state weakened). Net diff confined to `protect-run-control.sh` deny(), its test file, the deferred-findings ledger, the PRC-4 plan, and the prior closeout's doc/log. No product/UI/contract/manifest/schema/fixture drift; no marker/breaker tampering committed into source; no test deletion/weakening; no `OcrQueueError`-code or security-boundary surface change.
- Broker note: the read-only sandbox could not create temp dirs to run the hook suite; the PRC-4 jq-present deny path was verified directly by the broker, and the full suite (54 protect-run-control + all sibling hooks) passed locally on merged `main`.

## Per-WI provenance (already independently verified)

- #43 PRC-4: review-plan `review-plan-mpznrewh-0cwh19` (READY-WITH-LOW) → audit `audit-mpzo7b65-tjwv93` (PASS C0 H0 M0, 1 Low) → Low fixed → verify `verify-mpzob4dd-7sg1nq` (ALL CLOSED).
- #42 BRCBW-IND: audited in the prior Layer-B pass (`audit-mpzmw82j-ji5rrr` BATCH-PASS for `d36bf00..b60a061`).
- `c08d01b` is the doc-only product of the prior closeout.
- This Layer-B pass checks the ACCUMULATION, not the per-WI line findings.
