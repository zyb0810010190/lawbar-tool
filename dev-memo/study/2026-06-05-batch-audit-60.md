# Batch-audit study packet — WI-804/fact-review-ui (#58) + interim closeout, Layer B

**Date**: 2026-06-06.
**Batch range**: `867985b..6c7f767` (3 commits since the prior `last-batch-audit` marker `867985b`).
**Trigger**: `BATCH_AUDIT_EVERY=3`; the #58 merge brought commits-since-marker to 3 (≥ 3) → `batch-commit-guard.sh` breaker DUE on post-merge main.
**Audit broker job**: `audit-mq1o8o9x-8pzc6s` (Path 1 runner v0.2.18, `gpt-5.5`, effort high, sandbox read-only, native `--background`).
**Verdict**: **BATCH-PASS (C0 / H0 / M0 / L1)**.
**Closeout**: twenty-second production use of `scripts/workflow/batch-closeout.mjs`. FINAL WI of BATCH-CASEBOX-FACT-REVIEW-00 → batch fully implemented.
**Note**: the WI-804 governance commit `867985b` is this range's BASE/marker (already audited by the INTERIM closeout `b6c0b8d..867985b`, `audit-mq1nh0gy-jlmk06`), so it is excluded here; this range covers the interim closeout doc + the WI-804 implementation + the merge.

<!-- batch-audit-attestation v1
range_base: 867985be134c0a5d87703fd4a7f26c9f435fc126
target_sha: 6c7f767ce0574fb9c04f25da9369127c3aa61390
verdict: BATCH-PASS
findings: C0 H0 M0 L1
broker_job_id: audit-mq1o8o9x-8pzc6s
broker_kind: audit
broker_output_sha256: 5e68421164db868ad9f2e5b13e0ca4c5c31e7e1cfff527ea61a0bf6122270368
-->

## Tasks completed in the batch range

| Commit | PR | Behavior |
|---|---|---|
| `7b6db7b` | (workflow) | DOC-ONLY: the INTERIM breaker-reset closeout's study packet (`dev-memo/study/2026-06-05-batch-audit-59.md`, range `b6c0b8d..867985b`) + a `dev-memo/batch-closeout-log.md` line. The interim closeout (Option A) restored breaker headroom between WI-804 governance and implementation — docs/governance only, no product change. |
| `fef623a` | #58 | WI-804: fact review UI — bridge (preload+api+types transition DTO + dto-sync PAIR) + per-fact Review/Accept/Reject controls + status rendering in `viewMatterFacts.ts`, consuming the merged `casebox:fact:transition` (WI-802). +9 review tests (+1 api strip, +1 dto-sync); suite 442→453. |
| `6c7f767` | #58 | GitHub PR merge of WI-804 to main. |

## Decisions (alternatives, why chosen, what would falsify)

1. **Option A (govern → interim closeout → implement)** — WI-804 needed two commits (governance + impl) but only one headroom remained; the user authorized committing governance, running an interim docs/governance Layer-B closeout to advance the marker + restore headroom, then implementing. The interim closeout (`7b6db7b`) is strictly docs/governance (audit A1) — the sanctioned breaker-reset mechanism, not a prohibited cleanup commit.
2. **Legal-edges-only controls; state machine in persistence** — the renderer offers only candidate→Review/Reject, reviewed→Accept/Reject; accepted/rejected are terminal; NO candidate→accepted (no-auto-accept ADR). A backend `illegal_transition` surfaces inline (audit A3). Falsifier: a candidate→accepted control or a renderer-side state machine (none). Per-WI audited `audit-mq1nu2h8-jq9nmk` (PASS C0 H0 M0 L0) + verified `verify-mq1nzfv5-wolw08` (ALL CLOSED).
3. **Renderer forwards only the 4 allowlisted fields** — `{matterId, factId, to, rejection_reason?}`; server injects reviewer + timestamps + computes lifecycle; status is text-first (visible label + `data-status`, not color-only) (audit A4).

## Audit findings + disposition

- **C0 / H0 / M0 / L1.** No security or behavioral drift: the interim closeout is clean docs/governance (A1); WI-804 stayed within Allowed-files and the Forbidden files (`renderer-view-matter.test.mjs`, `renderer-fact-write.test.mjs`, `_view-matter-dom.mjs`, `viewMatterDeadlines.ts`, `src/caseBox/**`, `caseBoxHandlers.ts`, services/contracts) are UNTOUCHED; `package.json` only appends the new test path (no dependency); no hook/marker/breaker tamper or test deletion (A2); legal-edges-only controls with inline `illegal_transition` (A3); renderer forwards only the 4 fields, status not color-only (A4).
- **L1 (CBW-UI-804-HEADER, docs-only — DEFERRED, safe-to-proceed YES)**: the `viewMatterFacts.ts` file header still reads "NO … review" (it was last updated in WI-701 for the add-fact control), but WI-804 added per-fact review controls in the same file. Pure comment drift — no behavioral, security, or contract impact. The functional code (legal-edges controls, authority boundary, projection) is correct and tested. Fix requires touching a renderer file, so it is a follow-up (a tiny doc-fix, naturally folded into the optional WI-805 test-split WI or a standalone doc WI). Recommended follow-up: a one-line header correction. Safe-to-proceed: YES.

## Per-WI provenance (already independently verified)

- #58 WI-804: review-plan `review-plan-mq1n7pks-secu8y` (READY-WITH-LOW; pagination-reset Low accepted) → audit `audit-mq1nu2h8-jq9nmk` (PASS C0 H0 M0 L0) → verify `verify-mq1nzfv5-wolw08` (ALL CLOSED). Gate `npm --prefix apps/lawbar-desktop test` = 453 pass / 0 fail.
- `7b6db7b` is the doc-only product of the interim breaker-reset closeout (`audit-mq1nh0gy-jlmk06` BATCH-PASS for `b6c0b8d..867985b`, which covered WI-803 design + WI-804 governance).
- This Layer-B pass checks the ACCUMULATION; it independently surfaced the L1 header drift.

## Batch state after this closeout — BATCH-CASEBOX-FACT-REVIEW-00 COMPLETE

This closeout advances `dev-memo/run/last-batch-audit` from `867985b` to `6c7f767`, clearing the breaker. **All BATCH-CASEBOX-FACT-REVIEW-00 work is merged**: WI-802 (transition IPC, #57), WI-803 (design artifact, `b105863`), WI-804 (review UI, #58). The lawyer can now review / accept / reject facts end-to-end from the matter view over the tenant-scoped IPC (fail-closed scoped preflight; legal-edges-only UI; server-injected reviewer/timestamps). **Remaining follow-ups (non-blocking, NOT started)**: the optional **WI-805** (split the 1534-LOC `ipc-handlers.unit.test.mjs` monolith) and the new **CBW-UI-804-HEADER** Low (one-line `viewMatterFacts.ts` header correction) — both deferred.
