# Batch audit 285 — audited-reason hardening + matter-details edit screen (Phase D) window

**Range:** `99c5517..307abab`. **Branch:** `feature/pta-claimtrack-vertical-slice` (local; no push).

## Window contents (3 commits)
- `a1735ce` — prior batch-audit closeout (study 284; governance-only — study packet + closeout-log line).
- `e9ff43c` — **audited-reason-guard hardening**: the 6 court-facing persistence reason-guards tightened from
  `.length === 0` to `.trim().length === 0` (matter transition/archive, matter-details update, privilege
  dismiss/waive, docket dismiss, deadline missed→met, fact reject); condition-only, existing codes/messages kept,
  stored value NOT trimmed (hash byte-stable). Whitespace-rejection parity tests (in-memory + SQLite) per op.
  Closes batch-284 L1 `BATCH284-REASON-WS-1`. Origin: batch-284 L1 + Codex scope consult
  `review-plan-msfy1isz-eh3j00` (whole-class fix). Per-WI audit `audit-msfyi7q3-gxvv1s`: NO findings.
  Persistence 1236/1236.
- `307abab` — **matter-details edit screen (Phase D)**: the renderer edit screen `#/matters/:id/edit` — the
  user-visible completion of issue #2 ("edit case info after creation"). Dirty-gate (submit disabled until ≥1
  change + name non-empty), "Changes to be recorded" summary, required reason (min 10/max 500), archived
  read-only + banner, patch = 6 editable fields only, zh-CN safe error messages, route in both tables. Per-WI
  audit `audit-msfzpdvv-o7sak8`: no C/H; 2 Medium FIXED — (M1) a form-submit could fire a no-op IPC → dirty-gate
  guard in `handleSubmit` + regression test; (M2) divergent ULID regexes across the two route tables → deleted the
  duplicate `index.ts` parser, routed `navigate()` through `router.ts` `parseHash` (single source of truth).
  Verify `verify-msg02nd5-6vp36k`: ALL CLOSED. Desktop 907/906/1 (only the pre-existing env `smoke:21`).

## Decision / disposition
This window closes the edit-case vertical (issue #2) END TO END: contract (Phase A, batch-283) → persistence
(Phase B, batch-284) → IPC (Phase C, batch-284) → reason hardening (this window) → the edit SCREEN (this window).
A lawyer can now edit a matter's 6 descriptive fields after creation, with a required court-facing reason recorded
in the audit chain; the reason can no longer be whitespace-blank at any of the 6 audited persistence sites. UX was
settled via a Codex consult (`review-plan-msfyrorm-0qavjf`), evaluated + accepted, with one adaptation: no
audit-history panel (the in-app audit VIEW was removed earlier per the user; the mechanism still records events).

## Audit findings + resolution
- **Batch-285 Layer-B** (`audit-msg06ihx-1bx31i`): **BATCH-PASS C0 H0 M0 L0.** No findings. Cross-WI consistency
  holds (persistence rejects whitespace reason; the screen applies a stricter UI policy trim + 10–500 before the
  IPC — not a contradiction). Court-facing integrity confirmed (renderer sends only `{matterId, patch(6 fields),
  reason}`; archived read-only/no-submit; errors via `errorMessage()` not raw messages). Scope contained; no
  deleted/weakened tests; no renamed/removed error codes.
- Both Phase-D per-WI Mediums were FIXED + verified CLOSED before commit; the reason-hardening per-WI audit had no
  findings. Nothing open.

## Mistakes / near-misses
- **The line-indexed-allowlist lesson held.** After the M2 fix deleted `index.ts`'s duplicate parser, the i18n
  allowlist was regenerated (104→103; the removed entry was a `ParsedRoute["name"]` type-annotation literal the
  scanner had caught) and the FULL desktop gate was re-run green BEFORE commit — no override needed
  ([[feedback_rerun_gate_after_any_edit]]).
- **No override consumed this window.** Fully-green-before-commit held across both WIs (per-WI audit → fix Mediums
  → verify → re-gate → exact-path staging).

## Review-later / carried
- `MDU-B-HEADCHECK-XCUT-1` (from Phase B) — still `open`; the shared SQLite write path (archiveMatter/
  unarchiveMatter/ensureMatterPartyIds) wants the same write-time head-consistency assert Phase B added to
  updateMatterDetails. Its own hardening WI.
- The Phase-D e2e (`casebox-ui.electron.test.mjs`) does not yet exercise the edit flow — optional release-smoke
  parity with archive (a follow-up), not a correctness gap (unit coverage is complete).
- Deferred issue #3 (evidence browsing) remains behind the A0.7 STOP-grade gate.

## Verdict
- **AUDIT-RANGE: 99c5517c31796468fb100bd44b8ccc57faf61323..307abab3abb74f6ca83394a09c8acfa47c71f86f**
- **AUDIT-VERDICT: BATCH-PASS C0 H0 M0 L0** — the reason-hardening restores a court-facing invariant repo-wide
  without touching stored values or codes; the edit screen completes issue #2 with server-authority preserved and
  no wire leak; both Phase-D Mediums fixed + verified. Job `audit-msg06ihx-1bx31i`, rawOutput sha256
  `3aa171e8579595270c1217833efe40e1678521e5322feac67dcf4310b6d0f9d3`.

## Gate evidence
- `npm --prefix services/case-box-persistence test` — 1236/1236 (reason-hardening window).
- `npm --prefix apps/lawbar-desktop test` — 907 tests, 906 pass, 1 fail = only the pre-existing environmental
  `smoke.electron.test.mjs:21` launch timeout. i18n guard + `test:ui-color` + `renderer-dto-sync` green.
- `npm --prefix docs/contracts/case-box-contract test` — 564/564 unchanged.

## Outcome
The edit-case vertical (issue #2) is complete + batch-closed at `307abab`. Everything local; nothing pushed. Three
unpushed branches await the user's push/merge decision (`feature/pta-claimtrack-vertical-slice`,
`chore/workflow-pretest-gate`). Next candidates: the Phase-D edit e2e smoke, the `MDU-B-HEADCHECK-XCUT-1`
cross-cutting hardening, or issue #3 (blocked by A0.7).

<!-- batch-audit-attestation v1
range_base: 99c5517c31796468fb100bd44b8ccc57faf61323
target_sha: 307abab3abb74f6ca83394a09c8acfa47c71f86f
verdict: BATCH-PASS
findings: C0 H0 M0 L0
broker_job_id: audit-msg06ihx-1bx31i
broker_output_sha256: 3aa171e8579595270c1217833efe40e1678521e5322feac67dcf4310b6d0f9d3
-->
