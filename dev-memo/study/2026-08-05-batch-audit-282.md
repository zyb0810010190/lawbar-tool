# Batch audit 282 — § print-marker removal window

**Range:** `1c542ef..6078d35`. **Branch:** `feature/pta-claimtrack-vertical-slice`.

## Window contents (3 commits)
- `365b93a` — prior batch-audit closeout (study 281; governance-only).
- `f13ef0a` — deferred-audit-findings.md backlog rows for the WI-2d cleanups (doc-only).
- `6078d35` — **WI-UI-RESTYLE-2c: remove the two rendered § print-markers** (colophon-marker in `viewMatter.ts`
  + the aria-hidden empty-state glyph in `listMatters.ts`) + orphan `.colophon-marker`/`.empty-glyph` CSS +
  regenerated `ui-strings-allowlist.json` (106 → 104; both § entries dropped). Pure presentation.

## Decision
Narrow ornament cut per the restyle direction (design artifact §2.4). Deliberately scoped to the two *rendered* §
glyphs only — NOT the diffuse uppercase-eyebrow rules (too broad/low-value) and NOT the serif display (WI-2b). The
already-orphan `.section-marker` CSS was left for the deferred dead-CSS cleanup (WI2D-CLEANUP-1).

## Audit findings + disposition
- **batch-282** (`audit-msfrh2zm-jm22o1`): **BATCH-PASS C0 H0 M0 L0** — no findings. Allowlist confirmed current
  (scanner 104 == saved 104, 0 § entries); scope renderer-only; palette/serif/mechanism untouched; empty-state
  test-ids intact.

## Mistakes / near-misses
- **None this window** — the WI-2d lesson held: after the edits I regenerated the allowlist AND re-ran the full
  gate (and independently re-verified the focused i18n guard on the current tree) BEFORE committing, so no stale
  allowlist and no override cascade. Clean single-audit closeout.

## Verdict
- **AUDIT-RANGE: 1c542efe755dd0427c52f08f73e1d708de398a80..6078d3515d0065e56961b40696b4117d13919a72**
- **AUDIT-VERDICT: BATCH-PASS C0 H0 M0 L0** — decorative § markers removed cleanly, renderer-only; palette,
  serif, mechanism, and the deferred residuals all untouched. Job `audit-msfrh2zm-jm22o1`, rawOutput sha256
  `41322468cf76c495f0e2c2146582431427fa15162260076e0299861075251d6c`.

## Gate evidence
- `npm --prefix apps/lawbar-desktop test` — 870/869/1 (the 1 fail = pre-existing environmental
  `smoke.electron.test.mjs:21` launch timeout). `renderer-i18n-guard` 9/9; palette-sync ✔; `renderer-list-matters`
  25/25.
- `npm --prefix docs/contracts/case-box-contract test` — 542/0.

## Outcome
Restyle so far: radius + soft elevation + audit-view removal + § markers removed — all committed + batch-closed,
palette preserved. Remaining restyle: **WI-2b (serif drop)** — carries S1-offline-font-test risk and is the last
polish item. Next product item after the restyle: **#2 edit-case-info** (governed vertical). #3 evidence gated on
A0.7. All local; no push.

<!-- batch-audit-attestation v1
range_base: 1c542efe755dd0427c52f08f73e1d708de398a80
target_sha: 6078d3515d0065e56961b40696b4117d13919a72
verdict: BATCH-PASS
findings: C0 H0 M0 L0
broker_job_id: audit-msfrh2zm-jm22o1
broker_output_sha256: 41322468cf76c495f0e2c2146582431427fa15162260076e0299861075251d6c
-->
