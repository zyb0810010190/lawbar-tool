# Batch audit — window d9ade65..a7977ce (PR #250 synthetic restore drill)

**Verdict**: BATCH-PASS C0 H0 M0. **Broker job**: `audit-mrdnr79z-uislqh` (Path 1 runner `cc-suite/0.2.18`, gpt-5.5/high/read-only; inlined-diff audit).

## Window

`d9ade6584bfc9ff718710b8eb365aa1a69e37201..a7977ce04d433aa3083cb46541df13808d1a0c2e`:
- `6d5688e` chore(workflow): batch-audit closeout f14eaf6..d9ade65.
- `f54d4dd` test(desktop): synthetic backup→restore drill (WI-15).
- `a7977ce` merge PR #250.

## Checks (Layer B)

- **Net delta**: a new test + `package.json` test-list entry + docs + run-control records. No product source /
  contracts / persistence / IPC / schema / UI, and **no backup-script change** — the drill drives the existing
  `--data-dir`/`--out` options and the `LAWBAR_BACKUP_FORCE_LSOF=closed` test hook.
- **Security / confidentiality**: the drill uses synthetic data only, is guarded by an `assertSafeTempRoot()`
  preflight (refuses a `$TMPDIR` inside the repo or `~/Library` before any write), and asserts no document
  filename leaks to stdout+stderr. Nothing is written into the repo tree or `~/Library`.
- **Restore proof is real**: creates a real `sqlite3` DB, backs up, extracts to a fresh dir, opens the DB, and
  round-trips a synthetic row.

## Findings

None (C0 H0 M0).

<!-- batch-audit-attestation v1
range_base: d9ade6584bfc9ff718710b8eb365aa1a69e37201
target_sha: a7977ce04d433aa3083cb46541df13808d1a0c2e
verdict: BATCH-PASS
findings: C0 H0 M0 L0
broker_job_id: audit-mrdnr79z-uislqh
broker_output_sha256: 2b6435a5794fb45f52c2437c983f7494670d4197a3b2ea3e7c8a913e60364b2d
-->
