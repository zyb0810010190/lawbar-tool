# Batch-audit study packet — WI-302/BCG-4+5 (#47) + prior closeout, Layer B

**Date**: 2026-06-05.
**Batch range**: `9cf96a0..b737122` (3 commits since the prior `last-batch-audit` marker `9cf96a0`).
**Trigger**: `BATCH_AUDIT_EVERY=3`; 3 commits ≥ 3 → `batch-commit-guard.sh` breaker fired DUE on post-merge main.
**Audit broker job**: `audit-mq0igtnv-3vrkdo` (Path 1 runner v0.2.18, `gpt-5.5`, effort high, sandbox read-only, native `--background`).
**Verdict**: **BATCH-PASS (C0 / H0 / M0 / L0)**.
**Closeout**: ninth production use of `scripts/workflow/batch-closeout.mjs`; runs on post-merge `main`.

<!-- batch-audit-attestation v1
range_base: 9cf96a0d1d8235e9f95f6671c0f46d956c5c774a
target_sha: b73712283b0f1846d30dcdab0df0c8b43db75590
verdict: BATCH-PASS
findings: C0 H0 M0 L0
broker_job_id: audit-mq0igtnv-3vrkdo
broker_kind: audit
broker_output_sha256: c7b1d866e52fb1acf2d4121ca66c898652ab39d51e7ce139b8266c0bc89e95e5
-->

## Tasks completed in the batch

| Commit | PR | Behavior |
|---|---|---|
| `8a05e9e` | (workflow) | DOC-ONLY: the prior batch-closeout's study packet (`4f806a6..9cf96a0`, #46) + a `dev-memo/batch-closeout-log.md` line. |
| `77c337b` | #47 | WI-302 / BCG-4 + BCG-5: atomic (consume-on-success) consumption of `human.ack` and `human.override` in `batch-commit-guard.sh` — ack authorized only if removed; override granted only if reason readable+non-empty (checked before removal), token removed, THEN logged (rm-then-log). Tests 40→48. |
| `b737122` | #47 | GitHub PR merge of WI-302 to main. |

## Decisions (alternatives, why chosen, what would falsify)

1. **Consume-on-success (fail-closed) for both tokens** — NOT a nonce/recency redesign (the open-row's original suggestion); the minimal correct fix is to gate authorization on confirmed removal (ack) and on reason-check + removal + log (override). Falsifier: any path reaching `exit 0`/`OVERRIDE=1` without full consumption. Audited Q1–Q6 in `audit-mq0i47h7-xni5ci`; verify `verify-mq0i9tz2-m5ztwy` ALL CLOSED.
2. **rm-then-log ordering** (review Medium) so `log.md` never records a "consumed" override that is still present; **reason-nonempty BEFORE removal** (review Medium) so a blank/unreadable reason denies without spending the token.
3. **Root-portable test fixtures** (audit Low) — append-fail + unreadable-reason use directory fixtures (is-a-directory fails for any uid); only chmod-unwritable rm-fail stays non-root with an explicit SKIP.

## Audit findings + disposition

- **C0 / H0 / M0 / L0.** No accumulation drift. WI-302 is confined to the two token-consumption blocks of `batch-commit-guard.sh`; it composes independently with WI-301's config validation (separate blocks) and does NOT alter count/governed/risk/BASE logic, gated/batch routing, or the PRC-4-hardened `deny()`. Both fixes only TIGHTEN authorization (more denies), never loosen. The live config (`AUTO_ADVANCE_MAX=3`, `BATCH_AUDIT_EVERY=3`) and the normal governed-batch path (no ack/override present) are unaffected — the batch's own commits still authorize via `queue.governed`. No product/UI/contract/manifest/schema/fixture drift; no test deletion/weakening; no marker/breaker source tampering; no `OcrQueueError`/security-boundary surface change. The BCG-7 L1 ledger backfill is bookkeeping only.
- Broker note: the read-only sandbox could not run the writable hook suites (`mktemp`/git locks denied); the diff-inspection passed. The full suites (48 base + detect + all siblings) passed locally on merged `main`, including the root-portable BCG-5 append-fail/unreadable cases.

## Per-WI provenance (already independently verified)

- #47 WI-302/BCG-4+5: review-plan `review-plan-mq0hqvvj-21ivbf` (NEEDS-FIX) → `review-plan-mq0hulp0-82zzsq` (NEEDS-FIX) → `review-plan-mq0hxn25-e81iy9` (READY) → audit `audit-mq0i47h7-xni5ci` (C0 H0 M0; 1 Low root-coverage → fixed) → verify `verify-mq0i9tz2-m5ztwy` (ALL CLOSED).
- `8a05e9e` is the doc-only product of the prior closeout (`audit-mq0hg6uz-z31u00` BATCH-PASS for `4f806a6..9cf96a0`).
- This Layer-B pass checks the ACCUMULATION, not the per-WI line findings.
