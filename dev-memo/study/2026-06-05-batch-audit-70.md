# Batch-audit study packet — WI-DTO1/dto split (#67), Layer B — BATCH-CASEBOX-DTO-SPLIT-00 COMPLETE

**Date**: 2026-06-07.
**Batch range**: `9b98726..3d14c1c` (4 commits since the prior `last-batch-audit` marker `9b98726`).
**Trigger**: `BATCH_AUDIT_EVERY=3`; the #67 merge brought commits-since-marker to 4 → breaker DUE.
**Audit broker job**: `audit-mq3vt07k-53ufuu` (Path 1 runner v0.2.18, `gpt-5.5`, effort high, sandbox read-only, foreground).
**Verdict**: **BATCH-PASS (C0 / H0 / M0 / L0)** — `DTO-LOC-797` is now closed; no open Low remains from this lane.
**Closeout**: thirty-second production use of `scripts/workflow/batch-closeout.mjs`. Single-WI batch complete.
**Note**: range includes the prior WI-D4 closeout doc `ce61083` (wrote `9b98726` into the marker), the DTO-split governance `6936a0b`, the WI-DTO1 implementation `0530724`, and the merge `3d14c1c`.

<!-- batch-audit-attestation v1
range_base: 9b98726c952fe0d88cbb321be6d5decd6b5068dd
target_sha: 3d14c1c7a2534b5d3fc54771402e3afb28b82ced
verdict: BATCH-PASS
findings: C0 H0 M0 L0
broker_job_id: audit-mq3vt07k-53ufuu
broker_kind: audit
broker_output_sha256: ff2dace83705e73afff53407357a0633c7fb3f7a6aa3967758e9a1ec234bd1a5
-->

## Tasks completed in the batch range

| Commit | PR | Behavior |
|---|---|---|
| `ce61083` | (workflow) | DOC-ONLY: prior WI-D4 Layer-B closeout study packet (`2026-06-05-batch-audit-69.md`, range `e2ca889..9b98726`) + log line. Advanced marker to `9b98726`. |
| `6936a0b` | (workflow) | GOVERNANCE: BATCH-CASEBOX-DTO-SPLIT-00 queue (WI-DTO1) + `queue.linted/reviewed/governed` + `reviews/queue-review-017.md`. review-plan attempt-1 `review-plan-mq3u8bef-nlcw1r` TIMEOUT-class (wide prompt hung, empty verdict) → attempt-2 COMPACT `review-plan-mq3v327m-ps3zc6` PASS (cc-suite retry policy). |
| `0530724` | #67 | WI-DTO1: split `src/caseBox/dto.ts` (797 LOC) into per-entity modules under `src/caseBox/dto/` (`shared` 22, `matter` 124, `audit` 43, `document` 178, `deadline` 49, `docket` 237, `fact` 253) behind a 12-LOC re-export barrel. All 97 exports verbatim; no importer churn; `renderer-dto-sync.test.mjs` reader updated to read `dto/*.ts`; DTO-LOC-797 closed. |
| `3d14c1c` | #67 | GitHub PR merge of WI-DTO1 to main. |

## Decisions (alternatives, why chosen, what would falsify)

1. **Barrel-preserving split (no import churn)** — `dto.ts` → `export *` barrel; all 9 importers + `dto-contract.test.mjs` unchanged. Falsifier: any importer diff (audit confirmed none).
2. **Deterministic by-symbol-name splitter** — the 797-LOC file's blocks were interleaved per entity; a Python splitter classified each top-level export by its exact symbol name (97/97 classified), avoiding manual mis-carve. Falsifier: a dropped/duplicated/altered export (audit confirmed 97==97 byte-equal).
3. **Per-module precise type imports + no entity imports the barrel** — entity→shared→contract layering keeps it acyclic; `strict:true` has no `noUnusedLocals` so the layering is safe. Falsifier: an entity module importing the barrel (none).
4. **renderer-dto-sync reader reads dto/*.ts with a ≥1-module assert** — preserves the set-equality + `*_RESPONSE_FIELDS` authority-exclusion checks and forbids a false-green on empty extraction. Falsifier: an empty extraction passing (the assert prevents it).

## Audit findings + disposition

- **C0 / H0 / M0 / L0.** Range scope confined to the DTO split + `renderer-dto-sync.test.mjs` + governance/ledger docs. 97→97 exports, zero byte changes within exported declarations, barrel re-exports all 7 modules, no entity imports the barrel. No importer / `dto-contract.test.mjs` / renderer / electron-ipc / services / contracts / hooks / markers / secrets / dependency change. dto-sync assertions intact. Governance content-bound (`queue_sha256` matches `queue.md`); `queue-review-017.md` single PASS recording the timeout→compact retry. `DTO-LOC-797` closed; `dto.ts` 12 LOC, each module < 800. Prior closeout `ce61083` docs-only.
- Per-WI: audit `audit-mq3vevh7-p7g7k5` (C0 H0 M1 L1 — M1 pre-commit staging resolved by staging `dto/**`; L1 unrelated scratch) → verify `verify-mq3vmq2y-w17vyz` (ALL CLOSED).
- **Reliability note (CCSUITE retry policy fired):** the WI-DTO1 review-plan attempt-1 (full packet) hung in codex ~22 min with empty output (TIMEOUT-class); the compact-packet retry (attempt-2) returned normally with PASS. Also observed: a 9-DAY-OLD orphaned `codex exec` process (gpt-5.3-codex, unrelated "S2.5 editorial token-tier" audit) alive in the environment — NOT this batch's; left untouched.

## Batch state after this closeout — COMPLETE

Advances `dev-memo/run/last-batch-audit` from `9b98726` to `3d14c1c`, clearing the breaker. **BATCH-CASEBOX-DTO-SPLIT-00 complete** (WI-DTO1, #67). `src/caseBox/dto.ts` is now a 12-LOC barrel over 7 per-entity modules (all well under the 800 cap), so a future DTO addition lands in the relevant per-entity module without breaching loc-guardian. **`DTO-LOC-797` CLOSED.** Deferred ledger: 0 open High, 0 open Medium (the docket-lifecycle + dto-split lanes are clean); remaining open rows are pre-existing unrelated B-series / commit-guard-hardening Lows.
