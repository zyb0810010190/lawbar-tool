# Batch-audit study packet — WI-A3-EXPORT-00 merge window

**Date**: 2026-06-25. **Range**: `2ea7f97..839a5c2` (`evidence-a3-export-citation-builder`, branched from `main`).
**Type**: CLOSEOUT + GOVERNANCE + DESIGN + MERGE (the A3-EXPORT-00 export-degradation DESIGN lane).

## What shipped in this range
- `79cfab1` — `chore(workflow): batch-audit closeout 3401e0b..2ea7f97` (study 157; marker → `2ea7f97`).
- `b7df472` — `chore(workflow): govern queue — WI-A3-EXPORT-00 …` (queue.* + queue-review-079; design-only,
  not A0.7-gated).
- `b15bfad` — `docs(evidence): A3 export-degradation behavior — A3-EXPORT-00` — the export-degradation ADR +
  the `plan-batch-casebox-evidence-a3-export-degradation-00.md` sequencing plan.
- `839a5c2` — `Merge pull request #128 …` — merge into main.

Confined to `docs/adr/**` + `dev-memo/**`. No product source, no `schema.ts`, no migration, no
`apps/**`/`native/**`/`scripts/workflow/**`/`.claude/**`/`.github/**`, no dependency/package-lock, no
marker/ledger/key/evidence-run files, no `dev-memo/run/evidence/**`. The ADR is DESIGN-ONLY: deterministic
`exportFlag` precedence (broken→BROKEN; needs_review→NEEDS_REVIEW default, REPLACED only as a recorded impl-WI
refinement; valid→clean unless AMBIGUOUS/NON_CITABLE; valid never REPLACED); A10 no-drop (missing → deterministic
BROKEN object, never omitted); resolver status is source of truth (export runs the resolver first); audit-event
shape + anchor-delete cascade + full Evidence document lifecycle deferred/non-inferable. No export/cascade/lifecycle
code; the future export IMPL WI (WI-A3-EXPORT-T1) remains A0.7-gated (custody 9b).

## Audit trail
- `audit-mqtjprtv-hbjpor` (Path 1 runner v0.2.18, native `--background`, gpt-5.5 / high / read-only) over
  `2ea7f97..839a5c2`: **BATCH-PASS C0 H0 M0 L0**. rawOutput sha256
  `1247c4ada27ddcca154d511d5ca28a6746e44fe0f61a3fd6dd8c640bcd0d35c5`.
  - Required recording: kind=audit; scope=`2ea7f97..839a5c2` (4 commits: closeout + governance + design +
    merge); runner=`~/.claude/plugins/cache/xiaolai/cc-suite/0.2.18/scripts/codex-runner.mjs`;
    model/effort/sandbox=gpt-5.5/high/read-only; jobId=`audit-mqtjprtv-hbjpor`; status retrievable=YES;
    first-attempt success (no TIMEOUT).
- (Lane-internal records, already audited: review-plan `review-plan-mqtfzimy-j3y7qc` READY
  CONFIRMED-DESIGN-ONLY-NOT-GATED; design audit `audit-mqtg4vxn-3a47zw` FAIL C0 H0 M2 L1 → fixed; re-audit
  `audit-mqtgb089-xfx4mh` FAIL M1 → fixed; re-audit `audit-mqtgd8lr-hda8vg` PASS C0 H0 M0 L0.)

## Low disposition
- None. C0 H0 M0 L0.

## State
- Clears the audit-DUE window for `2ea7f97..839a5c2` and advances the marker to `839a5c2`. PR #128
  (A3-EXPORT-00 export-degradation DESIGN) is merged to main. This closeout runs on the new branch
  `evidence-a3-export-citation-builder`, OPENING WI-A3-EXPORT-T1 — the A0.7-gated (custody 9b) headless
  export-citation builder that runs `resolveLinkStatuses` first and derives deterministic export-citation
  objects (linkStatus + exportFlag + DocumentPage citation) per the A3-EXPORT-00 contract.

**Verdict: BATCH-PASS C0 H0 M0 L0** (safe to proceed to the WI-A3-EXPORT-T1 governance).

<!-- batch-audit-attestation v1
range_base: 2ea7f977ffd6d5c550386fbd5399d75174d168a9
target_sha: 839a5c2d05e708e75ae23634b8a09d01b233dc80
verdict: BATCH-PASS
findings: C0 H0 M0 L0
broker_job_id: audit-mqtjprtv-hbjpor
broker_output_sha256: 1247c4ada27ddcca154d511d5ca28a6746e44fe0f61a3fd6dd8c640bcd0d35c5
-->
