<!-- batch-audit-attestation v1
range_base: d0387577a09663da549ab479407777e8cc7bc5d8
target_sha: e63982f9f80b9a50d3d14525093bf6e661a5e54d
verdict: BATCH-PASS
findings: C0 H0 M0 L1
broker_job_id: audit-mq57zm07-ozjves
broker_output_sha256: e2f62119d4116ef6a36058151ad0529ba1ea9119b4f69bf59596286a3fb958a3
-->

# Batch audit 89 — BATCH-CASEBOX-AUDIT-EVENT-KIND-V2-CONTRACT-00 (batch-end, post merge #81)

**Window**: `d038757..e63982f` (3 commits). **Verdict**: BATCH-PASS (C0 H0 M0 L1).
**Broker**: Path 1 runner v0.2.18 native --background, gpt-5.5/high/read-only, job `audit-mq57zm07-ozjves`.
**Date**: 2026-06-08. **Closes BATCH-CASEBOX-AUDIT-EVENT-KIND-V2-CONTRACT-00 (Batch 1 of 2).**

## Tasks in window
| Commit | What |
|--------|------|
| `2c263b7` | Mid-batch closeout (post WI-V1). |
| `15be9a6` | WI-V2 — persistence conformance (6 Sqlite-v2 cases) + DT-V1-L1 deferred row. Test-only. |
| `e63982f` | Merge PR #81 to main. |

## Batch summary (both WIs) — audit-event-kind v2 at the contract/hash boundary
- **WI-V1 (SOURCE)** `d038757`: optional hashed `event_kind` + `audit_schema_version=2` via versioned
  canonicalization (v1 byte-identical, golden untouched; v2 = 14 fields); `verifyAuditChain` enforces
  the ADR §4 `{action,entity_type,reasonRequired}` consistency at the verification boundary
  (`event_kind_inconsistent`); mixed v1/v2 chains verify; the canonicalizer throws on
  partial-pair/version≠2/unknown-kind. New contract test file (14 cases) registered via one
  package.json test-line. 393 contract tests pass.
- **WI-V2 (TEST)** `15be9a6`: 6 Sqlite-v2 conformance cases proving the change flows through persistence
  with NO src/migration/column — round-trip via event_json (no column), v2 verify, cross-class tamper →
  `event_kind_inconsistent`, **same-class tamper (hashed) → `prev_event_hash_mismatch`**, legacy v1 row
  still verifies, mixed v1→v2 verifies end-to-end. 440 persistence tests pass (ABI smoke OK).

The load-bearing property is proven end-to-end: the met/missed/withdrawn distinction (which collapses
to the same `{action,entity_type}`) is now durable AND tamper-evident, in both the contract and the
SQLite layer.

## Audit / review trail (this batch)
- review-plan: 5 rounds (CONDITIONAL PASS → 2 FAIL resolving the consistency-check location +
  circular-import + the ADR `reasonRequired` source conflict + a doc-Low) → `review-plan-mq571l86`
  GOVERNABLE.
- WI-V1 audit `audit-mq57c37l` PASS-WITH-FINDINGS (Lows: downgrade/version tests added) → verify
  `verify-mq57fzl9` Low 1 CLOSED. Mid-batch audit `audit-mq57iffm` BATCH-PASS (L1 = DT-V1-L1, deferred).
- WI-V2 audit `audit-mq57s2ye` PASS-WITH-FINDINGS (Low: same-class hashed-tamper test added) → verify
  `verify-mq57vvty` ALL CLOSED. Batch-end audit `audit-mq57zm07` BATCH-PASS (L1 = DT-V1-L1, re-confirmed).

## Findings + disposition
- **C0 H0 M0** throughout; range net diff is the mid-closeout doc + WI-V2 test + DT-V1-L1 row + merge.
- **L1 = DT-V1-L1** (already recorded): schema/generated description says "TS validator" enforces the
  consistency check; it is actually `verifyAuditChain`. Cleanup-only doc wording; OPEN/deferred to the
  next contract-touching WI per `dev-memo/deferred-audit-findings.md`.

## Stop point (per the batch authorization)
**Batch 1 (contract + persistence) complete.** Per the authorization, STOP here for study before
Batch 2 (DTO projection + renderer humanized labels). Do NOT start Batch 2.

## Review-later
- Batch 2 (deferred): project `event_kind` to the renderer DTO + map to humanized labels with
  `action·entity_type` fallback (needs a design artifact). Keep `audit_schema_version` internal.
- DT-V1-L1 doc wording; BRCBW-SUBSTR. Cadence overhead persists (batches 74-89).
