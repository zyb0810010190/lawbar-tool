# Batch-audit study packet — WI-A3-UNLINK-RESOLVE implementation window

**Date**: 2026-06-26. **Range**: `27dbe51..ebd6bb7` (`evidence-a3-unlink-operation`, branched from `main`).
**Type**: CLOSEOUT + IMPL + MERGE (the resolver/export durable-unlink marker-awareness lane).

## What shipped in this range
- `983338f` — `chore(workflow): batch-audit closeout e553060..27dbe51` (study 165; marker → `27dbe51`).
- `fab65e5` — `chore(workflow): govern queue — WI-A3-UNLINK-RESOLVE …` (queue.* + queue-review-087; A0.7-gated).
- `9b79320` — `feat(case-box-persistence): resolver/export durable-unlink marker-awareness` —
  `linkStatusResolverQueries.ts` (prepend `WHEN l.unlinked_at IS NOT NULL THEN 'broken'` as the highest-precedence
  rung) + `exportCitationQueries.ts` (add a distinct `UNLINKED` ExportCitationFlag read from `unlinked_at` as the
  highest-precedence branch) + the two hardening test files (8 A3-UNLINK-RESOLVE cases).
- `ebd6bb7` — `Merge pull request #135 …` — merge into main.

Confined to `services/case-box-persistence/src/sqlite/linkStatusResolverQueries.ts` + `exportCitationQueries.ts`
+ their two test files + `dev-memo/**` governance/closeout. NO `schema.ts` change (CURRENT_SCHEMA_VERSION stays
12), NO guard/errors/index/package.json change, NO `apps/**`/`native/**`/`scripts/workflow/**`/`.claude/**`/
`.github/**`, NO dependency/package-lock. READ/EXPORT-only marker-awareness: the resolver writes only
`case_box_links.status`; nothing writes the marker columns; no row deletion; LinkStatus unchanged; legacy
NULL-marker rows unchanged. Source plain UTF-8 text.

## Audit trail
- `audit-mqug9rny-20ehxg` (Path 1 runner v0.2.18, native `--background`, gpt-5.5 / high / read-only) over
  `27dbe51..ebd6bb7`: **BATCH-PASS C0 H0 M0 L0**. rawOutput sha256
  `69dac0cb53e39f7aa29b32ca96bfb6e51682591bc8abef479e3c8ed8a04916b3`.
  - Required recording: kind=audit; scope=`27dbe51..ebd6bb7` (4 commits: closeout + governance + impl + merge);
    runner=`~/.claude/plugins/cache/xiaolai/cc-suite/0.2.18/scripts/codex-runner.mjs`;
    model/effort/sandbox=gpt-5.5/high/read-only; jobId=`audit-mqug9rny-20ehxg`; status retrievable=YES;
    first-attempt success.
- (Lane-internal records, already audited: review-plan `review-plan-mquf2144-jsn6vs`; human 9b gated PASS; impl
  audit `audit-mquff8vg-j6e5fe` PASS C0 H0 M0 L1 (L1 fixed); verify `verify-mqufjerw-fnvx51` ALL CLOSED.)

## Low disposition
- None. C0 H0 M0 L0.

## State
- Clears the audit-DUE window for `27dbe51..ebd6bb7` and advances the marker to `ebd6bb7`. PR #135 (resolver/
  export durable-unlink marker-awareness) is merged to main: a row with `unlinked_at` set now resolves `broken`
  and exports `UNLINKED`. This closeout runs on the new branch `evidence-a3-unlink-operation`, intended to OPEN
  WI-A3-UNLINK-T1 (the A0.7-gated unlink/relink OPERATION that writes/clears the marker). NOTE: a scope decision
  was surfaced before authoring that WI — the tamper-evident audit-CHAIN event for unlink/relink needs a
  `docs/contracts/**` change (a `link` entity_type + LINK_UNLINKED/RELINKED audit kinds), which is broader scope;
  the operation + on-row `unlink_reason`/`unlinked_at` audit metadata are in-scope. Pending the user's
  audit-scope decision.

**Verdict: BATCH-PASS C0 H0 M0 L0** (safe; the WI-A3-UNLINK-T1 governance is paused on the audit-scope decision).

<!-- batch-audit-attestation v1
range_base: 27dbe517404c02ff497cb6fd165f2481e9137731
target_sha: ebd6bb73bfd9c39046da3db86574ab8fa83a4714
verdict: BATCH-PASS
findings: C0 H0 M0 L0
broker_job_id: audit-mqug9rny-20ehxg
broker_output_sha256: 69dac0cb53e39f7aa29b32ca96bfb6e51682591bc8abef479e3c8ed8a04916b3
-->
