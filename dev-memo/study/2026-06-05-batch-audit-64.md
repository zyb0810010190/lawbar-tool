# Batch-audit study packet — WI-G1/govern-content-bind (#61), Layer B

**Date**: 2026-06-06.
**Batch range**: `0786f13..7685646` (3 commits since the prior `last-batch-audit` marker `0786f13`).
**Trigger**: `BATCH_AUDIT_EVERY=3`; the #61 merge brought commits-since-marker to 3 (≥ 3) → `batch-commit-guard.sh` breaker DUE on post-merge main.
**Audit broker job**: `audit-mq204gkx-unexpa` (Path 1 runner v0.2.18, `gpt-5.5`, effort high, sandbox read-only, foreground).
**Verdict**: **BATCH-PASS (C0 / H0 / M0 / L0)**.
**Closeout**: twenty-sixth production use of `scripts/workflow/batch-closeout.mjs`. First WI of WORKFLOW-GOVERNANCE-INTEGRITY-00 implemented (WI-G2/BCG-9 still to follow).
**Note**: the range includes the INTERIM breaker-reset closeout doc `7687661` (which wrote `0786f13` into the marker and is the first commit after the marker content), the WI-G1 implementation `65eb7fd`, and the merge `7685646`.

<!-- batch-audit-attestation v1
range_base: 0786f13f3833541ef4c0c9818f8f8520f4026f6a
target_sha: 76856469150b73a5c95092307ac445bcd1054d39
verdict: BATCH-PASS
findings: C0 H0 M0 L0
broker_job_id: audit-mq204gkx-unexpa
broker_kind: audit
broker_output_sha256: e45414edcfacd5244d987fcf42deede88f7fddd444c27144e8f5f854a56c930f
-->

## Tasks completed in the batch range

| Commit | PR | Behavior |
|---|---|---|
| `7687661` | (workflow) | DOC-ONLY: the INTERIM breaker-reset closeout's study packet (`dev-memo/study/2026-06-05-batch-audit-63.md`, range `132aa30..0786f13`) + a `dev-memo/batch-closeout-log.md` line. Advanced the marker to `0786f13`, restoring headroom between the WGI governance/amendment and the WI-G1 implementation. Docs/governance only — SEPARATE from WI-G1 implementation. |
| `65eb7fd` | #61 | WI-G1: bind the governed-queue attestation to `sha256(queue.md)` (closes BCG-6 / GOVERNANCE-CHAIN-001). `govern-queue.sh` records `queue_sha256=sha256(queue.md)` (both governance paths; fails closed on missing/unhashable/write-fail); `batch-commit-guard.sh` adds an additive content-binding check after the presence gate — require exactly one canonical `queue_sha256=<64-lowercase-hex>` line (`TOTAL==1 && CANON==1`), recompute, and DENY on absent/malformed/unreadable/mismatch. +10 BCG-6 base-test cases (60 pass); the two collateral guard-driving harnesses updated to content-bound fixtures (detect 28, closeout 28). `queue.governed` refreshed to the content-bound format; BCG-6 ledger row closed. |
| `7685646` | #61 | GitHub PR merge of WI-G1 to main → the content-binding guard is now LIVE on main, with `queue.governed` matching `queue.md`. |

## Decisions (alternatives, why chosen, what would falsify)

1. **Amend WI-G1 (Option A) rather than split a WI-G0 for the collateral harnesses** — the BCG-6 invariant breaks any harness that builds a legacy hashless governed queue; updating those fixtures is intrinsic, not independent. The amendment re-review (`review-plan-mq1zfopj-e9lvdy`) confirmed it is not scope creep. Falsifier: a harness edit that flips a DENY assertion or deletes a negative case (audit A3 confirmed none).
2. **Interim closeout `7687661` to bridge governance/amendment → implementation** — the governance amendment took commits-since-marker to 3 (breaker due) before code landed; an interim docs/governance closeout restored headroom. Falsifier: any implementation folded into `7687661` (audit confirmed it is docs-only).
3. **Strict `TOTAL==1 && CANON==1` parse** (after the audit's L1 + the verify's partial finding) — counting only canonical lines would still allow a valid line plus an extra malformed `queue_sha256=` companion; requiring the total assignment-line count to be 1 closes that. Falsifier: a valid+extra-malformed shape that still ALLOWs (re-verify `verify-mq1zyj9n-lhvzh8` confirmed deny).

## Audit findings + disposition

- **C0 / H0 / M0 / L0.** Range changes only `.claude/hooks/**`, `scripts/workflow/**`, and `dev-memo/**` — no product/renderer/electron/services/contracts/settings.json. `govern-queue.sh` writes the content hash on both paths + fails closed; the guard's content-binding block is additive (after the presence gate, only adds denials for absent/malformed/duplicate/unreadable/unhashable/mismatch). Collateral harness updates are setup-only with no weakened deny expectation. The interim closeout `7687661` is docs-only and separate. BCG-6 recorded closed; BCG-9 remains open. Live `queue.governed` hash equals `sha256(queue.md)`.
- The per-WI audit `audit-mq1zotjp-0mmgj0` surfaced 2 Lows (lax parse; unchecked write); both fixed in-WI; the verify caught an L1 partial (valid+extra-malformed gap), re-fixed to the strict `TOTAL/CANON` check, re-verified ALL CLOSED (`verify-mq1zyj9n-lhvzh8`). This Layer-B pass found nothing further.

## Per-WI provenance (already independently verified)

- #61 WI-G1: review-plan `review-plan-mq1yykph-48tr82` (queue PASS) + amendment `review-plan-mq1zfopj-e9lvdy` (PASS) → audit `audit-mq1zotjp-0mmgj0` (C0 H0 M0 L2; both fixed) → verify `verify-mq1zuueo-j20z1w` (L1 partial) → re-verify `verify-mq1zyj9n-lhvzh8` (ALL CLOSED). Guard suites: base 60 / detect 28 / closeout 28. PR-time `check-ui-design-artifact` = SUCCESS (no renderer path).
- `7687661` is the doc-only product of the interim breaker-reset closeout (`audit-mq1zi3qz-pbxdlr` BATCH-PASS for `132aa30..0786f13`).

## Batch state after this closeout

This closeout advances `dev-memo/run/last-batch-audit` from `0786f13` to `7685646`, clearing the breaker (commits-since-marker → 0). **BCG-6 / GOVERNANCE-CHAIN-001 is CLOSED** — the governed-batch attestation is now content-bound on main, so a post-governance `queue.md` edit fails closed at the commit gate. **WORKFLOW-GOVERNANCE-INTEGRITY-00 is one WI from complete: WI-G2 (BCG-9, the quote-aware commit-count parser) remains GOVERNED but UNIMPLEMENTED and untouched.** Remaining open ledger High: none (BCG-6 closed); remaining open Medium: BCG-9 (to be closed by WI-G2).
