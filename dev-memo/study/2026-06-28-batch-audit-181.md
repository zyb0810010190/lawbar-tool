# Batch audit 181 — `bc40439..b360369`

**Date:** 2026-06-28. **Window:** marker `bc40439` → target `b360369` (3 commits).

## Tasks in window
| Commit | WI | Files | Note |
|---|---|---|---|
| `513204a` | batch-180 closeout | study + log | Workflow artifact (closeout of `2174100..bc40439`). |
| `189fd2d` | PR #148 merge | — | Merge of the already-audited packaging ADR (`bc40439`); no new product content. |
| `b360369` | WI-A3-INTERNAL-DEPS-COMMIT-TARBALLS-T2 | 19 files: committed internal tarballs + manifest + narrow lock refresh + drift guard + 3 A3-link audit labels + governance | Implements DESKTOP-DEPS-00 Option 1; CLOSES DESKTOP-DEPS-STALE-LOCK-01. Per-WI review-plan(5x)+audit+verify clean. |

## Audit
Layer-B broker audit (Path 1 runner 0.2.18, gpt-5.5/high/read-only), job `audit-mqxxmxax-wcz4v9`.

Verified: diff confined to the 19 governed T2 files (no `electron/main.ts`, D1 hook/test, `src/caseBox/**`, IPC/preload, persistence/schema/contract-source, services, native); `package-lock.json` diff = only the 2 internal `file:dist-tarballs` integrity strings; renderer diff = only the 3 LINK_* audit-event labels; `tests/renderer-i18n.test.mjs` = count 50→53 only (non-weakening); `DESKTOP-DEPS-STALE-LOCK-01` flipped to `closed`; `LINK-IPC-T1-D1` + `T2-AUD-L1` remain open; `CURRENT_SCHEMA_VERSION` = 12; no marker/evidence/key/report/root-intake file; tarball hashes match manifest + lock; targeted extraction confirms the committed contract tarball carries the 3 LINK_* kinds. The one Low is `T2-AUD-L1` (already-recorded/deferred: the renderer-i18n runtime `ALL_EVENT_KINDS` loop omits LINK_*, mitigated by the compile-time exhaustive `Record` + the 53-count test).

## State
- Clears the audit-DUE window for `bc40439..b360369` and advances the marker to `b360369`.
- The packaging defect is FIXED end-to-end: clean `npm ci` + `npm run dist` now bundle a `case-box-contract` carrying the LINK_* audit kinds; a semantic-payload + lock-integrity drift guard (`scripts/workflow/check-internal-tarballs.mjs`, wired into `check-gates.sh`) prevents recurrence. **D1 (`LINK-IPC-T1-D1`) is now UNBLOCKED** — the D1 round-trip lane can resume to close it.

**Verdict: BATCH-PASS C0 H0 M0 L1** (clean; the L1 is the deferred `T2-AUD-L1` test-hardening item; no runtime/security/contract/scope defect).

AUDIT-RANGE: bc404395a958e883d58d60e91f8cd3f7a6528020..b3603690069fa03e184f5536271af70558b3c823
AUDIT-VERDICT: BATCH-PASS C0 H0 M0 L1

<!-- batch-audit-attestation v1
range_base: bc404395a958e883d58d60e91f8cd3f7a6528020
target_sha: b3603690069fa03e184f5536271af70558b3c823
verdict: BATCH-PASS
findings: C0 H0 M0 L1
broker_job_id: audit-mqxxmxax-wcz4v9
broker_output_sha256: 51dd8f10b324f1a523527079702188a9ffa64ca027cc50af5c3113f6c25d3634
-->
