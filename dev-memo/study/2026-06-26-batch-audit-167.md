# Batch-audit study packet — WI-A3-UNLINK-AUDIT-KINDS contract window

**Date**: 2026-06-26. **Range**: `ebd6bb7..c72e8bf` (`evidence-a3-unlink-audit-kinds`, branched from `main`).
**Type**: CLOSEOUT + GOVERN + IMPL + MERGE (the `link` audit entity_type + LINK_UNLINKED/LINK_RELINKED
audit-event-kind contract lane).

## What shipped in this range
- `e1eac83` — `chore(workflow): batch-audit closeout 27dbe51..ebd6bb7` (study 166; marker → `ebd6bb7`).
- `192300f` — `chore(workflow): govern queue — WI-A3-UNLINK-AUDIT-KINDS …` (queue.* + queue-review-088;
  NOT A0.7-gated; HIGH-RISK contract).
- `ef13019` — `feat(case-box-contract): add link unlink/relink audit-event kinds` — additive, append-only:
  `entity_type` gains `link` (schema enum + `CASE_BOX_AUDIT_ENTITY_TYPES`); `event_kind` gains
  `LINK_UNLINKED {action:update, entity_type:link, reasonRequired:true}` +
  `LINK_RELINKED {action:update, entity_type:link, reasonRequired:false}` (schema enum +
  `CASE_BOX_AUDIT_EVENT_KINDS`); regenerated `src/generated/case-box-audit-event.ts` (AUTO-GENERATED banner
  intact); 5 LINK cases in `tests/audit-event-kind-v2.test.mjs`; record dev-memo.
- `c72e8bf` — `Merge pull request #136 …` — merge into main.

Confined to `docs/contracts/case-box-contract/**` (schema + `src/audit-log.ts` + generated type + v2 test)
+ `dev-memo/**` governance/closeout. NO `services/**` change (CURRENT_SCHEMA_VERSION stays 12), NO SQLite
schema/FK/CHECK/index change, NO emitter/operation, NO resolver/export/UI/canonicalization/verifier-logic
change, NO `apps/**`/`native/**`/`scripts/workflow/**`/`.claude/**`/`.github/**`, NO dependency/package-lock.
Append-only suffix additions; schema/TS/generated unions in sync; the existing schema-vs-registry drift tests
stay green. Source plain UTF-8 text. No emitter — the kinds exist but nothing produces them yet (that is the
separate WI-A3-UNLINK-T1 operation).

## Audit trail
- `audit-mquifbnq-77c9bs` (Path 1 runner v0.2.18, native `--background`, gpt-5.5 / high / read-only) over
  `ebd6bb7..c72e8bf`: **BATCH-PASS C0 H0 M0 L0**. rawOutput sha256
  `6da16d572837efd1780a08596d923db3ff6a401ddd212be3a33b6f193c593aee`.
  - Required recording: kind=audit; scope=`ebd6bb7..c72e8bf` (4 commits: closeout + governance + impl + merge);
    runner=`~/.claude/plugins/cache/xiaolai/cc-suite/0.2.18/scripts/codex-runner.mjs`;
    model/effort/sandbox=gpt-5.5/high/read-only; jobId=`audit-mquifbnq-77c9bs`; status retrievable=YES;
    first-attempt success. (Auditor noted it did not run the contract package test because the test path runs
    `gen:types`/`build` which writes under a read-only sandbox; the in-sync/append-only checks were done by
    read-only diff inspection — consistent with the PR's own per-WI green test run.)
- (Lane-internal records, already audited: review-plan `review-plan-mqugufiv-brs3tr` READY /
  CONFIRMED-NOT-GATED-HIGH-RISK / KINDS AS-PROPOSED-OK; impl audit `audit-mquh4ges-nk9huk` PASS C0 H0 M0 L1
  (L1 fixed — record dev-memo staged); verify `verify-mquh7et6-crjuex` ALL CLOSED.)

## Low disposition
- None. C0 H0 M0 L0.

## State
- Clears the audit-DUE window for `ebd6bb7..c72e8bf` and advances the marker to `c72e8bf`. PR #136 (the `link`
  audit-kind contract support) is merged to main: `entity_type: link` + `LINK_UNLINKED`/`LINK_RELINKED` are now
  live in the case-box-contract audit vocabulary. This closeout is the prerequisite Layer-B audit before opening
  WI-A3-UNLINK-T1 (the A0.7-gated unlink/relink OPERATION that writes/clears the V12 marker columns AND emits the
  now-available LINK_UNLINKED/LINK_RELINKED chain events).

**Verdict: BATCH-PASS C0 H0 M0 L0** (safe; contract-only; the emitter remains the future WI-A3-UNLINK-T1).

<!-- batch-audit-attestation v1
range_base: ebd6bb73bfd9c39046da3db86574ab8fa83a4714
target_sha: c72e8bf74d97b4fecd3c71bf3c2c774d6e58c64a
verdict: BATCH-PASS
findings: C0 H0 M0 L0
broker_job_id: audit-mquifbnq-77c9bs
broker_output_sha256: 6da16d572837efd1780a08596d923db3ff6a401ddd212be3a33b6f193c593aee
-->
