# Batch-audit study packet — WI-A3-T5-RESOLVE lane window

**Date**: 2026-06-25. **Range**: `3401e0b..2ea7f97` (`evidence-a3-export-degradation-design`, branched from `main`).
**Type**: CLOSEOUT + GOVERNANCE + IMPL + MERGE (the A3 link-status resolver implementation lane).

## What shipped in this range
- `b8ff414` — `chore(workflow): batch-audit closeout c1e3321..3401e0b` (study 156; marker → `3401e0b`).
- `2b6fb41` — `chore(workflow): govern queue — WI-A3-T5-RESOLVE …` (queue.* + queue-review-078; A0.7-gated,
  custody 9b).
- `7d3ddec` — `feat(case-box-persistence): add A3 link status resolver` — new
  `src/sqlite/linkStatusResolverQueries.ts` (the resolver) + additive `src/index.ts` export +
  `tests/hardening-link-status-resolver.test.mjs` (15 A3-T5 tests) + `package.json` test-script wiring.
- `2ea7f97` — `Merge pull request #127 …` — merge into main.

Confined to `services/case-box-persistence` (resolver + index export + test + package.json test wiring) +
`dev-memo/**` governance/closeout. NO `schema.ts` change, NO migration, NO `apps/**`/`native/**`/
`scripts/workflow/**`/`.claude/**`/`.github/**`, NO dependency/package-lock. `CURRENT_SCHEMA_VERSION` stays 11.
The resolver realizes the A3-RESOLVE-00 ladder (broken > needs_review > valid; valid never default), writes
ONLY `case_box_links.status` (changed rows only) inside one `BEGIN IMMEDIATE` transaction (atomic per matter
snapshot); document-replacement scoped to the `supersedes_document_id` reverse-lookup signal (Option 1);
status-only (no audit events). No resolver-created/deleted links/anchors; no cascade; no export; no UI.

## Audit trail
- `audit-mqtfuu5n-lvdjtd` (Path 1 runner v0.2.18, native `--background`, gpt-5.5 / high / read-only) over
  `3401e0b..2ea7f97`: **BATCH-PASS C0 H0 M0 L0**. rawOutput sha256
  `b8472fb18e0f36f5566ae5d95ecbde5ed4e631e7c4798e76fd434eab63e21a38`.
  - Required recording: kind=audit; scope=`3401e0b..2ea7f97` (4 commits: closeout + governance + impl +
    merge); runner=`~/.claude/plugins/cache/xiaolai/cc-suite/0.2.18/scripts/codex-runner.mjs`;
    model/effort/sandbox=gpt-5.5/high/read-only; jobId=`audit-mqtfuu5n-lvdjtd`; status retrievable=YES;
    first-attempt success (no TIMEOUT).
- (Lane-internal records, already audited: review-plan `review-plan-mqsrp52g-rvwkmq` READY CONFIRMED-A07-GATED
  OPTION1 ACCEPTED-POINTER-ONLY; impl audit `audit-mqssanrs-penax7` PASS C0 H0 M1 L2 — M1+L1+L2 fixed; verify
  `verify-mqssgs5v-schhf5` ALL CLOSED; human 9b gated PASS.)

## Low disposition
- None. C0 H0 M0 L0.

## State
- Clears the audit-DUE window for `3401e0b..2ea7f97` and advances the marker to `2ea7f97`. PR #127 (the A3
  link-status resolver) is merged to main. This closeout runs on the new branch
  `evidence-a3-export-degradation-design`, OPENING WI-A3-EXPORT-00 — the export-degradation DESIGN lane (NOT
  A0.7-gated) that specifies how exports degrade when links are `needs_review` / `broken` (ExportCitationFlag
  semantics) before any export implementation. The future export IMPL WI is A0.7-gated (`Requires-A07: yes`).

**Verdict: BATCH-PASS C0 H0 M0 L0** (safe to proceed to the A3-EXPORT-00 governance).

<!-- batch-audit-attestation v1
range_base: 3401e0bb8b3a021c3a20c5e88f0ae48c8f0ba258
target_sha: 2ea7f977ffd6d5c550386fbd5399d75174d168a9
verdict: BATCH-PASS
findings: C0 H0 M0 L0
broker_job_id: audit-mqtfuu5n-lvdjtd
broker_output_sha256: b8472fb18e0f36f5566ae5d95ecbde5ed4e631e7c4798e76fd434eab63e21a38
-->
