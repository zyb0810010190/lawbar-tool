# Batch-audit study packet — WI-A3-LINK-CREATE-T1 implementation + merge window

**Date**: 2026-06-26. **Range**: `43a272b..6fa2870` (`evidence-a3-link-ipc-design`, branched from `main`).
**Type**: CLOSEOUT + IMPL + MERGE (the audited createLink persistence operation).

## What shipped in this range
- `aedb2fa` — `chore(workflow): batch-audit closeout ba0cf3e..43a272b` (study 171; marker → `43a272b`).
- `859ea1b` — `feat(case-box-persistence): audited createLink operation (WI-A3-LINK-CREATE-T1)` — `createLink`
  inserts a durable active `case_box_links` row (generated ULID id; provisional `needs_review`; NULL markers;
  deterministic `payload_json`) and appends EXACTLY ONE `LINK_CREATED` event (action create, entity_type link,
  before_state_hash null, after_state_hash excluding the resolver-derived status, single stamp) in one
  `BEGIN IMMEDIATE`; validates matter/tenant/anchor + evidence existence (source_type=evidence) + duplicate-id.
- `6fa2870` — `Merge pull request #140 …` — merge into `main`.

Confined to `services/case-box-persistence/src/sqlite/{linkRepoQueries,SqliteCaseBoxPersistence}.ts` +
`src/index.ts` + `tests/hardening-link-status-resolver.test.mjs` + `dev-memo/**` governance/closeout/plan. NO
`schema.ts` change (CURRENT_SCHEMA_VERSION stays 12), NO `docs/contracts/**` change, NO resolver/export/UI change,
NO row delete, NO InMemory/shared-interface change, NO `apps/**`/`native/**`/`scripts/workflow/**`/`.claude/**`/
`.github/**`, NO dependency/package-lock. Source plain UTF-8 text. No secrets / no marker/evidence-run committed;
fixtures synthetic-only.

## Audit trail
- `audit-mquywzt6-e61yqz` (Path 1 runner v0.2.18, native `--background`, gpt-5.5 / high / read-only) over
  `43a272b..6fa2870`: **BATCH-PASS C0 H0 M0 L0**. rawOutput sha256
  `42563e7c11218bb50d9bfb9c40b61b52888448af5dee808654e62131d5fab080`.
  - Required recording: kind=audit; scope=`43a272b..6fa2870` (3 commits: closeout + impl + merge);
    runner=`~/.claude/plugins/cache/xiaolai/cc-suite/0.2.18/scripts/codex-runner.mjs`;
    model/effort/sandbox=gpt-5.5/high/read-only; jobId=`audit-mquywzt6-e61yqz`; status retrievable=YES;
    first-attempt success.
- (Lane-internal records, already audited: review-plan `review-plan-mquxn69s-vrqyeo` READY; human 9b gated PASS;
  impl audit `audit-mquy9fs0-mppfuv` PASS C0 H0 M0 L2 (both Lows fixed); verify `verify-mquydd5q-4jvsmb` ALL
  CLOSED; Layer-B governance closeout `audit-mquy59sn-jwz63m` BATCH-PASS.)

## Low disposition
- None. C0 H0 M0 L0.

## State
- Clears the audit-DUE window for `43a272b..6fa2870` and advances the marker to `6fa2870`. The audited createLink
  operation is merged to `main`: the A3 link persistence lifecycle is complete (create → resolve/export →
  unlink/relink), all audited (`LINK_CREATED`/`LINK_UNLINKED`/`LINK_RELINKED`). This closeout opens the
  WI-A3-LINK-IPC-DESIGN-00 lane (design-only ADR for the IPC/API surface exposing the link lifecycle to the app).

**Verdict: BATCH-PASS C0 H0 M0 L0** (safe; the createLink emitter is live; the IPC surface is the next design lane).

<!-- batch-audit-attestation v1
range_base: 43a272b2016c3bda02e7fff4c35e86b9801a4cad
target_sha: 6fa28709cb00c4265600feb22b45ddc13559279d
verdict: BATCH-PASS
findings: C0 H0 M0 L0
broker_job_id: audit-mquywzt6-e61yqz
broker_output_sha256: 42563e7c11218bb50d9bfb9c40b61b52888448af5dee808654e62131d5fab080
-->
