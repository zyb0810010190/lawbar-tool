# Batch-audit study packet — PRC-4-FU2 (#45) + prior closeout, Layer B

**Date**: 2026-06-05.
**Batch range**: `f446807..4f806a6` (3 commits since the prior `last-batch-audit` marker `f446807`).
**Trigger**: `BATCH_AUDIT_EVERY=3`; 3 commits ≥ 3 → `batch-commit-guard.sh` breaker fired DUE on post-merge main.
**Audit broker job**: `audit-mq0fp3v2-klvz5u` (Path 1 runner v0.2.18, `gpt-5.5`, effort high, sandbox read-only, native `--background`).
**Verdict**: **BATCH-PASS (C0 / H0 / M0 / L0)**.
**Closeout**: seventh production use of `scripts/workflow/batch-closeout.mjs`; runs on post-merge `main`.

<!-- batch-audit-attestation v1
range_base: f446807691f844532fb6ae3bf34811e6c6b8de30
target_sha: 4f806a61d265197573cddb9ebb352e8cf56408e0
verdict: BATCH-PASS
findings: C0 H0 M0 L0
broker_job_id: audit-mq0fp3v2-klvz5u
broker_kind: audit
broker_output_sha256: 3d141101be57c79144999f6c226acfc57b36dae63970fb8d9d024d6b50f04c13
-->

## Tasks completed in the batch

| Commit | PR | Behavior |
|---|---|---|
| `61b47b8` | (workflow) | DOC-ONLY: the prior batch-closeout's study packet (`350a7ab..f446807`, #44) + a `dev-memo/batch-closeout-log.md` line. Advanced the local gitignored marker; no source/code change. |
| `7262ba6` | #45 | PRC-4-FU2: ported the PRC-4 control-character-safe `deny()` JSON hardening to the last two PreToolUse emitters — `block-git-add-all.sh` `emit_deny()` and `block-commit-stage-all.sh` `emit_deny()`. Tests +13 each (red-before + 12 isolation cases). Only the emitter bodies changed. Completes the series: all FIVE deny emitters now hardened. |
| `4f806a6` | #45 | GitHub PR merge of PRC-4-FU2 to main. |

## Decisions (alternatives, why chosen, what would falsify)

1. **Port the PRC-4 emitter body verbatim into the two staging-hook emitters** (names + call sites + control flow unchanged) — NOT a broader refactor. Falsifier: a reason byte/condition that still emits invalid JSON or empty output in either emitter. Audited Q1–Q6 in `audit-mq05o1pf-lg2l0z`: none found.
2. **Fail-closed jq-fallback test stub** — the audit Low + a first verify (`verify-mq05sajv-4ykrgf` findings-remaining: `jqrc` init hole) drove a `stub_ok` flag + `command -v jq` identity check so the jq-fallback variant runs only when the stub genuinely shadows real jq and exits nonzero; otherwise the suite registers a failure. Closed by `verify-mq05vcaz-ev4lug` (ALL CLOSED). Falsifier: a setup failure mode that still silently runs jq-fallback against real jq.
3. **Series complete; no sixth emitter** — review-plan + audit both inventoried `.claude/hooks` and found exactly five PreToolUse deny emitters, all now hardened. PRC-4-FU2 closes honestly without a global-parity overclaim.

## Audit findings + disposition

- **C0 / H0 / M0 / L0.** No accumulation drift. The PRC-4-FU2 emitter changes compose cleanly with the already-merged PRC-4 / PRC-4-FU hooks; detection/gate/breaker logic in both staging hooks unchanged (only emitter bodies). Broad-staging detection (`git add .`/`-A`/`-u`, `git commit -a`/`--all`) still denies; quoted echo/message cases still allow. The `61b47b8` closeout commit did not commit the local marker into source, did not tamper with the breaker, and only added a study packet + log line. No product/UI/contract/manifest/schema/fixture drift; no test deletion/weakening; no `OcrQueueError`-code or security-boundary surface change.
- Broker note: the read-only sandbox could not run the full writable hook suites (`mktemp`/git lock denial); the broker ran direct no-write JSON emitter probes (jq-present + jq-fallback) for all five emitters and broad-detection smoke checks. The full suites passed locally on merged `main` (0 non-pass).

## Per-WI provenance (already independently verified)

- #45 PRC-4-FU2: review-plan `review-plan-mq05hi3k-3a026d` (READY) → audit `audit-mq05o1pf-lg2l0z` (PASS C0 H0 M0; 1 Low test-harness) → Low fixed → verify `verify-mq05sajv-4ykrgf` (findings remaining) → strengthened → verify `verify-mq05vcaz-ev4lug` (ALL CLOSED).
- `61b47b8` is the doc-only product of the prior closeout (`audit-mq05884f-juk8gv` BATCH-PASS for `350a7ab..f446807`).
- This Layer-B pass checks the ACCUMULATION, not the per-WI line findings.
