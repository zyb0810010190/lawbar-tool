<!-- batch-audit-attestation v1
range_base: a429ebf993fb07c6dee3359e8e53035e03c64d41
target_sha: 22d180a5584c03bff9fefa6a70f05a688fa211ce
verdict: BATCH-PASS
findings: C0 H0 M0 L0
broker_job_id: audit-mq4l3cag-fe3yp1
broker_output_sha256: 2ab5e4833730f52a65b3b4eedf02d815e06653af24f58400fbd6deb2b65d0a2f
-->

# Batch audit 78 — BATCH-CASEBOX-AUDIT-PROJECTION-00 (post WI-AP1, final)

**Window**: `a429ebf..22d180a` (4 commits). **Verdict**: BATCH-PASS (C0 H0 M0 L0).
**Broker**: Path 1 runner v0.2.18, gpt-5.5/high/read-only, job `audit-mq4l3cag-fe3yp1`.
**Date**: 2026-06-07. **Closes the batch — AUDIT-AUD-1 fixed.**

## Tasks completed

| Commit | What |
|--------|------|
| `7e53f93` | Prior cleanup-batch final closeout (housekeeping). |
| `8032094` | Governed BATCH-CASEBOX-AUDIT-PROJECTION-00. |
| `9ab43d4` | WI-AP1 — audit-list IPC response projection (close AUDIT-AUD-1). Security-adjacent; 513 pass. |
| `22d180a` | merge PR #74. |

## Decisions
- Closed a live authority-field leak on `casebox:audit:listEvents` by mirroring the FACTS-AUD-3 list-channel projection: `LIST_AUDIT_EVENTS_RESPONSE_FIELDS` allowlist + `projectPage`, stripping `tenant_id`/`actor_user_id`/`id`/`matter_id` + open-index extras while keeping the renderer-consumed display fields + chain hashes. `chainHeadHandler` left untouched (already safe). No persistence/contract/renderer/electron change. *Falsifier:* if a future allowlist entry names an authority field, the leak reopens — the dto-sync `RESPONSE_ALLOWLISTS` authority-exclusion test guards against that.

## Audit findings + disposition
- Per-WI: review-plan `review-plan-mq4kc7jy-bswp51` PASS; audit `audit-mq4kysuo-1zoi2p` **CLEAN (no C/H/M/L)** → no verify needed (GET-AUD-1 precedent).
- Layer-B `audit-mq4l3cag-fe3yp1`: **no findings**; scope/goal/security-projection all pass; only product change is the projection + tests; zero renderer/electron/services/contracts.

## Mistakes / near-misses
- The first review-plan invocation was reaped: the harness auto-backgrounded the long foreground runner call, the orchestrating shell died (0-byte sink, no job registered, no orphan). Recovered per the cc-suite background-invocation discipline by re-running with the runner's **native `--background`** (returns a jobId in <1s, detached worker survives) for review-plan AND the subsequent audits. Lesson reinforced: for long cc-suite calls, prefer native `--background` + state-file polling over a plain foreground call the harness may auto-background.

## Batch outcome / review-later
- BATCH-CASEBOX-AUDIT-PROJECTION-00 complete. AUDIT-AUD-1 closed. All LIST channels (facts/documents/deadlines/audit) + getDocument now project responses to renderer-safe DTOs — the authority-leak class is fully closed across the read surface.
- No new deferred findings. Remaining open Lows are pre-existing B-series/guard-esoterica, unrelated.
- Watch item (carried, not acted on): small batches incur many Layer-B closeouts under BATCH_AUDIT_EVERY=3 (governance/merge/closeout commits count).
