# Batch audit — window 86dd59c..2744605 (PR #247 backup/restore + FIX1 hardening)

**Verdict**: BATCH-PASS C0 H0 M0. **Broker job**: `audit-mrdksf0f-9beaqk` (Path 1 runner `cc-suite/0.2.18`, gpt-5.5/high/read-only; inlined-diff post-fix re-audit).

## Window

`86dd59c21a627436c2820334e7bb043240443189..2744605346827cc663edd61e218461e1229695a8`:
- `84137e1` chore(workflow): batch-audit closeout c61bbc9..86dd59c.
- `282bb39` feat(desktop): local data backup script + restore procedure (WI-11).
- `c234f30` merge PR #247.
- `2744605` fix(desktop): fail-closed WAL-safety for local backup (WI-BACKUP-HARDEN-11-FIX1).

## Remediation history (why this is a re-audit)

The first Layer-B pass over `86dd59c..c234f30` (`audit-mrdk82ad-vvfvvr`) returned **BATCH-FAIL C0 H1 M2** on
`backup-local-data.mjs`: (H) `--allow-running` could copy an in-use WAL DB; (M) detector failed open when `lsof`
was unavailable but `pgrep` was clear; (M) post-tar `dirSize()`/reporting could throw a Node error naming a
document path. FIX1 (`2744605`, single-use override, marker held at `86dd59c`) made the WAL-safety gate
fail-closed and reporting exception-safe; verify `verify-mrdknpid-zbif3p` → ALL CLOSED (3/3). This attestation is
the post-fix re-audit covering `86dd59c..<fix-head>`.

## Checks (Layer B)

- **Net delta**: CI-config/docs + one local-first developer backup script (+ tests). No product source /
  contracts / persistence / IPC / schema / UI.
- **WAL-safety holds**: a positive `lsof` open handle refuses non-overridably; `lsof`-unavailable fails closed
  unless the verbose flag, and `pgrep` is advisory-only (never clearance).
- **Confidentiality**: reports counts/sizes only; `tar` stderr suppressed; `dirSize()`/reporting exception-safe
  (unreadable entries counted, never named). `execFileSync` uses argv arrays (no shell injection); label +
  `--out` are traversal/symlink guarded.

## Findings

None (C0 H0 M0).

<!-- batch-audit-attestation v1
range_base: 86dd59c21a627436c2820334e7bb043240443189
target_sha: 2744605346827cc663edd61e218461e1229695a8
verdict: BATCH-PASS
findings: C0 H0 M0 L0
broker_job_id: audit-mrdksf0f-9beaqk
broker_output_sha256: c09099bd81ee3090e6954fd476146a36838a5b690be2e91f273d6f8c46d6c3cb
-->
