<!-- batch-audit-attestation v1
range_base: 64a2aec5a7bea4808efd463ce1a3b1185a593c4c
target_sha: 0f2281aa67420db95d2c41f16a8bcf7719bf1e78
verdict: BATCH-PASS
findings: C0 H0 M0 L0
broker_job_id: audit-mq4vw9wg-1x1exh
broker_output_sha256: 8d18933972af054b57bb7cf8836ad15d47c83e9465584bcd5cb21878f2294a71
-->

# Batch audit 85 — BATCH-CASEBOX-AUDIT-EVENT-KIND-ADR-00 (batch-end, post merge #79)

**Window**: `64a2aec..0f2281a` (4 commits). **Verdict**: BATCH-PASS (C0 H0 M0 L0).
**Broker**: Path 1 runner v0.2.18 native --background, gpt-5.5/high/read-only, job `audit-mq4vw9wg-1x1exh`.
**Date**: 2026-06-08. **Closes BATCH-CASEBOX-AUDIT-EVENT-KIND-ADR-00.**

## Tasks completed in window
| Commit | What |
|--------|------|
| `4cfb572` | Prior batch (renderer a11y) final closeout doc. |
| `dc2d877` | Governed BATCH-CASEBOX-AUDIT-EVENT-KIND-ADR-00 (fresh queue, revised decision). |
| `874eb77` | WI-AK1 — ADR `docs/adr/audit-event-kind-preservation.md`. |
| `0f2281a` | Merge PR #79 to main. |

## Batch summary
ADR-only. Decided how to preserve audit-event **kind** for future humanized labels in a
**tamper-evident** way:
- Persist normalized optional `event_kind` (existing `CASE_BOX_AUDIT_EVENT_KINDS` key).
- **Hash `event_kind` in a new v2 canonicalization**; keep v1 byte-identical (golden test unchanged);
  mixed v1/v2 chains verify. v2 = 12 fields + `event_kind` + a **hashed** `audit_schema_version`.
- Field-pair selection (both ⟹ v2, neither ⟹ v1, partial ⟹ reject); consistency invariant
  `event_kind ↔ {action,entity_type}` (necessary-but-insufficient → hence hashing); no backfill of
  collided legacy rows; no SQL column first path (event_json carries it); honest fallback.
- Authorizes NO implementation; the contract + hash-chain change is deferred to a separately-governed
  security-WI loop.

## Decision provenance (the load-bearing story)
Initial decision (unhashed display metadata) was **blocked** by broker review-plan
`review-plan-mq4v6gfn-cdy5f7` (High: a DB edit `DEADLINE_MET`→`DEADLINE_MISSED` passes verification
while flipping legal meaning, since those kinds share `{action:update,entity_type:deadline}`). Per the
hash-boundary hard-stop, this was **escalated to the user**, who chose Option 3 (revise to a
tamper-evident, versioned-canonicalization design). Old run-control files were restored and a fresh
queue authored. Revised ADR: `review-plan-mq4vn7yl-65dsb3` ACCEPT (2 Mediums — field-pair-rule
explicitness + verifier-model accuracy/full-rewrite limitation — fixed in §3.3/§3.4/§3.5/§10) →
`review-plan-mq4vrrhe-5vhp7k` READY/ACCEPT (no findings).

## Audit findings + disposition
- **C0 H0 M0 L0** — range touches only `dev-memo/**` governance + `docs/adr/...`; no
  contract/persistence/renderer/source/test code; ADR authorizes no implementation; versioned-canon
  decision internally consistent; `queue.governed` hash matches `queue.md`; `git diff --check` clean.

## cc-suite invocation log (this batch)
| Kind | Job | Outcome |
|------|-----|---------|
| review-plan | `review-plan-mq4v6gfn-cdy5f7` | FAIL/BLOCK (unhashed draft) → escalated |
| review-plan | `review-plan-mq4vn7yl-65dsb3` | ACCEPT w/ 2 Mediums (fixed) |
| review-plan | `review-plan-mq4vrrhe-5vhp7k` | READY/ACCEPT |
| audit (end) | `audit-mq4vw9wg-1x1exh` | BATCH-PASS C0 H0 M0 L0 |

All Path 1 runner native --background, gpt-5.5/high/read-only.

## Review-later
- Follow-up implementation is a **high-risk contract + hash-chain change** → full security-WI loop
  (plan-review → tests-first → implement → audit → verify → sign-off), contract-first. Suggested WI
  chain: contract (schema + v2 canonicalization + builder + tests) → persistence conformance →
  DTO/projection → UI (with design artifact). Indexed-column option is a separately-governed deferral.
- BRCBW-SUBSTR remains an open deferred Low.
- Cadence overhead persists (governance + closeout commits vs BATCH_AUDIT_EVERY=3), batches 74-85.
