# Batch audit 215 — `faef514..54b5083`

**Date:** 2026-07-04. **Window:** marker `faef514` → target `54b5083` (3 commits, 14 files).

## Tasks in window
| Commit | WI | Files | Note |
|---|---|---|---|
| `80803da` | batch-214 closeout | study 214 + closeout-log entry | Workflow artifact (closeout of `3f748bf..faef514`), direct-push. |
| `3b644a7` | WI-SEC-CASEBOX-TENANT-SCOPING-DEFENSE-00 (executed) | 9 `sqlite/*RepoQueries.ts` + `tests/tenant-filter-lists.test.mjs` + `docs/release/casebox-persistence-security-audit-00.md` §8 sign-off + `reviews/queue-review-133.md` | The gate-10 follow-up security fix (Type: IMPL). M-1: row-level `tenant_id` predicate on the 8 child reads (caller-tenant enforcement). M-2: 7 child-entity mutation UPDATEs scoped `WHERE id = ? AND tenant_id = ? AND matter_id = ?` bound to the resolved row's own tenant/matter + `RunResult.changes === 1` guard (stable `invalid_argument`) before the audit-event write (atomic-consistency, NOT caller-rejection; `updateMatterRow` excluded; `updateLinkMarkerRow` private gained tenantId/matterId params + caller updated). L-1: +14 tenant-filter regressions (red-before/green-after). Predicate-only, no DDL, `CURRENT_SCHEMA_VERSION` 12, in-memory source untouched (already tenant-filters). Dedicated broker security audit CLEAN (see below). Gate 10 recorded CLEARED pending user go-live approval (report §8). |
| `54b5083` | PR #183 merge | — | Merge of the security fix; no tree delta over `3b644a7`. |

## Audit
Layer-B broker audit (Path 1 runner 0.2.18, gpt-5.5/medium/read-only), job `audit-mr749mhu-qmubnx`.

**Timeout + retry (reliability note).** This window contains a dense 9-file SQL diff. Attempt 1 (`audit-mr735wfw-clsmy2`, medium effort, full re-derivation prompt) FAILED **TIMEOUT** (`spawnSync codex ETIMEDOUT`) — a full line-by-line re-derivation of the security fix is expensive AND redundant (that exact diff already passed a dedicated CLEAN security audit `audit-mr71e737-lqz7sg` C0 H0 M0 L0 in the exec lane). Attempt 2 (`audit-mr749mhu-qmubnx`, medium effort, LEAN batch prompt = scope-confinement + governance honesty + a 3-sample spot-check, citing the prior clean dedicated audit) completed first pass. Failure class **TIMEOUT** per `.claude/rules/cc-suite.md`; the retry did NOT downgrade scope — it audited the full 14-file window, delegating the exhaustive SQL correctness to the dedicated audit and batch-checking scope + invariants + governance. (This closeout adds one commit to the next window; no formal `dev-memo/cc-suite-reliability-log.md` entry — the closeout commit is restricted to study + log by `batch-closeout.mjs`; the timeout trail is captured here + in the exec-lane review-133.)

**Result.** Verified the cumulative diff (14 files) is confined to the 9 named `sqlite/*RepoQueries.ts` + the one test + the audit-report §8 sign-off + the batch-214 closeout artifacts. NO `schema.ts`/DDL, NO `CURRENT_SCHEMA_VERSION` change (stays **12**), NO `docs/contracts/**`, NO `package*.json`/dependency, NO native, NO `apps/**`/renderer, NO `inMemory*` source, NO migration. Spot-check of `updateFactRow` + `updatePrivilegeMarkerRow` (parameterized `tenant_id = ? AND matter_id = ?`, `changes === 1` guard throwing stable `invalid_argument` before returning) + `listAuditEventsSqlite` (parameterized tenant predicate) PASS; no error-code rename. Governance: the §8 sign-off records Gate 10 CLEARED pending user go-live approval (NOT final go-live), records both the CLEAN `audit-mr71e737-lqz7sg` and the honest TIMEOUT `audit-mr70anim-61n6u0`, and asserts no go-live; `queue-review-133.md` carries exactly one `QUEUE_REVIEW_VERDICT=PASS`. batch-214 study well-formed; closeout-log entry matches. No `dev-memo/run/intake/**` sample; no final GO/NO-GO decision.

## State
- Clears the audit-DUE window for `faef514..54b5083` and advances the marker to `54b5083`.
- On `main`: the case-box persistence tenant-scoping defense is shipped + independently audited CLEAN; **gate 10 is CLEARED pending user go-live approval** (report §8). This does NOT imply go-live.
- M0 gating now reduces to: the 3 user STOP-AND-ASK hard-stops (framework/public-distribution/signing; 律师法 compliance; final sign-off) + remaining client release-hardening (gate 3) + release documentation + the optional gate-5 ocr-worker flake. Gates 10 (security) is cleared-pending; the go-live decision itself is the user's.
- Forms track: T3 COMPLETE (M0); T4 DECIDED-but-post-v1; T5 design-gated.
- Next natural lanes (NOT started; need authorization): the three go-live hard-stop USER decisions; the gate-5 ocr-worker flake WI; T4 post-v1 next slice; T5 design.
- Deferred-findings backlog: 0 open Evidence Lows; 37 open scaffold/hygiene Lows (all Low, Safe=YES, non-blocking).
- Delivery note: direct-push closeout convention (adds one commit to the next window).

**Verdict: BATCH-PASS C0 H0 M0 L0** (clean; a predicate-only security-boundary fix independently audited CLEAN + its governance/closeout artifacts; scope confined, no schema/contract/dependency/native/renderer/in-memory change, no error-code rename, no read-layer re-sort, no go-live decision; schema version + no-out-of-scope invariants intact; no raw sample committed).

AUDIT-RANGE: faef5146853220018c2f09de06baf2db9f7943b4..54b508378389ab5a34b9f20cae3265292b827093
AUDIT-VERDICT: BATCH-PASS C0 H0 M0 L0

<!-- batch-audit-attestation v1
range_base: faef5146853220018c2f09de06baf2db9f7943b4
target_sha: 54b508378389ab5a34b9f20cae3265292b827093
verdict: BATCH-PASS
findings: C0 H0 M0 L0
broker_job_id: audit-mr749mhu-qmubnx
broker_output_sha256: 037f9b01b2815b9f0686f403d387abd269b84453f0f9ade9e13f179e482191d8
-->
