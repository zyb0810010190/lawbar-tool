# Batch-audit study packet — WI-A3-LINK-CREATE-T1 governance window

**Date**: 2026-06-26. **Range**: `ba0cf3e..43a272b` (`evidence-a3-link-create-operation`, branched from `main`).
**Type**: CLOSEOUT + MERGE + GOVERN (the WI-A3-LINK-CREATE-T1 governance; the createLink IMPL is uncommitted
in the working tree and audited separately).

## What shipped in this range
- `e260fc6` — `chore(workflow): batch-audit closeout 9b0067d..ba0cf3e` (study 170; marker → `ba0cf3e`).
- `57beaab` — `Merge pull request #139 …` (the additive `LINK_CREATED` audit kind into `main`; already audited).
- `43a272b` — `chore(workflow): govern queue — WI-A3-LINK-CREATE-T1 …` (queue.* + queue-review-092; the
  A0.7-gated createLink lane governance). queue.md sha256 `059009d7`.

Confined to `dev-memo/**` governance/closeout + the merged `docs/contracts/**` LINK_CREATED kind (already
audited in window 170 / PR #139). NO product-runtime change in the committed range: no `services/**` source, no
`schema.ts` / `CURRENT_SCHEMA_VERSION` change, no `apps/**`/`native/**`/`package.json`/dependency. The createLink
IMPLEMENTATION (linkRepoQueries.ts + SqliteCaseBoxPersistence.ts + index.ts + the test) is UNCOMMITTED in the
working tree at the time of this closeout — it is gated by the per-WI broker audit/verify + the A0.7 custody-9b
human gate, and lands in the SEPARATE implementation commit after this closeout. No secrets / no marker/evidence-run.

## Audit trail
- `audit-mquy59sn-jwz63m` (Path 1 runner v0.2.18, native `--background`, gpt-5.5 / high / read-only) over
  `ba0cf3e..43a272b`: **BATCH-PASS C0 H0 M0 L0**. rawOutput sha256
  `2c3810aa6f2e095a3afafdbba2b784386499e928a85e4c9a4d534ccdb29383d3`.
  - Required recording: kind=audit; scope=`ba0cf3e..43a272b` (3 commits: closeout + PR #139 merge + governance);
    runner=`~/.claude/plugins/cache/xiaolai/cc-suite/0.2.18/scripts/codex-runner.mjs`;
    model/effort/sandbox=gpt-5.5/high/read-only; jobId=`audit-mquy59sn-jwz63m`; status retrievable=YES;
    first-attempt success.
- (Lane-internal record, already audited: review-plan `review-plan-mquxn69s-vrqyeo` READY C0 H0 M0 (1 Low —
  duplicate_id precheck — applied in the impl). The createLink impl's own broker audit + verify run AFTER this
  closeout, before the implementation commit.)

## Low disposition
- None. C0 H0 M0 L0.

## State
- Clears the audit-DUE window for `ba0cf3e..43a272b` and advances the marker to `43a272b`. This closeout resets
  the batch cadence so the WI-A3-LINK-CREATE-T1 implementation commit (the uncommitted createLink op) can land
  next, after its broker audit + verify. PR #139 (`LINK_CREATED` kind) is merged to `main`; the createLink
  operation is the emitter.

**Verdict: BATCH-PASS C0 H0 M0 L0** (safe; governance-only committed window; the impl is separately gated).

<!-- batch-audit-attestation v1
range_base: ba0cf3ec58bf4f443edd2a6a9d22bd1088006728
target_sha: 43a272b2016c3bda02e7fff4c35e86b9801a4cad
verdict: BATCH-PASS
findings: C0 H0 M0 L0
broker_job_id: audit-mquy59sn-jwz63m
broker_output_sha256: 2c3810aa6f2e095a3afafdbba2b784386499e928a85e4c9a4d534ccdb29383d3
-->
