# Batch-audit study packet — WI-702/deadline-ui (#55) + Option-B amendment + prior closeout, Layer B

**Date**: 2026-06-05.
**Batch range**: `7ddc9b8..e15d833` (4 commits since the prior `last-batch-audit` marker `7ddc9b8`).
**Trigger**: `BATCH_AUDIT_EVERY=3`; the #55 merge brought commits-since-marker to 4 (≥ 3) → `batch-commit-guard.sh` breaker DUE on post-merge main.
**Audit broker job**: `audit-mq1256bf-zflal4` (Path 1 runner v0.2.18, `gpt-5.5`, effort high, sandbox read-only, native `--background`).
**Verdict**: **BATCH-PASS (C0 / H0 / M0 / L0)**.
**Closeout**: eighteenth production use of `scripts/workflow/batch-closeout.mjs`. FINAL WI of the UI batch BATCH-CASEBOX-UI-00 → batch fully implemented.

<!-- batch-audit-attestation v1
range_base: 7ddc9b8dbeab8e743ae4a5d78151a8addb1ba4fd
target_sha: e15d83353e9f607d817efe121fa67ec436432a1c
verdict: BATCH-PASS
findings: C0 H0 M0 L0
broker_job_id: audit-mq1256bf-zflal4
broker_kind: audit
broker_output_sha256: 525d0322b9c83ada498dbbdfb708b2e610d071207a09becd3219dbacc22d13b7
-->

## Tasks completed in the batch range

| Commit | PR | Behavior |
|---|---|---|
| `3da3051` | (workflow) | DOC-ONLY: the prior WI-703 closeout's study packet (`dev-memo/study/2026-06-05-batch-audit-55.md`, range `f416a4e..7ddc9b8`) + a `dev-memo/batch-closeout-log.md` line. |
| `9b73a79` | (workflow) | GOVERNANCE (Option B): moved `renderer-view-matter.test.mjs` to WI-702 Forbidden-files (it is at the loc-guardian 1200-LOC cap), added a NEW dedicated `tests/renderer-deadline-write.test.mjs` + `package.json` to WI-702 Allowed-files, recorded the user-approved pre-existing-LOC deferral as ledger row `LOC-RVM-1200`. queue.linted/reviewed/governed + `reviews/queue-review-007.md`. |
| `2944a6b` | #55 | WI-702: Add/Confirm Deadline UI + renderer bridge — two-step propose→confirm consuming `casebox:docket:create` + `casebox:docket:confirm`; exported pure `findUniqueInstant` DST resolver (±48h transition-agnostic scan); `loadDeadlines` listContainer/loadGen stale guard; fail-closed confirm. New `renderer-deadline-write.test.mjs` (13 tests) wired into the `package.json` "test" gate; suite 409→422. |
| `e15d833` | #55 | GitHub PR merge of WI-702 to main. |

## Decisions (alternatives, why chosen, what would falsify)

1. **Option B (dedicated deadline test file) instead of growing the at-cap file** — `renderer-view-matter.test.mjs` was exactly 1200 LOC (loc-guardian fail = raw LOC ≥ 1200) from WI-701/WI-703; rather than breach it or silently fix it, WI-702's deadline tests went to a new `renderer-deadline-write.test.mjs` and the pre-existing violation was deferred (`LOC-RVM-1200`). Falsifier: any change to `renderer-view-matter.test.mjs` in the range (none — audit A3).
2. **Exported pure `findUniqueInstant` + synthetic-zone tests** — per-test host-TZ control is unreliable, so the transition-size-agnostic gap/overlap algorithm is tested deterministically against injected zone models (NY gap/overlap + 30-min Lord-Howe). Falsifier: a DST gap/overlap forwarded to the server, or a 30-min overlap missed. Per-WI audited `audit-mq11upl8-dpzjnj` (PASS C0 H0 M0 L3, all fixed in-WI) + verified `verify-mq11ysem-f07o98` (ALL CLOSED).
3. **Two-step ephemeral entryId; fail-closed confirm** — entryId comes only from the create response (no docket-read IPC; reload loses unconfirmed proposals = accepted slice limitation), and a foreign/unknown/wrong-tenant confirm returns `invalid_payload` → inline alert, proposed kept, no refresh.

## Audit findings + disposition

- **C0 / H0 / M0 / L0.** No accumulation drift (A1 — docket writes routed through the allowlist-stripping bridge; no backend authority-injection code touched). No forbidden-area change across the range (A2 — no `services/case-box-persistence/src`, `docs/contracts`, `src/caseBox`, `caseBoxHandlers.ts`, `electron/main.ts`, hook, error-code surface; `package.json` adds ONLY the new test path to the `test` script, NO dependency; no test deletion). `renderer-view-matter.test.mjs` UNCHANGED, `LOC-RVM-1200` recorded as an open deferred follow-up (not fixed/bundled), new test in the gate (A3). Governance internally consistent (A4). No deferred-findings disposition required (L0).

## Per-WI provenance (already independently verified)

- #55 WI-702: review-plan `review-plan-mq10xhyu-gmr9ui` (READY-WITH-LOW) → audit `audit-mq11upl8-dpzjnj` (PASS C0 H0 M0 L3, all fixed in-WI) → verify `verify-mq11ysem-f07o98` (ALL CLOSED). Gate `npm --prefix apps/lawbar-desktop test` = 422 pass / 0 fail.
- `9b73a79` Option-B amendment: queue-lint PASSED; review-plan chain `review-plan-mq11is6s-atrgyr` → `review-plan-mq11ma6c-x6ivqm` → `review-plan-mq11osul-ueoszl` (READY TO GOVERN); `reviews/queue-review-007.md`.
- `3da3051` is the doc-only product of the prior WI-703 closeout (`audit-mq10q8em-3bbahz` BATCH-PASS for `f416a4e..7ddc9b8`).
- This Layer-B pass checks the ACCUMULATION; clean.

## Batch state after this closeout — BATCH-CASEBOX-UI-00 COMPLETE

This closeout advances `dev-memo/run/last-batch-audit` from `7ddc9b8` to `e15d833`, clearing the breaker. **All three governed WIs of BATCH-CASEBOX-UI-00 are merged**: WI-701 (Add Fact UI + bridge, #53), WI-703 (Add-Fact default-purpose fix, #54), WI-702 (Add/Confirm Deadline UI + bridge, #55). The governed queue holds no further WIs. The lawyer-facing matter view now has write affordances for both case-box write flows (facts + deadlines), each consuming the WI-601/WI-602 tenant-scoped write IPC. **Remaining deferred follow-up (non-blocking)**: `LOC-RVM-1200` — a future bounded TEST WI to split `renderer-view-matter.test.mjs` below the 1200-LOC cap (NOT done here; user deferred).
