# Batch-audit study packet — WI-A3-DELETE-T1 lane window

**Date**: 2026-06-25. **Range**: `c81ce5a..98abd11` (`evidence-a3-unlink-workflow-design`, branched from `main`).
**Type**: CLOSEOUT + GOVERNANCE + IMPL + MERGE (the A3 referenced-anchor-delete refusal-guard lane).

## What shipped in this range
- `bc50f1c` — `chore(workflow): batch-audit closeout 34f2d0f..c81ce5a` (study 160; marker → `c81ce5a`).
- `c88fc18` — `chore(workflow): govern queue — WI-A3-DELETE-T1 …` (queue.* + queue-review-082; A0.7-gated,
  custody 9b).
- `91ca92f` — `feat(case-box-persistence): add A3 referenced-anchor-delete refusal guard` — new
  `src/sqlite/anchorDeleteGuardQueries.ts` (assertCanDeleteAnchor) + one additive `errors.ts` code
  (`anchor_referenced`) + additive `src/index.ts` export + `tests/hardening-anchor-delete-guard.test.mjs`
  (9 A3-DELETE-T1 tests) + `package.json` test-script wiring.
- `98abd11` — `Merge pull request #131 …` — merge into main.

Confined to `services/case-box-persistence` (guard + one additive errors.ts code + index export + test +
package.json test wiring) + `dev-memo/**` governance/closeout. NO `schema.ts` change, NO resolver
(`linkStatusResolverQueries.ts`) / export (`exportCitationQueries.ts`) change, NO migration, NO
`apps/**`/`native/**`/`scripts/workflow/**`/`.claude/**`/`.github/**`, NO dependency/package-lock.
`CURRENT_SCHEMA_VERSION` stays 11; `errors.ts` adds exactly one additive code (`anchor_referenced`). The guard
`assertCanDeleteAnchor` throws on a referenced (`anchor_referenced`) / missing (`invalid_argument`) anchor and
returns void when allowable — NEGATIVE-PATH only, no physical delete, no link mutation, no audit event; the
existence + scoped reference checks run in one `transaction().immediate()`. Source files are plain UTF-8 text
(no NUL bytes).

## Audit trail
- `audit-mqto2ovl-hqxn39` (Path 1 runner v0.2.18, native `--background`, gpt-5.5 / high / read-only) over
  `c81ce5a..98abd11`: **BATCH-PASS C0 H0 M0 L0**. rawOutput sha256
  `f4eafe14544bd14ed76178a1431dd1aac1ad03921efb3a786b30f9d6fbeaab30`.
  - Required recording: kind=audit; scope=`c81ce5a..98abd11` (4 commits: closeout + governance + impl +
    merge); runner=`~/.claude/plugins/cache/xiaolai/cc-suite/0.2.18/scripts/codex-runner.mjs`;
    model/effort/sandbox=gpt-5.5/high/read-only; jobId=`audit-mqto2ovl-hqxn39`; status retrievable=YES;
    first-attempt success (no TIMEOUT).
- (Lane-internal records, already audited: review-plan `review-plan-mqtmb0jp-7lkj1i` READY CONFIRMED-A07-GATED
  ERROR-CODE ADD-DEDICATED-CODE SCOPE TENANT-MATTER-SCOPED-OK; impl audit `audit-mqtmvgqb-e74okk` PASS C0 H0 M0
  L1 — L1 test-coverage gap fixed; human 9b gated PASS.)

## Low disposition
- None. C0 H0 M0 L0.

## State
- Clears the audit-DUE window for `c81ce5a..98abd11` and advances the marker to `98abd11`. PR #131 (the
  referenced-anchor-delete refusal guard) is merged to main. This closeout runs on the new branch
  `evidence-a3-unlink-workflow-design`, OPENING WI-A3-UNLINK-00 — the unlink/break-link workflow DESIGN lane
  (NOT A0.7-gated) that specifies how a link is intentionally detached from an anchor (never an implicit
  cascade), and records the M0 constraint that a durable link-preserving unlink needs a schema decision (the
  resolver would revalidate a status-only `broken`; `anchor_id` is NOT NULL; LinkStatus has no `unlinked`).

**Verdict: BATCH-PASS C0 H0 M0 L0** (safe to proceed to the WI-A3-UNLINK-00 governance).

<!-- batch-audit-attestation v1
range_base: c81ce5a497f792a9b1696a87ccc18f90fe913e68
target_sha: 98abd11695e610583d1b60b836c3daa4881c163a
verdict: BATCH-PASS
findings: C0 H0 M0 L0
broker_job_id: audit-mqto2ovl-hqxn39
broker_output_sha256: f4eafe14544bd14ed76178a1431dd1aac1ad03921efb3a786b30f9d6fbeaab30
-->
