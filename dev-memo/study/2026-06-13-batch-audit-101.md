# Batch-audit study packet — PR2 (matter list desktop variant) + M1/M2 remediation

**Date**: 2026-06-13. **Range**: `3011afa..96d0a99` (branch `product-ui-detail-aria`).
**Type**: UI (renderer list route) + governance + a BATCH-FAIL remediation.

## What shipped in this range
- **PR2 — matter list desktop variant** (commit `346e359`, merged via `dd1faf5`/PR #91): reworked the
  `#/matters` list onto the desktop classes (`.tabs-row`/`.desk-tab`/`.matter-table-desktop`/
  `.list-empty-desktop` + `.status-pill`/`.conf-pill`) with hardcoded Chinese labels (no i18n layer;
  list-local, `format.ts` untouched). Active/archived tabs + cursor paging preserved.
- **`96d0a99` — M1/M2 remediation** of the prior BATCH-FAIL (see below).
- Governance commits in range: `a94c25b` (batch-audit closeout 2c57f7f..3011afa), `b7543c1`
  (record UISHELL-L1 deferred).

## Prior BATCH-FAIL and its remediation
The first Layer-B audit over `3011afa..dd1faf5` (job `audit-mqc8usn6-v520fm`) returned
**BATCH-FAIL C0 H0 M2 L0**:
- **M1** — the list table added a synthetic `序号` ordinal column rendering `String(index+1)`,
  which is not a `MatterRow` field (fabricated display data; unstable under paging/filter/sort).
- **M2** — `smoke.packaged.electron.test.mjs` + `casebox-ui.electron.test.mjs` still asserted the
  retired English list strings (`lawbar — case-box`, the old empty copy) after PR2 changed them to
  Chinese; these packaged tests are not in the default gate, so they passed CI while contradicting
  the shipped renderer.

**Remediation** (`96d0a99`, one combined commit, single-use `human.override` consumed to clear the
audit-DUE batch-guard deadlock — logged in `dev-memo/run/log.md` at 2026-06-13T14:34:06Z):
- M1: removed the `序号` column header + ordinal cell from `listMatters.ts` (loop back to `for…of`);
  updated `renderer-list-matters.test.mjs` (dropped ordinal assertions, shifted column indices).
- M2: refreshed both packaged tests to assert `案件台账` + `/仅保存在本机/` for the list route
  (create/detail-screen English assertions retained — those screens are not yet reworked).

## Layer-B batch audit (cumulative, post-remediation)
Broker audit `audit-mqchm4uv-hvqvtx` (Path 1 runner v0.2.18 native `--background`, gpt-5.5 /
**medium** / read-only) over `3011afa..96d0a99`. Verdict: **BATCH-PASS C0 H0 M0 L0** — both prior
Mediums confirmed resolved in the net diff, no new findings; standing invariants (real MatterRow
fields only, tab/paging intact, unique `.list-new-btn`, `format.ts` unchanged, no raw renderer-TS
colors, no v08/v11/v13 leakage) spot-checked clean.

Reliability note: the first two re-audit attempts on this window failed on broker infrastructure,
not content — `audit-mqc8sg6n-xrzwgh` (RUNNER_ERROR `spawnSync codex ENOBUFS`, foreground) and
`audit-mqcgiih2-ita3yr` (TIMEOUT `spawnSync codex ETIMEDOUT`, high effort > 30-min cap). Resolved by
native `--background` + medium effort with a leaner prompt (this job).

## Gates
- `npm --prefix apps/lawbar-desktop test` → **584/584** at `96d0a99` (build + renderer-import lint +
  no-real-data + ui-color + smoke.electron + palette-sync + font-wiring + shell test). Both packaged
  test files `node --check` clean.

## Review-later
- UISHELL-L1 (shell `aria-current` hardcoded) — still open in `dev-memo/deferred-audit-findings.md`;
  slated as the first item of the next renderer UI PR (PR3).

**Verdict: BATCH-PASS C0 H0 M0 L0.**

<!-- batch-audit-attestation v1
range_base: 3011afa7e86b65b40f54d7e7817f19d25774530b
target_sha: 96d0a9959f75f273dcc292dee546e5a7e32a1ff3
verdict: BATCH-PASS
findings: C0 H0 M0 L0
broker_job_id: audit-mqchm4uv-hvqvtx
broker_output_sha256: 5c6ed5a43ea1aa6be3aca63ca3f6809d867db1626ae55d2eb8758c5511f7cad8
-->
