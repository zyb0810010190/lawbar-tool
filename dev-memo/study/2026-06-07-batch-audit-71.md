<!-- batch-audit-attestation v1
range_base: 3d14c1c7a2534b5d3fc54771402e3afb28b82ced
target_sha: ed8e58847819b9b1798a1df2e9113dfbbc8303de
verdict: BATCH-PASS
findings: C0 H0 M0 L0
broker_job_id: audit-mq3ximrv-oi7hvq
broker_output_sha256: 544551f5dea7a002bba0e1e799a8172da9a8ab8f16ccf5cf0021233c8455364e
-->

# Batch audit 71 — BATCH-GOVERN-QUEUE-HARDENING-00

**Window**: `3d14c1c..ed8e588` (4 commits). **Verdict**: BATCH-PASS (C0 H0 M0 L0).
**Broker**: Path 1 runner v0.2.18, gpt-5.5/high/read-only, job `audit-mq3ximrv-oi7hvq`.
**Date**: 2026-06-07.

## Tasks completed

| WI | Commit | Files | Behavior |
|----|--------|-------|----------|
| WI-GQ1 (govern) | `ff70ae1` | `dev-memo/run/queue.{md,linted,reviewed,governed}`, `reviews/queue-review-018.md` | Governed the one-WI WORKFLOW queue (lint+review, content-bound). review-plan NEEDS-FIX (gate incomplete) → fixed → PASS. |
| WI-GQ1 (impl) | `d440c9f` | `scripts/workflow/govern-queue.sh` (+56), `scripts/workflow/govern-queue.test.sh` (new, 118) | Hardened `govern-queue.sh`: atomic temp+`mv`, post-write self-verify (exactly-one canonical hash line + `governed=` line + recorded==recompute, fail-loud+rm), verbose success (hash + abs path). New 9-case test. |
| merge | `ed8e588` | — | PR #68 merged to main. |

(`1ff29ab`, the previous batch's own closeout commit, also falls in this window — expected housekeeping.)

## Decisions

- **Defense-in-depth, not a bug fix.** A scratch reproduction (`/tmp/gq_repro.py`, recorded in study 70) proved `govern-queue.sh` already refreshes `queue.governed` correctly across standalone/piped/compound/`sh` shapes, and the WI-G1 content-binding guard already fails closed — so the two live "stale governed" incidents were unreproducible operational artifacts. Chosen direction: make a future silent-stale a LOUD writer failure rather than touch the guard. *Falsifier:* if a stale-governed incident recurs **with** the hardened writer reporting success, the root cause is elsewhere (e.g. a wrapper rewriting the file post-govern) and this WI's premise was wrong.
- **Self-verify mirrors the guard's exact shape** (exactly one `^queue_sha256=[0-9a-f]{64}$` line), per audit M1 — so the writer never claims success on a file the guard would reject. *Falsifier:* a guard reader change that no longer requires exactly-one would make the writer stricter than the guard (acceptable direction, but worth noting).
- **Atomic temp inside `$RUN`** (same filesystem) so `mv` is atomic; private dotfile name avoids protected run-control names; `trap` cleans it on interruption.

## Audit findings + disposition

- Per-WI audit `audit-mq3x9g61-tgr408`: 0C/0H, **1 Medium + 3 Low — ALL fixed in-WI** (M1 count-canonical-lines==1; L1 trap-clean temp; L2 guard `date` + verify `governed=` line; L3 assert rc==0 in test). Verify `verify-mq3xdlyu-2h06iz`: VERDICT ALL CLOSED.
- Layer-B batch audit `audit-mq3ximrv-oi7hvq`: **no findings**; scope matches the governed queue; no product/UI/service/contract/hook/settings paths touched; static check confirmed `queue.governed` is the expected two-line content with `queue_sha256` matching `queue.md`.

## Mistakes / near-misses

- Run-control guard denied a `git rev-list ... $(cat dev-memo/run/last-batch-audit)` one-liner (command-substitution referencing a protected `dev-memo/run/` file). Resolved by not embedding the protected path in a substitution. No state change.
- Live smoke runs of `govern-queue.sh` left a timestamp-only drift on the tracked `queue.governed` (hash unchanged); restored via single-path `git restore` before closeout to keep main clean.

## Review-later

- `scripts/workflow/govern-queue.sh` self-verify `grep -cE` / `sed` portability is BSD+GNU-exercised by `govern-queue.test.sh`, but the duplicate-canonical-line branch is unreachable through the writer (defensive only) — no dedicated test. If a future change lets the writer emit multiple hash lines, add one.
