# Batch-audit study packet — WI-601/docket-write (#51) + governance + prior closeout, Layer B

**Date**: 2026-06-05.
**Batch range**: `bf8df09..7c30664` (4 commits since the prior `last-batch-audit` marker `bf8df09`).
**Trigger**: `BATCH_AUDIT_EVERY=3`; 4 commits ≥ 3 → `batch-commit-guard.sh` breaker fired DUE on post-merge main.
**Audit broker job**: `audit-mq0q7zj2-tq0fwy` (Path 1 runner v0.2.18, `gpt-5.5`, effort high, sandbox read-only, native `--background`).
**Verdict**: **BATCH-PASS (C0 / H0 / M0 / L1)**.
**Closeout**: thirteenth production use of `scripts/workflow/batch-closeout.mjs`; runs on post-merge `main`. First WI of the case-box write batch BATCH-CASEBOX-WRITE-00.

<!-- batch-audit-attestation v1
range_base: bf8df092abf847349d6f736ed90119117aa364f4
target_sha: 7c306648ac1e9b198211555d5e5dcd615e004471
verdict: BATCH-PASS
findings: C0 H0 M0 L1
broker_job_id: audit-mq0q7zj2-tq0fwy
broker_kind: audit
broker_output_sha256: 7162c5c799926027a876ceb10f21201a8ede72c1c51f473aa0f5b6bf73c727f1
-->

## Tasks completed in the batch

| Commit | PR | Behavior |
|---|---|---|
| `f68348f` | (workflow) | DOC-ONLY: the prior batch-closeout's study packet (`f444325..bf8df09`, #50) + a `dev-memo/batch-closeout-log.md` line. |
| `d868689` | (workflow) | GOVERNANCE: replaced the closed BATCH-PRODUCT-PROBE-00 queue with BATCH-CASEBOX-WRITE-00 (WI-601/WI-602), refreshed `queue.linted`/`reviewed`/`governed`, added `reviews/queue-review-004.md`. No product code. |
| `21dddd7` | #51 | WI-601: docket-entry create + confirm IPC (deadline write path) — tenant-scoped write channels, scoped confirm preflight (fail-closed), allowlisted projections. New `docketHandlers.ts`; +10 IPC tests; suite 369→379. |
| `7c30664` | #51 | GitHub PR merge of WI-601 to main. |

## Decisions (alternatives, why chosen, what would falsify)

1. **Docket create + confirm in one WI** (not propose-only) — the lawyer-visible deadline requires confirm to materialize the `CaseBoxDeadline`. Falsifier: confirm not feeding `listDeadlines`, or a path to `confirmDocketEntry` without the scoped preflight. Audited Q1–Q7 in `audit-mq0o2ujb-oy46d6` (PASS); verify `verify-mq0obd15-8n3ha8` ALL CLOSED.
2. **Reuse `invalid_payload` for scoped-confirm miss (Option 1, user-authorized)** — a precise `unknown_docket_entry` code would change a stable error surface + exceed the WI's no-persistence-src scope; surfaced as a stop-and-report and resolved by the user.
3. **Barrel-bypass kept in-scope** — `handlers.ts` was outside WI-601 Allowed-files, so docket handlers are imported directly; recorded as CBW-601-BARREL (the L1 below).

## Audit findings + disposition

- **C0 / H0 / M0 / L1.** No accumulation drift. WI-601 is a strict ADDITION (two tenant-scoped write channels + new module) — it does not weaken the read handlers or existing write paths; both writes enforce matter-existence + active-tenant; the confirm scoped `getDocketEntry` preflight is fail-closed (confirmDocketEntry not reached on a scoped miss); responses use dedicated allowlisted projections stripping authority identities. The governance commit `d868689` is internally consistent (queue.md ↔ queue.governed; one `QUEUE_REVIEW_VERDICT=PASS`). No renderer/UI, no `services/case-box-persistence/src`, no contract schema, no `errors.ts`/error-code surface, no manifest/dependency, no hook, no marker/breaker source tampering, no test deletion/weakening.
- **L1 (CBW-601-BARREL, cleanup-only) — DISPOSITIONED**: `docketHandlers.ts` is imported directly (not via the `handlers.js` barrel) because `handlers.ts` was outside WI-601's governed Allowed-files. Functional + tested; no behavior/security impact. **Already recorded** in `dev-memo/deferred-audit-findings.md` (CBW-601-BARREL, open) with a follow-up to add the barrel re-export when `handlers.ts` is next in scope (folds with WI-602 or a barrel-reconciliation WI). Safe-to-proceed: YES.

## Per-WI provenance (already independently verified)

- #51 WI-601: review-plan `review-plan-mq0mc4ag-jgpcf4` (NEEDS-FIX → user Option 1) → revised → `review-plan-mq0mlwed-39i0wf` (TIMEOUT) → compact retry `review-plan-mq0npsuu-i8m5ai` (READY) → audit `audit-mq0o2ujb-oy46d6` (PASS C0 H0 M0 L0) → verify `verify-mq0obd15-8n3ha8` (ALL CLOSED).
- `d868689` governance: review-plan `review-plan-mq0lwv2m-fonzmq` (READY-WITH-LOW); queue-lint PASSED; `reviews/queue-review-004.md`.
- `f68348f` is the doc-only product of the prior closeout (`audit-mq0l2wsh-9ht7f0` BATCH-PASS for `f444325..bf8df09`).
- This Layer-B pass checks the ACCUMULATION, not the per-WI line findings.
