# Smoke plan — CCSUITE-PATH1-RCA-01

A trivial plan for testing cc-suite Path 1 health.

## §1 Scope

Test that `codex exec` returns output within reasonable time.

## §2 Acceptance

`/review-plan dev-memo/ccsuite-smoke-plan.md --wait` returns a verdict or a clear error within 5 minutes.
