{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://litigation-platform.local/case-box-contract/case-box-evidence-item.schema.json",
  "title": "CaseBoxEvidenceItem",
  "description": "v1-simplified evidence shape: exhibit binding to a matter with optional source document + page range + lawyer weight. The full fact-binding-to-element model is a later case-box step.",
  "type": "object",
  "required": [
    "id",
    "tenant_id",
    "actor_user_id",
    "matter_id",
    "lawyer_weight",
    "status",
    "created_at"
  ],
  "properties": {
    "id":            { "$ref": "#/$defs/ulid" },
    "tenant_id":     { "type": "string", "minLength": 1 },
    "actor_user_id": { "type": "string", "minLength": 1 },
    "matter_id":     { "$ref": "#/$defs/ulid" },
    "source_document_id": {
      "anyOf": [
        { "type": "null" },
        { "$ref": "#/$defs/ulid" }
      ]
    },
    "exhibit_page_range": {
      "anyOf": [
        { "type": "null" },
        { "type": "string", "minLength": 1, "description": "Free-form e.g. \"1-3\" or \"7\". v1 does not constrain the format." }
      ]
    },
    "lawyer_weight": {
      "enum": ["weak", "moderate", "strong"]
    },
    "status": {
      "enum": ["proposed", "accepted", "rejected", "superseded"]
    },
    "supersedes_evidence_id": {
      "anyOf": [
        { "type": "null" },
        { "$ref": "#/$defs/ulid" }
      ],
      "description": "Required when status is \"superseded\"."
    },
    "notes":      { "type": "string" },
    "evidence_title": {
      "type": "string",
      "minLength": 1,
      "description": "Lawyer-entered display name for the evidence item (T3 证据名称). Optional; absent on legacy rows. NOT derivable from a document filename. FORMS-T3-S0-SCHEMA-00 §4 Option A."
    },
    "proof_statement": {
      "type": "string",
      "minLength": 1,
      "description": "Lawyer-entered proof statement (T3 证明内容). Optional; blank content is expressed by ABSENCE — persistence normalizes whitespace-only input to absent, never stores an empty string. FORMS-T3-S0-SCHEMA-00 §4 Option A."
    },
    "display_order": {
      "type": "integer",
      "minimum": 0,
      "description": "Optional lawyer-controlled catalogue order (absence-only: present or absent, no null). Absent rows sort after ordered rows by created_at ASC, id ASC. FORMS-T3-S0-SCHEMA-00 §4 Option A."
    },
    "created_at": { "type": "string", "format": "date-time" },
    "party_side": {
      "anyOf": [
        { "type": "null" },
        { "enum": ["our", "opposing"] }
      ],
      "description": "Optional marker for which party introduced the evidence. v1 supports §7.A 'evidence list for both parties'. R-5(g)."
    }
  },
  "allOf": [
    {
      "if": {
        "properties": { "status": { "const": "superseded" } },
        "required": ["status"]
      },
      "then": {
        "required": ["supersedes_evidence_id"],
        "properties": {
          "supersedes_evidence_id": { "$ref": "#/$defs/ulid" }
        }
      }
    }
  ],
  "$defs": {
    "ulid": {
      "type": "string",
      "pattern": "^[0-9a-z]{26}$"
    }
  }
}
