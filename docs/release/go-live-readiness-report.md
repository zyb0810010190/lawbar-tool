# Go-Live Readiness Report — INTERIM SNAPSHOT

> **STATUS: INTERIM SNAPSHOT — NO FINAL GO-LIVE VERDICT.** This is a mid-flight readiness
> refresh against `main` @ `febbaf2` (2026-07-04), produced by `WI-GOLIVE-READINESS-REFRESH-00`
> (Type: EVIDENCE). It is **NOT** the final go-live gate. The **final GO / NO-GO verdict is
> blueprint gate 21, which remains BLOCKED** until every v1-blocking gate clears and the three
> user hard-stops are resolved. Emitting a GO here would be false; none is emitted.
> Authoritative gate list: `dev-memo/plan-go-live-readiness-00.md` §1 (21 gates) + §4 (STOP-AND-ASK).

## Verdict

**NO FINAL VERDICT (interim).** Final GO / NO-GO is blocked pending the three user hard-stops
(§4 below) and closure of the OPEN gates.

## 1. Refreshed 21-gate matrix (as of `main` @ `24be566`, 2026-07-08 — WI-RELEASE-HOLISTIC-READINESS-REFRESH-00)

Refreshed against the rev-4 blueprint baseline. Material deltas since that baseline are flagged **[Δ]**.

| # | Gate | Status | Evidence (current) | Blocker / next action |
|---|---|---|---|---|
| 1 | Case-box persistence (memory + SQLite) | **CLEARED** | `CURRENT_SCHEMA_VERSION=12` (`services/case-box-persistence/src/sqlite/schema.ts:51`); persistence tests 573+273+288/0 (this refresh) | — |
| 2 | OCR pipeline (worker/queue/persistence/ingestion/review) | **PARTIAL — per-package OCR test sweep + integration PASS [Δ]** (still NOT cleared) | `services/ocr-*` packages present + prior sweeps; **`docs/release/gate2-ocr-pipeline-verify-00.md`** (WI-RELEASE-G2-OCR-PIPELINE-VERIFY-00): test-only sweep of the 4 production packages — persistence **231/0** (abi-smoke OK), worker **472/0** (3 env-gated skips = `127.0.0.2` loopback alias unavailable on macOS, sub-case 1 still runs), ingestion **30/0**, review **38/0** = **771 pass / 0 fail / 3 skip**; `AGENTS.md` critical invariants (coordinator lifecycle; queue dedupe key; stable `OcrQueueError` codes; no FK queue→ocr_jobs) directly asserted by named tests; `ocr-worker-bakeoff` (ADR-11A.1) excluded. Result **PASS**. | Test sweep **PASS**. Gate 2 stays **PARTIAL** — a test sweep is not a go-live sign-off; it does NOT clear gates 5/6/9/12/14/15/18/19/20. Residuals: no dedicated e2e harness (R-G2-1); 127.0.0.2-loopback transport sub-cases platform-gated (R-G2-2, gate-9 adjacent). PARTIAL→CLEARED belongs to a holistic readiness refresh (gate 21 STOP-AND-ASK, user). |
| 3 | Client surface (Mac desktop app) | **PARTIAL [Δ]** — was "NOT STARTED"; corrected by evidence inventory; self-declaration blocker now closed (WI-GATE3) | **11 renderer screens** (`renderer/screens/`: listMatters/createMatter/archiveMatter/viewMatter + viewMatter{Audit,Deadlines,DocketProposals,Documents,Facts,Links,T3Catalog}) + `auditEventLabels.ts` (a non-screen label map — a renderer-only `event_kind`→human-label lookup, not a screen); **26 case-box IPC channels** (`src/caseBox/handlerShared.ts`); electron `main.ts` + `preload.mts` present; **forms-T3 证据目录及说明 preview + DOCX export** shipped (`t3*.ts`; channels `casebox:t3:previewCatalog`/`exportDocx`); the stale `package.json` "fixture only / NOT product UI / NOT for distribution" self-declaration is **corrected** (WI-GATE3-CLIENT-RELEASE-HARDENING-00) to an accurate v1 local-first case-box client framing that preserves the honest gate-4 signing/distribution deferral | **Substantial functional case-box UI is built + now accurately self-described.** Residual (gate 3 stays PARTIAL — see `docs/release/gate3-client-release-readiness-00.md`): **R1** signing/notarization/public-distribution/release-posture → gate 4 STOP-AND-ASK (user); **R2** the *global* overdue-deadline dashboard banner (brief §10 — app-open/cross-matter) is now **SHIPPED** (WI-GATE3-R2-OVERDUE-DASHBOARD-BANNER-00: a `role="status"` banner on the matter-list home, client-side aggregation over the existing `casebox:matter:list` + `casebox:deadline:list` channels with pagination drain, reusing `classifyDeadlineUrgency`; broker audit → H1 fixed → verify ALL CLOSED; the per-matter urgency banner in `viewMatterDeadlines.ts` (brief §18) is unchanged); **R3** ASSESSED **non-M0 / deferred** (`docs/release/gate3-r3-polish-assessment-00.md`, WI-RELEASE-G3-R3-POLISH-ASSESS-00): a read-only inspection of the **11 renderer screens** (the accurate count, now reconciled in the evidence column above — `auditEventLabels.ts` is a non-screen label map; WI-RELEASE-G3-SCREEN-COUNT-READINESS-HYGIENE-00 corrected the prior twelve-screen count) found **no M0-blocking error/empty-state defect** (no crash-on-empty, no white-screening unhandled rejection, no silent loss of lawyer input; every screen surfaces the `role="alert"` envelope-error path + a clear empty state, and the lawyer-input write paths catch transport throws). Residual = post-v1 polish (R3-FUP-1: add a transport-error `catch` to 3 read/list loads — `viewMatterAudit`/`viewMatterDocuments`/`viewMatterDocketProposals` — matching the `viewMatterLinks` exemplar, degrades only to a stuck spinner today; R3-FUP-2: error/empty copy + i18n consistency). No R3 implementation WI is required before gate 6. Gate 3 stays **PARTIAL** — its release posture depends on gate 4 (STOP-AND-ASK, R1); the R3 assessment does not clear gate 3 or auto-authorize gate 6. |
| 4 | Distribution + signing + manual download (v1) | **STOP-AND-ASK** | brief §4/§16 | **User decision**: Apple Developer ID + code-sign + notarization for PUBLIC release; framework confirm. Dev-mode signing is acceptable-deferred; public distribution cannot clear without this. |
| 5 | All package tests pass | **CLEARED [Δ]** | contract 455/0, persistence 573+273+288/0; desktop **801/0** (refreshed post-R2 — was 791/0; +10 for the WI-GATE3-R2 overdue-dashboard-banner tests); ocr-worker **472/0** (3 pre-existing engine skips) — the prior SIGINT idle-loop spawn flake is **deterministically resolved** (WI-GATE5-OCR-WORKER-SIGINT-FLAKE-00: a child-side stderr readiness marker emitted after the SIGINT/SIGTERM handlers are armed, with the spawn test waiting for it before sending SIGINT; 15/15 targeted + 5/5 full-file repeated-run determinism; broker audit CLEAN `audit-mr7f42oq-jaqa51`) | — (gate 5 CLEARED; does NOT imply go-live — the final GO/NO-GO verdict + the three STOP-AND-ASK hard-stops remain the user's) |
| 6 | Full audit clean (no C/H/M) | **CLEARED — full-project audit clean at HEAD `57a746f` (C0/H0/M0) [Δ]** | **`docs/release/gate6-full-project-audit-00.md`** (WI-RELEASE-G6-FULL-PROJECT-AUDIT-00): the final autonomous full-project audit at `main` HEAD `57a746f`. Fresh cross-cutting cc-suite audit `audit-mrc11uyg-deh2lt` → **C0/H0/M0, documented Lows only**; corroborated by the 226-entry Layer-B batch-closeout chain (linear ancestry to HEAD, every window BATCH-PASS, with disclosed in-lane M1/H1 remediations + one governance/docs-only batch-147 log-numbering gap) + the deferred-findings backlog reconfirmed all-Low/Safe=YES, **0 open Medium+** (the one High row is closed) + critical-invariant spot-checks (OcrQueueError codes intact; case-box schema v12; SSRF/TLS/DNS fetcher present) + green test state (gate-5 all-suites; desktop 801/0 post electron 39.8.5). "None omitted" per-surface checklist: all product surfaces corroborated/freshly-audited, `ocr-worker-bakeoff` excluded (out of production graph). | Full-project audit **clean (no C/H/M)** — gate 6's condition met. **This clears gate 6 ONLY** (its clearance is a clean-audit condition, not a user decision). It does **NOT** clear the user-owned STOP-AND-ASK gates 4/11/17/21, does **NOT** decide gate-7 D-G7-1/D-G7-2, and does **NOT** imply a final GO/NO-GO. Go-live independence preserved; the holistic readiness refresh (reconciling the §4 roll-up) is the separate next lane. |
| 7 | Mutation-test / robustness policy | **OPEN — policy authored + crash-recovery drill PASS [Δ]** (was "OPEN — policy authored"; still NOT cleared) | **`docs/release/gate7-robustness-policy-00.md`** (WI-RELEASE-G7-ROBUSTNESS-POLICY-00): the v1 local-first robustness policy — six closure criteria (crash/data-loss tolerance; local-first persistence/recovery; offline/online reconciliation = **N/A v1**; audit/event integrity under failure; failure-mode handling; sufficient-evidence-to-clear) mapped to existing evidence (`auditChain.ts`, `better-sqlite3` WAL/synchronous + startup schema-integrity check per brief §14, stable `CaseBoxPersistenceError`/`OcrQueueError` codes, gate-5 graceful shutdown); dependencies mapped to gates 6/12/14/15/5 (not cleared). **`docs/release/gate7-crash-recovery-drill-00.md`** (WI-RELEASE-G7-CRASH-RECOVERY-DRILL-00): the crash-recovery drill EXECUTED against the real persistence layer on a disposable fixture — **PASS** (`PRAGMA integrity_check=ok` after a `kill -9` mid-write; in-flight uncommitted txn rolled back atomically; audit `event_count==COUNT==MAX(sequence)` holds across crash + restore round-trip; counts preserved). Honest finding: v1 uses a single `case-box.sqlite` (audit tables inside it), not the brief §14 two-file model → residual reconciliation follow-up | **Gate 7 stays NOT CLEARED — BLOCKED on user risk-acceptance (the crash-recovery drill is now PASS, but the clear is still user-owned).** The policy **recommends** accept-v1-without-mutation-testing + a documented crash-recovery drill (now DONE, PASS; mutation/fuzz deferred post-v1) but **surfaces the risk-acceptance as a USER decision** (D-G7-1 accept-without-mutation-testing?; D-G7-2 require the crash-recovery drill? — a PASS satisfies the drill requirement IF the user answers YES). Clear depends on those user decisions; a drill PASS + a gate policy do NOT imply go-live. |
| 8 | Deferred Lows triage | **PARTIAL — deferred-Lows reconfirmed: 37 open, all Safe=YES, 0 M0-blockers [Δ]** (still NOT cleared) | `dev-memo/deferred-audit-findings.md`: **37 open** (35 wide-table + 2 vertical), **ALL Low, ALL Safe=YES, 0 Medium+**; **`docs/release/gate8-deferred-lows-reconfirm-00.md`** (WI-RELEASE-G8-DEFERRED-LOWS-RECONFIRM-00): read-only gate-close reconfirmation — each open Low re-confirmed Safe=YES + not an M0-product blocker (enforcement-hook defense-in-depth [some user-deferred; BRCBW-9 = user threat-model decision], desktop-client convention/coverage [behavior correct+tested], Phase-A/B SQLite LOC/cleanup/perf debt, release-doc precision nits). 0 required escalation; no registry status flip needed. Result **PASS** | Reconfirmed **PASS** — none is a court-facing correctness/security defect. Gate 8 stays **PARTIAL** (registry accrues new Lows; a triage is not a go-live sign-off). Follow-up WIs proposed (shell-aware-parsing, BRCBW-9 user decision, barrel/enum/FileVault-smoke, doc-precision) — none M0-blocking. PARTIAL→CLEARED belongs to a holistic readiness refresh; the STOP-AND-ASK hard-stops (4/11/17/21) + the final GO/NO-GO remain the user's. |
| 9 | SSRF / TLS / DNS-pinning (OCR fetcher) | **CLEARED** | `docs/release/wi-03-security-signoff.md` (WI-03d `ce3f287`) | — |
| 10 | Case-box persistence security boundary | **CLEARED pending user go-live approval [Δ]** — was "OPEN" | the read-only security-boundary audit shipped: `docs/release/casebox-persistence-security-audit-00.md` (broker audit `audit-mr6eouaf-p0k7yg`, C0 H0 M2 L1) — PASS on IPC/server-authority boundary, SQLite parameterization + payload-json canonicality + no-queue→ocr_jobs-FK, error projection, and write-time `child.tenant==matter.tenant`. Its two Medium **defense-in-depth** findings (M-1 child-reads / M-2 id-mutations lacked a row-level `tenant_id` predicate) + the Low (L-1 tests) were **FIXED** by WI-SEC-CASEBOX-TENANT-SCOPING-DEFENSE-00 (predicate-only; drainless caller-tenant enforcement on the 8 child reads + resolved-row-scoped UPDATE + `changes===1` on the 7 child mutations) with a **clean re-audit** (`audit-mr71e737-lqz7sg` C0 H0 M0 L0); report §8 records gate 10 CLEARED pending user go-live approval. `CURRENT_SCHEMA_VERSION` stays 12 | — (gate 10 CLEARED pending user go-live approval; does NOT imply go-live — the final GO/NO-GO verdict + the three STOP-AND-ASK hard-stops remain the user's; a separate follow-up hardening WI could still further scope, but no Medium+ remains) |
| 11 | 律师法 confidentiality compliance | **STOP-AND-ASK** | brief §15 | **User/legal decision**: legal review of audit log + privilege markers + classification model. |
| 12 | Tamper-evidence / audit-chain integrity | **PARTIAL — audit-chain recovery + tamper-evidence PASS [Δ]** (still NOT cleared) | `auditChain.ts`; case-box-step-4-audit-log-shape ADR; **`docs/release/gate12-audit-chain-recovery-00.md`** (WI-RELEASE-G12-AUDIT-CHAIN-RECOVERY-00): the operational drill run against the REAL persistence layer on a disposable `/tmp` fixture — seed 3 matters × 3 audit events → clean `verifyAuditChainForMatter` = `ChainVerifyOk` + invariant `event_count==COUNT==MAX(sequence)` → backup-as-directory → **tamper a mid-chain event → DETECTED as `ChainVerifyErr(prev_event_hash_mismatch)`** → restore → `ChainVerifyOk` + invariant. Result **PASS**. Honest nuance: the count/sequence invariant is structural-completeness, NOT tamper-evidence (the hash chain is). | Drill **done, PASS**. Gate 12 stays **PARTIAL** — a tamper-evidence drill is not a go-live sign-off; it does NOT clear gates 14/18/15 or imply gate 7 cleared. PARTIAL→CLEARED belongs to a holistic readiness refresh (gate 21 STOP-AND-ASK, user). |
| 13 | Operator checklist + runbook | **OPEN — operator checklist + runbook docs completed [Δ]** (still NOT cleared; movement, if any, belongs to the next holistic readiness refresh) | **`docs/release/operator-checklist.md`** (WI-RELEASE-G13-OPERATOR-CHECKLIST-RUNBOOK-00): completed for the v1 Mac-client surface — a Mac-client operator section (install/launch incl. the FileVault production gate; data at `~/Library/Application Support/lawbar/` = `case-box.sqlite` + `case-box-documents/` + `theme-preference.json`, grounded in `main.ts`/`caseBoxRuntime.ts`; first-run; normal op; quit/restart; offline/local-first) + case-box local-first operating procedures + references to gate 14 (backup/recovery) / gate 15 (rollback) / gate 2 (OCR) / gate 20 (telemetry) evidence **without clearing them** + a "before gate 13 can move" completion checklist. **`docs/release/ocr-worker-runbook.md`**: completed + **RECONCILED** — the OCR worker is NOT wired into the v1 Mac client (verified: no OCR import in `apps/lawbar-desktop/`), so the stale OCR *service* production-profile framing (S3/HTTPS-allowlist/production-env/healthcheck) is labelled **`post-v1 / deferred`** with a three-place `RECONCILIATION-NEEDED` audit trail (runbook + commit + review artifact) per `.claude/rules/client-local-first.md`, never a silent rewrite. | Docs now completed. Gate 13 stays **OPEN** — a completed operator checklist is not a go-live sign-off; it does NOT clear gates 2/6/12/14/15/18/19/20 (references, not clearances). The OCR-service→local-first reconciliation may need a follow-up ADR (post-v1). OPEN→PARTIAL/CLEARED belongs to a holistic readiness refresh (gate 21 STOP-AND-ASK, user). |
| 14 | Backup + recovery procedure | **OPEN — procedure documented + drill-verified [Δ]** (PARTIAL-eligible at next readiness refresh; still NOT cleared) | **`docs/release/gate14-backup-recovery-00.md`** (WI-RELEASE-G14-BACKUP-RECOVERY-00): the operator backup + recovery runbook, manifest grounded in the REAL shipped v1 layout read from source (single `case-box.sqlite` with audit tables inside + `case-box-documents/` blob dir + `theme-preference.json` under `~/Library/Application Support/lawbar/` — `caseBoxRuntime.ts` / `main.ts`), backup-as-directory (WAL-sidecar caveat) + drop-directory restore + `PRAGMA integrity_check` + audit `event_count==COUNT==MAX(sequence)` verification. **Verified by** the executed gate-7 crash-recovery drill (`gate7-crash-recovery-drill-00.md`, PASS). Logs a brief §14 divergence (two-file model vs shipped single-file) as `RECONCILIATION-NEEDED` (§7 — brief not edited). | Procedure now documented + drill-verified. Gate 14 stays **OPEN** pending: (a) the brief §14 manifest reconciliation (R14-FUP-1); (b) the gate-18 export-format certification overlap; (c) a holistic readiness refresh to move the roll-up bucket. A documented procedure does NOT imply go-live (gate 21 STOP-AND-ASK, user). |
| 15 | Rollback procedure tested | **PARTIAL — rollback dry-run PASS [Δ]** (still NOT cleared) | `dev-memo/rollback-00.md`; the git-revert / closeout discipline exercised repeatedly; **`docs/release/gate15-rollback-dry-run-00.md`** (WI-RELEASE-G15-ROLLBACK-DRY-RUN-00): the committed-rollback dry-run (`rollback-00.md` §3 — `git revert`, NEVER `git reset`) run on a **disposable scratch commit** on a **temporary branch** (`dry-run/g15-rollback-scratch`, deleted after; never pushed/merged), with **`main` proven unmutated** (HEAD `99d7935…` identical before/after; scratch commit shown to exist only on the temp branch, unreachable after cleanup). Result **PASS** (revert applied cleanly with no conflict; `git diff main..temp` empty = tree restored; no `git reset` used). | Dry-run **done, PASS**. Gate 15 stays **PARTIAL** — a rollback dry-run is not a go-live sign-off; it does NOT clear gates 12/14/18 or imply gate 7 cleared. PARTIAL→CLEARED belongs to a holistic readiness refresh (gate 21 STOP-AND-ASK, user). |
| 16 | Project brief READY + reconciliation log | **PARTIAL — brief READY + reconciliation log clean [Δ]** (still NOT cleared) | brief `status: READY` (rev 5); the T4 brief-vs-PRD reconciliation is RESOLVED as accepted divergence (`docs/adr/ADR-forms-t4-proof-model-scope.md`, DECIDED); **`docs/release/gate16-brief-ready-reconciliation-verify-00.md`** (WI-RELEASE-G16-BRIEF-READY-RECONCILIATION-VERIFY-00): read-only per-entry classification of the reconciliation log — R-1/R-2/R-3(+grouped)/R-6/R-8/R-9 = **post-v1**, R-4/R-7 = post-v1/non-blocking, **R-5 (the only v1-day-one entry) = RESOLVED** (its additive contract surface — `CaseBoxFact.purpose`/`as_of_date`, `CaseBoxDocument.purpose`, `CaseBoxEvidenceItem.party_side`, `CaseBoxMatter.successor_matter_id`, deadline-kind ext — ships in the schemas), T4 = accepted-divergence. **0 unresolved M0-blocking entries.** Result **PASS**. | Verified **PASS** — brief READY, reconciliation log clean (0 M0-blocking). Gate 16 stays **PARTIAL** — a verification is not a go-live sign-off. Residual: post-v1 SYNC reconciliation program (R-1/2/3) + docs-only reconciliation WIs (R-4/7/8) — none M0-blocking. PARTIAL→CLEARED belongs to a holistic readiness refresh; the STOP-AND-ASK hard-stops (4/11/17/21) + the final GO/NO-GO remain the user's. |
| 17 | License + copyright + privacy notice | **OPEN** | brief §17 | Add LICENSE/NOTICE + privacy notice; license choice is a legal/business STOP-AND-ASK. |
| 18 | Data export / backup format certification | **OPEN — v1 export/backup formats certified [Δ]** (PARTIAL-eligible at next readiness refresh; NOT a clearance of gates 12/14/15/7, NOT go-live) | **`docs/release/gate18-export-backup-format-cert-00.md`** (WI-RELEASE-G18-EXPORT-BACKUP-FORMAT-CERT-00): certifies the bounded v1 surface — backup-as-directory (brief §165; the gate-14 runbook cited as *supporting evidence* for the backup format, NOT clearing gate 14) + the T3 证据目录及说明 DOCX export as the only shipped structured export. Fidelity run green 2026-07-07 (60/0): A10 CanonicalExportModel byte-determinism (golden + sha256 + rebuild-stable — the reproducibility layer per evidence-genie invariant 9, not raw .docx bytes) + T3 DOCX/IPC/catalog-model tests. Post-v1 structured exports (brief §166 signed bundle / PDF chronology / proof matrix / privilege log / fact dump; §64 no bulk/privilege; §164 no redacted) recorded as STOP-AND-ASK exclusions. | v1 export/backup formats now certified. Gate 18 stays **OPEN** — a bounded-surface certification is NOT a clearance of gates 12/14/15, does NOT imply gate 7 cleared, and is NOT a go-live sign-off (gate 21 STOP-AND-ASK, user). PARTIAL-eligible at a holistic readiness refresh (which owns the roll-up bucket move). |
| 19 | Supply-chain posture | **OPEN — supply-chain inventory + audit + provenance recorded [Δ]** (still NOT cleared; movement, if any, belongs to the next holistic readiness refresh) | **`docs/release/gate19-supply-chain-posture-00.md`** (WI-RELEASE-G19-SUPPLY-CHAIN-POSTURE-00): a read-only inventory/`npm audit`/provenance sweep (Node 24.14.0/npm 11.9.0, 2026-07-08). **v1 production surface CLEAN** — `npm audit --omit=dev` = **0 vulnerabilities** across all 6 packages (`apps/lawbar-desktop` + `services/ocr-{worker,persistence,ingestion,review}` + `case-box-persistence`). Runtime deps inventoried: `better-sqlite3` 12.10.0 (MIT, native; `prebuild-install`\|node-gyp; `abi-smoke` guard), `docx` 9.7.1 (MIT/pure-JS; prior ACCEPTABLE review `audit-mr63d6y7`), two first-party committed tarballs (traced to in-repo source), OCR engine `@gutenye/ocr-node` 1.4.8 (MIT; not wired into v1 client, model-gated/post-v1). License **inventory** recorded (all MIT / first-party) — NOT a gate-17 decision. **R-G19-1 REMEDIATED** (`docs/release/electron-runtime-advisory-bump-00.md`, WI-RELEASE-ELECTRON-RUNTIME-ADVISORY-BUMP-00, **FULL-REMEDIATION PASS**): the shipped Electron runtime was bumped **34.5.8 → 39.8.5** (the minimal published version above the `<=39.8.4` vulnerable range — npm's `fixAvailable` 43.1.0 was latest-not-minimal), which **clears the electron HIGH advisories** (`npm audit --omit=dev` = 0; the electron entry is gone, dev-inclusive 13→12); desktop tests **801/0**, `npm run dist` packaging **green** on both mac arches (electron-builder 25.1.8 ↔ electron 39 compatible, no electron-builder bump needed), `better-sqlite3` native binding rebuilt + ABI-validated; lockfile diff confined to the electron subtree. Remaining dev-inclusive advisories (11 high/1 mod) are the `electron-builder` build toolchain (not shipped, advisory-only). | Posture **recorded**; **R-G19-1 remediated (FULL PASS)**. Gate 19 stays **OPEN** — a supply-chain record + a runtime-advisory bump are not a go-live sign-off; they do NOT clear gates 4/6/12/13/17/20 (references, not clearances). Gate-4 signing/notarization of the packaged runtime + a license **decision** (gate 17) stay user-owned; the `release/` builds here are unsigned dev artifacts. OPEN→PARTIAL/CLEARED belongs to a holistic readiness refresh (gate 21 STOP-AND-ASK, user). |
| 20 | Telemetry / crash-reporting policy | **PARTIAL — no-telemetry + crash-reporting-off verified [Δ]** (still NOT cleared) | brief §4 (no telemetry; crash reporting OFF default); **`docs/release/gate20-telemetry-crash-reporting-verify-00.md`** (WI-RELEASE-G20-TELEMETRY-CRASH-REPORTING-VERIFY-00): case-insensitive read-only inspection of the v1 Mac client (`electron/main.ts`+`preload.mts`, `src/**`, `renderer/**`, `package.json`+`package-lock.json`) — **NO** `crashReporter`/Sentry/Bugsnag/Crashpad/analytics/metrics/telemetry/`sendBeacon`/`autoUpdater` matches; **no `crashReporter.start()`** (Electron crash reporting is opt-in → absent = OFF by default); **no `fetch()`/external beacon**; deps clean (`better-sqlite3`/`docx`/internal tarballs + dev electron/playwright/tsc). The only bare-`tracking` hits are CSS `--tracking-*` letter-spacing tokens (typography false positives, excluded); no telemetry-relevant tracking. Result **PASS**. | Verified **PASS**. Gate 20 stays **PARTIAL** — a code verification is not a go-live sign-off; it does NOT clear gates 4/6/7/11/12/14/15/18/19/21. The App Sandbox no-network entitlement is gate-4 packaging; a runtime egress observation (R-G20-1) + supply-chain sweep (gate 19) are follow-ups. PARTIAL→CLEARED belongs to a holistic readiness refresh (gate 21 STOP-AND-ASK, user). |
| 21 | Final go-live readiness report | **STOP-AND-ASK / BLOCKED — this doc is INTERIM, not the final gate** | this file | The final go-live sign-off is a USER STOP-AND-ASK decision. Fill the FINAL report only AFTER every other gate clears + the three STOP-AND-ASK hard-stops (4/11/21) resolve. **BLOCKED — no agent lane clears this.** |

### Refreshed roll-up (as of `main` @ `24be566`, 2026-07-08; refreshed by WI-RELEASE-HOLISTIC-READINESS-REFRESH-00 — all 21 gates accounted for, none dropped)
- **CLEARED**: 1, 5, 6 **[Δ up from OPEN — full-project audit clean at HEAD `57a746f`, C0/H0/M0; `docs/release/gate6-full-project-audit-00.md`. The audit remains valid at this refresh HEAD `24be566`: `57a746f` is a direct ancestor of `24be566`, and the only commits since are docs/governance (the gate-6 closeout + the readiness-refresh governance + closeouts) — no product source/test/dependency change]**, 9 (4).
- **CLEARED pending user go-live approval**: 10 (1).
- **PARTIAL / residual (agent-remit evidence complete; full clearance awaits the user-owned gates)**: 2, 3, 8, 12, 13 **[Δ up from OPEN — operator checklist + runbook completed]**, 14 **[Δ up from OPEN — backup/recovery documented + drill-verified]**, 15, 16, 18 **[Δ up from OPEN — v1 export/backup formats certified]**, 19 **[Δ up from OPEN — supply-chain posture recorded + R-G19-1 electron runtime advisory remediated (34.5.8 → 39.8.5)]**, 20 (11).
- **OPEN — user-decision-gated (NOT an agent-remit gate)**: 7 **[policy authored + crash-recovery drill PASS, but BLOCKED on the user's D-G7-1/D-G7-2 risk-acceptance — the drill does NOT resolve those; gate 7 stays user-decision-gated]**, 17 **[license/copyright/privacy — a user legal/business decision]** (2).
- **STOP-AND-ASK (user decision)**: 4 (distribution/signing/notarization), 11 (律师法 confidentiality), 21 (final go-live sign-off) **[gate 21 = final go-live sign-off, a user STOP-AND-ASK; this doc remains INTERIM until gate 21 is filled]** (3).
- **Net delta vs the prior interim snapshot (WI-RELEASE-READINESS-REFRESH-00, `main` @ `febbaf2`):** the agent-remit gate sweep is now **complete** — every non-user-owned gate is CLEARED or PARTIAL/evidence-complete. Advances: **gate 6 OPEN → CLEARED** (the final autonomous full-project cc-suite audit at HEAD `57a746f`, C0/H0/M0, corroborated by the unbroken all-BATCH-PASS Layer-B chain + the all-Low/0-open-Medium+ deferred backlog); **gate 13 OPEN → PARTIAL** (operator checklist + runbook completed for the v1 Mac client, `gate3-r3`-style docs; the stale OCR-service framing reconciled post-v1); **gate 14 OPEN → PARTIAL** (backup + recovery runbook documented + gate-7-drill-verified); **gate 18 OPEN → PARTIAL** (v1 export/backup formats certified — A10 CanonicalExportModel + T3 DOCX); **gate 19 OPEN → PARTIAL** (supply-chain inventory/`npm audit`/provenance recorded — production surface 0 vulns — AND the surfaced R-G19-1 Electron runtime advisory REMEDIATED via the minimal electron 34.5.8 → 39.8.5 bump, FULL PASS: `run dist` green both arches, desktop 801/0, `better-sqlite3` ABI rebuilt); **gate 3** reconciled — R2 shipped, **R3 assessed non-M0 / deferred** (`gate3-r3-polish-assessment-00.md`), the readiness-row screen count corrected 12 → 11 (+ label map); gates **2/8/12/16/20** evidence-advanced (OCR verify; deferred-Lows reconfirmed all-Safe/0-Medium+; tamper-evidence drill; brief-READY + reconciliation clean; no-telemetry verified). **Gate 7 stays user-decision-gated** (policy + drill PASS do not clear it; D-G7-1/D-G7-2 are the user's). No gate dropped; every still-non-cleared gate is preserved above; the user-owned gates 4/7(D-G7)/11/17/21 are NOT cleared; **NO final GO/NO-GO is asserted (gate 21 stays BLOCKED, this doc stays INTERIM).**

## 2. M0-ready surfaces (built + evidenced)
- Case-box persistence + contract (SQLite, schema v12, deterministic tests green).
- OCR pipeline packages present (verify-pending).
- Append-only hash-chained audit log.
- Case-box functional UI: matters (list/create/view/archive), documents, deadlines, docket, facts, links, audit, **forms-T3 证据目录及说明 preview + DOCX export** — 12 screens, 26 IPC channels.
- HTTPS/SSRF/TLS/DNS-pinning security sign-off (gate 9 CLEARED).

## 3. M0 blockers (must clear before go-live) — reconciled by WI-RELEASE-HOLISTIC-READINESS-REFRESH-00 (each prior blocker marked closed / still-open / transferred-to-user; none dropped)
1. **Client release-hardening** (gate 3, PARTIAL) — **partially closed, remainder transferred to user**: the self-declaration is corrected, R2 (global overdue-deadline dashboard banner) **shipped**, and **R3 is now ASSESSED non-M0 / deferred** (`gate3-r3-polish-assessment-00.md` — no crash/white-screen/input-loss; post-v1 R3-FUP-1/R3-FUP-2 only) — so R3 is **closed as a blocker**. The remaining residual = **R1** signing/notarization/public-distribution → **transferred to gate-4 STOP-AND-ASK (user)**.
2. **Full-audit + robustness closure** (gates 6, 7): gate 6 (full-project audit) is **CLOSED — CLEARED** (`gate6-full-project-audit-00.md`, C0/H0/M0 at HEAD `57a746f`); gate 5 CLEARED + gate 10 CLEARED-pending remain as before. What remains is gate 7 — **still open, transferred to user**: policy authored + crash-recovery drill PASS, but **BLOCKED on the user's D-G7-1/D-G7-2 risk-acceptance** (the drill does not resolve those).
3. **Release documentation** (gates 13, 14, 15, 18, 19, 20) — **closed as blockers (now PARTIAL/evidence-complete)**: operator checklist + runbook completed (13); backup+recovery documented + drill-verified (14); rollback dry-run PASS (15); data-export/backup formats certified (18); supply-chain posture recorded + the R-G19-1 Electron runtime advisory REMEDIATED (19); telemetry no-op verified (20). Gate **17** (license/copyright/privacy) is **still open, transferred to user** (a legal/business decision).
4. **Partial / verify closure** (gates 2, 8, 12, 16) — **closed as blockers (now PARTIAL/evidence-complete)**: OCR-pipeline test sweep PASS (2); the deferred Lows reconfirmed all Safe=YES / 0 open Medium+ (8); audit-chain tamper-evidence drill PASS (12); brief-READY + reconciliation-log clean (16). These are evidence-advanced PARTIAL; a `PARTIAL → CLEARED` move for the v1-day-one surface awaits only the user-owned gates.
5. **User STOP-AND-ASK decisions** (gates 4, 11, 21 — and the user-decision-gated 7, 17) — **still open, user-owned (no agent lane clears them)**: distribution + code-signing/notarization (gate 4); 律师法 confidentiality/compliance sign-off (gate 11); the final go-live sign-off (gate 21); plus gate 7 (D-G7-1/D-G7-2 robustness risk-acceptance) + gate 17 (license/legal/business). These are the true release gates and are the user's to decide.

**Reconciliation summary:** every prior agent-remit blocker (gates 2/3-R3/6/8/12/13/14/15/16/18/19/20) is now closed-as-blocker (CLEARED or PARTIAL/evidence-complete). What remains M0-blocking is **entirely user-owned**: gates 4, 7 (D-G7-1/D-G7-2), 11, 17, 21. No agent lane clears these; **NO final GO/NO-GO is asserted here.**

## 4. STOP-AND-ASK decisions (user-owned; NOT resolved here)
1. **Framework confirm + public distribution + code-sign/notarization** (gate 4) — Apple Developer ID, signing identity, notarization profile, distribution channel. Dev-mode signing is acceptable-deferred; public release cannot clear without this.
2. **律师法 confidentiality / legal-compliance sign-off** (gate 11).
3. **Final go-live sign-off** (gate 21) — engineering + legal.

## 5. Non-blocking deferred findings
`dev-memo/deferred-audit-findings.md`: **37 open, ALL Low, ALL `Safe=YES`, 0 Medium+**. Categories: shell/git-hook guard refinements, case-box barrel/enum convention drift, SQLite-schema LOC hygiene, one FileVault smoke-test gap. **None are M0-product blockers.**

## 6. Post-v1 exclusions (not blocking M0)
Sync bridge + WeChat mini-program; remote+local LLM extraction; document text-extraction engine; auto-update; multi-user/tenant; encryption beyond FileVault; backup/export tooling (signed bundle, PDF chronology, privilege log, **proof matrix = T4**); redaction; lawyer-letter/contract-review lifecycle state machines. (brief §5/§8/§12/§14/§20 + reconciliation WIs.)

## 7. Forms track release status
- **T3 (证据目录及说明)**: **COMPLETE (M0 feature)** — S0 contract fields → S1 logical model → S2 preview → S3 DOCX export, all on `main`.
- **T4 (举证质证表)**: **DECIDED post-v1**, issue-centric proof model (`docs/adr/ADR-forms-t4-proof-model-scope.md`); data foundation not built. Not M0.
- **T5 (质证意见/质证记录)**: **design-gated** (narrative-vs-structured fork); no governed WI. Not M0.

## 8. Recommended next steps (after this refresh)
**The agent-remit gate sweep is complete.** Every non-user-owned M0 gate is now CLEARED or PARTIAL/evidence-complete (where "PARTIAL/evidence-complete" means the gate's v1-day-one evidence is in place and it carries **no further agent-remit M0 blocker** — its `PARTIAL → CLEARED` move awaits only the user-owned gates). Gate 6 (the final full-project audit) CLEARED is the last autonomous closure. There is **no remaining agent-remit gate WI** to author.

What remains M0-blocking is **entirely user-owned** (no agent lane clears these):
- **Gate 4** — distribution + code-signing/notarization/public-download framework (an Apple Developer ID + signing identity + notarization + distribution-channel choice). This also owns the gate-3 R1 residual + the gate-19 R-G19-1 follow-up posture (the Electron runtime advisory is remediated; signing the packaged runtime is gate 4).
- **Gate 7** — the D-G7-1 (accept v1 without mutation testing?) + D-G7-2 (require the crash-recovery drill? — a PASS satisfies it if you answer YES) robustness risk-acceptance decisions.
- **Gate 11** — 律师法 confidentiality/compliance sign-off.
- **Gate 17** — license / copyright / privacy-notice legal/business decision.
- **Gate 21** — the final go-live sign-off (this doc stays INTERIM / BLOCKED until you fill it).

Optional post-v1 (non-blocking): R3-FUP-1 (transport-error `catch` on 3 read/list loads) + R3-FUP-2 (error/empty copy + i18n consistency); the electron-builder dev-toolchain advisory monitoring; an SBOM. These are documented non-M0.

Once you resolve the user-owned gates above, the final go-live readiness report (gate 21) can be filled — that remains your decision.

## Audience
Engineering Lead + On-Call + Legal (per the blueprint's gate-21 audience). This interim snapshot is decision-support, NOT a release authorization.
