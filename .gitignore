# Build output and dependencies
node_modules/
dist/
coverage/
*.tsbuildinfo
.DS_Store

# Secrets / env
.env
.env.*
!.env.example

# AI assistant local state (settings and per-machine overrides are never shared)
.claude/settings.json
.claude/settings.local.json
.claude/CLAUDE.local.md
.claude/*.local.*
CLAUDE.local.md
.claude/nlpm-history.json
.cc-suite/
.claude-english-buddy.json

# Superseded historical drafts — preserved on disk, never tracked. Kept because
# docs/product/product-definition.md Part I cites case-box-plan.md.
dev-memo/superseded/

# docs-guardian generated artifacts
.claude/docs-guardian/audit-report.md

# tdd-guardian generated artifacts
.claude/tdd-guardian/state.json

# Codex CLI run log. Dropped in the repo root by the cc-suite runner and NOT
# generated by this project. It records a ChatGPT account_id, so it must never
# reach git history — this repo has had an identifier-in-history incident before.
error.log

# vmark editor working directory (lock files, not project content).
.vmark/
