node_modules/
dist/
release/
*.tsbuildinfo

# Auto-generated wrapper test fixtures (regenerated each run by tests/wrapper.test.mjs; not source).
tests/_wrapper-noop.test.mjs
tests/_wrapper-seeder*.test.mjs
tests/_wrapper-suicide.test.mjs

# Internal-dependency tarballs are now TRACKED (committed), per
# docs/adr/ADR-evidence-desktop-internal-deps-packaging.md (DESKTOP-DEPS-00, Option 1):
# the committed `package-lock.json` pins their bytes so `npm ci` is reproducible and
# clean/packaged builds bundle the current contract/persistence payloads. A semantic
# drift guard (scripts/workflow/check-internal-tarballs.mjs) keeps them in sync with source.
# (Previously gitignored per the tarball-PoC plan §5.2; superseded by DESKTOP-DEPS-00.)
