<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!--
    Hardened-runtime entitlements for Developer-ID signing + notarization
    (WI-DESKTOP-MACOS-SIGNING-NOTARIZATION-07). Required because Electron's V8
    JITs, and the app loads a native module (better-sqlite3, asarUnpacked).
    This is NOT App Sandbox (the app is intentionally unsandboxed, local-first
    file access) and adds NO network entitlement — notarization needs hardened
    runtime, not the sandbox. No secrets.
  -->
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>
</dict>
</plist>
